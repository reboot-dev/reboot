# Singleton state-id for the application's `CouponBook`. The
# app stores all coupon codes in a single instance addressed by
# this id, created once from `main.initialize` and referenced
# during checkout.
COUPON_BOOK_ID = "coupon-book"
