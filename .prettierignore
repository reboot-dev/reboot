# Don't check the submodules; those are the responsiblity of their own repos.
/examples
/reboot/server/sha1
/public/submodules
/public/reboot/server/sha1

# Don't check Bazel's generated output.
/bazel-*

# Don't check VSCode's settings. They're generated by VSCode.
/.vscode

# Don't check generated output.
/cloud/web/src/gen

# Don't check Helm chart templates, since they claim to be `yaml`
# (https://helm.sh/docs/chart_best_practices/templates/#structure-of-templates)
# but aren't actually.
/public/charts/**/templates/

# Don't check rbt documentation with generated code snippets -
# markdown-autodocs and prettier disagree slightly about formatting.
/public/documentation/**/*.md
/public/documentation/**/*.mdx
/reboot/examples/**/*.md
/public/reboot/examples/**/*.md

# Don't check golden files, since we check the exact match to the
# actual generated files, which are not formatted the same.
public/tests/reboot/greeter_rbt_react.golden.js
public/tests/reboot/greeter_rbt.golden.js
public/tests/reboot/greeter_rbt_web.golden.js
public/tests/reboot/general_servicer.golden.ts

# Don't check our generated `pnpm-lock.yaml` files.
pnpm-lock.yaml
