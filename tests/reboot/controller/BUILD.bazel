load("@rbt_pypi//:requirements.bzl", "requirement")
load("@rules_python//python:defs.bzl", "py_test")

py_test(
    name = "placement_planner_test_py",
    size = "small",
    srcs = ["placement_planner_test.py"],
    main = "placement_planner_test.py",
    deps = [
        requirement("grpcio"),
        requirement("kubernetes_asyncio"),
        requirement("protobuf"),
        "//tests/reboot:settings_py",
        "//tests/reboot:test_helpers_py",
        "@com_github_reboot_dev_reboot//rbt/v1alpha1:application_config_py_proto",
        "@com_github_reboot_dev_reboot//rbt/v1alpha1:placement_planner_py_grpc",
        "@com_github_reboot_dev_reboot//rbt/v1alpha1:placement_planner_py_proto",
        "@com_github_reboot_dev_reboot//reboot:helpers_py",
        "@com_github_reboot_dev_reboot//reboot/aio:exceptions_py",
        "@com_github_reboot_dev_reboot//reboot/aio:resolvers_py",
        "@com_github_reboot_dev_reboot//reboot/controller:application_config_py",
        "@com_github_reboot_dev_reboot//reboot/controller:application_config_trackers_py",
        "@com_github_reboot_dev_reboot//reboot/controller:placement_planner_py",
        "@com_github_reboot_dev_reboot//reboot/controller:server_managers_py",
    ],
)

py_test(
    name = "application_config_trackers_test_py",
    srcs = ["application_config_trackers_test.py"],
    main = "application_config_trackers_test.py",
    deps = [
        "@com_github_reboot_dev_reboot//reboot/controller:application_config_py",
        "@com_github_reboot_dev_reboot//reboot/controller:application_config_trackers_py",
    ],
)

py_test(
    name = "server_managers_test_py",
    srcs = ["server_managers_test.py"],
    main = "server_managers_test.py",
    deps = [
        requirement("protobuf"),
        "//tests/reboot:settings_py",
        "@com_github_reboot_dev_reboot//rbt/v1alpha1:placement_planner_py_proto",
        "@com_github_reboot_dev_reboot//reboot/controller:server_managers_py",
    ],
)

py_test(
    name = "config_server_test_py",
    srcs = ["config_server_test.py"],
    main = "config_server_test.py",
    deps = [
        # We should add all deps after the 'grpcio' requirement, to
        # make sure 'grpcio' path will be before any other dependency on a
        # PYTHONPATH, while generating a bootstrap python for the 'py_binary'.
        # We had an issue about using two versions of the 'cygrpc.so' which
        # are coming from 'grpcio' and the 'com_github_grpc_grpc' repo.
        # See https://github.com/reboot-dev/mono/issues/2347
        requirement("grpcio"),
        requirement("protobuf"),
        "@com_github_reboot_dev_reboot//reboot/aio:servers_py",
        "//tests/reboot:echo_servicers_py",
        "//tests/reboot:greeter_servicers_py",
        "@com_github_reboot_dev_reboot//reboot:helpers_py",
        "@com_github_reboot_dev_reboot//reboot:version_py",
        "@com_github_reboot_dev_reboot//reboot/grpc:options_py",
    ],
)

py_test(
    name = "config_extractor_test_py",
    srcs = ["config_extractor_test.py"],
    main = "config_extractor_test.py",
    deps = [
        # We should add all deps after the 'grpcio' requirement, to
        # make sure 'grpcio' path will be before any other dependency on a
        # PYTHONPATH, while generating a bootstrap python for the 'py_binary'.
        # We had an issue about using two versions of the 'cygrpc.so' which
        # are coming from 'grpcio' and the 'com_github_grpc_grpc' repo.
        # See https://github.com/reboot-dev/mono/issues/2347
        requirement("grpcio"),
        requirement("protobuf"),
        "@com_github_reboot_dev_reboot//reboot:naming_py",
        "@com_github_reboot_dev_reboot//reboot/controller:config_extractor_py",
        "//tests/reboot:greeter_servicers_py",
        "@com_github_reboot_dev_reboot//reboot/aio:servicers_py",
    ],
)

py_test(
    name = "shards_test_py",
    srcs = ["shards_test.py"],
    main = "shards_test.py",
    deps = [
        "@com_github_reboot_dev_reboot//reboot/controller:shards_py",
    ],
)
