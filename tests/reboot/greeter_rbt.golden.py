# yapf: disable
# isort: skip_file
# ruff: noqa
# mypy: disable-error-code="func-returns-value"



# To not generate code where imported names might get shadowed when a user
# specifies some name in their proto file to be the same as one of our imported
# names, (for example: a request field named `uuid`) we bind all imports to
# names that are forbidden in 'proto' and therefore can never collide.

# Standard imports.
from __future__ import annotations as IMPORT_future_annotations

# The following MUST appear before the rest of the imports, since those imports
# may be invalid (broken) if the generated code is mismatched with the installed
# libraries.
import reboot.versioning as IMPORT_reboot_versioning
IMPORT_reboot_versioning.check_generated_code_compatible("1.0.3")

# ATTENTION: no types in this file should be imported with their unqualified
#            name (e.g. `from typing import Any`). That would cause clashes
#            with user-defined methods that have the same name. Use
#            fully-qualified names (e.g. `IMPORT_typing.Any`) instead.
import asyncio as IMPORT_asyncio
import builtins as IMPORT_builtins
import contextvars as IMPORT_contextvars
import dataclasses as IMPORT_dataclasses
import google.protobuf.descriptor as IMPORT_google_protobuf_descriptor
import google.protobuf.json_format as IMPORT_google_protobuf_json_format
import google.protobuf.message as IMPORT_google_protobuf_message
import grpc as IMPORT_grpc
import grpc_status._async as IMPORT_rpc_status_async
from grpc_status import rpc_status as IMPORT_rpc_status_sync
import json as IMPORT_json
import os as IMPORT_os
import traceback as IMPORT_traceback
import uuid as IMPORT_uuid
import pickle as IMPORT_pickle
import reboot as IMPORT_reboot
import log.log as IMPORT_log_log   # type: ignore[import]
import typing as IMPORT_typing
import reboot.aio.backoff as IMPORT_reboot_aio_backoff
import functools as IMPORT_functools
from abc import abstractmethod as IMPORT_abc_abstractmethod
from datetime import datetime as IMPORT_datetime_datetime
from datetime import timedelta as IMPORT_datetime_timedelta
from datetime import timezone as IMPORT_datetime_timezone
from google.protobuf import timestamp_pb2 as IMPORT_google_protobuf_timestamp_pb2
from google.protobuf import wrappers_pb2 as IMPORT_google_protobuf_wrappers_pb2
from google.protobuf.empty_pb2 import Empty as IMPORT_google_protobuf_empty_pb2_Empty
import reboot.aio.tracing as IMPORT_reboot_aio_tracing
from google.rpc import status_pb2 as IMPORT_google_rpc_status_pb2
from tzlocal import get_localzone as IMPORT_tzlocal_get_localzone
import reboot.aio.call as IMPORT_reboot_aio_call
import reboot.aio.caller_id as IMPORT_reboot_aio_caller_id
import reboot.aio.contexts as IMPORT_reboot_aio_contexts
import reboot.aio.headers as IMPORT_reboot_aio_headers
import reboot.aio.idempotency as IMPORT_reboot_aio_idempotency
import reboot.aio.internals.channel_manager as IMPORT_reboot_aio_internals_channel_manager
import reboot.aio.internals.middleware as IMPORT_reboot_aio_internals_middleware
import reboot.aio.internals.tasks_cache as IMPORT_reboot_aio_internals_tasks_cache
import reboot.aio.internals.tasks_dispatcher as IMPORT_reboot_aio_internals_tasks_dispatcher
import reboot.aio.placement as IMPORT_reboot_aio_placement
import reboot.aio.servicers as IMPORT_reboot_aio_servicers
import reboot.aio.state_managers as IMPORT_reboot_aio_state_managers
import reboot.aio.stubs as IMPORT_reboot_aio_stubs
import reboot.aio.tasks as IMPORT_reboot_aio_tasks
import reboot.aio.types as IMPORT_reboot_aio_types
import reboot.aio.external as IMPORT_reboot_aio_external
import reboot.aio.workflows as IMPORT_reboot_aio_workflows
import reboot.aio.auth as IMPORT_reboot_aio_auth
import reboot.aio.auth.authorizers as IMPORT_reboot_aio_auth_authorizers
import reboot.aio.auth.token_verifiers as IMPORT_reboot_aio_auth_token_verifiers
import reboot.aio.aborted as IMPORT_reboot_aio_aborted
import reboot.settings as IMPORT_reboot_settings
import reboot.nodejs.python as IMPORT_reboot_nodejs_python
from reboot.time import DateTimeWithTimeZone as IMPORT_reboot_time_DateTimeWithTimeZone
import rbt.v1alpha1 as IMPORT_rbt_v1alpha1
import rbt.v1alpha1.nodejs_pb2 as IMPORT_rbt_v1alpha1_nodejs_pb2
import google.protobuf.any_pb2 as IMPORT_google_protobuf_any_pb2
import sys as IMPORT_sys
import mcp.server.fastmcp as IMPORT_mcp_server_fastmcp
import reboot.mcp.context as IMPORT_reboot_mcp_context
import reboot.mcp.ui as IMPORT_reboot_mcp_ui

# Additionally re-export all messages and enums from the pb2 module.
from tests.reboot.greeter_pb2 import (
    Color,
    ConstructAndStoreRecursiveMessageRequest,
    ConstructAndStoreRecursiveMessageResponse,
    CreateRequest,
    CreateResponse,
    DangerousFieldsRequest,
    ErrorWithValue,
    GetWholeStateRequest,
    GreetRequest,
    GreetResponse,
    MatchColorRequest,
    MatchColorResponse,
    ReadRecursiveMessageRequest,
    ReadRecursiveMessageResponse,
    RecursiveMessage,
    SetAdjectiveRequest,
    SetAdjectiveResponse,
    StopwatchRequest,
    StopwatchResponse,
    StoreRecursiveMessageRequest,
    StoreRecursiveMessageResponse,
    TestLongRunningFetchRequest,
    Time,
    WorkflowResponse,
)

# User defined or referenced imports.
import google.api.annotations_pb2
import google.api.http_pb2
import google.protobuf.any_pb2
import google.protobuf.descriptor_pb2
import google.protobuf.empty_pb2
import google.protobuf.timestamp_pb2
import rbt.v1alpha1.options_pb2
import rbt.v1alpha1.tasks_pb2
import rbt.v1alpha1.tasks_pb2_grpc
import tests.reboot.greeter_pb2
import tests.reboot.greeter_pb2_grpc

logger = IMPORT_log_log.get_logger(__name__)

# We won't validate Pydantic state models while they are under construction.
states_being_constructed: set[str] = set()

class Unset:
    pass

UNSET = Unset()



def GreeterToProto(state: Greeter.State, protobuf_state: tests.reboot.greeter_pb2.Greeter):
    pass

def GreeterFromProto(
    state: tests.reboot.greeter_pb2.Greeter,
    is_initial_state: bool = False,
) -> Greeter.State:
    return state
def GreeterCreateResponseToProto(
    response: Greeter.CreateResponse
) -> tests.reboot.greeter_pb2.CreateResponse:
    return response

def GreeterCreateResponseFromProto(
    response: tests.reboot.greeter_pb2.CreateResponse
) -> Greeter.CreateResponse:
    return response

def GreeterCreateRequestToProto(
    request: Greeter.CreateRequest
) -> tests.reboot.greeter_pb2.CreateRequest:
    return request

def GreeterCreateRequestFromProto(
    request: tests.reboot.greeter_pb2.CreateRequest
) -> Greeter.CreateRequest:
    return request

def GreeterCreateRequestFromInputFields(
    title: IMPORT_typing.Optional[str] | Unset,
    name: IMPORT_typing.Optional[str] | Unset,
    adjective: IMPORT_typing.Optional[str] | Unset,
):
    assert Greeter.CreateRequest is not None

    if not isinstance(title, Unset) and title is not None and not isinstance(
        title,
        str,
    ):
        raise TypeError(
            f"Can not construct protobuf message of type "
            f"'tests.reboot.greeter_pb2.CreateRequest': field 'title' is not "
            f"of required type 'str'"
        )
    if not isinstance(name, Unset) and name is not None and not isinstance(
        name,
        str,
    ):
        raise TypeError(
            f"Can not construct protobuf message of type "
            f"'tests.reboot.greeter_pb2.CreateRequest': field 'name' is not "
            f"of required type 'str'"
        )
    if not isinstance(adjective, Unset) and adjective is not None and not isinstance(
        adjective,
        str,
    ):
        raise TypeError(
            f"Can not construct protobuf message of type "
            f"'tests.reboot.greeter_pb2.CreateRequest': field 'adjective' is not "
            f"of required type 'str'"
        )


    __args__: dict[str, IMPORT_typing.Any] = {}

    if not isinstance(title, Unset):
        __args__['title'] = title
    if not isinstance(name, Unset):
        __args__['name'] = name
    if not isinstance(adjective, Unset):
        __args__['adjective'] = adjective

    return Greeter.CreateRequest(
        **__args__,
    )

def GreeterGreetResponseToProto(
    response: Greeter.GreetResponse
) -> tests.reboot.greeter_pb2.GreetResponse:
    return response

def GreeterGreetResponseFromProto(
    response: tests.reboot.greeter_pb2.GreetResponse
) -> Greeter.GreetResponse:
    return response

def GreeterGreetRequestToProto(
    request: Greeter.GreetRequest
) -> tests.reboot.greeter_pb2.GreetRequest:
    return request

def GreeterGreetRequestFromProto(
    request: tests.reboot.greeter_pb2.GreetRequest
) -> Greeter.GreetRequest:
    return request

def GreeterGreetRequestFromInputFields(
    name: IMPORT_typing.Optional[str] | Unset,
    metadata: IMPORT_typing.Optional[dict[str, str]] | Unset,
):
    assert Greeter.GreetRequest is not None

    if not isinstance(name, Unset) and name is not None and not isinstance(
        name,
        str,
    ):
        raise TypeError(
            f"Can not construct protobuf message of type "
            f"'tests.reboot.greeter_pb2.GreetRequest': field 'name' is not "
            f"of required type 'str'"
        )
    if not isinstance(metadata, Unset) and metadata is not None and not isinstance(
        metadata,
        dict,
    ):
        raise TypeError(
            f"Can not construct protobuf message of type "
            f"'tests.reboot.greeter_pb2.GreetRequest': field 'metadata' is not "
            f"of required type 'dict[str, str]'"
        )


    __args__: dict[str, IMPORT_typing.Any] = {}

    if not isinstance(name, Unset):
        __args__['name'] = name
    if not isinstance(metadata, Unset):
        __args__['metadata'] = metadata

    return Greeter.GreetRequest(
        **__args__,
    )

def GreeterSetAdjectiveResponseToProto(
    response: Greeter.SetAdjectiveResponse
) -> tests.reboot.greeter_pb2.SetAdjectiveResponse:
    return response

def GreeterSetAdjectiveResponseFromProto(
    response: tests.reboot.greeter_pb2.SetAdjectiveResponse
) -> Greeter.SetAdjectiveResponse:
    return response

def GreeterSetAdjectiveRequestToProto(
    request: Greeter.SetAdjectiveRequest
) -> tests.reboot.greeter_pb2.SetAdjectiveRequest:
    return request

def GreeterSetAdjectiveRequestFromProto(
    request: tests.reboot.greeter_pb2.SetAdjectiveRequest
) -> Greeter.SetAdjectiveRequest:
    return request

def GreeterSetAdjectiveRequestFromInputFields(
    adjective: IMPORT_typing.Optional[str] | Unset,
    arbitrary_message: IMPORT_typing.Optional[tests.reboot.greeter_pb2.SetAdjectiveRequest.NestedMessage] | Unset,
    revert_after_1_second: IMPORT_typing.Optional[bool] | Unset,
):
    assert Greeter.SetAdjectiveRequest is not None

    if not isinstance(adjective, Unset) and adjective is not None and not isinstance(
        adjective,
        str,
    ):
        raise TypeError(
            f"Can not construct protobuf message of type "
            f"'tests.reboot.greeter_pb2.SetAdjectiveRequest': field 'adjective' is not "
            f"of required type 'str'"
        )
    if not isinstance(arbitrary_message, Unset) and arbitrary_message is not None and not isinstance(
        arbitrary_message,
        tests.reboot.greeter_pb2.SetAdjectiveRequest.NestedMessage,
    ):
        raise TypeError(
            f"Can not construct protobuf message of type "
            f"'tests.reboot.greeter_pb2.SetAdjectiveRequest': field 'arbitrary_message' is not "
            f"of required type 'tests.reboot.greeter_pb2.SetAdjectiveRequest.NestedMessage'"
        )
    if not isinstance(revert_after_1_second, Unset) and revert_after_1_second is not None and not isinstance(
        revert_after_1_second,
        bool,
    ):
        raise TypeError(
            f"Can not construct protobuf message of type "
            f"'tests.reboot.greeter_pb2.SetAdjectiveRequest': field 'revert_after_1_second' is not "
            f"of required type 'bool'"
        )


    __args__: dict[str, IMPORT_typing.Any] = {}

    if not isinstance(adjective, Unset):
        __args__['adjective'] = adjective
    if not isinstance(arbitrary_message, Unset):
        __args__['arbitrary_message'] = arbitrary_message
    if not isinstance(revert_after_1_second, Unset):
        __args__['revert_after_1_second'] = revert_after_1_second

    return Greeter.SetAdjectiveRequest(
        **__args__,
    )

def GreeterTransactionSetAdjectiveResponseToProto(
    response: Greeter.TransactionSetAdjectiveResponse
) -> tests.reboot.greeter_pb2.SetAdjectiveResponse:
    return response

def GreeterTransactionSetAdjectiveResponseFromProto(
    response: tests.reboot.greeter_pb2.SetAdjectiveResponse
) -> Greeter.TransactionSetAdjectiveResponse:
    return response

def GreeterTransactionSetAdjectiveRequestToProto(
    request: Greeter.TransactionSetAdjectiveRequest
) -> tests.reboot.greeter_pb2.SetAdjectiveRequest:
    return request

def GreeterTransactionSetAdjectiveRequestFromProto(
    request: tests.reboot.greeter_pb2.SetAdjectiveRequest
) -> Greeter.TransactionSetAdjectiveRequest:
    return request

def GreeterTransactionSetAdjectiveRequestFromInputFields(
    adjective: IMPORT_typing.Optional[str] | Unset,
    arbitrary_message: IMPORT_typing.Optional[tests.reboot.greeter_pb2.SetAdjectiveRequest.NestedMessage] | Unset,
    revert_after_1_second: IMPORT_typing.Optional[bool] | Unset,
):
    assert Greeter.TransactionSetAdjectiveRequest is not None

    if not isinstance(adjective, Unset) and adjective is not None and not isinstance(
        adjective,
        str,
    ):
        raise TypeError(
            f"Can not construct protobuf message of type "
            f"'tests.reboot.greeter_pb2.SetAdjectiveRequest': field 'adjective' is not "
            f"of required type 'str'"
        )
    if not isinstance(arbitrary_message, Unset) and arbitrary_message is not None and not isinstance(
        arbitrary_message,
        tests.reboot.greeter_pb2.SetAdjectiveRequest.NestedMessage,
    ):
        raise TypeError(
            f"Can not construct protobuf message of type "
            f"'tests.reboot.greeter_pb2.SetAdjectiveRequest': field 'arbitrary_message' is not "
            f"of required type 'tests.reboot.greeter_pb2.SetAdjectiveRequest.NestedMessage'"
        )
    if not isinstance(revert_after_1_second, Unset) and revert_after_1_second is not None and not isinstance(
        revert_after_1_second,
        bool,
    ):
        raise TypeError(
            f"Can not construct protobuf message of type "
            f"'tests.reboot.greeter_pb2.SetAdjectiveRequest': field 'revert_after_1_second' is not "
            f"of required type 'bool'"
        )


    __args__: dict[str, IMPORT_typing.Any] = {}

    if not isinstance(adjective, Unset):
        __args__['adjective'] = adjective
    if not isinstance(arbitrary_message, Unset):
        __args__['arbitrary_message'] = arbitrary_message
    if not isinstance(revert_after_1_second, Unset):
        __args__['revert_after_1_second'] = revert_after_1_second

    return Greeter.TransactionSetAdjectiveRequest(
        **__args__,
    )

def GreeterTryToConstructContextResponseToProto(
    response: Greeter.TryToConstructContextResponse
) -> google.protobuf.empty_pb2.Empty:
    return response

def GreeterTryToConstructContextResponseFromProto(
    response: google.protobuf.empty_pb2.Empty
) -> Greeter.TryToConstructContextResponse:
    return response

def GreeterTryToConstructContextRequestToProto(
    request: Greeter.TryToConstructContextRequest
) -> google.protobuf.empty_pb2.Empty:
    return request

def GreeterTryToConstructContextRequestFromProto(
    request: google.protobuf.empty_pb2.Empty
) -> Greeter.TryToConstructContextRequest:
    return request

def GreeterTryToConstructContextRequestFromInputFields(
):
    assert Greeter.TryToConstructContextRequest is not None



    __args__: dict[str, IMPORT_typing.Any] = {}


    return Greeter.TryToConstructContextRequest(
        **__args__,
    )

def GreeterTryToConstructExternalContextResponseToProto(
    response: Greeter.TryToConstructExternalContextResponse
) -> google.protobuf.empty_pb2.Empty:
    return response

def GreeterTryToConstructExternalContextResponseFromProto(
    response: google.protobuf.empty_pb2.Empty
) -> Greeter.TryToConstructExternalContextResponse:
    return response

def GreeterTryToConstructExternalContextRequestToProto(
    request: Greeter.TryToConstructExternalContextRequest
) -> google.protobuf.empty_pb2.Empty:
    return request

def GreeterTryToConstructExternalContextRequestFromProto(
    request: google.protobuf.empty_pb2.Empty
) -> Greeter.TryToConstructExternalContextRequest:
    return request

def GreeterTryToConstructExternalContextRequestFromInputFields(
):
    assert Greeter.TryToConstructExternalContextRequest is not None



    __args__: dict[str, IMPORT_typing.Any] = {}


    return Greeter.TryToConstructExternalContextRequest(
        **__args__,
    )

def GreeterTestLongRunningFetchResponseToProto(
    response: Greeter.TestLongRunningFetchResponse
) -> google.protobuf.empty_pb2.Empty:
    return response

def GreeterTestLongRunningFetchResponseFromProto(
    response: google.protobuf.empty_pb2.Empty
) -> Greeter.TestLongRunningFetchResponse:
    return response

def GreeterTestLongRunningFetchRequestToProto(
    request: Greeter.TestLongRunningFetchRequest
) -> tests.reboot.greeter_pb2.TestLongRunningFetchRequest:
    return request

def GreeterTestLongRunningFetchRequestFromProto(
    request: tests.reboot.greeter_pb2.TestLongRunningFetchRequest
) -> Greeter.TestLongRunningFetchRequest:
    return request

def GreeterTestLongRunningFetchRequestFromInputFields(
    sleep_time_seconds: IMPORT_typing.Optional[int] | Unset,
):
    assert Greeter.TestLongRunningFetchRequest is not None

    if not isinstance(sleep_time_seconds, Unset) and sleep_time_seconds is not None and not isinstance(
        sleep_time_seconds,
        int,
    ):
        raise TypeError(
            f"Can not construct protobuf message of type "
            f"'tests.reboot.greeter_pb2.TestLongRunningFetchRequest': field 'sleep_time_seconds' is not "
            f"of required type 'int'"
        )


    __args__: dict[str, IMPORT_typing.Any] = {}

    if not isinstance(sleep_time_seconds, Unset):
        __args__['sleep_time_seconds'] = sleep_time_seconds

    return Greeter.TestLongRunningFetchRequest(
        **__args__,
    )

def GreeterTestLongRunningWriterResponseToProto(
    response: Greeter.TestLongRunningWriterResponse
) -> google.protobuf.empty_pb2.Empty:
    return response

def GreeterTestLongRunningWriterResponseFromProto(
    response: google.protobuf.empty_pb2.Empty
) -> Greeter.TestLongRunningWriterResponse:
    return response

def GreeterTestLongRunningWriterRequestToProto(
    request: Greeter.TestLongRunningWriterRequest
) -> google.protobuf.empty_pb2.Empty:
    return request

def GreeterTestLongRunningWriterRequestFromProto(
    request: google.protobuf.empty_pb2.Empty
) -> Greeter.TestLongRunningWriterRequest:
    return request

def GreeterTestLongRunningWriterRequestFromInputFields(
):
    assert Greeter.TestLongRunningWriterRequest is not None



    __args__: dict[str, IMPORT_typing.Any] = {}


    return Greeter.TestLongRunningWriterRequest(
        **__args__,
    )

def GreeterGetWholeStateResponseToProto(
    response: Greeter.GetWholeStateResponse
) -> tests.reboot.greeter_pb2.Greeter:
    return response

def GreeterGetWholeStateResponseFromProto(
    response: tests.reboot.greeter_pb2.Greeter
) -> Greeter.GetWholeStateResponse:
    return response

def GreeterGetWholeStateRequestToProto(
    request: Greeter.GetWholeStateRequest
) -> tests.reboot.greeter_pb2.GetWholeStateRequest:
    return request

def GreeterGetWholeStateRequestFromProto(
    request: tests.reboot.greeter_pb2.GetWholeStateRequest
) -> Greeter.GetWholeStateRequest:
    return request

def GreeterGetWholeStateRequestFromInputFields(
):
    assert Greeter.GetWholeStateRequest is not None



    __args__: dict[str, IMPORT_typing.Any] = {}


    return Greeter.GetWholeStateRequest(
        **__args__,
    )

def GreeterFailWithExceptionResponseToProto(
    response: Greeter.FailWithExceptionResponse
) -> google.protobuf.empty_pb2.Empty:
    return response

def GreeterFailWithExceptionResponseFromProto(
    response: google.protobuf.empty_pb2.Empty
) -> Greeter.FailWithExceptionResponse:
    return response

def GreeterFailWithExceptionRequestToProto(
    request: Greeter.FailWithExceptionRequest
) -> google.protobuf.empty_pb2.Empty:
    return request

def GreeterFailWithExceptionRequestFromProto(
    request: google.protobuf.empty_pb2.Empty
) -> Greeter.FailWithExceptionRequest:
    return request

def GreeterFailWithExceptionRequestFromInputFields(
):
    assert Greeter.FailWithExceptionRequest is not None



    __args__: dict[str, IMPORT_typing.Any] = {}


    return Greeter.FailWithExceptionRequest(
        **__args__,
    )

def GreeterFailWithAbortedResponseToProto(
    response: Greeter.FailWithAbortedResponse
) -> google.protobuf.empty_pb2.Empty:
    return response

def GreeterFailWithAbortedResponseFromProto(
    response: google.protobuf.empty_pb2.Empty
) -> Greeter.FailWithAbortedResponse:
    return response

def GreeterFailWithAbortedRequestToProto(
    request: Greeter.FailWithAbortedRequest
) -> google.protobuf.empty_pb2.Empty:
    return request

def GreeterFailWithAbortedRequestFromProto(
    request: google.protobuf.empty_pb2.Empty
) -> Greeter.FailWithAbortedRequest:
    return request

def GreeterFailWithAbortedRequestFromInputFields(
):
    assert Greeter.FailWithAbortedRequest is not None



    __args__: dict[str, IMPORT_typing.Any] = {}


    return Greeter.FailWithAbortedRequest(
        **__args__,
    )

def GreeterWorkflowResponseToProto(
    response: Greeter.WorkflowResponse
) -> tests.reboot.greeter_pb2.WorkflowResponse:
    return response

def GreeterWorkflowResponseFromProto(
    response: tests.reboot.greeter_pb2.WorkflowResponse
) -> Greeter.WorkflowResponse:
    return response

def GreeterWorkflowRequestToProto(
    request: Greeter.WorkflowRequest
) -> google.protobuf.empty_pb2.Empty:
    return request

def GreeterWorkflowRequestFromProto(
    request: google.protobuf.empty_pb2.Empty
) -> Greeter.WorkflowRequest:
    return request

def GreeterWorkflowRequestFromInputFields(
):
    assert Greeter.WorkflowRequest is not None



    __args__: dict[str, IMPORT_typing.Any] = {}


    return Greeter.WorkflowRequest(
        **__args__,
    )

def GreeterDangerousFieldsResponseToProto(
    response: Greeter.DangerousFieldsResponse
) -> google.protobuf.empty_pb2.Empty:
    return response

def GreeterDangerousFieldsResponseFromProto(
    response: google.protobuf.empty_pb2.Empty
) -> Greeter.DangerousFieldsResponse:
    return response

def GreeterDangerousFieldsRequestToProto(
    request: Greeter.DangerousFieldsRequest
) -> tests.reboot.greeter_pb2.DangerousFieldsRequest:
    return request

def GreeterDangerousFieldsRequestFromProto(
    request: tests.reboot.greeter_pb2.DangerousFieldsRequest
) -> Greeter.DangerousFieldsRequest:
    return request

def GreeterDangerousFieldsRequestFromInputFields(
    metadata: IMPORT_typing.Optional[int] | Unset,
    request: IMPORT_typing.Optional[int] | Unset,
    response: IMPORT_typing.Optional[int] | Unset,
    self: IMPORT_typing.Optional[int] | Unset,
    context: IMPORT_typing.Optional[int] | Unset,
    bearer_token: IMPORT_typing.Optional[int] | Unset,
    reboot: IMPORT_typing.Optional[int] | Unset,
    uuid: IMPORT_typing.Optional[int] | Unset,
    stub: IMPORT_typing.Optional[int] | Unset,
    idempotency: IMPORT_typing.Optional[str] | Unset,
    state_id: IMPORT_typing.Optional[str] | Unset,
):
    assert Greeter.DangerousFieldsRequest is not None

    if not isinstance(metadata, Unset) and metadata is not None and not isinstance(
        metadata,
        int,
    ):
        raise TypeError(
            f"Can not construct protobuf message of type "
            f"'tests.reboot.greeter_pb2.DangerousFieldsRequest': field 'metadata' is not "
            f"of required type 'int'"
        )
    if not isinstance(request, Unset) and request is not None and not isinstance(
        request,
        int,
    ):
        raise TypeError(
            f"Can not construct protobuf message of type "
            f"'tests.reboot.greeter_pb2.DangerousFieldsRequest': field 'request' is not "
            f"of required type 'int'"
        )
    if not isinstance(response, Unset) and response is not None and not isinstance(
        response,
        int,
    ):
        raise TypeError(
            f"Can not construct protobuf message of type "
            f"'tests.reboot.greeter_pb2.DangerousFieldsRequest': field 'response' is not "
            f"of required type 'int'"
        )
    if not isinstance(self, Unset) and self is not None and not isinstance(
        self,
        int,
    ):
        raise TypeError(
            f"Can not construct protobuf message of type "
            f"'tests.reboot.greeter_pb2.DangerousFieldsRequest': field 'self' is not "
            f"of required type 'int'"
        )
    if not isinstance(context, Unset) and context is not None and not isinstance(
        context,
        int,
    ):
        raise TypeError(
            f"Can not construct protobuf message of type "
            f"'tests.reboot.greeter_pb2.DangerousFieldsRequest': field 'context' is not "
            f"of required type 'int'"
        )
    if not isinstance(bearer_token, Unset) and bearer_token is not None and not isinstance(
        bearer_token,
        int,
    ):
        raise TypeError(
            f"Can not construct protobuf message of type "
            f"'tests.reboot.greeter_pb2.DangerousFieldsRequest': field 'bearer_token' is not "
            f"of required type 'int'"
        )
    if not isinstance(reboot, Unset) and reboot is not None and not isinstance(
        reboot,
        int,
    ):
        raise TypeError(
            f"Can not construct protobuf message of type "
            f"'tests.reboot.greeter_pb2.DangerousFieldsRequest': field 'reboot' is not "
            f"of required type 'int'"
        )
    if not isinstance(uuid, Unset) and uuid is not None and not isinstance(
        uuid,
        int,
    ):
        raise TypeError(
            f"Can not construct protobuf message of type "
            f"'tests.reboot.greeter_pb2.DangerousFieldsRequest': field 'uuid' is not "
            f"of required type 'int'"
        )
    if not isinstance(stub, Unset) and stub is not None and not isinstance(
        stub,
        int,
    ):
        raise TypeError(
            f"Can not construct protobuf message of type "
            f"'tests.reboot.greeter_pb2.DangerousFieldsRequest': field 'stub' is not "
            f"of required type 'int'"
        )
    if not isinstance(idempotency, Unset) and idempotency is not None and not isinstance(
        idempotency,
        str,
    ):
        raise TypeError(
            f"Can not construct protobuf message of type "
            f"'tests.reboot.greeter_pb2.DangerousFieldsRequest': field 'idempotency' is not "
            f"of required type 'str'"
        )
    if not isinstance(state_id, Unset) and state_id is not None and not isinstance(
        state_id,
        str,
    ):
        raise TypeError(
            f"Can not construct protobuf message of type "
            f"'tests.reboot.greeter_pb2.DangerousFieldsRequest': field 'state_id' is not "
            f"of required type 'str'"
        )


    __args__: dict[str, IMPORT_typing.Any] = {}

    if not isinstance(metadata, Unset):
        __args__['metadata'] = metadata
    if not isinstance(request, Unset):
        __args__['request'] = request
    if not isinstance(response, Unset):
        __args__['response'] = response
    if not isinstance(self, Unset):
        __args__['self'] = self
    if not isinstance(context, Unset):
        __args__['context'] = context
    if not isinstance(bearer_token, Unset):
        __args__['bearer_token'] = bearer_token
    if not isinstance(reboot, Unset):
        __args__['reboot'] = reboot
    if not isinstance(uuid, Unset):
        __args__['uuid'] = uuid
    if not isinstance(stub, Unset):
        __args__['stub'] = stub
    if not isinstance(idempotency, Unset):
        __args__['idempotency'] = idempotency
    if not isinstance(state_id, Unset):
        __args__['state_id'] = state_id

    return Greeter.DangerousFieldsRequest(
        **__args__,
    )

def GreeterStoreRecursiveMessageResponseToProto(
    response: Greeter.StoreRecursiveMessageResponse
) -> tests.reboot.greeter_pb2.StoreRecursiveMessageResponse:
    return response

def GreeterStoreRecursiveMessageResponseFromProto(
    response: tests.reboot.greeter_pb2.StoreRecursiveMessageResponse
) -> Greeter.StoreRecursiveMessageResponse:
    return response

def GreeterStoreRecursiveMessageRequestToProto(
    request: Greeter.StoreRecursiveMessageRequest
) -> tests.reboot.greeter_pb2.StoreRecursiveMessageRequest:
    return request

def GreeterStoreRecursiveMessageRequestFromProto(
    request: tests.reboot.greeter_pb2.StoreRecursiveMessageRequest
) -> Greeter.StoreRecursiveMessageRequest:
    return request

def GreeterStoreRecursiveMessageRequestFromInputFields(
    message: IMPORT_typing.Optional[tests.reboot.greeter_pb2.RecursiveMessage] | Unset,
):
    assert Greeter.StoreRecursiveMessageRequest is not None

    if not isinstance(message, Unset) and message is not None and not isinstance(
        message,
        tests.reboot.greeter_pb2.RecursiveMessage,
    ):
        raise TypeError(
            f"Can not construct protobuf message of type "
            f"'tests.reboot.greeter_pb2.StoreRecursiveMessageRequest': field 'message' is not "
            f"of required type 'tests.reboot.greeter_pb2.RecursiveMessage'"
        )


    __args__: dict[str, IMPORT_typing.Any] = {}

    if not isinstance(message, Unset):
        __args__['message'] = message

    return Greeter.StoreRecursiveMessageRequest(
        **__args__,
    )

def GreeterReadRecursiveMessageResponseToProto(
    response: Greeter.ReadRecursiveMessageResponse
) -> tests.reboot.greeter_pb2.ReadRecursiveMessageResponse:
    return response

def GreeterReadRecursiveMessageResponseFromProto(
    response: tests.reboot.greeter_pb2.ReadRecursiveMessageResponse
) -> Greeter.ReadRecursiveMessageResponse:
    return response

def GreeterReadRecursiveMessageRequestToProto(
    request: Greeter.ReadRecursiveMessageRequest
) -> tests.reboot.greeter_pb2.ReadRecursiveMessageRequest:
    return request

def GreeterReadRecursiveMessageRequestFromProto(
    request: tests.reboot.greeter_pb2.ReadRecursiveMessageRequest
) -> Greeter.ReadRecursiveMessageRequest:
    return request

def GreeterReadRecursiveMessageRequestFromInputFields(
):
    assert Greeter.ReadRecursiveMessageRequest is not None



    __args__: dict[str, IMPORT_typing.Any] = {}


    return Greeter.ReadRecursiveMessageRequest(
        **__args__,
    )

def GreeterConstructAndStoreRecursiveMessageResponseToProto(
    response: Greeter.ConstructAndStoreRecursiveMessageResponse
) -> tests.reboot.greeter_pb2.ConstructAndStoreRecursiveMessageResponse:
    return response

def GreeterConstructAndStoreRecursiveMessageResponseFromProto(
    response: tests.reboot.greeter_pb2.ConstructAndStoreRecursiveMessageResponse
) -> Greeter.ConstructAndStoreRecursiveMessageResponse:
    return response

def GreeterConstructAndStoreRecursiveMessageRequestToProto(
    request: Greeter.ConstructAndStoreRecursiveMessageRequest
) -> tests.reboot.greeter_pb2.ConstructAndStoreRecursiveMessageRequest:
    return request

def GreeterConstructAndStoreRecursiveMessageRequestFromProto(
    request: tests.reboot.greeter_pb2.ConstructAndStoreRecursiveMessageRequest
) -> Greeter.ConstructAndStoreRecursiveMessageRequest:
    return request

def GreeterConstructAndStoreRecursiveMessageRequestFromInputFields(
    depth: IMPORT_typing.Optional[int] | Unset,
):
    assert Greeter.ConstructAndStoreRecursiveMessageRequest is not None

    if not isinstance(depth, Unset) and depth is not None and not isinstance(
        depth,
        int,
    ):
        raise TypeError(
            f"Can not construct protobuf message of type "
            f"'tests.reboot.greeter_pb2.ConstructAndStoreRecursiveMessageRequest': field 'depth' is not "
            f"of required type 'int'"
        )


    __args__: dict[str, IMPORT_typing.Any] = {}

    if not isinstance(depth, Unset):
        __args__['depth'] = depth

    return Greeter.ConstructAndStoreRecursiveMessageRequest(
        **__args__,
    )


############################ Legacy gRPC Servicers ############################
# This section is relevant (only) for servicers that implement a legacy gRPC
# service in a Reboot context. It is irrelevant to clients.

def MakeLegacyGrpcServiceable(
    # A legacy gRPC servicer type can't be more specific than `type`,
    # because legacy gRPC servicers (as generated by the gRPC `protoc`
    # plugin) do not share any common base class other than `object`.
    servicer_type: type
) -> IMPORT_reboot_aio_servicers.Serviceable:
    if issubclass(servicer_type, tests.reboot.greeter_pb2_grpc.ClockServicer):
        return ClockServiceable(servicer_type)
    if issubclass(servicer_type, tests.reboot.greeter_pb2_grpc.ColorMatcherServicer):
        return ColorMatcherServiceable(servicer_type)
    raise ValueError(f"Unknown legacy gRPC servicer type '{servicer_type}'")


class ClockServiceable(IMPORT_reboot_aio_servicers.Serviceable):
    """ A Servicable that makes it possible to host a legacy gRPC
    `ClockServicer` on Reboot."""

    def __init__(self, legacy_grpc_servicer_type: type[tests.reboot.greeter_pb2_grpc.ClockServicer]):
        self._legacy_grpc_servicer_type = legacy_grpc_servicer_type

    def service_names(self) -> list[IMPORT_reboot_aio_types.ServiceName]:
        return [IMPORT_reboot_aio_types.ServiceName('tests.reboot.Clock')]

    def state_type_name(self) -> None:
        # Legacy gRPC servicers don't have an associated state type.
        return None

    def file_descriptor(self) -> IMPORT_google_protobuf_descriptor.FileDescriptor:
        return tests.reboot.greeter_pb2.DESCRIPTOR

    def instantiate(self) -> tuple[
        tests.reboot.greeter_pb2_grpc.ClockServicer,
        IMPORT_typing.Optional[IMPORT_typing.Callable[
            [object, IMPORT_grpc.aio.Server],
            None
        ]]
    ]:
        servicer = self._legacy_grpc_servicer_type()
        add_to_server_method = tests.reboot.greeter_pb2_grpc.add_ClockServicer_to_server
        return servicer, add_to_server_method

class ColorMatcherServiceable(IMPORT_reboot_aio_servicers.Serviceable):
    """ A Servicable that makes it possible to host a legacy gRPC
    `ColorMatcherServicer` on Reboot."""

    def __init__(self, legacy_grpc_servicer_type: type[tests.reboot.greeter_pb2_grpc.ColorMatcherServicer]):
        self._legacy_grpc_servicer_type = legacy_grpc_servicer_type

    def service_names(self) -> list[IMPORT_reboot_aio_types.ServiceName]:
        return [IMPORT_reboot_aio_types.ServiceName('tests.reboot.ColorMatcher')]

    def state_type_name(self) -> None:
        # Legacy gRPC servicers don't have an associated state type.
        return None

    def file_descriptor(self) -> IMPORT_google_protobuf_descriptor.FileDescriptor:
        return tests.reboot.greeter_pb2.DESCRIPTOR

    def instantiate(self) -> tuple[
        tests.reboot.greeter_pb2_grpc.ColorMatcherServicer,
        IMPORT_typing.Optional[IMPORT_typing.Callable[
            [object, IMPORT_grpc.aio.Server],
            None
        ]]
    ]:
        servicer = self._legacy_grpc_servicer_type()
        add_to_server_method = tests.reboot.greeter_pb2_grpc.add_ColorMatcherServicer_to_server
        return servicer, add_to_server_method


############################ Reboot Servicer Middlewares ############################
# This section is relevant (only) for servicers implementing a Reboot servicer. It
# is irrelevant to clients, except for the fact that some clients are _also_ such
# servicers.

# For internal calls, we can use a magic token to bypass token verification and
# authorization checks. The token provides no auth information (e.g.,
# `context.auth is None`).
__internal_magic_token__: str = f'internal-{IMPORT_uuid.uuid4()}'

class GreeterServicerMiddleware(IMPORT_reboot_aio_internals_middleware.Middleware):

    def __init__(
        self,
        *,
        servicer: GreeterBaseServicer,
        application_id: IMPORT_reboot_aio_types.ApplicationId,
        server_id: IMPORT_reboot_aio_types.ServerId,
        state_manager: IMPORT_reboot_aio_state_managers.StateManager,
        placement_client: IMPORT_reboot_aio_placement.PlacementClient,
        channel_manager: IMPORT_reboot_aio_internals_channel_manager._ChannelManager,
        tasks_cache: IMPORT_reboot_aio_internals_tasks_cache.TasksCache,
        token_verifier: IMPORT_typing.Optional[IMPORT_reboot_aio_auth_token_verifiers.TokenVerifier],
        effect_validation: IMPORT_reboot_aio_contexts.EffectValidation,
        ready: IMPORT_asyncio.Event,
    ):
        super().__init__(
            application_id=application_id,
            server_id=server_id,
            state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            service_names = [
                IMPORT_reboot_aio_types.ServiceName("tests.reboot.GreeterMethods"),
            ],
            placement_client=placement_client,
            channel_manager=channel_manager,
            effect_validation=effect_validation,
        )

        self._servicer = servicer
        self._state_manager = state_manager
        self.tasks_dispatcher = IMPORT_reboot_aio_internals_tasks_dispatcher.TasksDispatcher(
            application_id=application_id,
            dispatch=self.dispatch,
            tasks_cache=tasks_cache,
            ready=ready,
            complete_task=self._state_manager.complete_task,
        )

        # Store the type of each method's request so that stored requests can be
        # deserialized into the correct type.
        self.request_type_by_method_name: dict[str, type[IMPORT_google_protobuf_message.Message]] = {
            'Create': tests.reboot.greeter_pb2.CreateRequest,
            'Greet': tests.reboot.greeter_pb2.GreetRequest,
            'SetAdjective': tests.reboot.greeter_pb2.SetAdjectiveRequest,
            'TransactionSetAdjective': tests.reboot.greeter_pb2.SetAdjectiveRequest,
            'TryToConstructContext': google.protobuf.empty_pb2.Empty,
            'TryToConstructExternalContext': google.protobuf.empty_pb2.Empty,
            'TestLongRunningFetch': tests.reboot.greeter_pb2.TestLongRunningFetchRequest,
            'TestLongRunningWriter': google.protobuf.empty_pb2.Empty,
            'GetWholeState': tests.reboot.greeter_pb2.GetWholeStateRequest,
            'FailWithException': google.protobuf.empty_pb2.Empty,
            'FailWithAborted': google.protobuf.empty_pb2.Empty,
            'Workflow': google.protobuf.empty_pb2.Empty,
            'DangerousFields': tests.reboot.greeter_pb2.DangerousFieldsRequest,
            'StoreRecursiveMessage': tests.reboot.greeter_pb2.StoreRecursiveMessageRequest,
            'ReadRecursiveMessage': tests.reboot.greeter_pb2.ReadRecursiveMessageRequest,
            'ConstructAndStoreRecursiveMessage': tests.reboot.greeter_pb2.ConstructAndStoreRecursiveMessageRequest,
        }

        # Get authorizer, if any, converting from a rule if necessary.
        def convert_authorizer_rule_if_necessary(
            authorizer_or_rule: IMPORT_typing.Optional[
                IMPORT_reboot_aio_auth_authorizers.Authorizer | IMPORT_reboot_aio_auth_authorizers.AuthorizerRule
            ]
        ) -> IMPORT_reboot_aio_auth_authorizers.Authorizer:

            # If no authorizer or rule is provided, return the default
            # authorizer which allows if app internal or allows if in
            # dev mode (and logs some warnings to help the user
            # realize where they are missing authorization).
            if authorizer_or_rule is None:
                return IMPORT_reboot_aio_auth_authorizers.DefaultAuthorizer(
                    'Greeter',
                    is_user_type=False,
                )

            if isinstance(authorizer_or_rule, IMPORT_reboot_aio_auth_authorizers.AuthorizerRule):
                return GreeterAuthorizer(
                    _default=authorizer_or_rule
                )

            return authorizer_or_rule

        self._authorizer = convert_authorizer_rule_if_necessary(
            servicer.authorizer()
        )

        # Create token verifier.
        self._token_verifier: IMPORT_typing.Optional[IMPORT_reboot_aio_auth_token_verifiers.TokenVerifier] = (
            servicer.token_verifier() or token_verifier
        )

        # Since users specify errors as proto messages they can't raise them
        # directly - to do so they have to use the `Aborted` wrapper, which will
        # hold the original proto message. On errors we'll need to check whether
        # such wrappers hold a proto message for a specified error, so we can
        # avoid retrying tasks that complete with a specified error.
        self._specified_errors_by_service_method_name: dict[str, list[str]] = {
            'tests.reboot.GreeterMethods.FailWithAborted': [
                'tests.reboot.greeter_pb2.ErrorWithValue',
            ],
            'tests.reboot.GreeterMethods.Workflow': [
                'tests.reboot.greeter_pb2.ErrorWithValue',
            ],
        }


    def add_to_server(self, server: IMPORT_grpc.aio.Server) -> None:
        tests.reboot.greeter_pb2_grpc.add_GreeterMethodsServicer_to_server(
            self, server
        )

    async def inspect(self, state_ref: IMPORT_reboot_aio_types.StateRef) -> IMPORT_typing.AsyncIterator[IMPORT_google_protobuf_message.Message]:
        """Implementation of `Middleware.inspect()`."""
        context = self.create_context(
            headers=IMPORT_reboot_aio_headers.Headers(
                application_id=self.application_id,
                state_ref=state_ref,
            ),
            state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            method="inspect",
            context_type=IMPORT_reboot_aio_contexts.ReaderContext,
        )

        async with self._state_manager.streaming_reader_idempotency_key(
            context,
            self._servicer.__state_type__,
            authorize=None,
        ) as states:
            async for (state, idempotency_key) in states:
                yield state

    async def react_query(
        self,
        headers: IMPORT_reboot_aio_headers.Headers,
        method: str,
        request_bytes: bytes,
    ) -> IMPORT_typing.AsyncIterator[tuple[IMPORT_typing.Optional[IMPORT_google_protobuf_message.Message], list[IMPORT_uuid.UUID]]]:
        """Returns the response of calling 'method' given a message
        deserialized from the provided 'request_bytes' for each state
        update that creates a different response.

        # The caller (react.py) should have already ensured that this server
        # is authoritative for this traffic.
        assert self.placement_client.server_for_actor(
            headers.application_id,
            headers.state_ref,
        ) == self._server_id

        NOTE: only unary reader methods are supported."""
        # Need to define these up here since we can only do that once.
        last_response: IMPORT_typing.Optional[IMPORT_google_protobuf_message.Message] = None
        aggregated_idempotency_keys: list[IMPORT_uuid.UUID] = []
        if method == 'Create':
            # Invariant here is that users should not have called this
            # directly but only through code generated React
            # components which should not have been generated except
            # for valid method candidates.
            logger.warning(
                "Got a React query request with an invalid method name: "
                f"Method '{method}' is invalid for servicer Greeter."
                "\n"
                "Do you have a browser tab open for an older version "
                "of this application, or for a different application all together?"
            )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.InvalidMethod(),
                message=
                    f"Method '{method}' is invalid"
            )
            yield  # Necessary for type checking.
        elif method == 'Greet':

            context = self.create_context(
                headers=headers,
                state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                method='Greet',
                context_type=IMPORT_reboot_aio_contexts.ReaderContext,
            )

            with IMPORT_reboot_aio_tracing.span(
                state_name=f"{context.state_type_name}('{context.state_id}')",
                span_name="Greet() (reactively)",
                # The naming above matches Python, but not TypeScript.
                python_specific=True,
                level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
            ):
                context.auth = await self._maybe_verify_token(
                    headers=headers, method='Greet'
                )

                request = tests.reboot.greeter_pb2.GreetRequest()
                request.ParseFromString(request_bytes)

                async with self._state_manager.reactively(
                    context,
                    self._servicer.__state_type__,
                    authorize=self._maybe_authorize(
                        method_name='tests.reboot.GreeterMethods.Greet',
                        headers=headers,
                        auth=context.auth,
                        request=request,
                    ),
                ) as states:
                    async for (state, idempotency_keys) in states:

                        aggregated_idempotency_keys.extend(idempotency_keys)

                        # Note: This does not do any defensive copying currently:
                        # see https://github.com/reboot-dev/respect/issues/2636.
                        @IMPORT_reboot_aio_internals_middleware.maybe_run_function_twice_to_validate_effects
                        async def run__Greet(validating_effects: bool) -> IMPORT_google_protobuf_message.Message:
                            return await self.__Greet(
                                context,
                                state,
                                request,
                                validating_effects=validating_effects,
                            )

                        response = await run__Greet()

                        if last_response != response:
                            yield (response, aggregated_idempotency_keys)
                            last_response = response
                        else:
                            yield (None, aggregated_idempotency_keys)

                        aggregated_idempotency_keys.clear()
        elif method == 'SetAdjective':
            # Invariant here is that users should not have called this
            # directly but only through code generated React
            # components which should not have been generated except
            # for valid method candidates.
            logger.warning(
                "Got a React query request with an invalid method name: "
                f"Method '{method}' is invalid for servicer Greeter."
                "\n"
                "Do you have a browser tab open for an older version "
                "of this application, or for a different application all together?"
            )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.InvalidMethod(),
                message=
                    f"Method '{method}' is invalid"
            )
            yield  # Necessary for type checking.
        elif method == 'TransactionSetAdjective':
            # Invariant here is that users should not have called this
            # directly but only through code generated React
            # components which should not have been generated except
            # for valid method candidates.
            logger.warning(
                "Got a React query request with an invalid method name: "
                f"Method '{method}' is invalid for servicer Greeter."
                "\n"
                "Do you have a browser tab open for an older version "
                "of this application, or for a different application all together?"
            )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.InvalidMethod(),
                message=
                    f"Method '{method}' is invalid"
            )
            yield  # Necessary for type checking.
        elif method == 'TryToConstructContext':

            context = self.create_context(
                headers=headers,
                state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                method='TryToConstructContext',
                context_type=IMPORT_reboot_aio_contexts.ReaderContext,
            )

            with IMPORT_reboot_aio_tracing.span(
                state_name=f"{context.state_type_name}('{context.state_id}')",
                span_name="TryToConstructContext() (reactively)",
                # The naming above matches Python, but not TypeScript.
                python_specific=True,
                level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
            ):
                context.auth = await self._maybe_verify_token(
                    headers=headers, method='TryToConstructContext'
                )

                request = google.protobuf.empty_pb2.Empty()
                request.ParseFromString(request_bytes)

                async with self._state_manager.reactively(
                    context,
                    self._servicer.__state_type__,
                    authorize=self._maybe_authorize(
                        method_name='tests.reboot.GreeterMethods.TryToConstructContext',
                        headers=headers,
                        auth=context.auth,
                        request=request,
                    ),
                ) as states:
                    async for (state, idempotency_keys) in states:

                        aggregated_idempotency_keys.extend(idempotency_keys)

                        # Note: This does not do any defensive copying currently:
                        # see https://github.com/reboot-dev/respect/issues/2636.
                        @IMPORT_reboot_aio_internals_middleware.maybe_run_function_twice_to_validate_effects
                        async def run__TryToConstructContext(validating_effects: bool) -> IMPORT_google_protobuf_message.Message:
                            return await self.__TryToConstructContext(
                                context,
                                state,
                                request,
                                validating_effects=validating_effects,
                            )

                        response = await run__TryToConstructContext()

                        if last_response != response:
                            yield (response, aggregated_idempotency_keys)
                            last_response = response
                        else:
                            yield (None, aggregated_idempotency_keys)

                        aggregated_idempotency_keys.clear()
        elif method == 'TryToConstructExternalContext':

            context = self.create_context(
                headers=headers,
                state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                method='TryToConstructExternalContext',
                context_type=IMPORT_reboot_aio_contexts.ReaderContext,
            )

            with IMPORT_reboot_aio_tracing.span(
                state_name=f"{context.state_type_name}('{context.state_id}')",
                span_name="TryToConstructExternalContext() (reactively)",
                # The naming above matches Python, but not TypeScript.
                python_specific=True,
                level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
            ):
                context.auth = await self._maybe_verify_token(
                    headers=headers, method='TryToConstructExternalContext'
                )

                request = google.protobuf.empty_pb2.Empty()
                request.ParseFromString(request_bytes)

                async with self._state_manager.reactively(
                    context,
                    self._servicer.__state_type__,
                    authorize=self._maybe_authorize(
                        method_name='tests.reboot.GreeterMethods.TryToConstructExternalContext',
                        headers=headers,
                        auth=context.auth,
                        request=request,
                    ),
                ) as states:
                    async for (state, idempotency_keys) in states:

                        aggregated_idempotency_keys.extend(idempotency_keys)

                        # Note: This does not do any defensive copying currently:
                        # see https://github.com/reboot-dev/respect/issues/2636.
                        @IMPORT_reboot_aio_internals_middleware.maybe_run_function_twice_to_validate_effects
                        async def run__TryToConstructExternalContext(validating_effects: bool) -> IMPORT_google_protobuf_message.Message:
                            return await self.__TryToConstructExternalContext(
                                context,
                                state,
                                request,
                                validating_effects=validating_effects,
                            )

                        response = await run__TryToConstructExternalContext()

                        if last_response != response:
                            yield (response, aggregated_idempotency_keys)
                            last_response = response
                        else:
                            yield (None, aggregated_idempotency_keys)

                        aggregated_idempotency_keys.clear()
        elif method == 'TestLongRunningFetch':

            context = self.create_context(
                headers=headers,
                state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                method='TestLongRunningFetch',
                context_type=IMPORT_reboot_aio_contexts.ReaderContext,
            )

            with IMPORT_reboot_aio_tracing.span(
                state_name=f"{context.state_type_name}('{context.state_id}')",
                span_name="TestLongRunningFetch() (reactively)",
                # The naming above matches Python, but not TypeScript.
                python_specific=True,
                level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
            ):
                context.auth = await self._maybe_verify_token(
                    headers=headers, method='TestLongRunningFetch'
                )

                request = tests.reboot.greeter_pb2.TestLongRunningFetchRequest()
                request.ParseFromString(request_bytes)

                async with self._state_manager.reactively(
                    context,
                    self._servicer.__state_type__,
                    authorize=self._maybe_authorize(
                        method_name='tests.reboot.GreeterMethods.TestLongRunningFetch',
                        headers=headers,
                        auth=context.auth,
                        request=request,
                    ),
                ) as states:
                    async for (state, idempotency_keys) in states:

                        aggregated_idempotency_keys.extend(idempotency_keys)

                        # Note: This does not do any defensive copying currently:
                        # see https://github.com/reboot-dev/respect/issues/2636.
                        @IMPORT_reboot_aio_internals_middleware.maybe_run_function_twice_to_validate_effects
                        async def run__TestLongRunningFetch(validating_effects: bool) -> IMPORT_google_protobuf_message.Message:
                            return await self.__TestLongRunningFetch(
                                context,
                                state,
                                request,
                                validating_effects=validating_effects,
                            )

                        response = await run__TestLongRunningFetch()

                        if last_response != response:
                            yield (response, aggregated_idempotency_keys)
                            last_response = response
                        else:
                            yield (None, aggregated_idempotency_keys)

                        aggregated_idempotency_keys.clear()
        elif method == 'TestLongRunningWriter':
            # Invariant here is that users should not have called this
            # directly but only through code generated React
            # components which should not have been generated except
            # for valid method candidates.
            logger.warning(
                "Got a React query request with an invalid method name: "
                f"Method '{method}' is invalid for servicer Greeter."
                "\n"
                "Do you have a browser tab open for an older version "
                "of this application, or for a different application all together?"
            )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.InvalidMethod(),
                message=
                    f"Method '{method}' is invalid"
            )
            yield  # Necessary for type checking.
        elif method == 'GetWholeState':

            context = self.create_context(
                headers=headers,
                state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                method='GetWholeState',
                context_type=IMPORT_reboot_aio_contexts.ReaderContext,
            )

            with IMPORT_reboot_aio_tracing.span(
                state_name=f"{context.state_type_name}('{context.state_id}')",
                span_name="GetWholeState() (reactively)",
                # The naming above matches Python, but not TypeScript.
                python_specific=True,
                level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
            ):
                context.auth = await self._maybe_verify_token(
                    headers=headers, method='GetWholeState'
                )

                request = tests.reboot.greeter_pb2.GetWholeStateRequest()
                request.ParseFromString(request_bytes)

                async with self._state_manager.reactively(
                    context,
                    self._servicer.__state_type__,
                    authorize=self._maybe_authorize(
                        method_name='tests.reboot.GreeterMethods.GetWholeState',
                        headers=headers,
                        auth=context.auth,
                        request=request,
                    ),
                ) as states:
                    async for (state, idempotency_keys) in states:

                        aggregated_idempotency_keys.extend(idempotency_keys)

                        # Note: This does not do any defensive copying currently:
                        # see https://github.com/reboot-dev/respect/issues/2636.
                        @IMPORT_reboot_aio_internals_middleware.maybe_run_function_twice_to_validate_effects
                        async def run__GetWholeState(validating_effects: bool) -> IMPORT_google_protobuf_message.Message:
                            return await self.__GetWholeState(
                                context,
                                state,
                                request,
                                validating_effects=validating_effects,
                            )

                        response = await run__GetWholeState()

                        if last_response != response:
                            yield (response, aggregated_idempotency_keys)
                            last_response = response
                        else:
                            yield (None, aggregated_idempotency_keys)

                        aggregated_idempotency_keys.clear()
        elif method == 'FailWithException':

            context = self.create_context(
                headers=headers,
                state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                method='FailWithException',
                context_type=IMPORT_reboot_aio_contexts.ReaderContext,
            )

            with IMPORT_reboot_aio_tracing.span(
                state_name=f"{context.state_type_name}('{context.state_id}')",
                span_name="FailWithException() (reactively)",
                # The naming above matches Python, but not TypeScript.
                python_specific=True,
                level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
            ):
                context.auth = await self._maybe_verify_token(
                    headers=headers, method='FailWithException'
                )

                request = google.protobuf.empty_pb2.Empty()
                request.ParseFromString(request_bytes)

                async with self._state_manager.reactively(
                    context,
                    self._servicer.__state_type__,
                    authorize=self._maybe_authorize(
                        method_name='tests.reboot.GreeterMethods.FailWithException',
                        headers=headers,
                        auth=context.auth,
                        request=request,
                    ),
                ) as states:
                    async for (state, idempotency_keys) in states:

                        aggregated_idempotency_keys.extend(idempotency_keys)

                        # Note: This does not do any defensive copying currently:
                        # see https://github.com/reboot-dev/respect/issues/2636.
                        @IMPORT_reboot_aio_internals_middleware.maybe_run_function_twice_to_validate_effects
                        async def run__FailWithException(validating_effects: bool) -> IMPORT_google_protobuf_message.Message:
                            return await self.__FailWithException(
                                context,
                                state,
                                request,
                                validating_effects=validating_effects,
                            )

                        response = await run__FailWithException()

                        if last_response != response:
                            yield (response, aggregated_idempotency_keys)
                            last_response = response
                        else:
                            yield (None, aggregated_idempotency_keys)

                        aggregated_idempotency_keys.clear()
        elif method == 'FailWithAborted':

            context = self.create_context(
                headers=headers,
                state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                method='FailWithAborted',
                context_type=IMPORT_reboot_aio_contexts.ReaderContext,
            )

            with IMPORT_reboot_aio_tracing.span(
                state_name=f"{context.state_type_name}('{context.state_id}')",
                span_name="FailWithAborted() (reactively)",
                # The naming above matches Python, but not TypeScript.
                python_specific=True,
                level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
            ):
                context.auth = await self._maybe_verify_token(
                    headers=headers, method='FailWithAborted'
                )

                request = google.protobuf.empty_pb2.Empty()
                request.ParseFromString(request_bytes)

                async with self._state_manager.reactively(
                    context,
                    self._servicer.__state_type__,
                    authorize=self._maybe_authorize(
                        method_name='tests.reboot.GreeterMethods.FailWithAborted',
                        headers=headers,
                        auth=context.auth,
                        request=request,
                    ),
                ) as states:
                    async for (state, idempotency_keys) in states:

                        aggregated_idempotency_keys.extend(idempotency_keys)

                        # Note: This does not do any defensive copying currently:
                        # see https://github.com/reboot-dev/respect/issues/2636.
                        @IMPORT_reboot_aio_internals_middleware.maybe_run_function_twice_to_validate_effects
                        async def run__FailWithAborted(validating_effects: bool) -> IMPORT_google_protobuf_message.Message:
                            return await self.__FailWithAborted(
                                context,
                                state,
                                request,
                                validating_effects=validating_effects,
                            )

                        response = await run__FailWithAborted()

                        if last_response != response:
                            yield (response, aggregated_idempotency_keys)
                            last_response = response
                        else:
                            yield (None, aggregated_idempotency_keys)

                        aggregated_idempotency_keys.clear()
        elif method == 'Workflow':
            # Invariant here is that users should not have called this
            # directly but only through code generated React
            # components which should not have been generated except
            # for valid method candidates.
            logger.warning(
                "Got a React query request with an invalid method name: "
                f"Method '{method}' is invalid for servicer Greeter."
                "\n"
                "Do you have a browser tab open for an older version "
                "of this application, or for a different application all together?"
            )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.InvalidMethod(),
                message=
                    f"Method '{method}' is invalid"
            )
            yield  # Necessary for type checking.
        elif method == 'DangerousFields':
            # Invariant here is that users should not have called this
            # directly but only through code generated React
            # components which should not have been generated except
            # for valid method candidates.
            logger.warning(
                "Got a React query request with an invalid method name: "
                f"Method '{method}' is invalid for servicer Greeter."
                "\n"
                "Do you have a browser tab open for an older version "
                "of this application, or for a different application all together?"
            )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.InvalidMethod(),
                message=
                    f"Method '{method}' is invalid"
            )
            yield  # Necessary for type checking.
        elif method == 'StoreRecursiveMessage':
            # Invariant here is that users should not have called this
            # directly but only through code generated React
            # components which should not have been generated except
            # for valid method candidates.
            logger.warning(
                "Got a React query request with an invalid method name: "
                f"Method '{method}' is invalid for servicer Greeter."
                "\n"
                "Do you have a browser tab open for an older version "
                "of this application, or for a different application all together?"
            )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.InvalidMethod(),
                message=
                    f"Method '{method}' is invalid"
            )
            yield  # Necessary for type checking.
        elif method == 'ReadRecursiveMessage':

            context = self.create_context(
                headers=headers,
                state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                method='ReadRecursiveMessage',
                context_type=IMPORT_reboot_aio_contexts.ReaderContext,
            )

            with IMPORT_reboot_aio_tracing.span(
                state_name=f"{context.state_type_name}('{context.state_id}')",
                span_name="ReadRecursiveMessage() (reactively)",
                # The naming above matches Python, but not TypeScript.
                python_specific=True,
                level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
            ):
                context.auth = await self._maybe_verify_token(
                    headers=headers, method='ReadRecursiveMessage'
                )

                request = tests.reboot.greeter_pb2.ReadRecursiveMessageRequest()
                request.ParseFromString(request_bytes)

                async with self._state_manager.reactively(
                    context,
                    self._servicer.__state_type__,
                    authorize=self._maybe_authorize(
                        method_name='tests.reboot.GreeterMethods.ReadRecursiveMessage',
                        headers=headers,
                        auth=context.auth,
                        request=request,
                    ),
                ) as states:
                    async for (state, idempotency_keys) in states:

                        aggregated_idempotency_keys.extend(idempotency_keys)

                        # Note: This does not do any defensive copying currently:
                        # see https://github.com/reboot-dev/respect/issues/2636.
                        @IMPORT_reboot_aio_internals_middleware.maybe_run_function_twice_to_validate_effects
                        async def run__ReadRecursiveMessage(validating_effects: bool) -> IMPORT_google_protobuf_message.Message:
                            return await self.__ReadRecursiveMessage(
                                context,
                                state,
                                request,
                                validating_effects=validating_effects,
                            )

                        response = await run__ReadRecursiveMessage()

                        if last_response != response:
                            yield (response, aggregated_idempotency_keys)
                            last_response = response
                        else:
                            yield (None, aggregated_idempotency_keys)

                        aggregated_idempotency_keys.clear()
        elif method == 'ConstructAndStoreRecursiveMessage':
            # Invariant here is that users should not have called this
            # directly but only through code generated React
            # components which should not have been generated except
            # for valid method candidates.
            logger.warning(
                "Got a React query request with an invalid method name: "
                f"Method '{method}' is invalid for servicer Greeter."
                "\n"
                "Do you have a browser tab open for an older version "
                "of this application, or for a different application all together?"
            )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.InvalidMethod(),
                message=
                    f"Method '{method}' is invalid"
            )
            yield  # Necessary for type checking.
        else:
            logger.warning(
                "Got a React query request with an invalid method name: "
                f"Method '{method}' is invalid for servicer Greeter."
                "\n"
                "Do you have a browser tab open for an older version "
                "of this application, or for a different application all together?"
            )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.InvalidMethod(),
                message=
                    f"Method '{method}' not found"
            )
            yield  # Unreachable but necessary for mypy.

    async def react_mutate(
        self,
        headers: IMPORT_reboot_aio_headers.Headers,
        method: str,
        request_bytes: bytes,
    ) -> IMPORT_google_protobuf_message.Message:
        """Returns the response of calling 'method' given a message
        deserialized from the provided 'request_bytes'."""
        if method == 'Create':
            request = tests.reboot.greeter_pb2.CreateRequest()
            request.ParseFromString(request_bytes)

            # NOTE: we automatically retry mutations that come through
            # React when we get a `IMPORT_grpc.StatusCode.UNAVAILABLE` to
            # match the retry logic we do in the React code generated
            # to handle lack/loss of connectivity.
            #
            # TODO(benh): revisit this decision if we ever see reason
            # to call `react_mutate()` from any place other than where
            # we're executing React (e.g., browser, next.js server
            # component, etc).
            call_backoff = IMPORT_reboot_aio_backoff.Backoff()
            while True:
                # We make a full-fledged gRPC call, so that if this traffic
                # was misrouted (i.e. this server is not authoritative
                # for the state), it will now go to the right place. The
                # receiving middleware will handle things like effect
                # validation and so forth.
                assert headers.application_id is not None  # Guaranteed by `Headers`.
                stub = tests.reboot.greeter_pb2_grpc.GreeterMethodsStub(
                    self.channel_manager.get_channel_to(
                        self.placement_client.address_for_actor(
                            headers.application_id,
                            headers.state_ref,
                        )
                    )
                )
                call = stub.Create(
                    request=request,
                    metadata=headers.to_grpc_metadata(),
                )
                try:
                    return await call
                except IMPORT_grpc.aio.AioRpcError as error:
                    if error.code() == IMPORT_grpc.StatusCode.UNAVAILABLE:
                        await call_backoff()
                        continue

                    # Reconstitute the error that the server threw, if it was a declared error.
                    status = await IMPORT_rpc_status_async.from_call(call)
                    if status is not None:
                        raise Greeter.CreateAborted.from_status(
                            status
                        ) from None
                    raise Greeter.CreateAborted.from_grpc_aio_rpc_error(
                        error
                     ) from None

        elif method == 'Greet':
            # Invariant here is that users should not have called this
            # directly but only through code generated React
            # components which should not have been generated except
            # for valid method candidates.
            logger.warning(
                "Got a react mutate request with an invalid method name: "
                "Method 'Greet' is invalid for servicer Greeter."
                "\n"
                "Do you have an old browser tab still open for an older version "
                "of this application, or a different application all together?"
            )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.InvalidMethod(),
                message=f"Method '{method}' is invalid"
            )
        elif method == 'SetAdjective':
            request = tests.reboot.greeter_pb2.SetAdjectiveRequest()
            request.ParseFromString(request_bytes)

            # NOTE: we automatically retry mutations that come through
            # React when we get a `IMPORT_grpc.StatusCode.UNAVAILABLE` to
            # match the retry logic we do in the React code generated
            # to handle lack/loss of connectivity.
            #
            # TODO(benh): revisit this decision if we ever see reason
            # to call `react_mutate()` from any place other than where
            # we're executing React (e.g., browser, next.js server
            # component, etc).
            call_backoff = IMPORT_reboot_aio_backoff.Backoff()
            while True:
                # We make a full-fledged gRPC call, so that if this traffic
                # was misrouted (i.e. this server is not authoritative
                # for the state), it will now go to the right place. The
                # receiving middleware will handle things like effect
                # validation and so forth.
                assert headers.application_id is not None  # Guaranteed by `Headers`.
                stub = tests.reboot.greeter_pb2_grpc.GreeterMethodsStub(
                    self.channel_manager.get_channel_to(
                        self.placement_client.address_for_actor(
                            headers.application_id,
                            headers.state_ref,
                        )
                    )
                )
                call = stub.SetAdjective(
                    request=request,
                    metadata=headers.to_grpc_metadata(),
                )
                try:
                    return await call
                except IMPORT_grpc.aio.AioRpcError as error:
                    if error.code() == IMPORT_grpc.StatusCode.UNAVAILABLE:
                        await call_backoff()
                        continue

                    # Reconstitute the error that the server threw, if it was a declared error.
                    status = await IMPORT_rpc_status_async.from_call(call)
                    if status is not None:
                        raise Greeter.SetAdjectiveAborted.from_status(
                            status
                        ) from None
                    raise Greeter.SetAdjectiveAborted.from_grpc_aio_rpc_error(
                        error
                     ) from None

        elif method == 'TransactionSetAdjective':
            request = tests.reboot.greeter_pb2.SetAdjectiveRequest()
            request.ParseFromString(request_bytes)

            # NOTE: we automatically retry mutations that come through
            # React when we get a `IMPORT_grpc.StatusCode.UNAVAILABLE` to
            # match the retry logic we do in the React code generated
            # to handle lack/loss of connectivity.
            #
            # TODO(benh): revisit this decision if we ever see reason
            # to call `react_mutate()` from any place other than where
            # we're executing React (e.g., browser, next.js server
            # component, etc).
            call_backoff = IMPORT_reboot_aio_backoff.Backoff()
            while True:
                # We make a full-fledged gRPC call, so that if this traffic
                # was misrouted (i.e. this server is not authoritative
                # for the state), it will now go to the right place. The
                # receiving middleware will handle things like effect
                # validation and so forth.
                assert headers.application_id is not None  # Guaranteed by `Headers`.
                stub = tests.reboot.greeter_pb2_grpc.GreeterMethodsStub(
                    self.channel_manager.get_channel_to(
                        self.placement_client.address_for_actor(
                            headers.application_id,
                            headers.state_ref,
                        )
                    )
                )
                call = stub.TransactionSetAdjective(
                    request=request,
                    metadata=headers.to_grpc_metadata(),
                )
                try:
                    return await call
                except IMPORT_grpc.aio.AioRpcError as error:
                    if error.code() == IMPORT_grpc.StatusCode.UNAVAILABLE:
                        await call_backoff()
                        continue

                    # Reconstitute the error that the server threw, if it was a declared error.
                    status = await IMPORT_rpc_status_async.from_call(call)
                    if status is not None:
                        raise Greeter.TransactionSetAdjectiveAborted.from_status(
                            status
                        ) from None
                    raise Greeter.TransactionSetAdjectiveAborted.from_grpc_aio_rpc_error(
                        error
                     ) from None

        elif method == 'TryToConstructContext':
            # Invariant here is that users should not have called this
            # directly but only through code generated React
            # components which should not have been generated except
            # for valid method candidates.
            logger.warning(
                "Got a react mutate request with an invalid method name: "
                "Method 'TryToConstructContext' is invalid for servicer Greeter."
                "\n"
                "Do you have an old browser tab still open for an older version "
                "of this application, or a different application all together?"
            )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.InvalidMethod(),
                message=f"Method '{method}' is invalid"
            )
        elif method == 'TryToConstructExternalContext':
            # Invariant here is that users should not have called this
            # directly but only through code generated React
            # components which should not have been generated except
            # for valid method candidates.
            logger.warning(
                "Got a react mutate request with an invalid method name: "
                "Method 'TryToConstructExternalContext' is invalid for servicer Greeter."
                "\n"
                "Do you have an old browser tab still open for an older version "
                "of this application, or a different application all together?"
            )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.InvalidMethod(),
                message=f"Method '{method}' is invalid"
            )
        elif method == 'TestLongRunningFetch':
            # Invariant here is that users should not have called this
            # directly but only through code generated React
            # components which should not have been generated except
            # for valid method candidates.
            logger.warning(
                "Got a react mutate request with an invalid method name: "
                "Method 'TestLongRunningFetch' is invalid for servicer Greeter."
                "\n"
                "Do you have an old browser tab still open for an older version "
                "of this application, or a different application all together?"
            )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.InvalidMethod(),
                message=f"Method '{method}' is invalid"
            )
        elif method == 'TestLongRunningWriter':
            request = google.protobuf.empty_pb2.Empty()
            request.ParseFromString(request_bytes)

            # NOTE: we automatically retry mutations that come through
            # React when we get a `IMPORT_grpc.StatusCode.UNAVAILABLE` to
            # match the retry logic we do in the React code generated
            # to handle lack/loss of connectivity.
            #
            # TODO(benh): revisit this decision if we ever see reason
            # to call `react_mutate()` from any place other than where
            # we're executing React (e.g., browser, next.js server
            # component, etc).
            call_backoff = IMPORT_reboot_aio_backoff.Backoff()
            while True:
                # We make a full-fledged gRPC call, so that if this traffic
                # was misrouted (i.e. this server is not authoritative
                # for the state), it will now go to the right place. The
                # receiving middleware will handle things like effect
                # validation and so forth.
                assert headers.application_id is not None  # Guaranteed by `Headers`.
                stub = tests.reboot.greeter_pb2_grpc.GreeterMethodsStub(
                    self.channel_manager.get_channel_to(
                        self.placement_client.address_for_actor(
                            headers.application_id,
                            headers.state_ref,
                        )
                    )
                )
                call = stub.TestLongRunningWriter(
                    request=request,
                    metadata=headers.to_grpc_metadata(),
                )
                try:
                    return await call
                except IMPORT_grpc.aio.AioRpcError as error:
                    if error.code() == IMPORT_grpc.StatusCode.UNAVAILABLE:
                        await call_backoff()
                        continue

                    # Reconstitute the error that the server threw, if it was a declared error.
                    status = await IMPORT_rpc_status_async.from_call(call)
                    if status is not None:
                        raise Greeter.TestLongRunningWriterAborted.from_status(
                            status
                        ) from None
                    raise Greeter.TestLongRunningWriterAborted.from_grpc_aio_rpc_error(
                        error
                     ) from None

        elif method == 'GetWholeState':
            # Invariant here is that users should not have called this
            # directly but only through code generated React
            # components which should not have been generated except
            # for valid method candidates.
            logger.warning(
                "Got a react mutate request with an invalid method name: "
                "Method 'GetWholeState' is invalid for servicer Greeter."
                "\n"
                "Do you have an old browser tab still open for an older version "
                "of this application, or a different application all together?"
            )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.InvalidMethod(),
                message=f"Method '{method}' is invalid"
            )
        elif method == 'FailWithException':
            # Invariant here is that users should not have called this
            # directly but only through code generated React
            # components which should not have been generated except
            # for valid method candidates.
            logger.warning(
                "Got a react mutate request with an invalid method name: "
                "Method 'FailWithException' is invalid for servicer Greeter."
                "\n"
                "Do you have an old browser tab still open for an older version "
                "of this application, or a different application all together?"
            )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.InvalidMethod(),
                message=f"Method '{method}' is invalid"
            )
        elif method == 'FailWithAborted':
            # Invariant here is that users should not have called this
            # directly but only through code generated React
            # components which should not have been generated except
            # for valid method candidates.
            logger.warning(
                "Got a react mutate request with an invalid method name: "
                "Method 'FailWithAborted' is invalid for servicer Greeter."
                "\n"
                "Do you have an old browser tab still open for an older version "
                "of this application, or a different application all together?"
            )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.InvalidMethod(),
                message=f"Method '{method}' is invalid"
            )
        elif method == 'Workflow':
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.InvalidMethod(),
                message=
                f"Method 'Workflow' can not be called via React (for now)"
            )
        elif method == 'DangerousFields':
            request = tests.reboot.greeter_pb2.DangerousFieldsRequest()
            request.ParseFromString(request_bytes)

            # NOTE: we automatically retry mutations that come through
            # React when we get a `IMPORT_grpc.StatusCode.UNAVAILABLE` to
            # match the retry logic we do in the React code generated
            # to handle lack/loss of connectivity.
            #
            # TODO(benh): revisit this decision if we ever see reason
            # to call `react_mutate()` from any place other than where
            # we're executing React (e.g., browser, next.js server
            # component, etc).
            call_backoff = IMPORT_reboot_aio_backoff.Backoff()
            while True:
                # We make a full-fledged gRPC call, so that if this traffic
                # was misrouted (i.e. this server is not authoritative
                # for the state), it will now go to the right place. The
                # receiving middleware will handle things like effect
                # validation and so forth.
                assert headers.application_id is not None  # Guaranteed by `Headers`.
                stub = tests.reboot.greeter_pb2_grpc.GreeterMethodsStub(
                    self.channel_manager.get_channel_to(
                        self.placement_client.address_for_actor(
                            headers.application_id,
                            headers.state_ref,
                        )
                    )
                )
                call = stub.DangerousFields(
                    request=request,
                    metadata=headers.to_grpc_metadata(),
                )
                try:
                    return await call
                except IMPORT_grpc.aio.AioRpcError as error:
                    if error.code() == IMPORT_grpc.StatusCode.UNAVAILABLE:
                        await call_backoff()
                        continue

                    # Reconstitute the error that the server threw, if it was a declared error.
                    status = await IMPORT_rpc_status_async.from_call(call)
                    if status is not None:
                        raise Greeter.DangerousFieldsAborted.from_status(
                            status
                        ) from None
                    raise Greeter.DangerousFieldsAborted.from_grpc_aio_rpc_error(
                        error
                     ) from None

        elif method == 'StoreRecursiveMessage':
            request = tests.reboot.greeter_pb2.StoreRecursiveMessageRequest()
            request.ParseFromString(request_bytes)

            # NOTE: we automatically retry mutations that come through
            # React when we get a `IMPORT_grpc.StatusCode.UNAVAILABLE` to
            # match the retry logic we do in the React code generated
            # to handle lack/loss of connectivity.
            #
            # TODO(benh): revisit this decision if we ever see reason
            # to call `react_mutate()` from any place other than where
            # we're executing React (e.g., browser, next.js server
            # component, etc).
            call_backoff = IMPORT_reboot_aio_backoff.Backoff()
            while True:
                # We make a full-fledged gRPC call, so that if this traffic
                # was misrouted (i.e. this server is not authoritative
                # for the state), it will now go to the right place. The
                # receiving middleware will handle things like effect
                # validation and so forth.
                assert headers.application_id is not None  # Guaranteed by `Headers`.
                stub = tests.reboot.greeter_pb2_grpc.GreeterMethodsStub(
                    self.channel_manager.get_channel_to(
                        self.placement_client.address_for_actor(
                            headers.application_id,
                            headers.state_ref,
                        )
                    )
                )
                call = stub.StoreRecursiveMessage(
                    request=request,
                    metadata=headers.to_grpc_metadata(),
                )
                try:
                    return await call
                except IMPORT_grpc.aio.AioRpcError as error:
                    if error.code() == IMPORT_grpc.StatusCode.UNAVAILABLE:
                        await call_backoff()
                        continue

                    # Reconstitute the error that the server threw, if it was a declared error.
                    status = await IMPORT_rpc_status_async.from_call(call)
                    if status is not None:
                        raise Greeter.StoreRecursiveMessageAborted.from_status(
                            status
                        ) from None
                    raise Greeter.StoreRecursiveMessageAborted.from_grpc_aio_rpc_error(
                        error
                     ) from None

        elif method == 'ReadRecursiveMessage':
            # Invariant here is that users should not have called this
            # directly but only through code generated React
            # components which should not have been generated except
            # for valid method candidates.
            logger.warning(
                "Got a react mutate request with an invalid method name: "
                "Method 'ReadRecursiveMessage' is invalid for servicer Greeter."
                "\n"
                "Do you have an old browser tab still open for an older version "
                "of this application, or a different application all together?"
            )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.InvalidMethod(),
                message=f"Method '{method}' is invalid"
            )
        elif method == 'ConstructAndStoreRecursiveMessage':
            request = tests.reboot.greeter_pb2.ConstructAndStoreRecursiveMessageRequest()
            request.ParseFromString(request_bytes)

            # NOTE: we automatically retry mutations that come through
            # React when we get a `IMPORT_grpc.StatusCode.UNAVAILABLE` to
            # match the retry logic we do in the React code generated
            # to handle lack/loss of connectivity.
            #
            # TODO(benh): revisit this decision if we ever see reason
            # to call `react_mutate()` from any place other than where
            # we're executing React (e.g., browser, next.js server
            # component, etc).
            call_backoff = IMPORT_reboot_aio_backoff.Backoff()
            while True:
                # We make a full-fledged gRPC call, so that if this traffic
                # was misrouted (i.e. this server is not authoritative
                # for the state), it will now go to the right place. The
                # receiving middleware will handle things like effect
                # validation and so forth.
                assert headers.application_id is not None  # Guaranteed by `Headers`.
                stub = tests.reboot.greeter_pb2_grpc.GreeterMethodsStub(
                    self.channel_manager.get_channel_to(
                        self.placement_client.address_for_actor(
                            headers.application_id,
                            headers.state_ref,
                        )
                    )
                )
                call = stub.ConstructAndStoreRecursiveMessage(
                    request=request,
                    metadata=headers.to_grpc_metadata(),
                )
                try:
                    return await call
                except IMPORT_grpc.aio.AioRpcError as error:
                    if error.code() == IMPORT_grpc.StatusCode.UNAVAILABLE:
                        await call_backoff()
                        continue

                    # Reconstitute the error that the server threw, if it was a declared error.
                    status = await IMPORT_rpc_status_async.from_call(call)
                    if status is not None:
                        raise Greeter.ConstructAndStoreRecursiveMessageAborted.from_status(
                            status
                        ) from None
                    raise Greeter.ConstructAndStoreRecursiveMessageAborted.from_grpc_aio_rpc_error(
                        error
                     ) from None

        else:
            logger.warning(
                "Got a react mutate request with an invalid method name: "
                f"Method '{method}' is invalid for servicer Greeter."
                "\n"
                "Do you have an old browser tab still open for an older version "
                "of this application, or a different application all together?"
            )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.InvalidMethod(),
                message=
                    f"Method '{method}' not found"
            )

    async def dispatch(
        self,
        task: IMPORT_reboot_aio_tasks.TaskEffect,
        *,
        only_validate: bool = False,
        on_loop_iteration: IMPORT_reboot_aio_internals_tasks_dispatcher.OnLoopIterationCallable = (lambda iteration, next_iteration_schedule: None),
    ) -> IMPORT_reboot_aio_internals_tasks_dispatcher.TaskResponseOrStatus:
        """Dispatches the tasks to execute unless 'only_validate' is set to
        true, in which case just ensures that the task actually exists.
        Note that this function will be called *by* tasks_dispatcher; it will
        not itself call into tasks_dispatcher."""

        if 'Create' == task.method_name:
            if only_validate:
                # TODO(benh): validate 'task.request' is correct type.
                return (tests.reboot.greeter_pb2.CreateResponse(), None)

            # Use an inline method to create a new scope, so that we can use
            # variable names like `context` and `effects` in multiple branches
            # in this code (notably when there are multiple task types) without
            # hitting a mypy error that the variable's type is not consistent.
            async def run_Create(
                context: IMPORT_reboot_aio_contexts.WorkflowContext,
                *,
                validating_effects: bool = False,
            ):
                async with self._state_manager.task_workflow(
                    context,
                    task,
                    on_loop_iteration=on_loop_iteration,
                    validating_effects=validating_effects,
                ) as complete:
                    try:
                        response = await (GreeterWorkflowStub(
                            context=context,
                            state_ref=context._state_ref,
                        ).Create(
                            GreeterCreateRequestFromProto(IMPORT_typing.cast(tests.reboot.greeter_pb2.CreateRequest, task.request)),
                            bearer_token=__internal_magic_token__,
                            idempotency=IMPORT_reboot_aio_idempotency.Idempotency(
                                alias=f'Task {IMPORT_uuid.UUID(bytes=task.task_id.task_uuid)}',
                            ),
                        ))
                        await complete(task, (response, None))
                        return (response, None)
                    except IMPORT_asyncio.CancelledError:
                        # Do not retry a task if it was cancelled by a caller.
                        if self.tasks_dispatcher.is_task_cancelled(task.task_id.task_uuid):
                            result = (
                                None,
                                IMPORT_reboot.aio.aborted.SystemAborted(
                                    IMPORT_rbt_v1alpha1.errors_pb2.Cancelled(),
                                ).to_status(),
                            )
                            await complete(task, result)
                            return result
                        else:
                            raise
                    except IMPORT_reboot.aio.aborted.Aborted as aborted:
                        error_type = f'{aborted._protobuf_error.__class__.__module__}.{aborted._protobuf_error.__class__.__qualname__}'
                        # Do not retry a task if the error was specified in the
                        # proto file.
                        if error_type in self._specified_errors_by_service_method_name.get('tests.reboot.GreeterMethods.Create', []):
                            result = (None, aborted.to_status())
                            await complete(task, result)
                            return result
                        raise


            return await run_Create(
                self.create_context(
                    headers=IMPORT_reboot_aio_headers.Headers(
                        application_id=self.application_id,
                        state_ref=IMPORT_reboot_aio_types.StateRef(task.task_id.state_ref),
                    ),
                    state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                    method='Create',
                    context_type=IMPORT_reboot_aio_contexts.WorkflowContext,
                    task=task,
                    # Propagate state manager and state type so that
                    # `until()` calls (via `WorkflowContext.retry_reactively_until()`)
                    # can enter `reactively()` scoped to each `until()`
                    # invocation.
                    reactively_state_manager=self._state_manager,
                    reactively_state_type=(
                        self._servicer.__state_type__
                    ),
                )
            )
        elif 'Greet' == task.method_name:
            if only_validate:
                # TODO(benh): validate 'task.request' is correct type.
                return (tests.reboot.greeter_pb2.GreetResponse(), None)

            # Use an inline method to create a new scope, so that we can use
            # variable names like `context` and `effects` in multiple branches
            # in this code (notably when there are multiple task types) without
            # hitting a mypy error that the variable's type is not consistent.
            async def run_Greet(
                context: IMPORT_reboot_aio_contexts.WorkflowContext,
                *,
                validating_effects: bool = False,
            ):
                async with self._state_manager.task_workflow(
                    context,
                    task,
                    on_loop_iteration=on_loop_iteration,
                    validating_effects=validating_effects,
                ) as complete:
                    try:
                        response = await (GreeterWorkflowStub(
                            context=context,
                            state_ref=context._state_ref,
                        ).Greet(
                            GreeterGreetRequestFromProto(IMPORT_typing.cast(tests.reboot.greeter_pb2.GreetRequest, task.request)),
                            bearer_token=__internal_magic_token__,
                        ))
                        await complete(task, (response, None))
                        return (response, None)
                    except IMPORT_asyncio.CancelledError:
                        # Do not retry a task if it was cancelled by a caller.
                        if self.tasks_dispatcher.is_task_cancelled(task.task_id.task_uuid):
                            result = (
                                None,
                                IMPORT_reboot.aio.aborted.SystemAborted(
                                    IMPORT_rbt_v1alpha1.errors_pb2.Cancelled(),
                                ).to_status(),
                            )
                            await complete(task, result)
                            return result
                        else:
                            raise
                    except IMPORT_reboot.aio.aborted.Aborted as aborted:
                        error_type = f'{aborted._protobuf_error.__class__.__module__}.{aborted._protobuf_error.__class__.__qualname__}'
                        # Do not retry a task if the error was specified in the
                        # proto file.
                        if error_type in self._specified_errors_by_service_method_name.get('tests.reboot.GreeterMethods.Greet', []):
                            result = (None, aborted.to_status())
                            await complete(task, result)
                            return result
                        raise


            return await run_Greet(
                self.create_context(
                    headers=IMPORT_reboot_aio_headers.Headers(
                        application_id=self.application_id,
                        state_ref=IMPORT_reboot_aio_types.StateRef(task.task_id.state_ref),
                    ),
                    state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                    method='Greet',
                    context_type=IMPORT_reboot_aio_contexts.WorkflowContext,
                    task=task,
                    # Propagate state manager and state type so that
                    # `until()` calls (via `WorkflowContext.retry_reactively_until()`)
                    # can enter `reactively()` scoped to each `until()`
                    # invocation.
                    reactively_state_manager=self._state_manager,
                    reactively_state_type=(
                        self._servicer.__state_type__
                    ),
                )
            )
        elif 'SetAdjective' == task.method_name:
            if only_validate:
                # TODO(benh): validate 'task.request' is correct type.
                return (tests.reboot.greeter_pb2.SetAdjectiveResponse(), None)

            # Use an inline method to create a new scope, so that we can use
            # variable names like `context` and `effects` in multiple branches
            # in this code (notably when there are multiple task types) without
            # hitting a mypy error that the variable's type is not consistent.
            async def run_SetAdjective(
                context: IMPORT_reboot_aio_contexts.WorkflowContext,
                *,
                validating_effects: bool = False,
            ):
                async with self._state_manager.task_workflow(
                    context,
                    task,
                    on_loop_iteration=on_loop_iteration,
                    validating_effects=validating_effects,
                ) as complete:
                    try:
                        response = await (GreeterWorkflowStub(
                            context=context,
                            state_ref=context._state_ref,
                        ).SetAdjective(
                            GreeterSetAdjectiveRequestFromProto(IMPORT_typing.cast(tests.reboot.greeter_pb2.SetAdjectiveRequest, task.request)),
                            bearer_token=__internal_magic_token__,
                            idempotency=IMPORT_reboot_aio_idempotency.Idempotency(
                                alias=f'Task {IMPORT_uuid.UUID(bytes=task.task_id.task_uuid)}',
                            ),
                        ))
                        await complete(task, (response, None))
                        return (response, None)
                    except IMPORT_asyncio.CancelledError:
                        # Do not retry a task if it was cancelled by a caller.
                        if self.tasks_dispatcher.is_task_cancelled(task.task_id.task_uuid):
                            result = (
                                None,
                                IMPORT_reboot.aio.aborted.SystemAborted(
                                    IMPORT_rbt_v1alpha1.errors_pb2.Cancelled(),
                                ).to_status(),
                            )
                            await complete(task, result)
                            return result
                        else:
                            raise
                    except IMPORT_reboot.aio.aborted.Aborted as aborted:
                        error_type = f'{aborted._protobuf_error.__class__.__module__}.{aborted._protobuf_error.__class__.__qualname__}'
                        # Do not retry a task if the error was specified in the
                        # proto file.
                        if error_type in self._specified_errors_by_service_method_name.get('tests.reboot.GreeterMethods.SetAdjective', []):
                            result = (None, aborted.to_status())
                            await complete(task, result)
                            return result
                        raise


            return await run_SetAdjective(
                self.create_context(
                    headers=IMPORT_reboot_aio_headers.Headers(
                        application_id=self.application_id,
                        state_ref=IMPORT_reboot_aio_types.StateRef(task.task_id.state_ref),
                    ),
                    state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                    method='SetAdjective',
                    context_type=IMPORT_reboot_aio_contexts.WorkflowContext,
                    task=task,
                    # Propagate state manager and state type so that
                    # `until()` calls (via `WorkflowContext.retry_reactively_until()`)
                    # can enter `reactively()` scoped to each `until()`
                    # invocation.
                    reactively_state_manager=self._state_manager,
                    reactively_state_type=(
                        self._servicer.__state_type__
                    ),
                )
            )
        elif 'TransactionSetAdjective' == task.method_name:
            if only_validate:
                # TODO(benh): validate 'task.request' is correct type.
                return (tests.reboot.greeter_pb2.SetAdjectiveResponse(), None)

            # Use an inline method to create a new scope, so that we can use
            # variable names like `context` and `effects` in multiple branches
            # in this code (notably when there are multiple task types) without
            # hitting a mypy error that the variable's type is not consistent.
            async def run_TransactionSetAdjective(
                context: IMPORT_reboot_aio_contexts.WorkflowContext,
                *,
                validating_effects: bool = False,
            ):
                async with self._state_manager.task_workflow(
                    context,
                    task,
                    on_loop_iteration=on_loop_iteration,
                    validating_effects=validating_effects,
                ) as complete:
                    try:
                        response = await (GreeterWorkflowStub(
                            context=context,
                            state_ref=context._state_ref,
                        ).TransactionSetAdjective(
                            GreeterTransactionSetAdjectiveRequestFromProto(IMPORT_typing.cast(tests.reboot.greeter_pb2.SetAdjectiveRequest, task.request)),
                            bearer_token=__internal_magic_token__,
                            idempotency=IMPORT_reboot_aio_idempotency.Idempotency(
                                alias=f'Task {IMPORT_uuid.UUID(bytes=task.task_id.task_uuid)}',
                            ),
                        ))
                        await complete(task, (response, None))
                        return (response, None)
                    except IMPORT_asyncio.CancelledError:
                        # Do not retry a task if it was cancelled by a caller.
                        if self.tasks_dispatcher.is_task_cancelled(task.task_id.task_uuid):
                            result = (
                                None,
                                IMPORT_reboot.aio.aborted.SystemAborted(
                                    IMPORT_rbt_v1alpha1.errors_pb2.Cancelled(),
                                ).to_status(),
                            )
                            await complete(task, result)
                            return result
                        else:
                            raise
                    except IMPORT_reboot.aio.aborted.Aborted as aborted:
                        error_type = f'{aborted._protobuf_error.__class__.__module__}.{aborted._protobuf_error.__class__.__qualname__}'
                        # Do not retry a task if the error was specified in the
                        # proto file.
                        if error_type in self._specified_errors_by_service_method_name.get('tests.reboot.GreeterMethods.TransactionSetAdjective', []):
                            result = (None, aborted.to_status())
                            await complete(task, result)
                            return result
                        raise


            return await run_TransactionSetAdjective(
                self.create_context(
                    headers=IMPORT_reboot_aio_headers.Headers(
                        application_id=self.application_id,
                        state_ref=IMPORT_reboot_aio_types.StateRef(task.task_id.state_ref),
                    ),
                    state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                    method='TransactionSetAdjective',
                    context_type=IMPORT_reboot_aio_contexts.WorkflowContext,
                    task=task,
                    # Propagate state manager and state type so that
                    # `until()` calls (via `WorkflowContext.retry_reactively_until()`)
                    # can enter `reactively()` scoped to each `until()`
                    # invocation.
                    reactively_state_manager=self._state_manager,
                    reactively_state_type=(
                        self._servicer.__state_type__
                    ),
                )
            )
        elif 'TryToConstructContext' == task.method_name:
            if only_validate:
                # TODO(benh): validate 'task.request' is correct type.
                return (google.protobuf.empty_pb2.Empty(), None)

            # Use an inline method to create a new scope, so that we can use
            # variable names like `context` and `effects` in multiple branches
            # in this code (notably when there are multiple task types) without
            # hitting a mypy error that the variable's type is not consistent.
            async def run_TryToConstructContext(
                context: IMPORT_reboot_aio_contexts.WorkflowContext,
                *,
                validating_effects: bool = False,
            ):
                async with self._state_manager.task_workflow(
                    context,
                    task,
                    on_loop_iteration=on_loop_iteration,
                    validating_effects=validating_effects,
                ) as complete:
                    try:
                        response = await (GreeterWorkflowStub(
                            context=context,
                            state_ref=context._state_ref,
                        ).TryToConstructContext(
                            GreeterTryToConstructContextRequestFromProto(IMPORT_typing.cast(google.protobuf.empty_pb2.Empty, task.request)),
                            bearer_token=__internal_magic_token__,
                        ))
                        await complete(task, (response, None))
                        return (response, None)
                    except IMPORT_asyncio.CancelledError:
                        # Do not retry a task if it was cancelled by a caller.
                        if self.tasks_dispatcher.is_task_cancelled(task.task_id.task_uuid):
                            result = (
                                None,
                                IMPORT_reboot.aio.aborted.SystemAborted(
                                    IMPORT_rbt_v1alpha1.errors_pb2.Cancelled(),
                                ).to_status(),
                            )
                            await complete(task, result)
                            return result
                        else:
                            raise
                    except IMPORT_reboot.aio.aborted.Aborted as aborted:
                        error_type = f'{aborted._protobuf_error.__class__.__module__}.{aborted._protobuf_error.__class__.__qualname__}'
                        # Do not retry a task if the error was specified in the
                        # proto file.
                        if error_type in self._specified_errors_by_service_method_name.get('tests.reboot.GreeterMethods.TryToConstructContext', []):
                            result = (None, aborted.to_status())
                            await complete(task, result)
                            return result
                        raise


            return await run_TryToConstructContext(
                self.create_context(
                    headers=IMPORT_reboot_aio_headers.Headers(
                        application_id=self.application_id,
                        state_ref=IMPORT_reboot_aio_types.StateRef(task.task_id.state_ref),
                    ),
                    state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                    method='TryToConstructContext',
                    context_type=IMPORT_reboot_aio_contexts.WorkflowContext,
                    task=task,
                    # Propagate state manager and state type so that
                    # `until()` calls (via `WorkflowContext.retry_reactively_until()`)
                    # can enter `reactively()` scoped to each `until()`
                    # invocation.
                    reactively_state_manager=self._state_manager,
                    reactively_state_type=(
                        self._servicer.__state_type__
                    ),
                )
            )
        elif 'TryToConstructExternalContext' == task.method_name:
            if only_validate:
                # TODO(benh): validate 'task.request' is correct type.
                return (google.protobuf.empty_pb2.Empty(), None)

            # Use an inline method to create a new scope, so that we can use
            # variable names like `context` and `effects` in multiple branches
            # in this code (notably when there are multiple task types) without
            # hitting a mypy error that the variable's type is not consistent.
            async def run_TryToConstructExternalContext(
                context: IMPORT_reboot_aio_contexts.WorkflowContext,
                *,
                validating_effects: bool = False,
            ):
                async with self._state_manager.task_workflow(
                    context,
                    task,
                    on_loop_iteration=on_loop_iteration,
                    validating_effects=validating_effects,
                ) as complete:
                    try:
                        response = await (GreeterWorkflowStub(
                            context=context,
                            state_ref=context._state_ref,
                        ).TryToConstructExternalContext(
                            GreeterTryToConstructExternalContextRequestFromProto(IMPORT_typing.cast(google.protobuf.empty_pb2.Empty, task.request)),
                            bearer_token=__internal_magic_token__,
                        ))
                        await complete(task, (response, None))
                        return (response, None)
                    except IMPORT_asyncio.CancelledError:
                        # Do not retry a task if it was cancelled by a caller.
                        if self.tasks_dispatcher.is_task_cancelled(task.task_id.task_uuid):
                            result = (
                                None,
                                IMPORT_reboot.aio.aborted.SystemAborted(
                                    IMPORT_rbt_v1alpha1.errors_pb2.Cancelled(),
                                ).to_status(),
                            )
                            await complete(task, result)
                            return result
                        else:
                            raise
                    except IMPORT_reboot.aio.aborted.Aborted as aborted:
                        error_type = f'{aborted._protobuf_error.__class__.__module__}.{aborted._protobuf_error.__class__.__qualname__}'
                        # Do not retry a task if the error was specified in the
                        # proto file.
                        if error_type in self._specified_errors_by_service_method_name.get('tests.reboot.GreeterMethods.TryToConstructExternalContext', []):
                            result = (None, aborted.to_status())
                            await complete(task, result)
                            return result
                        raise


            return await run_TryToConstructExternalContext(
                self.create_context(
                    headers=IMPORT_reboot_aio_headers.Headers(
                        application_id=self.application_id,
                        state_ref=IMPORT_reboot_aio_types.StateRef(task.task_id.state_ref),
                    ),
                    state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                    method='TryToConstructExternalContext',
                    context_type=IMPORT_reboot_aio_contexts.WorkflowContext,
                    task=task,
                    # Propagate state manager and state type so that
                    # `until()` calls (via `WorkflowContext.retry_reactively_until()`)
                    # can enter `reactively()` scoped to each `until()`
                    # invocation.
                    reactively_state_manager=self._state_manager,
                    reactively_state_type=(
                        self._servicer.__state_type__
                    ),
                )
            )
        elif 'TestLongRunningFetch' == task.method_name:
            if only_validate:
                # TODO(benh): validate 'task.request' is correct type.
                return (google.protobuf.empty_pb2.Empty(), None)

            # Use an inline method to create a new scope, so that we can use
            # variable names like `context` and `effects` in multiple branches
            # in this code (notably when there are multiple task types) without
            # hitting a mypy error that the variable's type is not consistent.
            async def run_TestLongRunningFetch(
                context: IMPORT_reboot_aio_contexts.WorkflowContext,
                *,
                validating_effects: bool = False,
            ):
                async with self._state_manager.task_workflow(
                    context,
                    task,
                    on_loop_iteration=on_loop_iteration,
                    validating_effects=validating_effects,
                ) as complete:
                    try:
                        response = await (GreeterWorkflowStub(
                            context=context,
                            state_ref=context._state_ref,
                        ).TestLongRunningFetch(
                            GreeterTestLongRunningFetchRequestFromProto(IMPORT_typing.cast(tests.reboot.greeter_pb2.TestLongRunningFetchRequest, task.request)),
                            bearer_token=__internal_magic_token__,
                        ))
                        await complete(task, (response, None))
                        return (response, None)
                    except IMPORT_asyncio.CancelledError:
                        # Do not retry a task if it was cancelled by a caller.
                        if self.tasks_dispatcher.is_task_cancelled(task.task_id.task_uuid):
                            result = (
                                None,
                                IMPORT_reboot.aio.aborted.SystemAborted(
                                    IMPORT_rbt_v1alpha1.errors_pb2.Cancelled(),
                                ).to_status(),
                            )
                            await complete(task, result)
                            return result
                        else:
                            raise
                    except IMPORT_reboot.aio.aborted.Aborted as aborted:
                        error_type = f'{aborted._protobuf_error.__class__.__module__}.{aborted._protobuf_error.__class__.__qualname__}'
                        # Do not retry a task if the error was specified in the
                        # proto file.
                        if error_type in self._specified_errors_by_service_method_name.get('tests.reboot.GreeterMethods.TestLongRunningFetch', []):
                            result = (None, aborted.to_status())
                            await complete(task, result)
                            return result
                        raise


            return await run_TestLongRunningFetch(
                self.create_context(
                    headers=IMPORT_reboot_aio_headers.Headers(
                        application_id=self.application_id,
                        state_ref=IMPORT_reboot_aio_types.StateRef(task.task_id.state_ref),
                    ),
                    state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                    method='TestLongRunningFetch',
                    context_type=IMPORT_reboot_aio_contexts.WorkflowContext,
                    task=task,
                    # Propagate state manager and state type so that
                    # `until()` calls (via `WorkflowContext.retry_reactively_until()`)
                    # can enter `reactively()` scoped to each `until()`
                    # invocation.
                    reactively_state_manager=self._state_manager,
                    reactively_state_type=(
                        self._servicer.__state_type__
                    ),
                )
            )
        elif 'TestLongRunningWriter' == task.method_name:
            if only_validate:
                # TODO(benh): validate 'task.request' is correct type.
                return (google.protobuf.empty_pb2.Empty(), None)

            # Use an inline method to create a new scope, so that we can use
            # variable names like `context` and `effects` in multiple branches
            # in this code (notably when there are multiple task types) without
            # hitting a mypy error that the variable's type is not consistent.
            async def run_TestLongRunningWriter(
                context: IMPORT_reboot_aio_contexts.WorkflowContext,
                *,
                validating_effects: bool = False,
            ):
                async with self._state_manager.task_workflow(
                    context,
                    task,
                    on_loop_iteration=on_loop_iteration,
                    validating_effects=validating_effects,
                ) as complete:
                    try:
                        response = await (GreeterWorkflowStub(
                            context=context,
                            state_ref=context._state_ref,
                        ).TestLongRunningWriter(
                            GreeterTestLongRunningWriterRequestFromProto(IMPORT_typing.cast(google.protobuf.empty_pb2.Empty, task.request)),
                            bearer_token=__internal_magic_token__,
                            idempotency=IMPORT_reboot_aio_idempotency.Idempotency(
                                alias=f'Task {IMPORT_uuid.UUID(bytes=task.task_id.task_uuid)}',
                            ),
                        ))
                        await complete(task, (response, None))
                        return (response, None)
                    except IMPORT_asyncio.CancelledError:
                        # Do not retry a task if it was cancelled by a caller.
                        if self.tasks_dispatcher.is_task_cancelled(task.task_id.task_uuid):
                            result = (
                                None,
                                IMPORT_reboot.aio.aborted.SystemAborted(
                                    IMPORT_rbt_v1alpha1.errors_pb2.Cancelled(),
                                ).to_status(),
                            )
                            await complete(task, result)
                            return result
                        else:
                            raise
                    except IMPORT_reboot.aio.aborted.Aborted as aborted:
                        error_type = f'{aborted._protobuf_error.__class__.__module__}.{aborted._protobuf_error.__class__.__qualname__}'
                        # Do not retry a task if the error was specified in the
                        # proto file.
                        if error_type in self._specified_errors_by_service_method_name.get('tests.reboot.GreeterMethods.TestLongRunningWriter', []):
                            result = (None, aborted.to_status())
                            await complete(task, result)
                            return result
                        raise


            return await run_TestLongRunningWriter(
                self.create_context(
                    headers=IMPORT_reboot_aio_headers.Headers(
                        application_id=self.application_id,
                        state_ref=IMPORT_reboot_aio_types.StateRef(task.task_id.state_ref),
                    ),
                    state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                    method='TestLongRunningWriter',
                    context_type=IMPORT_reboot_aio_contexts.WorkflowContext,
                    task=task,
                    # Propagate state manager and state type so that
                    # `until()` calls (via `WorkflowContext.retry_reactively_until()`)
                    # can enter `reactively()` scoped to each `until()`
                    # invocation.
                    reactively_state_manager=self._state_manager,
                    reactively_state_type=(
                        self._servicer.__state_type__
                    ),
                )
            )
        elif 'GetWholeState' == task.method_name:
            if only_validate:
                # TODO(benh): validate 'task.request' is correct type.
                return (tests.reboot.greeter_pb2.Greeter(), None)

            # Use an inline method to create a new scope, so that we can use
            # variable names like `context` and `effects` in multiple branches
            # in this code (notably when there are multiple task types) without
            # hitting a mypy error that the variable's type is not consistent.
            async def run_GetWholeState(
                context: IMPORT_reboot_aio_contexts.WorkflowContext,
                *,
                validating_effects: bool = False,
            ):
                async with self._state_manager.task_workflow(
                    context,
                    task,
                    on_loop_iteration=on_loop_iteration,
                    validating_effects=validating_effects,
                ) as complete:
                    try:
                        response = await (GreeterWorkflowStub(
                            context=context,
                            state_ref=context._state_ref,
                        ).GetWholeState(
                            GreeterGetWholeStateRequestFromProto(IMPORT_typing.cast(tests.reboot.greeter_pb2.GetWholeStateRequest, task.request)),
                            bearer_token=__internal_magic_token__,
                        ))
                        await complete(task, (response, None))
                        return (response, None)
                    except IMPORT_asyncio.CancelledError:
                        # Do not retry a task if it was cancelled by a caller.
                        if self.tasks_dispatcher.is_task_cancelled(task.task_id.task_uuid):
                            result = (
                                None,
                                IMPORT_reboot.aio.aborted.SystemAborted(
                                    IMPORT_rbt_v1alpha1.errors_pb2.Cancelled(),
                                ).to_status(),
                            )
                            await complete(task, result)
                            return result
                        else:
                            raise
                    except IMPORT_reboot.aio.aborted.Aborted as aborted:
                        error_type = f'{aborted._protobuf_error.__class__.__module__}.{aborted._protobuf_error.__class__.__qualname__}'
                        # Do not retry a task if the error was specified in the
                        # proto file.
                        if error_type in self._specified_errors_by_service_method_name.get('tests.reboot.GreeterMethods.GetWholeState', []):
                            result = (None, aborted.to_status())
                            await complete(task, result)
                            return result
                        raise


            return await run_GetWholeState(
                self.create_context(
                    headers=IMPORT_reboot_aio_headers.Headers(
                        application_id=self.application_id,
                        state_ref=IMPORT_reboot_aio_types.StateRef(task.task_id.state_ref),
                    ),
                    state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                    method='GetWholeState',
                    context_type=IMPORT_reboot_aio_contexts.WorkflowContext,
                    task=task,
                    # Propagate state manager and state type so that
                    # `until()` calls (via `WorkflowContext.retry_reactively_until()`)
                    # can enter `reactively()` scoped to each `until()`
                    # invocation.
                    reactively_state_manager=self._state_manager,
                    reactively_state_type=(
                        self._servicer.__state_type__
                    ),
                )
            )
        elif 'FailWithException' == task.method_name:
            if only_validate:
                # TODO(benh): validate 'task.request' is correct type.
                return (google.protobuf.empty_pb2.Empty(), None)

            # Use an inline method to create a new scope, so that we can use
            # variable names like `context` and `effects` in multiple branches
            # in this code (notably when there are multiple task types) without
            # hitting a mypy error that the variable's type is not consistent.
            async def run_FailWithException(
                context: IMPORT_reboot_aio_contexts.WorkflowContext,
                *,
                validating_effects: bool = False,
            ):
                async with self._state_manager.task_workflow(
                    context,
                    task,
                    on_loop_iteration=on_loop_iteration,
                    validating_effects=validating_effects,
                ) as complete:
                    try:
                        response = await (GreeterWorkflowStub(
                            context=context,
                            state_ref=context._state_ref,
                        ).FailWithException(
                            GreeterFailWithExceptionRequestFromProto(IMPORT_typing.cast(google.protobuf.empty_pb2.Empty, task.request)),
                            bearer_token=__internal_magic_token__,
                        ))
                        await complete(task, (response, None))
                        return (response, None)
                    except IMPORT_asyncio.CancelledError:
                        # Do not retry a task if it was cancelled by a caller.
                        if self.tasks_dispatcher.is_task_cancelled(task.task_id.task_uuid):
                            result = (
                                None,
                                IMPORT_reboot.aio.aborted.SystemAborted(
                                    IMPORT_rbt_v1alpha1.errors_pb2.Cancelled(),
                                ).to_status(),
                            )
                            await complete(task, result)
                            return result
                        else:
                            raise
                    except IMPORT_reboot.aio.aborted.Aborted as aborted:
                        error_type = f'{aborted._protobuf_error.__class__.__module__}.{aborted._protobuf_error.__class__.__qualname__}'
                        # Do not retry a task if the error was specified in the
                        # proto file.
                        if error_type in self._specified_errors_by_service_method_name.get('tests.reboot.GreeterMethods.FailWithException', []):
                            result = (None, aborted.to_status())
                            await complete(task, result)
                            return result
                        raise


            return await run_FailWithException(
                self.create_context(
                    headers=IMPORT_reboot_aio_headers.Headers(
                        application_id=self.application_id,
                        state_ref=IMPORT_reboot_aio_types.StateRef(task.task_id.state_ref),
                    ),
                    state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                    method='FailWithException',
                    context_type=IMPORT_reboot_aio_contexts.WorkflowContext,
                    task=task,
                    # Propagate state manager and state type so that
                    # `until()` calls (via `WorkflowContext.retry_reactively_until()`)
                    # can enter `reactively()` scoped to each `until()`
                    # invocation.
                    reactively_state_manager=self._state_manager,
                    reactively_state_type=(
                        self._servicer.__state_type__
                    ),
                )
            )
        elif 'FailWithAborted' == task.method_name:
            if only_validate:
                # TODO(benh): validate 'task.request' is correct type.
                return (google.protobuf.empty_pb2.Empty(), None)

            # Use an inline method to create a new scope, so that we can use
            # variable names like `context` and `effects` in multiple branches
            # in this code (notably when there are multiple task types) without
            # hitting a mypy error that the variable's type is not consistent.
            async def run_FailWithAborted(
                context: IMPORT_reboot_aio_contexts.WorkflowContext,
                *,
                validating_effects: bool = False,
            ):
                async with self._state_manager.task_workflow(
                    context,
                    task,
                    on_loop_iteration=on_loop_iteration,
                    validating_effects=validating_effects,
                ) as complete:
                    try:
                        response = await (GreeterWorkflowStub(
                            context=context,
                            state_ref=context._state_ref,
                        ).FailWithAborted(
                            GreeterFailWithAbortedRequestFromProto(IMPORT_typing.cast(google.protobuf.empty_pb2.Empty, task.request)),
                            bearer_token=__internal_magic_token__,
                        ))
                        await complete(task, (response, None))
                        return (response, None)
                    except IMPORT_asyncio.CancelledError:
                        # Do not retry a task if it was cancelled by a caller.
                        if self.tasks_dispatcher.is_task_cancelled(task.task_id.task_uuid):
                            result = (
                                None,
                                IMPORT_reboot.aio.aborted.SystemAborted(
                                    IMPORT_rbt_v1alpha1.errors_pb2.Cancelled(),
                                ).to_status(),
                            )
                            await complete(task, result)
                            return result
                        else:
                            raise
                    except IMPORT_reboot.aio.aborted.Aborted as aborted:
                        error_type = f'{aborted._protobuf_error.__class__.__module__}.{aborted._protobuf_error.__class__.__qualname__}'
                        # Do not retry a task if the error was specified in the
                        # proto file.
                        if error_type in self._specified_errors_by_service_method_name.get('tests.reboot.GreeterMethods.FailWithAborted', []):
                            result = (None, aborted.to_status())
                            await complete(task, result)
                            return result
                        raise


            return await run_FailWithAborted(
                self.create_context(
                    headers=IMPORT_reboot_aio_headers.Headers(
                        application_id=self.application_id,
                        state_ref=IMPORT_reboot_aio_types.StateRef(task.task_id.state_ref),
                    ),
                    state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                    method='FailWithAborted',
                    context_type=IMPORT_reboot_aio_contexts.WorkflowContext,
                    task=task,
                    # Propagate state manager and state type so that
                    # `until()` calls (via `WorkflowContext.retry_reactively_until()`)
                    # can enter `reactively()` scoped to each `until()`
                    # invocation.
                    reactively_state_manager=self._state_manager,
                    reactively_state_type=(
                        self._servicer.__state_type__
                    ),
                )
            )
        elif 'Workflow' == task.method_name:
            if only_validate:
                # TODO(benh): validate 'task.request' is correct type.
                return (tests.reboot.greeter_pb2.WorkflowResponse(), None)

            # Use an inline method to create a new scope, so that we can use
            # variable names like `context` and `effects` in multiple branches
            # in this code (notably when there are multiple task types) without
            # hitting a mypy error that the variable's type is not consistent.
            async def run_Workflow(
                context: IMPORT_reboot_aio_contexts.WorkflowContext,
                *,
                validating_effects: bool = False,
            ):
                async with self._state_manager.task_workflow(
                    context,
                    task,
                    on_loop_iteration=on_loop_iteration,
                    validating_effects=validating_effects,
                ) as complete:
                    try:
                        response = await self.__Workflow(
                            context,
                            IMPORT_typing.cast(google.protobuf.empty_pb2.Empty, task.request),
                            validating_effects=validating_effects,
                        )
                        await complete(task, (response, None))
                        return (response, None)
                    except IMPORT_asyncio.CancelledError:
                        # Do not retry a task if it was cancelled by a caller.
                        if self.tasks_dispatcher.is_task_cancelled(task.task_id.task_uuid):
                            result = (
                                None,
                                IMPORT_reboot.aio.aborted.SystemAborted(
                                    IMPORT_rbt_v1alpha1.errors_pb2.Cancelled(),
                                ).to_status(),
                            )
                            await complete(task, result)
                            return result
                        else:
                            raise
                    except IMPORT_reboot.aio.aborted.Aborted as aborted:
                        error_type = f'{aborted._protobuf_error.__class__.__module__}.{aborted._protobuf_error.__class__.__qualname__}'
                        # Do not retry a task if the error was specified in the
                        # proto file.
                        if error_type in self._specified_errors_by_service_method_name.get('tests.reboot.GreeterMethods.Workflow', []):
                            result = (None, aborted.to_status())
                            await complete(task, result)
                            return result
                        raise

            @IMPORT_reboot_aio_internals_middleware.maybe_run_function_twice_to_validate_effects
            async def run_Workflow_workflow(
                validating_effects: bool,
                context: IMPORT_reboot_aio_contexts.WorkflowContext,
            ):
                try:
                    # When we're validating effects we
                    # periodically timeout so that we can log
                    # that a workflow might be hung, i.e., the
                    # user has a bug.
                    task = IMPORT_asyncio.create_task(
                        run_Workflow(
                            context,
                            validating_effects=validating_effects,
                        )
                    )
                    timeout = None if not validating_effects else 5  # seconds
                    while True:
                        done, pending = await IMPORT_asyncio.wait(
                            [task],
                            timeout=timeout,
                        )
                        # Check if we've timed out, which
                        # should only occur if we're
                        # validating effects.
                        if len(done) == 0:
                            assert validating_effects and timeout is not None
                            logger.warning(
                                f'Still waiting for method Greeter.Workflow '
                                'to complete after re-running to validate effects.'
                            )
                            timeout += 5  # seconds
                            continue
                        return task.result()
                finally:
                    if not task.done():
                        task.cancel()
                        # Need to actually await the task so if
                        # there is an exception we don't get a
                        # warning logged that the exception was
                        # never retrieved, but we don't care about
                        # the exception because we're done with
                        # the task.
                        try:
                            await task
                        except:
                            pass

            return await run_Workflow_workflow(
                self.create_context(
                    headers=IMPORT_reboot_aio_headers.Headers(
                        application_id=self.application_id,
                        state_ref=IMPORT_reboot_aio_types.StateRef(task.task_id.state_ref),
                        workflow_id=IMPORT_uuid.UUID(bytes=task.task_id.task_uuid),
                    ),
                    state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                    method='Workflow',
                    context_type=IMPORT_reboot_aio_contexts.WorkflowContext,
                    task=task,
                    # Propagate state manager and state type so that
                    # `until()` calls (via `WorkflowContext.retry_reactively_until()`)
                    # can enter `reactively()` scoped to each `until()`
                    # invocation.
                    reactively_state_manager=self._state_manager,
                    reactively_state_type=(
                        self._servicer.__state_type__
                    ),
                )
            )
        elif 'DangerousFields' == task.method_name:
            if only_validate:
                # TODO(benh): validate 'task.request' is correct type.
                return (google.protobuf.empty_pb2.Empty(), None)

            # Use an inline method to create a new scope, so that we can use
            # variable names like `context` and `effects` in multiple branches
            # in this code (notably when there are multiple task types) without
            # hitting a mypy error that the variable's type is not consistent.
            async def run_DangerousFields(
                context: IMPORT_reboot_aio_contexts.WorkflowContext,
                *,
                validating_effects: bool = False,
            ):
                async with self._state_manager.task_workflow(
                    context,
                    task,
                    on_loop_iteration=on_loop_iteration,
                    validating_effects=validating_effects,
                ) as complete:
                    try:
                        response = await (GreeterWorkflowStub(
                            context=context,
                            state_ref=context._state_ref,
                        ).DangerousFields(
                            GreeterDangerousFieldsRequestFromProto(IMPORT_typing.cast(tests.reboot.greeter_pb2.DangerousFieldsRequest, task.request)),
                            bearer_token=__internal_magic_token__,
                            idempotency=IMPORT_reboot_aio_idempotency.Idempotency(
                                alias=f'Task {IMPORT_uuid.UUID(bytes=task.task_id.task_uuid)}',
                            ),
                        ))
                        await complete(task, (response, None))
                        return (response, None)
                    except IMPORT_asyncio.CancelledError:
                        # Do not retry a task if it was cancelled by a caller.
                        if self.tasks_dispatcher.is_task_cancelled(task.task_id.task_uuid):
                            result = (
                                None,
                                IMPORT_reboot.aio.aborted.SystemAborted(
                                    IMPORT_rbt_v1alpha1.errors_pb2.Cancelled(),
                                ).to_status(),
                            )
                            await complete(task, result)
                            return result
                        else:
                            raise
                    except IMPORT_reboot.aio.aborted.Aborted as aborted:
                        error_type = f'{aborted._protobuf_error.__class__.__module__}.{aborted._protobuf_error.__class__.__qualname__}'
                        # Do not retry a task if the error was specified in the
                        # proto file.
                        if error_type in self._specified_errors_by_service_method_name.get('tests.reboot.GreeterMethods.DangerousFields', []):
                            result = (None, aborted.to_status())
                            await complete(task, result)
                            return result
                        raise


            return await run_DangerousFields(
                self.create_context(
                    headers=IMPORT_reboot_aio_headers.Headers(
                        application_id=self.application_id,
                        state_ref=IMPORT_reboot_aio_types.StateRef(task.task_id.state_ref),
                    ),
                    state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                    method='DangerousFields',
                    context_type=IMPORT_reboot_aio_contexts.WorkflowContext,
                    task=task,
                    # Propagate state manager and state type so that
                    # `until()` calls (via `WorkflowContext.retry_reactively_until()`)
                    # can enter `reactively()` scoped to each `until()`
                    # invocation.
                    reactively_state_manager=self._state_manager,
                    reactively_state_type=(
                        self._servicer.__state_type__
                    ),
                )
            )
        elif 'StoreRecursiveMessage' == task.method_name:
            if only_validate:
                # TODO(benh): validate 'task.request' is correct type.
                return (tests.reboot.greeter_pb2.StoreRecursiveMessageResponse(), None)

            # Use an inline method to create a new scope, so that we can use
            # variable names like `context` and `effects` in multiple branches
            # in this code (notably when there are multiple task types) without
            # hitting a mypy error that the variable's type is not consistent.
            async def run_StoreRecursiveMessage(
                context: IMPORT_reboot_aio_contexts.WorkflowContext,
                *,
                validating_effects: bool = False,
            ):
                async with self._state_manager.task_workflow(
                    context,
                    task,
                    on_loop_iteration=on_loop_iteration,
                    validating_effects=validating_effects,
                ) as complete:
                    try:
                        response = await (GreeterWorkflowStub(
                            context=context,
                            state_ref=context._state_ref,
                        ).StoreRecursiveMessage(
                            GreeterStoreRecursiveMessageRequestFromProto(IMPORT_typing.cast(tests.reboot.greeter_pb2.StoreRecursiveMessageRequest, task.request)),
                            bearer_token=__internal_magic_token__,
                            idempotency=IMPORT_reboot_aio_idempotency.Idempotency(
                                alias=f'Task {IMPORT_uuid.UUID(bytes=task.task_id.task_uuid)}',
                            ),
                        ))
                        await complete(task, (response, None))
                        return (response, None)
                    except IMPORT_asyncio.CancelledError:
                        # Do not retry a task if it was cancelled by a caller.
                        if self.tasks_dispatcher.is_task_cancelled(task.task_id.task_uuid):
                            result = (
                                None,
                                IMPORT_reboot.aio.aborted.SystemAborted(
                                    IMPORT_rbt_v1alpha1.errors_pb2.Cancelled(),
                                ).to_status(),
                            )
                            await complete(task, result)
                            return result
                        else:
                            raise
                    except IMPORT_reboot.aio.aborted.Aborted as aborted:
                        error_type = f'{aborted._protobuf_error.__class__.__module__}.{aborted._protobuf_error.__class__.__qualname__}'
                        # Do not retry a task if the error was specified in the
                        # proto file.
                        if error_type in self._specified_errors_by_service_method_name.get('tests.reboot.GreeterMethods.StoreRecursiveMessage', []):
                            result = (None, aborted.to_status())
                            await complete(task, result)
                            return result
                        raise


            return await run_StoreRecursiveMessage(
                self.create_context(
                    headers=IMPORT_reboot_aio_headers.Headers(
                        application_id=self.application_id,
                        state_ref=IMPORT_reboot_aio_types.StateRef(task.task_id.state_ref),
                    ),
                    state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                    method='StoreRecursiveMessage',
                    context_type=IMPORT_reboot_aio_contexts.WorkflowContext,
                    task=task,
                    # Propagate state manager and state type so that
                    # `until()` calls (via `WorkflowContext.retry_reactively_until()`)
                    # can enter `reactively()` scoped to each `until()`
                    # invocation.
                    reactively_state_manager=self._state_manager,
                    reactively_state_type=(
                        self._servicer.__state_type__
                    ),
                )
            )
        elif 'ReadRecursiveMessage' == task.method_name:
            if only_validate:
                # TODO(benh): validate 'task.request' is correct type.
                return (tests.reboot.greeter_pb2.ReadRecursiveMessageResponse(), None)

            # Use an inline method to create a new scope, so that we can use
            # variable names like `context` and `effects` in multiple branches
            # in this code (notably when there are multiple task types) without
            # hitting a mypy error that the variable's type is not consistent.
            async def run_ReadRecursiveMessage(
                context: IMPORT_reboot_aio_contexts.WorkflowContext,
                *,
                validating_effects: bool = False,
            ):
                async with self._state_manager.task_workflow(
                    context,
                    task,
                    on_loop_iteration=on_loop_iteration,
                    validating_effects=validating_effects,
                ) as complete:
                    try:
                        response = await (GreeterWorkflowStub(
                            context=context,
                            state_ref=context._state_ref,
                        ).ReadRecursiveMessage(
                            GreeterReadRecursiveMessageRequestFromProto(IMPORT_typing.cast(tests.reboot.greeter_pb2.ReadRecursiveMessageRequest, task.request)),
                            bearer_token=__internal_magic_token__,
                        ))
                        await complete(task, (response, None))
                        return (response, None)
                    except IMPORT_asyncio.CancelledError:
                        # Do not retry a task if it was cancelled by a caller.
                        if self.tasks_dispatcher.is_task_cancelled(task.task_id.task_uuid):
                            result = (
                                None,
                                IMPORT_reboot.aio.aborted.SystemAborted(
                                    IMPORT_rbt_v1alpha1.errors_pb2.Cancelled(),
                                ).to_status(),
                            )
                            await complete(task, result)
                            return result
                        else:
                            raise
                    except IMPORT_reboot.aio.aborted.Aborted as aborted:
                        error_type = f'{aborted._protobuf_error.__class__.__module__}.{aborted._protobuf_error.__class__.__qualname__}'
                        # Do not retry a task if the error was specified in the
                        # proto file.
                        if error_type in self._specified_errors_by_service_method_name.get('tests.reboot.GreeterMethods.ReadRecursiveMessage', []):
                            result = (None, aborted.to_status())
                            await complete(task, result)
                            return result
                        raise


            return await run_ReadRecursiveMessage(
                self.create_context(
                    headers=IMPORT_reboot_aio_headers.Headers(
                        application_id=self.application_id,
                        state_ref=IMPORT_reboot_aio_types.StateRef(task.task_id.state_ref),
                    ),
                    state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                    method='ReadRecursiveMessage',
                    context_type=IMPORT_reboot_aio_contexts.WorkflowContext,
                    task=task,
                    # Propagate state manager and state type so that
                    # `until()` calls (via `WorkflowContext.retry_reactively_until()`)
                    # can enter `reactively()` scoped to each `until()`
                    # invocation.
                    reactively_state_manager=self._state_manager,
                    reactively_state_type=(
                        self._servicer.__state_type__
                    ),
                )
            )
        elif 'ConstructAndStoreRecursiveMessage' == task.method_name:
            if only_validate:
                # TODO(benh): validate 'task.request' is correct type.
                return (tests.reboot.greeter_pb2.ConstructAndStoreRecursiveMessageResponse(), None)

            # Use an inline method to create a new scope, so that we can use
            # variable names like `context` and `effects` in multiple branches
            # in this code (notably when there are multiple task types) without
            # hitting a mypy error that the variable's type is not consistent.
            async def run_ConstructAndStoreRecursiveMessage(
                context: IMPORT_reboot_aio_contexts.WorkflowContext,
                *,
                validating_effects: bool = False,
            ):
                async with self._state_manager.task_workflow(
                    context,
                    task,
                    on_loop_iteration=on_loop_iteration,
                    validating_effects=validating_effects,
                ) as complete:
                    try:
                        response = await (GreeterWorkflowStub(
                            context=context,
                            state_ref=context._state_ref,
                        ).ConstructAndStoreRecursiveMessage(
                            GreeterConstructAndStoreRecursiveMessageRequestFromProto(IMPORT_typing.cast(tests.reboot.greeter_pb2.ConstructAndStoreRecursiveMessageRequest, task.request)),
                            bearer_token=__internal_magic_token__,
                            idempotency=IMPORT_reboot_aio_idempotency.Idempotency(
                                alias=f'Task {IMPORT_uuid.UUID(bytes=task.task_id.task_uuid)}',
                            ),
                        ))
                        await complete(task, (response, None))
                        return (response, None)
                    except IMPORT_asyncio.CancelledError:
                        # Do not retry a task if it was cancelled by a caller.
                        if self.tasks_dispatcher.is_task_cancelled(task.task_id.task_uuid):
                            result = (
                                None,
                                IMPORT_reboot.aio.aborted.SystemAborted(
                                    IMPORT_rbt_v1alpha1.errors_pb2.Cancelled(),
                                ).to_status(),
                            )
                            await complete(task, result)
                            return result
                        else:
                            raise
                    except IMPORT_reboot.aio.aborted.Aborted as aborted:
                        error_type = f'{aborted._protobuf_error.__class__.__module__}.{aborted._protobuf_error.__class__.__qualname__}'
                        # Do not retry a task if the error was specified in the
                        # proto file.
                        if error_type in self._specified_errors_by_service_method_name.get('tests.reboot.GreeterMethods.ConstructAndStoreRecursiveMessage', []):
                            result = (None, aborted.to_status())
                            await complete(task, result)
                            return result
                        raise


            return await run_ConstructAndStoreRecursiveMessage(
                self.create_context(
                    headers=IMPORT_reboot_aio_headers.Headers(
                        application_id=self.application_id,
                        state_ref=IMPORT_reboot_aio_types.StateRef(task.task_id.state_ref),
                    ),
                    state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                    method='ConstructAndStoreRecursiveMessage',
                    context_type=IMPORT_reboot_aio_contexts.WorkflowContext,
                    task=task,
                    # Propagate state manager and state type so that
                    # `until()` calls (via `WorkflowContext.retry_reactively_until()`)
                    # can enter `reactively()` scoped to each `until()`
                    # invocation.
                    reactively_state_manager=self._state_manager,
                    reactively_state_type=(
                        self._servicer.__state_type__
                    ),
                )
            )

        # There are no tasks for this service.
        start_or_validate = "start" if not only_validate else "validate"
        raise RuntimeError(
            f"Attempted to {start_or_validate} task '{task.method_name}' "
            f"on 'Greeter' which does not exist"
        )

    # Greeter specific methods:
    async def __Create(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: tests.reboot.greeter_pb2.Greeter,
        request: tests.reboot.greeter_pb2.CreateRequest,
        *,
        validating_effects: bool,
    ) -> Greeter.CreateEffects:
        try:
            states_being_constructed.add(context.state_id)
            typed_state: Greeter.State = GreeterFromProto(state, is_initial_state=(context.state_id in states_being_constructed))

            response = (
                await self._servicer._Create(
                    context=context,
                    state=typed_state,
                    request=request
                )
            )


            GreeterToProto(typed_state, state)

            IMPORT_reboot_aio_types.assert_type(
                response,
                [tests.reboot.greeter_pb2.CreateResponse],
            )
            self.maybe_raise_effect_validation_retry(
                logger=logger,
                idempotency_manager=context,
                method_name='Greeter.Create',
                validating_effects=validating_effects,
                context=context,
            )
            return Greeter.CreateEffects(
                state=state,
                response=response,
                tasks=context._tasks,
                _colocated_upserts=context._colocated_upserts,
            )
        except IMPORT_reboot_aio_contexts.EffectValidationRetry:
            # Doing effect validation, just let this propagate.
            raise
        except IMPORT_reboot.aio.aborted.Aborted as aborted:
            # If the caller aborted due to a retryable error, just
            # propagate the aborted instead of propagating `Unknown`
            # so that a client can transparently retry.
            if IMPORT_reboot.aio.aborted.is_retryable(aborted):
                raise aborted
            # Log any _unhandled_ abort stack traces to make it
            # easier for debugging.
            #
            # NOTE: we don't log if we're a task as it will be logged
            # in `public/reboot/aio/internals/tasks_dispatcher.py` instead.
            aborted_type: IMPORT_typing.Optional[type] = None
            aborted_type = Greeter.CreateAborted
            if isinstance(aborted, IMPORT_reboot.aio.aborted.SystemAborted):
                # Not logging when within `node` as we already log there.
                if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                    logger.warning(
                        f"Unhandled (in 'tests.reboot.Greeter.Create') {aborted}; propagating as 'Unknown'\n" +
                        ''.join(IMPORT_traceback.format_exception(aborted))
                    )
                raise IMPORT_reboot.aio.aborted.SystemAborted(
                    IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                    # TODO(benh): consider whether or not we want to
                    # include the 'package.service.method' which may
                    # get concatenated together forming a kind of
                    # "stack trace"; while it's super helpful for
                    # debugging, it does expose implementation
                    # information.
                    message=f"unhandled (in 'tests.reboot.Greeter.Create') {aborted}"
                )
            else:
                if (
                    aborted_type is not None and
                    not isinstance(aborted, aborted_type) and
                    aborted_type.is_declared_error(aborted.error)
                ):
                    # We propagate declared errors that might have
                    # come from another call, i.e., we might have an
                    # `Aborted` but not for this method but the
                    # `Aborted` that we have has an error that this
                    # method declared. This allows a developer to
                    # simply add the declared error to their `.proto`
                    # file rather than having to catch and re-raise
                    # the error with their own aborted type.
                    if context.task is None:
                        logger.warning(
                            f"Propagating unhandled but declared error (in 'tests.reboot.Greeter.Create') {aborted}"
                        )
                elif (
                    aborted_type is None or
                    not isinstance(aborted, aborted_type)
                ):
                    # Not logging when within `node` as we already log there.
                    if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                        logger.warning(
                            f"Unhandled (in 'tests.reboot.Greeter.Create') {aborted}; propagating as 'Unknown'\n" +
                            ''.join(IMPORT_traceback.format_exception(aborted))
                        )
                    # If this wasn't a declared error than we
                    # propagate it as `Unknown`.
                    raise IMPORT_reboot.aio.aborted.SystemAborted(
                        IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                        # TODO(benh): consider whether or not we want to
                        # include the 'package.service.method' which may
                        # get concatenated together forming a kind of
                        # "stack trace"; while it's super helpful for
                        # debugging, it does expose implementation
                        # information.
                        message=f"unhandled (in 'tests.reboot.Greeter.Create') {aborted}"
                    )

            raise
        except IMPORT_asyncio.CancelledError:
            # It's pretty normal for an RPC to be cancelled; it's not useful to
            # print a stack trace.
            raise
        except IMPORT_google_protobuf_message.DecodeError as decode_error:
            # We usually see this error when we are trying to construct a proto
            # message which is too deeply nested: protobuf has a limit of 100
            # nested messages. See the limits here:
            #   https://protobuf.dev/programming-guides/proto-limits/

            if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                logger.warning(
                    "Unhandled (in 'tests.reboot.Greeter.Create') "
                    f"{type(decode_error).__name__}{': ' + str(decode_error) if len(str(decode_error)) > 0 else ''}; "
                    "This is usually caused by a deeply nested protobuf message, which is not supported by protobuf.\n"
                    "See the limits here: https://protobuf.dev/programming-guides/proto-limits/" +
                    ''.join(IMPORT_traceback.format_exception(decode_error))
                )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                message=f"unhandled (in 'tests.reboot.Greeter.Create') {decode_error}; "
                        "This is usually caused by a deeply nested protobuf message, which is not supported by protobuf.\n"
                        "See the limits here: https://protobuf.dev/programming-guides/proto-limits/"
            )
        except BaseException as exception:
            # Not logging when within `node` as we already log there.
            if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                logger.warning(
                    "Unhandled (in 'tests.reboot.Greeter.Create') "
                    f"{type(exception).__name__}{': ' + str(exception) if len(str(exception)) > 0 else ''}; "
                    "propagating as 'Unknown'\n" +
                    ''.join(IMPORT_traceback.format_exception(exception))
                )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                # TODO(benh): consider whether or not we want to
                # include the 'package.service.method' which may
                # get concatenated together forming a kind of
                # "stack trace"; while it's super helpful for
                # debugging, it does expose implementation
                # information.
                message=f"unhandled (in 'tests.reboot.Greeter.Create') {type(exception).__name__}: {exception}"
            )
        finally:
            states_being_constructed.remove(context.state_id)

    @IMPORT_reboot_aio_tracing.function_span(
        # We expect an `EffectValidationRetry` exception; that's not an error.
        set_status_on_exception=False
    )
    async def _Create(
        self,
        request: tests.reboot.greeter_pb2.CreateRequest,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        *,
        validating_effects: bool,
        grpc_context: IMPORT_typing.Optional[IMPORT_grpc.aio.ServicerContext] = None,
    ) -> tests.reboot.greeter_pb2.CreateResponse:
        # Try to verify the token if a token verifier exists.
        context.auth = await self._maybe_verify_token(
            headers=context._headers, method='Create'
        )

        # Check if we already have performed this mutation!
        #
        # We do this _before_ calling 'transactionally()' because
        # if this call is for a transaction method _and_ we've
        # already performed the transaction then we don't want to
        # become a transaction participant (again) we just want to
        # return the transaction's response.
        idempotent_mutation = await self._state_manager.check_for_idempotent_mutation(
            context
        )

        if idempotent_mutation is not None:
            response = tests.reboot.greeter_pb2.CreateResponse()
            response.ParseFromString(idempotent_mutation.response)
            return response

        async with self._state_manager.transactionally(
            context,
            self.tasks_dispatcher,
            aborted_type=Greeter.CreateAborted,
        ) as transaction:
            if transaction is not None:
                context.participants.add(
                    self._servicer.__state_type_name__, context._state_ref
                )
            async with self._state_manager.writer(
                context,
                self._servicer.__state_type__,
                self.tasks_dispatcher,
                authorize=self._maybe_authorize(
                    method_name='tests.reboot.GreeterMethods.Create',
                    headers=context._headers,
                    auth=context.auth,
                    request=request,
                ),
                transaction=transaction,
                from_constructor=True,
                requires_constructor=True,
            ) as (state, writer):

                effects = await self.__Create(
                    context,
                    state,
                    request,
                    validating_effects=validating_effects,
                )

                await writer.complete(effects)

                # TODO: We need a single `Effects` superclass for all methods, so we
                # would need to make it "partially" generic (with per-method subclasses
                # filling out the rest of the generic parameters) in order to fix this.
                return effects.response  # type: ignore[return-value]

    async def _schedule_Create(
        self,
        *,
        request: tests.reboot.greeter_pb2.CreateRequest,
        headers: IMPORT_reboot_aio_headers.Headers,
        grpc_context: IMPORT_grpc.aio.ServicerContext,
    ) -> tuple[IMPORT_reboot_aio_contexts.WriterContext, tests.reboot.greeter_pb2.CreateResponse]:
        context: IMPORT_reboot_aio_contexts.WriterContext = self.create_context(
            headers=headers,
            state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            method='Create',
            context_type=IMPORT_reboot_aio_contexts.WriterContext,
        )
        response = tests.reboot.greeter_pb2.CreateResponse()

        # Check if we already have performed this schedule! Note that
        # we need to do this for all kinds of methods because this is
        # effectively a mutation (actually a `writer`, see below).
        #
        # We do this _before_ calling 'transactionally()' because
        # if this call is for a transaction method _and_ we've
        # already performed the transaction then we don't want to
        # become a transaction participant (again) we just want to
        # return the transaction's response.
        idempotent_mutation = await self._state_manager.check_for_idempotent_mutation(
            context
        )

        if idempotent_mutation is not None:
            response.ParseFromString(idempotent_mutation.response)

            # We should have only scheduled a single task!
            assert len(idempotent_mutation.task_ids) == 1
            assert grpc_context is not None
            grpc_context.set_trailing_metadata(
                grpc_context.trailing_metadata() +
                (
                    (
                        IMPORT_reboot_aio_headers.TASK_ID_UUID,
                        str(IMPORT_uuid.UUID(bytes=idempotent_mutation.task_ids[0].task_uuid))
                    ),
                )
            )

            return context, response

        async with self._state_manager.transactionally(
            context,
            self.tasks_dispatcher,
            aborted_type=Greeter.CreateAborted,
        ) as transaction:
            if transaction is not None:
                context.participants.add(
                    self._servicer.__state_type_name__, context._state_ref
                )

            # Try to verify the token if a token verifier exists.
            context.auth = await self._maybe_verify_token(
                headers=headers, method='Create'
            )

            async with self._state_manager.writer(
                context,
                self._servicer.__state_type__,
                self.tasks_dispatcher,
                transaction=transaction,
                authorize=self._maybe_authorize(
                    method_name='tests.reboot.GreeterMethods.Create',
                    headers=context._headers,
                    auth=context.auth,
                    request=request,
                ),
                from_constructor=True,
                requires_constructor=True
            ) as (state, writer):

                task = await GreeterServicerTasks(
                    context=context,
                    state_ref=context._state_ref,
                ).Create(
                    GreeterCreateRequestFromProto(request),
                    schedule=context._headers.task_schedule,
                )

                effects = IMPORT_reboot_aio_state_managers.Effects(
                    response=response,
                    state=state,
                    tasks=[task],
                )

                assert effects.tasks is not None

                await writer.complete(effects)

                assert grpc_context is not None

                grpc_context.set_trailing_metadata(
                    grpc_context.trailing_metadata() +
                    (
                        (
                            IMPORT_reboot_aio_headers.TASK_ID_UUID,
                            str(IMPORT_uuid.UUID(bytes=task.task_id.task_uuid))
                        ),
                    )
                )

                return context, response

        return context, response


    # Entrypoint for non-reactive network calls (i.e. typical gRPC calls).
    async def Create(
        self,
        request: tests.reboot.greeter_pb2.CreateRequest,
        grpc_context: IMPORT_grpc.aio.ServicerContext,
    ) -> tests.reboot.greeter_pb2.CreateResponse:
        headers = IMPORT_reboot_aio_headers.Headers.from_grpc_context(grpc_context)
        assert headers.application_id is not None  # Guaranteed by `Headers`.

        # Confirm whether this is the right server to be serving this
        # request.
        try:
            authoritative_server = self.placement_client.server_for_actor(
                headers.application_id,
                headers.state_ref,
            )
        except IMPORT_reboot_aio_placement.UnknownApplicationError:
            # It's possible that the user did indeed type an application ID
            # that doesn't exist, but it's also quite possible that this
            # request reached us before the placement planner had gossipped
            # out the information about which applications exist (we see
            # this e.g. after `rbt dev`'s chaos monkey restarts). For that
            # reason, abort with a retryable error.
            await grpc_context.abort(
                IMPORT_grpc.StatusCode.UNAVAILABLE,
                f"Application '{headers.application_id}' not found. If you "
                "are confident the application exists, this may be because "
                "the system is still starting.",
            )
            raise  # Unreachable but necessary for mypy.
        if authoritative_server != self.server_id:
            # This is NOT the correct server. Fail.
            await grpc_context.abort(
                IMPORT_grpc.StatusCode.UNAVAILABLE,
                f"Server '{self.server_id}' is not authoritative for this "
                f"request; server '{authoritative_server}' is.",
            )
            raise  # Unreachable but necessary for mypy.

        @IMPORT_reboot_aio_internals_middleware.maybe_run_function_twice_to_validate_effects
        async def _run(
            validating_effects: bool,
        ) -> tests.reboot.greeter_pb2.CreateResponse:
            context: IMPORT_typing.Optional[IMPORT_reboot_aio_contexts.Context] = None
            try:
                if headers.task_schedule is not None:
                    context, response = await self._schedule_Create(
                        headers=headers,
                        request=request,
                        grpc_context=grpc_context,
                    )
                    return response

                context = self.create_context(
                    headers=headers,
                    state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                    method='Create',
                    context_type=IMPORT_reboot_aio_contexts.WriterContext,
                )
                assert context is not None

                return await self._Create(
                    request,
                    context,
                    validating_effects=validating_effects,
                    grpc_context=grpc_context,
                )
            except IMPORT_reboot_aio_contexts.EffectValidationRetry:
                # Doing effect validation, just let this propagate.
                raise
            except IMPORT_reboot.aio.aborted.Aborted as aborted:
                status = IMPORT_rpc_status_sync.to_status(aborted.to_status())
                # Need to add transaction participants here because
                # calling `grpc_context.abort_with_status()` will
                # ignore any other trailing metadata. Only propagate
                # transaction participants metadata if the caller cares.
                # Callers that care are those that are themselves transactions.
                # It's important to not just send this information to everyone;
                # some clients can't tolerate trailers, see:
                #   https://github.com/reboot-dev/mono/issues/5081
                if context is not None and headers.transaction_ids is not None:
                    assert context.transaction_id is not None
                    status = status._replace(
                        trailing_metadata=status.trailing_metadata + context.participants.to_grpc_metadata()
                    )
                await grpc_context.abort_with_status(status)
                raise  # Unreachable but necessary for mypy.
            except IMPORT_asyncio.CancelledError:
                # It's pretty normal for an RPC to be cancelled; it's not useful to
                # print a stack trace.
                raise
            except BaseException as exception:
                # Print the exception stack trace for easier debugging. Note
                # that we don't include the stack trace in an error message
                # for the same reason that gRPC doesn't do so by default,
                # see https://github.com/grpc/grpc/issues/14897, but since this
                # should only get logged on the server side it is safe.
                logger.warning(
                    'Unhandled exception\n' +
                    ''.join(IMPORT_traceback.format_exc() if IMPORT_reboot_nodejs_python.should_print_stacktrace() else [f"{type(exception).__name__}: {exception}"])
                )

                # Re-raise the exception for gRPC to handle!
                #
                # TODO: gRPC will print a stack trace from this
                # exception which we don't want if we're executing via
                # Node.js.
                raise
            finally:
                # Propagate transaction participants, if the caller cares.
                # Callers that care are those that are themselves transactions.
                # It's important to not just send this information to everyone;
                # some clients can't tolerate trailers, see:
                #   https://github.com/reboot-dev/mono/issues/5081
                if context is not None and headers.transaction_ids is not None:
                    assert context.transaction_id is not None
                    grpc_context.set_trailing_metadata(
                        grpc_context.trailing_metadata() +
                        context.participants.to_grpc_metadata()
                    )

        with IMPORT_reboot_aio_tracing.context_from_headers(headers):
            return await _run()

    async def __Greet(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: tests.reboot.greeter_pb2.Greeter,
        request: tests.reboot.greeter_pb2.GreetRequest,
        *,
        validating_effects: bool,
    ) -> tests.reboot.greeter_pb2.GreetResponse:
        try:
            typed_state: Greeter.State = GreeterFromProto(state, is_initial_state=(context.state_id in states_being_constructed))

            response = (
                await self._servicer._Greet(
                    context=context,
                    state=typed_state,
                    request=request
                )
            )


            GreeterToProto(typed_state, state)

            IMPORT_reboot_aio_types.assert_type(
                response,
                [tests.reboot.greeter_pb2.GreetResponse],
            )
            self.maybe_raise_effect_validation_retry(
                logger=logger,
                idempotency_manager=context,
                method_name='Greeter.Greet',
                validating_effects=validating_effects,
                context=context,
            )
            return response
        except IMPORT_reboot_aio_contexts.EffectValidationRetry:
            # Doing effect validation, just let this propagate.
            raise
        except IMPORT_reboot.aio.aborted.Aborted as aborted:
            # If the caller aborted due to a retryable error, just
            # propagate the aborted instead of propagating `Unknown`
            # so that a client can transparently retry.
            if IMPORT_reboot.aio.aborted.is_retryable(aborted):
                raise aborted
            # Log any _unhandled_ abort stack traces to make it
            # easier for debugging.
            #
            # NOTE: we don't log if we're a task as it will be logged
            # in `public/reboot/aio/internals/tasks_dispatcher.py` instead.
            aborted_type: IMPORT_typing.Optional[type] = None
            aborted_type = Greeter.GreetAborted
            if isinstance(aborted, IMPORT_reboot.aio.aborted.SystemAborted):
                # Not logging when within `node` as we already log there.
                if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                    logger.warning(
                        f"Unhandled (in 'tests.reboot.Greeter.Greet') {aborted}; propagating as 'Unknown'\n" +
                        ''.join(IMPORT_traceback.format_exception(aborted))
                    )
                raise IMPORT_reboot.aio.aborted.SystemAborted(
                    IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                    # TODO(benh): consider whether or not we want to
                    # include the 'package.service.method' which may
                    # get concatenated together forming a kind of
                    # "stack trace"; while it's super helpful for
                    # debugging, it does expose implementation
                    # information.
                    message=f"unhandled (in 'tests.reboot.Greeter.Greet') {aborted}"
                )
            else:
                if (
                    aborted_type is not None and
                    not isinstance(aborted, aborted_type) and
                    aborted_type.is_declared_error(aborted.error)
                ):
                    # We propagate declared errors that might have
                    # come from another call, i.e., we might have an
                    # `Aborted` but not for this method but the
                    # `Aborted` that we have has an error that this
                    # method declared. This allows a developer to
                    # simply add the declared error to their `.proto`
                    # file rather than having to catch and re-raise
                    # the error with their own aborted type.
                    if context.task is None:
                        logger.warning(
                            f"Propagating unhandled but declared error (in 'tests.reboot.Greeter.Greet') {aborted}"
                        )
                elif (
                    aborted_type is None or
                    not isinstance(aborted, aborted_type)
                ):
                    # Not logging when within `node` as we already log there.
                    if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                        logger.warning(
                            f"Unhandled (in 'tests.reboot.Greeter.Greet') {aborted}; propagating as 'Unknown'\n" +
                            ''.join(IMPORT_traceback.format_exception(aborted))
                        )
                    # If this wasn't a declared error than we
                    # propagate it as `Unknown`.
                    raise IMPORT_reboot.aio.aborted.SystemAborted(
                        IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                        # TODO(benh): consider whether or not we want to
                        # include the 'package.service.method' which may
                        # get concatenated together forming a kind of
                        # "stack trace"; while it's super helpful for
                        # debugging, it does expose implementation
                        # information.
                        message=f"unhandled (in 'tests.reboot.Greeter.Greet') {aborted}"
                    )

            raise
        except IMPORT_asyncio.CancelledError:
            # It's pretty normal for an RPC to be cancelled; it's not useful to
            # print a stack trace.
            raise
        except IMPORT_google_protobuf_message.DecodeError as decode_error:
            # We usually see this error when we are trying to construct a proto
            # message which is too deeply nested: protobuf has a limit of 100
            # nested messages. See the limits here:
            #   https://protobuf.dev/programming-guides/proto-limits/

            if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                logger.warning(
                    "Unhandled (in 'tests.reboot.Greeter.Greet') "
                    f"{type(decode_error).__name__}{': ' + str(decode_error) if len(str(decode_error)) > 0 else ''}; "
                    "This is usually caused by a deeply nested protobuf message, which is not supported by protobuf.\n"
                    "See the limits here: https://protobuf.dev/programming-guides/proto-limits/" +
                    ''.join(IMPORT_traceback.format_exception(decode_error))
                )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                message=f"unhandled (in 'tests.reboot.Greeter.Greet') {decode_error}; "
                        "This is usually caused by a deeply nested protobuf message, which is not supported by protobuf.\n"
                        "See the limits here: https://protobuf.dev/programming-guides/proto-limits/"
            )
        except BaseException as exception:
            # Not logging when within `node` as we already log there.
            if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                logger.warning(
                    "Unhandled (in 'tests.reboot.Greeter.Greet') "
                    f"{type(exception).__name__}{': ' + str(exception) if len(str(exception)) > 0 else ''}; "
                    "propagating as 'Unknown'\n" +
                    ''.join(IMPORT_traceback.format_exception(exception))
                )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                # TODO(benh): consider whether or not we want to
                # include the 'package.service.method' which may
                # get concatenated together forming a kind of
                # "stack trace"; while it's super helpful for
                # debugging, it does expose implementation
                # information.
                message=f"unhandled (in 'tests.reboot.Greeter.Greet') {type(exception).__name__}: {exception}"
            )
        finally:
            pass

    @IMPORT_reboot_aio_tracing.function_span(
        # We expect an `EffectValidationRetry` exception; that's not an error.
        set_status_on_exception=False
    )
    async def _Greet(
        self,
        request: tests.reboot.greeter_pb2.GreetRequest,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        *,
        validating_effects: bool,
        grpc_context: IMPORT_typing.Optional[IMPORT_grpc.aio.ServicerContext] = None,
    ) -> tests.reboot.greeter_pb2.GreetResponse:
        # Try to verify the token if a token verifier exists.
        context.auth = await self._maybe_verify_token(
            headers=context._headers, method='Greet'
        )


        async with self._state_manager.transactionally(
            context,
            self.tasks_dispatcher,
            aborted_type=Greeter.GreetAborted,
        ) as transaction:
            if transaction is not None:
                context.participants.add(
                    self._servicer.__state_type_name__, context._state_ref
                )
            authorizer = self._maybe_authorize(
                method_name='tests.reboot.GreeterMethods.Greet',
                headers=context._headers,
                auth=context.auth,
                request=request,
            )
            async with self._state_manager.reader(
                context,
                self._servicer.__state_type__,
                authorize=authorizer,
            ) as state:
                response = await self.__Greet(
                    context,
                    state,
                    request,
                    validating_effects=validating_effects,
                )
                return response

    async def _schedule_Greet(
        self,
        *,
        request: tests.reboot.greeter_pb2.GreetRequest,
        headers: IMPORT_reboot_aio_headers.Headers,
        grpc_context: IMPORT_grpc.aio.ServicerContext,
    ) -> tuple[IMPORT_reboot_aio_contexts.WriterContext, tests.reboot.greeter_pb2.GreetResponse]:
        context: IMPORT_reboot_aio_contexts.WriterContext = self.create_context(
            headers=headers,
            state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            method='Greet',
            context_type=IMPORT_reboot_aio_contexts.WriterContext,
        )
        response = tests.reboot.greeter_pb2.GreetResponse()

        # Check if we already have performed this schedule! Note that
        # we need to do this for all kinds of methods because this is
        # effectively a mutation (actually a `writer`, see below).
        #
        # We do this _before_ calling 'transactionally()' because
        # if this call is for a transaction method _and_ we've
        # already performed the transaction then we don't want to
        # become a transaction participant (again) we just want to
        # return the transaction's response.
        idempotent_mutation = await self._state_manager.check_for_idempotent_mutation(
            context
        )

        if idempotent_mutation is not None:
            response.ParseFromString(idempotent_mutation.response)

            # We should have only scheduled a single task!
            assert len(idempotent_mutation.task_ids) == 1
            assert grpc_context is not None
            grpc_context.set_trailing_metadata(
                grpc_context.trailing_metadata() +
                (
                    (
                        IMPORT_reboot_aio_headers.TASK_ID_UUID,
                        str(IMPORT_uuid.UUID(bytes=idempotent_mutation.task_ids[0].task_uuid))
                    ),
                )
            )

            return context, response

        async with self._state_manager.transactionally(
            context,
            self.tasks_dispatcher,
            aborted_type=Greeter.GreetAborted,
        ) as transaction:
            if transaction is not None:
                context.participants.add(
                    self._servicer.__state_type_name__, context._state_ref
                )

            # Try to verify the token if a token verifier exists.
            context.auth = await self._maybe_verify_token(
                headers=headers, method='Greet'
            )

            async with self._state_manager.writer(
                context,
                self._servicer.__state_type__,
                self.tasks_dispatcher,
                transaction=transaction,
                authorize=self._maybe_authorize(
                    method_name='tests.reboot.GreeterMethods.Greet',
                    headers=context._headers,
                    auth=context.auth,
                    request=request,
                ),
                from_constructor=False,
                requires_constructor=True
            ) as (state, writer):

                task = await GreeterServicerTasks(
                    context=context,
                    state_ref=context._state_ref,
                ).Greet(
                    GreeterGreetRequestFromProto(request),
                    schedule=context._headers.task_schedule,
                )

                effects = IMPORT_reboot_aio_state_managers.Effects(
                    response=response,
                    state=state,
                    tasks=[task],
                )

                assert effects.tasks is not None

                await writer.complete(effects)

                assert grpc_context is not None

                grpc_context.set_trailing_metadata(
                    grpc_context.trailing_metadata() +
                    (
                        (
                            IMPORT_reboot_aio_headers.TASK_ID_UUID,
                            str(IMPORT_uuid.UUID(bytes=task.task_id.task_uuid))
                        ),
                    )
                )

                return context, response

        return context, response


    # Entrypoint for non-reactive network calls (i.e. typical gRPC calls).
    async def Greet(
        self,
        request: tests.reboot.greeter_pb2.GreetRequest,
        grpc_context: IMPORT_grpc.aio.ServicerContext,
    ) -> tests.reboot.greeter_pb2.GreetResponse:
        headers = IMPORT_reboot_aio_headers.Headers.from_grpc_context(grpc_context)
        assert headers.application_id is not None  # Guaranteed by `Headers`.

        # Confirm whether this is the right server to be serving this
        # request.
        try:
            authoritative_server = self.placement_client.server_for_actor(
                headers.application_id,
                headers.state_ref,
            )
        except IMPORT_reboot_aio_placement.UnknownApplicationError:
            # It's possible that the user did indeed type an application ID
            # that doesn't exist, but it's also quite possible that this
            # request reached us before the placement planner had gossipped
            # out the information about which applications exist (we see
            # this e.g. after `rbt dev`'s chaos monkey restarts). For that
            # reason, abort with a retryable error.
            await grpc_context.abort(
                IMPORT_grpc.StatusCode.UNAVAILABLE,
                f"Application '{headers.application_id}' not found. If you "
                "are confident the application exists, this may be because "
                "the system is still starting.",
            )
            raise  # Unreachable but necessary for mypy.
        if authoritative_server != self.server_id:
            # This is NOT the correct server. Fail.
            await grpc_context.abort(
                IMPORT_grpc.StatusCode.UNAVAILABLE,
                f"Server '{self.server_id}' is not authoritative for this "
                f"request; server '{authoritative_server}' is.",
            )
            raise  # Unreachable but necessary for mypy.

        @IMPORT_reboot_aio_internals_middleware.maybe_run_function_twice_to_validate_effects
        async def _run(
            validating_effects: bool,
        ) -> tests.reboot.greeter_pb2.GreetResponse:
            context: IMPORT_typing.Optional[IMPORT_reboot_aio_contexts.Context] = None
            try:
                if headers.task_schedule is not None:
                    context, response = await self._schedule_Greet(
                        headers=headers,
                        request=request,
                        grpc_context=grpc_context,
                    )
                    return response

                context = self.create_context(
                    headers=headers,
                    state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                    method='Greet',
                    context_type=IMPORT_reboot_aio_contexts.ReaderContext,
                )
                assert context is not None

                return await self._Greet(
                    request,
                    context,
                    validating_effects=validating_effects,
                    grpc_context=grpc_context,
                )
            except IMPORT_reboot_aio_contexts.EffectValidationRetry:
                # Doing effect validation, just let this propagate.
                raise
            except IMPORT_reboot.aio.aborted.Aborted as aborted:
                status = IMPORT_rpc_status_sync.to_status(aborted.to_status())
                # Need to add transaction participants here because
                # calling `grpc_context.abort_with_status()` will
                # ignore any other trailing metadata. Only propagate
                # transaction participants metadata if the caller cares.
                # Callers that care are those that are themselves transactions.
                # It's important to not just send this information to everyone;
                # some clients can't tolerate trailers, see:
                #   https://github.com/reboot-dev/mono/issues/5081
                if context is not None and headers.transaction_ids is not None:
                    assert context.transaction_id is not None
                    status = status._replace(
                        trailing_metadata=status.trailing_metadata + context.participants.to_grpc_metadata()
                    )
                await grpc_context.abort_with_status(status)
                raise  # Unreachable but necessary for mypy.
            except IMPORT_asyncio.CancelledError:
                # It's pretty normal for an RPC to be cancelled; it's not useful to
                # print a stack trace.
                raise
            except BaseException as exception:
                # Print the exception stack trace for easier debugging. Note
                # that we don't include the stack trace in an error message
                # for the same reason that gRPC doesn't do so by default,
                # see https://github.com/grpc/grpc/issues/14897, but since this
                # should only get logged on the server side it is safe.
                logger.warning(
                    'Unhandled exception\n' +
                    ''.join(IMPORT_traceback.format_exc() if IMPORT_reboot_nodejs_python.should_print_stacktrace() else [f"{type(exception).__name__}: {exception}"])
                )

                # Re-raise the exception for gRPC to handle!
                #
                # TODO: gRPC will print a stack trace from this
                # exception which we don't want if we're executing via
                # Node.js.
                raise
            finally:
                # Propagate transaction participants, if the caller cares.
                # Callers that care are those that are themselves transactions.
                # It's important to not just send this information to everyone;
                # some clients can't tolerate trailers, see:
                #   https://github.com/reboot-dev/mono/issues/5081
                if context is not None and headers.transaction_ids is not None:
                    assert context.transaction_id is not None
                    grpc_context.set_trailing_metadata(
                        grpc_context.trailing_metadata() +
                        context.participants.to_grpc_metadata()
                    )

        with IMPORT_reboot_aio_tracing.context_from_headers(headers):
            return await _run()

    async def __SetAdjective(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: tests.reboot.greeter_pb2.Greeter,
        request: tests.reboot.greeter_pb2.SetAdjectiveRequest,
        *,
        validating_effects: bool,
    ) -> Greeter.SetAdjectiveEffects:
        try:
            typed_state: Greeter.State = GreeterFromProto(state, is_initial_state=(context.state_id in states_being_constructed))

            response = (
                await self._servicer._SetAdjective(
                    context=context,
                    state=typed_state,
                    request=request
                )
            )


            GreeterToProto(typed_state, state)

            IMPORT_reboot_aio_types.assert_type(
                response,
                [tests.reboot.greeter_pb2.SetAdjectiveResponse],
            )
            self.maybe_raise_effect_validation_retry(
                logger=logger,
                idempotency_manager=context,
                method_name='Greeter.SetAdjective',
                validating_effects=validating_effects,
                context=context,
            )
            return Greeter.SetAdjectiveEffects(
                state=state,
                response=response,
                tasks=context._tasks,
                _colocated_upserts=context._colocated_upserts,
            )
        except IMPORT_reboot_aio_contexts.EffectValidationRetry:
            # Doing effect validation, just let this propagate.
            raise
        except IMPORT_reboot.aio.aborted.Aborted as aborted:
            # If the caller aborted due to a retryable error, just
            # propagate the aborted instead of propagating `Unknown`
            # so that a client can transparently retry.
            if IMPORT_reboot.aio.aborted.is_retryable(aborted):
                raise aborted
            # Log any _unhandled_ abort stack traces to make it
            # easier for debugging.
            #
            # NOTE: we don't log if we're a task as it will be logged
            # in `public/reboot/aio/internals/tasks_dispatcher.py` instead.
            aborted_type: IMPORT_typing.Optional[type] = None
            aborted_type = Greeter.SetAdjectiveAborted
            if isinstance(aborted, IMPORT_reboot.aio.aborted.SystemAborted):
                # Not logging when within `node` as we already log there.
                if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                    logger.warning(
                        f"Unhandled (in 'tests.reboot.Greeter.SetAdjective') {aborted}; propagating as 'Unknown'\n" +
                        ''.join(IMPORT_traceback.format_exception(aborted))
                    )
                raise IMPORT_reboot.aio.aborted.SystemAborted(
                    IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                    # TODO(benh): consider whether or not we want to
                    # include the 'package.service.method' which may
                    # get concatenated together forming a kind of
                    # "stack trace"; while it's super helpful for
                    # debugging, it does expose implementation
                    # information.
                    message=f"unhandled (in 'tests.reboot.Greeter.SetAdjective') {aborted}"
                )
            else:
                if (
                    aborted_type is not None and
                    not isinstance(aborted, aborted_type) and
                    aborted_type.is_declared_error(aborted.error)
                ):
                    # We propagate declared errors that might have
                    # come from another call, i.e., we might have an
                    # `Aborted` but not for this method but the
                    # `Aborted` that we have has an error that this
                    # method declared. This allows a developer to
                    # simply add the declared error to their `.proto`
                    # file rather than having to catch and re-raise
                    # the error with their own aborted type.
                    if context.task is None:
                        logger.warning(
                            f"Propagating unhandled but declared error (in 'tests.reboot.Greeter.SetAdjective') {aborted}"
                        )
                elif (
                    aborted_type is None or
                    not isinstance(aborted, aborted_type)
                ):
                    # Not logging when within `node` as we already log there.
                    if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                        logger.warning(
                            f"Unhandled (in 'tests.reboot.Greeter.SetAdjective') {aborted}; propagating as 'Unknown'\n" +
                            ''.join(IMPORT_traceback.format_exception(aborted))
                        )
                    # If this wasn't a declared error than we
                    # propagate it as `Unknown`.
                    raise IMPORT_reboot.aio.aborted.SystemAborted(
                        IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                        # TODO(benh): consider whether or not we want to
                        # include the 'package.service.method' which may
                        # get concatenated together forming a kind of
                        # "stack trace"; while it's super helpful for
                        # debugging, it does expose implementation
                        # information.
                        message=f"unhandled (in 'tests.reboot.Greeter.SetAdjective') {aborted}"
                    )

            raise
        except IMPORT_asyncio.CancelledError:
            # It's pretty normal for an RPC to be cancelled; it's not useful to
            # print a stack trace.
            raise
        except IMPORT_google_protobuf_message.DecodeError as decode_error:
            # We usually see this error when we are trying to construct a proto
            # message which is too deeply nested: protobuf has a limit of 100
            # nested messages. See the limits here:
            #   https://protobuf.dev/programming-guides/proto-limits/

            if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                logger.warning(
                    "Unhandled (in 'tests.reboot.Greeter.SetAdjective') "
                    f"{type(decode_error).__name__}{': ' + str(decode_error) if len(str(decode_error)) > 0 else ''}; "
                    "This is usually caused by a deeply nested protobuf message, which is not supported by protobuf.\n"
                    "See the limits here: https://protobuf.dev/programming-guides/proto-limits/" +
                    ''.join(IMPORT_traceback.format_exception(decode_error))
                )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                message=f"unhandled (in 'tests.reboot.Greeter.SetAdjective') {decode_error}; "
                        "This is usually caused by a deeply nested protobuf message, which is not supported by protobuf.\n"
                        "See the limits here: https://protobuf.dev/programming-guides/proto-limits/"
            )
        except BaseException as exception:
            # Not logging when within `node` as we already log there.
            if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                logger.warning(
                    "Unhandled (in 'tests.reboot.Greeter.SetAdjective') "
                    f"{type(exception).__name__}{': ' + str(exception) if len(str(exception)) > 0 else ''}; "
                    "propagating as 'Unknown'\n" +
                    ''.join(IMPORT_traceback.format_exception(exception))
                )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                # TODO(benh): consider whether or not we want to
                # include the 'package.service.method' which may
                # get concatenated together forming a kind of
                # "stack trace"; while it's super helpful for
                # debugging, it does expose implementation
                # information.
                message=f"unhandled (in 'tests.reboot.Greeter.SetAdjective') {type(exception).__name__}: {exception}"
            )
        finally:
            pass

    @IMPORT_reboot_aio_tracing.function_span(
        # We expect an `EffectValidationRetry` exception; that's not an error.
        set_status_on_exception=False
    )
    async def _SetAdjective(
        self,
        request: tests.reboot.greeter_pb2.SetAdjectiveRequest,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        *,
        validating_effects: bool,
        grpc_context: IMPORT_typing.Optional[IMPORT_grpc.aio.ServicerContext] = None,
    ) -> tests.reboot.greeter_pb2.SetAdjectiveResponse:
        # Try to verify the token if a token verifier exists.
        context.auth = await self._maybe_verify_token(
            headers=context._headers, method='SetAdjective'
        )

        # Check if we already have performed this mutation!
        #
        # We do this _before_ calling 'transactionally()' because
        # if this call is for a transaction method _and_ we've
        # already performed the transaction then we don't want to
        # become a transaction participant (again) we just want to
        # return the transaction's response.
        idempotent_mutation = await self._state_manager.check_for_idempotent_mutation(
            context
        )

        if idempotent_mutation is not None:
            response = tests.reboot.greeter_pb2.SetAdjectiveResponse()
            response.ParseFromString(idempotent_mutation.response)
            return response

        async with self._state_manager.transactionally(
            context,
            self.tasks_dispatcher,
            aborted_type=Greeter.SetAdjectiveAborted,
        ) as transaction:
            if transaction is not None:
                context.participants.add(
                    self._servicer.__state_type_name__, context._state_ref
                )
            async with self._state_manager.writer(
                context,
                self._servicer.__state_type__,
                self.tasks_dispatcher,
                authorize=self._maybe_authorize(
                    method_name='tests.reboot.GreeterMethods.SetAdjective',
                    headers=context._headers,
                    auth=context.auth,
                    request=request,
                ),
                transaction=transaction,
                from_constructor=False,
                requires_constructor=True,
            ) as (state, writer):

                effects = await self.__SetAdjective(
                    context,
                    state,
                    request,
                    validating_effects=validating_effects,
                )

                await writer.complete(effects)

                # TODO: We need a single `Effects` superclass for all methods, so we
                # would need to make it "partially" generic (with per-method subclasses
                # filling out the rest of the generic parameters) in order to fix this.
                return effects.response  # type: ignore[return-value]

    async def _schedule_SetAdjective(
        self,
        *,
        request: tests.reboot.greeter_pb2.SetAdjectiveRequest,
        headers: IMPORT_reboot_aio_headers.Headers,
        grpc_context: IMPORT_grpc.aio.ServicerContext,
    ) -> tuple[IMPORT_reboot_aio_contexts.WriterContext, tests.reboot.greeter_pb2.SetAdjectiveResponse]:
        context: IMPORT_reboot_aio_contexts.WriterContext = self.create_context(
            headers=headers,
            state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            method='SetAdjective',
            context_type=IMPORT_reboot_aio_contexts.WriterContext,
        )
        response = tests.reboot.greeter_pb2.SetAdjectiveResponse()

        # Check if we already have performed this schedule! Note that
        # we need to do this for all kinds of methods because this is
        # effectively a mutation (actually a `writer`, see below).
        #
        # We do this _before_ calling 'transactionally()' because
        # if this call is for a transaction method _and_ we've
        # already performed the transaction then we don't want to
        # become a transaction participant (again) we just want to
        # return the transaction's response.
        idempotent_mutation = await self._state_manager.check_for_idempotent_mutation(
            context
        )

        if idempotent_mutation is not None:
            response.ParseFromString(idempotent_mutation.response)

            # We should have only scheduled a single task!
            assert len(idempotent_mutation.task_ids) == 1
            assert grpc_context is not None
            grpc_context.set_trailing_metadata(
                grpc_context.trailing_metadata() +
                (
                    (
                        IMPORT_reboot_aio_headers.TASK_ID_UUID,
                        str(IMPORT_uuid.UUID(bytes=idempotent_mutation.task_ids[0].task_uuid))
                    ),
                )
            )

            return context, response

        async with self._state_manager.transactionally(
            context,
            self.tasks_dispatcher,
            aborted_type=Greeter.SetAdjectiveAborted,
        ) as transaction:
            if transaction is not None:
                context.participants.add(
                    self._servicer.__state_type_name__, context._state_ref
                )

            # Try to verify the token if a token verifier exists.
            context.auth = await self._maybe_verify_token(
                headers=headers, method='SetAdjective'
            )

            async with self._state_manager.writer(
                context,
                self._servicer.__state_type__,
                self.tasks_dispatcher,
                transaction=transaction,
                authorize=self._maybe_authorize(
                    method_name='tests.reboot.GreeterMethods.SetAdjective',
                    headers=context._headers,
                    auth=context.auth,
                    request=request,
                ),
                from_constructor=False,
                requires_constructor=True
            ) as (state, writer):

                task = await GreeterServicerTasks(
                    context=context,
                    state_ref=context._state_ref,
                ).SetAdjective(
                    GreeterSetAdjectiveRequestFromProto(request),
                    schedule=context._headers.task_schedule,
                )

                effects = IMPORT_reboot_aio_state_managers.Effects(
                    response=response,
                    state=state,
                    tasks=[task],
                )

                assert effects.tasks is not None

                await writer.complete(effects)

                assert grpc_context is not None

                grpc_context.set_trailing_metadata(
                    grpc_context.trailing_metadata() +
                    (
                        (
                            IMPORT_reboot_aio_headers.TASK_ID_UUID,
                            str(IMPORT_uuid.UUID(bytes=task.task_id.task_uuid))
                        ),
                    )
                )

                return context, response

        return context, response


    # Entrypoint for non-reactive network calls (i.e. typical gRPC calls).
    async def SetAdjective(
        self,
        request: tests.reboot.greeter_pb2.SetAdjectiveRequest,
        grpc_context: IMPORT_grpc.aio.ServicerContext,
    ) -> tests.reboot.greeter_pb2.SetAdjectiveResponse:
        headers = IMPORT_reboot_aio_headers.Headers.from_grpc_context(grpc_context)
        assert headers.application_id is not None  # Guaranteed by `Headers`.

        # Confirm whether this is the right server to be serving this
        # request.
        try:
            authoritative_server = self.placement_client.server_for_actor(
                headers.application_id,
                headers.state_ref,
            )
        except IMPORT_reboot_aio_placement.UnknownApplicationError:
            # It's possible that the user did indeed type an application ID
            # that doesn't exist, but it's also quite possible that this
            # request reached us before the placement planner had gossipped
            # out the information about which applications exist (we see
            # this e.g. after `rbt dev`'s chaos monkey restarts). For that
            # reason, abort with a retryable error.
            await grpc_context.abort(
                IMPORT_grpc.StatusCode.UNAVAILABLE,
                f"Application '{headers.application_id}' not found. If you "
                "are confident the application exists, this may be because "
                "the system is still starting.",
            )
            raise  # Unreachable but necessary for mypy.
        if authoritative_server != self.server_id:
            # This is NOT the correct server. Fail.
            await grpc_context.abort(
                IMPORT_grpc.StatusCode.UNAVAILABLE,
                f"Server '{self.server_id}' is not authoritative for this "
                f"request; server '{authoritative_server}' is.",
            )
            raise  # Unreachable but necessary for mypy.

        @IMPORT_reboot_aio_internals_middleware.maybe_run_function_twice_to_validate_effects
        async def _run(
            validating_effects: bool,
        ) -> tests.reboot.greeter_pb2.SetAdjectiveResponse:
            context: IMPORT_typing.Optional[IMPORT_reboot_aio_contexts.Context] = None
            try:
                if headers.task_schedule is not None:
                    context, response = await self._schedule_SetAdjective(
                        headers=headers,
                        request=request,
                        grpc_context=grpc_context,
                    )
                    return response

                context = self.create_context(
                    headers=headers,
                    state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                    method='SetAdjective',
                    context_type=IMPORT_reboot_aio_contexts.WriterContext,
                )
                assert context is not None

                return await self._SetAdjective(
                    request,
                    context,
                    validating_effects=validating_effects,
                    grpc_context=grpc_context,
                )
            except IMPORT_reboot_aio_contexts.EffectValidationRetry:
                # Doing effect validation, just let this propagate.
                raise
            except IMPORT_reboot.aio.aborted.Aborted as aborted:
                status = IMPORT_rpc_status_sync.to_status(aborted.to_status())
                # Need to add transaction participants here because
                # calling `grpc_context.abort_with_status()` will
                # ignore any other trailing metadata. Only propagate
                # transaction participants metadata if the caller cares.
                # Callers that care are those that are themselves transactions.
                # It's important to not just send this information to everyone;
                # some clients can't tolerate trailers, see:
                #   https://github.com/reboot-dev/mono/issues/5081
                if context is not None and headers.transaction_ids is not None:
                    assert context.transaction_id is not None
                    status = status._replace(
                        trailing_metadata=status.trailing_metadata + context.participants.to_grpc_metadata()
                    )
                await grpc_context.abort_with_status(status)
                raise  # Unreachable but necessary for mypy.
            except IMPORT_asyncio.CancelledError:
                # It's pretty normal for an RPC to be cancelled; it's not useful to
                # print a stack trace.
                raise
            except BaseException as exception:
                # Print the exception stack trace for easier debugging. Note
                # that we don't include the stack trace in an error message
                # for the same reason that gRPC doesn't do so by default,
                # see https://github.com/grpc/grpc/issues/14897, but since this
                # should only get logged on the server side it is safe.
                logger.warning(
                    'Unhandled exception\n' +
                    ''.join(IMPORT_traceback.format_exc() if IMPORT_reboot_nodejs_python.should_print_stacktrace() else [f"{type(exception).__name__}: {exception}"])
                )

                # Re-raise the exception for gRPC to handle!
                #
                # TODO: gRPC will print a stack trace from this
                # exception which we don't want if we're executing via
                # Node.js.
                raise
            finally:
                # Propagate transaction participants, if the caller cares.
                # Callers that care are those that are themselves transactions.
                # It's important to not just send this information to everyone;
                # some clients can't tolerate trailers, see:
                #   https://github.com/reboot-dev/mono/issues/5081
                if context is not None and headers.transaction_ids is not None:
                    assert context.transaction_id is not None
                    grpc_context.set_trailing_metadata(
                        grpc_context.trailing_metadata() +
                        context.participants.to_grpc_metadata()
                    )

        with IMPORT_reboot_aio_tracing.context_from_headers(headers):
            return await _run()

    async def __TransactionSetAdjective(
        self,
        context: IMPORT_reboot_aio_contexts.TransactionContext,
        state: tests.reboot.greeter_pb2.Greeter,
        request: tests.reboot.greeter_pb2.SetAdjectiveRequest,
        *,
        validating_effects: bool,
    ) -> tests.reboot.greeter_pb2.SetAdjectiveResponse:
        try:
            typed_state: Greeter.State = GreeterFromProto(state, is_initial_state=(context.state_id in states_being_constructed))

            response = (
                await self._servicer._TransactionSetAdjective(
                    context=context,
                    state=typed_state,
                    request=request
                )
            )


            GreeterToProto(typed_state, state)

            IMPORT_reboot_aio_types.assert_type(
                response,
                [tests.reboot.greeter_pb2.SetAdjectiveResponse],
            )
            self.maybe_raise_effect_validation_retry(
                logger=logger,
                idempotency_manager=context,
                method_name='Greeter.TransactionSetAdjective',
                validating_effects=validating_effects,
                context=context,
            )
            return response
        except IMPORT_reboot_aio_contexts.EffectValidationRetry:
            # Doing effect validation, just let this propagate.
            raise
        except IMPORT_reboot.aio.aborted.Aborted as aborted:
            # If the caller aborted due to a retryable error, just
            # propagate the aborted instead of propagating `Unknown`
            # so that a client can transparently retry.
            if IMPORT_reboot.aio.aborted.is_retryable(aborted):
                raise aborted
            # Log any _unhandled_ abort stack traces to make it
            # easier for debugging.
            #
            # NOTE: we don't log if we're a task as it will be logged
            # in `public/reboot/aio/internals/tasks_dispatcher.py` instead.
            aborted_type: IMPORT_typing.Optional[type] = None
            aborted_type = Greeter.TransactionSetAdjectiveAborted
            if isinstance(aborted, IMPORT_reboot.aio.aborted.SystemAborted):
                # Not logging when within `node` as we already log there.
                if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                    logger.warning(
                        f"Unhandled (in 'tests.reboot.Greeter.TransactionSetAdjective') {aborted}; propagating as 'Unknown'\n" +
                        ''.join(IMPORT_traceback.format_exception(aborted))
                    )
                raise IMPORT_reboot.aio.aborted.SystemAborted(
                    IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                    # TODO(benh): consider whether or not we want to
                    # include the 'package.service.method' which may
                    # get concatenated together forming a kind of
                    # "stack trace"; while it's super helpful for
                    # debugging, it does expose implementation
                    # information.
                    message=f"unhandled (in 'tests.reboot.Greeter.TransactionSetAdjective') {aborted}"
                )
            else:
                if (
                    aborted_type is not None and
                    not isinstance(aborted, aborted_type) and
                    aborted_type.is_declared_error(aborted.error)
                ):
                    # We propagate declared errors that might have
                    # come from another call, i.e., we might have an
                    # `Aborted` but not for this method but the
                    # `Aborted` that we have has an error that this
                    # method declared. This allows a developer to
                    # simply add the declared error to their `.proto`
                    # file rather than having to catch and re-raise
                    # the error with their own aborted type.
                    if context.task is None:
                        logger.warning(
                            f"Propagating unhandled but declared error (in 'tests.reboot.Greeter.TransactionSetAdjective') {aborted}"
                        )
                elif (
                    aborted_type is None or
                    not isinstance(aborted, aborted_type)
                ):
                    # Not logging when within `node` as we already log there.
                    if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                        logger.warning(
                            f"Unhandled (in 'tests.reboot.Greeter.TransactionSetAdjective') {aborted}; propagating as 'Unknown'\n" +
                            ''.join(IMPORT_traceback.format_exception(aborted))
                        )
                    # If this wasn't a declared error than we
                    # propagate it as `Unknown`.
                    raise IMPORT_reboot.aio.aborted.SystemAborted(
                        IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                        # TODO(benh): consider whether or not we want to
                        # include the 'package.service.method' which may
                        # get concatenated together forming a kind of
                        # "stack trace"; while it's super helpful for
                        # debugging, it does expose implementation
                        # information.
                        message=f"unhandled (in 'tests.reboot.Greeter.TransactionSetAdjective') {aborted}"
                    )

            raise
        except IMPORT_asyncio.CancelledError:
            # It's pretty normal for an RPC to be cancelled; it's not useful to
            # print a stack trace.
            raise
        except IMPORT_google_protobuf_message.DecodeError as decode_error:
            # We usually see this error when we are trying to construct a proto
            # message which is too deeply nested: protobuf has a limit of 100
            # nested messages. See the limits here:
            #   https://protobuf.dev/programming-guides/proto-limits/

            if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                logger.warning(
                    "Unhandled (in 'tests.reboot.Greeter.TransactionSetAdjective') "
                    f"{type(decode_error).__name__}{': ' + str(decode_error) if len(str(decode_error)) > 0 else ''}; "
                    "This is usually caused by a deeply nested protobuf message, which is not supported by protobuf.\n"
                    "See the limits here: https://protobuf.dev/programming-guides/proto-limits/" +
                    ''.join(IMPORT_traceback.format_exception(decode_error))
                )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                message=f"unhandled (in 'tests.reboot.Greeter.TransactionSetAdjective') {decode_error}; "
                        "This is usually caused by a deeply nested protobuf message, which is not supported by protobuf.\n"
                        "See the limits here: https://protobuf.dev/programming-guides/proto-limits/"
            )
        except BaseException as exception:
            # Not logging when within `node` as we already log there.
            if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                logger.warning(
                    "Unhandled (in 'tests.reboot.Greeter.TransactionSetAdjective') "
                    f"{type(exception).__name__}{': ' + str(exception) if len(str(exception)) > 0 else ''}; "
                    "propagating as 'Unknown'\n" +
                    ''.join(IMPORT_traceback.format_exception(exception))
                )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                # TODO(benh): consider whether or not we want to
                # include the 'package.service.method' which may
                # get concatenated together forming a kind of
                # "stack trace"; while it's super helpful for
                # debugging, it does expose implementation
                # information.
                message=f"unhandled (in 'tests.reboot.Greeter.TransactionSetAdjective') {type(exception).__name__}: {exception}"
            )
        finally:
            pass

    @IMPORT_reboot_aio_tracing.function_span(
        # We expect an `EffectValidationRetry` exception; that's not an error.
        set_status_on_exception=False
    )
    async def _TransactionSetAdjective(
        self,
        request: tests.reboot.greeter_pb2.SetAdjectiveRequest,
        context: IMPORT_reboot_aio_contexts.TransactionContext,
        *,
        validating_effects: bool,
        grpc_context: IMPORT_typing.Optional[IMPORT_grpc.aio.ServicerContext] = None,
    ) -> tests.reboot.greeter_pb2.SetAdjectiveResponse:
        # Try to verify the token if a token verifier exists.
        context.auth = await self._maybe_verify_token(
            headers=context._headers, method='TransactionSetAdjective'
        )

        # Check if we already have performed this mutation!
        #
        # We do this _before_ calling 'transactionally()' because
        # if this call is for a transaction method _and_ we've
        # already performed the transaction then we don't want to
        # become a transaction participant (again) we just want to
        # return the transaction's response.
        idempotent_mutation = await self._state_manager.check_for_idempotent_mutation(
            context
        )

        if idempotent_mutation is not None:
            response = tests.reboot.greeter_pb2.SetAdjectiveResponse()
            response.ParseFromString(idempotent_mutation.response)
            return response

        async with self._state_manager.transactionally(
            context,
            self.tasks_dispatcher,
            aborted_type=Greeter.TransactionSetAdjectiveAborted,
        ) as transaction:
            if transaction is not None:
                context.participants.add(
                    self._servicer.__state_type_name__, context._state_ref
                )
            assert transaction is not None
            async with self._state_manager.transaction(
                context,
                self._servicer.__state_type__,
                transaction,
                authorize=self._maybe_authorize(
                    method_name='tests.reboot.GreeterMethods.TransactionSetAdjective',
                    headers=context._headers,
                    auth=context.auth,
                    request=request,
                ),
                from_constructor=False,
                requires_constructor=True
            ) as (state, complete):

                response = await self.__TransactionSetAdjective(
                    context,
                    state,
                    request,
                    validating_effects=validating_effects,
                )

                await complete(
                    IMPORT_reboot_aio_state_managers.Effects(
                        state=state,
                        response=response,
                    )
                )
                return response

    async def _schedule_TransactionSetAdjective(
        self,
        *,
        request: tests.reboot.greeter_pb2.SetAdjectiveRequest,
        headers: IMPORT_reboot_aio_headers.Headers,
        grpc_context: IMPORT_grpc.aio.ServicerContext,
    ) -> tuple[IMPORT_reboot_aio_contexts.WriterContext, tests.reboot.greeter_pb2.SetAdjectiveResponse]:
        context: IMPORT_reboot_aio_contexts.WriterContext = self.create_context(
            headers=headers,
            state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            method='TransactionSetAdjective',
            context_type=IMPORT_reboot_aio_contexts.WriterContext,
        )
        response = tests.reboot.greeter_pb2.SetAdjectiveResponse()

        # Check if we already have performed this schedule! Note that
        # we need to do this for all kinds of methods because this is
        # effectively a mutation (actually a `writer`, see below).
        #
        # We do this _before_ calling 'transactionally()' because
        # if this call is for a transaction method _and_ we've
        # already performed the transaction then we don't want to
        # become a transaction participant (again) we just want to
        # return the transaction's response.
        idempotent_mutation = await self._state_manager.check_for_idempotent_mutation(
            context
        )

        if idempotent_mutation is not None:
            response.ParseFromString(idempotent_mutation.response)

            # We should have only scheduled a single task!
            assert len(idempotent_mutation.task_ids) == 1
            assert grpc_context is not None
            grpc_context.set_trailing_metadata(
                grpc_context.trailing_metadata() +
                (
                    (
                        IMPORT_reboot_aio_headers.TASK_ID_UUID,
                        str(IMPORT_uuid.UUID(bytes=idempotent_mutation.task_ids[0].task_uuid))
                    ),
                )
            )

            return context, response

        async with self._state_manager.transactionally(
            context,
            self.tasks_dispatcher,
            aborted_type=Greeter.TransactionSetAdjectiveAborted,
        ) as transaction:
            if transaction is not None:
                context.participants.add(
                    self._servicer.__state_type_name__, context._state_ref
                )

            # Try to verify the token if a token verifier exists.
            context.auth = await self._maybe_verify_token(
                headers=headers, method='TransactionSetAdjective'
            )

            async with self._state_manager.writer(
                context,
                self._servicer.__state_type__,
                self.tasks_dispatcher,
                transaction=transaction,
                authorize=self._maybe_authorize(
                    method_name='tests.reboot.GreeterMethods.TransactionSetAdjective',
                    headers=context._headers,
                    auth=context.auth,
                    request=request,
                ),
                from_constructor=False,
                requires_constructor=True
            ) as (state, writer):

                task = await GreeterServicerTasks(
                    context=context,
                    state_ref=context._state_ref,
                ).TransactionSetAdjective(
                    GreeterTransactionSetAdjectiveRequestFromProto(request),
                    schedule=context._headers.task_schedule,
                )

                effects = IMPORT_reboot_aio_state_managers.Effects(
                    response=response,
                    state=state,
                    tasks=[task],
                )

                assert effects.tasks is not None

                await writer.complete(effects)

                assert grpc_context is not None

                grpc_context.set_trailing_metadata(
                    grpc_context.trailing_metadata() +
                    (
                        (
                            IMPORT_reboot_aio_headers.TASK_ID_UUID,
                            str(IMPORT_uuid.UUID(bytes=task.task_id.task_uuid))
                        ),
                    )
                )

                return context, response

        return context, response


    # Entrypoint for non-reactive network calls (i.e. typical gRPC calls).
    async def TransactionSetAdjective(
        self,
        request: tests.reboot.greeter_pb2.SetAdjectiveRequest,
        grpc_context: IMPORT_grpc.aio.ServicerContext,
    ) -> tests.reboot.greeter_pb2.SetAdjectiveResponse:
        headers = IMPORT_reboot_aio_headers.Headers.from_grpc_context(grpc_context)
        assert headers.application_id is not None  # Guaranteed by `Headers`.

        # Confirm whether this is the right server to be serving this
        # request.
        try:
            authoritative_server = self.placement_client.server_for_actor(
                headers.application_id,
                headers.state_ref,
            )
        except IMPORT_reboot_aio_placement.UnknownApplicationError:
            # It's possible that the user did indeed type an application ID
            # that doesn't exist, but it's also quite possible that this
            # request reached us before the placement planner had gossipped
            # out the information about which applications exist (we see
            # this e.g. after `rbt dev`'s chaos monkey restarts). For that
            # reason, abort with a retryable error.
            await grpc_context.abort(
                IMPORT_grpc.StatusCode.UNAVAILABLE,
                f"Application '{headers.application_id}' not found. If you "
                "are confident the application exists, this may be because "
                "the system is still starting.",
            )
            raise  # Unreachable but necessary for mypy.
        if authoritative_server != self.server_id:
            # This is NOT the correct server. Fail.
            await grpc_context.abort(
                IMPORT_grpc.StatusCode.UNAVAILABLE,
                f"Server '{self.server_id}' is not authoritative for this "
                f"request; server '{authoritative_server}' is.",
            )
            raise  # Unreachable but necessary for mypy.

        @IMPORT_reboot_aio_internals_middleware.maybe_run_function_twice_to_validate_effects
        async def _run(
            validating_effects: bool,
        ) -> tests.reboot.greeter_pb2.SetAdjectiveResponse:
            context: IMPORT_typing.Optional[IMPORT_reboot_aio_contexts.Context] = None
            try:
                if headers.task_schedule is not None:
                    context, response = await self._schedule_TransactionSetAdjective(
                        headers=headers,
                        request=request,
                        grpc_context=grpc_context,
                    )
                    return response

                context = self.create_context(
                    headers=headers,
                    state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                    method='TransactionSetAdjective',
                    context_type=IMPORT_reboot_aio_contexts.TransactionContext,
                )
                assert context is not None

                return await self._TransactionSetAdjective(
                    request,
                    context,
                    validating_effects=validating_effects,
                    grpc_context=grpc_context,
                )
            except IMPORT_reboot_aio_contexts.EffectValidationRetry:
                # Doing effect validation, just let this propagate.
                raise
            except IMPORT_reboot.aio.aborted.Aborted as aborted:
                status = IMPORT_rpc_status_sync.to_status(aborted.to_status())
                # Need to add transaction participants here because
                # calling `grpc_context.abort_with_status()` will
                # ignore any other trailing metadata. Only propagate
                # transaction participants metadata if the caller cares.
                # Callers that care are those that are themselves transactions.
                # It's important to not just send this information to everyone;
                # some clients can't tolerate trailers, see:
                #   https://github.com/reboot-dev/mono/issues/5081
                if context is not None and headers.transaction_ids is not None:
                    assert context.transaction_id is not None
                    status = status._replace(
                        trailing_metadata=status.trailing_metadata + context.participants.to_grpc_metadata()
                    )
                await grpc_context.abort_with_status(status)
                raise  # Unreachable but necessary for mypy.
            except IMPORT_asyncio.CancelledError:
                # It's pretty normal for an RPC to be cancelled; it's not useful to
                # print a stack trace.
                raise
            except BaseException as exception:
                # Print the exception stack trace for easier debugging. Note
                # that we don't include the stack trace in an error message
                # for the same reason that gRPC doesn't do so by default,
                # see https://github.com/grpc/grpc/issues/14897, but since this
                # should only get logged on the server side it is safe.
                logger.warning(
                    'Unhandled exception\n' +
                    ''.join(IMPORT_traceback.format_exc() if IMPORT_reboot_nodejs_python.should_print_stacktrace() else [f"{type(exception).__name__}: {exception}"])
                )

                # Re-raise the exception for gRPC to handle!
                #
                # TODO: gRPC will print a stack trace from this
                # exception which we don't want if we're executing via
                # Node.js.
                raise
            finally:
                # Propagate transaction participants, if the caller cares.
                # Callers that care are those that are themselves transactions.
                # It's important to not just send this information to everyone;
                # some clients can't tolerate trailers, see:
                #   https://github.com/reboot-dev/mono/issues/5081
                if context is not None and headers.transaction_ids is not None:
                    assert context.transaction_id is not None
                    grpc_context.set_trailing_metadata(
                        grpc_context.trailing_metadata() +
                        context.participants.to_grpc_metadata()
                    )

        with IMPORT_reboot_aio_tracing.context_from_headers(headers):
            return await _run()

    async def __TryToConstructContext(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: tests.reboot.greeter_pb2.Greeter,
        request: google.protobuf.empty_pb2.Empty,
        *,
        validating_effects: bool,
    ) -> google.protobuf.empty_pb2.Empty:
        try:
            typed_state: Greeter.State = GreeterFromProto(state, is_initial_state=(context.state_id in states_being_constructed))

            response = (
                await self._servicer._TryToConstructContext(
                    context=context,
                    state=typed_state,
                    request=request
                )
            )


            GreeterToProto(typed_state, state)

            IMPORT_reboot_aio_types.assert_type(
                response,
                [google.protobuf.empty_pb2.Empty],
            )
            self.maybe_raise_effect_validation_retry(
                logger=logger,
                idempotency_manager=context,
                method_name='Greeter.TryToConstructContext',
                validating_effects=validating_effects,
                context=context,
            )
            return response
        except IMPORT_reboot_aio_contexts.EffectValidationRetry:
            # Doing effect validation, just let this propagate.
            raise
        except IMPORT_reboot.aio.aborted.Aborted as aborted:
            # If the caller aborted due to a retryable error, just
            # propagate the aborted instead of propagating `Unknown`
            # so that a client can transparently retry.
            if IMPORT_reboot.aio.aborted.is_retryable(aborted):
                raise aborted
            # Log any _unhandled_ abort stack traces to make it
            # easier for debugging.
            #
            # NOTE: we don't log if we're a task as it will be logged
            # in `public/reboot/aio/internals/tasks_dispatcher.py` instead.
            aborted_type: IMPORT_typing.Optional[type] = None
            aborted_type = Greeter.TryToConstructContextAborted
            if isinstance(aborted, IMPORT_reboot.aio.aborted.SystemAborted):
                # Not logging when within `node` as we already log there.
                if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                    logger.warning(
                        f"Unhandled (in 'tests.reboot.Greeter.TryToConstructContext') {aborted}; propagating as 'Unknown'\n" +
                        ''.join(IMPORT_traceback.format_exception(aborted))
                    )
                raise IMPORT_reboot.aio.aborted.SystemAborted(
                    IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                    # TODO(benh): consider whether or not we want to
                    # include the 'package.service.method' which may
                    # get concatenated together forming a kind of
                    # "stack trace"; while it's super helpful for
                    # debugging, it does expose implementation
                    # information.
                    message=f"unhandled (in 'tests.reboot.Greeter.TryToConstructContext') {aborted}"
                )
            else:
                if (
                    aborted_type is not None and
                    not isinstance(aborted, aborted_type) and
                    aborted_type.is_declared_error(aborted.error)
                ):
                    # We propagate declared errors that might have
                    # come from another call, i.e., we might have an
                    # `Aborted` but not for this method but the
                    # `Aborted` that we have has an error that this
                    # method declared. This allows a developer to
                    # simply add the declared error to their `.proto`
                    # file rather than having to catch and re-raise
                    # the error with their own aborted type.
                    if context.task is None:
                        logger.warning(
                            f"Propagating unhandled but declared error (in 'tests.reboot.Greeter.TryToConstructContext') {aborted}"
                        )
                elif (
                    aborted_type is None or
                    not isinstance(aborted, aborted_type)
                ):
                    # Not logging when within `node` as we already log there.
                    if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                        logger.warning(
                            f"Unhandled (in 'tests.reboot.Greeter.TryToConstructContext') {aborted}; propagating as 'Unknown'\n" +
                            ''.join(IMPORT_traceback.format_exception(aborted))
                        )
                    # If this wasn't a declared error than we
                    # propagate it as `Unknown`.
                    raise IMPORT_reboot.aio.aborted.SystemAborted(
                        IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                        # TODO(benh): consider whether or not we want to
                        # include the 'package.service.method' which may
                        # get concatenated together forming a kind of
                        # "stack trace"; while it's super helpful for
                        # debugging, it does expose implementation
                        # information.
                        message=f"unhandled (in 'tests.reboot.Greeter.TryToConstructContext') {aborted}"
                    )

            raise
        except IMPORT_asyncio.CancelledError:
            # It's pretty normal for an RPC to be cancelled; it's not useful to
            # print a stack trace.
            raise
        except IMPORT_google_protobuf_message.DecodeError as decode_error:
            # We usually see this error when we are trying to construct a proto
            # message which is too deeply nested: protobuf has a limit of 100
            # nested messages. See the limits here:
            #   https://protobuf.dev/programming-guides/proto-limits/

            if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                logger.warning(
                    "Unhandled (in 'tests.reboot.Greeter.TryToConstructContext') "
                    f"{type(decode_error).__name__}{': ' + str(decode_error) if len(str(decode_error)) > 0 else ''}; "
                    "This is usually caused by a deeply nested protobuf message, which is not supported by protobuf.\n"
                    "See the limits here: https://protobuf.dev/programming-guides/proto-limits/" +
                    ''.join(IMPORT_traceback.format_exception(decode_error))
                )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                message=f"unhandled (in 'tests.reboot.Greeter.TryToConstructContext') {decode_error}; "
                        "This is usually caused by a deeply nested protobuf message, which is not supported by protobuf.\n"
                        "See the limits here: https://protobuf.dev/programming-guides/proto-limits/"
            )
        except BaseException as exception:
            # Not logging when within `node` as we already log there.
            if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                logger.warning(
                    "Unhandled (in 'tests.reboot.Greeter.TryToConstructContext') "
                    f"{type(exception).__name__}{': ' + str(exception) if len(str(exception)) > 0 else ''}; "
                    "propagating as 'Unknown'\n" +
                    ''.join(IMPORT_traceback.format_exception(exception))
                )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                # TODO(benh): consider whether or not we want to
                # include the 'package.service.method' which may
                # get concatenated together forming a kind of
                # "stack trace"; while it's super helpful for
                # debugging, it does expose implementation
                # information.
                message=f"unhandled (in 'tests.reboot.Greeter.TryToConstructContext') {type(exception).__name__}: {exception}"
            )
        finally:
            pass

    @IMPORT_reboot_aio_tracing.function_span(
        # We expect an `EffectValidationRetry` exception; that's not an error.
        set_status_on_exception=False
    )
    async def _TryToConstructContext(
        self,
        request: google.protobuf.empty_pb2.Empty,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        *,
        validating_effects: bool,
        grpc_context: IMPORT_typing.Optional[IMPORT_grpc.aio.ServicerContext] = None,
    ) -> google.protobuf.empty_pb2.Empty:
        # Try to verify the token if a token verifier exists.
        context.auth = await self._maybe_verify_token(
            headers=context._headers, method='TryToConstructContext'
        )


        async with self._state_manager.transactionally(
            context,
            self.tasks_dispatcher,
            aborted_type=Greeter.TryToConstructContextAborted,
        ) as transaction:
            if transaction is not None:
                context.participants.add(
                    self._servicer.__state_type_name__, context._state_ref
                )
            authorizer = self._maybe_authorize(
                method_name='tests.reboot.GreeterMethods.TryToConstructContext',
                headers=context._headers,
                auth=context.auth,
                request=request,
            )
            async with self._state_manager.reader(
                context,
                self._servicer.__state_type__,
                authorize=authorizer,
            ) as state:
                response = await self.__TryToConstructContext(
                    context,
                    state,
                    request,
                    validating_effects=validating_effects,
                )
                return response

    async def _schedule_TryToConstructContext(
        self,
        *,
        request: google.protobuf.empty_pb2.Empty,
        headers: IMPORT_reboot_aio_headers.Headers,
        grpc_context: IMPORT_grpc.aio.ServicerContext,
    ) -> tuple[IMPORT_reboot_aio_contexts.WriterContext, google.protobuf.empty_pb2.Empty]:
        context: IMPORT_reboot_aio_contexts.WriterContext = self.create_context(
            headers=headers,
            state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            method='TryToConstructContext',
            context_type=IMPORT_reboot_aio_contexts.WriterContext,
        )
        response = google.protobuf.empty_pb2.Empty()

        # Check if we already have performed this schedule! Note that
        # we need to do this for all kinds of methods because this is
        # effectively a mutation (actually a `writer`, see below).
        #
        # We do this _before_ calling 'transactionally()' because
        # if this call is for a transaction method _and_ we've
        # already performed the transaction then we don't want to
        # become a transaction participant (again) we just want to
        # return the transaction's response.
        idempotent_mutation = await self._state_manager.check_for_idempotent_mutation(
            context
        )

        if idempotent_mutation is not None:
            response.ParseFromString(idempotent_mutation.response)

            # We should have only scheduled a single task!
            assert len(idempotent_mutation.task_ids) == 1
            assert grpc_context is not None
            grpc_context.set_trailing_metadata(
                grpc_context.trailing_metadata() +
                (
                    (
                        IMPORT_reboot_aio_headers.TASK_ID_UUID,
                        str(IMPORT_uuid.UUID(bytes=idempotent_mutation.task_ids[0].task_uuid))
                    ),
                )
            )

            return context, response

        async with self._state_manager.transactionally(
            context,
            self.tasks_dispatcher,
            aborted_type=Greeter.TryToConstructContextAborted,
        ) as transaction:
            if transaction is not None:
                context.participants.add(
                    self._servicer.__state_type_name__, context._state_ref
                )

            # Try to verify the token if a token verifier exists.
            context.auth = await self._maybe_verify_token(
                headers=headers, method='TryToConstructContext'
            )

            async with self._state_manager.writer(
                context,
                self._servicer.__state_type__,
                self.tasks_dispatcher,
                transaction=transaction,
                authorize=self._maybe_authorize(
                    method_name='tests.reboot.GreeterMethods.TryToConstructContext',
                    headers=context._headers,
                    auth=context.auth,
                    request=request,
                ),
                from_constructor=False,
                requires_constructor=True
            ) as (state, writer):

                task = await GreeterServicerTasks(
                    context=context,
                    state_ref=context._state_ref,
                ).TryToConstructContext(
                    GreeterTryToConstructContextRequestFromProto(request),
                    schedule=context._headers.task_schedule,
                )

                effects = IMPORT_reboot_aio_state_managers.Effects(
                    response=response,
                    state=state,
                    tasks=[task],
                )

                assert effects.tasks is not None

                await writer.complete(effects)

                assert grpc_context is not None

                grpc_context.set_trailing_metadata(
                    grpc_context.trailing_metadata() +
                    (
                        (
                            IMPORT_reboot_aio_headers.TASK_ID_UUID,
                            str(IMPORT_uuid.UUID(bytes=task.task_id.task_uuid))
                        ),
                    )
                )

                return context, response

        return context, response


    # Entrypoint for non-reactive network calls (i.e. typical gRPC calls).
    async def TryToConstructContext(
        self,
        request: google.protobuf.empty_pb2.Empty,
        grpc_context: IMPORT_grpc.aio.ServicerContext,
    ) -> google.protobuf.empty_pb2.Empty:
        headers = IMPORT_reboot_aio_headers.Headers.from_grpc_context(grpc_context)
        assert headers.application_id is not None  # Guaranteed by `Headers`.

        # Confirm whether this is the right server to be serving this
        # request.
        try:
            authoritative_server = self.placement_client.server_for_actor(
                headers.application_id,
                headers.state_ref,
            )
        except IMPORT_reboot_aio_placement.UnknownApplicationError:
            # It's possible that the user did indeed type an application ID
            # that doesn't exist, but it's also quite possible that this
            # request reached us before the placement planner had gossipped
            # out the information about which applications exist (we see
            # this e.g. after `rbt dev`'s chaos monkey restarts). For that
            # reason, abort with a retryable error.
            await grpc_context.abort(
                IMPORT_grpc.StatusCode.UNAVAILABLE,
                f"Application '{headers.application_id}' not found. If you "
                "are confident the application exists, this may be because "
                "the system is still starting.",
            )
            raise  # Unreachable but necessary for mypy.
        if authoritative_server != self.server_id:
            # This is NOT the correct server. Fail.
            await grpc_context.abort(
                IMPORT_grpc.StatusCode.UNAVAILABLE,
                f"Server '{self.server_id}' is not authoritative for this "
                f"request; server '{authoritative_server}' is.",
            )
            raise  # Unreachable but necessary for mypy.

        @IMPORT_reboot_aio_internals_middleware.maybe_run_function_twice_to_validate_effects
        async def _run(
            validating_effects: bool,
        ) -> google.protobuf.empty_pb2.Empty:
            context: IMPORT_typing.Optional[IMPORT_reboot_aio_contexts.Context] = None
            try:
                if headers.task_schedule is not None:
                    context, response = await self._schedule_TryToConstructContext(
                        headers=headers,
                        request=request,
                        grpc_context=grpc_context,
                    )
                    return response

                context = self.create_context(
                    headers=headers,
                    state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                    method='TryToConstructContext',
                    context_type=IMPORT_reboot_aio_contexts.ReaderContext,
                )
                assert context is not None

                return await self._TryToConstructContext(
                    request,
                    context,
                    validating_effects=validating_effects,
                    grpc_context=grpc_context,
                )
            except IMPORT_reboot_aio_contexts.EffectValidationRetry:
                # Doing effect validation, just let this propagate.
                raise
            except IMPORT_reboot.aio.aborted.Aborted as aborted:
                status = IMPORT_rpc_status_sync.to_status(aborted.to_status())
                # Need to add transaction participants here because
                # calling `grpc_context.abort_with_status()` will
                # ignore any other trailing metadata. Only propagate
                # transaction participants metadata if the caller cares.
                # Callers that care are those that are themselves transactions.
                # It's important to not just send this information to everyone;
                # some clients can't tolerate trailers, see:
                #   https://github.com/reboot-dev/mono/issues/5081
                if context is not None and headers.transaction_ids is not None:
                    assert context.transaction_id is not None
                    status = status._replace(
                        trailing_metadata=status.trailing_metadata + context.participants.to_grpc_metadata()
                    )
                await grpc_context.abort_with_status(status)
                raise  # Unreachable but necessary for mypy.
            except IMPORT_asyncio.CancelledError:
                # It's pretty normal for an RPC to be cancelled; it's not useful to
                # print a stack trace.
                raise
            except BaseException as exception:
                # Print the exception stack trace for easier debugging. Note
                # that we don't include the stack trace in an error message
                # for the same reason that gRPC doesn't do so by default,
                # see https://github.com/grpc/grpc/issues/14897, but since this
                # should only get logged on the server side it is safe.
                logger.warning(
                    'Unhandled exception\n' +
                    ''.join(IMPORT_traceback.format_exc() if IMPORT_reboot_nodejs_python.should_print_stacktrace() else [f"{type(exception).__name__}: {exception}"])
                )

                # Re-raise the exception for gRPC to handle!
                #
                # TODO: gRPC will print a stack trace from this
                # exception which we don't want if we're executing via
                # Node.js.
                raise
            finally:
                # Propagate transaction participants, if the caller cares.
                # Callers that care are those that are themselves transactions.
                # It's important to not just send this information to everyone;
                # some clients can't tolerate trailers, see:
                #   https://github.com/reboot-dev/mono/issues/5081
                if context is not None and headers.transaction_ids is not None:
                    assert context.transaction_id is not None
                    grpc_context.set_trailing_metadata(
                        grpc_context.trailing_metadata() +
                        context.participants.to_grpc_metadata()
                    )

        with IMPORT_reboot_aio_tracing.context_from_headers(headers):
            return await _run()

    async def __TryToConstructExternalContext(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: tests.reboot.greeter_pb2.Greeter,
        request: google.protobuf.empty_pb2.Empty,
        *,
        validating_effects: bool,
    ) -> google.protobuf.empty_pb2.Empty:
        try:
            typed_state: Greeter.State = GreeterFromProto(state, is_initial_state=(context.state_id in states_being_constructed))

            response = (
                await self._servicer._TryToConstructExternalContext(
                    context=context,
                    state=typed_state,
                    request=request
                )
            )


            GreeterToProto(typed_state, state)

            IMPORT_reboot_aio_types.assert_type(
                response,
                [google.protobuf.empty_pb2.Empty],
            )
            self.maybe_raise_effect_validation_retry(
                logger=logger,
                idempotency_manager=context,
                method_name='Greeter.TryToConstructExternalContext',
                validating_effects=validating_effects,
                context=context,
            )
            return response
        except IMPORT_reboot_aio_contexts.EffectValidationRetry:
            # Doing effect validation, just let this propagate.
            raise
        except IMPORT_reboot.aio.aborted.Aborted as aborted:
            # If the caller aborted due to a retryable error, just
            # propagate the aborted instead of propagating `Unknown`
            # so that a client can transparently retry.
            if IMPORT_reboot.aio.aborted.is_retryable(aborted):
                raise aborted
            # Log any _unhandled_ abort stack traces to make it
            # easier for debugging.
            #
            # NOTE: we don't log if we're a task as it will be logged
            # in `public/reboot/aio/internals/tasks_dispatcher.py` instead.
            aborted_type: IMPORT_typing.Optional[type] = None
            aborted_type = Greeter.TryToConstructExternalContextAborted
            if isinstance(aborted, IMPORT_reboot.aio.aborted.SystemAborted):
                # Not logging when within `node` as we already log there.
                if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                    logger.warning(
                        f"Unhandled (in 'tests.reboot.Greeter.TryToConstructExternalContext') {aborted}; propagating as 'Unknown'\n" +
                        ''.join(IMPORT_traceback.format_exception(aborted))
                    )
                raise IMPORT_reboot.aio.aborted.SystemAborted(
                    IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                    # TODO(benh): consider whether or not we want to
                    # include the 'package.service.method' which may
                    # get concatenated together forming a kind of
                    # "stack trace"; while it's super helpful for
                    # debugging, it does expose implementation
                    # information.
                    message=f"unhandled (in 'tests.reboot.Greeter.TryToConstructExternalContext') {aborted}"
                )
            else:
                if (
                    aborted_type is not None and
                    not isinstance(aborted, aborted_type) and
                    aborted_type.is_declared_error(aborted.error)
                ):
                    # We propagate declared errors that might have
                    # come from another call, i.e., we might have an
                    # `Aborted` but not for this method but the
                    # `Aborted` that we have has an error that this
                    # method declared. This allows a developer to
                    # simply add the declared error to their `.proto`
                    # file rather than having to catch and re-raise
                    # the error with their own aborted type.
                    if context.task is None:
                        logger.warning(
                            f"Propagating unhandled but declared error (in 'tests.reboot.Greeter.TryToConstructExternalContext') {aborted}"
                        )
                elif (
                    aborted_type is None or
                    not isinstance(aborted, aborted_type)
                ):
                    # Not logging when within `node` as we already log there.
                    if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                        logger.warning(
                            f"Unhandled (in 'tests.reboot.Greeter.TryToConstructExternalContext') {aborted}; propagating as 'Unknown'\n" +
                            ''.join(IMPORT_traceback.format_exception(aborted))
                        )
                    # If this wasn't a declared error than we
                    # propagate it as `Unknown`.
                    raise IMPORT_reboot.aio.aborted.SystemAborted(
                        IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                        # TODO(benh): consider whether or not we want to
                        # include the 'package.service.method' which may
                        # get concatenated together forming a kind of
                        # "stack trace"; while it's super helpful for
                        # debugging, it does expose implementation
                        # information.
                        message=f"unhandled (in 'tests.reboot.Greeter.TryToConstructExternalContext') {aborted}"
                    )

            raise
        except IMPORT_asyncio.CancelledError:
            # It's pretty normal for an RPC to be cancelled; it's not useful to
            # print a stack trace.
            raise
        except IMPORT_google_protobuf_message.DecodeError as decode_error:
            # We usually see this error when we are trying to construct a proto
            # message which is too deeply nested: protobuf has a limit of 100
            # nested messages. See the limits here:
            #   https://protobuf.dev/programming-guides/proto-limits/

            if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                logger.warning(
                    "Unhandled (in 'tests.reboot.Greeter.TryToConstructExternalContext') "
                    f"{type(decode_error).__name__}{': ' + str(decode_error) if len(str(decode_error)) > 0 else ''}; "
                    "This is usually caused by a deeply nested protobuf message, which is not supported by protobuf.\n"
                    "See the limits here: https://protobuf.dev/programming-guides/proto-limits/" +
                    ''.join(IMPORT_traceback.format_exception(decode_error))
                )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                message=f"unhandled (in 'tests.reboot.Greeter.TryToConstructExternalContext') {decode_error}; "
                        "This is usually caused by a deeply nested protobuf message, which is not supported by protobuf.\n"
                        "See the limits here: https://protobuf.dev/programming-guides/proto-limits/"
            )
        except BaseException as exception:
            # Not logging when within `node` as we already log there.
            if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                logger.warning(
                    "Unhandled (in 'tests.reboot.Greeter.TryToConstructExternalContext') "
                    f"{type(exception).__name__}{': ' + str(exception) if len(str(exception)) > 0 else ''}; "
                    "propagating as 'Unknown'\n" +
                    ''.join(IMPORT_traceback.format_exception(exception))
                )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                # TODO(benh): consider whether or not we want to
                # include the 'package.service.method' which may
                # get concatenated together forming a kind of
                # "stack trace"; while it's super helpful for
                # debugging, it does expose implementation
                # information.
                message=f"unhandled (in 'tests.reboot.Greeter.TryToConstructExternalContext') {type(exception).__name__}: {exception}"
            )
        finally:
            pass

    @IMPORT_reboot_aio_tracing.function_span(
        # We expect an `EffectValidationRetry` exception; that's not an error.
        set_status_on_exception=False
    )
    async def _TryToConstructExternalContext(
        self,
        request: google.protobuf.empty_pb2.Empty,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        *,
        validating_effects: bool,
        grpc_context: IMPORT_typing.Optional[IMPORT_grpc.aio.ServicerContext] = None,
    ) -> google.protobuf.empty_pb2.Empty:
        # Try to verify the token if a token verifier exists.
        context.auth = await self._maybe_verify_token(
            headers=context._headers, method='TryToConstructExternalContext'
        )


        async with self._state_manager.transactionally(
            context,
            self.tasks_dispatcher,
            aborted_type=Greeter.TryToConstructExternalContextAborted,
        ) as transaction:
            if transaction is not None:
                context.participants.add(
                    self._servicer.__state_type_name__, context._state_ref
                )
            authorizer = self._maybe_authorize(
                method_name='tests.reboot.GreeterMethods.TryToConstructExternalContext',
                headers=context._headers,
                auth=context.auth,
                request=request,
            )
            async with self._state_manager.reader(
                context,
                self._servicer.__state_type__,
                authorize=authorizer,
            ) as state:
                response = await self.__TryToConstructExternalContext(
                    context,
                    state,
                    request,
                    validating_effects=validating_effects,
                )
                return response

    async def _schedule_TryToConstructExternalContext(
        self,
        *,
        request: google.protobuf.empty_pb2.Empty,
        headers: IMPORT_reboot_aio_headers.Headers,
        grpc_context: IMPORT_grpc.aio.ServicerContext,
    ) -> tuple[IMPORT_reboot_aio_contexts.WriterContext, google.protobuf.empty_pb2.Empty]:
        context: IMPORT_reboot_aio_contexts.WriterContext = self.create_context(
            headers=headers,
            state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            method='TryToConstructExternalContext',
            context_type=IMPORT_reboot_aio_contexts.WriterContext,
        )
        response = google.protobuf.empty_pb2.Empty()

        # Check if we already have performed this schedule! Note that
        # we need to do this for all kinds of methods because this is
        # effectively a mutation (actually a `writer`, see below).
        #
        # We do this _before_ calling 'transactionally()' because
        # if this call is for a transaction method _and_ we've
        # already performed the transaction then we don't want to
        # become a transaction participant (again) we just want to
        # return the transaction's response.
        idempotent_mutation = await self._state_manager.check_for_idempotent_mutation(
            context
        )

        if idempotent_mutation is not None:
            response.ParseFromString(idempotent_mutation.response)

            # We should have only scheduled a single task!
            assert len(idempotent_mutation.task_ids) == 1
            assert grpc_context is not None
            grpc_context.set_trailing_metadata(
                grpc_context.trailing_metadata() +
                (
                    (
                        IMPORT_reboot_aio_headers.TASK_ID_UUID,
                        str(IMPORT_uuid.UUID(bytes=idempotent_mutation.task_ids[0].task_uuid))
                    ),
                )
            )

            return context, response

        async with self._state_manager.transactionally(
            context,
            self.tasks_dispatcher,
            aborted_type=Greeter.TryToConstructExternalContextAborted,
        ) as transaction:
            if transaction is not None:
                context.participants.add(
                    self._servicer.__state_type_name__, context._state_ref
                )

            # Try to verify the token if a token verifier exists.
            context.auth = await self._maybe_verify_token(
                headers=headers, method='TryToConstructExternalContext'
            )

            async with self._state_manager.writer(
                context,
                self._servicer.__state_type__,
                self.tasks_dispatcher,
                transaction=transaction,
                authorize=self._maybe_authorize(
                    method_name='tests.reboot.GreeterMethods.TryToConstructExternalContext',
                    headers=context._headers,
                    auth=context.auth,
                    request=request,
                ),
                from_constructor=False,
                requires_constructor=True
            ) as (state, writer):

                task = await GreeterServicerTasks(
                    context=context,
                    state_ref=context._state_ref,
                ).TryToConstructExternalContext(
                    GreeterTryToConstructExternalContextRequestFromProto(request),
                    schedule=context._headers.task_schedule,
                )

                effects = IMPORT_reboot_aio_state_managers.Effects(
                    response=response,
                    state=state,
                    tasks=[task],
                )

                assert effects.tasks is not None

                await writer.complete(effects)

                assert grpc_context is not None

                grpc_context.set_trailing_metadata(
                    grpc_context.trailing_metadata() +
                    (
                        (
                            IMPORT_reboot_aio_headers.TASK_ID_UUID,
                            str(IMPORT_uuid.UUID(bytes=task.task_id.task_uuid))
                        ),
                    )
                )

                return context, response

        return context, response


    # Entrypoint for non-reactive network calls (i.e. typical gRPC calls).
    async def TryToConstructExternalContext(
        self,
        request: google.protobuf.empty_pb2.Empty,
        grpc_context: IMPORT_grpc.aio.ServicerContext,
    ) -> google.protobuf.empty_pb2.Empty:
        headers = IMPORT_reboot_aio_headers.Headers.from_grpc_context(grpc_context)
        assert headers.application_id is not None  # Guaranteed by `Headers`.

        # Confirm whether this is the right server to be serving this
        # request.
        try:
            authoritative_server = self.placement_client.server_for_actor(
                headers.application_id,
                headers.state_ref,
            )
        except IMPORT_reboot_aio_placement.UnknownApplicationError:
            # It's possible that the user did indeed type an application ID
            # that doesn't exist, but it's also quite possible that this
            # request reached us before the placement planner had gossipped
            # out the information about which applications exist (we see
            # this e.g. after `rbt dev`'s chaos monkey restarts). For that
            # reason, abort with a retryable error.
            await grpc_context.abort(
                IMPORT_grpc.StatusCode.UNAVAILABLE,
                f"Application '{headers.application_id}' not found. If you "
                "are confident the application exists, this may be because "
                "the system is still starting.",
            )
            raise  # Unreachable but necessary for mypy.
        if authoritative_server != self.server_id:
            # This is NOT the correct server. Fail.
            await grpc_context.abort(
                IMPORT_grpc.StatusCode.UNAVAILABLE,
                f"Server '{self.server_id}' is not authoritative for this "
                f"request; server '{authoritative_server}' is.",
            )
            raise  # Unreachable but necessary for mypy.

        @IMPORT_reboot_aio_internals_middleware.maybe_run_function_twice_to_validate_effects
        async def _run(
            validating_effects: bool,
        ) -> google.protobuf.empty_pb2.Empty:
            context: IMPORT_typing.Optional[IMPORT_reboot_aio_contexts.Context] = None
            try:
                if headers.task_schedule is not None:
                    context, response = await self._schedule_TryToConstructExternalContext(
                        headers=headers,
                        request=request,
                        grpc_context=grpc_context,
                    )
                    return response

                context = self.create_context(
                    headers=headers,
                    state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                    method='TryToConstructExternalContext',
                    context_type=IMPORT_reboot_aio_contexts.ReaderContext,
                )
                assert context is not None

                return await self._TryToConstructExternalContext(
                    request,
                    context,
                    validating_effects=validating_effects,
                    grpc_context=grpc_context,
                )
            except IMPORT_reboot_aio_contexts.EffectValidationRetry:
                # Doing effect validation, just let this propagate.
                raise
            except IMPORT_reboot.aio.aborted.Aborted as aborted:
                status = IMPORT_rpc_status_sync.to_status(aborted.to_status())
                # Need to add transaction participants here because
                # calling `grpc_context.abort_with_status()` will
                # ignore any other trailing metadata. Only propagate
                # transaction participants metadata if the caller cares.
                # Callers that care are those that are themselves transactions.
                # It's important to not just send this information to everyone;
                # some clients can't tolerate trailers, see:
                #   https://github.com/reboot-dev/mono/issues/5081
                if context is not None and headers.transaction_ids is not None:
                    assert context.transaction_id is not None
                    status = status._replace(
                        trailing_metadata=status.trailing_metadata + context.participants.to_grpc_metadata()
                    )
                await grpc_context.abort_with_status(status)
                raise  # Unreachable but necessary for mypy.
            except IMPORT_asyncio.CancelledError:
                # It's pretty normal for an RPC to be cancelled; it's not useful to
                # print a stack trace.
                raise
            except BaseException as exception:
                # Print the exception stack trace for easier debugging. Note
                # that we don't include the stack trace in an error message
                # for the same reason that gRPC doesn't do so by default,
                # see https://github.com/grpc/grpc/issues/14897, but since this
                # should only get logged on the server side it is safe.
                logger.warning(
                    'Unhandled exception\n' +
                    ''.join(IMPORT_traceback.format_exc() if IMPORT_reboot_nodejs_python.should_print_stacktrace() else [f"{type(exception).__name__}: {exception}"])
                )

                # Re-raise the exception for gRPC to handle!
                #
                # TODO: gRPC will print a stack trace from this
                # exception which we don't want if we're executing via
                # Node.js.
                raise
            finally:
                # Propagate transaction participants, if the caller cares.
                # Callers that care are those that are themselves transactions.
                # It's important to not just send this information to everyone;
                # some clients can't tolerate trailers, see:
                #   https://github.com/reboot-dev/mono/issues/5081
                if context is not None and headers.transaction_ids is not None:
                    assert context.transaction_id is not None
                    grpc_context.set_trailing_metadata(
                        grpc_context.trailing_metadata() +
                        context.participants.to_grpc_metadata()
                    )

        with IMPORT_reboot_aio_tracing.context_from_headers(headers):
            return await _run()

    async def __TestLongRunningFetch(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: tests.reboot.greeter_pb2.Greeter,
        request: tests.reboot.greeter_pb2.TestLongRunningFetchRequest,
        *,
        validating_effects: bool,
    ) -> google.protobuf.empty_pb2.Empty:
        try:
            typed_state: Greeter.State = GreeterFromProto(state, is_initial_state=(context.state_id in states_being_constructed))

            response = (
                await self._servicer._TestLongRunningFetch(
                    context=context,
                    state=typed_state,
                    request=request
                )
            )


            GreeterToProto(typed_state, state)

            IMPORT_reboot_aio_types.assert_type(
                response,
                [google.protobuf.empty_pb2.Empty],
            )
            self.maybe_raise_effect_validation_retry(
                logger=logger,
                idempotency_manager=context,
                method_name='Greeter.TestLongRunningFetch',
                validating_effects=validating_effects,
                context=context,
            )
            return response
        except IMPORT_reboot_aio_contexts.EffectValidationRetry:
            # Doing effect validation, just let this propagate.
            raise
        except IMPORT_reboot.aio.aborted.Aborted as aborted:
            # If the caller aborted due to a retryable error, just
            # propagate the aborted instead of propagating `Unknown`
            # so that a client can transparently retry.
            if IMPORT_reboot.aio.aborted.is_retryable(aborted):
                raise aborted
            # Log any _unhandled_ abort stack traces to make it
            # easier for debugging.
            #
            # NOTE: we don't log if we're a task as it will be logged
            # in `public/reboot/aio/internals/tasks_dispatcher.py` instead.
            aborted_type: IMPORT_typing.Optional[type] = None
            aborted_type = Greeter.TestLongRunningFetchAborted
            if isinstance(aborted, IMPORT_reboot.aio.aborted.SystemAborted):
                # Not logging when within `node` as we already log there.
                if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                    logger.warning(
                        f"Unhandled (in 'tests.reboot.Greeter.TestLongRunningFetch') {aborted}; propagating as 'Unknown'\n" +
                        ''.join(IMPORT_traceback.format_exception(aborted))
                    )
                raise IMPORT_reboot.aio.aborted.SystemAborted(
                    IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                    # TODO(benh): consider whether or not we want to
                    # include the 'package.service.method' which may
                    # get concatenated together forming a kind of
                    # "stack trace"; while it's super helpful for
                    # debugging, it does expose implementation
                    # information.
                    message=f"unhandled (in 'tests.reboot.Greeter.TestLongRunningFetch') {aborted}"
                )
            else:
                if (
                    aborted_type is not None and
                    not isinstance(aborted, aborted_type) and
                    aborted_type.is_declared_error(aborted.error)
                ):
                    # We propagate declared errors that might have
                    # come from another call, i.e., we might have an
                    # `Aborted` but not for this method but the
                    # `Aborted` that we have has an error that this
                    # method declared. This allows a developer to
                    # simply add the declared error to their `.proto`
                    # file rather than having to catch and re-raise
                    # the error with their own aborted type.
                    if context.task is None:
                        logger.warning(
                            f"Propagating unhandled but declared error (in 'tests.reboot.Greeter.TestLongRunningFetch') {aborted}"
                        )
                elif (
                    aborted_type is None or
                    not isinstance(aborted, aborted_type)
                ):
                    # Not logging when within `node` as we already log there.
                    if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                        logger.warning(
                            f"Unhandled (in 'tests.reboot.Greeter.TestLongRunningFetch') {aborted}; propagating as 'Unknown'\n" +
                            ''.join(IMPORT_traceback.format_exception(aborted))
                        )
                    # If this wasn't a declared error than we
                    # propagate it as `Unknown`.
                    raise IMPORT_reboot.aio.aborted.SystemAborted(
                        IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                        # TODO(benh): consider whether or not we want to
                        # include the 'package.service.method' which may
                        # get concatenated together forming a kind of
                        # "stack trace"; while it's super helpful for
                        # debugging, it does expose implementation
                        # information.
                        message=f"unhandled (in 'tests.reboot.Greeter.TestLongRunningFetch') {aborted}"
                    )

            raise
        except IMPORT_asyncio.CancelledError:
            # It's pretty normal for an RPC to be cancelled; it's not useful to
            # print a stack trace.
            raise
        except IMPORT_google_protobuf_message.DecodeError as decode_error:
            # We usually see this error when we are trying to construct a proto
            # message which is too deeply nested: protobuf has a limit of 100
            # nested messages. See the limits here:
            #   https://protobuf.dev/programming-guides/proto-limits/

            if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                logger.warning(
                    "Unhandled (in 'tests.reboot.Greeter.TestLongRunningFetch') "
                    f"{type(decode_error).__name__}{': ' + str(decode_error) if len(str(decode_error)) > 0 else ''}; "
                    "This is usually caused by a deeply nested protobuf message, which is not supported by protobuf.\n"
                    "See the limits here: https://protobuf.dev/programming-guides/proto-limits/" +
                    ''.join(IMPORT_traceback.format_exception(decode_error))
                )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                message=f"unhandled (in 'tests.reboot.Greeter.TestLongRunningFetch') {decode_error}; "
                        "This is usually caused by a deeply nested protobuf message, which is not supported by protobuf.\n"
                        "See the limits here: https://protobuf.dev/programming-guides/proto-limits/"
            )
        except BaseException as exception:
            # Not logging when within `node` as we already log there.
            if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                logger.warning(
                    "Unhandled (in 'tests.reboot.Greeter.TestLongRunningFetch') "
                    f"{type(exception).__name__}{': ' + str(exception) if len(str(exception)) > 0 else ''}; "
                    "propagating as 'Unknown'\n" +
                    ''.join(IMPORT_traceback.format_exception(exception))
                )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                # TODO(benh): consider whether or not we want to
                # include the 'package.service.method' which may
                # get concatenated together forming a kind of
                # "stack trace"; while it's super helpful for
                # debugging, it does expose implementation
                # information.
                message=f"unhandled (in 'tests.reboot.Greeter.TestLongRunningFetch') {type(exception).__name__}: {exception}"
            )
        finally:
            pass

    @IMPORT_reboot_aio_tracing.function_span(
        # We expect an `EffectValidationRetry` exception; that's not an error.
        set_status_on_exception=False
    )
    async def _TestLongRunningFetch(
        self,
        request: tests.reboot.greeter_pb2.TestLongRunningFetchRequest,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        *,
        validating_effects: bool,
        grpc_context: IMPORT_typing.Optional[IMPORT_grpc.aio.ServicerContext] = None,
    ) -> google.protobuf.empty_pb2.Empty:
        # Try to verify the token if a token verifier exists.
        context.auth = await self._maybe_verify_token(
            headers=context._headers, method='TestLongRunningFetch'
        )


        async with self._state_manager.transactionally(
            context,
            self.tasks_dispatcher,
            aborted_type=Greeter.TestLongRunningFetchAborted,
        ) as transaction:
            if transaction is not None:
                context.participants.add(
                    self._servicer.__state_type_name__, context._state_ref
                )
            authorizer = self._maybe_authorize(
                method_name='tests.reboot.GreeterMethods.TestLongRunningFetch',
                headers=context._headers,
                auth=context.auth,
                request=request,
            )
            async with self._state_manager.reader(
                context,
                self._servicer.__state_type__,
                authorize=authorizer,
            ) as state:
                response = await self.__TestLongRunningFetch(
                    context,
                    state,
                    request,
                    validating_effects=validating_effects,
                )
                return response

    async def _schedule_TestLongRunningFetch(
        self,
        *,
        request: tests.reboot.greeter_pb2.TestLongRunningFetchRequest,
        headers: IMPORT_reboot_aio_headers.Headers,
        grpc_context: IMPORT_grpc.aio.ServicerContext,
    ) -> tuple[IMPORT_reboot_aio_contexts.WriterContext, google.protobuf.empty_pb2.Empty]:
        context: IMPORT_reboot_aio_contexts.WriterContext = self.create_context(
            headers=headers,
            state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            method='TestLongRunningFetch',
            context_type=IMPORT_reboot_aio_contexts.WriterContext,
        )
        response = google.protobuf.empty_pb2.Empty()

        # Check if we already have performed this schedule! Note that
        # we need to do this for all kinds of methods because this is
        # effectively a mutation (actually a `writer`, see below).
        #
        # We do this _before_ calling 'transactionally()' because
        # if this call is for a transaction method _and_ we've
        # already performed the transaction then we don't want to
        # become a transaction participant (again) we just want to
        # return the transaction's response.
        idempotent_mutation = await self._state_manager.check_for_idempotent_mutation(
            context
        )

        if idempotent_mutation is not None:
            response.ParseFromString(idempotent_mutation.response)

            # We should have only scheduled a single task!
            assert len(idempotent_mutation.task_ids) == 1
            assert grpc_context is not None
            grpc_context.set_trailing_metadata(
                grpc_context.trailing_metadata() +
                (
                    (
                        IMPORT_reboot_aio_headers.TASK_ID_UUID,
                        str(IMPORT_uuid.UUID(bytes=idempotent_mutation.task_ids[0].task_uuid))
                    ),
                )
            )

            return context, response

        async with self._state_manager.transactionally(
            context,
            self.tasks_dispatcher,
            aborted_type=Greeter.TestLongRunningFetchAborted,
        ) as transaction:
            if transaction is not None:
                context.participants.add(
                    self._servicer.__state_type_name__, context._state_ref
                )

            # Try to verify the token if a token verifier exists.
            context.auth = await self._maybe_verify_token(
                headers=headers, method='TestLongRunningFetch'
            )

            async with self._state_manager.writer(
                context,
                self._servicer.__state_type__,
                self.tasks_dispatcher,
                transaction=transaction,
                authorize=self._maybe_authorize(
                    method_name='tests.reboot.GreeterMethods.TestLongRunningFetch',
                    headers=context._headers,
                    auth=context.auth,
                    request=request,
                ),
                from_constructor=False,
                requires_constructor=True
            ) as (state, writer):

                task = await GreeterServicerTasks(
                    context=context,
                    state_ref=context._state_ref,
                ).TestLongRunningFetch(
                    GreeterTestLongRunningFetchRequestFromProto(request),
                    schedule=context._headers.task_schedule,
                )

                effects = IMPORT_reboot_aio_state_managers.Effects(
                    response=response,
                    state=state,
                    tasks=[task],
                )

                assert effects.tasks is not None

                await writer.complete(effects)

                assert grpc_context is not None

                grpc_context.set_trailing_metadata(
                    grpc_context.trailing_metadata() +
                    (
                        (
                            IMPORT_reboot_aio_headers.TASK_ID_UUID,
                            str(IMPORT_uuid.UUID(bytes=task.task_id.task_uuid))
                        ),
                    )
                )

                return context, response

        return context, response


    # Entrypoint for non-reactive network calls (i.e. typical gRPC calls).
    async def TestLongRunningFetch(
        self,
        request: tests.reboot.greeter_pb2.TestLongRunningFetchRequest,
        grpc_context: IMPORT_grpc.aio.ServicerContext,
    ) -> google.protobuf.empty_pb2.Empty:
        headers = IMPORT_reboot_aio_headers.Headers.from_grpc_context(grpc_context)
        assert headers.application_id is not None  # Guaranteed by `Headers`.

        # Confirm whether this is the right server to be serving this
        # request.
        try:
            authoritative_server = self.placement_client.server_for_actor(
                headers.application_id,
                headers.state_ref,
            )
        except IMPORT_reboot_aio_placement.UnknownApplicationError:
            # It's possible that the user did indeed type an application ID
            # that doesn't exist, but it's also quite possible that this
            # request reached us before the placement planner had gossipped
            # out the information about which applications exist (we see
            # this e.g. after `rbt dev`'s chaos monkey restarts). For that
            # reason, abort with a retryable error.
            await grpc_context.abort(
                IMPORT_grpc.StatusCode.UNAVAILABLE,
                f"Application '{headers.application_id}' not found. If you "
                "are confident the application exists, this may be because "
                "the system is still starting.",
            )
            raise  # Unreachable but necessary for mypy.
        if authoritative_server != self.server_id:
            # This is NOT the correct server. Fail.
            await grpc_context.abort(
                IMPORT_grpc.StatusCode.UNAVAILABLE,
                f"Server '{self.server_id}' is not authoritative for this "
                f"request; server '{authoritative_server}' is.",
            )
            raise  # Unreachable but necessary for mypy.

        @IMPORT_reboot_aio_internals_middleware.maybe_run_function_twice_to_validate_effects
        async def _run(
            validating_effects: bool,
        ) -> google.protobuf.empty_pb2.Empty:
            context: IMPORT_typing.Optional[IMPORT_reboot_aio_contexts.Context] = None
            try:
                if headers.task_schedule is not None:
                    context, response = await self._schedule_TestLongRunningFetch(
                        headers=headers,
                        request=request,
                        grpc_context=grpc_context,
                    )
                    return response

                context = self.create_context(
                    headers=headers,
                    state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                    method='TestLongRunningFetch',
                    context_type=IMPORT_reboot_aio_contexts.ReaderContext,
                )
                assert context is not None

                return await self._TestLongRunningFetch(
                    request,
                    context,
                    validating_effects=validating_effects,
                    grpc_context=grpc_context,
                )
            except IMPORT_reboot_aio_contexts.EffectValidationRetry:
                # Doing effect validation, just let this propagate.
                raise
            except IMPORT_reboot.aio.aborted.Aborted as aborted:
                status = IMPORT_rpc_status_sync.to_status(aborted.to_status())
                # Need to add transaction participants here because
                # calling `grpc_context.abort_with_status()` will
                # ignore any other trailing metadata. Only propagate
                # transaction participants metadata if the caller cares.
                # Callers that care are those that are themselves transactions.
                # It's important to not just send this information to everyone;
                # some clients can't tolerate trailers, see:
                #   https://github.com/reboot-dev/mono/issues/5081
                if context is not None and headers.transaction_ids is not None:
                    assert context.transaction_id is not None
                    status = status._replace(
                        trailing_metadata=status.trailing_metadata + context.participants.to_grpc_metadata()
                    )
                await grpc_context.abort_with_status(status)
                raise  # Unreachable but necessary for mypy.
            except IMPORT_asyncio.CancelledError:
                # It's pretty normal for an RPC to be cancelled; it's not useful to
                # print a stack trace.
                raise
            except BaseException as exception:
                # Print the exception stack trace for easier debugging. Note
                # that we don't include the stack trace in an error message
                # for the same reason that gRPC doesn't do so by default,
                # see https://github.com/grpc/grpc/issues/14897, but since this
                # should only get logged on the server side it is safe.
                logger.warning(
                    'Unhandled exception\n' +
                    ''.join(IMPORT_traceback.format_exc() if IMPORT_reboot_nodejs_python.should_print_stacktrace() else [f"{type(exception).__name__}: {exception}"])
                )

                # Re-raise the exception for gRPC to handle!
                #
                # TODO: gRPC will print a stack trace from this
                # exception which we don't want if we're executing via
                # Node.js.
                raise
            finally:
                # Propagate transaction participants, if the caller cares.
                # Callers that care are those that are themselves transactions.
                # It's important to not just send this information to everyone;
                # some clients can't tolerate trailers, see:
                #   https://github.com/reboot-dev/mono/issues/5081
                if context is not None and headers.transaction_ids is not None:
                    assert context.transaction_id is not None
                    grpc_context.set_trailing_metadata(
                        grpc_context.trailing_metadata() +
                        context.participants.to_grpc_metadata()
                    )

        with IMPORT_reboot_aio_tracing.context_from_headers(headers):
            return await _run()

    async def __TestLongRunningWriter(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: tests.reboot.greeter_pb2.Greeter,
        request: google.protobuf.empty_pb2.Empty,
        *,
        validating_effects: bool,
    ) -> Greeter.TestLongRunningWriterEffects:
        try:
            typed_state: Greeter.State = GreeterFromProto(state, is_initial_state=(context.state_id in states_being_constructed))

            response = (
                await self._servicer._TestLongRunningWriter(
                    context=context,
                    state=typed_state,
                    request=request
                )
            )


            GreeterToProto(typed_state, state)

            IMPORT_reboot_aio_types.assert_type(
                response,
                [google.protobuf.empty_pb2.Empty],
            )
            self.maybe_raise_effect_validation_retry(
                logger=logger,
                idempotency_manager=context,
                method_name='Greeter.TestLongRunningWriter',
                validating_effects=validating_effects,
                context=context,
            )
            return Greeter.TestLongRunningWriterEffects(
                state=state,
                response=response,
                tasks=context._tasks,
                _colocated_upserts=context._colocated_upserts,
            )
        except IMPORT_reboot_aio_contexts.EffectValidationRetry:
            # Doing effect validation, just let this propagate.
            raise
        except IMPORT_reboot.aio.aborted.Aborted as aborted:
            # If the caller aborted due to a retryable error, just
            # propagate the aborted instead of propagating `Unknown`
            # so that a client can transparently retry.
            if IMPORT_reboot.aio.aborted.is_retryable(aborted):
                raise aborted
            # Log any _unhandled_ abort stack traces to make it
            # easier for debugging.
            #
            # NOTE: we don't log if we're a task as it will be logged
            # in `public/reboot/aio/internals/tasks_dispatcher.py` instead.
            aborted_type: IMPORT_typing.Optional[type] = None
            aborted_type = Greeter.TestLongRunningWriterAborted
            if isinstance(aborted, IMPORT_reboot.aio.aborted.SystemAborted):
                # Not logging when within `node` as we already log there.
                if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                    logger.warning(
                        f"Unhandled (in 'tests.reboot.Greeter.TestLongRunningWriter') {aborted}; propagating as 'Unknown'\n" +
                        ''.join(IMPORT_traceback.format_exception(aborted))
                    )
                raise IMPORT_reboot.aio.aborted.SystemAborted(
                    IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                    # TODO(benh): consider whether or not we want to
                    # include the 'package.service.method' which may
                    # get concatenated together forming a kind of
                    # "stack trace"; while it's super helpful for
                    # debugging, it does expose implementation
                    # information.
                    message=f"unhandled (in 'tests.reboot.Greeter.TestLongRunningWriter') {aborted}"
                )
            else:
                if (
                    aborted_type is not None and
                    not isinstance(aborted, aborted_type) and
                    aborted_type.is_declared_error(aborted.error)
                ):
                    # We propagate declared errors that might have
                    # come from another call, i.e., we might have an
                    # `Aborted` but not for this method but the
                    # `Aborted` that we have has an error that this
                    # method declared. This allows a developer to
                    # simply add the declared error to their `.proto`
                    # file rather than having to catch and re-raise
                    # the error with their own aborted type.
                    if context.task is None:
                        logger.warning(
                            f"Propagating unhandled but declared error (in 'tests.reboot.Greeter.TestLongRunningWriter') {aborted}"
                        )
                elif (
                    aborted_type is None or
                    not isinstance(aborted, aborted_type)
                ):
                    # Not logging when within `node` as we already log there.
                    if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                        logger.warning(
                            f"Unhandled (in 'tests.reboot.Greeter.TestLongRunningWriter') {aborted}; propagating as 'Unknown'\n" +
                            ''.join(IMPORT_traceback.format_exception(aborted))
                        )
                    # If this wasn't a declared error than we
                    # propagate it as `Unknown`.
                    raise IMPORT_reboot.aio.aborted.SystemAborted(
                        IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                        # TODO(benh): consider whether or not we want to
                        # include the 'package.service.method' which may
                        # get concatenated together forming a kind of
                        # "stack trace"; while it's super helpful for
                        # debugging, it does expose implementation
                        # information.
                        message=f"unhandled (in 'tests.reboot.Greeter.TestLongRunningWriter') {aborted}"
                    )

            raise
        except IMPORT_asyncio.CancelledError:
            # It's pretty normal for an RPC to be cancelled; it's not useful to
            # print a stack trace.
            raise
        except IMPORT_google_protobuf_message.DecodeError as decode_error:
            # We usually see this error when we are trying to construct a proto
            # message which is too deeply nested: protobuf has a limit of 100
            # nested messages. See the limits here:
            #   https://protobuf.dev/programming-guides/proto-limits/

            if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                logger.warning(
                    "Unhandled (in 'tests.reboot.Greeter.TestLongRunningWriter') "
                    f"{type(decode_error).__name__}{': ' + str(decode_error) if len(str(decode_error)) > 0 else ''}; "
                    "This is usually caused by a deeply nested protobuf message, which is not supported by protobuf.\n"
                    "See the limits here: https://protobuf.dev/programming-guides/proto-limits/" +
                    ''.join(IMPORT_traceback.format_exception(decode_error))
                )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                message=f"unhandled (in 'tests.reboot.Greeter.TestLongRunningWriter') {decode_error}; "
                        "This is usually caused by a deeply nested protobuf message, which is not supported by protobuf.\n"
                        "See the limits here: https://protobuf.dev/programming-guides/proto-limits/"
            )
        except BaseException as exception:
            # Not logging when within `node` as we already log there.
            if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                logger.warning(
                    "Unhandled (in 'tests.reboot.Greeter.TestLongRunningWriter') "
                    f"{type(exception).__name__}{': ' + str(exception) if len(str(exception)) > 0 else ''}; "
                    "propagating as 'Unknown'\n" +
                    ''.join(IMPORT_traceback.format_exception(exception))
                )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                # TODO(benh): consider whether or not we want to
                # include the 'package.service.method' which may
                # get concatenated together forming a kind of
                # "stack trace"; while it's super helpful for
                # debugging, it does expose implementation
                # information.
                message=f"unhandled (in 'tests.reboot.Greeter.TestLongRunningWriter') {type(exception).__name__}: {exception}"
            )
        finally:
            pass

    @IMPORT_reboot_aio_tracing.function_span(
        # We expect an `EffectValidationRetry` exception; that's not an error.
        set_status_on_exception=False
    )
    async def _TestLongRunningWriter(
        self,
        request: google.protobuf.empty_pb2.Empty,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        *,
        validating_effects: bool,
        grpc_context: IMPORT_typing.Optional[IMPORT_grpc.aio.ServicerContext] = None,
    ) -> google.protobuf.empty_pb2.Empty:
        # Try to verify the token if a token verifier exists.
        context.auth = await self._maybe_verify_token(
            headers=context._headers, method='TestLongRunningWriter'
        )

        # Check if we already have performed this mutation!
        #
        # We do this _before_ calling 'transactionally()' because
        # if this call is for a transaction method _and_ we've
        # already performed the transaction then we don't want to
        # become a transaction participant (again) we just want to
        # return the transaction's response.
        idempotent_mutation = await self._state_manager.check_for_idempotent_mutation(
            context
        )

        if idempotent_mutation is not None:
            response = google.protobuf.empty_pb2.Empty()
            response.ParseFromString(idempotent_mutation.response)
            return response

        async with self._state_manager.transactionally(
            context,
            self.tasks_dispatcher,
            aborted_type=Greeter.TestLongRunningWriterAborted,
        ) as transaction:
            if transaction is not None:
                context.participants.add(
                    self._servicer.__state_type_name__, context._state_ref
                )
            async with self._state_manager.writer(
                context,
                self._servicer.__state_type__,
                self.tasks_dispatcher,
                authorize=self._maybe_authorize(
                    method_name='tests.reboot.GreeterMethods.TestLongRunningWriter',
                    headers=context._headers,
                    auth=context.auth,
                    request=request,
                ),
                transaction=transaction,
                from_constructor=False,
                requires_constructor=True,
            ) as (state, writer):

                effects = await self.__TestLongRunningWriter(
                    context,
                    state,
                    request,
                    validating_effects=validating_effects,
                )

                await writer.complete(effects)

                # TODO: We need a single `Effects` superclass for all methods, so we
                # would need to make it "partially" generic (with per-method subclasses
                # filling out the rest of the generic parameters) in order to fix this.
                return effects.response  # type: ignore[return-value]

    async def _schedule_TestLongRunningWriter(
        self,
        *,
        request: google.protobuf.empty_pb2.Empty,
        headers: IMPORT_reboot_aio_headers.Headers,
        grpc_context: IMPORT_grpc.aio.ServicerContext,
    ) -> tuple[IMPORT_reboot_aio_contexts.WriterContext, google.protobuf.empty_pb2.Empty]:
        context: IMPORT_reboot_aio_contexts.WriterContext = self.create_context(
            headers=headers,
            state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            method='TestLongRunningWriter',
            context_type=IMPORT_reboot_aio_contexts.WriterContext,
        )
        response = google.protobuf.empty_pb2.Empty()

        # Check if we already have performed this schedule! Note that
        # we need to do this for all kinds of methods because this is
        # effectively a mutation (actually a `writer`, see below).
        #
        # We do this _before_ calling 'transactionally()' because
        # if this call is for a transaction method _and_ we've
        # already performed the transaction then we don't want to
        # become a transaction participant (again) we just want to
        # return the transaction's response.
        idempotent_mutation = await self._state_manager.check_for_idempotent_mutation(
            context
        )

        if idempotent_mutation is not None:
            response.ParseFromString(idempotent_mutation.response)

            # We should have only scheduled a single task!
            assert len(idempotent_mutation.task_ids) == 1
            assert grpc_context is not None
            grpc_context.set_trailing_metadata(
                grpc_context.trailing_metadata() +
                (
                    (
                        IMPORT_reboot_aio_headers.TASK_ID_UUID,
                        str(IMPORT_uuid.UUID(bytes=idempotent_mutation.task_ids[0].task_uuid))
                    ),
                )
            )

            return context, response

        async with self._state_manager.transactionally(
            context,
            self.tasks_dispatcher,
            aborted_type=Greeter.TestLongRunningWriterAborted,
        ) as transaction:
            if transaction is not None:
                context.participants.add(
                    self._servicer.__state_type_name__, context._state_ref
                )

            # Try to verify the token if a token verifier exists.
            context.auth = await self._maybe_verify_token(
                headers=headers, method='TestLongRunningWriter'
            )

            async with self._state_manager.writer(
                context,
                self._servicer.__state_type__,
                self.tasks_dispatcher,
                transaction=transaction,
                authorize=self._maybe_authorize(
                    method_name='tests.reboot.GreeterMethods.TestLongRunningWriter',
                    headers=context._headers,
                    auth=context.auth,
                    request=request,
                ),
                from_constructor=False,
                requires_constructor=True
            ) as (state, writer):

                task = await GreeterServicerTasks(
                    context=context,
                    state_ref=context._state_ref,
                ).TestLongRunningWriter(
                    GreeterTestLongRunningWriterRequestFromProto(request),
                    schedule=context._headers.task_schedule,
                )

                effects = IMPORT_reboot_aio_state_managers.Effects(
                    response=response,
                    state=state,
                    tasks=[task],
                )

                assert effects.tasks is not None

                await writer.complete(effects)

                assert grpc_context is not None

                grpc_context.set_trailing_metadata(
                    grpc_context.trailing_metadata() +
                    (
                        (
                            IMPORT_reboot_aio_headers.TASK_ID_UUID,
                            str(IMPORT_uuid.UUID(bytes=task.task_id.task_uuid))
                        ),
                    )
                )

                return context, response

        return context, response


    # Entrypoint for non-reactive network calls (i.e. typical gRPC calls).
    async def TestLongRunningWriter(
        self,
        request: google.protobuf.empty_pb2.Empty,
        grpc_context: IMPORT_grpc.aio.ServicerContext,
    ) -> google.protobuf.empty_pb2.Empty:
        headers = IMPORT_reboot_aio_headers.Headers.from_grpc_context(grpc_context)
        assert headers.application_id is not None  # Guaranteed by `Headers`.

        # Confirm whether this is the right server to be serving this
        # request.
        try:
            authoritative_server = self.placement_client.server_for_actor(
                headers.application_id,
                headers.state_ref,
            )
        except IMPORT_reboot_aio_placement.UnknownApplicationError:
            # It's possible that the user did indeed type an application ID
            # that doesn't exist, but it's also quite possible that this
            # request reached us before the placement planner had gossipped
            # out the information about which applications exist (we see
            # this e.g. after `rbt dev`'s chaos monkey restarts). For that
            # reason, abort with a retryable error.
            await grpc_context.abort(
                IMPORT_grpc.StatusCode.UNAVAILABLE,
                f"Application '{headers.application_id}' not found. If you "
                "are confident the application exists, this may be because "
                "the system is still starting.",
            )
            raise  # Unreachable but necessary for mypy.
        if authoritative_server != self.server_id:
            # This is NOT the correct server. Fail.
            await grpc_context.abort(
                IMPORT_grpc.StatusCode.UNAVAILABLE,
                f"Server '{self.server_id}' is not authoritative for this "
                f"request; server '{authoritative_server}' is.",
            )
            raise  # Unreachable but necessary for mypy.

        @IMPORT_reboot_aio_internals_middleware.maybe_run_function_twice_to_validate_effects
        async def _run(
            validating_effects: bool,
        ) -> google.protobuf.empty_pb2.Empty:
            context: IMPORT_typing.Optional[IMPORT_reboot_aio_contexts.Context] = None
            try:
                if headers.task_schedule is not None:
                    context, response = await self._schedule_TestLongRunningWriter(
                        headers=headers,
                        request=request,
                        grpc_context=grpc_context,
                    )
                    return response

                context = self.create_context(
                    headers=headers,
                    state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                    method='TestLongRunningWriter',
                    context_type=IMPORT_reboot_aio_contexts.WriterContext,
                )
                assert context is not None

                return await self._TestLongRunningWriter(
                    request,
                    context,
                    validating_effects=validating_effects,
                    grpc_context=grpc_context,
                )
            except IMPORT_reboot_aio_contexts.EffectValidationRetry:
                # Doing effect validation, just let this propagate.
                raise
            except IMPORT_reboot.aio.aborted.Aborted as aborted:
                status = IMPORT_rpc_status_sync.to_status(aborted.to_status())
                # Need to add transaction participants here because
                # calling `grpc_context.abort_with_status()` will
                # ignore any other trailing metadata. Only propagate
                # transaction participants metadata if the caller cares.
                # Callers that care are those that are themselves transactions.
                # It's important to not just send this information to everyone;
                # some clients can't tolerate trailers, see:
                #   https://github.com/reboot-dev/mono/issues/5081
                if context is not None and headers.transaction_ids is not None:
                    assert context.transaction_id is not None
                    status = status._replace(
                        trailing_metadata=status.trailing_metadata + context.participants.to_grpc_metadata()
                    )
                await grpc_context.abort_with_status(status)
                raise  # Unreachable but necessary for mypy.
            except IMPORT_asyncio.CancelledError:
                # It's pretty normal for an RPC to be cancelled; it's not useful to
                # print a stack trace.
                raise
            except BaseException as exception:
                # Print the exception stack trace for easier debugging. Note
                # that we don't include the stack trace in an error message
                # for the same reason that gRPC doesn't do so by default,
                # see https://github.com/grpc/grpc/issues/14897, but since this
                # should only get logged on the server side it is safe.
                logger.warning(
                    'Unhandled exception\n' +
                    ''.join(IMPORT_traceback.format_exc() if IMPORT_reboot_nodejs_python.should_print_stacktrace() else [f"{type(exception).__name__}: {exception}"])
                )

                # Re-raise the exception for gRPC to handle!
                #
                # TODO: gRPC will print a stack trace from this
                # exception which we don't want if we're executing via
                # Node.js.
                raise
            finally:
                # Propagate transaction participants, if the caller cares.
                # Callers that care are those that are themselves transactions.
                # It's important to not just send this information to everyone;
                # some clients can't tolerate trailers, see:
                #   https://github.com/reboot-dev/mono/issues/5081
                if context is not None and headers.transaction_ids is not None:
                    assert context.transaction_id is not None
                    grpc_context.set_trailing_metadata(
                        grpc_context.trailing_metadata() +
                        context.participants.to_grpc_metadata()
                    )

        with IMPORT_reboot_aio_tracing.context_from_headers(headers):
            return await _run()

    async def __GetWholeState(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: tests.reboot.greeter_pb2.Greeter,
        request: tests.reboot.greeter_pb2.GetWholeStateRequest,
        *,
        validating_effects: bool,
    ) -> tests.reboot.greeter_pb2.Greeter:
        try:
            typed_state: Greeter.State = GreeterFromProto(state, is_initial_state=(context.state_id in states_being_constructed))

            response = (
                await self._servicer._GetWholeState(
                    context=context,
                    state=typed_state,
                    request=request
                )
            )


            GreeterToProto(typed_state, state)

            IMPORT_reboot_aio_types.assert_type(
                response,
                [tests.reboot.greeter_pb2.Greeter],
            )
            self.maybe_raise_effect_validation_retry(
                logger=logger,
                idempotency_manager=context,
                method_name='Greeter.GetWholeState',
                validating_effects=validating_effects,
                context=context,
            )
            return response
        except IMPORT_reboot_aio_contexts.EffectValidationRetry:
            # Doing effect validation, just let this propagate.
            raise
        except IMPORT_reboot.aio.aborted.Aborted as aborted:
            # If the caller aborted due to a retryable error, just
            # propagate the aborted instead of propagating `Unknown`
            # so that a client can transparently retry.
            if IMPORT_reboot.aio.aborted.is_retryable(aborted):
                raise aborted
            # Log any _unhandled_ abort stack traces to make it
            # easier for debugging.
            #
            # NOTE: we don't log if we're a task as it will be logged
            # in `public/reboot/aio/internals/tasks_dispatcher.py` instead.
            aborted_type: IMPORT_typing.Optional[type] = None
            aborted_type = Greeter.GetWholeStateAborted
            if isinstance(aborted, IMPORT_reboot.aio.aborted.SystemAborted):
                # Not logging when within `node` as we already log there.
                if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                    logger.warning(
                        f"Unhandled (in 'tests.reboot.Greeter.GetWholeState') {aborted}; propagating as 'Unknown'\n" +
                        ''.join(IMPORT_traceback.format_exception(aborted))
                    )
                raise IMPORT_reboot.aio.aborted.SystemAborted(
                    IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                    # TODO(benh): consider whether or not we want to
                    # include the 'package.service.method' which may
                    # get concatenated together forming a kind of
                    # "stack trace"; while it's super helpful for
                    # debugging, it does expose implementation
                    # information.
                    message=f"unhandled (in 'tests.reboot.Greeter.GetWholeState') {aborted}"
                )
            else:
                if (
                    aborted_type is not None and
                    not isinstance(aborted, aborted_type) and
                    aborted_type.is_declared_error(aborted.error)
                ):
                    # We propagate declared errors that might have
                    # come from another call, i.e., we might have an
                    # `Aborted` but not for this method but the
                    # `Aborted` that we have has an error that this
                    # method declared. This allows a developer to
                    # simply add the declared error to their `.proto`
                    # file rather than having to catch and re-raise
                    # the error with their own aborted type.
                    if context.task is None:
                        logger.warning(
                            f"Propagating unhandled but declared error (in 'tests.reboot.Greeter.GetWholeState') {aborted}"
                        )
                elif (
                    aborted_type is None or
                    not isinstance(aborted, aborted_type)
                ):
                    # Not logging when within `node` as we already log there.
                    if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                        logger.warning(
                            f"Unhandled (in 'tests.reboot.Greeter.GetWholeState') {aborted}; propagating as 'Unknown'\n" +
                            ''.join(IMPORT_traceback.format_exception(aborted))
                        )
                    # If this wasn't a declared error than we
                    # propagate it as `Unknown`.
                    raise IMPORT_reboot.aio.aborted.SystemAborted(
                        IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                        # TODO(benh): consider whether or not we want to
                        # include the 'package.service.method' which may
                        # get concatenated together forming a kind of
                        # "stack trace"; while it's super helpful for
                        # debugging, it does expose implementation
                        # information.
                        message=f"unhandled (in 'tests.reboot.Greeter.GetWholeState') {aborted}"
                    )

            raise
        except IMPORT_asyncio.CancelledError:
            # It's pretty normal for an RPC to be cancelled; it's not useful to
            # print a stack trace.
            raise
        except IMPORT_google_protobuf_message.DecodeError as decode_error:
            # We usually see this error when we are trying to construct a proto
            # message which is too deeply nested: protobuf has a limit of 100
            # nested messages. See the limits here:
            #   https://protobuf.dev/programming-guides/proto-limits/

            if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                logger.warning(
                    "Unhandled (in 'tests.reboot.Greeter.GetWholeState') "
                    f"{type(decode_error).__name__}{': ' + str(decode_error) if len(str(decode_error)) > 0 else ''}; "
                    "This is usually caused by a deeply nested protobuf message, which is not supported by protobuf.\n"
                    "See the limits here: https://protobuf.dev/programming-guides/proto-limits/" +
                    ''.join(IMPORT_traceback.format_exception(decode_error))
                )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                message=f"unhandled (in 'tests.reboot.Greeter.GetWholeState') {decode_error}; "
                        "This is usually caused by a deeply nested protobuf message, which is not supported by protobuf.\n"
                        "See the limits here: https://protobuf.dev/programming-guides/proto-limits/"
            )
        except BaseException as exception:
            # Not logging when within `node` as we already log there.
            if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                logger.warning(
                    "Unhandled (in 'tests.reboot.Greeter.GetWholeState') "
                    f"{type(exception).__name__}{': ' + str(exception) if len(str(exception)) > 0 else ''}; "
                    "propagating as 'Unknown'\n" +
                    ''.join(IMPORT_traceback.format_exception(exception))
                )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                # TODO(benh): consider whether or not we want to
                # include the 'package.service.method' which may
                # get concatenated together forming a kind of
                # "stack trace"; while it's super helpful for
                # debugging, it does expose implementation
                # information.
                message=f"unhandled (in 'tests.reboot.Greeter.GetWholeState') {type(exception).__name__}: {exception}"
            )
        finally:
            pass

    @IMPORT_reboot_aio_tracing.function_span(
        # We expect an `EffectValidationRetry` exception; that's not an error.
        set_status_on_exception=False
    )
    async def _GetWholeState(
        self,
        request: tests.reboot.greeter_pb2.GetWholeStateRequest,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        *,
        validating_effects: bool,
        grpc_context: IMPORT_typing.Optional[IMPORT_grpc.aio.ServicerContext] = None,
    ) -> tests.reboot.greeter_pb2.Greeter:
        # Try to verify the token if a token verifier exists.
        context.auth = await self._maybe_verify_token(
            headers=context._headers, method='GetWholeState'
        )


        async with self._state_manager.transactionally(
            context,
            self.tasks_dispatcher,
            aborted_type=Greeter.GetWholeStateAborted,
        ) as transaction:
            if transaction is not None:
                context.participants.add(
                    self._servicer.__state_type_name__, context._state_ref
                )
            authorizer = self._maybe_authorize(
                method_name='tests.reboot.GreeterMethods.GetWholeState',
                headers=context._headers,
                auth=context.auth,
                request=request,
            )
            async with self._state_manager.reader(
                context,
                self._servicer.__state_type__,
                authorize=authorizer,
            ) as state:
                response = await self.__GetWholeState(
                    context,
                    state,
                    request,
                    validating_effects=validating_effects,
                )
                return response

    async def _schedule_GetWholeState(
        self,
        *,
        request: tests.reboot.greeter_pb2.GetWholeStateRequest,
        headers: IMPORT_reboot_aio_headers.Headers,
        grpc_context: IMPORT_grpc.aio.ServicerContext,
    ) -> tuple[IMPORT_reboot_aio_contexts.WriterContext, tests.reboot.greeter_pb2.Greeter]:
        context: IMPORT_reboot_aio_contexts.WriterContext = self.create_context(
            headers=headers,
            state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            method='GetWholeState',
            context_type=IMPORT_reboot_aio_contexts.WriterContext,
        )
        response = tests.reboot.greeter_pb2.Greeter()

        # Check if we already have performed this schedule! Note that
        # we need to do this for all kinds of methods because this is
        # effectively a mutation (actually a `writer`, see below).
        #
        # We do this _before_ calling 'transactionally()' because
        # if this call is for a transaction method _and_ we've
        # already performed the transaction then we don't want to
        # become a transaction participant (again) we just want to
        # return the transaction's response.
        idempotent_mutation = await self._state_manager.check_for_idempotent_mutation(
            context
        )

        if idempotent_mutation is not None:
            response.ParseFromString(idempotent_mutation.response)

            # We should have only scheduled a single task!
            assert len(idempotent_mutation.task_ids) == 1
            assert grpc_context is not None
            grpc_context.set_trailing_metadata(
                grpc_context.trailing_metadata() +
                (
                    (
                        IMPORT_reboot_aio_headers.TASK_ID_UUID,
                        str(IMPORT_uuid.UUID(bytes=idempotent_mutation.task_ids[0].task_uuid))
                    ),
                )
            )

            return context, response

        async with self._state_manager.transactionally(
            context,
            self.tasks_dispatcher,
            aborted_type=Greeter.GetWholeStateAborted,
        ) as transaction:
            if transaction is not None:
                context.participants.add(
                    self._servicer.__state_type_name__, context._state_ref
                )

            # Try to verify the token if a token verifier exists.
            context.auth = await self._maybe_verify_token(
                headers=headers, method='GetWholeState'
            )

            async with self._state_manager.writer(
                context,
                self._servicer.__state_type__,
                self.tasks_dispatcher,
                transaction=transaction,
                authorize=self._maybe_authorize(
                    method_name='tests.reboot.GreeterMethods.GetWholeState',
                    headers=context._headers,
                    auth=context.auth,
                    request=request,
                ),
                from_constructor=False,
                requires_constructor=True
            ) as (state, writer):

                task = await GreeterServicerTasks(
                    context=context,
                    state_ref=context._state_ref,
                ).GetWholeState(
                    GreeterGetWholeStateRequestFromProto(request),
                    schedule=context._headers.task_schedule,
                )

                effects = IMPORT_reboot_aio_state_managers.Effects(
                    response=response,
                    state=state,
                    tasks=[task],
                )

                assert effects.tasks is not None

                await writer.complete(effects)

                assert grpc_context is not None

                grpc_context.set_trailing_metadata(
                    grpc_context.trailing_metadata() +
                    (
                        (
                            IMPORT_reboot_aio_headers.TASK_ID_UUID,
                            str(IMPORT_uuid.UUID(bytes=task.task_id.task_uuid))
                        ),
                    )
                )

                return context, response

        return context, response


    # Entrypoint for non-reactive network calls (i.e. typical gRPC calls).
    async def GetWholeState(
        self,
        request: tests.reboot.greeter_pb2.GetWholeStateRequest,
        grpc_context: IMPORT_grpc.aio.ServicerContext,
    ) -> tests.reboot.greeter_pb2.Greeter:
        headers = IMPORT_reboot_aio_headers.Headers.from_grpc_context(grpc_context)
        assert headers.application_id is not None  # Guaranteed by `Headers`.

        # Confirm whether this is the right server to be serving this
        # request.
        try:
            authoritative_server = self.placement_client.server_for_actor(
                headers.application_id,
                headers.state_ref,
            )
        except IMPORT_reboot_aio_placement.UnknownApplicationError:
            # It's possible that the user did indeed type an application ID
            # that doesn't exist, but it's also quite possible that this
            # request reached us before the placement planner had gossipped
            # out the information about which applications exist (we see
            # this e.g. after `rbt dev`'s chaos monkey restarts). For that
            # reason, abort with a retryable error.
            await grpc_context.abort(
                IMPORT_grpc.StatusCode.UNAVAILABLE,
                f"Application '{headers.application_id}' not found. If you "
                "are confident the application exists, this may be because "
                "the system is still starting.",
            )
            raise  # Unreachable but necessary for mypy.
        if authoritative_server != self.server_id:
            # This is NOT the correct server. Fail.
            await grpc_context.abort(
                IMPORT_grpc.StatusCode.UNAVAILABLE,
                f"Server '{self.server_id}' is not authoritative for this "
                f"request; server '{authoritative_server}' is.",
            )
            raise  # Unreachable but necessary for mypy.

        @IMPORT_reboot_aio_internals_middleware.maybe_run_function_twice_to_validate_effects
        async def _run(
            validating_effects: bool,
        ) -> tests.reboot.greeter_pb2.Greeter:
            context: IMPORT_typing.Optional[IMPORT_reboot_aio_contexts.Context] = None
            try:
                if headers.task_schedule is not None:
                    context, response = await self._schedule_GetWholeState(
                        headers=headers,
                        request=request,
                        grpc_context=grpc_context,
                    )
                    return response

                context = self.create_context(
                    headers=headers,
                    state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                    method='GetWholeState',
                    context_type=IMPORT_reboot_aio_contexts.ReaderContext,
                )
                assert context is not None

                return await self._GetWholeState(
                    request,
                    context,
                    validating_effects=validating_effects,
                    grpc_context=grpc_context,
                )
            except IMPORT_reboot_aio_contexts.EffectValidationRetry:
                # Doing effect validation, just let this propagate.
                raise
            except IMPORT_reboot.aio.aborted.Aborted as aborted:
                status = IMPORT_rpc_status_sync.to_status(aborted.to_status())
                # Need to add transaction participants here because
                # calling `grpc_context.abort_with_status()` will
                # ignore any other trailing metadata. Only propagate
                # transaction participants metadata if the caller cares.
                # Callers that care are those that are themselves transactions.
                # It's important to not just send this information to everyone;
                # some clients can't tolerate trailers, see:
                #   https://github.com/reboot-dev/mono/issues/5081
                if context is not None and headers.transaction_ids is not None:
                    assert context.transaction_id is not None
                    status = status._replace(
                        trailing_metadata=status.trailing_metadata + context.participants.to_grpc_metadata()
                    )
                await grpc_context.abort_with_status(status)
                raise  # Unreachable but necessary for mypy.
            except IMPORT_asyncio.CancelledError:
                # It's pretty normal for an RPC to be cancelled; it's not useful to
                # print a stack trace.
                raise
            except BaseException as exception:
                # Print the exception stack trace for easier debugging. Note
                # that we don't include the stack trace in an error message
                # for the same reason that gRPC doesn't do so by default,
                # see https://github.com/grpc/grpc/issues/14897, but since this
                # should only get logged on the server side it is safe.
                logger.warning(
                    'Unhandled exception\n' +
                    ''.join(IMPORT_traceback.format_exc() if IMPORT_reboot_nodejs_python.should_print_stacktrace() else [f"{type(exception).__name__}: {exception}"])
                )

                # Re-raise the exception for gRPC to handle!
                #
                # TODO: gRPC will print a stack trace from this
                # exception which we don't want if we're executing via
                # Node.js.
                raise
            finally:
                # Propagate transaction participants, if the caller cares.
                # Callers that care are those that are themselves transactions.
                # It's important to not just send this information to everyone;
                # some clients can't tolerate trailers, see:
                #   https://github.com/reboot-dev/mono/issues/5081
                if context is not None and headers.transaction_ids is not None:
                    assert context.transaction_id is not None
                    grpc_context.set_trailing_metadata(
                        grpc_context.trailing_metadata() +
                        context.participants.to_grpc_metadata()
                    )

        with IMPORT_reboot_aio_tracing.context_from_headers(headers):
            return await _run()

    async def __FailWithException(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: tests.reboot.greeter_pb2.Greeter,
        request: google.protobuf.empty_pb2.Empty,
        *,
        validating_effects: bool,
    ) -> google.protobuf.empty_pb2.Empty:
        try:
            typed_state: Greeter.State = GreeterFromProto(state, is_initial_state=(context.state_id in states_being_constructed))

            response = (
                await self._servicer._FailWithException(
                    context=context,
                    state=typed_state,
                    request=request
                )
            )


            GreeterToProto(typed_state, state)

            IMPORT_reboot_aio_types.assert_type(
                response,
                [google.protobuf.empty_pb2.Empty],
            )
            self.maybe_raise_effect_validation_retry(
                logger=logger,
                idempotency_manager=context,
                method_name='Greeter.FailWithException',
                validating_effects=validating_effects,
                context=context,
            )
            return response
        except IMPORT_reboot_aio_contexts.EffectValidationRetry:
            # Doing effect validation, just let this propagate.
            raise
        except IMPORT_reboot.aio.aborted.Aborted as aborted:
            # If the caller aborted due to a retryable error, just
            # propagate the aborted instead of propagating `Unknown`
            # so that a client can transparently retry.
            if IMPORT_reboot.aio.aborted.is_retryable(aborted):
                raise aborted
            # Log any _unhandled_ abort stack traces to make it
            # easier for debugging.
            #
            # NOTE: we don't log if we're a task as it will be logged
            # in `public/reboot/aio/internals/tasks_dispatcher.py` instead.
            aborted_type: IMPORT_typing.Optional[type] = None
            aborted_type = Greeter.FailWithExceptionAborted
            if isinstance(aborted, IMPORT_reboot.aio.aborted.SystemAborted):
                # Not logging when within `node` as we already log there.
                if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                    logger.warning(
                        f"Unhandled (in 'tests.reboot.Greeter.FailWithException') {aborted}; propagating as 'Unknown'\n" +
                        ''.join(IMPORT_traceback.format_exception(aborted))
                    )
                raise IMPORT_reboot.aio.aborted.SystemAborted(
                    IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                    # TODO(benh): consider whether or not we want to
                    # include the 'package.service.method' which may
                    # get concatenated together forming a kind of
                    # "stack trace"; while it's super helpful for
                    # debugging, it does expose implementation
                    # information.
                    message=f"unhandled (in 'tests.reboot.Greeter.FailWithException') {aborted}"
                )
            else:
                if (
                    aborted_type is not None and
                    not isinstance(aborted, aborted_type) and
                    aborted_type.is_declared_error(aborted.error)
                ):
                    # We propagate declared errors that might have
                    # come from another call, i.e., we might have an
                    # `Aborted` but not for this method but the
                    # `Aborted` that we have has an error that this
                    # method declared. This allows a developer to
                    # simply add the declared error to their `.proto`
                    # file rather than having to catch and re-raise
                    # the error with their own aborted type.
                    if context.task is None:
                        logger.warning(
                            f"Propagating unhandled but declared error (in 'tests.reboot.Greeter.FailWithException') {aborted}"
                        )
                elif (
                    aborted_type is None or
                    not isinstance(aborted, aborted_type)
                ):
                    # Not logging when within `node` as we already log there.
                    if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                        logger.warning(
                            f"Unhandled (in 'tests.reboot.Greeter.FailWithException') {aborted}; propagating as 'Unknown'\n" +
                            ''.join(IMPORT_traceback.format_exception(aborted))
                        )
                    # If this wasn't a declared error than we
                    # propagate it as `Unknown`.
                    raise IMPORT_reboot.aio.aborted.SystemAborted(
                        IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                        # TODO(benh): consider whether or not we want to
                        # include the 'package.service.method' which may
                        # get concatenated together forming a kind of
                        # "stack trace"; while it's super helpful for
                        # debugging, it does expose implementation
                        # information.
                        message=f"unhandled (in 'tests.reboot.Greeter.FailWithException') {aborted}"
                    )

            raise
        except IMPORT_asyncio.CancelledError:
            # It's pretty normal for an RPC to be cancelled; it's not useful to
            # print a stack trace.
            raise
        except IMPORT_google_protobuf_message.DecodeError as decode_error:
            # We usually see this error when we are trying to construct a proto
            # message which is too deeply nested: protobuf has a limit of 100
            # nested messages. See the limits here:
            #   https://protobuf.dev/programming-guides/proto-limits/

            if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                logger.warning(
                    "Unhandled (in 'tests.reboot.Greeter.FailWithException') "
                    f"{type(decode_error).__name__}{': ' + str(decode_error) if len(str(decode_error)) > 0 else ''}; "
                    "This is usually caused by a deeply nested protobuf message, which is not supported by protobuf.\n"
                    "See the limits here: https://protobuf.dev/programming-guides/proto-limits/" +
                    ''.join(IMPORT_traceback.format_exception(decode_error))
                )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                message=f"unhandled (in 'tests.reboot.Greeter.FailWithException') {decode_error}; "
                        "This is usually caused by a deeply nested protobuf message, which is not supported by protobuf.\n"
                        "See the limits here: https://protobuf.dev/programming-guides/proto-limits/"
            )
        except BaseException as exception:
            # Not logging when within `node` as we already log there.
            if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                logger.warning(
                    "Unhandled (in 'tests.reboot.Greeter.FailWithException') "
                    f"{type(exception).__name__}{': ' + str(exception) if len(str(exception)) > 0 else ''}; "
                    "propagating as 'Unknown'\n" +
                    ''.join(IMPORT_traceback.format_exception(exception))
                )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                # TODO(benh): consider whether or not we want to
                # include the 'package.service.method' which may
                # get concatenated together forming a kind of
                # "stack trace"; while it's super helpful for
                # debugging, it does expose implementation
                # information.
                message=f"unhandled (in 'tests.reboot.Greeter.FailWithException') {type(exception).__name__}: {exception}"
            )
        finally:
            pass

    @IMPORT_reboot_aio_tracing.function_span(
        # We expect an `EffectValidationRetry` exception; that's not an error.
        set_status_on_exception=False
    )
    async def _FailWithException(
        self,
        request: google.protobuf.empty_pb2.Empty,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        *,
        validating_effects: bool,
        grpc_context: IMPORT_typing.Optional[IMPORT_grpc.aio.ServicerContext] = None,
    ) -> google.protobuf.empty_pb2.Empty:
        # Try to verify the token if a token verifier exists.
        context.auth = await self._maybe_verify_token(
            headers=context._headers, method='FailWithException'
        )


        async with self._state_manager.transactionally(
            context,
            self.tasks_dispatcher,
            aborted_type=Greeter.FailWithExceptionAborted,
        ) as transaction:
            if transaction is not None:
                context.participants.add(
                    self._servicer.__state_type_name__, context._state_ref
                )
            authorizer = self._maybe_authorize(
                method_name='tests.reboot.GreeterMethods.FailWithException',
                headers=context._headers,
                auth=context.auth,
                request=request,
            )
            async with self._state_manager.reader(
                context,
                self._servicer.__state_type__,
                authorize=authorizer,
            ) as state:
                response = await self.__FailWithException(
                    context,
                    state,
                    request,
                    validating_effects=validating_effects,
                )
                return response

    async def _schedule_FailWithException(
        self,
        *,
        request: google.protobuf.empty_pb2.Empty,
        headers: IMPORT_reboot_aio_headers.Headers,
        grpc_context: IMPORT_grpc.aio.ServicerContext,
    ) -> tuple[IMPORT_reboot_aio_contexts.WriterContext, google.protobuf.empty_pb2.Empty]:
        context: IMPORT_reboot_aio_contexts.WriterContext = self.create_context(
            headers=headers,
            state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            method='FailWithException',
            context_type=IMPORT_reboot_aio_contexts.WriterContext,
        )
        response = google.protobuf.empty_pb2.Empty()

        # Check if we already have performed this schedule! Note that
        # we need to do this for all kinds of methods because this is
        # effectively a mutation (actually a `writer`, see below).
        #
        # We do this _before_ calling 'transactionally()' because
        # if this call is for a transaction method _and_ we've
        # already performed the transaction then we don't want to
        # become a transaction participant (again) we just want to
        # return the transaction's response.
        idempotent_mutation = await self._state_manager.check_for_idempotent_mutation(
            context
        )

        if idempotent_mutation is not None:
            response.ParseFromString(idempotent_mutation.response)

            # We should have only scheduled a single task!
            assert len(idempotent_mutation.task_ids) == 1
            assert grpc_context is not None
            grpc_context.set_trailing_metadata(
                grpc_context.trailing_metadata() +
                (
                    (
                        IMPORT_reboot_aio_headers.TASK_ID_UUID,
                        str(IMPORT_uuid.UUID(bytes=idempotent_mutation.task_ids[0].task_uuid))
                    ),
                )
            )

            return context, response

        async with self._state_manager.transactionally(
            context,
            self.tasks_dispatcher,
            aborted_type=Greeter.FailWithExceptionAborted,
        ) as transaction:
            if transaction is not None:
                context.participants.add(
                    self._servicer.__state_type_name__, context._state_ref
                )

            # Try to verify the token if a token verifier exists.
            context.auth = await self._maybe_verify_token(
                headers=headers, method='FailWithException'
            )

            async with self._state_manager.writer(
                context,
                self._servicer.__state_type__,
                self.tasks_dispatcher,
                transaction=transaction,
                authorize=self._maybe_authorize(
                    method_name='tests.reboot.GreeterMethods.FailWithException',
                    headers=context._headers,
                    auth=context.auth,
                    request=request,
                ),
                from_constructor=False,
                requires_constructor=True
            ) as (state, writer):

                task = await GreeterServicerTasks(
                    context=context,
                    state_ref=context._state_ref,
                ).FailWithException(
                    GreeterFailWithExceptionRequestFromProto(request),
                    schedule=context._headers.task_schedule,
                )

                effects = IMPORT_reboot_aio_state_managers.Effects(
                    response=response,
                    state=state,
                    tasks=[task],
                )

                assert effects.tasks is not None

                await writer.complete(effects)

                assert grpc_context is not None

                grpc_context.set_trailing_metadata(
                    grpc_context.trailing_metadata() +
                    (
                        (
                            IMPORT_reboot_aio_headers.TASK_ID_UUID,
                            str(IMPORT_uuid.UUID(bytes=task.task_id.task_uuid))
                        ),
                    )
                )

                return context, response

        return context, response


    # Entrypoint for non-reactive network calls (i.e. typical gRPC calls).
    async def FailWithException(
        self,
        request: google.protobuf.empty_pb2.Empty,
        grpc_context: IMPORT_grpc.aio.ServicerContext,
    ) -> google.protobuf.empty_pb2.Empty:
        headers = IMPORT_reboot_aio_headers.Headers.from_grpc_context(grpc_context)
        assert headers.application_id is not None  # Guaranteed by `Headers`.

        # Confirm whether this is the right server to be serving this
        # request.
        try:
            authoritative_server = self.placement_client.server_for_actor(
                headers.application_id,
                headers.state_ref,
            )
        except IMPORT_reboot_aio_placement.UnknownApplicationError:
            # It's possible that the user did indeed type an application ID
            # that doesn't exist, but it's also quite possible that this
            # request reached us before the placement planner had gossipped
            # out the information about which applications exist (we see
            # this e.g. after `rbt dev`'s chaos monkey restarts). For that
            # reason, abort with a retryable error.
            await grpc_context.abort(
                IMPORT_grpc.StatusCode.UNAVAILABLE,
                f"Application '{headers.application_id}' not found. If you "
                "are confident the application exists, this may be because "
                "the system is still starting.",
            )
            raise  # Unreachable but necessary for mypy.
        if authoritative_server != self.server_id:
            # This is NOT the correct server. Fail.
            await grpc_context.abort(
                IMPORT_grpc.StatusCode.UNAVAILABLE,
                f"Server '{self.server_id}' is not authoritative for this "
                f"request; server '{authoritative_server}' is.",
            )
            raise  # Unreachable but necessary for mypy.

        @IMPORT_reboot_aio_internals_middleware.maybe_run_function_twice_to_validate_effects
        async def _run(
            validating_effects: bool,
        ) -> google.protobuf.empty_pb2.Empty:
            context: IMPORT_typing.Optional[IMPORT_reboot_aio_contexts.Context] = None
            try:
                if headers.task_schedule is not None:
                    context, response = await self._schedule_FailWithException(
                        headers=headers,
                        request=request,
                        grpc_context=grpc_context,
                    )
                    return response

                context = self.create_context(
                    headers=headers,
                    state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                    method='FailWithException',
                    context_type=IMPORT_reboot_aio_contexts.ReaderContext,
                )
                assert context is not None

                return await self._FailWithException(
                    request,
                    context,
                    validating_effects=validating_effects,
                    grpc_context=grpc_context,
                )
            except IMPORT_reboot_aio_contexts.EffectValidationRetry:
                # Doing effect validation, just let this propagate.
                raise
            except IMPORT_reboot.aio.aborted.Aborted as aborted:
                status = IMPORT_rpc_status_sync.to_status(aborted.to_status())
                # Need to add transaction participants here because
                # calling `grpc_context.abort_with_status()` will
                # ignore any other trailing metadata. Only propagate
                # transaction participants metadata if the caller cares.
                # Callers that care are those that are themselves transactions.
                # It's important to not just send this information to everyone;
                # some clients can't tolerate trailers, see:
                #   https://github.com/reboot-dev/mono/issues/5081
                if context is not None and headers.transaction_ids is not None:
                    assert context.transaction_id is not None
                    status = status._replace(
                        trailing_metadata=status.trailing_metadata + context.participants.to_grpc_metadata()
                    )
                await grpc_context.abort_with_status(status)
                raise  # Unreachable but necessary for mypy.
            except IMPORT_asyncio.CancelledError:
                # It's pretty normal for an RPC to be cancelled; it's not useful to
                # print a stack trace.
                raise
            except BaseException as exception:
                # Print the exception stack trace for easier debugging. Note
                # that we don't include the stack trace in an error message
                # for the same reason that gRPC doesn't do so by default,
                # see https://github.com/grpc/grpc/issues/14897, but since this
                # should only get logged on the server side it is safe.
                logger.warning(
                    'Unhandled exception\n' +
                    ''.join(IMPORT_traceback.format_exc() if IMPORT_reboot_nodejs_python.should_print_stacktrace() else [f"{type(exception).__name__}: {exception}"])
                )

                # Re-raise the exception for gRPC to handle!
                #
                # TODO: gRPC will print a stack trace from this
                # exception which we don't want if we're executing via
                # Node.js.
                raise
            finally:
                # Propagate transaction participants, if the caller cares.
                # Callers that care are those that are themselves transactions.
                # It's important to not just send this information to everyone;
                # some clients can't tolerate trailers, see:
                #   https://github.com/reboot-dev/mono/issues/5081
                if context is not None and headers.transaction_ids is not None:
                    assert context.transaction_id is not None
                    grpc_context.set_trailing_metadata(
                        grpc_context.trailing_metadata() +
                        context.participants.to_grpc_metadata()
                    )

        with IMPORT_reboot_aio_tracing.context_from_headers(headers):
            return await _run()

    async def __FailWithAborted(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: tests.reboot.greeter_pb2.Greeter,
        request: google.protobuf.empty_pb2.Empty,
        *,
        validating_effects: bool,
    ) -> google.protobuf.empty_pb2.Empty:
        try:
            typed_state: Greeter.State = GreeterFromProto(state, is_initial_state=(context.state_id in states_being_constructed))

            response = (
                await self._servicer._FailWithAborted(
                    context=context,
                    state=typed_state,
                    request=request
                )
            )


            GreeterToProto(typed_state, state)

            IMPORT_reboot_aio_types.assert_type(
                response,
                [google.protobuf.empty_pb2.Empty],
            )
            self.maybe_raise_effect_validation_retry(
                logger=logger,
                idempotency_manager=context,
                method_name='Greeter.FailWithAborted',
                validating_effects=validating_effects,
                context=context,
            )
            return response
        except IMPORT_reboot_aio_contexts.EffectValidationRetry:
            # Doing effect validation, just let this propagate.
            raise
        except IMPORT_reboot.aio.aborted.Aborted as aborted:
            # If the caller aborted due to a retryable error, just
            # propagate the aborted instead of propagating `Unknown`
            # so that a client can transparently retry.
            if IMPORT_reboot.aio.aborted.is_retryable(aborted):
                raise aborted
            # Log any _unhandled_ abort stack traces to make it
            # easier for debugging.
            #
            # NOTE: we don't log if we're a task as it will be logged
            # in `public/reboot/aio/internals/tasks_dispatcher.py` instead.
            aborted_type: IMPORT_typing.Optional[type] = None
            aborted_type = Greeter.FailWithAbortedAborted
            if isinstance(aborted, IMPORT_reboot.aio.aborted.SystemAborted):
                # Not logging when within `node` as we already log there.
                if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                    logger.warning(
                        f"Unhandled (in 'tests.reboot.Greeter.FailWithAborted') {aborted}; propagating as 'Unknown'\n" +
                        ''.join(IMPORT_traceback.format_exception(aborted))
                    )
                raise IMPORT_reboot.aio.aborted.SystemAborted(
                    IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                    # TODO(benh): consider whether or not we want to
                    # include the 'package.service.method' which may
                    # get concatenated together forming a kind of
                    # "stack trace"; while it's super helpful for
                    # debugging, it does expose implementation
                    # information.
                    message=f"unhandled (in 'tests.reboot.Greeter.FailWithAborted') {aborted}"
                )
            else:
                if (
                    aborted_type is not None and
                    not isinstance(aborted, aborted_type) and
                    aborted_type.is_declared_error(aborted.error)
                ):
                    # We propagate declared errors that might have
                    # come from another call, i.e., we might have an
                    # `Aborted` but not for this method but the
                    # `Aborted` that we have has an error that this
                    # method declared. This allows a developer to
                    # simply add the declared error to their `.proto`
                    # file rather than having to catch and re-raise
                    # the error with their own aborted type.
                    if context.task is None:
                        logger.warning(
                            f"Propagating unhandled but declared error (in 'tests.reboot.Greeter.FailWithAborted') {aborted}"
                        )
                elif (
                    aborted_type is None or
                    not isinstance(aborted, aborted_type)
                ):
                    # Not logging when within `node` as we already log there.
                    if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                        logger.warning(
                            f"Unhandled (in 'tests.reboot.Greeter.FailWithAborted') {aborted}; propagating as 'Unknown'\n" +
                            ''.join(IMPORT_traceback.format_exception(aborted))
                        )
                    # If this wasn't a declared error than we
                    # propagate it as `Unknown`.
                    raise IMPORT_reboot.aio.aborted.SystemAborted(
                        IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                        # TODO(benh): consider whether or not we want to
                        # include the 'package.service.method' which may
                        # get concatenated together forming a kind of
                        # "stack trace"; while it's super helpful for
                        # debugging, it does expose implementation
                        # information.
                        message=f"unhandled (in 'tests.reboot.Greeter.FailWithAborted') {aborted}"
                    )

            raise
        except IMPORT_asyncio.CancelledError:
            # It's pretty normal for an RPC to be cancelled; it's not useful to
            # print a stack trace.
            raise
        except IMPORT_google_protobuf_message.DecodeError as decode_error:
            # We usually see this error when we are trying to construct a proto
            # message which is too deeply nested: protobuf has a limit of 100
            # nested messages. See the limits here:
            #   https://protobuf.dev/programming-guides/proto-limits/

            if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                logger.warning(
                    "Unhandled (in 'tests.reboot.Greeter.FailWithAborted') "
                    f"{type(decode_error).__name__}{': ' + str(decode_error) if len(str(decode_error)) > 0 else ''}; "
                    "This is usually caused by a deeply nested protobuf message, which is not supported by protobuf.\n"
                    "See the limits here: https://protobuf.dev/programming-guides/proto-limits/" +
                    ''.join(IMPORT_traceback.format_exception(decode_error))
                )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                message=f"unhandled (in 'tests.reboot.Greeter.FailWithAborted') {decode_error}; "
                        "This is usually caused by a deeply nested protobuf message, which is not supported by protobuf.\n"
                        "See the limits here: https://protobuf.dev/programming-guides/proto-limits/"
            )
        except BaseException as exception:
            # Not logging when within `node` as we already log there.
            if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                logger.warning(
                    "Unhandled (in 'tests.reboot.Greeter.FailWithAborted') "
                    f"{type(exception).__name__}{': ' + str(exception) if len(str(exception)) > 0 else ''}; "
                    "propagating as 'Unknown'\n" +
                    ''.join(IMPORT_traceback.format_exception(exception))
                )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                # TODO(benh): consider whether or not we want to
                # include the 'package.service.method' which may
                # get concatenated together forming a kind of
                # "stack trace"; while it's super helpful for
                # debugging, it does expose implementation
                # information.
                message=f"unhandled (in 'tests.reboot.Greeter.FailWithAborted') {type(exception).__name__}: {exception}"
            )
        finally:
            pass

    @IMPORT_reboot_aio_tracing.function_span(
        # We expect an `EffectValidationRetry` exception; that's not an error.
        set_status_on_exception=False
    )
    async def _FailWithAborted(
        self,
        request: google.protobuf.empty_pb2.Empty,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        *,
        validating_effects: bool,
        grpc_context: IMPORT_typing.Optional[IMPORT_grpc.aio.ServicerContext] = None,
    ) -> google.protobuf.empty_pb2.Empty:
        # Try to verify the token if a token verifier exists.
        context.auth = await self._maybe_verify_token(
            headers=context._headers, method='FailWithAborted'
        )


        async with self._state_manager.transactionally(
            context,
            self.tasks_dispatcher,
            aborted_type=Greeter.FailWithAbortedAborted,
        ) as transaction:
            if transaction is not None:
                context.participants.add(
                    self._servicer.__state_type_name__, context._state_ref
                )
            authorizer = self._maybe_authorize(
                method_name='tests.reboot.GreeterMethods.FailWithAborted',
                headers=context._headers,
                auth=context.auth,
                request=request,
            )
            async with self._state_manager.reader(
                context,
                self._servicer.__state_type__,
                authorize=authorizer,
            ) as state:
                response = await self.__FailWithAborted(
                    context,
                    state,
                    request,
                    validating_effects=validating_effects,
                )
                return response

    async def _schedule_FailWithAborted(
        self,
        *,
        request: google.protobuf.empty_pb2.Empty,
        headers: IMPORT_reboot_aio_headers.Headers,
        grpc_context: IMPORT_grpc.aio.ServicerContext,
    ) -> tuple[IMPORT_reboot_aio_contexts.WriterContext, google.protobuf.empty_pb2.Empty]:
        context: IMPORT_reboot_aio_contexts.WriterContext = self.create_context(
            headers=headers,
            state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            method='FailWithAborted',
            context_type=IMPORT_reboot_aio_contexts.WriterContext,
        )
        response = google.protobuf.empty_pb2.Empty()

        # Check if we already have performed this schedule! Note that
        # we need to do this for all kinds of methods because this is
        # effectively a mutation (actually a `writer`, see below).
        #
        # We do this _before_ calling 'transactionally()' because
        # if this call is for a transaction method _and_ we've
        # already performed the transaction then we don't want to
        # become a transaction participant (again) we just want to
        # return the transaction's response.
        idempotent_mutation = await self._state_manager.check_for_idempotent_mutation(
            context
        )

        if idempotent_mutation is not None:
            response.ParseFromString(idempotent_mutation.response)

            # We should have only scheduled a single task!
            assert len(idempotent_mutation.task_ids) == 1
            assert grpc_context is not None
            grpc_context.set_trailing_metadata(
                grpc_context.trailing_metadata() +
                (
                    (
                        IMPORT_reboot_aio_headers.TASK_ID_UUID,
                        str(IMPORT_uuid.UUID(bytes=idempotent_mutation.task_ids[0].task_uuid))
                    ),
                )
            )

            return context, response

        async with self._state_manager.transactionally(
            context,
            self.tasks_dispatcher,
            aborted_type=Greeter.FailWithAbortedAborted,
        ) as transaction:
            if transaction is not None:
                context.participants.add(
                    self._servicer.__state_type_name__, context._state_ref
                )

            # Try to verify the token if a token verifier exists.
            context.auth = await self._maybe_verify_token(
                headers=headers, method='FailWithAborted'
            )

            async with self._state_manager.writer(
                context,
                self._servicer.__state_type__,
                self.tasks_dispatcher,
                transaction=transaction,
                authorize=self._maybe_authorize(
                    method_name='tests.reboot.GreeterMethods.FailWithAborted',
                    headers=context._headers,
                    auth=context.auth,
                    request=request,
                ),
                from_constructor=False,
                requires_constructor=True
            ) as (state, writer):

                task = await GreeterServicerTasks(
                    context=context,
                    state_ref=context._state_ref,
                ).FailWithAborted(
                    GreeterFailWithAbortedRequestFromProto(request),
                    schedule=context._headers.task_schedule,
                )

                effects = IMPORT_reboot_aio_state_managers.Effects(
                    response=response,
                    state=state,
                    tasks=[task],
                )

                assert effects.tasks is not None

                await writer.complete(effects)

                assert grpc_context is not None

                grpc_context.set_trailing_metadata(
                    grpc_context.trailing_metadata() +
                    (
                        (
                            IMPORT_reboot_aio_headers.TASK_ID_UUID,
                            str(IMPORT_uuid.UUID(bytes=task.task_id.task_uuid))
                        ),
                    )
                )

                return context, response

        return context, response


    # Entrypoint for non-reactive network calls (i.e. typical gRPC calls).
    async def FailWithAborted(
        self,
        request: google.protobuf.empty_pb2.Empty,
        grpc_context: IMPORT_grpc.aio.ServicerContext,
    ) -> google.protobuf.empty_pb2.Empty:
        headers = IMPORT_reboot_aio_headers.Headers.from_grpc_context(grpc_context)
        assert headers.application_id is not None  # Guaranteed by `Headers`.

        # Confirm whether this is the right server to be serving this
        # request.
        try:
            authoritative_server = self.placement_client.server_for_actor(
                headers.application_id,
                headers.state_ref,
            )
        except IMPORT_reboot_aio_placement.UnknownApplicationError:
            # It's possible that the user did indeed type an application ID
            # that doesn't exist, but it's also quite possible that this
            # request reached us before the placement planner had gossipped
            # out the information about which applications exist (we see
            # this e.g. after `rbt dev`'s chaos monkey restarts). For that
            # reason, abort with a retryable error.
            await grpc_context.abort(
                IMPORT_grpc.StatusCode.UNAVAILABLE,
                f"Application '{headers.application_id}' not found. If you "
                "are confident the application exists, this may be because "
                "the system is still starting.",
            )
            raise  # Unreachable but necessary for mypy.
        if authoritative_server != self.server_id:
            # This is NOT the correct server. Fail.
            await grpc_context.abort(
                IMPORT_grpc.StatusCode.UNAVAILABLE,
                f"Server '{self.server_id}' is not authoritative for this "
                f"request; server '{authoritative_server}' is.",
            )
            raise  # Unreachable but necessary for mypy.

        @IMPORT_reboot_aio_internals_middleware.maybe_run_function_twice_to_validate_effects
        async def _run(
            validating_effects: bool,
        ) -> google.protobuf.empty_pb2.Empty:
            context: IMPORT_typing.Optional[IMPORT_reboot_aio_contexts.Context] = None
            try:
                if headers.task_schedule is not None:
                    context, response = await self._schedule_FailWithAborted(
                        headers=headers,
                        request=request,
                        grpc_context=grpc_context,
                    )
                    return response

                context = self.create_context(
                    headers=headers,
                    state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                    method='FailWithAborted',
                    context_type=IMPORT_reboot_aio_contexts.ReaderContext,
                )
                assert context is not None

                return await self._FailWithAborted(
                    request,
                    context,
                    validating_effects=validating_effects,
                    grpc_context=grpc_context,
                )
            except IMPORT_reboot_aio_contexts.EffectValidationRetry:
                # Doing effect validation, just let this propagate.
                raise
            except IMPORT_reboot.aio.aborted.Aborted as aborted:
                status = IMPORT_rpc_status_sync.to_status(aborted.to_status())
                # Need to add transaction participants here because
                # calling `grpc_context.abort_with_status()` will
                # ignore any other trailing metadata. Only propagate
                # transaction participants metadata if the caller cares.
                # Callers that care are those that are themselves transactions.
                # It's important to not just send this information to everyone;
                # some clients can't tolerate trailers, see:
                #   https://github.com/reboot-dev/mono/issues/5081
                if context is not None and headers.transaction_ids is not None:
                    assert context.transaction_id is not None
                    status = status._replace(
                        trailing_metadata=status.trailing_metadata + context.participants.to_grpc_metadata()
                    )
                await grpc_context.abort_with_status(status)
                raise  # Unreachable but necessary for mypy.
            except IMPORT_asyncio.CancelledError:
                # It's pretty normal for an RPC to be cancelled; it's not useful to
                # print a stack trace.
                raise
            except BaseException as exception:
                # Print the exception stack trace for easier debugging. Note
                # that we don't include the stack trace in an error message
                # for the same reason that gRPC doesn't do so by default,
                # see https://github.com/grpc/grpc/issues/14897, but since this
                # should only get logged on the server side it is safe.
                logger.warning(
                    'Unhandled exception\n' +
                    ''.join(IMPORT_traceback.format_exc() if IMPORT_reboot_nodejs_python.should_print_stacktrace() else [f"{type(exception).__name__}: {exception}"])
                )

                # Re-raise the exception for gRPC to handle!
                #
                # TODO: gRPC will print a stack trace from this
                # exception which we don't want if we're executing via
                # Node.js.
                raise
            finally:
                # Propagate transaction participants, if the caller cares.
                # Callers that care are those that are themselves transactions.
                # It's important to not just send this information to everyone;
                # some clients can't tolerate trailers, see:
                #   https://github.com/reboot-dev/mono/issues/5081
                if context is not None and headers.transaction_ids is not None:
                    assert context.transaction_id is not None
                    grpc_context.set_trailing_metadata(
                        grpc_context.trailing_metadata() +
                        context.participants.to_grpc_metadata()
                    )

        with IMPORT_reboot_aio_tracing.context_from_headers(headers):
            return await _run()

    async def __Workflow(
        self,
        context: IMPORT_reboot_aio_contexts.WorkflowContext,
        request: google.protobuf.empty_pb2.Empty,
        *,
        validating_effects: bool,
    ) -> tests.reboot.greeter_pb2.WorkflowResponse:
        try:
            # We should have an asyncio task and thus context per request,
            # let's confirm this assumption by making sure that
            # `__servicer__ is None`.
            assert self._servicer.__servicer__.get() is None
            self._servicer.__servicer__.set(self._servicer)
            response = (
                await self._servicer._Workflow(
                    context=context,
                    request=request
                )
            )



            IMPORT_reboot_aio_types.assert_type(
                response,
                [tests.reboot.greeter_pb2.WorkflowResponse],
            )
            self.maybe_raise_effect_validation_retry(
                logger=logger,
                idempotency_manager=context,
                method_name='Greeter.Workflow',
                validating_effects=validating_effects,
                context=context,
            )
            return response
        except IMPORT_reboot_aio_contexts.EffectValidationRetry:
            # Doing effect validation, just let this propagate.
            raise
        except IMPORT_reboot.aio.aborted.Aborted as aborted:
            # If the caller aborted due to a retryable error, just
            # propagate the aborted instead of propagating `Unknown`
            # so that a client can transparently retry.
            if IMPORT_reboot.aio.aborted.is_retryable(aborted):
                raise aborted
            # Log any _unhandled_ abort stack traces to make it
            # easier for debugging.
            #
            # NOTE: we don't log if we're a task as it will be logged
            # in `public/reboot/aio/internals/tasks_dispatcher.py` instead.
            aborted_type: IMPORT_typing.Optional[type] = None
            aborted_type = Greeter.WorkflowAborted
            if isinstance(aborted, IMPORT_reboot.aio.aborted.SystemAborted):
                # Not logging when within `node` as we already log there.
                if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                    logger.warning(
                        f"Unhandled (in 'tests.reboot.Greeter.Workflow') {aborted}; propagating as 'Unknown'\n" +
                        ''.join(IMPORT_traceback.format_exception(aborted))
                    )
                raise IMPORT_reboot.aio.aborted.SystemAborted(
                    IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                    # TODO(benh): consider whether or not we want to
                    # include the 'package.service.method' which may
                    # get concatenated together forming a kind of
                    # "stack trace"; while it's super helpful for
                    # debugging, it does expose implementation
                    # information.
                    message=f"unhandled (in 'tests.reboot.Greeter.Workflow') {aborted}"
                )
            else:
                if (
                    aborted_type is not None and
                    not isinstance(aborted, aborted_type) and
                    aborted_type.is_declared_error(aborted.error)
                ):
                    # We propagate declared errors that might have
                    # come from another call, i.e., we might have an
                    # `Aborted` but not for this method but the
                    # `Aborted` that we have has an error that this
                    # method declared. This allows a developer to
                    # simply add the declared error to their `.proto`
                    # file rather than having to catch and re-raise
                    # the error with their own aborted type.
                    if context.task is None:
                        logger.warning(
                            f"Propagating unhandled but declared error (in 'tests.reboot.Greeter.Workflow') {aborted}"
                        )
                elif (
                    aborted_type is None or
                    not isinstance(aborted, aborted_type)
                ):
                    # Not logging when within `node` as we already log there.
                    if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                        logger.warning(
                            f"Unhandled (in 'tests.reboot.Greeter.Workflow') {aborted}; propagating as 'Unknown'\n" +
                            ''.join(IMPORT_traceback.format_exception(aborted))
                        )
                    # If this wasn't a declared error than we
                    # propagate it as `Unknown`.
                    raise IMPORT_reboot.aio.aborted.SystemAborted(
                        IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                        # TODO(benh): consider whether or not we want to
                        # include the 'package.service.method' which may
                        # get concatenated together forming a kind of
                        # "stack trace"; while it's super helpful for
                        # debugging, it does expose implementation
                        # information.
                        message=f"unhandled (in 'tests.reboot.Greeter.Workflow') {aborted}"
                    )

            raise
        except IMPORT_asyncio.CancelledError:
            # It's pretty normal for an RPC to be cancelled; it's not useful to
            # print a stack trace.
            raise
        except IMPORT_google_protobuf_message.DecodeError as decode_error:
            # We usually see this error when we are trying to construct a proto
            # message which is too deeply nested: protobuf has a limit of 100
            # nested messages. See the limits here:
            #   https://protobuf.dev/programming-guides/proto-limits/

            if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                logger.warning(
                    "Unhandled (in 'tests.reboot.Greeter.Workflow') "
                    f"{type(decode_error).__name__}{': ' + str(decode_error) if len(str(decode_error)) > 0 else ''}; "
                    "This is usually caused by a deeply nested protobuf message, which is not supported by protobuf.\n"
                    "See the limits here: https://protobuf.dev/programming-guides/proto-limits/" +
                    ''.join(IMPORT_traceback.format_exception(decode_error))
                )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                message=f"unhandled (in 'tests.reboot.Greeter.Workflow') {decode_error}; "
                        "This is usually caused by a deeply nested protobuf message, which is not supported by protobuf.\n"
                        "See the limits here: https://protobuf.dev/programming-guides/proto-limits/"
            )
        except BaseException as exception:
            # Not logging when within `node` as we already log there.
            if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                logger.warning(
                    "Unhandled (in 'tests.reboot.Greeter.Workflow') "
                    f"{type(exception).__name__}{': ' + str(exception) if len(str(exception)) > 0 else ''}; "
                    "propagating as 'Unknown'\n" +
                    ''.join(IMPORT_traceback.format_exception(exception))
                )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                # TODO(benh): consider whether or not we want to
                # include the 'package.service.method' which may
                # get concatenated together forming a kind of
                # "stack trace"; while it's super helpful for
                # debugging, it does expose implementation
                # information.
                message=f"unhandled (in 'tests.reboot.Greeter.Workflow') {type(exception).__name__}: {exception}"
            )
        finally:
            self._servicer.__servicer__.set(None)
            pass


    async def _schedule_Workflow(
        self,
        *,
        request: google.protobuf.empty_pb2.Empty,
        headers: IMPORT_reboot_aio_headers.Headers,
        grpc_context: IMPORT_grpc.aio.ServicerContext,
    ) -> tuple[IMPORT_reboot_aio_contexts.WriterContext, tests.reboot.greeter_pb2.WorkflowResponse]:
        context: IMPORT_reboot_aio_contexts.WriterContext = self.create_context(
            headers=headers,
            state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            method='Workflow',
            context_type=IMPORT_reboot_aio_contexts.WriterContext,
        )
        response = tests.reboot.greeter_pb2.WorkflowResponse()

        # Check if we already have performed this schedule! Note that
        # we need to do this for all kinds of methods because this is
        # effectively a mutation (actually a `writer`, see below).
        #
        # We do this _before_ calling 'transactionally()' because
        # if this call is for a transaction method _and_ we've
        # already performed the transaction then we don't want to
        # become a transaction participant (again) we just want to
        # return the transaction's response.
        idempotent_mutation = await self._state_manager.check_for_idempotent_mutation(
            context
        )

        if idempotent_mutation is not None:
            response.ParseFromString(idempotent_mutation.response)

            # We should have only scheduled a single task!
            assert len(idempotent_mutation.task_ids) == 1
            assert grpc_context is not None
            grpc_context.set_trailing_metadata(
                grpc_context.trailing_metadata() +
                (
                    (
                        IMPORT_reboot_aio_headers.TASK_ID_UUID,
                        str(IMPORT_uuid.UUID(bytes=idempotent_mutation.task_ids[0].task_uuid))
                    ),
                )
            )

            return context, response

        async with self._state_manager.transactionally(
            context,
            self.tasks_dispatcher,
            aborted_type=Greeter.WorkflowAborted,
        ) as transaction:
            if transaction is not None:
                context.participants.add(
                    self._servicer.__state_type_name__, context._state_ref
                )

            # Try to verify the token if a token verifier exists.
            context.auth = await self._maybe_verify_token(
                headers=headers, method='Workflow'
            )

            async with self._state_manager.writer(
                context,
                self._servicer.__state_type__,
                self.tasks_dispatcher,
                transaction=transaction,
                authorize=self._maybe_authorize(
                    method_name='tests.reboot.GreeterMethods.Workflow',
                    headers=context._headers,
                    auth=context.auth,
                    request=request,
                ),
                from_constructor=False,
                requires_constructor=True
            ) as (state, writer):

                task = await GreeterServicerTasks(
                    context=context,
                    state_ref=context._state_ref,
                ).Workflow(
                    GreeterWorkflowRequestFromProto(request),
                    schedule=context._headers.task_schedule,
                )

                effects = IMPORT_reboot_aio_state_managers.Effects(
                    response=response,
                    state=state,
                    tasks=[task],
                )

                assert effects.tasks is not None

                await writer.complete(effects)

                assert grpc_context is not None

                grpc_context.set_trailing_metadata(
                    grpc_context.trailing_metadata() +
                    (
                        (
                            IMPORT_reboot_aio_headers.TASK_ID_UUID,
                            str(IMPORT_uuid.UUID(bytes=task.task_id.task_uuid))
                        ),
                    )
                )

                return context, response

        return context, response


    # Entrypoint for non-reactive network calls (i.e. typical gRPC calls).
    async def Workflow(
        self,
        request: google.protobuf.empty_pb2.Empty,
        grpc_context: IMPORT_grpc.aio.ServicerContext,
    ) -> tests.reboot.greeter_pb2.WorkflowResponse:
        headers = IMPORT_reboot_aio_headers.Headers.from_grpc_context(grpc_context)
        assert headers.application_id is not None  # Guaranteed by `Headers`.

        # Confirm whether this is the right server to be serving this
        # request.
        try:
            authoritative_server = self.placement_client.server_for_actor(
                headers.application_id,
                headers.state_ref,
            )
        except IMPORT_reboot_aio_placement.UnknownApplicationError:
            # It's possible that the user did indeed type an application ID
            # that doesn't exist, but it's also quite possible that this
            # request reached us before the placement planner had gossipped
            # out the information about which applications exist (we see
            # this e.g. after `rbt dev`'s chaos monkey restarts). For that
            # reason, abort with a retryable error.
            await grpc_context.abort(
                IMPORT_grpc.StatusCode.UNAVAILABLE,
                f"Application '{headers.application_id}' not found. If you "
                "are confident the application exists, this may be because "
                "the system is still starting.",
            )
            raise  # Unreachable but necessary for mypy.
        if authoritative_server != self.server_id:
            # This is NOT the correct server. Fail.
            await grpc_context.abort(
                IMPORT_grpc.StatusCode.UNAVAILABLE,
                f"Server '{self.server_id}' is not authoritative for this "
                f"request; server '{authoritative_server}' is.",
            )
            raise  # Unreachable but necessary for mypy.

        @IMPORT_reboot_aio_internals_middleware.maybe_run_function_twice_to_validate_effects
        async def _run(
            validating_effects: bool,
        ) -> tests.reboot.greeter_pb2.WorkflowResponse:
            context: IMPORT_typing.Optional[IMPORT_reboot_aio_contexts.Context] = None
            try:
                # reboot.aio.contexts.WorkflowContext must be scheduled!
                assert headers.task_schedule is not None
                context, response = await self._schedule_Workflow(
                    headers=headers,
                    request=request,
                    grpc_context=grpc_context,
                )
                return response

            except IMPORT_reboot_aio_contexts.EffectValidationRetry:
                # Doing effect validation, just let this propagate.
                raise
            except IMPORT_reboot.aio.aborted.Aborted as aborted:
                status = IMPORT_rpc_status_sync.to_status(aborted.to_status())
                # Need to add transaction participants here because
                # calling `grpc_context.abort_with_status()` will
                # ignore any other trailing metadata. Only propagate
                # transaction participants metadata if the caller cares.
                # Callers that care are those that are themselves transactions.
                # It's important to not just send this information to everyone;
                # some clients can't tolerate trailers, see:
                #   https://github.com/reboot-dev/mono/issues/5081
                if context is not None and headers.transaction_ids is not None:
                    assert context.transaction_id is not None
                    status = status._replace(
                        trailing_metadata=status.trailing_metadata + context.participants.to_grpc_metadata()
                    )
                await grpc_context.abort_with_status(status)
                raise  # Unreachable but necessary for mypy.
            except IMPORT_asyncio.CancelledError:
                # It's pretty normal for an RPC to be cancelled; it's not useful to
                # print a stack trace.
                raise
            except BaseException as exception:
                # Print the exception stack trace for easier debugging. Note
                # that we don't include the stack trace in an error message
                # for the same reason that gRPC doesn't do so by default,
                # see https://github.com/grpc/grpc/issues/14897, but since this
                # should only get logged on the server side it is safe.
                logger.warning(
                    'Unhandled exception\n' +
                    ''.join(IMPORT_traceback.format_exc() if IMPORT_reboot_nodejs_python.should_print_stacktrace() else [f"{type(exception).__name__}: {exception}"])
                )

                # Re-raise the exception for gRPC to handle!
                #
                # TODO: gRPC will print a stack trace from this
                # exception which we don't want if we're executing via
                # Node.js.
                raise
            finally:
                # Propagate transaction participants, if the caller cares.
                # Callers that care are those that are themselves transactions.
                # It's important to not just send this information to everyone;
                # some clients can't tolerate trailers, see:
                #   https://github.com/reboot-dev/mono/issues/5081
                if context is not None and headers.transaction_ids is not None:
                    assert context.transaction_id is not None
                    grpc_context.set_trailing_metadata(
                        grpc_context.trailing_metadata() +
                        context.participants.to_grpc_metadata()
                    )

        with IMPORT_reboot_aio_tracing.context_from_headers(headers):
            return await _run()

    async def __DangerousFields(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: tests.reboot.greeter_pb2.Greeter,
        request: tests.reboot.greeter_pb2.DangerousFieldsRequest,
        *,
        validating_effects: bool,
    ) -> Greeter.DangerousFieldsEffects:
        try:
            typed_state: Greeter.State = GreeterFromProto(state, is_initial_state=(context.state_id in states_being_constructed))

            response = (
                await self._servicer._DangerousFields(
                    context=context,
                    state=typed_state,
                    request=request
                )
            )


            GreeterToProto(typed_state, state)

            IMPORT_reboot_aio_types.assert_type(
                response,
                [google.protobuf.empty_pb2.Empty],
            )
            self.maybe_raise_effect_validation_retry(
                logger=logger,
                idempotency_manager=context,
                method_name='Greeter.DangerousFields',
                validating_effects=validating_effects,
                context=context,
            )
            return Greeter.DangerousFieldsEffects(
                state=state,
                response=response,
                tasks=context._tasks,
                _colocated_upserts=context._colocated_upserts,
            )
        except IMPORT_reboot_aio_contexts.EffectValidationRetry:
            # Doing effect validation, just let this propagate.
            raise
        except IMPORT_reboot.aio.aborted.Aborted as aborted:
            # If the caller aborted due to a retryable error, just
            # propagate the aborted instead of propagating `Unknown`
            # so that a client can transparently retry.
            if IMPORT_reboot.aio.aborted.is_retryable(aborted):
                raise aborted
            # Log any _unhandled_ abort stack traces to make it
            # easier for debugging.
            #
            # NOTE: we don't log if we're a task as it will be logged
            # in `public/reboot/aio/internals/tasks_dispatcher.py` instead.
            aborted_type: IMPORT_typing.Optional[type] = None
            aborted_type = Greeter.DangerousFieldsAborted
            if isinstance(aborted, IMPORT_reboot.aio.aborted.SystemAborted):
                # Not logging when within `node` as we already log there.
                if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                    logger.warning(
                        f"Unhandled (in 'tests.reboot.Greeter.DangerousFields') {aborted}; propagating as 'Unknown'\n" +
                        ''.join(IMPORT_traceback.format_exception(aborted))
                    )
                raise IMPORT_reboot.aio.aborted.SystemAborted(
                    IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                    # TODO(benh): consider whether or not we want to
                    # include the 'package.service.method' which may
                    # get concatenated together forming a kind of
                    # "stack trace"; while it's super helpful for
                    # debugging, it does expose implementation
                    # information.
                    message=f"unhandled (in 'tests.reboot.Greeter.DangerousFields') {aborted}"
                )
            else:
                if (
                    aborted_type is not None and
                    not isinstance(aborted, aborted_type) and
                    aborted_type.is_declared_error(aborted.error)
                ):
                    # We propagate declared errors that might have
                    # come from another call, i.e., we might have an
                    # `Aborted` but not for this method but the
                    # `Aborted` that we have has an error that this
                    # method declared. This allows a developer to
                    # simply add the declared error to their `.proto`
                    # file rather than having to catch and re-raise
                    # the error with their own aborted type.
                    if context.task is None:
                        logger.warning(
                            f"Propagating unhandled but declared error (in 'tests.reboot.Greeter.DangerousFields') {aborted}"
                        )
                elif (
                    aborted_type is None or
                    not isinstance(aborted, aborted_type)
                ):
                    # Not logging when within `node` as we already log there.
                    if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                        logger.warning(
                            f"Unhandled (in 'tests.reboot.Greeter.DangerousFields') {aborted}; propagating as 'Unknown'\n" +
                            ''.join(IMPORT_traceback.format_exception(aborted))
                        )
                    # If this wasn't a declared error than we
                    # propagate it as `Unknown`.
                    raise IMPORT_reboot.aio.aborted.SystemAborted(
                        IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                        # TODO(benh): consider whether or not we want to
                        # include the 'package.service.method' which may
                        # get concatenated together forming a kind of
                        # "stack trace"; while it's super helpful for
                        # debugging, it does expose implementation
                        # information.
                        message=f"unhandled (in 'tests.reboot.Greeter.DangerousFields') {aborted}"
                    )

            raise
        except IMPORT_asyncio.CancelledError:
            # It's pretty normal for an RPC to be cancelled; it's not useful to
            # print a stack trace.
            raise
        except IMPORT_google_protobuf_message.DecodeError as decode_error:
            # We usually see this error when we are trying to construct a proto
            # message which is too deeply nested: protobuf has a limit of 100
            # nested messages. See the limits here:
            #   https://protobuf.dev/programming-guides/proto-limits/

            if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                logger.warning(
                    "Unhandled (in 'tests.reboot.Greeter.DangerousFields') "
                    f"{type(decode_error).__name__}{': ' + str(decode_error) if len(str(decode_error)) > 0 else ''}; "
                    "This is usually caused by a deeply nested protobuf message, which is not supported by protobuf.\n"
                    "See the limits here: https://protobuf.dev/programming-guides/proto-limits/" +
                    ''.join(IMPORT_traceback.format_exception(decode_error))
                )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                message=f"unhandled (in 'tests.reboot.Greeter.DangerousFields') {decode_error}; "
                        "This is usually caused by a deeply nested protobuf message, which is not supported by protobuf.\n"
                        "See the limits here: https://protobuf.dev/programming-guides/proto-limits/"
            )
        except BaseException as exception:
            # Not logging when within `node` as we already log there.
            if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                logger.warning(
                    "Unhandled (in 'tests.reboot.Greeter.DangerousFields') "
                    f"{type(exception).__name__}{': ' + str(exception) if len(str(exception)) > 0 else ''}; "
                    "propagating as 'Unknown'\n" +
                    ''.join(IMPORT_traceback.format_exception(exception))
                )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                # TODO(benh): consider whether or not we want to
                # include the 'package.service.method' which may
                # get concatenated together forming a kind of
                # "stack trace"; while it's super helpful for
                # debugging, it does expose implementation
                # information.
                message=f"unhandled (in 'tests.reboot.Greeter.DangerousFields') {type(exception).__name__}: {exception}"
            )
        finally:
            pass

    @IMPORT_reboot_aio_tracing.function_span(
        # We expect an `EffectValidationRetry` exception; that's not an error.
        set_status_on_exception=False
    )
    async def _DangerousFields(
        self,
        request: tests.reboot.greeter_pb2.DangerousFieldsRequest,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        *,
        validating_effects: bool,
        grpc_context: IMPORT_typing.Optional[IMPORT_grpc.aio.ServicerContext] = None,
    ) -> google.protobuf.empty_pb2.Empty:
        # Try to verify the token if a token verifier exists.
        context.auth = await self._maybe_verify_token(
            headers=context._headers, method='DangerousFields'
        )

        # Check if we already have performed this mutation!
        #
        # We do this _before_ calling 'transactionally()' because
        # if this call is for a transaction method _and_ we've
        # already performed the transaction then we don't want to
        # become a transaction participant (again) we just want to
        # return the transaction's response.
        idempotent_mutation = await self._state_manager.check_for_idempotent_mutation(
            context
        )

        if idempotent_mutation is not None:
            response = google.protobuf.empty_pb2.Empty()
            response.ParseFromString(idempotent_mutation.response)
            return response

        async with self._state_manager.transactionally(
            context,
            self.tasks_dispatcher,
            aborted_type=Greeter.DangerousFieldsAborted,
        ) as transaction:
            if transaction is not None:
                context.participants.add(
                    self._servicer.__state_type_name__, context._state_ref
                )
            async with self._state_manager.writer(
                context,
                self._servicer.__state_type__,
                self.tasks_dispatcher,
                authorize=self._maybe_authorize(
                    method_name='tests.reboot.GreeterMethods.DangerousFields',
                    headers=context._headers,
                    auth=context.auth,
                    request=request,
                ),
                transaction=transaction,
                from_constructor=False,
                requires_constructor=True,
            ) as (state, writer):

                effects = await self.__DangerousFields(
                    context,
                    state,
                    request,
                    validating_effects=validating_effects,
                )

                await writer.complete(effects)

                # TODO: We need a single `Effects` superclass for all methods, so we
                # would need to make it "partially" generic (with per-method subclasses
                # filling out the rest of the generic parameters) in order to fix this.
                return effects.response  # type: ignore[return-value]

    async def _schedule_DangerousFields(
        self,
        *,
        request: tests.reboot.greeter_pb2.DangerousFieldsRequest,
        headers: IMPORT_reboot_aio_headers.Headers,
        grpc_context: IMPORT_grpc.aio.ServicerContext,
    ) -> tuple[IMPORT_reboot_aio_contexts.WriterContext, google.protobuf.empty_pb2.Empty]:
        context: IMPORT_reboot_aio_contexts.WriterContext = self.create_context(
            headers=headers,
            state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            method='DangerousFields',
            context_type=IMPORT_reboot_aio_contexts.WriterContext,
        )
        response = google.protobuf.empty_pb2.Empty()

        # Check if we already have performed this schedule! Note that
        # we need to do this for all kinds of methods because this is
        # effectively a mutation (actually a `writer`, see below).
        #
        # We do this _before_ calling 'transactionally()' because
        # if this call is for a transaction method _and_ we've
        # already performed the transaction then we don't want to
        # become a transaction participant (again) we just want to
        # return the transaction's response.
        idempotent_mutation = await self._state_manager.check_for_idempotent_mutation(
            context
        )

        if idempotent_mutation is not None:
            response.ParseFromString(idempotent_mutation.response)

            # We should have only scheduled a single task!
            assert len(idempotent_mutation.task_ids) == 1
            assert grpc_context is not None
            grpc_context.set_trailing_metadata(
                grpc_context.trailing_metadata() +
                (
                    (
                        IMPORT_reboot_aio_headers.TASK_ID_UUID,
                        str(IMPORT_uuid.UUID(bytes=idempotent_mutation.task_ids[0].task_uuid))
                    ),
                )
            )

            return context, response

        async with self._state_manager.transactionally(
            context,
            self.tasks_dispatcher,
            aborted_type=Greeter.DangerousFieldsAborted,
        ) as transaction:
            if transaction is not None:
                context.participants.add(
                    self._servicer.__state_type_name__, context._state_ref
                )

            # Try to verify the token if a token verifier exists.
            context.auth = await self._maybe_verify_token(
                headers=headers, method='DangerousFields'
            )

            async with self._state_manager.writer(
                context,
                self._servicer.__state_type__,
                self.tasks_dispatcher,
                transaction=transaction,
                authorize=self._maybe_authorize(
                    method_name='tests.reboot.GreeterMethods.DangerousFields',
                    headers=context._headers,
                    auth=context.auth,
                    request=request,
                ),
                from_constructor=False,
                requires_constructor=True
            ) as (state, writer):

                task = await GreeterServicerTasks(
                    context=context,
                    state_ref=context._state_ref,
                ).DangerousFields(
                    GreeterDangerousFieldsRequestFromProto(request),
                    schedule=context._headers.task_schedule,
                )

                effects = IMPORT_reboot_aio_state_managers.Effects(
                    response=response,
                    state=state,
                    tasks=[task],
                )

                assert effects.tasks is not None

                await writer.complete(effects)

                assert grpc_context is not None

                grpc_context.set_trailing_metadata(
                    grpc_context.trailing_metadata() +
                    (
                        (
                            IMPORT_reboot_aio_headers.TASK_ID_UUID,
                            str(IMPORT_uuid.UUID(bytes=task.task_id.task_uuid))
                        ),
                    )
                )

                return context, response

        return context, response


    # Entrypoint for non-reactive network calls (i.e. typical gRPC calls).
    async def DangerousFields(
        self,
        request: tests.reboot.greeter_pb2.DangerousFieldsRequest,
        grpc_context: IMPORT_grpc.aio.ServicerContext,
    ) -> google.protobuf.empty_pb2.Empty:
        headers = IMPORT_reboot_aio_headers.Headers.from_grpc_context(grpc_context)
        assert headers.application_id is not None  # Guaranteed by `Headers`.

        # Confirm whether this is the right server to be serving this
        # request.
        try:
            authoritative_server = self.placement_client.server_for_actor(
                headers.application_id,
                headers.state_ref,
            )
        except IMPORT_reboot_aio_placement.UnknownApplicationError:
            # It's possible that the user did indeed type an application ID
            # that doesn't exist, but it's also quite possible that this
            # request reached us before the placement planner had gossipped
            # out the information about which applications exist (we see
            # this e.g. after `rbt dev`'s chaos monkey restarts). For that
            # reason, abort with a retryable error.
            await grpc_context.abort(
                IMPORT_grpc.StatusCode.UNAVAILABLE,
                f"Application '{headers.application_id}' not found. If you "
                "are confident the application exists, this may be because "
                "the system is still starting.",
            )
            raise  # Unreachable but necessary for mypy.
        if authoritative_server != self.server_id:
            # This is NOT the correct server. Fail.
            await grpc_context.abort(
                IMPORT_grpc.StatusCode.UNAVAILABLE,
                f"Server '{self.server_id}' is not authoritative for this "
                f"request; server '{authoritative_server}' is.",
            )
            raise  # Unreachable but necessary for mypy.

        @IMPORT_reboot_aio_internals_middleware.maybe_run_function_twice_to_validate_effects
        async def _run(
            validating_effects: bool,
        ) -> google.protobuf.empty_pb2.Empty:
            context: IMPORT_typing.Optional[IMPORT_reboot_aio_contexts.Context] = None
            try:
                if headers.task_schedule is not None:
                    context, response = await self._schedule_DangerousFields(
                        headers=headers,
                        request=request,
                        grpc_context=grpc_context,
                    )
                    return response

                context = self.create_context(
                    headers=headers,
                    state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                    method='DangerousFields',
                    context_type=IMPORT_reboot_aio_contexts.WriterContext,
                )
                assert context is not None

                return await self._DangerousFields(
                    request,
                    context,
                    validating_effects=validating_effects,
                    grpc_context=grpc_context,
                )
            except IMPORT_reboot_aio_contexts.EffectValidationRetry:
                # Doing effect validation, just let this propagate.
                raise
            except IMPORT_reboot.aio.aborted.Aborted as aborted:
                status = IMPORT_rpc_status_sync.to_status(aborted.to_status())
                # Need to add transaction participants here because
                # calling `grpc_context.abort_with_status()` will
                # ignore any other trailing metadata. Only propagate
                # transaction participants metadata if the caller cares.
                # Callers that care are those that are themselves transactions.
                # It's important to not just send this information to everyone;
                # some clients can't tolerate trailers, see:
                #   https://github.com/reboot-dev/mono/issues/5081
                if context is not None and headers.transaction_ids is not None:
                    assert context.transaction_id is not None
                    status = status._replace(
                        trailing_metadata=status.trailing_metadata + context.participants.to_grpc_metadata()
                    )
                await grpc_context.abort_with_status(status)
                raise  # Unreachable but necessary for mypy.
            except IMPORT_asyncio.CancelledError:
                # It's pretty normal for an RPC to be cancelled; it's not useful to
                # print a stack trace.
                raise
            except BaseException as exception:
                # Print the exception stack trace for easier debugging. Note
                # that we don't include the stack trace in an error message
                # for the same reason that gRPC doesn't do so by default,
                # see https://github.com/grpc/grpc/issues/14897, but since this
                # should only get logged on the server side it is safe.
                logger.warning(
                    'Unhandled exception\n' +
                    ''.join(IMPORT_traceback.format_exc() if IMPORT_reboot_nodejs_python.should_print_stacktrace() else [f"{type(exception).__name__}: {exception}"])
                )

                # Re-raise the exception for gRPC to handle!
                #
                # TODO: gRPC will print a stack trace from this
                # exception which we don't want if we're executing via
                # Node.js.
                raise
            finally:
                # Propagate transaction participants, if the caller cares.
                # Callers that care are those that are themselves transactions.
                # It's important to not just send this information to everyone;
                # some clients can't tolerate trailers, see:
                #   https://github.com/reboot-dev/mono/issues/5081
                if context is not None and headers.transaction_ids is not None:
                    assert context.transaction_id is not None
                    grpc_context.set_trailing_metadata(
                        grpc_context.trailing_metadata() +
                        context.participants.to_grpc_metadata()
                    )

        with IMPORT_reboot_aio_tracing.context_from_headers(headers):
            return await _run()

    async def __StoreRecursiveMessage(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: tests.reboot.greeter_pb2.Greeter,
        request: tests.reboot.greeter_pb2.StoreRecursiveMessageRequest,
        *,
        validating_effects: bool,
    ) -> Greeter.StoreRecursiveMessageEffects:
        try:
            typed_state: Greeter.State = GreeterFromProto(state, is_initial_state=(context.state_id in states_being_constructed))

            response = (
                await self._servicer._StoreRecursiveMessage(
                    context=context,
                    state=typed_state,
                    request=request
                )
            )


            GreeterToProto(typed_state, state)

            IMPORT_reboot_aio_types.assert_type(
                response,
                [tests.reboot.greeter_pb2.StoreRecursiveMessageResponse],
            )
            self.maybe_raise_effect_validation_retry(
                logger=logger,
                idempotency_manager=context,
                method_name='Greeter.StoreRecursiveMessage',
                validating_effects=validating_effects,
                context=context,
            )
            return Greeter.StoreRecursiveMessageEffects(
                state=state,
                response=response,
                tasks=context._tasks,
                _colocated_upserts=context._colocated_upserts,
            )
        except IMPORT_reboot_aio_contexts.EffectValidationRetry:
            # Doing effect validation, just let this propagate.
            raise
        except IMPORT_reboot.aio.aborted.Aborted as aborted:
            # If the caller aborted due to a retryable error, just
            # propagate the aborted instead of propagating `Unknown`
            # so that a client can transparently retry.
            if IMPORT_reboot.aio.aborted.is_retryable(aborted):
                raise aborted
            # Log any _unhandled_ abort stack traces to make it
            # easier for debugging.
            #
            # NOTE: we don't log if we're a task as it will be logged
            # in `public/reboot/aio/internals/tasks_dispatcher.py` instead.
            aborted_type: IMPORT_typing.Optional[type] = None
            aborted_type = Greeter.StoreRecursiveMessageAborted
            if isinstance(aborted, IMPORT_reboot.aio.aborted.SystemAborted):
                # Not logging when within `node` as we already log there.
                if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                    logger.warning(
                        f"Unhandled (in 'tests.reboot.Greeter.StoreRecursiveMessage') {aborted}; propagating as 'Unknown'\n" +
                        ''.join(IMPORT_traceback.format_exception(aborted))
                    )
                raise IMPORT_reboot.aio.aborted.SystemAborted(
                    IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                    # TODO(benh): consider whether or not we want to
                    # include the 'package.service.method' which may
                    # get concatenated together forming a kind of
                    # "stack trace"; while it's super helpful for
                    # debugging, it does expose implementation
                    # information.
                    message=f"unhandled (in 'tests.reboot.Greeter.StoreRecursiveMessage') {aborted}"
                )
            else:
                if (
                    aborted_type is not None and
                    not isinstance(aborted, aborted_type) and
                    aborted_type.is_declared_error(aborted.error)
                ):
                    # We propagate declared errors that might have
                    # come from another call, i.e., we might have an
                    # `Aborted` but not for this method but the
                    # `Aborted` that we have has an error that this
                    # method declared. This allows a developer to
                    # simply add the declared error to their `.proto`
                    # file rather than having to catch and re-raise
                    # the error with their own aborted type.
                    if context.task is None:
                        logger.warning(
                            f"Propagating unhandled but declared error (in 'tests.reboot.Greeter.StoreRecursiveMessage') {aborted}"
                        )
                elif (
                    aborted_type is None or
                    not isinstance(aborted, aborted_type)
                ):
                    # Not logging when within `node` as we already log there.
                    if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                        logger.warning(
                            f"Unhandled (in 'tests.reboot.Greeter.StoreRecursiveMessage') {aborted}; propagating as 'Unknown'\n" +
                            ''.join(IMPORT_traceback.format_exception(aborted))
                        )
                    # If this wasn't a declared error than we
                    # propagate it as `Unknown`.
                    raise IMPORT_reboot.aio.aborted.SystemAborted(
                        IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                        # TODO(benh): consider whether or not we want to
                        # include the 'package.service.method' which may
                        # get concatenated together forming a kind of
                        # "stack trace"; while it's super helpful for
                        # debugging, it does expose implementation
                        # information.
                        message=f"unhandled (in 'tests.reboot.Greeter.StoreRecursiveMessage') {aborted}"
                    )

            raise
        except IMPORT_asyncio.CancelledError:
            # It's pretty normal for an RPC to be cancelled; it's not useful to
            # print a stack trace.
            raise
        except IMPORT_google_protobuf_message.DecodeError as decode_error:
            # We usually see this error when we are trying to construct a proto
            # message which is too deeply nested: protobuf has a limit of 100
            # nested messages. See the limits here:
            #   https://protobuf.dev/programming-guides/proto-limits/

            if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                logger.warning(
                    "Unhandled (in 'tests.reboot.Greeter.StoreRecursiveMessage') "
                    f"{type(decode_error).__name__}{': ' + str(decode_error) if len(str(decode_error)) > 0 else ''}; "
                    "This is usually caused by a deeply nested protobuf message, which is not supported by protobuf.\n"
                    "See the limits here: https://protobuf.dev/programming-guides/proto-limits/" +
                    ''.join(IMPORT_traceback.format_exception(decode_error))
                )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                message=f"unhandled (in 'tests.reboot.Greeter.StoreRecursiveMessage') {decode_error}; "
                        "This is usually caused by a deeply nested protobuf message, which is not supported by protobuf.\n"
                        "See the limits here: https://protobuf.dev/programming-guides/proto-limits/"
            )
        except BaseException as exception:
            # Not logging when within `node` as we already log there.
            if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                logger.warning(
                    "Unhandled (in 'tests.reboot.Greeter.StoreRecursiveMessage') "
                    f"{type(exception).__name__}{': ' + str(exception) if len(str(exception)) > 0 else ''}; "
                    "propagating as 'Unknown'\n" +
                    ''.join(IMPORT_traceback.format_exception(exception))
                )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                # TODO(benh): consider whether or not we want to
                # include the 'package.service.method' which may
                # get concatenated together forming a kind of
                # "stack trace"; while it's super helpful for
                # debugging, it does expose implementation
                # information.
                message=f"unhandled (in 'tests.reboot.Greeter.StoreRecursiveMessage') {type(exception).__name__}: {exception}"
            )
        finally:
            pass

    @IMPORT_reboot_aio_tracing.function_span(
        # We expect an `EffectValidationRetry` exception; that's not an error.
        set_status_on_exception=False
    )
    async def _StoreRecursiveMessage(
        self,
        request: tests.reboot.greeter_pb2.StoreRecursiveMessageRequest,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        *,
        validating_effects: bool,
        grpc_context: IMPORT_typing.Optional[IMPORT_grpc.aio.ServicerContext] = None,
    ) -> tests.reboot.greeter_pb2.StoreRecursiveMessageResponse:
        # Try to verify the token if a token verifier exists.
        context.auth = await self._maybe_verify_token(
            headers=context._headers, method='StoreRecursiveMessage'
        )

        # Check if we already have performed this mutation!
        #
        # We do this _before_ calling 'transactionally()' because
        # if this call is for a transaction method _and_ we've
        # already performed the transaction then we don't want to
        # become a transaction participant (again) we just want to
        # return the transaction's response.
        idempotent_mutation = await self._state_manager.check_for_idempotent_mutation(
            context
        )

        if idempotent_mutation is not None:
            response = tests.reboot.greeter_pb2.StoreRecursiveMessageResponse()
            response.ParseFromString(idempotent_mutation.response)
            return response

        async with self._state_manager.transactionally(
            context,
            self.tasks_dispatcher,
            aborted_type=Greeter.StoreRecursiveMessageAborted,
        ) as transaction:
            if transaction is not None:
                context.participants.add(
                    self._servicer.__state_type_name__, context._state_ref
                )
            async with self._state_manager.writer(
                context,
                self._servicer.__state_type__,
                self.tasks_dispatcher,
                authorize=self._maybe_authorize(
                    method_name='tests.reboot.GreeterMethods.StoreRecursiveMessage',
                    headers=context._headers,
                    auth=context.auth,
                    request=request,
                ),
                transaction=transaction,
                from_constructor=False,
                requires_constructor=True,
            ) as (state, writer):

                effects = await self.__StoreRecursiveMessage(
                    context,
                    state,
                    request,
                    validating_effects=validating_effects,
                )

                await writer.complete(effects)

                # TODO: We need a single `Effects` superclass for all methods, so we
                # would need to make it "partially" generic (with per-method subclasses
                # filling out the rest of the generic parameters) in order to fix this.
                return effects.response  # type: ignore[return-value]

    async def _schedule_StoreRecursiveMessage(
        self,
        *,
        request: tests.reboot.greeter_pb2.StoreRecursiveMessageRequest,
        headers: IMPORT_reboot_aio_headers.Headers,
        grpc_context: IMPORT_grpc.aio.ServicerContext,
    ) -> tuple[IMPORT_reboot_aio_contexts.WriterContext, tests.reboot.greeter_pb2.StoreRecursiveMessageResponse]:
        context: IMPORT_reboot_aio_contexts.WriterContext = self.create_context(
            headers=headers,
            state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            method='StoreRecursiveMessage',
            context_type=IMPORT_reboot_aio_contexts.WriterContext,
        )
        response = tests.reboot.greeter_pb2.StoreRecursiveMessageResponse()

        # Check if we already have performed this schedule! Note that
        # we need to do this for all kinds of methods because this is
        # effectively a mutation (actually a `writer`, see below).
        #
        # We do this _before_ calling 'transactionally()' because
        # if this call is for a transaction method _and_ we've
        # already performed the transaction then we don't want to
        # become a transaction participant (again) we just want to
        # return the transaction's response.
        idempotent_mutation = await self._state_manager.check_for_idempotent_mutation(
            context
        )

        if idempotent_mutation is not None:
            response.ParseFromString(idempotent_mutation.response)

            # We should have only scheduled a single task!
            assert len(idempotent_mutation.task_ids) == 1
            assert grpc_context is not None
            grpc_context.set_trailing_metadata(
                grpc_context.trailing_metadata() +
                (
                    (
                        IMPORT_reboot_aio_headers.TASK_ID_UUID,
                        str(IMPORT_uuid.UUID(bytes=idempotent_mutation.task_ids[0].task_uuid))
                    ),
                )
            )

            return context, response

        async with self._state_manager.transactionally(
            context,
            self.tasks_dispatcher,
            aborted_type=Greeter.StoreRecursiveMessageAborted,
        ) as transaction:
            if transaction is not None:
                context.participants.add(
                    self._servicer.__state_type_name__, context._state_ref
                )

            # Try to verify the token if a token verifier exists.
            context.auth = await self._maybe_verify_token(
                headers=headers, method='StoreRecursiveMessage'
            )

            async with self._state_manager.writer(
                context,
                self._servicer.__state_type__,
                self.tasks_dispatcher,
                transaction=transaction,
                authorize=self._maybe_authorize(
                    method_name='tests.reboot.GreeterMethods.StoreRecursiveMessage',
                    headers=context._headers,
                    auth=context.auth,
                    request=request,
                ),
                from_constructor=False,
                requires_constructor=True
            ) as (state, writer):

                task = await GreeterServicerTasks(
                    context=context,
                    state_ref=context._state_ref,
                ).StoreRecursiveMessage(
                    GreeterStoreRecursiveMessageRequestFromProto(request),
                    schedule=context._headers.task_schedule,
                )

                effects = IMPORT_reboot_aio_state_managers.Effects(
                    response=response,
                    state=state,
                    tasks=[task],
                )

                assert effects.tasks is not None

                await writer.complete(effects)

                assert grpc_context is not None

                grpc_context.set_trailing_metadata(
                    grpc_context.trailing_metadata() +
                    (
                        (
                            IMPORT_reboot_aio_headers.TASK_ID_UUID,
                            str(IMPORT_uuid.UUID(bytes=task.task_id.task_uuid))
                        ),
                    )
                )

                return context, response

        return context, response


    # Entrypoint for non-reactive network calls (i.e. typical gRPC calls).
    async def StoreRecursiveMessage(
        self,
        request: tests.reboot.greeter_pb2.StoreRecursiveMessageRequest,
        grpc_context: IMPORT_grpc.aio.ServicerContext,
    ) -> tests.reboot.greeter_pb2.StoreRecursiveMessageResponse:
        headers = IMPORT_reboot_aio_headers.Headers.from_grpc_context(grpc_context)
        assert headers.application_id is not None  # Guaranteed by `Headers`.

        # Confirm whether this is the right server to be serving this
        # request.
        try:
            authoritative_server = self.placement_client.server_for_actor(
                headers.application_id,
                headers.state_ref,
            )
        except IMPORT_reboot_aio_placement.UnknownApplicationError:
            # It's possible that the user did indeed type an application ID
            # that doesn't exist, but it's also quite possible that this
            # request reached us before the placement planner had gossipped
            # out the information about which applications exist (we see
            # this e.g. after `rbt dev`'s chaos monkey restarts). For that
            # reason, abort with a retryable error.
            await grpc_context.abort(
                IMPORT_grpc.StatusCode.UNAVAILABLE,
                f"Application '{headers.application_id}' not found. If you "
                "are confident the application exists, this may be because "
                "the system is still starting.",
            )
            raise  # Unreachable but necessary for mypy.
        if authoritative_server != self.server_id:
            # This is NOT the correct server. Fail.
            await grpc_context.abort(
                IMPORT_grpc.StatusCode.UNAVAILABLE,
                f"Server '{self.server_id}' is not authoritative for this "
                f"request; server '{authoritative_server}' is.",
            )
            raise  # Unreachable but necessary for mypy.

        @IMPORT_reboot_aio_internals_middleware.maybe_run_function_twice_to_validate_effects
        async def _run(
            validating_effects: bool,
        ) -> tests.reboot.greeter_pb2.StoreRecursiveMessageResponse:
            context: IMPORT_typing.Optional[IMPORT_reboot_aio_contexts.Context] = None
            try:
                if headers.task_schedule is not None:
                    context, response = await self._schedule_StoreRecursiveMessage(
                        headers=headers,
                        request=request,
                        grpc_context=grpc_context,
                    )
                    return response

                context = self.create_context(
                    headers=headers,
                    state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                    method='StoreRecursiveMessage',
                    context_type=IMPORT_reboot_aio_contexts.WriterContext,
                )
                assert context is not None

                return await self._StoreRecursiveMessage(
                    request,
                    context,
                    validating_effects=validating_effects,
                    grpc_context=grpc_context,
                )
            except IMPORT_reboot_aio_contexts.EffectValidationRetry:
                # Doing effect validation, just let this propagate.
                raise
            except IMPORT_reboot.aio.aborted.Aborted as aborted:
                status = IMPORT_rpc_status_sync.to_status(aborted.to_status())
                # Need to add transaction participants here because
                # calling `grpc_context.abort_with_status()` will
                # ignore any other trailing metadata. Only propagate
                # transaction participants metadata if the caller cares.
                # Callers that care are those that are themselves transactions.
                # It's important to not just send this information to everyone;
                # some clients can't tolerate trailers, see:
                #   https://github.com/reboot-dev/mono/issues/5081
                if context is not None and headers.transaction_ids is not None:
                    assert context.transaction_id is not None
                    status = status._replace(
                        trailing_metadata=status.trailing_metadata + context.participants.to_grpc_metadata()
                    )
                await grpc_context.abort_with_status(status)
                raise  # Unreachable but necessary for mypy.
            except IMPORT_asyncio.CancelledError:
                # It's pretty normal for an RPC to be cancelled; it's not useful to
                # print a stack trace.
                raise
            except BaseException as exception:
                # Print the exception stack trace for easier debugging. Note
                # that we don't include the stack trace in an error message
                # for the same reason that gRPC doesn't do so by default,
                # see https://github.com/grpc/grpc/issues/14897, but since this
                # should only get logged on the server side it is safe.
                logger.warning(
                    'Unhandled exception\n' +
                    ''.join(IMPORT_traceback.format_exc() if IMPORT_reboot_nodejs_python.should_print_stacktrace() else [f"{type(exception).__name__}: {exception}"])
                )

                # Re-raise the exception for gRPC to handle!
                #
                # TODO: gRPC will print a stack trace from this
                # exception which we don't want if we're executing via
                # Node.js.
                raise
            finally:
                # Propagate transaction participants, if the caller cares.
                # Callers that care are those that are themselves transactions.
                # It's important to not just send this information to everyone;
                # some clients can't tolerate trailers, see:
                #   https://github.com/reboot-dev/mono/issues/5081
                if context is not None and headers.transaction_ids is not None:
                    assert context.transaction_id is not None
                    grpc_context.set_trailing_metadata(
                        grpc_context.trailing_metadata() +
                        context.participants.to_grpc_metadata()
                    )

        with IMPORT_reboot_aio_tracing.context_from_headers(headers):
            return await _run()

    async def __ReadRecursiveMessage(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: tests.reboot.greeter_pb2.Greeter,
        request: tests.reboot.greeter_pb2.ReadRecursiveMessageRequest,
        *,
        validating_effects: bool,
    ) -> tests.reboot.greeter_pb2.ReadRecursiveMessageResponse:
        try:
            typed_state: Greeter.State = GreeterFromProto(state, is_initial_state=(context.state_id in states_being_constructed))

            response = (
                await self._servicer._ReadRecursiveMessage(
                    context=context,
                    state=typed_state,
                    request=request
                )
            )


            GreeterToProto(typed_state, state)

            IMPORT_reboot_aio_types.assert_type(
                response,
                [tests.reboot.greeter_pb2.ReadRecursiveMessageResponse],
            )
            self.maybe_raise_effect_validation_retry(
                logger=logger,
                idempotency_manager=context,
                method_name='Greeter.ReadRecursiveMessage',
                validating_effects=validating_effects,
                context=context,
            )
            return response
        except IMPORT_reboot_aio_contexts.EffectValidationRetry:
            # Doing effect validation, just let this propagate.
            raise
        except IMPORT_reboot.aio.aborted.Aborted as aborted:
            # If the caller aborted due to a retryable error, just
            # propagate the aborted instead of propagating `Unknown`
            # so that a client can transparently retry.
            if IMPORT_reboot.aio.aborted.is_retryable(aborted):
                raise aborted
            # Log any _unhandled_ abort stack traces to make it
            # easier for debugging.
            #
            # NOTE: we don't log if we're a task as it will be logged
            # in `public/reboot/aio/internals/tasks_dispatcher.py` instead.
            aborted_type: IMPORT_typing.Optional[type] = None
            aborted_type = Greeter.ReadRecursiveMessageAborted
            if isinstance(aborted, IMPORT_reboot.aio.aborted.SystemAborted):
                # Not logging when within `node` as we already log there.
                if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                    logger.warning(
                        f"Unhandled (in 'tests.reboot.Greeter.ReadRecursiveMessage') {aborted}; propagating as 'Unknown'\n" +
                        ''.join(IMPORT_traceback.format_exception(aborted))
                    )
                raise IMPORT_reboot.aio.aborted.SystemAborted(
                    IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                    # TODO(benh): consider whether or not we want to
                    # include the 'package.service.method' which may
                    # get concatenated together forming a kind of
                    # "stack trace"; while it's super helpful for
                    # debugging, it does expose implementation
                    # information.
                    message=f"unhandled (in 'tests.reboot.Greeter.ReadRecursiveMessage') {aborted}"
                )
            else:
                if (
                    aborted_type is not None and
                    not isinstance(aborted, aborted_type) and
                    aborted_type.is_declared_error(aborted.error)
                ):
                    # We propagate declared errors that might have
                    # come from another call, i.e., we might have an
                    # `Aborted` but not for this method but the
                    # `Aborted` that we have has an error that this
                    # method declared. This allows a developer to
                    # simply add the declared error to their `.proto`
                    # file rather than having to catch and re-raise
                    # the error with their own aborted type.
                    if context.task is None:
                        logger.warning(
                            f"Propagating unhandled but declared error (in 'tests.reboot.Greeter.ReadRecursiveMessage') {aborted}"
                        )
                elif (
                    aborted_type is None or
                    not isinstance(aborted, aborted_type)
                ):
                    # Not logging when within `node` as we already log there.
                    if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                        logger.warning(
                            f"Unhandled (in 'tests.reboot.Greeter.ReadRecursiveMessage') {aborted}; propagating as 'Unknown'\n" +
                            ''.join(IMPORT_traceback.format_exception(aborted))
                        )
                    # If this wasn't a declared error than we
                    # propagate it as `Unknown`.
                    raise IMPORT_reboot.aio.aborted.SystemAborted(
                        IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                        # TODO(benh): consider whether or not we want to
                        # include the 'package.service.method' which may
                        # get concatenated together forming a kind of
                        # "stack trace"; while it's super helpful for
                        # debugging, it does expose implementation
                        # information.
                        message=f"unhandled (in 'tests.reboot.Greeter.ReadRecursiveMessage') {aborted}"
                    )

            raise
        except IMPORT_asyncio.CancelledError:
            # It's pretty normal for an RPC to be cancelled; it's not useful to
            # print a stack trace.
            raise
        except IMPORT_google_protobuf_message.DecodeError as decode_error:
            # We usually see this error when we are trying to construct a proto
            # message which is too deeply nested: protobuf has a limit of 100
            # nested messages. See the limits here:
            #   https://protobuf.dev/programming-guides/proto-limits/

            if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                logger.warning(
                    "Unhandled (in 'tests.reboot.Greeter.ReadRecursiveMessage') "
                    f"{type(decode_error).__name__}{': ' + str(decode_error) if len(str(decode_error)) > 0 else ''}; "
                    "This is usually caused by a deeply nested protobuf message, which is not supported by protobuf.\n"
                    "See the limits here: https://protobuf.dev/programming-guides/proto-limits/" +
                    ''.join(IMPORT_traceback.format_exception(decode_error))
                )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                message=f"unhandled (in 'tests.reboot.Greeter.ReadRecursiveMessage') {decode_error}; "
                        "This is usually caused by a deeply nested protobuf message, which is not supported by protobuf.\n"
                        "See the limits here: https://protobuf.dev/programming-guides/proto-limits/"
            )
        except BaseException as exception:
            # Not logging when within `node` as we already log there.
            if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                logger.warning(
                    "Unhandled (in 'tests.reboot.Greeter.ReadRecursiveMessage') "
                    f"{type(exception).__name__}{': ' + str(exception) if len(str(exception)) > 0 else ''}; "
                    "propagating as 'Unknown'\n" +
                    ''.join(IMPORT_traceback.format_exception(exception))
                )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                # TODO(benh): consider whether or not we want to
                # include the 'package.service.method' which may
                # get concatenated together forming a kind of
                # "stack trace"; while it's super helpful for
                # debugging, it does expose implementation
                # information.
                message=f"unhandled (in 'tests.reboot.Greeter.ReadRecursiveMessage') {type(exception).__name__}: {exception}"
            )
        finally:
            pass

    @IMPORT_reboot_aio_tracing.function_span(
        # We expect an `EffectValidationRetry` exception; that's not an error.
        set_status_on_exception=False
    )
    async def _ReadRecursiveMessage(
        self,
        request: tests.reboot.greeter_pb2.ReadRecursiveMessageRequest,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        *,
        validating_effects: bool,
        grpc_context: IMPORT_typing.Optional[IMPORT_grpc.aio.ServicerContext] = None,
    ) -> tests.reboot.greeter_pb2.ReadRecursiveMessageResponse:
        # Try to verify the token if a token verifier exists.
        context.auth = await self._maybe_verify_token(
            headers=context._headers, method='ReadRecursiveMessage'
        )


        async with self._state_manager.transactionally(
            context,
            self.tasks_dispatcher,
            aborted_type=Greeter.ReadRecursiveMessageAborted,
        ) as transaction:
            if transaction is not None:
                context.participants.add(
                    self._servicer.__state_type_name__, context._state_ref
                )
            authorizer = self._maybe_authorize(
                method_name='tests.reboot.GreeterMethods.ReadRecursiveMessage',
                headers=context._headers,
                auth=context.auth,
                request=request,
            )
            async with self._state_manager.reader(
                context,
                self._servicer.__state_type__,
                authorize=authorizer,
            ) as state:
                response = await self.__ReadRecursiveMessage(
                    context,
                    state,
                    request,
                    validating_effects=validating_effects,
                )
                return response

    async def _schedule_ReadRecursiveMessage(
        self,
        *,
        request: tests.reboot.greeter_pb2.ReadRecursiveMessageRequest,
        headers: IMPORT_reboot_aio_headers.Headers,
        grpc_context: IMPORT_grpc.aio.ServicerContext,
    ) -> tuple[IMPORT_reboot_aio_contexts.WriterContext, tests.reboot.greeter_pb2.ReadRecursiveMessageResponse]:
        context: IMPORT_reboot_aio_contexts.WriterContext = self.create_context(
            headers=headers,
            state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            method='ReadRecursiveMessage',
            context_type=IMPORT_reboot_aio_contexts.WriterContext,
        )
        response = tests.reboot.greeter_pb2.ReadRecursiveMessageResponse()

        # Check if we already have performed this schedule! Note that
        # we need to do this for all kinds of methods because this is
        # effectively a mutation (actually a `writer`, see below).
        #
        # We do this _before_ calling 'transactionally()' because
        # if this call is for a transaction method _and_ we've
        # already performed the transaction then we don't want to
        # become a transaction participant (again) we just want to
        # return the transaction's response.
        idempotent_mutation = await self._state_manager.check_for_idempotent_mutation(
            context
        )

        if idempotent_mutation is not None:
            response.ParseFromString(idempotent_mutation.response)

            # We should have only scheduled a single task!
            assert len(idempotent_mutation.task_ids) == 1
            assert grpc_context is not None
            grpc_context.set_trailing_metadata(
                grpc_context.trailing_metadata() +
                (
                    (
                        IMPORT_reboot_aio_headers.TASK_ID_UUID,
                        str(IMPORT_uuid.UUID(bytes=idempotent_mutation.task_ids[0].task_uuid))
                    ),
                )
            )

            return context, response

        async with self._state_manager.transactionally(
            context,
            self.tasks_dispatcher,
            aborted_type=Greeter.ReadRecursiveMessageAborted,
        ) as transaction:
            if transaction is not None:
                context.participants.add(
                    self._servicer.__state_type_name__, context._state_ref
                )

            # Try to verify the token if a token verifier exists.
            context.auth = await self._maybe_verify_token(
                headers=headers, method='ReadRecursiveMessage'
            )

            async with self._state_manager.writer(
                context,
                self._servicer.__state_type__,
                self.tasks_dispatcher,
                transaction=transaction,
                authorize=self._maybe_authorize(
                    method_name='tests.reboot.GreeterMethods.ReadRecursiveMessage',
                    headers=context._headers,
                    auth=context.auth,
                    request=request,
                ),
                from_constructor=False,
                requires_constructor=True
            ) as (state, writer):

                task = await GreeterServicerTasks(
                    context=context,
                    state_ref=context._state_ref,
                ).ReadRecursiveMessage(
                    GreeterReadRecursiveMessageRequestFromProto(request),
                    schedule=context._headers.task_schedule,
                )

                effects = IMPORT_reboot_aio_state_managers.Effects(
                    response=response,
                    state=state,
                    tasks=[task],
                )

                assert effects.tasks is not None

                await writer.complete(effects)

                assert grpc_context is not None

                grpc_context.set_trailing_metadata(
                    grpc_context.trailing_metadata() +
                    (
                        (
                            IMPORT_reboot_aio_headers.TASK_ID_UUID,
                            str(IMPORT_uuid.UUID(bytes=task.task_id.task_uuid))
                        ),
                    )
                )

                return context, response

        return context, response


    # Entrypoint for non-reactive network calls (i.e. typical gRPC calls).
    async def ReadRecursiveMessage(
        self,
        request: tests.reboot.greeter_pb2.ReadRecursiveMessageRequest,
        grpc_context: IMPORT_grpc.aio.ServicerContext,
    ) -> tests.reboot.greeter_pb2.ReadRecursiveMessageResponse:
        headers = IMPORT_reboot_aio_headers.Headers.from_grpc_context(grpc_context)
        assert headers.application_id is not None  # Guaranteed by `Headers`.

        # Confirm whether this is the right server to be serving this
        # request.
        try:
            authoritative_server = self.placement_client.server_for_actor(
                headers.application_id,
                headers.state_ref,
            )
        except IMPORT_reboot_aio_placement.UnknownApplicationError:
            # It's possible that the user did indeed type an application ID
            # that doesn't exist, but it's also quite possible that this
            # request reached us before the placement planner had gossipped
            # out the information about which applications exist (we see
            # this e.g. after `rbt dev`'s chaos monkey restarts). For that
            # reason, abort with a retryable error.
            await grpc_context.abort(
                IMPORT_grpc.StatusCode.UNAVAILABLE,
                f"Application '{headers.application_id}' not found. If you "
                "are confident the application exists, this may be because "
                "the system is still starting.",
            )
            raise  # Unreachable but necessary for mypy.
        if authoritative_server != self.server_id:
            # This is NOT the correct server. Fail.
            await grpc_context.abort(
                IMPORT_grpc.StatusCode.UNAVAILABLE,
                f"Server '{self.server_id}' is not authoritative for this "
                f"request; server '{authoritative_server}' is.",
            )
            raise  # Unreachable but necessary for mypy.

        @IMPORT_reboot_aio_internals_middleware.maybe_run_function_twice_to_validate_effects
        async def _run(
            validating_effects: bool,
        ) -> tests.reboot.greeter_pb2.ReadRecursiveMessageResponse:
            context: IMPORT_typing.Optional[IMPORT_reboot_aio_contexts.Context] = None
            try:
                if headers.task_schedule is not None:
                    context, response = await self._schedule_ReadRecursiveMessage(
                        headers=headers,
                        request=request,
                        grpc_context=grpc_context,
                    )
                    return response

                context = self.create_context(
                    headers=headers,
                    state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                    method='ReadRecursiveMessage',
                    context_type=IMPORT_reboot_aio_contexts.ReaderContext,
                )
                assert context is not None

                return await self._ReadRecursiveMessage(
                    request,
                    context,
                    validating_effects=validating_effects,
                    grpc_context=grpc_context,
                )
            except IMPORT_reboot_aio_contexts.EffectValidationRetry:
                # Doing effect validation, just let this propagate.
                raise
            except IMPORT_reboot.aio.aborted.Aborted as aborted:
                status = IMPORT_rpc_status_sync.to_status(aborted.to_status())
                # Need to add transaction participants here because
                # calling `grpc_context.abort_with_status()` will
                # ignore any other trailing metadata. Only propagate
                # transaction participants metadata if the caller cares.
                # Callers that care are those that are themselves transactions.
                # It's important to not just send this information to everyone;
                # some clients can't tolerate trailers, see:
                #   https://github.com/reboot-dev/mono/issues/5081
                if context is not None and headers.transaction_ids is not None:
                    assert context.transaction_id is not None
                    status = status._replace(
                        trailing_metadata=status.trailing_metadata + context.participants.to_grpc_metadata()
                    )
                await grpc_context.abort_with_status(status)
                raise  # Unreachable but necessary for mypy.
            except IMPORT_asyncio.CancelledError:
                # It's pretty normal for an RPC to be cancelled; it's not useful to
                # print a stack trace.
                raise
            except BaseException as exception:
                # Print the exception stack trace for easier debugging. Note
                # that we don't include the stack trace in an error message
                # for the same reason that gRPC doesn't do so by default,
                # see https://github.com/grpc/grpc/issues/14897, but since this
                # should only get logged on the server side it is safe.
                logger.warning(
                    'Unhandled exception\n' +
                    ''.join(IMPORT_traceback.format_exc() if IMPORT_reboot_nodejs_python.should_print_stacktrace() else [f"{type(exception).__name__}: {exception}"])
                )

                # Re-raise the exception for gRPC to handle!
                #
                # TODO: gRPC will print a stack trace from this
                # exception which we don't want if we're executing via
                # Node.js.
                raise
            finally:
                # Propagate transaction participants, if the caller cares.
                # Callers that care are those that are themselves transactions.
                # It's important to not just send this information to everyone;
                # some clients can't tolerate trailers, see:
                #   https://github.com/reboot-dev/mono/issues/5081
                if context is not None and headers.transaction_ids is not None:
                    assert context.transaction_id is not None
                    grpc_context.set_trailing_metadata(
                        grpc_context.trailing_metadata() +
                        context.participants.to_grpc_metadata()
                    )

        with IMPORT_reboot_aio_tracing.context_from_headers(headers):
            return await _run()

    async def __ConstructAndStoreRecursiveMessage(
        self,
        context: IMPORT_reboot_aio_contexts.TransactionContext,
        state: tests.reboot.greeter_pb2.Greeter,
        request: tests.reboot.greeter_pb2.ConstructAndStoreRecursiveMessageRequest,
        *,
        validating_effects: bool,
    ) -> tests.reboot.greeter_pb2.ConstructAndStoreRecursiveMessageResponse:
        try:
            typed_state: Greeter.State = GreeterFromProto(state, is_initial_state=(context.state_id in states_being_constructed))

            response = (
                await self._servicer._ConstructAndStoreRecursiveMessage(
                    context=context,
                    state=typed_state,
                    request=request
                )
            )


            GreeterToProto(typed_state, state)

            IMPORT_reboot_aio_types.assert_type(
                response,
                [tests.reboot.greeter_pb2.ConstructAndStoreRecursiveMessageResponse],
            )
            self.maybe_raise_effect_validation_retry(
                logger=logger,
                idempotency_manager=context,
                method_name='Greeter.ConstructAndStoreRecursiveMessage',
                validating_effects=validating_effects,
                context=context,
            )
            return response
        except IMPORT_reboot_aio_contexts.EffectValidationRetry:
            # Doing effect validation, just let this propagate.
            raise
        except IMPORT_reboot.aio.aborted.Aborted as aborted:
            # If the caller aborted due to a retryable error, just
            # propagate the aborted instead of propagating `Unknown`
            # so that a client can transparently retry.
            if IMPORT_reboot.aio.aborted.is_retryable(aborted):
                raise aborted
            # Log any _unhandled_ abort stack traces to make it
            # easier for debugging.
            #
            # NOTE: we don't log if we're a task as it will be logged
            # in `public/reboot/aio/internals/tasks_dispatcher.py` instead.
            aborted_type: IMPORT_typing.Optional[type] = None
            aborted_type = Greeter.ConstructAndStoreRecursiveMessageAborted
            if isinstance(aborted, IMPORT_reboot.aio.aborted.SystemAborted):
                # Not logging when within `node` as we already log there.
                if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                    logger.warning(
                        f"Unhandled (in 'tests.reboot.Greeter.ConstructAndStoreRecursiveMessage') {aborted}; propagating as 'Unknown'\n" +
                        ''.join(IMPORT_traceback.format_exception(aborted))
                    )
                raise IMPORT_reboot.aio.aborted.SystemAborted(
                    IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                    # TODO(benh): consider whether or not we want to
                    # include the 'package.service.method' which may
                    # get concatenated together forming a kind of
                    # "stack trace"; while it's super helpful for
                    # debugging, it does expose implementation
                    # information.
                    message=f"unhandled (in 'tests.reboot.Greeter.ConstructAndStoreRecursiveMessage') {aborted}"
                )
            else:
                if (
                    aborted_type is not None and
                    not isinstance(aborted, aborted_type) and
                    aborted_type.is_declared_error(aborted.error)
                ):
                    # We propagate declared errors that might have
                    # come from another call, i.e., we might have an
                    # `Aborted` but not for this method but the
                    # `Aborted` that we have has an error that this
                    # method declared. This allows a developer to
                    # simply add the declared error to their `.proto`
                    # file rather than having to catch and re-raise
                    # the error with their own aborted type.
                    if context.task is None:
                        logger.warning(
                            f"Propagating unhandled but declared error (in 'tests.reboot.Greeter.ConstructAndStoreRecursiveMessage') {aborted}"
                        )
                elif (
                    aborted_type is None or
                    not isinstance(aborted, aborted_type)
                ):
                    # Not logging when within `node` as we already log there.
                    if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                        logger.warning(
                            f"Unhandled (in 'tests.reboot.Greeter.ConstructAndStoreRecursiveMessage') {aborted}; propagating as 'Unknown'\n" +
                            ''.join(IMPORT_traceback.format_exception(aborted))
                        )
                    # If this wasn't a declared error than we
                    # propagate it as `Unknown`.
                    raise IMPORT_reboot.aio.aborted.SystemAborted(
                        IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                        # TODO(benh): consider whether or not we want to
                        # include the 'package.service.method' which may
                        # get concatenated together forming a kind of
                        # "stack trace"; while it's super helpful for
                        # debugging, it does expose implementation
                        # information.
                        message=f"unhandled (in 'tests.reboot.Greeter.ConstructAndStoreRecursiveMessage') {aborted}"
                    )

            raise
        except IMPORT_asyncio.CancelledError:
            # It's pretty normal for an RPC to be cancelled; it's not useful to
            # print a stack trace.
            raise
        except IMPORT_google_protobuf_message.DecodeError as decode_error:
            # We usually see this error when we are trying to construct a proto
            # message which is too deeply nested: protobuf has a limit of 100
            # nested messages. See the limits here:
            #   https://protobuf.dev/programming-guides/proto-limits/

            if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                logger.warning(
                    "Unhandled (in 'tests.reboot.Greeter.ConstructAndStoreRecursiveMessage') "
                    f"{type(decode_error).__name__}{': ' + str(decode_error) if len(str(decode_error)) > 0 else ''}; "
                    "This is usually caused by a deeply nested protobuf message, which is not supported by protobuf.\n"
                    "See the limits here: https://protobuf.dev/programming-guides/proto-limits/" +
                    ''.join(IMPORT_traceback.format_exception(decode_error))
                )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                message=f"unhandled (in 'tests.reboot.Greeter.ConstructAndStoreRecursiveMessage') {decode_error}; "
                        "This is usually caused by a deeply nested protobuf message, which is not supported by protobuf.\n"
                        "See the limits here: https://protobuf.dev/programming-guides/proto-limits/"
            )
        except BaseException as exception:
            # Not logging when within `node` as we already log there.
            if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                logger.warning(
                    "Unhandled (in 'tests.reboot.Greeter.ConstructAndStoreRecursiveMessage') "
                    f"{type(exception).__name__}{': ' + str(exception) if len(str(exception)) > 0 else ''}; "
                    "propagating as 'Unknown'\n" +
                    ''.join(IMPORT_traceback.format_exception(exception))
                )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                # TODO(benh): consider whether or not we want to
                # include the 'package.service.method' which may
                # get concatenated together forming a kind of
                # "stack trace"; while it's super helpful for
                # debugging, it does expose implementation
                # information.
                message=f"unhandled (in 'tests.reboot.Greeter.ConstructAndStoreRecursiveMessage') {type(exception).__name__}: {exception}"
            )
        finally:
            pass

    @IMPORT_reboot_aio_tracing.function_span(
        # We expect an `EffectValidationRetry` exception; that's not an error.
        set_status_on_exception=False
    )
    async def _ConstructAndStoreRecursiveMessage(
        self,
        request: tests.reboot.greeter_pb2.ConstructAndStoreRecursiveMessageRequest,
        context: IMPORT_reboot_aio_contexts.TransactionContext,
        *,
        validating_effects: bool,
        grpc_context: IMPORT_typing.Optional[IMPORT_grpc.aio.ServicerContext] = None,
    ) -> tests.reboot.greeter_pb2.ConstructAndStoreRecursiveMessageResponse:
        # Try to verify the token if a token verifier exists.
        context.auth = await self._maybe_verify_token(
            headers=context._headers, method='ConstructAndStoreRecursiveMessage'
        )

        # Check if we already have performed this mutation!
        #
        # We do this _before_ calling 'transactionally()' because
        # if this call is for a transaction method _and_ we've
        # already performed the transaction then we don't want to
        # become a transaction participant (again) we just want to
        # return the transaction's response.
        idempotent_mutation = await self._state_manager.check_for_idempotent_mutation(
            context
        )

        if idempotent_mutation is not None:
            response = tests.reboot.greeter_pb2.ConstructAndStoreRecursiveMessageResponse()
            response.ParseFromString(idempotent_mutation.response)
            return response

        async with self._state_manager.transactionally(
            context,
            self.tasks_dispatcher,
            aborted_type=Greeter.ConstructAndStoreRecursiveMessageAborted,
        ) as transaction:
            if transaction is not None:
                context.participants.add(
                    self._servicer.__state_type_name__, context._state_ref
                )
            assert transaction is not None
            async with self._state_manager.transaction(
                context,
                self._servicer.__state_type__,
                transaction,
                authorize=self._maybe_authorize(
                    method_name='tests.reboot.GreeterMethods.ConstructAndStoreRecursiveMessage',
                    headers=context._headers,
                    auth=context.auth,
                    request=request,
                ),
                from_constructor=False,
                requires_constructor=True
            ) as (state, complete):

                response = await self.__ConstructAndStoreRecursiveMessage(
                    context,
                    state,
                    request,
                    validating_effects=validating_effects,
                )

                await complete(
                    IMPORT_reboot_aio_state_managers.Effects(
                        state=state,
                        response=response,
                    )
                )
                return response

    async def _schedule_ConstructAndStoreRecursiveMessage(
        self,
        *,
        request: tests.reboot.greeter_pb2.ConstructAndStoreRecursiveMessageRequest,
        headers: IMPORT_reboot_aio_headers.Headers,
        grpc_context: IMPORT_grpc.aio.ServicerContext,
    ) -> tuple[IMPORT_reboot_aio_contexts.WriterContext, tests.reboot.greeter_pb2.ConstructAndStoreRecursiveMessageResponse]:
        context: IMPORT_reboot_aio_contexts.WriterContext = self.create_context(
            headers=headers,
            state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            method='ConstructAndStoreRecursiveMessage',
            context_type=IMPORT_reboot_aio_contexts.WriterContext,
        )
        response = tests.reboot.greeter_pb2.ConstructAndStoreRecursiveMessageResponse()

        # Check if we already have performed this schedule! Note that
        # we need to do this for all kinds of methods because this is
        # effectively a mutation (actually a `writer`, see below).
        #
        # We do this _before_ calling 'transactionally()' because
        # if this call is for a transaction method _and_ we've
        # already performed the transaction then we don't want to
        # become a transaction participant (again) we just want to
        # return the transaction's response.
        idempotent_mutation = await self._state_manager.check_for_idempotent_mutation(
            context
        )

        if idempotent_mutation is not None:
            response.ParseFromString(idempotent_mutation.response)

            # We should have only scheduled a single task!
            assert len(idempotent_mutation.task_ids) == 1
            assert grpc_context is not None
            grpc_context.set_trailing_metadata(
                grpc_context.trailing_metadata() +
                (
                    (
                        IMPORT_reboot_aio_headers.TASK_ID_UUID,
                        str(IMPORT_uuid.UUID(bytes=idempotent_mutation.task_ids[0].task_uuid))
                    ),
                )
            )

            return context, response

        async with self._state_manager.transactionally(
            context,
            self.tasks_dispatcher,
            aborted_type=Greeter.ConstructAndStoreRecursiveMessageAborted,
        ) as transaction:
            if transaction is not None:
                context.participants.add(
                    self._servicer.__state_type_name__, context._state_ref
                )

            # Try to verify the token if a token verifier exists.
            context.auth = await self._maybe_verify_token(
                headers=headers, method='ConstructAndStoreRecursiveMessage'
            )

            async with self._state_manager.writer(
                context,
                self._servicer.__state_type__,
                self.tasks_dispatcher,
                transaction=transaction,
                authorize=self._maybe_authorize(
                    method_name='tests.reboot.GreeterMethods.ConstructAndStoreRecursiveMessage',
                    headers=context._headers,
                    auth=context.auth,
                    request=request,
                ),
                from_constructor=False,
                requires_constructor=True
            ) as (state, writer):

                task = await GreeterServicerTasks(
                    context=context,
                    state_ref=context._state_ref,
                ).ConstructAndStoreRecursiveMessage(
                    GreeterConstructAndStoreRecursiveMessageRequestFromProto(request),
                    schedule=context._headers.task_schedule,
                )

                effects = IMPORT_reboot_aio_state_managers.Effects(
                    response=response,
                    state=state,
                    tasks=[task],
                )

                assert effects.tasks is not None

                await writer.complete(effects)

                assert grpc_context is not None

                grpc_context.set_trailing_metadata(
                    grpc_context.trailing_metadata() +
                    (
                        (
                            IMPORT_reboot_aio_headers.TASK_ID_UUID,
                            str(IMPORT_uuid.UUID(bytes=task.task_id.task_uuid))
                        ),
                    )
                )

                return context, response

        return context, response


    # Entrypoint for non-reactive network calls (i.e. typical gRPC calls).
    async def ConstructAndStoreRecursiveMessage(
        self,
        request: tests.reboot.greeter_pb2.ConstructAndStoreRecursiveMessageRequest,
        grpc_context: IMPORT_grpc.aio.ServicerContext,
    ) -> tests.reboot.greeter_pb2.ConstructAndStoreRecursiveMessageResponse:
        headers = IMPORT_reboot_aio_headers.Headers.from_grpc_context(grpc_context)
        assert headers.application_id is not None  # Guaranteed by `Headers`.

        # Confirm whether this is the right server to be serving this
        # request.
        try:
            authoritative_server = self.placement_client.server_for_actor(
                headers.application_id,
                headers.state_ref,
            )
        except IMPORT_reboot_aio_placement.UnknownApplicationError:
            # It's possible that the user did indeed type an application ID
            # that doesn't exist, but it's also quite possible that this
            # request reached us before the placement planner had gossipped
            # out the information about which applications exist (we see
            # this e.g. after `rbt dev`'s chaos monkey restarts). For that
            # reason, abort with a retryable error.
            await grpc_context.abort(
                IMPORT_grpc.StatusCode.UNAVAILABLE,
                f"Application '{headers.application_id}' not found. If you "
                "are confident the application exists, this may be because "
                "the system is still starting.",
            )
            raise  # Unreachable but necessary for mypy.
        if authoritative_server != self.server_id:
            # This is NOT the correct server. Fail.
            await grpc_context.abort(
                IMPORT_grpc.StatusCode.UNAVAILABLE,
                f"Server '{self.server_id}' is not authoritative for this "
                f"request; server '{authoritative_server}' is.",
            )
            raise  # Unreachable but necessary for mypy.

        @IMPORT_reboot_aio_internals_middleware.maybe_run_function_twice_to_validate_effects
        async def _run(
            validating_effects: bool,
        ) -> tests.reboot.greeter_pb2.ConstructAndStoreRecursiveMessageResponse:
            context: IMPORT_typing.Optional[IMPORT_reboot_aio_contexts.Context] = None
            try:
                if headers.task_schedule is not None:
                    context, response = await self._schedule_ConstructAndStoreRecursiveMessage(
                        headers=headers,
                        request=request,
                        grpc_context=grpc_context,
                    )
                    return response

                context = self.create_context(
                    headers=headers,
                    state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                    method='ConstructAndStoreRecursiveMessage',
                    context_type=IMPORT_reboot_aio_contexts.TransactionContext,
                )
                assert context is not None

                return await self._ConstructAndStoreRecursiveMessage(
                    request,
                    context,
                    validating_effects=validating_effects,
                    grpc_context=grpc_context,
                )
            except IMPORT_reboot_aio_contexts.EffectValidationRetry:
                # Doing effect validation, just let this propagate.
                raise
            except IMPORT_reboot.aio.aborted.Aborted as aborted:
                status = IMPORT_rpc_status_sync.to_status(aborted.to_status())
                # Need to add transaction participants here because
                # calling `grpc_context.abort_with_status()` will
                # ignore any other trailing metadata. Only propagate
                # transaction participants metadata if the caller cares.
                # Callers that care are those that are themselves transactions.
                # It's important to not just send this information to everyone;
                # some clients can't tolerate trailers, see:
                #   https://github.com/reboot-dev/mono/issues/5081
                if context is not None and headers.transaction_ids is not None:
                    assert context.transaction_id is not None
                    status = status._replace(
                        trailing_metadata=status.trailing_metadata + context.participants.to_grpc_metadata()
                    )
                await grpc_context.abort_with_status(status)
                raise  # Unreachable but necessary for mypy.
            except IMPORT_asyncio.CancelledError:
                # It's pretty normal for an RPC to be cancelled; it's not useful to
                # print a stack trace.
                raise
            except BaseException as exception:
                # Print the exception stack trace for easier debugging. Note
                # that we don't include the stack trace in an error message
                # for the same reason that gRPC doesn't do so by default,
                # see https://github.com/grpc/grpc/issues/14897, but since this
                # should only get logged on the server side it is safe.
                logger.warning(
                    'Unhandled exception\n' +
                    ''.join(IMPORT_traceback.format_exc() if IMPORT_reboot_nodejs_python.should_print_stacktrace() else [f"{type(exception).__name__}: {exception}"])
                )

                # Re-raise the exception for gRPC to handle!
                #
                # TODO: gRPC will print a stack trace from this
                # exception which we don't want if we're executing via
                # Node.js.
                raise
            finally:
                # Propagate transaction participants, if the caller cares.
                # Callers that care are those that are themselves transactions.
                # It's important to not just send this information to everyone;
                # some clients can't tolerate trailers, see:
                #   https://github.com/reboot-dev/mono/issues/5081
                if context is not None and headers.transaction_ids is not None:
                    assert context.transaction_id is not None
                    grpc_context.set_trailing_metadata(
                        grpc_context.trailing_metadata() +
                        context.participants.to_grpc_metadata()
                    )

        with IMPORT_reboot_aio_tracing.context_from_headers(headers):
            return await _run()

    def _maybe_authorize(
        self,
        *,
        method_name: str,
        headers: IMPORT_reboot_aio_headers.Headers,
        auth: IMPORT_typing.Optional[IMPORT_reboot_aio_auth.Auth],
        request: IMPORT_typing.Optional[GreeterRequestTypes] = None,
    ) -> IMPORT_typing.Optional[IMPORT_typing.Callable[[IMPORT_typing.Optional[GreeterStateType]], IMPORT_typing.Awaitable[None]]]:
        """Returns a function to check authorization for the given method.

        Raises `PermissionDenied` in case Authorizer is present but the request
        is not authorized.
        """
        # To authorize internal calls, we use an internal magic token.
        if headers.bearer_token == __internal_magic_token__:
            return None

        assert self._authorizer is not None

        async def authorize(state: IMPORT_typing.Optional[GreeterStateType]) -> None:
            # Create context for the authorizer. This is a `ReaderContext`
            # independently of the calling context.
            with self.use_context(
                headers=(
                    # Get headers suitable for doing authorization.
                    headers.copy_for_token_verification_and_authorization()
                ),
                state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                method=method_name,
                context_type=IMPORT_reboot_aio_contexts.ReaderContext,
            ) as context:
                context.auth = auth

                # Get the authorizer decision.
                authorization_decision = await self._authorizer.authorize(
                    method_name=method_name,
                    context=context,
                    state=state,
                    request=request,
                )

            # Enforce correct authorizer decision type.
            try:
                IMPORT_reboot_aio_types.assert_type(
                    authorization_decision,
                    [
                        IMPORT_rbt_v1alpha1.errors_pb2.Ok,
                        IMPORT_rbt_v1alpha1.errors_pb2.Unauthenticated,
                        IMPORT_rbt_v1alpha1.errors_pb2.PermissionDenied,
                    ]
                )
            except TypeError as e:
                # Retyping.cast the exception to provide more context.
                authorizer_type = f"{type(self._authorizer).__module__}.{type(self._authorizer).__name__}"
                raise TypeError(
                    f"Authorizer '{authorizer_type}' "
                    f"returned unexpected type '{type(authorization_decision).__name__}' "
                    f"for method '{method_name}' on "
                    f"`tests.reboot.Greeter('{headers.state_ref.id}')`"
                ) from e

            # If the decision is not `True`, raise a `SystemAborted` with either a
            # `PermissionDenied` error (in case of `False`) or an `Unauthenticated`
            # error.
            if not isinstance(authorization_decision, IMPORT_rbt_v1alpha1.errors_pb2.Ok):
                if isinstance(authorization_decision, IMPORT_rbt_v1alpha1.errors_pb2.Unauthenticated):
                    logger.warning(
                        f"Unauthenticated call to '{method_name}' on "
                        f"`tests.reboot.Greeter('{headers.state_ref.id}')`"
                    )

                raise IMPORT_reboot.aio.aborted.SystemAborted(
                    authorization_decision,
                    message=
                    f"You are not authorized to call '{method_name}' on "
                    f"`tests.reboot.Greeter('{headers.state_ref.id}')`"
                )

        return authorize

    async def _maybe_verify_token(
        self,
        *,
        headers: IMPORT_reboot_aio_headers.Headers,
        method: str,
    ) -> IMPORT_typing.Optional[IMPORT_reboot_aio_auth.Auth]:
        """Verify the bearer token and if a token verifier is present.

        Returns the (optional) `reboot.aio.auth.Auth` object
        produced by the token verifier if the token can be verified.
        """
        if self._token_verifier is not None:
            if headers.bearer_token == __internal_magic_token__:
                return None

            with self.use_context(
                headers=(
                    # Get headers suitable for doing token verification.
                    headers.copy_for_token_verification_and_authorization()
                ),
                state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                method=method,
                context_type=IMPORT_reboot_aio_contexts.ReaderContext,
            ) as context:
                result = await self._token_verifier.verify_token(
                    context=context,
                    token=headers.bearer_token,
                )
                if isinstance(result, IMPORT_rbt_v1alpha1.errors_pb2.Unauthenticated):
                    raise IMPORT_reboot_aio_aborted.SystemAborted(
                        result,
                        message=result.message or None,
                    )
                return result

        return None


############################ Client Stubs ############################
# This section is relevant for clients accessing a Reboot service. Since
# servicers are themselves often clients also, this code is generated for
# them also.


class _GreeterStub(IMPORT_reboot_aio_stubs.Stub):

    __state_type_name__ = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter')

    def __init__(
        self,
        *,
        context: IMPORT_reboot_aio_contexts.Context | IMPORT_reboot_aio_external.ExternalContext,
        state_ref: IMPORT_reboot_aio_types.StateRef,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ):
        # Within a Reboot context we do not pass on the caller's bearer token, as that might
        # have security implications - we cannot simply trust any service we are calling with
        # the user's credentials. Instead, the developer can rely on the default app-internal
        # auth, or override that and set an explicit bearer token.
        #
        # In the case of `ExternalContext`, however, its `bearer_token` was set specifically
        # by the developer for the purpose of making these calls.
        caller_id: IMPORT_typing.Optional[IMPORT_reboot_aio_caller_id.CallerID] = None
        if isinstance(context, IMPORT_reboot_aio_external.ExternalContext):
            # Note that only `ExternalContext` even has a `bearer_token` field.
            bearer_token = context.bearer_token
            # If the creator of the `ExternalContext` set an explicit caller ID, obey it.
            caller_id = context.caller_id
        else:
            caller_id = IMPORT_reboot_aio_caller_id.CallerID(
                application_id=context.application_id,
            )

        super().__init__(
            channel_manager=context.channel_manager,
            idempotency_manager=context,
            state_ref=state_ref,
            context=context if isinstance(context, IMPORT_reboot_aio_contexts.Context) else None,
            bearer_token=bearer_token,
            caller_id=caller_id,
        )

        # All the channels for all services of this state will go to the same
        # place, so we can just get a single channel and share it across all
        # stubs.
        channel = self._channel_manager.get_channel_to_state(
            self.__state_type_name__, state_ref
        )
        self._tests_reboot_greetermethods_stub = tests.reboot.greeter_pb2_grpc.GreeterMethodsStub(channel)


class GreeterReaderStub(_GreeterStub):

    def __init__(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
        *,
        state_ref: IMPORT_reboot_aio_types.StateRef,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ):
        IMPORT_reboot_aio_types.assert_type(context, [IMPORT_reboot_aio_contexts.ReaderContext, IMPORT_reboot_aio_contexts.WriterContext, IMPORT_reboot_aio_contexts.TransactionContext, IMPORT_reboot_aio_contexts.WorkflowContext, IMPORT_reboot_aio_external.ExternalContext])
        super().__init__(
            context=context,
            state_ref=state_ref,
            bearer_token=bearer_token,
        )

    # Greeter specific methods:

    async def Greet(
        self,
        request: Greeter.GreetRequest,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
    ) -> tests.reboot.greeter_pb2.GreetResponse:
        state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter')
        service_name = IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods')
        method = 'Greet'

        proto_request = GreeterGreetRequestToProto(
            request,
        )

        async def call():
            async with self._call(
                state_type_name,
                service_name,
                method,
                self._tests_reboot_greetermethods_stub.Greet,
                proto_request,
                unary=True,
                reader=True,
                response_type=tests.reboot.greeter_pb2.GreetResponse,
                aborted_type=Greeter.GreetAborted,
                metadata=metadata,
                bearer_token=bearer_token,
            ) as call:
                assert isinstance(call, IMPORT_typing.Awaitable)
                return await call

        if isinstance(self._context, IMPORT_reboot_aio_contexts.WorkflowContext):
            # Use the idempotency manager to make sure that this
            # reader is being called following the rules.
            with self._context.idempotently(
                state_type_name=state_type_name,
                state_ref=self._headers.state_ref,
                service_name=service_name,
                method=method,
                mutation=False,
                request=proto_request,
                metadata=metadata,
                idempotency=idempotency,
                # Only need to pass `aborted_type` for mutations.
                aborted_type=None,
            ) as idempotency_key:
                assert idempotency is not None
                # Check if this reader is from an `.always()` and if
                # so, don't memoize!
                if idempotency.always:
                    return await call()

                assert idempotency_key is not None
                return await IMPORT_reboot_aio_workflows.at_least_once(
                    (
                        # TODO: for easier debugging include the
                        # original alias (or generated alias in the
                        # case of `.per_iteration()` w/o an alias)
                        # instead of just `idempotency_key`.
                        f'{ service_name }.{ method } ({str(idempotency_key)})',
                        # NOTE: we want this to be `PER_WORKFLOW`
                        # because any per iteration concerns should
                        # have already been taken care of by caller
                        # using `.per_iteration()`.
                        IMPORT_reboot_aio_workflows.PER_WORKFLOW
                    ),
                    self._context,
                    call,
                    type=tests.reboot.greeter_pb2.GreetResponse,
                )
        return await call()



    async def TryToConstructContext(
        self,
        request: Greeter.TryToConstructContextRequest,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
    ) -> google.protobuf.empty_pb2.Empty:
        state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter')
        service_name = IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods')
        method = 'TryToConstructContext'

        proto_request = GreeterTryToConstructContextRequestToProto(
            request,
        )

        async def call():
            async with self._call(
                state_type_name,
                service_name,
                method,
                self._tests_reboot_greetermethods_stub.TryToConstructContext,
                proto_request,
                unary=True,
                reader=True,
                response_type=google.protobuf.empty_pb2.Empty,
                aborted_type=Greeter.TryToConstructContextAborted,
                metadata=metadata,
                bearer_token=bearer_token,
            ) as call:
                assert isinstance(call, IMPORT_typing.Awaitable)
                return await call

        if isinstance(self._context, IMPORT_reboot_aio_contexts.WorkflowContext):
            # Use the idempotency manager to make sure that this
            # reader is being called following the rules.
            with self._context.idempotently(
                state_type_name=state_type_name,
                state_ref=self._headers.state_ref,
                service_name=service_name,
                method=method,
                mutation=False,
                request=proto_request,
                metadata=metadata,
                idempotency=idempotency,
                # Only need to pass `aborted_type` for mutations.
                aborted_type=None,
            ) as idempotency_key:
                assert idempotency is not None
                # Check if this reader is from an `.always()` and if
                # so, don't memoize!
                if idempotency.always:
                    return await call()

                assert idempotency_key is not None
                return await IMPORT_reboot_aio_workflows.at_least_once(
                    (
                        # TODO: for easier debugging include the
                        # original alias (or generated alias in the
                        # case of `.per_iteration()` w/o an alias)
                        # instead of just `idempotency_key`.
                        f'{ service_name }.{ method } ({str(idempotency_key)})',
                        # NOTE: we want this to be `PER_WORKFLOW`
                        # because any per iteration concerns should
                        # have already been taken care of by caller
                        # using `.per_iteration()`.
                        IMPORT_reboot_aio_workflows.PER_WORKFLOW
                    ),
                    self._context,
                    call,
                    type=google.protobuf.empty_pb2.Empty,
                )
        return await call()

    async def TryToConstructExternalContext(
        self,
        request: Greeter.TryToConstructExternalContextRequest,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
    ) -> google.protobuf.empty_pb2.Empty:
        state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter')
        service_name = IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods')
        method = 'TryToConstructExternalContext'

        proto_request = GreeterTryToConstructExternalContextRequestToProto(
            request,
        )

        async def call():
            async with self._call(
                state_type_name,
                service_name,
                method,
                self._tests_reboot_greetermethods_stub.TryToConstructExternalContext,
                proto_request,
                unary=True,
                reader=True,
                response_type=google.protobuf.empty_pb2.Empty,
                aborted_type=Greeter.TryToConstructExternalContextAborted,
                metadata=metadata,
                bearer_token=bearer_token,
            ) as call:
                assert isinstance(call, IMPORT_typing.Awaitable)
                return await call

        if isinstance(self._context, IMPORT_reboot_aio_contexts.WorkflowContext):
            # Use the idempotency manager to make sure that this
            # reader is being called following the rules.
            with self._context.idempotently(
                state_type_name=state_type_name,
                state_ref=self._headers.state_ref,
                service_name=service_name,
                method=method,
                mutation=False,
                request=proto_request,
                metadata=metadata,
                idempotency=idempotency,
                # Only need to pass `aborted_type` for mutations.
                aborted_type=None,
            ) as idempotency_key:
                assert idempotency is not None
                # Check if this reader is from an `.always()` and if
                # so, don't memoize!
                if idempotency.always:
                    return await call()

                assert idempotency_key is not None
                return await IMPORT_reboot_aio_workflows.at_least_once(
                    (
                        # TODO: for easier debugging include the
                        # original alias (or generated alias in the
                        # case of `.per_iteration()` w/o an alias)
                        # instead of just `idempotency_key`.
                        f'{ service_name }.{ method } ({str(idempotency_key)})',
                        # NOTE: we want this to be `PER_WORKFLOW`
                        # because any per iteration concerns should
                        # have already been taken care of by caller
                        # using `.per_iteration()`.
                        IMPORT_reboot_aio_workflows.PER_WORKFLOW
                    ),
                    self._context,
                    call,
                    type=google.protobuf.empty_pb2.Empty,
                )
        return await call()

    async def TestLongRunningFetch(
        self,
        request: Greeter.TestLongRunningFetchRequest,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
    ) -> google.protobuf.empty_pb2.Empty:
        state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter')
        service_name = IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods')
        method = 'TestLongRunningFetch'

        proto_request = GreeterTestLongRunningFetchRequestToProto(
            request,
        )

        async def call():
            async with self._call(
                state_type_name,
                service_name,
                method,
                self._tests_reboot_greetermethods_stub.TestLongRunningFetch,
                proto_request,
                unary=True,
                reader=True,
                response_type=google.protobuf.empty_pb2.Empty,
                aborted_type=Greeter.TestLongRunningFetchAborted,
                metadata=metadata,
                bearer_token=bearer_token,
            ) as call:
                assert isinstance(call, IMPORT_typing.Awaitable)
                return await call

        if isinstance(self._context, IMPORT_reboot_aio_contexts.WorkflowContext):
            # Use the idempotency manager to make sure that this
            # reader is being called following the rules.
            with self._context.idempotently(
                state_type_name=state_type_name,
                state_ref=self._headers.state_ref,
                service_name=service_name,
                method=method,
                mutation=False,
                request=proto_request,
                metadata=metadata,
                idempotency=idempotency,
                # Only need to pass `aborted_type` for mutations.
                aborted_type=None,
            ) as idempotency_key:
                assert idempotency is not None
                # Check if this reader is from an `.always()` and if
                # so, don't memoize!
                if idempotency.always:
                    return await call()

                assert idempotency_key is not None
                return await IMPORT_reboot_aio_workflows.at_least_once(
                    (
                        # TODO: for easier debugging include the
                        # original alias (or generated alias in the
                        # case of `.per_iteration()` w/o an alias)
                        # instead of just `idempotency_key`.
                        f'{ service_name }.{ method } ({str(idempotency_key)})',
                        # NOTE: we want this to be `PER_WORKFLOW`
                        # because any per iteration concerns should
                        # have already been taken care of by caller
                        # using `.per_iteration()`.
                        IMPORT_reboot_aio_workflows.PER_WORKFLOW
                    ),
                    self._context,
                    call,
                    type=google.protobuf.empty_pb2.Empty,
                )
        return await call()


    async def GetWholeState(
        self,
        request: Greeter.GetWholeStateRequest,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
    ) -> tests.reboot.greeter_pb2.Greeter:
        state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter')
        service_name = IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods')
        method = 'GetWholeState'

        proto_request = GreeterGetWholeStateRequestToProto(
            request,
        )

        async def call():
            async with self._call(
                state_type_name,
                service_name,
                method,
                self._tests_reboot_greetermethods_stub.GetWholeState,
                proto_request,
                unary=True,
                reader=True,
                response_type=tests.reboot.greeter_pb2.Greeter,
                aborted_type=Greeter.GetWholeStateAborted,
                metadata=metadata,
                bearer_token=bearer_token,
            ) as call:
                assert isinstance(call, IMPORT_typing.Awaitable)
                return await call

        if isinstance(self._context, IMPORT_reboot_aio_contexts.WorkflowContext):
            # Use the idempotency manager to make sure that this
            # reader is being called following the rules.
            with self._context.idempotently(
                state_type_name=state_type_name,
                state_ref=self._headers.state_ref,
                service_name=service_name,
                method=method,
                mutation=False,
                request=proto_request,
                metadata=metadata,
                idempotency=idempotency,
                # Only need to pass `aborted_type` for mutations.
                aborted_type=None,
            ) as idempotency_key:
                assert idempotency is not None
                # Check if this reader is from an `.always()` and if
                # so, don't memoize!
                if idempotency.always:
                    return await call()

                assert idempotency_key is not None
                return await IMPORT_reboot_aio_workflows.at_least_once(
                    (
                        # TODO: for easier debugging include the
                        # original alias (or generated alias in the
                        # case of `.per_iteration()` w/o an alias)
                        # instead of just `idempotency_key`.
                        f'{ service_name }.{ method } ({str(idempotency_key)})',
                        # NOTE: we want this to be `PER_WORKFLOW`
                        # because any per iteration concerns should
                        # have already been taken care of by caller
                        # using `.per_iteration()`.
                        IMPORT_reboot_aio_workflows.PER_WORKFLOW
                    ),
                    self._context,
                    call,
                    type=tests.reboot.greeter_pb2.Greeter,
                )
        return await call()

    async def FailWithException(
        self,
        request: Greeter.FailWithExceptionRequest,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
    ) -> google.protobuf.empty_pb2.Empty:
        state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter')
        service_name = IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods')
        method = 'FailWithException'

        proto_request = GreeterFailWithExceptionRequestToProto(
            request,
        )

        async def call():
            async with self._call(
                state_type_name,
                service_name,
                method,
                self._tests_reboot_greetermethods_stub.FailWithException,
                proto_request,
                unary=True,
                reader=True,
                response_type=google.protobuf.empty_pb2.Empty,
                aborted_type=Greeter.FailWithExceptionAborted,
                metadata=metadata,
                bearer_token=bearer_token,
            ) as call:
                assert isinstance(call, IMPORT_typing.Awaitable)
                return await call

        if isinstance(self._context, IMPORT_reboot_aio_contexts.WorkflowContext):
            # Use the idempotency manager to make sure that this
            # reader is being called following the rules.
            with self._context.idempotently(
                state_type_name=state_type_name,
                state_ref=self._headers.state_ref,
                service_name=service_name,
                method=method,
                mutation=False,
                request=proto_request,
                metadata=metadata,
                idempotency=idempotency,
                # Only need to pass `aborted_type` for mutations.
                aborted_type=None,
            ) as idempotency_key:
                assert idempotency is not None
                # Check if this reader is from an `.always()` and if
                # so, don't memoize!
                if idempotency.always:
                    return await call()

                assert idempotency_key is not None
                return await IMPORT_reboot_aio_workflows.at_least_once(
                    (
                        # TODO: for easier debugging include the
                        # original alias (or generated alias in the
                        # case of `.per_iteration()` w/o an alias)
                        # instead of just `idempotency_key`.
                        f'{ service_name }.{ method } ({str(idempotency_key)})',
                        # NOTE: we want this to be `PER_WORKFLOW`
                        # because any per iteration concerns should
                        # have already been taken care of by caller
                        # using `.per_iteration()`.
                        IMPORT_reboot_aio_workflows.PER_WORKFLOW
                    ),
                    self._context,
                    call,
                    type=google.protobuf.empty_pb2.Empty,
                )
        return await call()

    async def FailWithAborted(
        self,
        request: Greeter.FailWithAbortedRequest,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
    ) -> google.protobuf.empty_pb2.Empty:
        state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter')
        service_name = IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods')
        method = 'FailWithAborted'

        proto_request = GreeterFailWithAbortedRequestToProto(
            request,
        )

        async def call():
            async with self._call(
                state_type_name,
                service_name,
                method,
                self._tests_reboot_greetermethods_stub.FailWithAborted,
                proto_request,
                unary=True,
                reader=True,
                response_type=google.protobuf.empty_pb2.Empty,
                aborted_type=Greeter.FailWithAbortedAborted,
                metadata=metadata,
                bearer_token=bearer_token,
            ) as call:
                assert isinstance(call, IMPORT_typing.Awaitable)
                return await call

        if isinstance(self._context, IMPORT_reboot_aio_contexts.WorkflowContext):
            # Use the idempotency manager to make sure that this
            # reader is being called following the rules.
            with self._context.idempotently(
                state_type_name=state_type_name,
                state_ref=self._headers.state_ref,
                service_name=service_name,
                method=method,
                mutation=False,
                request=proto_request,
                metadata=metadata,
                idempotency=idempotency,
                # Only need to pass `aborted_type` for mutations.
                aborted_type=None,
            ) as idempotency_key:
                assert idempotency is not None
                # Check if this reader is from an `.always()` and if
                # so, don't memoize!
                if idempotency.always:
                    return await call()

                assert idempotency_key is not None
                return await IMPORT_reboot_aio_workflows.at_least_once(
                    (
                        # TODO: for easier debugging include the
                        # original alias (or generated alias in the
                        # case of `.per_iteration()` w/o an alias)
                        # instead of just `idempotency_key`.
                        f'{ service_name }.{ method } ({str(idempotency_key)})',
                        # NOTE: we want this to be `PER_WORKFLOW`
                        # because any per iteration concerns should
                        # have already been taken care of by caller
                        # using `.per_iteration()`.
                        IMPORT_reboot_aio_workflows.PER_WORKFLOW
                    ),
                    self._context,
                    call,
                    type=google.protobuf.empty_pb2.Empty,
                )
        return await call()




    async def ReadRecursiveMessage(
        self,
        request: Greeter.ReadRecursiveMessageRequest,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
    ) -> tests.reboot.greeter_pb2.ReadRecursiveMessageResponse:
        state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter')
        service_name = IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods')
        method = 'ReadRecursiveMessage'

        proto_request = GreeterReadRecursiveMessageRequestToProto(
            request,
        )

        async def call():
            async with self._call(
                state_type_name,
                service_name,
                method,
                self._tests_reboot_greetermethods_stub.ReadRecursiveMessage,
                proto_request,
                unary=True,
                reader=True,
                response_type=tests.reboot.greeter_pb2.ReadRecursiveMessageResponse,
                aborted_type=Greeter.ReadRecursiveMessageAborted,
                metadata=metadata,
                bearer_token=bearer_token,
            ) as call:
                assert isinstance(call, IMPORT_typing.Awaitable)
                return await call

        if isinstance(self._context, IMPORT_reboot_aio_contexts.WorkflowContext):
            # Use the idempotency manager to make sure that this
            # reader is being called following the rules.
            with self._context.idempotently(
                state_type_name=state_type_name,
                state_ref=self._headers.state_ref,
                service_name=service_name,
                method=method,
                mutation=False,
                request=proto_request,
                metadata=metadata,
                idempotency=idempotency,
                # Only need to pass `aborted_type` for mutations.
                aborted_type=None,
            ) as idempotency_key:
                assert idempotency is not None
                # Check if this reader is from an `.always()` and if
                # so, don't memoize!
                if idempotency.always:
                    return await call()

                assert idempotency_key is not None
                return await IMPORT_reboot_aio_workflows.at_least_once(
                    (
                        # TODO: for easier debugging include the
                        # original alias (or generated alias in the
                        # case of `.per_iteration()` w/o an alias)
                        # instead of just `idempotency_key`.
                        f'{ service_name }.{ method } ({str(idempotency_key)})',
                        # NOTE: we want this to be `PER_WORKFLOW`
                        # because any per iteration concerns should
                        # have already been taken care of by caller
                        # using `.per_iteration()`.
                        IMPORT_reboot_aio_workflows.PER_WORKFLOW
                    ),
                    self._context,
                    call,
                    type=tests.reboot.greeter_pb2.ReadRecursiveMessageResponse,
                )
        return await call()




class GreeterWriterStub(_GreeterStub):

    def __init__(
        self,
        context: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
        *,
        state_ref: IMPORT_reboot_aio_types.StateRef,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ):
        IMPORT_reboot_aio_types.assert_type(context, [IMPORT_reboot_aio_contexts.TransactionContext, IMPORT_reboot_aio_contexts.WorkflowContext, IMPORT_reboot_aio_external.ExternalContext])
        super().__init__(
            context=context,
            state_ref=state_ref,
            bearer_token=bearer_token,
        )

    # Greeter specific methods:
    async def Create(
        self,
        request: Greeter.CreateRequest,
        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> tests.reboot.greeter_pb2.CreateResponse:
        proto_request = GreeterCreateRequestToProto(
            request,
        )
        with self._idempotency_manager.idempotently(
            state_type_name=IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            state_ref=self._headers.state_ref,
            service_name=IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
            method='Create',
            mutation=True,
            request=proto_request,
            metadata=metadata,
            idempotency=idempotency,
            aborted_type=Greeter.CreateAborted,
        ) as idempotency_key:
            async with self._call(
                IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
                'Create',
                self._tests_reboot_greetermethods_stub.Create,
                proto_request,
                unary=True,
                reader=False,
                response_type=tests.reboot.greeter_pb2.CreateResponse,
                aborted_type=Greeter.CreateAborted,
                metadata=metadata,
                idempotency_key=idempotency_key,
                per_iteration=idempotency.per_iteration if idempotency is not None else False,
                bearer_token=bearer_token,
            ) as call:
                assert isinstance(call, IMPORT_typing.Awaitable), type(call)
                return await call

    async def Greet(
        self,
        request: Greeter.GreetRequest,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> tests.reboot.greeter_pb2.GreetResponse:
        async with self._call(
            IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
            'Greet',
            self._tests_reboot_greetermethods_stub.Greet,
            GreeterGreetRequestToProto(
                request,
            ),
            unary=True,
            reader=True,
            response_type=tests.reboot.greeter_pb2.GreetResponse,
            aborted_type=Greeter.GreetAborted,
            metadata=metadata,
            bearer_token=bearer_token,
        ) as call:
            assert isinstance(call, IMPORT_typing.Awaitable), type(call)
            return await call

    async def SetAdjective(
        self,
        request: Greeter.SetAdjectiveRequest,
        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> tests.reboot.greeter_pb2.SetAdjectiveResponse:
        proto_request = GreeterSetAdjectiveRequestToProto(
            request,
        )
        with self._idempotency_manager.idempotently(
            state_type_name=IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            state_ref=self._headers.state_ref,
            service_name=IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
            method='SetAdjective',
            mutation=True,
            request=proto_request,
            metadata=metadata,
            idempotency=idempotency,
            aborted_type=Greeter.SetAdjectiveAborted,
        ) as idempotency_key:
            async with self._call(
                IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
                'SetAdjective',
                self._tests_reboot_greetermethods_stub.SetAdjective,
                proto_request,
                unary=True,
                reader=False,
                response_type=tests.reboot.greeter_pb2.SetAdjectiveResponse,
                aborted_type=Greeter.SetAdjectiveAborted,
                metadata=metadata,
                idempotency_key=idempotency_key,
                per_iteration=idempotency.per_iteration if idempotency is not None else False,
                bearer_token=bearer_token,
            ) as call:
                assert isinstance(call, IMPORT_typing.Awaitable), type(call)
                return await call


    async def TryToConstructContext(
        self,
        request: Greeter.TryToConstructContextRequest,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> google.protobuf.empty_pb2.Empty:
        async with self._call(
            IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
            'TryToConstructContext',
            self._tests_reboot_greetermethods_stub.TryToConstructContext,
            GreeterTryToConstructContextRequestToProto(
                request,
            ),
            unary=True,
            reader=True,
            response_type=google.protobuf.empty_pb2.Empty,
            aborted_type=Greeter.TryToConstructContextAborted,
            metadata=metadata,
            bearer_token=bearer_token,
        ) as call:
            assert isinstance(call, IMPORT_typing.Awaitable), type(call)
            return await call

    async def TryToConstructExternalContext(
        self,
        request: Greeter.TryToConstructExternalContextRequest,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> google.protobuf.empty_pb2.Empty:
        async with self._call(
            IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
            'TryToConstructExternalContext',
            self._tests_reboot_greetermethods_stub.TryToConstructExternalContext,
            GreeterTryToConstructExternalContextRequestToProto(
                request,
            ),
            unary=True,
            reader=True,
            response_type=google.protobuf.empty_pb2.Empty,
            aborted_type=Greeter.TryToConstructExternalContextAborted,
            metadata=metadata,
            bearer_token=bearer_token,
        ) as call:
            assert isinstance(call, IMPORT_typing.Awaitable), type(call)
            return await call

    async def TestLongRunningFetch(
        self,
        request: Greeter.TestLongRunningFetchRequest,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> google.protobuf.empty_pb2.Empty:
        async with self._call(
            IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
            'TestLongRunningFetch',
            self._tests_reboot_greetermethods_stub.TestLongRunningFetch,
            GreeterTestLongRunningFetchRequestToProto(
                request,
            ),
            unary=True,
            reader=True,
            response_type=google.protobuf.empty_pb2.Empty,
            aborted_type=Greeter.TestLongRunningFetchAborted,
            metadata=metadata,
            bearer_token=bearer_token,
        ) as call:
            assert isinstance(call, IMPORT_typing.Awaitable), type(call)
            return await call

    async def TestLongRunningWriter(
        self,
        request: Greeter.TestLongRunningWriterRequest,
        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> google.protobuf.empty_pb2.Empty:
        proto_request = GreeterTestLongRunningWriterRequestToProto(
            request,
        )
        with self._idempotency_manager.idempotently(
            state_type_name=IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            state_ref=self._headers.state_ref,
            service_name=IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
            method='TestLongRunningWriter',
            mutation=True,
            request=proto_request,
            metadata=metadata,
            idempotency=idempotency,
            aborted_type=Greeter.TestLongRunningWriterAborted,
        ) as idempotency_key:
            async with self._call(
                IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
                'TestLongRunningWriter',
                self._tests_reboot_greetermethods_stub.TestLongRunningWriter,
                proto_request,
                unary=True,
                reader=False,
                response_type=google.protobuf.empty_pb2.Empty,
                aborted_type=Greeter.TestLongRunningWriterAborted,
                metadata=metadata,
                idempotency_key=idempotency_key,
                per_iteration=idempotency.per_iteration if idempotency is not None else False,
                bearer_token=bearer_token,
            ) as call:
                assert isinstance(call, IMPORT_typing.Awaitable), type(call)
                return await call

    async def GetWholeState(
        self,
        request: Greeter.GetWholeStateRequest,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> tests.reboot.greeter_pb2.Greeter:
        async with self._call(
            IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
            'GetWholeState',
            self._tests_reboot_greetermethods_stub.GetWholeState,
            GreeterGetWholeStateRequestToProto(
                request,
            ),
            unary=True,
            reader=True,
            response_type=tests.reboot.greeter_pb2.Greeter,
            aborted_type=Greeter.GetWholeStateAborted,
            metadata=metadata,
            bearer_token=bearer_token,
        ) as call:
            assert isinstance(call, IMPORT_typing.Awaitable), type(call)
            return await call

    async def FailWithException(
        self,
        request: Greeter.FailWithExceptionRequest,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> google.protobuf.empty_pb2.Empty:
        async with self._call(
            IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
            'FailWithException',
            self._tests_reboot_greetermethods_stub.FailWithException,
            GreeterFailWithExceptionRequestToProto(
                request,
            ),
            unary=True,
            reader=True,
            response_type=google.protobuf.empty_pb2.Empty,
            aborted_type=Greeter.FailWithExceptionAborted,
            metadata=metadata,
            bearer_token=bearer_token,
        ) as call:
            assert isinstance(call, IMPORT_typing.Awaitable), type(call)
            return await call

    async def FailWithAborted(
        self,
        request: Greeter.FailWithAbortedRequest,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> google.protobuf.empty_pb2.Empty:
        async with self._call(
            IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
            'FailWithAborted',
            self._tests_reboot_greetermethods_stub.FailWithAborted,
            GreeterFailWithAbortedRequestToProto(
                request,
            ),
            unary=True,
            reader=True,
            response_type=google.protobuf.empty_pb2.Empty,
            aborted_type=Greeter.FailWithAbortedAborted,
            metadata=metadata,
            bearer_token=bearer_token,
        ) as call:
            assert isinstance(call, IMPORT_typing.Awaitable), type(call)
            return await call


    async def DangerousFields(
        self,
        request: Greeter.DangerousFieldsRequest,
        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> google.protobuf.empty_pb2.Empty:
        proto_request = GreeterDangerousFieldsRequestToProto(
            request,
        )
        with self._idempotency_manager.idempotently(
            state_type_name=IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            state_ref=self._headers.state_ref,
            service_name=IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
            method='DangerousFields',
            mutation=True,
            request=proto_request,
            metadata=metadata,
            idempotency=idempotency,
            aborted_type=Greeter.DangerousFieldsAborted,
        ) as idempotency_key:
            async with self._call(
                IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
                'DangerousFields',
                self._tests_reboot_greetermethods_stub.DangerousFields,
                proto_request,
                unary=True,
                reader=False,
                response_type=google.protobuf.empty_pb2.Empty,
                aborted_type=Greeter.DangerousFieldsAborted,
                metadata=metadata,
                idempotency_key=idempotency_key,
                per_iteration=idempotency.per_iteration if idempotency is not None else False,
                bearer_token=bearer_token,
            ) as call:
                assert isinstance(call, IMPORT_typing.Awaitable), type(call)
                return await call

    async def StoreRecursiveMessage(
        self,
        request: Greeter.StoreRecursiveMessageRequest,
        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> tests.reboot.greeter_pb2.StoreRecursiveMessageResponse:
        proto_request = GreeterStoreRecursiveMessageRequestToProto(
            request,
        )
        with self._idempotency_manager.idempotently(
            state_type_name=IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            state_ref=self._headers.state_ref,
            service_name=IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
            method='StoreRecursiveMessage',
            mutation=True,
            request=proto_request,
            metadata=metadata,
            idempotency=idempotency,
            aborted_type=Greeter.StoreRecursiveMessageAborted,
        ) as idempotency_key:
            async with self._call(
                IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
                'StoreRecursiveMessage',
                self._tests_reboot_greetermethods_stub.StoreRecursiveMessage,
                proto_request,
                unary=True,
                reader=False,
                response_type=tests.reboot.greeter_pb2.StoreRecursiveMessageResponse,
                aborted_type=Greeter.StoreRecursiveMessageAborted,
                metadata=metadata,
                idempotency_key=idempotency_key,
                per_iteration=idempotency.per_iteration if idempotency is not None else False,
                bearer_token=bearer_token,
            ) as call:
                assert isinstance(call, IMPORT_typing.Awaitable), type(call)
                return await call

    async def ReadRecursiveMessage(
        self,
        request: Greeter.ReadRecursiveMessageRequest,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> tests.reboot.greeter_pb2.ReadRecursiveMessageResponse:
        async with self._call(
            IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
            'ReadRecursiveMessage',
            self._tests_reboot_greetermethods_stub.ReadRecursiveMessage,
            GreeterReadRecursiveMessageRequestToProto(
                request,
            ),
            unary=True,
            reader=True,
            response_type=tests.reboot.greeter_pb2.ReadRecursiveMessageResponse,
            aborted_type=Greeter.ReadRecursiveMessageAborted,
            metadata=metadata,
            bearer_token=bearer_token,
        ) as call:
            assert isinstance(call, IMPORT_typing.Awaitable), type(call)
            return await call



class GreeterWorkflowStub(_GreeterStub):

    def __init__(
        self,
        *,
        context: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
        state_ref: IMPORT_reboot_aio_types.StateRef,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ):
        IMPORT_reboot_aio_types.assert_type(context, [IMPORT_reboot_aio_contexts.TransactionContext, IMPORT_reboot_aio_contexts.WorkflowContext, IMPORT_reboot_aio_external.ExternalContext])
        super().__init__(
            context=context,
            state_ref=state_ref,
            bearer_token=bearer_token,
        )

    # Greeter specific methods:
    async def Create(
        self,
        request: Greeter.CreateRequest,
        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> tests.reboot.greeter_pb2.CreateResponse:
        idempotency_key: IMPORT_typing.Optional[IMPORT_uuid.UUID]
        proto_request = GreeterCreateRequestToProto(
            request,
        )

        with self._idempotency_manager.idempotently(
            state_type_name=IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            state_ref=self._headers.state_ref,
            service_name=IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
            method='Create',
            mutation=True,
            request=proto_request,
            metadata=metadata,
            idempotency=idempotency,
            aborted_type=Greeter.CreateAborted,
        ) as idempotency_key:
            async with self._call(
                IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
                'Create',
                self._tests_reboot_greetermethods_stub.Create,
                proto_request,
                unary=True,
                reader=False,
                response_type=tests.reboot.greeter_pb2.CreateResponse,
                aborted_type=Greeter.CreateAborted,
                metadata=metadata,
                idempotency_key=idempotency_key,
                per_iteration=idempotency.per_iteration if idempotency is not None else False,
                bearer_token=bearer_token,
            ) as call:
                assert isinstance(call, IMPORT_typing.Awaitable), type(call)
                return await call

    async def Greet(
        self,
        request: Greeter.GreetRequest,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> tests.reboot.greeter_pb2.GreetResponse:
        async with self._call(
            IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
            'Greet',
            self._tests_reboot_greetermethods_stub.Greet,
            GreeterGreetRequestToProto(
                request,
            ),
            unary=True,
            reader=True,
            response_type=tests.reboot.greeter_pb2.GreetResponse,
            aborted_type=Greeter.GreetAborted,
            metadata=metadata,
            bearer_token=bearer_token,
        ) as call:
            assert isinstance(call, IMPORT_typing.Awaitable), type(call)
            return await call

    async def SetAdjective(
        self,
        request: Greeter.SetAdjectiveRequest,
        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> tests.reboot.greeter_pb2.SetAdjectiveResponse:
        idempotency_key: IMPORT_typing.Optional[IMPORT_uuid.UUID]
        proto_request = GreeterSetAdjectiveRequestToProto(
            request,
        )

        with self._idempotency_manager.idempotently(
            state_type_name=IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            state_ref=self._headers.state_ref,
            service_name=IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
            method='SetAdjective',
            mutation=True,
            request=proto_request,
            metadata=metadata,
            idempotency=idempotency,
            aborted_type=Greeter.SetAdjectiveAborted,
        ) as idempotency_key:
            async with self._call(
                IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
                'SetAdjective',
                self._tests_reboot_greetermethods_stub.SetAdjective,
                proto_request,
                unary=True,
                reader=False,
                response_type=tests.reboot.greeter_pb2.SetAdjectiveResponse,
                aborted_type=Greeter.SetAdjectiveAborted,
                metadata=metadata,
                idempotency_key=idempotency_key,
                per_iteration=idempotency.per_iteration if idempotency is not None else False,
                bearer_token=bearer_token,
            ) as call:
                assert isinstance(call, IMPORT_typing.Awaitable), type(call)
                return await call

    async def TransactionSetAdjective(
        self,
        request: Greeter.TransactionSetAdjectiveRequest,
        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> tests.reboot.greeter_pb2.SetAdjectiveResponse:
        idempotency_key: IMPORT_typing.Optional[IMPORT_uuid.UUID]
        proto_request = GreeterTransactionSetAdjectiveRequestToProto(
            request,
        )

        with self._idempotency_manager.idempotently(
            state_type_name=IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            state_ref=self._headers.state_ref,
            service_name=IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
            method='TransactionSetAdjective',
            mutation=True,
            request=proto_request,
            metadata=metadata,
            idempotency=idempotency,
            aborted_type=Greeter.TransactionSetAdjectiveAborted,
        ) as idempotency_key:
            async with self._call(
                IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
                'TransactionSetAdjective',
                self._tests_reboot_greetermethods_stub.TransactionSetAdjective,
                proto_request,
                unary=True,
                reader=False,
                response_type=tests.reboot.greeter_pb2.SetAdjectiveResponse,
                aborted_type=Greeter.TransactionSetAdjectiveAborted,
                metadata=metadata,
                idempotency_key=idempotency_key,
                per_iteration=idempotency.per_iteration if idempotency is not None else False,
                bearer_token=bearer_token,
            ) as call:
                assert isinstance(call, IMPORT_typing.Awaitable), type(call)
                return await call

    async def TryToConstructContext(
        self,
        request: Greeter.TryToConstructContextRequest,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> google.protobuf.empty_pb2.Empty:
        async with self._call(
            IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
            'TryToConstructContext',
            self._tests_reboot_greetermethods_stub.TryToConstructContext,
            GreeterTryToConstructContextRequestToProto(
                request,
            ),
            unary=True,
            reader=True,
            response_type=google.protobuf.empty_pb2.Empty,
            aborted_type=Greeter.TryToConstructContextAborted,
            metadata=metadata,
            bearer_token=bearer_token,
        ) as call:
            assert isinstance(call, IMPORT_typing.Awaitable), type(call)
            return await call

    async def TryToConstructExternalContext(
        self,
        request: Greeter.TryToConstructExternalContextRequest,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> google.protobuf.empty_pb2.Empty:
        async with self._call(
            IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
            'TryToConstructExternalContext',
            self._tests_reboot_greetermethods_stub.TryToConstructExternalContext,
            GreeterTryToConstructExternalContextRequestToProto(
                request,
            ),
            unary=True,
            reader=True,
            response_type=google.protobuf.empty_pb2.Empty,
            aborted_type=Greeter.TryToConstructExternalContextAborted,
            metadata=metadata,
            bearer_token=bearer_token,
        ) as call:
            assert isinstance(call, IMPORT_typing.Awaitable), type(call)
            return await call

    async def TestLongRunningFetch(
        self,
        request: Greeter.TestLongRunningFetchRequest,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> google.protobuf.empty_pb2.Empty:
        async with self._call(
            IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
            'TestLongRunningFetch',
            self._tests_reboot_greetermethods_stub.TestLongRunningFetch,
            GreeterTestLongRunningFetchRequestToProto(
                request,
            ),
            unary=True,
            reader=True,
            response_type=google.protobuf.empty_pb2.Empty,
            aborted_type=Greeter.TestLongRunningFetchAborted,
            metadata=metadata,
            bearer_token=bearer_token,
        ) as call:
            assert isinstance(call, IMPORT_typing.Awaitable), type(call)
            return await call

    async def TestLongRunningWriter(
        self,
        request: Greeter.TestLongRunningWriterRequest,
        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> google.protobuf.empty_pb2.Empty:
        idempotency_key: IMPORT_typing.Optional[IMPORT_uuid.UUID]
        proto_request = GreeterTestLongRunningWriterRequestToProto(
            request,
        )

        with self._idempotency_manager.idempotently(
            state_type_name=IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            state_ref=self._headers.state_ref,
            service_name=IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
            method='TestLongRunningWriter',
            mutation=True,
            request=proto_request,
            metadata=metadata,
            idempotency=idempotency,
            aborted_type=Greeter.TestLongRunningWriterAborted,
        ) as idempotency_key:
            async with self._call(
                IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
                'TestLongRunningWriter',
                self._tests_reboot_greetermethods_stub.TestLongRunningWriter,
                proto_request,
                unary=True,
                reader=False,
                response_type=google.protobuf.empty_pb2.Empty,
                aborted_type=Greeter.TestLongRunningWriterAborted,
                metadata=metadata,
                idempotency_key=idempotency_key,
                per_iteration=idempotency.per_iteration if idempotency is not None else False,
                bearer_token=bearer_token,
            ) as call:
                assert isinstance(call, IMPORT_typing.Awaitable), type(call)
                return await call

    async def GetWholeState(
        self,
        request: Greeter.GetWholeStateRequest,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> tests.reboot.greeter_pb2.Greeter:
        async with self._call(
            IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
            'GetWholeState',
            self._tests_reboot_greetermethods_stub.GetWholeState,
            GreeterGetWholeStateRequestToProto(
                request,
            ),
            unary=True,
            reader=True,
            response_type=tests.reboot.greeter_pb2.Greeter,
            aborted_type=Greeter.GetWholeStateAborted,
            metadata=metadata,
            bearer_token=bearer_token,
        ) as call:
            assert isinstance(call, IMPORT_typing.Awaitable), type(call)
            return await call

    async def FailWithException(
        self,
        request: Greeter.FailWithExceptionRequest,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> google.protobuf.empty_pb2.Empty:
        async with self._call(
            IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
            'FailWithException',
            self._tests_reboot_greetermethods_stub.FailWithException,
            GreeterFailWithExceptionRequestToProto(
                request,
            ),
            unary=True,
            reader=True,
            response_type=google.protobuf.empty_pb2.Empty,
            aborted_type=Greeter.FailWithExceptionAborted,
            metadata=metadata,
            bearer_token=bearer_token,
        ) as call:
            assert isinstance(call, IMPORT_typing.Awaitable), type(call)
            return await call

    async def FailWithAborted(
        self,
        request: Greeter.FailWithAbortedRequest,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> google.protobuf.empty_pb2.Empty:
        async with self._call(
            IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
            'FailWithAborted',
            self._tests_reboot_greetermethods_stub.FailWithAborted,
            GreeterFailWithAbortedRequestToProto(
                request,
            ),
            unary=True,
            reader=True,
            response_type=google.protobuf.empty_pb2.Empty,
            aborted_type=Greeter.FailWithAbortedAborted,
            metadata=metadata,
            bearer_token=bearer_token,
        ) as call:
            assert isinstance(call, IMPORT_typing.Awaitable), type(call)
            return await call


    async def DangerousFields(
        self,
        request: Greeter.DangerousFieldsRequest,
        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> google.protobuf.empty_pb2.Empty:
        idempotency_key: IMPORT_typing.Optional[IMPORT_uuid.UUID]
        proto_request = GreeterDangerousFieldsRequestToProto(
            request,
        )

        with self._idempotency_manager.idempotently(
            state_type_name=IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            state_ref=self._headers.state_ref,
            service_name=IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
            method='DangerousFields',
            mutation=True,
            request=proto_request,
            metadata=metadata,
            idempotency=idempotency,
            aborted_type=Greeter.DangerousFieldsAborted,
        ) as idempotency_key:
            async with self._call(
                IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
                'DangerousFields',
                self._tests_reboot_greetermethods_stub.DangerousFields,
                proto_request,
                unary=True,
                reader=False,
                response_type=google.protobuf.empty_pb2.Empty,
                aborted_type=Greeter.DangerousFieldsAborted,
                metadata=metadata,
                idempotency_key=idempotency_key,
                per_iteration=idempotency.per_iteration if idempotency is not None else False,
                bearer_token=bearer_token,
            ) as call:
                assert isinstance(call, IMPORT_typing.Awaitable), type(call)
                return await call

    async def StoreRecursiveMessage(
        self,
        request: Greeter.StoreRecursiveMessageRequest,
        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> tests.reboot.greeter_pb2.StoreRecursiveMessageResponse:
        idempotency_key: IMPORT_typing.Optional[IMPORT_uuid.UUID]
        proto_request = GreeterStoreRecursiveMessageRequestToProto(
            request,
        )

        with self._idempotency_manager.idempotently(
            state_type_name=IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            state_ref=self._headers.state_ref,
            service_name=IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
            method='StoreRecursiveMessage',
            mutation=True,
            request=proto_request,
            metadata=metadata,
            idempotency=idempotency,
            aborted_type=Greeter.StoreRecursiveMessageAborted,
        ) as idempotency_key:
            async with self._call(
                IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
                'StoreRecursiveMessage',
                self._tests_reboot_greetermethods_stub.StoreRecursiveMessage,
                proto_request,
                unary=True,
                reader=False,
                response_type=tests.reboot.greeter_pb2.StoreRecursiveMessageResponse,
                aborted_type=Greeter.StoreRecursiveMessageAborted,
                metadata=metadata,
                idempotency_key=idempotency_key,
                per_iteration=idempotency.per_iteration if idempotency is not None else False,
                bearer_token=bearer_token,
            ) as call:
                assert isinstance(call, IMPORT_typing.Awaitable), type(call)
                return await call

    async def ReadRecursiveMessage(
        self,
        request: Greeter.ReadRecursiveMessageRequest,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> tests.reboot.greeter_pb2.ReadRecursiveMessageResponse:
        async with self._call(
            IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
            'ReadRecursiveMessage',
            self._tests_reboot_greetermethods_stub.ReadRecursiveMessage,
            GreeterReadRecursiveMessageRequestToProto(
                request,
            ),
            unary=True,
            reader=True,
            response_type=tests.reboot.greeter_pb2.ReadRecursiveMessageResponse,
            aborted_type=Greeter.ReadRecursiveMessageAborted,
            metadata=metadata,
            bearer_token=bearer_token,
        ) as call:
            assert isinstance(call, IMPORT_typing.Awaitable), type(call)
            return await call

    async def ConstructAndStoreRecursiveMessage(
        self,
        request: Greeter.ConstructAndStoreRecursiveMessageRequest,
        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> tests.reboot.greeter_pb2.ConstructAndStoreRecursiveMessageResponse:
        idempotency_key: IMPORT_typing.Optional[IMPORT_uuid.UUID]
        proto_request = GreeterConstructAndStoreRecursiveMessageRequestToProto(
            request,
        )

        with self._idempotency_manager.idempotently(
            state_type_name=IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            state_ref=self._headers.state_ref,
            service_name=IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
            method='ConstructAndStoreRecursiveMessage',
            mutation=True,
            request=proto_request,
            metadata=metadata,
            idempotency=idempotency,
            aborted_type=Greeter.ConstructAndStoreRecursiveMessageAborted,
        ) as idempotency_key:
            async with self._call(
                IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
                'ConstructAndStoreRecursiveMessage',
                self._tests_reboot_greetermethods_stub.ConstructAndStoreRecursiveMessage,
                proto_request,
                unary=True,
                reader=False,
                response_type=tests.reboot.greeter_pb2.ConstructAndStoreRecursiveMessageResponse,
                aborted_type=Greeter.ConstructAndStoreRecursiveMessageAborted,
                metadata=metadata,
                idempotency_key=idempotency_key,
                per_iteration=idempotency.per_iteration if idempotency is not None else False,
                bearer_token=bearer_token,
            ) as call:
                assert isinstance(call, IMPORT_typing.Awaitable), type(call)
                return await call



class GreeterTasksStub(_GreeterStub):

    def __init__(
        self,
        context: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
        *,
        state_ref: IMPORT_reboot_aio_types.StateRef,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ):
        IMPORT_reboot_aio_types.assert_type(context, [IMPORT_reboot_aio_contexts.TransactionContext, IMPORT_reboot_aio_contexts.WorkflowContext, IMPORT_reboot_aio_external.ExternalContext])
        super().__init__(
            context=context,
            state_ref=state_ref,
            bearer_token=bearer_token,
        )

    # Greeter specific methods:
    async def Greet(
        self,
        request: Greeter.GreetRequest,
        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
        idempotency_key: IMPORT_typing.Optional[IMPORT_uuid.UUID]
        proto_request = GreeterGreetRequestToProto(
            request,
        )

        with self._idempotency_manager.idempotently(
            state_type_name=IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            state_ref=self._headers.state_ref,
            service_name=IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
            method='Greet',
            mutation=True,
            request=proto_request,
            metadata=metadata,
            idempotency=idempotency,
            aborted_type=Greeter.GreetAborted,
        ) as idempotency_key:
            async with self._call(
                IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
                'Greet',
                self._tests_reboot_greetermethods_stub.Greet,
                proto_request,
                unary=True,
                reader=False,
                response_type=tests.reboot.greeter_pb2.GreetResponse,
                aborted_type=Greeter.GreetAborted,
                metadata=metadata,
                idempotency_key=idempotency_key,
                per_iteration=idempotency.per_iteration if idempotency is not None else False,
                bearer_token=bearer_token,
            ) as call:
                assert isinstance(call, IMPORT_typing.Awaitable), type(call)
                await call
                for (key, value) in await call.trailing_metadata():  # type: ignore[misc, attr-defined]
                    if key == IMPORT_reboot_aio_headers.TASK_ID_UUID:
                        return IMPORT_rbt_v1alpha1.tasks_pb2.TaskId(
                            state_type=IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                            state_ref=self._headers.state_ref.to_str(),
                            task_uuid=IMPORT_uuid.UUID(value).bytes,
                        )
                raise IMPORT_reboot.aio.aborted.SystemAborted(
                    IMPORT_rbt_v1alpha1.errors_pb2.Internal(),
                    message='Trailing metadata missing for task schedule',
                )
    async def SetAdjective(
        self,
        request: Greeter.SetAdjectiveRequest,
        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
        idempotency_key: IMPORT_typing.Optional[IMPORT_uuid.UUID]
        proto_request = GreeterSetAdjectiveRequestToProto(
            request,
        )

        with self._idempotency_manager.idempotently(
            state_type_name=IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            state_ref=self._headers.state_ref,
            service_name=IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
            method='SetAdjective',
            mutation=True,
            request=proto_request,
            metadata=metadata,
            idempotency=idempotency,
            aborted_type=Greeter.SetAdjectiveAborted,
        ) as idempotency_key:
            async with self._call(
                IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
                'SetAdjective',
                self._tests_reboot_greetermethods_stub.SetAdjective,
                proto_request,
                unary=True,
                reader=False,
                response_type=tests.reboot.greeter_pb2.SetAdjectiveResponse,
                aborted_type=Greeter.SetAdjectiveAborted,
                metadata=metadata,
                idempotency_key=idempotency_key,
                per_iteration=idempotency.per_iteration if idempotency is not None else False,
                bearer_token=bearer_token,
            ) as call:
                assert isinstance(call, IMPORT_typing.Awaitable), type(call)
                await call
                for (key, value) in await call.trailing_metadata():  # type: ignore[misc, attr-defined]
                    if key == IMPORT_reboot_aio_headers.TASK_ID_UUID:
                        return IMPORT_rbt_v1alpha1.tasks_pb2.TaskId(
                            state_type=IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                            state_ref=self._headers.state_ref.to_str(),
                            task_uuid=IMPORT_uuid.UUID(value).bytes,
                        )
                raise IMPORT_reboot.aio.aborted.SystemAborted(
                    IMPORT_rbt_v1alpha1.errors_pb2.Internal(),
                    message='Trailing metadata missing for task schedule',
                )
    async def TransactionSetAdjective(
        self,
        request: Greeter.TransactionSetAdjectiveRequest,
        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
        idempotency_key: IMPORT_typing.Optional[IMPORT_uuid.UUID]
        proto_request = GreeterTransactionSetAdjectiveRequestToProto(
            request,
        )

        with self._idempotency_manager.idempotently(
            state_type_name=IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            state_ref=self._headers.state_ref,
            service_name=IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
            method='TransactionSetAdjective',
            mutation=True,
            request=proto_request,
            metadata=metadata,
            idempotency=idempotency,
            aborted_type=Greeter.TransactionSetAdjectiveAborted,
        ) as idempotency_key:
            async with self._call(
                IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
                'TransactionSetAdjective',
                self._tests_reboot_greetermethods_stub.TransactionSetAdjective,
                proto_request,
                unary=True,
                reader=False,
                response_type=tests.reboot.greeter_pb2.SetAdjectiveResponse,
                aborted_type=Greeter.TransactionSetAdjectiveAborted,
                metadata=metadata,
                idempotency_key=idempotency_key,
                per_iteration=idempotency.per_iteration if idempotency is not None else False,
                bearer_token=bearer_token,
            ) as call:
                assert isinstance(call, IMPORT_typing.Awaitable), type(call)
                await call
                for (key, value) in await call.trailing_metadata():  # type: ignore[misc, attr-defined]
                    if key == IMPORT_reboot_aio_headers.TASK_ID_UUID:
                        return IMPORT_rbt_v1alpha1.tasks_pb2.TaskId(
                            state_type=IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                            state_ref=self._headers.state_ref.to_str(),
                            task_uuid=IMPORT_uuid.UUID(value).bytes,
                        )
                raise IMPORT_reboot.aio.aborted.SystemAborted(
                    IMPORT_rbt_v1alpha1.errors_pb2.Internal(),
                    message='Trailing metadata missing for task schedule',
                )
    async def TryToConstructContext(
        self,
        request: Greeter.TryToConstructContextRequest,
        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
        idempotency_key: IMPORT_typing.Optional[IMPORT_uuid.UUID]
        proto_request = GreeterTryToConstructContextRequestToProto(
            request,
        )

        with self._idempotency_manager.idempotently(
            state_type_name=IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            state_ref=self._headers.state_ref,
            service_name=IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
            method='TryToConstructContext',
            mutation=True,
            request=proto_request,
            metadata=metadata,
            idempotency=idempotency,
            aborted_type=Greeter.TryToConstructContextAborted,
        ) as idempotency_key:
            async with self._call(
                IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
                'TryToConstructContext',
                self._tests_reboot_greetermethods_stub.TryToConstructContext,
                proto_request,
                unary=True,
                reader=False,
                response_type=google.protobuf.empty_pb2.Empty,
                aborted_type=Greeter.TryToConstructContextAborted,
                metadata=metadata,
                idempotency_key=idempotency_key,
                per_iteration=idempotency.per_iteration if idempotency is not None else False,
                bearer_token=bearer_token,
            ) as call:
                assert isinstance(call, IMPORT_typing.Awaitable), type(call)
                await call
                for (key, value) in await call.trailing_metadata():  # type: ignore[misc, attr-defined]
                    if key == IMPORT_reboot_aio_headers.TASK_ID_UUID:
                        return IMPORT_rbt_v1alpha1.tasks_pb2.TaskId(
                            state_type=IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                            state_ref=self._headers.state_ref.to_str(),
                            task_uuid=IMPORT_uuid.UUID(value).bytes,
                        )
                raise IMPORT_reboot.aio.aborted.SystemAborted(
                    IMPORT_rbt_v1alpha1.errors_pb2.Internal(),
                    message='Trailing metadata missing for task schedule',
                )
    async def TryToConstructExternalContext(
        self,
        request: Greeter.TryToConstructExternalContextRequest,
        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
        idempotency_key: IMPORT_typing.Optional[IMPORT_uuid.UUID]
        proto_request = GreeterTryToConstructExternalContextRequestToProto(
            request,
        )

        with self._idempotency_manager.idempotently(
            state_type_name=IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            state_ref=self._headers.state_ref,
            service_name=IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
            method='TryToConstructExternalContext',
            mutation=True,
            request=proto_request,
            metadata=metadata,
            idempotency=idempotency,
            aborted_type=Greeter.TryToConstructExternalContextAborted,
        ) as idempotency_key:
            async with self._call(
                IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
                'TryToConstructExternalContext',
                self._tests_reboot_greetermethods_stub.TryToConstructExternalContext,
                proto_request,
                unary=True,
                reader=False,
                response_type=google.protobuf.empty_pb2.Empty,
                aborted_type=Greeter.TryToConstructExternalContextAborted,
                metadata=metadata,
                idempotency_key=idempotency_key,
                per_iteration=idempotency.per_iteration if idempotency is not None else False,
                bearer_token=bearer_token,
            ) as call:
                assert isinstance(call, IMPORT_typing.Awaitable), type(call)
                await call
                for (key, value) in await call.trailing_metadata():  # type: ignore[misc, attr-defined]
                    if key == IMPORT_reboot_aio_headers.TASK_ID_UUID:
                        return IMPORT_rbt_v1alpha1.tasks_pb2.TaskId(
                            state_type=IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                            state_ref=self._headers.state_ref.to_str(),
                            task_uuid=IMPORT_uuid.UUID(value).bytes,
                        )
                raise IMPORT_reboot.aio.aborted.SystemAborted(
                    IMPORT_rbt_v1alpha1.errors_pb2.Internal(),
                    message='Trailing metadata missing for task schedule',
                )
    async def TestLongRunningFetch(
        self,
        request: Greeter.TestLongRunningFetchRequest,
        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
        idempotency_key: IMPORT_typing.Optional[IMPORT_uuid.UUID]
        proto_request = GreeterTestLongRunningFetchRequestToProto(
            request,
        )

        with self._idempotency_manager.idempotently(
            state_type_name=IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            state_ref=self._headers.state_ref,
            service_name=IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
            method='TestLongRunningFetch',
            mutation=True,
            request=proto_request,
            metadata=metadata,
            idempotency=idempotency,
            aborted_type=Greeter.TestLongRunningFetchAborted,
        ) as idempotency_key:
            async with self._call(
                IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
                'TestLongRunningFetch',
                self._tests_reboot_greetermethods_stub.TestLongRunningFetch,
                proto_request,
                unary=True,
                reader=False,
                response_type=google.protobuf.empty_pb2.Empty,
                aborted_type=Greeter.TestLongRunningFetchAborted,
                metadata=metadata,
                idempotency_key=idempotency_key,
                per_iteration=idempotency.per_iteration if idempotency is not None else False,
                bearer_token=bearer_token,
            ) as call:
                assert isinstance(call, IMPORT_typing.Awaitable), type(call)
                await call
                for (key, value) in await call.trailing_metadata():  # type: ignore[misc, attr-defined]
                    if key == IMPORT_reboot_aio_headers.TASK_ID_UUID:
                        return IMPORT_rbt_v1alpha1.tasks_pb2.TaskId(
                            state_type=IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                            state_ref=self._headers.state_ref.to_str(),
                            task_uuid=IMPORT_uuid.UUID(value).bytes,
                        )
                raise IMPORT_reboot.aio.aborted.SystemAborted(
                    IMPORT_rbt_v1alpha1.errors_pb2.Internal(),
                    message='Trailing metadata missing for task schedule',
                )
    async def TestLongRunningWriter(
        self,
        request: Greeter.TestLongRunningWriterRequest,
        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
        idempotency_key: IMPORT_typing.Optional[IMPORT_uuid.UUID]
        proto_request = GreeterTestLongRunningWriterRequestToProto(
            request,
        )

        with self._idempotency_manager.idempotently(
            state_type_name=IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            state_ref=self._headers.state_ref,
            service_name=IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
            method='TestLongRunningWriter',
            mutation=True,
            request=proto_request,
            metadata=metadata,
            idempotency=idempotency,
            aborted_type=Greeter.TestLongRunningWriterAborted,
        ) as idempotency_key:
            async with self._call(
                IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
                'TestLongRunningWriter',
                self._tests_reboot_greetermethods_stub.TestLongRunningWriter,
                proto_request,
                unary=True,
                reader=False,
                response_type=google.protobuf.empty_pb2.Empty,
                aborted_type=Greeter.TestLongRunningWriterAborted,
                metadata=metadata,
                idempotency_key=idempotency_key,
                per_iteration=idempotency.per_iteration if idempotency is not None else False,
                bearer_token=bearer_token,
            ) as call:
                assert isinstance(call, IMPORT_typing.Awaitable), type(call)
                await call
                for (key, value) in await call.trailing_metadata():  # type: ignore[misc, attr-defined]
                    if key == IMPORT_reboot_aio_headers.TASK_ID_UUID:
                        return IMPORT_rbt_v1alpha1.tasks_pb2.TaskId(
                            state_type=IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                            state_ref=self._headers.state_ref.to_str(),
                            task_uuid=IMPORT_uuid.UUID(value).bytes,
                        )
                raise IMPORT_reboot.aio.aborted.SystemAborted(
                    IMPORT_rbt_v1alpha1.errors_pb2.Internal(),
                    message='Trailing metadata missing for task schedule',
                )
    async def GetWholeState(
        self,
        request: Greeter.GetWholeStateRequest,
        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
        idempotency_key: IMPORT_typing.Optional[IMPORT_uuid.UUID]
        proto_request = GreeterGetWholeStateRequestToProto(
            request,
        )

        with self._idempotency_manager.idempotently(
            state_type_name=IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            state_ref=self._headers.state_ref,
            service_name=IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
            method='GetWholeState',
            mutation=True,
            request=proto_request,
            metadata=metadata,
            idempotency=idempotency,
            aborted_type=Greeter.GetWholeStateAborted,
        ) as idempotency_key:
            async with self._call(
                IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
                'GetWholeState',
                self._tests_reboot_greetermethods_stub.GetWholeState,
                proto_request,
                unary=True,
                reader=False,
                response_type=tests.reboot.greeter_pb2.Greeter,
                aborted_type=Greeter.GetWholeStateAborted,
                metadata=metadata,
                idempotency_key=idempotency_key,
                per_iteration=idempotency.per_iteration if idempotency is not None else False,
                bearer_token=bearer_token,
            ) as call:
                assert isinstance(call, IMPORT_typing.Awaitable), type(call)
                await call
                for (key, value) in await call.trailing_metadata():  # type: ignore[misc, attr-defined]
                    if key == IMPORT_reboot_aio_headers.TASK_ID_UUID:
                        return IMPORT_rbt_v1alpha1.tasks_pb2.TaskId(
                            state_type=IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                            state_ref=self._headers.state_ref.to_str(),
                            task_uuid=IMPORT_uuid.UUID(value).bytes,
                        )
                raise IMPORT_reboot.aio.aborted.SystemAborted(
                    IMPORT_rbt_v1alpha1.errors_pb2.Internal(),
                    message='Trailing metadata missing for task schedule',
                )
    async def FailWithException(
        self,
        request: Greeter.FailWithExceptionRequest,
        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
        idempotency_key: IMPORT_typing.Optional[IMPORT_uuid.UUID]
        proto_request = GreeterFailWithExceptionRequestToProto(
            request,
        )

        with self._idempotency_manager.idempotently(
            state_type_name=IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            state_ref=self._headers.state_ref,
            service_name=IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
            method='FailWithException',
            mutation=True,
            request=proto_request,
            metadata=metadata,
            idempotency=idempotency,
            aborted_type=Greeter.FailWithExceptionAborted,
        ) as idempotency_key:
            async with self._call(
                IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
                'FailWithException',
                self._tests_reboot_greetermethods_stub.FailWithException,
                proto_request,
                unary=True,
                reader=False,
                response_type=google.protobuf.empty_pb2.Empty,
                aborted_type=Greeter.FailWithExceptionAborted,
                metadata=metadata,
                idempotency_key=idempotency_key,
                per_iteration=idempotency.per_iteration if idempotency is not None else False,
                bearer_token=bearer_token,
            ) as call:
                assert isinstance(call, IMPORT_typing.Awaitable), type(call)
                await call
                for (key, value) in await call.trailing_metadata():  # type: ignore[misc, attr-defined]
                    if key == IMPORT_reboot_aio_headers.TASK_ID_UUID:
                        return IMPORT_rbt_v1alpha1.tasks_pb2.TaskId(
                            state_type=IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                            state_ref=self._headers.state_ref.to_str(),
                            task_uuid=IMPORT_uuid.UUID(value).bytes,
                        )
                raise IMPORT_reboot.aio.aborted.SystemAborted(
                    IMPORT_rbt_v1alpha1.errors_pb2.Internal(),
                    message='Trailing metadata missing for task schedule',
                )
    async def FailWithAborted(
        self,
        request: Greeter.FailWithAbortedRequest,
        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
        idempotency_key: IMPORT_typing.Optional[IMPORT_uuid.UUID]
        proto_request = GreeterFailWithAbortedRequestToProto(
            request,
        )

        with self._idempotency_manager.idempotently(
            state_type_name=IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            state_ref=self._headers.state_ref,
            service_name=IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
            method='FailWithAborted',
            mutation=True,
            request=proto_request,
            metadata=metadata,
            idempotency=idempotency,
            aborted_type=Greeter.FailWithAbortedAborted,
        ) as idempotency_key:
            async with self._call(
                IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
                'FailWithAborted',
                self._tests_reboot_greetermethods_stub.FailWithAborted,
                proto_request,
                unary=True,
                reader=False,
                response_type=google.protobuf.empty_pb2.Empty,
                aborted_type=Greeter.FailWithAbortedAborted,
                metadata=metadata,
                idempotency_key=idempotency_key,
                per_iteration=idempotency.per_iteration if idempotency is not None else False,
                bearer_token=bearer_token,
            ) as call:
                assert isinstance(call, IMPORT_typing.Awaitable), type(call)
                await call
                for (key, value) in await call.trailing_metadata():  # type: ignore[misc, attr-defined]
                    if key == IMPORT_reboot_aio_headers.TASK_ID_UUID:
                        return IMPORT_rbt_v1alpha1.tasks_pb2.TaskId(
                            state_type=IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                            state_ref=self._headers.state_ref.to_str(),
                            task_uuid=IMPORT_uuid.UUID(value).bytes,
                        )
                raise IMPORT_reboot.aio.aborted.SystemAborted(
                    IMPORT_rbt_v1alpha1.errors_pb2.Internal(),
                    message='Trailing metadata missing for task schedule',
                )
    async def Workflow(
        self,
        request: Greeter.WorkflowRequest,
        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
        idempotency_key: IMPORT_typing.Optional[IMPORT_uuid.UUID]
        proto_request = GreeterWorkflowRequestToProto(
            request,
        )

        with self._idempotency_manager.idempotently(
            state_type_name=IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            state_ref=self._headers.state_ref,
            service_name=IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
            method='Workflow',
            mutation=True,
            request=proto_request,
            metadata=metadata,
            idempotency=idempotency,
            aborted_type=Greeter.WorkflowAborted,
        ) as idempotency_key:
            async with self._call(
                IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
                'Workflow',
                self._tests_reboot_greetermethods_stub.Workflow,
                proto_request,
                unary=True,
                reader=False,
                response_type=tests.reboot.greeter_pb2.WorkflowResponse,
                aborted_type=Greeter.WorkflowAborted,
                metadata=metadata,
                idempotency_key=idempotency_key,
                per_iteration=idempotency.per_iteration if idempotency is not None else False,
                bearer_token=bearer_token,
            ) as call:
                assert isinstance(call, IMPORT_typing.Awaitable), type(call)
                await call
                for (key, value) in await call.trailing_metadata():  # type: ignore[misc, attr-defined]
                    if key == IMPORT_reboot_aio_headers.TASK_ID_UUID:
                        return IMPORT_rbt_v1alpha1.tasks_pb2.TaskId(
                            state_type=IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                            state_ref=self._headers.state_ref.to_str(),
                            task_uuid=IMPORT_uuid.UUID(value).bytes,
                        )
                raise IMPORT_reboot.aio.aborted.SystemAborted(
                    IMPORT_rbt_v1alpha1.errors_pb2.Internal(),
                    message='Trailing metadata missing for task schedule',
                )
    async def DangerousFields(
        self,
        request: Greeter.DangerousFieldsRequest,
        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
        idempotency_key: IMPORT_typing.Optional[IMPORT_uuid.UUID]
        proto_request = GreeterDangerousFieldsRequestToProto(
            request,
        )

        with self._idempotency_manager.idempotently(
            state_type_name=IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            state_ref=self._headers.state_ref,
            service_name=IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
            method='DangerousFields',
            mutation=True,
            request=proto_request,
            metadata=metadata,
            idempotency=idempotency,
            aborted_type=Greeter.DangerousFieldsAborted,
        ) as idempotency_key:
            async with self._call(
                IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
                'DangerousFields',
                self._tests_reboot_greetermethods_stub.DangerousFields,
                proto_request,
                unary=True,
                reader=False,
                response_type=google.protobuf.empty_pb2.Empty,
                aborted_type=Greeter.DangerousFieldsAborted,
                metadata=metadata,
                idempotency_key=idempotency_key,
                per_iteration=idempotency.per_iteration if idempotency is not None else False,
                bearer_token=bearer_token,
            ) as call:
                assert isinstance(call, IMPORT_typing.Awaitable), type(call)
                await call
                for (key, value) in await call.trailing_metadata():  # type: ignore[misc, attr-defined]
                    if key == IMPORT_reboot_aio_headers.TASK_ID_UUID:
                        return IMPORT_rbt_v1alpha1.tasks_pb2.TaskId(
                            state_type=IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                            state_ref=self._headers.state_ref.to_str(),
                            task_uuid=IMPORT_uuid.UUID(value).bytes,
                        )
                raise IMPORT_reboot.aio.aborted.SystemAborted(
                    IMPORT_rbt_v1alpha1.errors_pb2.Internal(),
                    message='Trailing metadata missing for task schedule',
                )
    async def StoreRecursiveMessage(
        self,
        request: Greeter.StoreRecursiveMessageRequest,
        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
        idempotency_key: IMPORT_typing.Optional[IMPORT_uuid.UUID]
        proto_request = GreeterStoreRecursiveMessageRequestToProto(
            request,
        )

        with self._idempotency_manager.idempotently(
            state_type_name=IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            state_ref=self._headers.state_ref,
            service_name=IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
            method='StoreRecursiveMessage',
            mutation=True,
            request=proto_request,
            metadata=metadata,
            idempotency=idempotency,
            aborted_type=Greeter.StoreRecursiveMessageAborted,
        ) as idempotency_key:
            async with self._call(
                IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
                'StoreRecursiveMessage',
                self._tests_reboot_greetermethods_stub.StoreRecursiveMessage,
                proto_request,
                unary=True,
                reader=False,
                response_type=tests.reboot.greeter_pb2.StoreRecursiveMessageResponse,
                aborted_type=Greeter.StoreRecursiveMessageAborted,
                metadata=metadata,
                idempotency_key=idempotency_key,
                per_iteration=idempotency.per_iteration if idempotency is not None else False,
                bearer_token=bearer_token,
            ) as call:
                assert isinstance(call, IMPORT_typing.Awaitable), type(call)
                await call
                for (key, value) in await call.trailing_metadata():  # type: ignore[misc, attr-defined]
                    if key == IMPORT_reboot_aio_headers.TASK_ID_UUID:
                        return IMPORT_rbt_v1alpha1.tasks_pb2.TaskId(
                            state_type=IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                            state_ref=self._headers.state_ref.to_str(),
                            task_uuid=IMPORT_uuid.UUID(value).bytes,
                        )
                raise IMPORT_reboot.aio.aborted.SystemAborted(
                    IMPORT_rbt_v1alpha1.errors_pb2.Internal(),
                    message='Trailing metadata missing for task schedule',
                )
    async def ReadRecursiveMessage(
        self,
        request: Greeter.ReadRecursiveMessageRequest,
        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
        idempotency_key: IMPORT_typing.Optional[IMPORT_uuid.UUID]
        proto_request = GreeterReadRecursiveMessageRequestToProto(
            request,
        )

        with self._idempotency_manager.idempotently(
            state_type_name=IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            state_ref=self._headers.state_ref,
            service_name=IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
            method='ReadRecursiveMessage',
            mutation=True,
            request=proto_request,
            metadata=metadata,
            idempotency=idempotency,
            aborted_type=Greeter.ReadRecursiveMessageAborted,
        ) as idempotency_key:
            async with self._call(
                IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
                'ReadRecursiveMessage',
                self._tests_reboot_greetermethods_stub.ReadRecursiveMessage,
                proto_request,
                unary=True,
                reader=False,
                response_type=tests.reboot.greeter_pb2.ReadRecursiveMessageResponse,
                aborted_type=Greeter.ReadRecursiveMessageAborted,
                metadata=metadata,
                idempotency_key=idempotency_key,
                per_iteration=idempotency.per_iteration if idempotency is not None else False,
                bearer_token=bearer_token,
            ) as call:
                assert isinstance(call, IMPORT_typing.Awaitable), type(call)
                await call
                for (key, value) in await call.trailing_metadata():  # type: ignore[misc, attr-defined]
                    if key == IMPORT_reboot_aio_headers.TASK_ID_UUID:
                        return IMPORT_rbt_v1alpha1.tasks_pb2.TaskId(
                            state_type=IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                            state_ref=self._headers.state_ref.to_str(),
                            task_uuid=IMPORT_uuid.UUID(value).bytes,
                        )
                raise IMPORT_reboot.aio.aborted.SystemAborted(
                    IMPORT_rbt_v1alpha1.errors_pb2.Internal(),
                    message='Trailing metadata missing for task schedule',
                )
    async def ConstructAndStoreRecursiveMessage(
        self,
        request: Greeter.ConstructAndStoreRecursiveMessageRequest,
        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
        idempotency_key: IMPORT_typing.Optional[IMPORT_uuid.UUID]
        proto_request = GreeterConstructAndStoreRecursiveMessageRequestToProto(
            request,
        )

        with self._idempotency_manager.idempotently(
            state_type_name=IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            state_ref=self._headers.state_ref,
            service_name=IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
            method='ConstructAndStoreRecursiveMessage',
            mutation=True,
            request=proto_request,
            metadata=metadata,
            idempotency=idempotency,
            aborted_type=Greeter.ConstructAndStoreRecursiveMessageAborted,
        ) as idempotency_key:
            async with self._call(
                IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
                'ConstructAndStoreRecursiveMessage',
                self._tests_reboot_greetermethods_stub.ConstructAndStoreRecursiveMessage,
                proto_request,
                unary=True,
                reader=False,
                response_type=tests.reboot.greeter_pb2.ConstructAndStoreRecursiveMessageResponse,
                aborted_type=Greeter.ConstructAndStoreRecursiveMessageAborted,
                metadata=metadata,
                idempotency_key=idempotency_key,
                per_iteration=idempotency.per_iteration if idempotency is not None else False,
                bearer_token=bearer_token,
            ) as call:
                assert isinstance(call, IMPORT_typing.Awaitable), type(call)
                await call
                for (key, value) in await call.trailing_metadata():  # type: ignore[misc, attr-defined]
                    if key == IMPORT_reboot_aio_headers.TASK_ID_UUID:
                        return IMPORT_rbt_v1alpha1.tasks_pb2.TaskId(
                            state_type=IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                            state_ref=self._headers.state_ref.to_str(),
                            task_uuid=IMPORT_uuid.UUID(value).bytes,
                        )
                raise IMPORT_reboot.aio.aborted.SystemAborted(
                    IMPORT_rbt_v1alpha1.errors_pb2.Internal(),
                    message='Trailing metadata missing for task schedule',
                )


class GreeterServicerTasks:

    _context: IMPORT_reboot_aio_contexts.WriterContext

    def __init__(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        *,
        state_ref: IMPORT_reboot_aio_types.StateRef,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ):
        IMPORT_reboot_aio_types.assert_type(context, [IMPORT_reboot_aio_contexts.WriterContext])
        self._context = context
        self._state_ref = state_ref

    # Greeter specific methods:
    async def Create(
        self,
        request: Greeter.CreateRequest,
        *,
        schedule: IMPORT_typing.Optional[IMPORT_datetime_datetime | IMPORT_datetime_timedelta] = None,
    ) -> IMPORT_reboot_aio_tasks.TaskEffect:
        schedule = ensure_has_timezone(when=schedule)
        task = IMPORT_reboot_aio_tasks.TaskEffect(
            state_type=IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            state_ref=self._state_ref,
            method_name='Create',
            request=GreeterCreateRequestToProto(
                request,
            ),
            schedule=(IMPORT_reboot_time_DateTimeWithTimeZone.now() + schedule) if isinstance(
                schedule, IMPORT_datetime_timedelta
            ) else schedule,
        )

        self._context._tasks.append(task)

        return task

    async def Greet(
        self,
        request: Greeter.GreetRequest,
        *,
        schedule: IMPORT_typing.Optional[IMPORT_datetime_datetime | IMPORT_datetime_timedelta] = None,
    ) -> IMPORT_reboot_aio_tasks.TaskEffect:
        schedule = ensure_has_timezone(when=schedule)
        task = IMPORT_reboot_aio_tasks.TaskEffect(
            state_type=IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            state_ref=self._state_ref,
            method_name='Greet',
            request=GreeterGreetRequestToProto(
                request,
            ),
            schedule=(IMPORT_reboot_time_DateTimeWithTimeZone.now() + schedule) if isinstance(
                schedule, IMPORT_datetime_timedelta
            ) else schedule,
        )

        self._context._tasks.append(task)

        return task

    async def SetAdjective(
        self,
        request: Greeter.SetAdjectiveRequest,
        *,
        schedule: IMPORT_typing.Optional[IMPORT_datetime_datetime | IMPORT_datetime_timedelta] = None,
    ) -> IMPORT_reboot_aio_tasks.TaskEffect:
        schedule = ensure_has_timezone(when=schedule)
        task = IMPORT_reboot_aio_tasks.TaskEffect(
            state_type=IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            state_ref=self._state_ref,
            method_name='SetAdjective',
            request=GreeterSetAdjectiveRequestToProto(
                request,
            ),
            schedule=(IMPORT_reboot_time_DateTimeWithTimeZone.now() + schedule) if isinstance(
                schedule, IMPORT_datetime_timedelta
            ) else schedule,
        )

        self._context._tasks.append(task)

        return task

    async def TransactionSetAdjective(
        self,
        request: Greeter.TransactionSetAdjectiveRequest,
        *,
        schedule: IMPORT_typing.Optional[IMPORT_datetime_datetime | IMPORT_datetime_timedelta] = None,
    ) -> IMPORT_reboot_aio_tasks.TaskEffect:
        schedule = ensure_has_timezone(when=schedule)
        task = IMPORT_reboot_aio_tasks.TaskEffect(
            state_type=IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            state_ref=self._state_ref,
            method_name='TransactionSetAdjective',
            request=GreeterTransactionSetAdjectiveRequestToProto(
                request,
            ),
            schedule=(IMPORT_reboot_time_DateTimeWithTimeZone.now() + schedule) if isinstance(
                schedule, IMPORT_datetime_timedelta
            ) else schedule,
        )

        self._context._tasks.append(task)

        return task

    async def TryToConstructContext(
        self,
        request: Greeter.TryToConstructContextRequest,
        *,
        schedule: IMPORT_typing.Optional[IMPORT_datetime_datetime | IMPORT_datetime_timedelta] = None,
    ) -> IMPORT_reboot_aio_tasks.TaskEffect:
        schedule = ensure_has_timezone(when=schedule)
        task = IMPORT_reboot_aio_tasks.TaskEffect(
            state_type=IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            state_ref=self._state_ref,
            method_name='TryToConstructContext',
            request=GreeterTryToConstructContextRequestToProto(
                request,
            ),
            schedule=(IMPORT_reboot_time_DateTimeWithTimeZone.now() + schedule) if isinstance(
                schedule, IMPORT_datetime_timedelta
            ) else schedule,
        )

        self._context._tasks.append(task)

        return task

    async def TryToConstructExternalContext(
        self,
        request: Greeter.TryToConstructExternalContextRequest,
        *,
        schedule: IMPORT_typing.Optional[IMPORT_datetime_datetime | IMPORT_datetime_timedelta] = None,
    ) -> IMPORT_reboot_aio_tasks.TaskEffect:
        schedule = ensure_has_timezone(when=schedule)
        task = IMPORT_reboot_aio_tasks.TaskEffect(
            state_type=IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            state_ref=self._state_ref,
            method_name='TryToConstructExternalContext',
            request=GreeterTryToConstructExternalContextRequestToProto(
                request,
            ),
            schedule=(IMPORT_reboot_time_DateTimeWithTimeZone.now() + schedule) if isinstance(
                schedule, IMPORT_datetime_timedelta
            ) else schedule,
        )

        self._context._tasks.append(task)

        return task

    async def TestLongRunningFetch(
        self,
        request: Greeter.TestLongRunningFetchRequest,
        *,
        schedule: IMPORT_typing.Optional[IMPORT_datetime_datetime | IMPORT_datetime_timedelta] = None,
    ) -> IMPORT_reboot_aio_tasks.TaskEffect:
        schedule = ensure_has_timezone(when=schedule)
        task = IMPORT_reboot_aio_tasks.TaskEffect(
            state_type=IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            state_ref=self._state_ref,
            method_name='TestLongRunningFetch',
            request=GreeterTestLongRunningFetchRequestToProto(
                request,
            ),
            schedule=(IMPORT_reboot_time_DateTimeWithTimeZone.now() + schedule) if isinstance(
                schedule, IMPORT_datetime_timedelta
            ) else schedule,
        )

        self._context._tasks.append(task)

        return task

    async def TestLongRunningWriter(
        self,
        request: Greeter.TestLongRunningWriterRequest,
        *,
        schedule: IMPORT_typing.Optional[IMPORT_datetime_datetime | IMPORT_datetime_timedelta] = None,
    ) -> IMPORT_reboot_aio_tasks.TaskEffect:
        schedule = ensure_has_timezone(when=schedule)
        task = IMPORT_reboot_aio_tasks.TaskEffect(
            state_type=IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            state_ref=self._state_ref,
            method_name='TestLongRunningWriter',
            request=GreeterTestLongRunningWriterRequestToProto(
                request,
            ),
            schedule=(IMPORT_reboot_time_DateTimeWithTimeZone.now() + schedule) if isinstance(
                schedule, IMPORT_datetime_timedelta
            ) else schedule,
        )

        self._context._tasks.append(task)

        return task

    async def GetWholeState(
        self,
        request: Greeter.GetWholeStateRequest,
        *,
        schedule: IMPORT_typing.Optional[IMPORT_datetime_datetime | IMPORT_datetime_timedelta] = None,
    ) -> IMPORT_reboot_aio_tasks.TaskEffect:
        schedule = ensure_has_timezone(when=schedule)
        task = IMPORT_reboot_aio_tasks.TaskEffect(
            state_type=IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            state_ref=self._state_ref,
            method_name='GetWholeState',
            request=GreeterGetWholeStateRequestToProto(
                request,
            ),
            schedule=(IMPORT_reboot_time_DateTimeWithTimeZone.now() + schedule) if isinstance(
                schedule, IMPORT_datetime_timedelta
            ) else schedule,
        )

        self._context._tasks.append(task)

        return task

    async def FailWithException(
        self,
        request: Greeter.FailWithExceptionRequest,
        *,
        schedule: IMPORT_typing.Optional[IMPORT_datetime_datetime | IMPORT_datetime_timedelta] = None,
    ) -> IMPORT_reboot_aio_tasks.TaskEffect:
        schedule = ensure_has_timezone(when=schedule)
        task = IMPORT_reboot_aio_tasks.TaskEffect(
            state_type=IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            state_ref=self._state_ref,
            method_name='FailWithException',
            request=GreeterFailWithExceptionRequestToProto(
                request,
            ),
            schedule=(IMPORT_reboot_time_DateTimeWithTimeZone.now() + schedule) if isinstance(
                schedule, IMPORT_datetime_timedelta
            ) else schedule,
        )

        self._context._tasks.append(task)

        return task

    async def FailWithAborted(
        self,
        request: Greeter.FailWithAbortedRequest,
        *,
        schedule: IMPORT_typing.Optional[IMPORT_datetime_datetime | IMPORT_datetime_timedelta] = None,
    ) -> IMPORT_reboot_aio_tasks.TaskEffect:
        schedule = ensure_has_timezone(when=schedule)
        task = IMPORT_reboot_aio_tasks.TaskEffect(
            state_type=IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            state_ref=self._state_ref,
            method_name='FailWithAborted',
            request=GreeterFailWithAbortedRequestToProto(
                request,
            ),
            schedule=(IMPORT_reboot_time_DateTimeWithTimeZone.now() + schedule) if isinstance(
                schedule, IMPORT_datetime_timedelta
            ) else schedule,
        )

        self._context._tasks.append(task)

        return task

    async def Workflow(
        self,
        request: Greeter.WorkflowRequest,
        *,
        schedule: IMPORT_typing.Optional[IMPORT_datetime_datetime | IMPORT_datetime_timedelta] = None,
    ) -> IMPORT_reboot_aio_tasks.TaskEffect:
        schedule = ensure_has_timezone(when=schedule)
        task = IMPORT_reboot_aio_tasks.TaskEffect(
            state_type=IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            state_ref=self._state_ref,
            method_name='Workflow',
            request=GreeterWorkflowRequestToProto(
                request,
            ),
            schedule=(IMPORT_reboot_time_DateTimeWithTimeZone.now() + schedule) if isinstance(
                schedule, IMPORT_datetime_timedelta
            ) else schedule,
        )

        self._context._tasks.append(task)

        return task

    async def DangerousFields(
        self,
        request: Greeter.DangerousFieldsRequest,
        *,
        schedule: IMPORT_typing.Optional[IMPORT_datetime_datetime | IMPORT_datetime_timedelta] = None,
    ) -> IMPORT_reboot_aio_tasks.TaskEffect:
        schedule = ensure_has_timezone(when=schedule)
        task = IMPORT_reboot_aio_tasks.TaskEffect(
            state_type=IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            state_ref=self._state_ref,
            method_name='DangerousFields',
            request=GreeterDangerousFieldsRequestToProto(
                request,
            ),
            schedule=(IMPORT_reboot_time_DateTimeWithTimeZone.now() + schedule) if isinstance(
                schedule, IMPORT_datetime_timedelta
            ) else schedule,
        )

        self._context._tasks.append(task)

        return task

    async def StoreRecursiveMessage(
        self,
        request: Greeter.StoreRecursiveMessageRequest,
        *,
        schedule: IMPORT_typing.Optional[IMPORT_datetime_datetime | IMPORT_datetime_timedelta] = None,
    ) -> IMPORT_reboot_aio_tasks.TaskEffect:
        schedule = ensure_has_timezone(when=schedule)
        task = IMPORT_reboot_aio_tasks.TaskEffect(
            state_type=IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            state_ref=self._state_ref,
            method_name='StoreRecursiveMessage',
            request=GreeterStoreRecursiveMessageRequestToProto(
                request,
            ),
            schedule=(IMPORT_reboot_time_DateTimeWithTimeZone.now() + schedule) if isinstance(
                schedule, IMPORT_datetime_timedelta
            ) else schedule,
        )

        self._context._tasks.append(task)

        return task

    async def ReadRecursiveMessage(
        self,
        request: Greeter.ReadRecursiveMessageRequest,
        *,
        schedule: IMPORT_typing.Optional[IMPORT_datetime_datetime | IMPORT_datetime_timedelta] = None,
    ) -> IMPORT_reboot_aio_tasks.TaskEffect:
        schedule = ensure_has_timezone(when=schedule)
        task = IMPORT_reboot_aio_tasks.TaskEffect(
            state_type=IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            state_ref=self._state_ref,
            method_name='ReadRecursiveMessage',
            request=GreeterReadRecursiveMessageRequestToProto(
                request,
            ),
            schedule=(IMPORT_reboot_time_DateTimeWithTimeZone.now() + schedule) if isinstance(
                schedule, IMPORT_datetime_timedelta
            ) else schedule,
        )

        self._context._tasks.append(task)

        return task

    async def ConstructAndStoreRecursiveMessage(
        self,
        request: Greeter.ConstructAndStoreRecursiveMessageRequest,
        *,
        schedule: IMPORT_typing.Optional[IMPORT_datetime_datetime | IMPORT_datetime_timedelta] = None,
    ) -> IMPORT_reboot_aio_tasks.TaskEffect:
        schedule = ensure_has_timezone(when=schedule)
        task = IMPORT_reboot_aio_tasks.TaskEffect(
            state_type=IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
            state_ref=self._state_ref,
            method_name='ConstructAndStoreRecursiveMessage',
            request=GreeterConstructAndStoreRecursiveMessageRequestToProto(
                request,
            ),
            schedule=(IMPORT_reboot_time_DateTimeWithTimeZone.now() + schedule) if isinstance(
                schedule, IMPORT_datetime_timedelta
            ) else schedule,
        )

        self._context._tasks.append(task)

        return task



############################ Authorizers ############################
# Relevant to servicers; irrelevant to clients.

GreeterStateType: IMPORT_typing.TypeAlias = tests.reboot.greeter_pb2.Greeter
GreeterRequestTypes: IMPORT_typing.TypeAlias = \
        tests.reboot.greeter_pb2.CreateRequest \
        | tests.reboot.greeter_pb2.GreetRequest \
        | tests.reboot.greeter_pb2.SetAdjectiveRequest \
        | google.protobuf.empty_pb2.Empty \
        | tests.reboot.greeter_pb2.TestLongRunningFetchRequest \
        | tests.reboot.greeter_pb2.GetWholeStateRequest \
        | tests.reboot.greeter_pb2.DangerousFieldsRequest \
        | tests.reboot.greeter_pb2.StoreRecursiveMessageRequest \
        | tests.reboot.greeter_pb2.ReadRecursiveMessageRequest \
        | tests.reboot.greeter_pb2.ConstructAndStoreRecursiveMessageRequest

class GreeterAuthorizer(
    IMPORT_reboot_aio_auth_authorizers.Authorizer[GreeterStateType, GreeterRequestTypes],
):
    StateType: IMPORT_typing.TypeAlias = GreeterStateType
    RequestTypes: IMPORT_typing.TypeAlias = GreeterRequestTypes
    Decision: IMPORT_typing.TypeAlias = IMPORT_reboot_aio_auth_authorizers.Authorizer.Decision

    def __init__(
        self,
        *,
        Create: IMPORT_typing.Optional[
            IMPORT_reboot_aio_auth_authorizers.AuthorizerRule[
              Greeter.State,
              Greeter.CreateRequest,
            ]
        ] = None,
        create: IMPORT_typing.Optional[
            IMPORT_reboot_aio_auth_authorizers.AuthorizerRule[
              Greeter.State,
              Greeter.CreateRequest,
            ]
        ] = None,
        Greet: IMPORT_typing.Optional[
            IMPORT_reboot_aio_auth_authorizers.AuthorizerRule[
              Greeter.State,
              Greeter.GreetRequest,
            ]
        ] = None,
        greet: IMPORT_typing.Optional[
            IMPORT_reboot_aio_auth_authorizers.AuthorizerRule[
              Greeter.State,
              Greeter.GreetRequest,
            ]
        ] = None,
        SetAdjective: IMPORT_typing.Optional[
            IMPORT_reboot_aio_auth_authorizers.AuthorizerRule[
              Greeter.State,
              Greeter.SetAdjectiveRequest,
            ]
        ] = None,
        set_adjective: IMPORT_typing.Optional[
            IMPORT_reboot_aio_auth_authorizers.AuthorizerRule[
              Greeter.State,
              Greeter.SetAdjectiveRequest,
            ]
        ] = None,
        TransactionSetAdjective: IMPORT_typing.Optional[
            IMPORT_reboot_aio_auth_authorizers.AuthorizerRule[
              Greeter.State,
              Greeter.TransactionSetAdjectiveRequest,
            ]
        ] = None,
        transaction_set_adjective: IMPORT_typing.Optional[
            IMPORT_reboot_aio_auth_authorizers.AuthorizerRule[
              Greeter.State,
              Greeter.TransactionSetAdjectiveRequest,
            ]
        ] = None,
        TryToConstructContext: IMPORT_typing.Optional[
            IMPORT_reboot_aio_auth_authorizers.AuthorizerRule[
              Greeter.State,
              Greeter.TryToConstructContextRequest,
            ]
        ] = None,
        try_to_construct_context: IMPORT_typing.Optional[
            IMPORT_reboot_aio_auth_authorizers.AuthorizerRule[
              Greeter.State,
              Greeter.TryToConstructContextRequest,
            ]
        ] = None,
        TryToConstructExternalContext: IMPORT_typing.Optional[
            IMPORT_reboot_aio_auth_authorizers.AuthorizerRule[
              Greeter.State,
              Greeter.TryToConstructExternalContextRequest,
            ]
        ] = None,
        try_to_construct_external_context: IMPORT_typing.Optional[
            IMPORT_reboot_aio_auth_authorizers.AuthorizerRule[
              Greeter.State,
              Greeter.TryToConstructExternalContextRequest,
            ]
        ] = None,
        TestLongRunningFetch: IMPORT_typing.Optional[
            IMPORT_reboot_aio_auth_authorizers.AuthorizerRule[
              Greeter.State,
              Greeter.TestLongRunningFetchRequest,
            ]
        ] = None,
        test_long_running_fetch: IMPORT_typing.Optional[
            IMPORT_reboot_aio_auth_authorizers.AuthorizerRule[
              Greeter.State,
              Greeter.TestLongRunningFetchRequest,
            ]
        ] = None,
        TestLongRunningWriter: IMPORT_typing.Optional[
            IMPORT_reboot_aio_auth_authorizers.AuthorizerRule[
              Greeter.State,
              Greeter.TestLongRunningWriterRequest,
            ]
        ] = None,
        test_long_running_writer: IMPORT_typing.Optional[
            IMPORT_reboot_aio_auth_authorizers.AuthorizerRule[
              Greeter.State,
              Greeter.TestLongRunningWriterRequest,
            ]
        ] = None,
        GetWholeState: IMPORT_typing.Optional[
            IMPORT_reboot_aio_auth_authorizers.AuthorizerRule[
              Greeter.State,
              Greeter.GetWholeStateRequest,
            ]
        ] = None,
        get_whole_state: IMPORT_typing.Optional[
            IMPORT_reboot_aio_auth_authorizers.AuthorizerRule[
              Greeter.State,
              Greeter.GetWholeStateRequest,
            ]
        ] = None,
        FailWithException: IMPORT_typing.Optional[
            IMPORT_reboot_aio_auth_authorizers.AuthorizerRule[
              Greeter.State,
              Greeter.FailWithExceptionRequest,
            ]
        ] = None,
        fail_with_exception: IMPORT_typing.Optional[
            IMPORT_reboot_aio_auth_authorizers.AuthorizerRule[
              Greeter.State,
              Greeter.FailWithExceptionRequest,
            ]
        ] = None,
        FailWithAborted: IMPORT_typing.Optional[
            IMPORT_reboot_aio_auth_authorizers.AuthorizerRule[
              Greeter.State,
              Greeter.FailWithAbortedRequest,
            ]
        ] = None,
        fail_with_aborted: IMPORT_typing.Optional[
            IMPORT_reboot_aio_auth_authorizers.AuthorizerRule[
              Greeter.State,
              Greeter.FailWithAbortedRequest,
            ]
        ] = None,
        Workflow: IMPORT_typing.Optional[
            IMPORT_reboot_aio_auth_authorizers.AuthorizerRule[
              Greeter.State,
              Greeter.WorkflowRequest,
            ]
        ] = None,
        workflow: IMPORT_typing.Optional[
            IMPORT_reboot_aio_auth_authorizers.AuthorizerRule[
              Greeter.State,
              Greeter.WorkflowRequest,
            ]
        ] = None,
        DangerousFields: IMPORT_typing.Optional[
            IMPORT_reboot_aio_auth_authorizers.AuthorizerRule[
              Greeter.State,
              Greeter.DangerousFieldsRequest,
            ]
        ] = None,
        dangerous_fields: IMPORT_typing.Optional[
            IMPORT_reboot_aio_auth_authorizers.AuthorizerRule[
              Greeter.State,
              Greeter.DangerousFieldsRequest,
            ]
        ] = None,
        StoreRecursiveMessage: IMPORT_typing.Optional[
            IMPORT_reboot_aio_auth_authorizers.AuthorizerRule[
              Greeter.State,
              Greeter.StoreRecursiveMessageRequest,
            ]
        ] = None,
        store_recursive_message: IMPORT_typing.Optional[
            IMPORT_reboot_aio_auth_authorizers.AuthorizerRule[
              Greeter.State,
              Greeter.StoreRecursiveMessageRequest,
            ]
        ] = None,
        ReadRecursiveMessage: IMPORT_typing.Optional[
            IMPORT_reboot_aio_auth_authorizers.AuthorizerRule[
              Greeter.State,
              Greeter.ReadRecursiveMessageRequest,
            ]
        ] = None,
        read_recursive_message: IMPORT_typing.Optional[
            IMPORT_reboot_aio_auth_authorizers.AuthorizerRule[
              Greeter.State,
              Greeter.ReadRecursiveMessageRequest,
            ]
        ] = None,
        ConstructAndStoreRecursiveMessage: IMPORT_typing.Optional[
            IMPORT_reboot_aio_auth_authorizers.AuthorizerRule[
              Greeter.State,
              Greeter.ConstructAndStoreRecursiveMessageRequest,
            ]
        ] = None,
        construct_and_store_recursive_message: IMPORT_typing.Optional[
            IMPORT_reboot_aio_auth_authorizers.AuthorizerRule[
              Greeter.State,
              Greeter.ConstructAndStoreRecursiveMessageRequest,
            ]
        ] = None,
        # NOTE: using `_` prefix for `_default` so as not to collide
        # with any method names since a prefixed `_` is forbidden by
        # our protoc plugins.
        _default: IMPORT_reboot_aio_auth_authorizers.AuthorizerRule[
            tests.reboot.greeter_pb2.Greeter,
            IMPORT_google_protobuf_message.Message,
        ] = IMPORT_reboot_aio_auth_authorizers.allow_if(
            all=[IMPORT_reboot_aio_auth_authorizers.is_app_internal],
        ),
    ):
        if create is not None and Create is not None:
            raise ValueError(
                f"Cannot specify both 'Create' and 'create' authorizer rules"
            )
        self._create = create or Create
        if greet is not None and Greet is not None:
            raise ValueError(
                f"Cannot specify both 'Greet' and 'greet' authorizer rules"
            )
        self._greet = greet or Greet
        if set_adjective is not None and SetAdjective is not None:
            raise ValueError(
                f"Cannot specify both 'SetAdjective' and 'set_adjective' authorizer rules"
            )
        self._set_adjective = set_adjective or SetAdjective
        if transaction_set_adjective is not None and TransactionSetAdjective is not None:
            raise ValueError(
                f"Cannot specify both 'TransactionSetAdjective' and 'transaction_set_adjective' authorizer rules"
            )
        self._transaction_set_adjective = transaction_set_adjective or TransactionSetAdjective
        if try_to_construct_context is not None and TryToConstructContext is not None:
            raise ValueError(
                f"Cannot specify both 'TryToConstructContext' and 'try_to_construct_context' authorizer rules"
            )
        self._try_to_construct_context = try_to_construct_context or TryToConstructContext
        if try_to_construct_external_context is not None and TryToConstructExternalContext is not None:
            raise ValueError(
                f"Cannot specify both 'TryToConstructExternalContext' and 'try_to_construct_external_context' authorizer rules"
            )
        self._try_to_construct_external_context = try_to_construct_external_context or TryToConstructExternalContext
        if test_long_running_fetch is not None and TestLongRunningFetch is not None:
            raise ValueError(
                f"Cannot specify both 'TestLongRunningFetch' and 'test_long_running_fetch' authorizer rules"
            )
        self._test_long_running_fetch = test_long_running_fetch or TestLongRunningFetch
        if test_long_running_writer is not None and TestLongRunningWriter is not None:
            raise ValueError(
                f"Cannot specify both 'TestLongRunningWriter' and 'test_long_running_writer' authorizer rules"
            )
        self._test_long_running_writer = test_long_running_writer or TestLongRunningWriter
        if get_whole_state is not None and GetWholeState is not None:
            raise ValueError(
                f"Cannot specify both 'GetWholeState' and 'get_whole_state' authorizer rules"
            )
        self._get_whole_state = get_whole_state or GetWholeState
        if fail_with_exception is not None and FailWithException is not None:
            raise ValueError(
                f"Cannot specify both 'FailWithException' and 'fail_with_exception' authorizer rules"
            )
        self._fail_with_exception = fail_with_exception or FailWithException
        if fail_with_aborted is not None and FailWithAborted is not None:
            raise ValueError(
                f"Cannot specify both 'FailWithAborted' and 'fail_with_aborted' authorizer rules"
            )
        self._fail_with_aborted = fail_with_aborted or FailWithAborted
        if workflow is not None and Workflow is not None:
            raise ValueError(
                f"Cannot specify both 'Workflow' and 'workflow' authorizer rules"
            )
        self._workflow = workflow or Workflow
        if dangerous_fields is not None and DangerousFields is not None:
            raise ValueError(
                f"Cannot specify both 'DangerousFields' and 'dangerous_fields' authorizer rules"
            )
        self._dangerous_fields = dangerous_fields or DangerousFields
        if store_recursive_message is not None and StoreRecursiveMessage is not None:
            raise ValueError(
                f"Cannot specify both 'StoreRecursiveMessage' and 'store_recursive_message' authorizer rules"
            )
        self._store_recursive_message = store_recursive_message or StoreRecursiveMessage
        if read_recursive_message is not None and ReadRecursiveMessage is not None:
            raise ValueError(
                f"Cannot specify both 'ReadRecursiveMessage' and 'read_recursive_message' authorizer rules"
            )
        self._read_recursive_message = read_recursive_message or ReadRecursiveMessage
        if construct_and_store_recursive_message is not None and ConstructAndStoreRecursiveMessage is not None:
            raise ValueError(
                f"Cannot specify both 'ConstructAndStoreRecursiveMessage' and 'construct_and_store_recursive_message' authorizer rules"
            )
        self._construct_and_store_recursive_message = construct_and_store_recursive_message or ConstructAndStoreRecursiveMessage
        self.__default = _default

    async def authorize(
        self,
        *,
        method_name: str,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: IMPORT_typing.Optional[GreeterStateType],
        request: IMPORT_typing.Optional[GreeterRequestTypes],
        **kwargs,
    ) -> IMPORT_reboot_aio_auth_authorizers.Authorizer.Decision:
        if method_name == 'tests.reboot.GreeterMethods.Create':
            return await self.Create(
                context=context,
                request=IMPORT_typing.cast(tests.reboot.greeter_pb2.CreateRequest, request),
            )
        elif method_name == 'tests.reboot.GreeterMethods.Greet':
            return await self.Greet(
                context=context,
                state=IMPORT_typing.cast(tests.reboot.greeter_pb2.Greeter, state),
                request=IMPORT_typing.cast(tests.reboot.greeter_pb2.GreetRequest, request),
            )
        elif method_name == 'tests.reboot.GreeterMethods.SetAdjective':
            return await self.SetAdjective(
                context=context,
                state=IMPORT_typing.cast(tests.reboot.greeter_pb2.Greeter, state),
                request=IMPORT_typing.cast(tests.reboot.greeter_pb2.SetAdjectiveRequest, request),
            )
        elif method_name == 'tests.reboot.GreeterMethods.TransactionSetAdjective':
            return await self.TransactionSetAdjective(
                context=context,
                state=IMPORT_typing.cast(tests.reboot.greeter_pb2.Greeter, state),
                request=IMPORT_typing.cast(tests.reboot.greeter_pb2.SetAdjectiveRequest, request),
            )
        elif method_name == 'tests.reboot.GreeterMethods.TryToConstructContext':
            return await self.TryToConstructContext(
                context=context,
                state=IMPORT_typing.cast(tests.reboot.greeter_pb2.Greeter, state),
                request=IMPORT_typing.cast(google.protobuf.empty_pb2.Empty, request),
            )
        elif method_name == 'tests.reboot.GreeterMethods.TryToConstructExternalContext':
            return await self.TryToConstructExternalContext(
                context=context,
                state=IMPORT_typing.cast(tests.reboot.greeter_pb2.Greeter, state),
                request=IMPORT_typing.cast(google.protobuf.empty_pb2.Empty, request),
            )
        elif method_name == 'tests.reboot.GreeterMethods.TestLongRunningFetch':
            return await self.TestLongRunningFetch(
                context=context,
                state=IMPORT_typing.cast(tests.reboot.greeter_pb2.Greeter, state),
                request=IMPORT_typing.cast(tests.reboot.greeter_pb2.TestLongRunningFetchRequest, request),
            )
        elif method_name == 'tests.reboot.GreeterMethods.TestLongRunningWriter':
            return await self.TestLongRunningWriter(
                context=context,
                state=IMPORT_typing.cast(tests.reboot.greeter_pb2.Greeter, state),
                request=IMPORT_typing.cast(google.protobuf.empty_pb2.Empty, request),
            )
        elif method_name == 'tests.reboot.GreeterMethods.GetWholeState':
            return await self.GetWholeState(
                context=context,
                state=IMPORT_typing.cast(tests.reboot.greeter_pb2.Greeter, state),
                request=IMPORT_typing.cast(tests.reboot.greeter_pb2.GetWholeStateRequest, request),
            )
        elif method_name == 'tests.reboot.GreeterMethods.FailWithException':
            return await self.FailWithException(
                context=context,
                state=IMPORT_typing.cast(tests.reboot.greeter_pb2.Greeter, state),
                request=IMPORT_typing.cast(google.protobuf.empty_pb2.Empty, request),
            )
        elif method_name == 'tests.reboot.GreeterMethods.FailWithAborted':
            return await self.FailWithAborted(
                context=context,
                state=IMPORT_typing.cast(tests.reboot.greeter_pb2.Greeter, state),
                request=IMPORT_typing.cast(google.protobuf.empty_pb2.Empty, request),
            )
        elif method_name == 'tests.reboot.GreeterMethods.Workflow':
            return await self.Workflow(
                context=context,
                state=IMPORT_typing.cast(tests.reboot.greeter_pb2.Greeter, state),
                request=IMPORT_typing.cast(google.protobuf.empty_pb2.Empty, request),
            )
        elif method_name == 'tests.reboot.GreeterMethods.DangerousFields':
            return await self.DangerousFields(
                context=context,
                state=IMPORT_typing.cast(tests.reboot.greeter_pb2.Greeter, state),
                request=IMPORT_typing.cast(tests.reboot.greeter_pb2.DangerousFieldsRequest, request),
            )
        elif method_name == 'tests.reboot.GreeterMethods.StoreRecursiveMessage':
            return await self.StoreRecursiveMessage(
                context=context,
                state=IMPORT_typing.cast(tests.reboot.greeter_pb2.Greeter, state),
                request=IMPORT_typing.cast(tests.reboot.greeter_pb2.StoreRecursiveMessageRequest, request),
            )
        elif method_name == 'tests.reboot.GreeterMethods.ReadRecursiveMessage':
            return await self.ReadRecursiveMessage(
                context=context,
                state=IMPORT_typing.cast(tests.reboot.greeter_pb2.Greeter, state),
                request=IMPORT_typing.cast(tests.reboot.greeter_pb2.ReadRecursiveMessageRequest, request),
            )
        elif method_name == 'tests.reboot.GreeterMethods.ConstructAndStoreRecursiveMessage':
            return await self.ConstructAndStoreRecursiveMessage(
                context=context,
                state=IMPORT_typing.cast(tests.reboot.greeter_pb2.Greeter, state),
                request=IMPORT_typing.cast(tests.reboot.greeter_pb2.ConstructAndStoreRecursiveMessageRequest, request),
            )
        else:
            return IMPORT_rbt_v1alpha1.errors_pb2.PermissionDenied()

    # For 'tests.reboot.GreeterMethods.Create'.
    async def Create(
        self,
        *,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        request: Greeter.CreateRequest,
    ) -> IMPORT_reboot_aio_auth_authorizers.Authorizer.Decision:
        return await (self._create or self.__default).execute(
            context=context,
            state=None,
            request=request,
        )

    # For 'tests.reboot.GreeterMethods.Greet'.
    async def Greet(
        self,
        *,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: Greeter.State,
        request: Greeter.GreetRequest,
    ) -> IMPORT_reboot_aio_auth_authorizers.Authorizer.Decision:
        return await (self._greet or self.__default).execute(
            context=context,
            state=state,
            request=request,
        )

    # For 'tests.reboot.GreeterMethods.SetAdjective'.
    async def SetAdjective(
        self,
        *,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: Greeter.State,
        request: Greeter.SetAdjectiveRequest,
    ) -> IMPORT_reboot_aio_auth_authorizers.Authorizer.Decision:
        return await (self._set_adjective or self.__default).execute(
            context=context,
            state=state,
            request=request,
        )

    # For 'tests.reboot.GreeterMethods.TransactionSetAdjective'.
    async def TransactionSetAdjective(
        self,
        *,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: Greeter.State,
        request: Greeter.TransactionSetAdjectiveRequest,
    ) -> IMPORT_reboot_aio_auth_authorizers.Authorizer.Decision:
        return await (self._transaction_set_adjective or self.__default).execute(
            context=context,
            state=state,
            request=request,
        )

    # For 'tests.reboot.GreeterMethods.TryToConstructContext'.
    async def TryToConstructContext(
        self,
        *,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: Greeter.State,
        request: Greeter.TryToConstructContextRequest,
    ) -> IMPORT_reboot_aio_auth_authorizers.Authorizer.Decision:
        return await (self._try_to_construct_context or self.__default).execute(
            context=context,
            state=state,
            request=request,
        )

    # For 'tests.reboot.GreeterMethods.TryToConstructExternalContext'.
    async def TryToConstructExternalContext(
        self,
        *,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: Greeter.State,
        request: Greeter.TryToConstructExternalContextRequest,
    ) -> IMPORT_reboot_aio_auth_authorizers.Authorizer.Decision:
        return await (self._try_to_construct_external_context or self.__default).execute(
            context=context,
            state=state,
            request=request,
        )

    # For 'tests.reboot.GreeterMethods.TestLongRunningFetch'.
    async def TestLongRunningFetch(
        self,
        *,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: Greeter.State,
        request: Greeter.TestLongRunningFetchRequest,
    ) -> IMPORT_reboot_aio_auth_authorizers.Authorizer.Decision:
        return await (self._test_long_running_fetch or self.__default).execute(
            context=context,
            state=state,
            request=request,
        )

    # For 'tests.reboot.GreeterMethods.TestLongRunningWriter'.
    async def TestLongRunningWriter(
        self,
        *,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: Greeter.State,
        request: Greeter.TestLongRunningWriterRequest,
    ) -> IMPORT_reboot_aio_auth_authorizers.Authorizer.Decision:
        return await (self._test_long_running_writer or self.__default).execute(
            context=context,
            state=state,
            request=request,
        )

    # For 'tests.reboot.GreeterMethods.GetWholeState'.
    async def GetWholeState(
        self,
        *,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: Greeter.State,
        request: Greeter.GetWholeStateRequest,
    ) -> IMPORT_reboot_aio_auth_authorizers.Authorizer.Decision:
        return await (self._get_whole_state or self.__default).execute(
            context=context,
            state=state,
            request=request,
        )

    # For 'tests.reboot.GreeterMethods.FailWithException'.
    async def FailWithException(
        self,
        *,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: Greeter.State,
        request: Greeter.FailWithExceptionRequest,
    ) -> IMPORT_reboot_aio_auth_authorizers.Authorizer.Decision:
        return await (self._fail_with_exception or self.__default).execute(
            context=context,
            state=state,
            request=request,
        )

    # For 'tests.reboot.GreeterMethods.FailWithAborted'.
    async def FailWithAborted(
        self,
        *,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: Greeter.State,
        request: Greeter.FailWithAbortedRequest,
    ) -> IMPORT_reboot_aio_auth_authorizers.Authorizer.Decision:
        return await (self._fail_with_aborted or self.__default).execute(
            context=context,
            state=state,
            request=request,
        )

    # For 'tests.reboot.GreeterMethods.Workflow'.
    async def Workflow(
        self,
        *,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: Greeter.State,
        request: Greeter.WorkflowRequest,
    ) -> IMPORT_reboot_aio_auth_authorizers.Authorizer.Decision:
        return await (self._workflow or self.__default).execute(
            context=context,
            state=state,
            request=request,
        )

    # For 'tests.reboot.GreeterMethods.DangerousFields'.
    async def DangerousFields(
        self,
        *,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: Greeter.State,
        request: Greeter.DangerousFieldsRequest,
    ) -> IMPORT_reboot_aio_auth_authorizers.Authorizer.Decision:
        return await (self._dangerous_fields or self.__default).execute(
            context=context,
            state=state,
            request=request,
        )

    # For 'tests.reboot.GreeterMethods.StoreRecursiveMessage'.
    async def StoreRecursiveMessage(
        self,
        *,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: Greeter.State,
        request: Greeter.StoreRecursiveMessageRequest,
    ) -> IMPORT_reboot_aio_auth_authorizers.Authorizer.Decision:
        return await (self._store_recursive_message or self.__default).execute(
            context=context,
            state=state,
            request=request,
        )

    # For 'tests.reboot.GreeterMethods.ReadRecursiveMessage'.
    async def ReadRecursiveMessage(
        self,
        *,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: Greeter.State,
        request: Greeter.ReadRecursiveMessageRequest,
    ) -> IMPORT_reboot_aio_auth_authorizers.Authorizer.Decision:
        return await (self._read_recursive_message or self.__default).execute(
            context=context,
            state=state,
            request=request,
        )

    # For 'tests.reboot.GreeterMethods.ConstructAndStoreRecursiveMessage'.
    async def ConstructAndStoreRecursiveMessage(
        self,
        *,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: Greeter.State,
        request: Greeter.ConstructAndStoreRecursiveMessageRequest,
    ) -> IMPORT_reboot_aio_auth_authorizers.Authorizer.Decision:
        return await (self._construct_and_store_recursive_message or self.__default).execute(
            context=context,
            state=state,
            request=request,
        )



############################ Reboot Servicers ############################
# Base classes for server-side implementations of Reboot servicers.
# Irrelevant to clients.

class GreeterBaseServicer(IMPORT_reboot_aio_servicers.Servicer):
    Authorizer: IMPORT_typing.TypeAlias = GreeterAuthorizer
    # Node-backed servicers attach a JS bridge object at runtime. We declare it
    # on the base class so mypy accepts accesses when a servicer is typed as
    # `GreeterBaseServicer` (e.g. in classmethod workflows).
    _js_servicer_reference: IMPORT_typing.Any

    __servicer__: IMPORT_contextvars.ContextVar[IMPORT_typing.Optional[GreeterBaseServicer]] = IMPORT_contextvars.ContextVar(
        'Provides access to a servicer in the current asyncio context. '
        'We need that to be able to do inline writes and reads inside '
        'a workflow',
        default=None,
    )

    __service_names__ = [
        IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
    ]
    __state_type_name__ = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter')
    __state_type__ = tests.reboot.greeter_pb2.Greeter
    __file_descriptor__ = tests.reboot.greeter_pb2.DESCRIPTOR

    def __init__(self):
        super().__init__()
        # NOTE: need to hold on to the middleware so we can do inline
        # writes (see 'self.write(...)').
        #
        # Because '_middleware' is not really private this does mean
        # users may do possibly dangerous things, but this is no more
        # likely given they could have already overridden
        # 'create_middleware()'.
        self._middleware: IMPORT_typing.Optional[GreeterServicerMiddleware] = None

    def create_middleware(
        self,
        *,
        application_id: IMPORT_reboot_aio_types.ApplicationId,
        server_id: IMPORT_reboot_aio_types.ServerId,
        state_manager: IMPORT_reboot_aio_state_managers.StateManager,
        placement_client: IMPORT_reboot_aio_placement.PlacementClient,
        channel_manager: IMPORT_reboot_aio_internals_channel_manager._ChannelManager,
        tasks_cache: IMPORT_reboot_aio_internals_tasks_cache.TasksCache,
        token_verifier: IMPORT_typing.Optional[IMPORT_reboot_aio_auth_token_verifiers.TokenVerifier],
        effect_validation: IMPORT_reboot_aio_contexts.EffectValidation,
        ready: IMPORT_asyncio.Event,
    ) -> GreeterServicerMiddleware:
        self._middleware = GreeterServicerMiddleware(
            servicer=self,
            application_id=application_id,
            server_id=server_id,
            state_manager=state_manager,
            placement_client=placement_client,
            channel_manager=channel_manager,
            tasks_cache=tasks_cache,
            token_verifier=token_verifier,
            effect_validation=effect_validation,
            ready=ready,
        )
        return self._middleware

    def authorizer(self) -> IMPORT_typing.Optional[IMPORT_reboot_aio_auth_authorizers.Authorizer | IMPORT_reboot_aio_auth_authorizers.AuthorizerRule]:
        return None

    def token_verifier(self) -> IMPORT_typing.Optional[IMPORT_reboot_aio_auth_token_verifiers.TokenVerifier]:
        return None

    _is_auto_construct = False




    def ref(
        self,
        *,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> Greeter.WeakReference[Greeter.WeakReference._WriterSchedule]:
        context = IMPORT_reboot_aio_contexts.Context.get()

        if context is None:
            raise RuntimeError(
                'Missing asyncio context variable `context`; '
                'are you using this class without Reboot?'
            )

        return Greeter.WeakReference(
            # TODO(https://github.com/reboot-dev/mono/issues/3226): add support for calling other applications.
            # For now this always stays within the application that creates the context.
            application_id=None,
            state_id=context._state_ref.id,
            schedule_type=Greeter.WeakReference._WriterSchedule,
            # If the user didn't specify a bearer token we may still end up using the app-internal bearer token,
            # but that's decided at the time of the call.
            bearer_token=bearer_token,
            servicer=self,
        )

    class Effects(IMPORT_reboot_aio_state_managers.Effects):
        def __init__(
            self,
            *,
            state: tests.reboot.greeter_pb2.Greeter,
            response: IMPORT_typing.Optional[IMPORT_google_protobuf_message.Message] = None,
            tasks: IMPORT_typing.Optional[list[IMPORT_reboot_aio_tasks.TaskEffect]] = None,
            _colocated_upserts: IMPORT_typing.Optional[list[tuple[str, IMPORT_typing.Optional[bytes]]]] = None,
        ):
            IMPORT_reboot_aio_types.assert_type(state, [tests.reboot.greeter_pb2.Greeter])

            super().__init__(state=state, response=response, tasks=tasks, _colocated_upserts=_colocated_upserts)

    # For 'tests.reboot.GreeterMethods.Create'.
    class CreateEffects(Effects):
        def __init__(
            self,
            *,
            state: tests.reboot.greeter_pb2.Greeter,
            response: tests.reboot.greeter_pb2.CreateResponse,
            tasks: IMPORT_typing.Optional[list[IMPORT_reboot_aio_tasks.TaskEffect]] = None,
            _colocated_upserts: IMPORT_typing.Optional[list[tuple[str, IMPORT_typing.Optional[bytes]]]] = None,
        ):
            IMPORT_reboot_aio_types.assert_type(state, [tests.reboot.greeter_pb2.Greeter])
            IMPORT_reboot_aio_types.assert_type(response, [tests.reboot.greeter_pb2.CreateResponse])

            super().__init__(state=state, response=response, tasks=tasks, _colocated_upserts=_colocated_upserts)



    # For 'tests.reboot.GreeterMethods.SetAdjective'.
    class SetAdjectiveEffects(Effects):
        def __init__(
            self,
            *,
            state: tests.reboot.greeter_pb2.Greeter,
            response: tests.reboot.greeter_pb2.SetAdjectiveResponse,
            tasks: IMPORT_typing.Optional[list[IMPORT_reboot_aio_tasks.TaskEffect]] = None,
            _colocated_upserts: IMPORT_typing.Optional[list[tuple[str, IMPORT_typing.Optional[bytes]]]] = None,
        ):
            IMPORT_reboot_aio_types.assert_type(state, [tests.reboot.greeter_pb2.Greeter])
            IMPORT_reboot_aio_types.assert_type(response, [tests.reboot.greeter_pb2.SetAdjectiveResponse])

            super().__init__(state=state, response=response, tasks=tasks, _colocated_upserts=_colocated_upserts)






    # For 'tests.reboot.GreeterMethods.TestLongRunningWriter'.
    class TestLongRunningWriterEffects(Effects):
        def __init__(
            self,
            *,
            state: tests.reboot.greeter_pb2.Greeter,
            response: google.protobuf.empty_pb2.Empty,
            tasks: IMPORT_typing.Optional[list[IMPORT_reboot_aio_tasks.TaskEffect]] = None,
            _colocated_upserts: IMPORT_typing.Optional[list[tuple[str, IMPORT_typing.Optional[bytes]]]] = None,
        ):
            IMPORT_reboot_aio_types.assert_type(state, [tests.reboot.greeter_pb2.Greeter])
            IMPORT_reboot_aio_types.assert_type(response, [google.protobuf.empty_pb2.Empty])

            super().__init__(state=state, response=response, tasks=tasks, _colocated_upserts=_colocated_upserts)






    # For 'tests.reboot.GreeterMethods.DangerousFields'.
    class DangerousFieldsEffects(Effects):
        def __init__(
            self,
            *,
            state: tests.reboot.greeter_pb2.Greeter,
            response: google.protobuf.empty_pb2.Empty,
            tasks: IMPORT_typing.Optional[list[IMPORT_reboot_aio_tasks.TaskEffect]] = None,
            _colocated_upserts: IMPORT_typing.Optional[list[tuple[str, IMPORT_typing.Optional[bytes]]]] = None,
        ):
            IMPORT_reboot_aio_types.assert_type(state, [tests.reboot.greeter_pb2.Greeter])
            IMPORT_reboot_aio_types.assert_type(response, [google.protobuf.empty_pb2.Empty])

            super().__init__(state=state, response=response, tasks=tasks, _colocated_upserts=_colocated_upserts)


    # For 'tests.reboot.GreeterMethods.StoreRecursiveMessage'.
    class StoreRecursiveMessageEffects(Effects):
        def __init__(
            self,
            *,
            state: tests.reboot.greeter_pb2.Greeter,
            response: tests.reboot.greeter_pb2.StoreRecursiveMessageResponse,
            tasks: IMPORT_typing.Optional[list[IMPORT_reboot_aio_tasks.TaskEffect]] = None,
            _colocated_upserts: IMPORT_typing.Optional[list[tuple[str, IMPORT_typing.Optional[bytes]]]] = None,
        ):
            IMPORT_reboot_aio_types.assert_type(state, [tests.reboot.greeter_pb2.Greeter])
            IMPORT_reboot_aio_types.assert_type(response, [tests.reboot.greeter_pb2.StoreRecursiveMessageResponse])

            super().__init__(state=state, response=response, tasks=tasks, _colocated_upserts=_colocated_upserts)





    InlineWriterCallableResult = IMPORT_typing.TypeVar('InlineWriterCallableResult', covariant=True)

    class InlineWriterCallable(IMPORT_typing.Protocol[InlineWriterCallableResult]):
        async def __call__(
            self,
            state: tests.reboot.greeter_pb2.Greeter
        ) -> GreeterBaseServicer.InlineWriterCallableResult:
            ...

    class WorkflowState:

        def __init__(
            self,
            servicer,
        ):
            self._servicer = servicer

        async def read(
            self, context: IMPORT_reboot_aio_contexts.WorkflowContext
        ) -> Greeter.State:
            """Read the current state within a workflow."""
            return await (
                self.always() if context.within_until()
                else (
                    self.per_iteration() if context.within_loop()
                    else self.per_workflow()
                )
            ).read(context)

        async def write(
            self,
            idempotency_alias: str,
            context: IMPORT_reboot_aio_contexts.WorkflowContext,
            writer: GreeterBaseServicer.InlineWriterCallable[GreeterBaseServicer.InlineWriterCallableResult],
            __options__: IMPORT_reboot_aio_call.Options = IMPORT_reboot_aio_call.Options(),
            *,
            type: IMPORT_reboot_aio_workflows.Type | IMPORT_reboot_aio_workflows._Unset = IMPORT_reboot_aio_workflows._UNSET,
        ) -> GreeterBaseServicer.InlineWriterCallableResult:
            """Perform an "inline write" within a workflow.

            `type=` is optional: when omitted, `writer`'s return
            annotation is used (falling back to `type(None)` if
            there is none).
            """
            type_t, _ = IMPORT_reboot_aio_workflows._resolve_callable_return_type(
                writer, type,
            )
            return await (
                self.per_iteration(idempotency_alias) if context.within_loop()
                else self.per_workflow(idempotency_alias)
            ).write(
                context, writer, __options__, type=type_t
            )

        class _Idempotently:

            def __init__(
                self,
                *,
                servicer: GreeterBaseServicer,
                alias: IMPORT_typing.Optional[str],
                how: IMPORT_reboot_aio_workflows.How,
            ):
                self._servicer = servicer
                self._alias = alias
                self._how = how

            async def read(
                self, context: IMPORT_reboot_aio_contexts.WorkflowContext
            ) -> Greeter.State:
                """Read the current state within a workflow."""
                return await self._read(
                    self._servicer,
                    context.idempotency(
                        key=IMPORT_reboot_aio_idempotency.make_expiring_idempotency_key(),
                        how=IMPORT_reboot_aio_idempotency.ALWAYS,
                    ) if self._how == IMPORT_reboot_aio_workflows.ALWAYS else context.idempotency(
                        alias=self._alias,
                        how=self._how,
                    ),
                    context,
                )

            @staticmethod
            async def _read(
                servicer: GreeterBaseServicer,
                idempotency: IMPORT_reboot_aio_idempotency.Idempotency,
                context: IMPORT_reboot_aio_contexts.WorkflowContext,
            ) -> Greeter.State:
                """Read the current state within a workflow."""
                IMPORT_reboot_aio_types.assert_type(context, [IMPORT_reboot_aio_contexts.WorkflowContext])

                if servicer._middleware is None:
                    raise RuntimeError(
                        'Reboot middleware was not created; '
                        'are you using this class without Reboot?'
                    )

                async def read() -> tests.reboot.greeter_pb2.Greeter:
                    assert servicer._middleware is not None
                    return await servicer._middleware._state_manager.read(
                        context, servicer.__state_type__
                    )

                if idempotency.always:
                    return GreeterFromProto(await read())

                state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter')

                # Use the idempotency manager to make sure that this
                # reader is being called following the rules.
                with context.idempotently(
                    state_type_name=state_type_name,
                    state_ref=context._state_ref,
                    # Not calling a method so `service_name`,
                    # `method`, `request`, etc are irrelevant.
                    service_name=None,
                    method=None,
                    mutation=False,
                    request=None,
                    metadata=None,
                    idempotency=idempotency,
                    # Only need to pass `aborted_type` for mutations.
                    aborted_type=None,
                ) as idempotency_key:
                    assert idempotency_key is not None
                    protobuf_state = await IMPORT_reboot_aio_workflows.at_least_once(
                        (
                            # TODO: for easier debugging include the
                            # original alias (or generated alias in
                            # the case of `.per_iteration()` w/o an
                            # alias) instead of just
                            # `idempotency_key`.
                            f"inline reader of '{ state_type_name }' ({str(idempotency_key)})",
                            # NOTE: we want this to be `PER_WORKFLOW`
                            # because any per iteration concerns
                            # should have already been taken care of
                            # by caller using `.per_iteration()`.
                            IMPORT_reboot_aio_workflows.PER_WORKFLOW
                        ),
                        context,
                        read,
                        type=tests.reboot.greeter_pb2.Greeter,
                    )

                    return GreeterFromProto(protobuf_state)

            async def write(
                self,
                context: IMPORT_reboot_aio_contexts.WorkflowContext,
                writer: GreeterBaseServicer.InlineWriterCallable[GreeterBaseServicer.InlineWriterCallableResult],
                __options__: IMPORT_reboot_aio_call.Options = IMPORT_reboot_aio_call.Options(),
                *,
                type: IMPORT_reboot_aio_workflows.Type | IMPORT_reboot_aio_workflows._Unset = IMPORT_reboot_aio_workflows._UNSET,
                check_type: bool = True,
            ) -> GreeterBaseServicer.InlineWriterCallableResult:
                # `type=` is optional: when omitted, `writer`'s
                # return annotation is used (falling back to
                # `type(None)` when there is none).
                type_t, _ = IMPORT_reboot_aio_workflows._resolve_callable_return_type(
                    writer, type,
                )
                return await self._write(
                    context,
                    writer,
                    __options__,
                    type_result=type_t,
                    check_type=check_type,
                )

            async def _write(
                self,
                context: IMPORT_reboot_aio_contexts.WorkflowContext,
                writer: GreeterBaseServicer.InlineWriterCallable[GreeterBaseServicer.InlineWriterCallableResult],
                __options__: IMPORT_reboot_aio_call.Options = IMPORT_reboot_aio_call.Options(),
                *,
                type_result: IMPORT_reboot_aio_workflows.TypeT,
                check_type: bool,
            ) -> GreeterBaseServicer.InlineWriterCallableResult:
                unidempotently = self._how == IMPORT_reboot_aio_workflows.ALWAYS
                idempotency = (
                    context.idempotency(
                        key=IMPORT_reboot_aio_idempotency.make_expiring_idempotency_key(),
                        how=IMPORT_reboot_aio_idempotency.ALWAYS,
                    ) if unidempotently else context.idempotency(
                        alias=self._alias,
                        how=self._how,
                    )
                )

                return await self._write_validating_effects(
                    self._servicer,
                    idempotency,
                    context,
                    writer,
                    __options__,
                    type_result=type_result,
                    check_type=check_type,
                    unidempotently=unidempotently,
                    checkpoint=context.checkpoint(),
                )

            @staticmethod
            @IMPORT_reboot_aio_internals_middleware.maybe_run_function_twice_to_validate_effects
            async def _write_validating_effects(
                validating_effects: bool,
                servicer: GreeterBaseServicer,
                idempotency: IMPORT_reboot_aio_idempotency.Idempotency,
                context: IMPORT_reboot_aio_contexts.WorkflowContext,
                writer: GreeterBaseServicer.InlineWriterCallable[GreeterBaseServicer.InlineWriterCallableResult],
                __options__: IMPORT_reboot_aio_call.Options = IMPORT_reboot_aio_call.Options(),
                *,
                type_result: IMPORT_reboot_aio_workflows.TypeT,
                check_type: bool,
                unidempotently: bool,
                checkpoint: IMPORT_reboot_aio_idempotency.Checkpoint,
            ) -> GreeterBaseServicer.InlineWriterCallableResult:
                IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])

                IMPORT_reboot_aio_types.assert_type(context, [IMPORT_reboot_aio_contexts.WorkflowContext])

                if servicer._middleware is None:
                    raise RuntimeError(
                        'Reboot middleware was not created; '
                        'are you using this class without Reboot?'
                    )

                metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None

                if __options__ is not None:
                    if __options__.metadata is not None:
                        metadata = __options__.metadata

                if metadata is None:
                    metadata = ()

                headers = IMPORT_reboot_aio_headers.Headers(
                    application_id=context.application_id,
                    state_ref=context._state_ref,
                    caller_id=IMPORT_reboot_aio_caller_id.CallerID(
                        application_id=context.application_id,
                    ),
                    workflow_id=context.workflow_id,
                    # Only set `workflow_iteration` for
                    # `per_iteration` calls so that the
                    # mutation is stored/recovered with
                    # iteration scoping.
                    workflow_iteration=(
                        context.workflow_iteration
                        if idempotency.per_iteration else None
                    ),
                )

                metadata += headers.to_grpc_metadata()

                idempotency_key: IMPORT_typing.Optional[IMPORT_uuid.UUID]
                with context.idempotently(
                    state_type_name=IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                    state_ref=context._state_ref,
                    service_name=None,  # Indicates an inline writer.
                    method=None,  # Indicates an inline writer.
                    mutation=True,
                    request=None,  # Indicates an inline writer.
                    metadata=metadata,
                    idempotency=idempotency,
                    aborted_type=None,  # Indicates an inline writer.
                ) as idempotency_key:

                    if any(t[0] == IMPORT_reboot_aio_headers.IDEMPOTENCY_KEY_HEADER for t in metadata):
                        raise ValueError(
                            f"Do not set '{IMPORT_reboot_aio_headers.IDEMPOTENCY_KEY_HEADER}' metadata yourself"
                        )

                    if idempotency_key is not None:
                        metadata += (
                            (IMPORT_reboot_aio_headers.IDEMPOTENCY_KEY_HEADER, str(idempotency_key)),
                        )

                    with servicer._middleware.use_context(
                        headers=IMPORT_reboot_aio_headers.Headers.from_grpc_metadata(metadata),
                        state_type_name = IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                        method='inline writer',
                        context_type=IMPORT_reboot_aio_contexts.WriterContext,
                    ) as writer_context:
                        # Check if we already have performed this mutation!
                        #
                        # We do this _before_ calling 'transactionally()' because
                        # if this call is for a transaction method _and_ we've
                        # already performed the transaction then we don't want to
                        # become a transaction participant (again) we just want to
                        # return the transaction's response.
                        idempotent_mutation = (
                            await servicer._middleware._state_manager.check_for_idempotent_mutation(
                                writer_context
                            )
                        )

                        if idempotent_mutation is not None:
                            assert len(idempotent_mutation.response) != 0
                            response = IMPORT_google_protobuf_wrappers_pb2.BytesValue()
                            response.ParseFromString(idempotent_mutation.response)
                            result: GreeterBaseServicer.InlineWriterCallableResult = IMPORT_pickle.loads(response.value)

                            if check_type and not isinstance(result, IMPORT_reboot_aio_workflows._isinstance_type(type_result)):
                                raise TypeError(
                                    f"Stored result of type '{type(result).__name__}' from 'writer' "
                                    f"is not of expected type '{IMPORT_reboot_aio_workflows._format_type(type_result)}'; have you changed "
                                    "the 'type' that you expect after having stored a result?"
                                )

                            return result

                        async with servicer._middleware._state_manager.transactionally(
                            writer_context,
                            servicer._middleware.tasks_dispatcher,
                            aborted_type=None,
                        ) as transaction:
                            async with servicer._middleware._state_manager.writer(
                                writer_context,
                                servicer.__state_type__,
                                servicer._middleware.tasks_dispatcher,
                                # TODO: Decide if we want to do any kind of authorization for inline
                                # writers otherwise passing `None` here is fine.
                                authorize=None,
                                transaction=transaction,
                            ) as (protobuf_state, state_manager_writer):
                                # Serialize the state so we can see if it changed.
                                serialized_state = protobuf_state.SerializeToString(
                                    deterministic=True,
                                )

                                typed_state = GreeterFromProto(protobuf_state)

                                result = await writer(state=typed_state)

                                GreeterToProto(typed_state, protobuf_state)

                                if check_type and not isinstance(result, IMPORT_reboot_aio_workflows._isinstance_type(type_result)):
                                    raise TypeError(
                                        f"Result of type '{type(result).__name__}' from 'writer' is "
                                        f"not of expected type '{IMPORT_reboot_aio_workflows._format_type(type_result)}'; "
                                        "did you specify an incorrect 'type'?"
                                    )

                                task: IMPORT_typing.Optional[IMPORT_reboot_aio_tasks.TaskEffect] = context.task

                                assert task is not None, (
                                    "Should always have a task when running a `workflow`"
                                )

                                method_name = f"Greeter.{task.method_name} inline writer"

                                if idempotency.alias is not None:
                                    method_name += " with idempotency alias '" + idempotency.alias + "'"
                                elif idempotency.key is not None:
                                    method_name += " with idempotency key=" + str(idempotency.key)

                                servicer._middleware.maybe_raise_effect_validation_retry(
                                    logger=logger,
                                    idempotency_manager=context,
                                    method_name=method_name,
                                    validating_effects=validating_effects,
                                    context=context,
                                    checkpoint=checkpoint,
                                )

                                # We don't pass the context to the
                                # writer, so we don't expect there to
                                # be any scheduled tasks!
                                assert len(context._tasks) == 0

                                effects = IMPORT_reboot_aio_state_managers.Effects(
                                    state=(
                                        # Pass `None` if the state hasn't changed!
                                        protobuf_state if serialized_state != protobuf_state.SerializeToString(
                                            deterministic=True,
                                        )
                                        else None
                                    ),
                                    response=IMPORT_google_protobuf_wrappers_pb2.BytesValue(
                                        value=IMPORT_pickle.dumps(result)
                                    ),
                                )

                                await state_manager_writer.complete(effects)

                                return result

        def per_workflow(self, alias: IMPORT_typing.Optional[str] = None):
            return GreeterBaseServicer.WorkflowState._Idempotently(
                servicer=self._servicer,
                alias=alias,
                how=IMPORT_reboot_aio_workflows.PER_WORKFLOW,
            )

        def per_iteration(self, alias: IMPORT_typing.Optional[str] = None):
            return GreeterBaseServicer.WorkflowState._Idempotently(
                servicer=self._servicer,
                alias=alias,
                how=IMPORT_reboot_aio_workflows.PER_ITERATION,
            )

        class _Always:
            """Helper class for providing better types for `write` that don't
            require passing `type` or `check_type`."""

            def __init__(
                self,
                *,
                servicer: GreeterBaseServicer,
            ):
                self._servicer = servicer

            async def read(
                self, context: IMPORT_reboot_aio_contexts.WorkflowContext
            ) -> Greeter.State:
                return await GreeterBaseServicer.WorkflowState._Idempotently(
                    servicer=self._servicer,
                    alias=None,
                    how=IMPORT_reboot_aio_workflows.ALWAYS,
                ).read(context)

            async def write(
                self,
                context: IMPORT_reboot_aio_contexts.WorkflowContext,
                writer: GreeterBaseServicer.InlineWriterCallable[GreeterBaseServicer.InlineWriterCallableResult],
                __options__: IMPORT_reboot_aio_call.Options = IMPORT_reboot_aio_call.Options(),
            ) -> GreeterBaseServicer.InlineWriterCallableResult:
                return await GreeterBaseServicer.WorkflowState._Idempotently(
                    servicer=self._servicer,
                    alias=None,
                    how=IMPORT_reboot_aio_workflows.ALWAYS,
                )._write(
                    context,
                    writer,
                    __options__,
                    type_result=type(None),
                    check_type=False,
                )

        def always(self):
            return GreeterBaseServicer.WorkflowState._Always(
                servicer=self._servicer,
            )

    # For 'tests.reboot.GreeterMethods.Create'.
    @IMPORT_abc_abstractmethod
    async def _Create(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: Greeter.State,
        request: tests.reboot.greeter_pb2.CreateRequest,
    ) -> tests.reboot.greeter_pb2.CreateResponse:
        raise NotImplementedError

    # For 'tests.reboot.GreeterMethods.Greet'.
    @IMPORT_abc_abstractmethod
    async def _Greet(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: Greeter.State,
        request: tests.reboot.greeter_pb2.GreetRequest,
    ) -> tests.reboot.greeter_pb2.GreetResponse:
        raise NotImplementedError

    # For 'tests.reboot.GreeterMethods.SetAdjective'.
    @IMPORT_abc_abstractmethod
    async def _SetAdjective(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: Greeter.State,
        request: tests.reboot.greeter_pb2.SetAdjectiveRequest,
    ) -> tests.reboot.greeter_pb2.SetAdjectiveResponse:
        raise NotImplementedError

    # For 'tests.reboot.GreeterMethods.TransactionSetAdjective'.
    @IMPORT_abc_abstractmethod
    async def _TransactionSetAdjective(
        self,
        context: IMPORT_reboot_aio_contexts.TransactionContext,
        state: Greeter.State,
        request: tests.reboot.greeter_pb2.SetAdjectiveRequest,
    ) -> tests.reboot.greeter_pb2.SetAdjectiveResponse:
        raise NotImplementedError

    # For 'tests.reboot.GreeterMethods.TryToConstructContext'.
    @IMPORT_abc_abstractmethod
    async def _TryToConstructContext(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: Greeter.State,
        request: google.protobuf.empty_pb2.Empty,
    ) -> google.protobuf.empty_pb2.Empty:
        raise NotImplementedError

    # For 'tests.reboot.GreeterMethods.TryToConstructExternalContext'.
    @IMPORT_abc_abstractmethod
    async def _TryToConstructExternalContext(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: Greeter.State,
        request: google.protobuf.empty_pb2.Empty,
    ) -> google.protobuf.empty_pb2.Empty:
        raise NotImplementedError

    # For 'tests.reboot.GreeterMethods.TestLongRunningFetch'.
    @IMPORT_abc_abstractmethod
    async def _TestLongRunningFetch(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: Greeter.State,
        request: tests.reboot.greeter_pb2.TestLongRunningFetchRequest,
    ) -> google.protobuf.empty_pb2.Empty:
        raise NotImplementedError

    # For 'tests.reboot.GreeterMethods.TestLongRunningWriter'.
    @IMPORT_abc_abstractmethod
    async def _TestLongRunningWriter(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: Greeter.State,
        request: google.protobuf.empty_pb2.Empty,
    ) -> google.protobuf.empty_pb2.Empty:
        raise NotImplementedError

    # For 'tests.reboot.GreeterMethods.GetWholeState'.
    @IMPORT_abc_abstractmethod
    async def _GetWholeState(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: Greeter.State,
        request: tests.reboot.greeter_pb2.GetWholeStateRequest,
    ) -> tests.reboot.greeter_pb2.Greeter:
        raise NotImplementedError

    # For 'tests.reboot.GreeterMethods.FailWithException'.
    @IMPORT_abc_abstractmethod
    async def _FailWithException(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: Greeter.State,
        request: google.protobuf.empty_pb2.Empty,
    ) -> google.protobuf.empty_pb2.Empty:
        raise NotImplementedError

    # For 'tests.reboot.GreeterMethods.FailWithAborted'.
    @IMPORT_abc_abstractmethod
    async def _FailWithAborted(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: Greeter.State,
        request: google.protobuf.empty_pb2.Empty,
    ) -> google.protobuf.empty_pb2.Empty:
        raise NotImplementedError

    # For 'tests.reboot.GreeterMethods.Workflow'.
    @IMPORT_abc_abstractmethod
    async def _Workflow(
        self,
        context: IMPORT_reboot_aio_contexts.WorkflowContext,
        request: google.protobuf.empty_pb2.Empty,
    ) -> tests.reboot.greeter_pb2.WorkflowResponse:
        raise NotImplementedError

    # For 'tests.reboot.GreeterMethods.DangerousFields'.
    @IMPORT_abc_abstractmethod
    async def _DangerousFields(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: Greeter.State,
        request: tests.reboot.greeter_pb2.DangerousFieldsRequest,
    ) -> google.protobuf.empty_pb2.Empty:
        raise NotImplementedError

    # For 'tests.reboot.GreeterMethods.StoreRecursiveMessage'.
    @IMPORT_abc_abstractmethod
    async def _StoreRecursiveMessage(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: Greeter.State,
        request: tests.reboot.greeter_pb2.StoreRecursiveMessageRequest,
    ) -> tests.reboot.greeter_pb2.StoreRecursiveMessageResponse:
        raise NotImplementedError

    # For 'tests.reboot.GreeterMethods.ReadRecursiveMessage'.
    @IMPORT_abc_abstractmethod
    async def _ReadRecursiveMessage(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: Greeter.State,
        request: tests.reboot.greeter_pb2.ReadRecursiveMessageRequest,
    ) -> tests.reboot.greeter_pb2.ReadRecursiveMessageResponse:
        raise NotImplementedError

    # For 'tests.reboot.GreeterMethods.ConstructAndStoreRecursiveMessage'.
    @IMPORT_abc_abstractmethod
    async def _ConstructAndStoreRecursiveMessage(
        self,
        context: IMPORT_reboot_aio_contexts.TransactionContext,
        state: Greeter.State,
        request: tests.reboot.greeter_pb2.ConstructAndStoreRecursiveMessageRequest,
    ) -> tests.reboot.greeter_pb2.ConstructAndStoreRecursiveMessageResponse:
        raise NotImplementedError



class GreeterSingletonServicer(GreeterBaseServicer):

    @property
    def state(self):
        return GreeterBaseServicer.WorkflowState(
            servicer=self
        )

    # For 'tests.reboot.GreeterMethods.Create'.
    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that new code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    async def Create(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: Greeter.State,
        request: Greeter.CreateRequest,
    ) -> Greeter.CreateResponse:
        # During the migration from 'PascalCase' to 'snake_case' method
        # naming in Python servicers, we call the 'snake_case' version
        # by default, so new names will do the correct thing making the
        # code to be backwards compatible for some time and if a servicer
        # overrides the 'PascalCase' version - it will override that
        # method and will just work.
        return await self.create(
            context,
            state,
            request,
        )

    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that existing code that
    # doesn't implement it continues to work.
    async def create(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: tests.reboot.greeter_pb2.Greeter,
        request: Greeter.CreateRequest,
    ) -> Greeter.CreateResponse:
        raise NotImplementedError

    # For 'tests.reboot.GreeterMethods.Greet'.
    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that new code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    async def Greet(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: Greeter.State,
        request: Greeter.GreetRequest,
    ) -> Greeter.GreetResponse:
        # During the migration from 'PascalCase' to 'snake_case' method
        # naming in Python servicers, we call the 'snake_case' version
        # by default, so new names will do the correct thing making the
        # code to be backwards compatible for some time and if a servicer
        # overrides the 'PascalCase' version - it will override that
        # method and will just work.
        return await self.greet(
            context,
            state,
            request,
        )

    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that existing code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    async def greet(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: Greeter.State,
        request: Greeter.GreetRequest,
    ) -> Greeter.GreetResponse:
        raise NotImplementedError

    # For 'tests.reboot.GreeterMethods.SetAdjective'.
    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that new code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    async def SetAdjective(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: Greeter.State,
        request: Greeter.SetAdjectiveRequest,
    ) -> Greeter.SetAdjectiveResponse:
        # During the migration from 'PascalCase' to 'snake_case' method
        # naming in Python servicers, we call the 'snake_case' version
        # by default, so new names will do the correct thing making the
        # code to be backwards compatible for some time and if a servicer
        # overrides the 'PascalCase' version - it will override that
        # method and will just work.
        return await self.set_adjective(
            context,
            state,
            request,
        )

    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that existing code that
    # doesn't implement it continues to work.
    async def set_adjective(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: tests.reboot.greeter_pb2.Greeter,
        request: Greeter.SetAdjectiveRequest,
    ) -> Greeter.SetAdjectiveResponse:
        raise NotImplementedError

    # For 'tests.reboot.GreeterMethods.TransactionSetAdjective'.
    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that new code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    async def TransactionSetAdjective(
        self,
        context: IMPORT_reboot_aio_contexts.TransactionContext,
        state: Greeter.State,
        request: Greeter.TransactionSetAdjectiveRequest,
    ) -> Greeter.TransactionSetAdjectiveResponse:
        # During the migration from 'PascalCase' to 'snake_case' method
        # naming in Python servicers, we call the 'snake_case' version
        # by default, so new names will do the correct thing making the
        # code to be backwards compatible for some time and if a servicer
        # overrides the 'PascalCase' version - it will override that
        # method and will just work.
        return await self.transaction_set_adjective(
            context,
            state,
            request,
        )

    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that existing code that
    # doesn't implement it continues to work.
    async def transaction_set_adjective(
        self,
        context: IMPORT_reboot_aio_contexts.TransactionContext,
        state: Greeter.State,
        request: Greeter.TransactionSetAdjectiveRequest,
    ) -> Greeter.TransactionSetAdjectiveResponse:
        raise NotImplementedError

    # For 'tests.reboot.GreeterMethods.TryToConstructContext'.
    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that new code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    async def TryToConstructContext(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: Greeter.State,
        request: Greeter.TryToConstructContextRequest,
    ) -> Greeter.TryToConstructContextResponse:
        # During the migration from 'PascalCase' to 'snake_case' method
        # naming in Python servicers, we call the 'snake_case' version
        # by default, so new names will do the correct thing making the
        # code to be backwards compatible for some time and if a servicer
        # overrides the 'PascalCase' version - it will override that
        # method and will just work.
        return await self.try_to_construct_context(
            context,
            state,
            request,
        )

    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that existing code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    async def try_to_construct_context(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: Greeter.State,
        request: Greeter.TryToConstructContextRequest,
    ) -> Greeter.TryToConstructContextResponse:
        raise NotImplementedError

    # For 'tests.reboot.GreeterMethods.TryToConstructExternalContext'.
    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that new code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    async def TryToConstructExternalContext(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: Greeter.State,
        request: Greeter.TryToConstructExternalContextRequest,
    ) -> Greeter.TryToConstructExternalContextResponse:
        # During the migration from 'PascalCase' to 'snake_case' method
        # naming in Python servicers, we call the 'snake_case' version
        # by default, so new names will do the correct thing making the
        # code to be backwards compatible for some time and if a servicer
        # overrides the 'PascalCase' version - it will override that
        # method and will just work.
        return await self.try_to_construct_external_context(
            context,
            state,
            request,
        )

    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that existing code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    async def try_to_construct_external_context(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: Greeter.State,
        request: Greeter.TryToConstructExternalContextRequest,
    ) -> Greeter.TryToConstructExternalContextResponse:
        raise NotImplementedError

    # For 'tests.reboot.GreeterMethods.TestLongRunningFetch'.
    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that new code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    async def TestLongRunningFetch(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: Greeter.State,
        request: Greeter.TestLongRunningFetchRequest,
    ) -> Greeter.TestLongRunningFetchResponse:
        # During the migration from 'PascalCase' to 'snake_case' method
        # naming in Python servicers, we call the 'snake_case' version
        # by default, so new names will do the correct thing making the
        # code to be backwards compatible for some time and if a servicer
        # overrides the 'PascalCase' version - it will override that
        # method and will just work.
        return await self.test_long_running_fetch(
            context,
            state,
            request,
        )

    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that existing code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    async def test_long_running_fetch(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: Greeter.State,
        request: Greeter.TestLongRunningFetchRequest,
    ) -> Greeter.TestLongRunningFetchResponse:
        raise NotImplementedError

    # For 'tests.reboot.GreeterMethods.TestLongRunningWriter'.
    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that new code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    async def TestLongRunningWriter(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: Greeter.State,
        request: Greeter.TestLongRunningWriterRequest,
    ) -> Greeter.TestLongRunningWriterResponse:
        # During the migration from 'PascalCase' to 'snake_case' method
        # naming in Python servicers, we call the 'snake_case' version
        # by default, so new names will do the correct thing making the
        # code to be backwards compatible for some time and if a servicer
        # overrides the 'PascalCase' version - it will override that
        # method and will just work.
        return await self.test_long_running_writer(
            context,
            state,
            request,
        )

    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that existing code that
    # doesn't implement it continues to work.
    async def test_long_running_writer(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: tests.reboot.greeter_pb2.Greeter,
        request: Greeter.TestLongRunningWriterRequest,
    ) -> Greeter.TestLongRunningWriterResponse:
        raise NotImplementedError

    # For 'tests.reboot.GreeterMethods.GetWholeState'.
    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that new code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    async def GetWholeState(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: Greeter.State,
        request: Greeter.GetWholeStateRequest,
    ) -> Greeter.GetWholeStateResponse:
        # During the migration from 'PascalCase' to 'snake_case' method
        # naming in Python servicers, we call the 'snake_case' version
        # by default, so new names will do the correct thing making the
        # code to be backwards compatible for some time and if a servicer
        # overrides the 'PascalCase' version - it will override that
        # method and will just work.
        return await self.get_whole_state(
            context,
            state,
            request,
        )

    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that existing code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    async def get_whole_state(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: Greeter.State,
        request: Greeter.GetWholeStateRequest,
    ) -> Greeter.GetWholeStateResponse:
        raise NotImplementedError

    # For 'tests.reboot.GreeterMethods.FailWithException'.
    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that new code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    async def FailWithException(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: Greeter.State,
        request: Greeter.FailWithExceptionRequest,
    ) -> Greeter.FailWithExceptionResponse:
        # During the migration from 'PascalCase' to 'snake_case' method
        # naming in Python servicers, we call the 'snake_case' version
        # by default, so new names will do the correct thing making the
        # code to be backwards compatible for some time and if a servicer
        # overrides the 'PascalCase' version - it will override that
        # method and will just work.
        return await self.fail_with_exception(
            context,
            state,
            request,
        )

    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that existing code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    async def fail_with_exception(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: Greeter.State,
        request: Greeter.FailWithExceptionRequest,
    ) -> Greeter.FailWithExceptionResponse:
        raise NotImplementedError

    # For 'tests.reboot.GreeterMethods.FailWithAborted'.
    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that new code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    async def FailWithAborted(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: Greeter.State,
        request: Greeter.FailWithAbortedRequest,
    ) -> Greeter.FailWithAbortedResponse:
        # During the migration from 'PascalCase' to 'snake_case' method
        # naming in Python servicers, we call the 'snake_case' version
        # by default, so new names will do the correct thing making the
        # code to be backwards compatible for some time and if a servicer
        # overrides the 'PascalCase' version - it will override that
        # method and will just work.
        return await self.fail_with_aborted(
            context,
            state,
            request,
        )

    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that existing code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    async def fail_with_aborted(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: Greeter.State,
        request: Greeter.FailWithAbortedRequest,
    ) -> Greeter.FailWithAbortedResponse:
        raise NotImplementedError

    # For 'tests.reboot.GreeterMethods.Workflow'.
    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that new code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    @classmethod
    async def Workflow(
        cls,
        context: IMPORT_reboot_aio_contexts.WorkflowContext,
        request: Greeter.WorkflowRequest,
    ) -> Greeter.WorkflowResponse:
        # During the migration from 'PascalCase' to 'snake_case' method
        # naming in Python servicers, we call the 'snake_case' version
        # by default, so new names will do the correct thing making the
        # code to be backwards compatible for some time and if a servicer
        # overrides the 'PascalCase' version - it will override that
        # method and will just work.
        return await cls.workflow(
            context,
            request,
        )

    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that existing code that
    # doesn't implement it continues to work.
    @classmethod
    async def workflow(
        cls,
        context: IMPORT_reboot_aio_contexts.WorkflowContext,
        request: Greeter.WorkflowRequest,
    ) -> Greeter.WorkflowResponse:
        raise NotImplementedError

    # For 'tests.reboot.GreeterMethods.DangerousFields'.
    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that new code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    async def DangerousFields(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: Greeter.State,
        request: Greeter.DangerousFieldsRequest,
    ) -> Greeter.DangerousFieldsResponse:
        # During the migration from 'PascalCase' to 'snake_case' method
        # naming in Python servicers, we call the 'snake_case' version
        # by default, so new names will do the correct thing making the
        # code to be backwards compatible for some time and if a servicer
        # overrides the 'PascalCase' version - it will override that
        # method and will just work.
        return await self.dangerous_fields(
            context,
            state,
            request,
        )

    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that existing code that
    # doesn't implement it continues to work.
    async def dangerous_fields(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: tests.reboot.greeter_pb2.Greeter,
        request: Greeter.DangerousFieldsRequest,
    ) -> Greeter.DangerousFieldsResponse:
        raise NotImplementedError

    # For 'tests.reboot.GreeterMethods.StoreRecursiveMessage'.
    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that new code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    async def StoreRecursiveMessage(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: Greeter.State,
        request: Greeter.StoreRecursiveMessageRequest,
    ) -> Greeter.StoreRecursiveMessageResponse:
        # During the migration from 'PascalCase' to 'snake_case' method
        # naming in Python servicers, we call the 'snake_case' version
        # by default, so new names will do the correct thing making the
        # code to be backwards compatible for some time and if a servicer
        # overrides the 'PascalCase' version - it will override that
        # method and will just work.
        return await self.store_recursive_message(
            context,
            state,
            request,
        )

    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that existing code that
    # doesn't implement it continues to work.
    async def store_recursive_message(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: tests.reboot.greeter_pb2.Greeter,
        request: Greeter.StoreRecursiveMessageRequest,
    ) -> Greeter.StoreRecursiveMessageResponse:
        raise NotImplementedError

    # For 'tests.reboot.GreeterMethods.ReadRecursiveMessage'.
    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that new code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    async def ReadRecursiveMessage(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: Greeter.State,
        request: Greeter.ReadRecursiveMessageRequest,
    ) -> Greeter.ReadRecursiveMessageResponse:
        # During the migration from 'PascalCase' to 'snake_case' method
        # naming in Python servicers, we call the 'snake_case' version
        # by default, so new names will do the correct thing making the
        # code to be backwards compatible for some time and if a servicer
        # overrides the 'PascalCase' version - it will override that
        # method and will just work.
        return await self.read_recursive_message(
            context,
            state,
            request,
        )

    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that existing code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    async def read_recursive_message(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: Greeter.State,
        request: Greeter.ReadRecursiveMessageRequest,
    ) -> Greeter.ReadRecursiveMessageResponse:
        raise NotImplementedError

    # For 'tests.reboot.GreeterMethods.ConstructAndStoreRecursiveMessage'.
    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that new code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    async def ConstructAndStoreRecursiveMessage(
        self,
        context: IMPORT_reboot_aio_contexts.TransactionContext,
        state: Greeter.State,
        request: Greeter.ConstructAndStoreRecursiveMessageRequest,
    ) -> Greeter.ConstructAndStoreRecursiveMessageResponse:
        # During the migration from 'PascalCase' to 'snake_case' method
        # naming in Python servicers, we call the 'snake_case' version
        # by default, so new names will do the correct thing making the
        # code to be backwards compatible for some time and if a servicer
        # overrides the 'PascalCase' version - it will override that
        # method and will just work.
        return await self.construct_and_store_recursive_message(
            context,
            state,
            request,
        )

    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that existing code that
    # doesn't implement it continues to work.
    async def construct_and_store_recursive_message(
        self,
        context: IMPORT_reboot_aio_contexts.TransactionContext,
        state: Greeter.State,
        request: Greeter.ConstructAndStoreRecursiveMessageRequest,
    ) -> Greeter.ConstructAndStoreRecursiveMessageResponse:
        raise NotImplementedError


    # For 'tests.reboot.GreeterMethods.Create'.
    async def _Create(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: Greeter.State,
        request: tests.reboot.greeter_pb2.CreateRequest,
    ) -> tests.reboot.greeter_pb2.CreateResponse:
        # Wrap the call to the developer's method in a `span` so that it
        # is traced using its fully-qualified Python name.
        with IMPORT_reboot_aio_tracing.span(
                state_name=f"tests.reboot.Greeter('{context.state_id}')",
                span_name=f"{IMPORT_reboot_aio_tracing.qualified_type_name(self)}.Create()",
                level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                python_specific=True,
        ):
            typed_request = GreeterCreateRequestFromProto(request)
            return GreeterCreateResponseToProto(
                await self.Create(
                    context,
                    state,
                    typed_request
                )
            )


    # For 'tests.reboot.GreeterMethods.Greet'.
    async def _Greet(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: Greeter.State,
        request: tests.reboot.greeter_pb2.GreetRequest,
    ) -> tests.reboot.greeter_pb2.GreetResponse:
        # Wrap the call to the developer's method in a `span` so that it
        # is traced using its fully-qualified Python name.
        with IMPORT_reboot_aio_tracing.span(
                state_name=f"tests.reboot.Greeter('{context.state_id}')",
                span_name=f"{IMPORT_reboot_aio_tracing.qualified_type_name(self)}.Greet()",
                level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                python_specific=True,
        ):
            typed_request = GreeterGreetRequestFromProto(request)
            response = (
                self.Greet(
                    context,
                    state,
                    typed_request,
                )
            )
            return GreeterGreetResponseToProto(await response)

    # For 'tests.reboot.GreeterMethods.SetAdjective'.
    async def _SetAdjective(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: Greeter.State,
        request: tests.reboot.greeter_pb2.SetAdjectiveRequest,
    ) -> tests.reboot.greeter_pb2.SetAdjectiveResponse:
        # Wrap the call to the developer's method in a `span` so that it
        # is traced using its fully-qualified Python name.
        with IMPORT_reboot_aio_tracing.span(
                state_name=f"tests.reboot.Greeter('{context.state_id}')",
                span_name=f"{IMPORT_reboot_aio_tracing.qualified_type_name(self)}.SetAdjective()",
                level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                python_specific=True,
        ):
            typed_request = GreeterSetAdjectiveRequestFromProto(request)
            return GreeterSetAdjectiveResponseToProto(
                await self.SetAdjective(
                    context,
                    state,
                    typed_request
                )
            )


    # For 'tests.reboot.GreeterMethods.TransactionSetAdjective'.
    async def _TransactionSetAdjective(
        self,
        context: IMPORT_reboot_aio_contexts.TransactionContext,
        state: Greeter.State,
        request: tests.reboot.greeter_pb2.SetAdjectiveRequest,
    ) -> tests.reboot.greeter_pb2.SetAdjectiveResponse:
        # Wrap the call to the developer's method in a `span` so that it
        # is traced using its fully-qualified Python name.
        with IMPORT_reboot_aio_tracing.span(
                state_name=f"tests.reboot.Greeter('{context.state_id}')",
                span_name=f"{IMPORT_reboot_aio_tracing.qualified_type_name(self)}.TransactionSetAdjective()",
                level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                python_specific=True,
        ):
            typed_request = GreeterTransactionSetAdjectiveRequestFromProto(request)
            return GreeterTransactionSetAdjectiveResponseToProto(
                await self.TransactionSetAdjective(
                    context,
                    state,
                    typed_request
                )
            )


    # For 'tests.reboot.GreeterMethods.TryToConstructContext'.
    async def _TryToConstructContext(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: Greeter.State,
        request: google.protobuf.empty_pb2.Empty,
    ) -> google.protobuf.empty_pb2.Empty:
        # Wrap the call to the developer's method in a `span` so that it
        # is traced using its fully-qualified Python name.
        with IMPORT_reboot_aio_tracing.span(
                state_name=f"tests.reboot.Greeter('{context.state_id}')",
                span_name=f"{IMPORT_reboot_aio_tracing.qualified_type_name(self)}.TryToConstructContext()",
                level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                python_specific=True,
        ):
            typed_request = GreeterTryToConstructContextRequestFromProto(request)
            response = (
                self.TryToConstructContext(
                    context,
                    state,
                    typed_request,
                )
            )
            return GreeterTryToConstructContextResponseToProto(await response)

    # For 'tests.reboot.GreeterMethods.TryToConstructExternalContext'.
    async def _TryToConstructExternalContext(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: Greeter.State,
        request: google.protobuf.empty_pb2.Empty,
    ) -> google.protobuf.empty_pb2.Empty:
        # Wrap the call to the developer's method in a `span` so that it
        # is traced using its fully-qualified Python name.
        with IMPORT_reboot_aio_tracing.span(
                state_name=f"tests.reboot.Greeter('{context.state_id}')",
                span_name=f"{IMPORT_reboot_aio_tracing.qualified_type_name(self)}.TryToConstructExternalContext()",
                level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                python_specific=True,
        ):
            typed_request = GreeterTryToConstructExternalContextRequestFromProto(request)
            response = (
                self.TryToConstructExternalContext(
                    context,
                    state,
                    typed_request,
                )
            )
            return GreeterTryToConstructExternalContextResponseToProto(await response)

    # For 'tests.reboot.GreeterMethods.TestLongRunningFetch'.
    async def _TestLongRunningFetch(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: Greeter.State,
        request: tests.reboot.greeter_pb2.TestLongRunningFetchRequest,
    ) -> google.protobuf.empty_pb2.Empty:
        # Wrap the call to the developer's method in a `span` so that it
        # is traced using its fully-qualified Python name.
        with IMPORT_reboot_aio_tracing.span(
                state_name=f"tests.reboot.Greeter('{context.state_id}')",
                span_name=f"{IMPORT_reboot_aio_tracing.qualified_type_name(self)}.TestLongRunningFetch()",
                level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                python_specific=True,
        ):
            typed_request = GreeterTestLongRunningFetchRequestFromProto(request)
            response = (
                self.TestLongRunningFetch(
                    context,
                    state,
                    typed_request,
                )
            )
            return GreeterTestLongRunningFetchResponseToProto(await response)

    # For 'tests.reboot.GreeterMethods.TestLongRunningWriter'.
    async def _TestLongRunningWriter(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: Greeter.State,
        request: google.protobuf.empty_pb2.Empty,
    ) -> google.protobuf.empty_pb2.Empty:
        # Wrap the call to the developer's method in a `span` so that it
        # is traced using its fully-qualified Python name.
        with IMPORT_reboot_aio_tracing.span(
                state_name=f"tests.reboot.Greeter('{context.state_id}')",
                span_name=f"{IMPORT_reboot_aio_tracing.qualified_type_name(self)}.TestLongRunningWriter()",
                level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                python_specific=True,
        ):
            typed_request = GreeterTestLongRunningWriterRequestFromProto(request)
            return GreeterTestLongRunningWriterResponseToProto(
                await self.TestLongRunningWriter(
                    context,
                    state,
                    typed_request
                )
            )


    # For 'tests.reboot.GreeterMethods.GetWholeState'.
    async def _GetWholeState(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: Greeter.State,
        request: tests.reboot.greeter_pb2.GetWholeStateRequest,
    ) -> tests.reboot.greeter_pb2.Greeter:
        # Wrap the call to the developer's method in a `span` so that it
        # is traced using its fully-qualified Python name.
        with IMPORT_reboot_aio_tracing.span(
                state_name=f"tests.reboot.Greeter('{context.state_id}')",
                span_name=f"{IMPORT_reboot_aio_tracing.qualified_type_name(self)}.GetWholeState()",
                level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                python_specific=True,
        ):
            typed_request = GreeterGetWholeStateRequestFromProto(request)
            response = (
                self.GetWholeState(
                    context,
                    state,
                    typed_request,
                )
            )
            return GreeterGetWholeStateResponseToProto(await response)

    # For 'tests.reboot.GreeterMethods.FailWithException'.
    async def _FailWithException(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: Greeter.State,
        request: google.protobuf.empty_pb2.Empty,
    ) -> google.protobuf.empty_pb2.Empty:
        # Wrap the call to the developer's method in a `span` so that it
        # is traced using its fully-qualified Python name.
        with IMPORT_reboot_aio_tracing.span(
                state_name=f"tests.reboot.Greeter('{context.state_id}')",
                span_name=f"{IMPORT_reboot_aio_tracing.qualified_type_name(self)}.FailWithException()",
                level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                python_specific=True,
        ):
            typed_request = GreeterFailWithExceptionRequestFromProto(request)
            response = (
                self.FailWithException(
                    context,
                    state,
                    typed_request,
                )
            )
            return GreeterFailWithExceptionResponseToProto(await response)

    # For 'tests.reboot.GreeterMethods.FailWithAborted'.
    async def _FailWithAborted(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: Greeter.State,
        request: google.protobuf.empty_pb2.Empty,
    ) -> google.protobuf.empty_pb2.Empty:
        # Wrap the call to the developer's method in a `span` so that it
        # is traced using its fully-qualified Python name.
        with IMPORT_reboot_aio_tracing.span(
                state_name=f"tests.reboot.Greeter('{context.state_id}')",
                span_name=f"{IMPORT_reboot_aio_tracing.qualified_type_name(self)}.FailWithAborted()",
                level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                python_specific=True,
        ):
            typed_request = GreeterFailWithAbortedRequestFromProto(request)
            response = (
                self.FailWithAborted(
                    context,
                    state,
                    typed_request,
                )
            )
            return GreeterFailWithAbortedResponseToProto(await response)

    # For 'tests.reboot.GreeterMethods.Workflow'.
    async def _Workflow(
        self,
        context: IMPORT_reboot_aio_contexts.WorkflowContext,
        request: google.protobuf.empty_pb2.Empty,
    ) -> tests.reboot.greeter_pb2.WorkflowResponse:
        # Wrap the call to the developer's method in a `span` so that it
        # is traced using its fully-qualified Python name.
        with IMPORT_reboot_aio_tracing.span(
                state_name=f"tests.reboot.Greeter('{context.state_id}')",
                span_name=f"{IMPORT_reboot_aio_tracing.qualified_type_name(self)}.Workflow()",
                level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                python_specific=True,
        ):
            typed_request = GreeterWorkflowRequestFromProto(request)
            return GreeterWorkflowResponseToProto(
                await self.Workflow(
                    context,
                    typed_request,
                )
            )


    # For 'tests.reboot.GreeterMethods.DangerousFields'.
    async def _DangerousFields(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: Greeter.State,
        request: tests.reboot.greeter_pb2.DangerousFieldsRequest,
    ) -> google.protobuf.empty_pb2.Empty:
        # Wrap the call to the developer's method in a `span` so that it
        # is traced using its fully-qualified Python name.
        with IMPORT_reboot_aio_tracing.span(
                state_name=f"tests.reboot.Greeter('{context.state_id}')",
                span_name=f"{IMPORT_reboot_aio_tracing.qualified_type_name(self)}.DangerousFields()",
                level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                python_specific=True,
        ):
            typed_request = GreeterDangerousFieldsRequestFromProto(request)
            return GreeterDangerousFieldsResponseToProto(
                await self.DangerousFields(
                    context,
                    state,
                    typed_request
                )
            )


    # For 'tests.reboot.GreeterMethods.StoreRecursiveMessage'.
    async def _StoreRecursiveMessage(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: Greeter.State,
        request: tests.reboot.greeter_pb2.StoreRecursiveMessageRequest,
    ) -> tests.reboot.greeter_pb2.StoreRecursiveMessageResponse:
        # Wrap the call to the developer's method in a `span` so that it
        # is traced using its fully-qualified Python name.
        with IMPORT_reboot_aio_tracing.span(
                state_name=f"tests.reboot.Greeter('{context.state_id}')",
                span_name=f"{IMPORT_reboot_aio_tracing.qualified_type_name(self)}.StoreRecursiveMessage()",
                level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                python_specific=True,
        ):
            typed_request = GreeterStoreRecursiveMessageRequestFromProto(request)
            return GreeterStoreRecursiveMessageResponseToProto(
                await self.StoreRecursiveMessage(
                    context,
                    state,
                    typed_request
                )
            )


    # For 'tests.reboot.GreeterMethods.ReadRecursiveMessage'.
    async def _ReadRecursiveMessage(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: Greeter.State,
        request: tests.reboot.greeter_pb2.ReadRecursiveMessageRequest,
    ) -> tests.reboot.greeter_pb2.ReadRecursiveMessageResponse:
        # Wrap the call to the developer's method in a `span` so that it
        # is traced using its fully-qualified Python name.
        with IMPORT_reboot_aio_tracing.span(
                state_name=f"tests.reboot.Greeter('{context.state_id}')",
                span_name=f"{IMPORT_reboot_aio_tracing.qualified_type_name(self)}.ReadRecursiveMessage()",
                level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                python_specific=True,
        ):
            typed_request = GreeterReadRecursiveMessageRequestFromProto(request)
            response = (
                self.ReadRecursiveMessage(
                    context,
                    state,
                    typed_request,
                )
            )
            return GreeterReadRecursiveMessageResponseToProto(await response)

    # For 'tests.reboot.GreeterMethods.ConstructAndStoreRecursiveMessage'.
    async def _ConstructAndStoreRecursiveMessage(
        self,
        context: IMPORT_reboot_aio_contexts.TransactionContext,
        state: Greeter.State,
        request: tests.reboot.greeter_pb2.ConstructAndStoreRecursiveMessageRequest,
    ) -> tests.reboot.greeter_pb2.ConstructAndStoreRecursiveMessageResponse:
        # Wrap the call to the developer's method in a `span` so that it
        # is traced using its fully-qualified Python name.
        with IMPORT_reboot_aio_tracing.span(
                state_name=f"tests.reboot.Greeter('{context.state_id}')",
                span_name=f"{IMPORT_reboot_aio_tracing.qualified_type_name(self)}.ConstructAndStoreRecursiveMessage()",
                level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                python_specific=True,
        ):
            typed_request = GreeterConstructAndStoreRecursiveMessageRequestFromProto(request)
            return GreeterConstructAndStoreRecursiveMessageResponseToProto(
                await self.ConstructAndStoreRecursiveMessage(
                    context,
                    state,
                    typed_request
                )
            )




class GreeterServicer(GreeterBaseServicer):

    _state: IMPORT_contextvars.ContextVar[
        IMPORT_typing.Optional[Greeter.State]
    ] = IMPORT_contextvars.ContextVar(
        'Provides access to state for each call, i.e., there may be '
        'multiple readers executing concurrently but each might have '
        'a different `state`',
        default=None,
    )

    # An instance of the derived class for each state.
    _instances: dict[str, GreeterServicer] = {}

    def _instance(self, state_id: str):
        instances = GreeterServicer._instances
        instance = instances.get(state_id)
        if instance is None:
            instance = self.__class__()
            instance._middleware = self._middleware
        instances[state_id] = instance
        return instance

    @property
    def state(self) -> Greeter.State:
        state = GreeterServicer._state.get()
        if state is None:
            raise RuntimeError(
                "`state` property is only relevant within a `Servicer` method"
            )
        return state

    @state.setter
    def state(self, new_state: Greeter.State):
        state = GreeterServicer._state.get()
        if state is None:
            raise RuntimeError(
                "`state` property is only relevant within a `Servicer` method"
            )
        state.CopyFrom(new_state)

    # For 'tests.reboot.GreeterMethods.Create'.
    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that new code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    async def Create(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        request: Greeter.CreateRequest,
    ) -> Greeter.CreateResponse:
        # During the migration from 'PascalCase' to 'snake_case' method
        # naming in Python servicers, we call the 'snake_case' version
        # by default, so new names will do the correct thing making the
        # code to be backwards compatible for some time and if a servicer
        # overrides the 'PascalCase' version - it will override that
        # method and will just work.
        return await self.create(
            context,
            request,
        )

    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that existing code that
    # doesn't implement it continues to work.
    async def create(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        request: Greeter.CreateRequest,
    ) -> Greeter.CreateResponse:
        raise NotImplementedError

    # For 'tests.reboot.GreeterMethods.Greet'.
    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that new code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    async def Greet(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        request: Greeter.GreetRequest,
    ) -> Greeter.GreetResponse:
        # During the migration from 'PascalCase' to 'snake_case' method
        # naming in Python servicers, we call the 'snake_case' version
        # by default, so new names will do the correct thing making the
        # code to be backwards compatible for some time and if a servicer
        # overrides the 'PascalCase' version - it will override that
        # method and will just work.
        return await self.greet(
            context,
            request,
        )

    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that existing code that
    # doesn't implement it continues to work.
    async def greet(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        request: Greeter.GreetRequest,
    ) -> Greeter.GreetResponse:
        raise NotImplementedError

    # For 'tests.reboot.GreeterMethods.SetAdjective'.
    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that new code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    async def SetAdjective(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        request: Greeter.SetAdjectiveRequest,
    ) -> Greeter.SetAdjectiveResponse:
        # During the migration from 'PascalCase' to 'snake_case' method
        # naming in Python servicers, we call the 'snake_case' version
        # by default, so new names will do the correct thing making the
        # code to be backwards compatible for some time and if a servicer
        # overrides the 'PascalCase' version - it will override that
        # method and will just work.
        return await self.set_adjective(
            context,
            request,
        )

    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that existing code that
    # doesn't implement it continues to work.
    async def set_adjective(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        request: Greeter.SetAdjectiveRequest,
    ) -> Greeter.SetAdjectiveResponse:
        raise NotImplementedError

    # For 'tests.reboot.GreeterMethods.TransactionSetAdjective'.
    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that new code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    async def TransactionSetAdjective(
        self,
        context: IMPORT_reboot_aio_contexts.TransactionContext,
        request: Greeter.TransactionSetAdjectiveRequest,
    ) -> Greeter.TransactionSetAdjectiveResponse:
        # During the migration from 'PascalCase' to 'snake_case' method
        # naming in Python servicers, we call the 'snake_case' version
        # by default, so new names will do the correct thing making the
        # code to be backwards compatible for some time and if a servicer
        # overrides the 'PascalCase' version - it will override that
        # method and will just work.
        return await self.transaction_set_adjective(
            context,
            request,
        )

    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that existing code that
    # doesn't implement it continues to work.
    async def transaction_set_adjective(
        self,
        context: IMPORT_reboot_aio_contexts.TransactionContext,
        request: Greeter.TransactionSetAdjectiveRequest,
    ) -> Greeter.TransactionSetAdjectiveResponse:
        raise NotImplementedError

    # For 'tests.reboot.GreeterMethods.TryToConstructContext'.
    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that new code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    async def TryToConstructContext(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        request: Greeter.TryToConstructContextRequest,
    ) -> Greeter.TryToConstructContextResponse:
        # During the migration from 'PascalCase' to 'snake_case' method
        # naming in Python servicers, we call the 'snake_case' version
        # by default, so new names will do the correct thing making the
        # code to be backwards compatible for some time and if a servicer
        # overrides the 'PascalCase' version - it will override that
        # method and will just work.
        return await self.try_to_construct_context(
            context,
            request,
        )

    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that existing code that
    # doesn't implement it continues to work.
    async def try_to_construct_context(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        request: Greeter.TryToConstructContextRequest,
    ) -> Greeter.TryToConstructContextResponse:
        raise NotImplementedError

    # For 'tests.reboot.GreeterMethods.TryToConstructExternalContext'.
    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that new code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    async def TryToConstructExternalContext(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        request: Greeter.TryToConstructExternalContextRequest,
    ) -> Greeter.TryToConstructExternalContextResponse:
        # During the migration from 'PascalCase' to 'snake_case' method
        # naming in Python servicers, we call the 'snake_case' version
        # by default, so new names will do the correct thing making the
        # code to be backwards compatible for some time and if a servicer
        # overrides the 'PascalCase' version - it will override that
        # method and will just work.
        return await self.try_to_construct_external_context(
            context,
            request,
        )

    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that existing code that
    # doesn't implement it continues to work.
    async def try_to_construct_external_context(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        request: Greeter.TryToConstructExternalContextRequest,
    ) -> Greeter.TryToConstructExternalContextResponse:
        raise NotImplementedError

    # For 'tests.reboot.GreeterMethods.TestLongRunningFetch'.
    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that new code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    async def TestLongRunningFetch(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        request: Greeter.TestLongRunningFetchRequest,
    ) -> Greeter.TestLongRunningFetchResponse:
        # During the migration from 'PascalCase' to 'snake_case' method
        # naming in Python servicers, we call the 'snake_case' version
        # by default, so new names will do the correct thing making the
        # code to be backwards compatible for some time and if a servicer
        # overrides the 'PascalCase' version - it will override that
        # method and will just work.
        return await self.test_long_running_fetch(
            context,
            request,
        )

    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that existing code that
    # doesn't implement it continues to work.
    async def test_long_running_fetch(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        request: Greeter.TestLongRunningFetchRequest,
    ) -> Greeter.TestLongRunningFetchResponse:
        raise NotImplementedError

    # For 'tests.reboot.GreeterMethods.TestLongRunningWriter'.
    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that new code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    async def TestLongRunningWriter(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        request: Greeter.TestLongRunningWriterRequest,
    ) -> Greeter.TestLongRunningWriterResponse:
        # During the migration from 'PascalCase' to 'snake_case' method
        # naming in Python servicers, we call the 'snake_case' version
        # by default, so new names will do the correct thing making the
        # code to be backwards compatible for some time and if a servicer
        # overrides the 'PascalCase' version - it will override that
        # method and will just work.
        return await self.test_long_running_writer(
            context,
            request,
        )

    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that existing code that
    # doesn't implement it continues to work.
    async def test_long_running_writer(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        request: Greeter.TestLongRunningWriterRequest,
    ) -> Greeter.TestLongRunningWriterResponse:
        raise NotImplementedError

    # For 'tests.reboot.GreeterMethods.GetWholeState'.
    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that new code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    async def GetWholeState(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        request: Greeter.GetWholeStateRequest,
    ) -> Greeter.GetWholeStateResponse:
        # During the migration from 'PascalCase' to 'snake_case' method
        # naming in Python servicers, we call the 'snake_case' version
        # by default, so new names will do the correct thing making the
        # code to be backwards compatible for some time and if a servicer
        # overrides the 'PascalCase' version - it will override that
        # method and will just work.
        return await self.get_whole_state(
            context,
            request,
        )

    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that existing code that
    # doesn't implement it continues to work.
    async def get_whole_state(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        request: Greeter.GetWholeStateRequest,
    ) -> Greeter.GetWholeStateResponse:
        raise NotImplementedError

    # For 'tests.reboot.GreeterMethods.FailWithException'.
    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that new code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    async def FailWithException(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        request: Greeter.FailWithExceptionRequest,
    ) -> Greeter.FailWithExceptionResponse:
        # During the migration from 'PascalCase' to 'snake_case' method
        # naming in Python servicers, we call the 'snake_case' version
        # by default, so new names will do the correct thing making the
        # code to be backwards compatible for some time and if a servicer
        # overrides the 'PascalCase' version - it will override that
        # method and will just work.
        return await self.fail_with_exception(
            context,
            request,
        )

    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that existing code that
    # doesn't implement it continues to work.
    async def fail_with_exception(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        request: Greeter.FailWithExceptionRequest,
    ) -> Greeter.FailWithExceptionResponse:
        raise NotImplementedError

    # For 'tests.reboot.GreeterMethods.FailWithAborted'.
    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that new code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    async def FailWithAborted(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        request: Greeter.FailWithAbortedRequest,
    ) -> Greeter.FailWithAbortedResponse:
        # During the migration from 'PascalCase' to 'snake_case' method
        # naming in Python servicers, we call the 'snake_case' version
        # by default, so new names will do the correct thing making the
        # code to be backwards compatible for some time and if a servicer
        # overrides the 'PascalCase' version - it will override that
        # method and will just work.
        return await self.fail_with_aborted(
            context,
            request,
        )

    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that existing code that
    # doesn't implement it continues to work.
    async def fail_with_aborted(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        request: Greeter.FailWithAbortedRequest,
    ) -> Greeter.FailWithAbortedResponse:
        raise NotImplementedError

    # For 'tests.reboot.GreeterMethods.Workflow'.
    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that new code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    @classmethod
    async def Workflow(
        cls,
        context: IMPORT_reboot_aio_contexts.WorkflowContext,
        request: Greeter.WorkflowRequest,
    ) -> Greeter.WorkflowResponse:
        # During the migration from 'PascalCase' to 'snake_case' method
        # naming in Python servicers, we call the 'snake_case' version
        # by default, so new names will do the correct thing making the
        # code to be backwards compatible for some time and if a servicer
        # overrides the 'PascalCase' version - it will override that
        # method and will just work.
        return await cls.workflow(
            context,
            request,
        )

    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that existing code that
    # doesn't implement it continues to work.
    @classmethod
    async def workflow(
        cls,
        context: IMPORT_reboot_aio_contexts.WorkflowContext,
        request: Greeter.WorkflowRequest,
    ) -> Greeter.WorkflowResponse:
        raise NotImplementedError

    # For 'tests.reboot.GreeterMethods.DangerousFields'.
    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that new code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    async def DangerousFields(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        request: Greeter.DangerousFieldsRequest,
    ) -> Greeter.DangerousFieldsResponse:
        # During the migration from 'PascalCase' to 'snake_case' method
        # naming in Python servicers, we call the 'snake_case' version
        # by default, so new names will do the correct thing making the
        # code to be backwards compatible for some time and if a servicer
        # overrides the 'PascalCase' version - it will override that
        # method and will just work.
        return await self.dangerous_fields(
            context,
            request,
        )

    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that existing code that
    # doesn't implement it continues to work.
    async def dangerous_fields(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        request: Greeter.DangerousFieldsRequest,
    ) -> Greeter.DangerousFieldsResponse:
        raise NotImplementedError

    # For 'tests.reboot.GreeterMethods.StoreRecursiveMessage'.
    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that new code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    async def StoreRecursiveMessage(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        request: Greeter.StoreRecursiveMessageRequest,
    ) -> Greeter.StoreRecursiveMessageResponse:
        # During the migration from 'PascalCase' to 'snake_case' method
        # naming in Python servicers, we call the 'snake_case' version
        # by default, so new names will do the correct thing making the
        # code to be backwards compatible for some time and if a servicer
        # overrides the 'PascalCase' version - it will override that
        # method and will just work.
        return await self.store_recursive_message(
            context,
            request,
        )

    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that existing code that
    # doesn't implement it continues to work.
    async def store_recursive_message(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        request: Greeter.StoreRecursiveMessageRequest,
    ) -> Greeter.StoreRecursiveMessageResponse:
        raise NotImplementedError

    # For 'tests.reboot.GreeterMethods.ReadRecursiveMessage'.
    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that new code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    async def ReadRecursiveMessage(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        request: Greeter.ReadRecursiveMessageRequest,
    ) -> Greeter.ReadRecursiveMessageResponse:
        # During the migration from 'PascalCase' to 'snake_case' method
        # naming in Python servicers, we call the 'snake_case' version
        # by default, so new names will do the correct thing making the
        # code to be backwards compatible for some time and if a servicer
        # overrides the 'PascalCase' version - it will override that
        # method and will just work.
        return await self.read_recursive_message(
            context,
            request,
        )

    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that existing code that
    # doesn't implement it continues to work.
    async def read_recursive_message(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        request: Greeter.ReadRecursiveMessageRequest,
    ) -> Greeter.ReadRecursiveMessageResponse:
        raise NotImplementedError

    # For 'tests.reboot.GreeterMethods.ConstructAndStoreRecursiveMessage'.
    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that new code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    async def ConstructAndStoreRecursiveMessage(
        self,
        context: IMPORT_reboot_aio_contexts.TransactionContext,
        request: Greeter.ConstructAndStoreRecursiveMessageRequest,
    ) -> Greeter.ConstructAndStoreRecursiveMessageResponse:
        # During the migration from 'PascalCase' to 'snake_case' method
        # naming in Python servicers, we call the 'snake_case' version
        # by default, so new names will do the correct thing making the
        # code to be backwards compatible for some time and if a servicer
        # overrides the 'PascalCase' version - it will override that
        # method and will just work.
        return await self.construct_and_store_recursive_message(
            context,
            request,
        )

    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that existing code that
    # doesn't implement it continues to work.
    async def construct_and_store_recursive_message(
        self,
        context: IMPORT_reboot_aio_contexts.TransactionContext,
        request: Greeter.ConstructAndStoreRecursiveMessageRequest,
    ) -> Greeter.ConstructAndStoreRecursiveMessageResponse:
        raise NotImplementedError


    # For 'tests.reboot.GreeterMethods.Create'.
    async def _Create(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: Greeter.State,
        request: tests.reboot.greeter_pb2.CreateRequest,
    ) -> tests.reboot.greeter_pb2.CreateResponse:
        # We should have an asyncio task and thus context per request,
        # let's confirm this assumption by making sure that
        # `_state is None`.
        assert GreeterServicer._state.get() is None
        GreeterServicer._state.set(state)
        try:
            # Wrap the call to the developer's method in a `span` so that it
            # is traced using its fully-qualified Python name.
            instance = self._instance(context.state_id)
            with IMPORT_reboot_aio_tracing.span(
                    state_name=f"tests.reboot.Greeter('{context.state_id}')",
                    span_name=f"{IMPORT_reboot_aio_tracing.qualified_type_name(instance)}.Create()",
                    level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                    python_specific=True,
            ):
                typed_request = GreeterCreateRequestFromProto(request)

                return GreeterCreateResponseToProto(
                    await instance.Create(
                        context,
                        typed_request,
                    )
                )
        finally:
            GreeterServicer._state.set(None)

    # For 'tests.reboot.GreeterMethods.Greet'.
    async def _Greet(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: Greeter.State,
        request: tests.reboot.greeter_pb2.GreetRequest,
    ) -> tests.reboot.greeter_pb2.GreetResponse:
        # We should have an asyncio task and thus context per request,
        # let's confirm this assumption by making sure that
        # `_state is None`.
        assert GreeterServicer._state.get() is None
        GreeterServicer._state.set(state)
        try:
            # Wrap the call to the developer's method in a `span` so that it
            # is traced using its fully-qualified Python name.
            instance = self._instance(context.state_id)
            with IMPORT_reboot_aio_tracing.span(
                    state_name=f"tests.reboot.Greeter('{context.state_id}')",
                    span_name=f"{IMPORT_reboot_aio_tracing.qualified_type_name(instance)}.Greet()",
                    level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                    python_specific=True,
            ):
                typed_request = GreeterGreetRequestFromProto(request)

                return GreeterGreetResponseToProto(
                    await instance.Greet(
                        context,
                        typed_request,
                    )
                )
        finally:
            GreeterServicer._state.set(None)

    # For 'tests.reboot.GreeterMethods.SetAdjective'.
    async def _SetAdjective(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: Greeter.State,
        request: tests.reboot.greeter_pb2.SetAdjectiveRequest,
    ) -> tests.reboot.greeter_pb2.SetAdjectiveResponse:
        # We should have an asyncio task and thus context per request,
        # let's confirm this assumption by making sure that
        # `_state is None`.
        assert GreeterServicer._state.get() is None
        GreeterServicer._state.set(state)
        try:
            # Wrap the call to the developer's method in a `span` so that it
            # is traced using its fully-qualified Python name.
            instance = self._instance(context.state_id)
            with IMPORT_reboot_aio_tracing.span(
                    state_name=f"tests.reboot.Greeter('{context.state_id}')",
                    span_name=f"{IMPORT_reboot_aio_tracing.qualified_type_name(instance)}.SetAdjective()",
                    level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                    python_specific=True,
            ):
                typed_request = GreeterSetAdjectiveRequestFromProto(request)

                return GreeterSetAdjectiveResponseToProto(
                    await instance.SetAdjective(
                        context,
                        typed_request,
                    )
                )
        finally:
            GreeterServicer._state.set(None)

    # For 'tests.reboot.GreeterMethods.TransactionSetAdjective'.
    async def _TransactionSetAdjective(
        self,
        context: IMPORT_reboot_aio_contexts.TransactionContext,
        state: Greeter.State,
        request: tests.reboot.greeter_pb2.SetAdjectiveRequest,
    ) -> tests.reboot.greeter_pb2.SetAdjectiveResponse:
        # We should have an asyncio task and thus context per request,
        # let's confirm this assumption by making sure that
        # `_state is None`.
        assert GreeterServicer._state.get() is None
        GreeterServicer._state.set(state)
        try:
            # Wrap the call to the developer's method in a `span` so that it
            # is traced using its fully-qualified Python name.
            instance = self._instance(context.state_id)
            with IMPORT_reboot_aio_tracing.span(
                    state_name=f"tests.reboot.Greeter('{context.state_id}')",
                    span_name=f"{IMPORT_reboot_aio_tracing.qualified_type_name(instance)}.TransactionSetAdjective()",
                    level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                    python_specific=True,
            ):
                typed_request = GreeterTransactionSetAdjectiveRequestFromProto(request)

                return GreeterTransactionSetAdjectiveResponseToProto(
                    await instance.TransactionSetAdjective(
                        context,
                        typed_request,
                    )
                )
        finally:
            GreeterServicer._state.set(None)

    # For 'tests.reboot.GreeterMethods.TryToConstructContext'.
    async def _TryToConstructContext(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: Greeter.State,
        request: google.protobuf.empty_pb2.Empty,
    ) -> google.protobuf.empty_pb2.Empty:
        # We should have an asyncio task and thus context per request,
        # let's confirm this assumption by making sure that
        # `_state is None`.
        assert GreeterServicer._state.get() is None
        GreeterServicer._state.set(state)
        try:
            # Wrap the call to the developer's method in a `span` so that it
            # is traced using its fully-qualified Python name.
            instance = self._instance(context.state_id)
            with IMPORT_reboot_aio_tracing.span(
                    state_name=f"tests.reboot.Greeter('{context.state_id}')",
                    span_name=f"{IMPORT_reboot_aio_tracing.qualified_type_name(instance)}.TryToConstructContext()",
                    level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                    python_specific=True,
            ):
                typed_request = GreeterTryToConstructContextRequestFromProto(request)

                return GreeterTryToConstructContextResponseToProto(
                    await instance.TryToConstructContext(
                        context,
                        typed_request,
                    )
                )
        finally:
            GreeterServicer._state.set(None)

    # For 'tests.reboot.GreeterMethods.TryToConstructExternalContext'.
    async def _TryToConstructExternalContext(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: Greeter.State,
        request: google.protobuf.empty_pb2.Empty,
    ) -> google.protobuf.empty_pb2.Empty:
        # We should have an asyncio task and thus context per request,
        # let's confirm this assumption by making sure that
        # `_state is None`.
        assert GreeterServicer._state.get() is None
        GreeterServicer._state.set(state)
        try:
            # Wrap the call to the developer's method in a `span` so that it
            # is traced using its fully-qualified Python name.
            instance = self._instance(context.state_id)
            with IMPORT_reboot_aio_tracing.span(
                    state_name=f"tests.reboot.Greeter('{context.state_id}')",
                    span_name=f"{IMPORT_reboot_aio_tracing.qualified_type_name(instance)}.TryToConstructExternalContext()",
                    level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                    python_specific=True,
            ):
                typed_request = GreeterTryToConstructExternalContextRequestFromProto(request)

                return GreeterTryToConstructExternalContextResponseToProto(
                    await instance.TryToConstructExternalContext(
                        context,
                        typed_request,
                    )
                )
        finally:
            GreeterServicer._state.set(None)

    # For 'tests.reboot.GreeterMethods.TestLongRunningFetch'.
    async def _TestLongRunningFetch(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: Greeter.State,
        request: tests.reboot.greeter_pb2.TestLongRunningFetchRequest,
    ) -> google.protobuf.empty_pb2.Empty:
        # We should have an asyncio task and thus context per request,
        # let's confirm this assumption by making sure that
        # `_state is None`.
        assert GreeterServicer._state.get() is None
        GreeterServicer._state.set(state)
        try:
            # Wrap the call to the developer's method in a `span` so that it
            # is traced using its fully-qualified Python name.
            instance = self._instance(context.state_id)
            with IMPORT_reboot_aio_tracing.span(
                    state_name=f"tests.reboot.Greeter('{context.state_id}')",
                    span_name=f"{IMPORT_reboot_aio_tracing.qualified_type_name(instance)}.TestLongRunningFetch()",
                    level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                    python_specific=True,
            ):
                typed_request = GreeterTestLongRunningFetchRequestFromProto(request)

                return GreeterTestLongRunningFetchResponseToProto(
                    await instance.TestLongRunningFetch(
                        context,
                        typed_request,
                    )
                )
        finally:
            GreeterServicer._state.set(None)

    # For 'tests.reboot.GreeterMethods.TestLongRunningWriter'.
    async def _TestLongRunningWriter(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: Greeter.State,
        request: google.protobuf.empty_pb2.Empty,
    ) -> google.protobuf.empty_pb2.Empty:
        # We should have an asyncio task and thus context per request,
        # let's confirm this assumption by making sure that
        # `_state is None`.
        assert GreeterServicer._state.get() is None
        GreeterServicer._state.set(state)
        try:
            # Wrap the call to the developer's method in a `span` so that it
            # is traced using its fully-qualified Python name.
            instance = self._instance(context.state_id)
            with IMPORT_reboot_aio_tracing.span(
                    state_name=f"tests.reboot.Greeter('{context.state_id}')",
                    span_name=f"{IMPORT_reboot_aio_tracing.qualified_type_name(instance)}.TestLongRunningWriter()",
                    level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                    python_specific=True,
            ):
                typed_request = GreeterTestLongRunningWriterRequestFromProto(request)

                return GreeterTestLongRunningWriterResponseToProto(
                    await instance.TestLongRunningWriter(
                        context,
                        typed_request,
                    )
                )
        finally:
            GreeterServicer._state.set(None)

    # For 'tests.reboot.GreeterMethods.GetWholeState'.
    async def _GetWholeState(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: Greeter.State,
        request: tests.reboot.greeter_pb2.GetWholeStateRequest,
    ) -> tests.reboot.greeter_pb2.Greeter:
        # We should have an asyncio task and thus context per request,
        # let's confirm this assumption by making sure that
        # `_state is None`.
        assert GreeterServicer._state.get() is None
        GreeterServicer._state.set(state)
        try:
            # Wrap the call to the developer's method in a `span` so that it
            # is traced using its fully-qualified Python name.
            instance = self._instance(context.state_id)
            with IMPORT_reboot_aio_tracing.span(
                    state_name=f"tests.reboot.Greeter('{context.state_id}')",
                    span_name=f"{IMPORT_reboot_aio_tracing.qualified_type_name(instance)}.GetWholeState()",
                    level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                    python_specific=True,
            ):
                typed_request = GreeterGetWholeStateRequestFromProto(request)

                return GreeterGetWholeStateResponseToProto(
                    await instance.GetWholeState(
                        context,
                        typed_request,
                    )
                )
        finally:
            GreeterServicer._state.set(None)

    # For 'tests.reboot.GreeterMethods.FailWithException'.
    async def _FailWithException(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: Greeter.State,
        request: google.protobuf.empty_pb2.Empty,
    ) -> google.protobuf.empty_pb2.Empty:
        # We should have an asyncio task and thus context per request,
        # let's confirm this assumption by making sure that
        # `_state is None`.
        assert GreeterServicer._state.get() is None
        GreeterServicer._state.set(state)
        try:
            # Wrap the call to the developer's method in a `span` so that it
            # is traced using its fully-qualified Python name.
            instance = self._instance(context.state_id)
            with IMPORT_reboot_aio_tracing.span(
                    state_name=f"tests.reboot.Greeter('{context.state_id}')",
                    span_name=f"{IMPORT_reboot_aio_tracing.qualified_type_name(instance)}.FailWithException()",
                    level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                    python_specific=True,
            ):
                typed_request = GreeterFailWithExceptionRequestFromProto(request)

                return GreeterFailWithExceptionResponseToProto(
                    await instance.FailWithException(
                        context,
                        typed_request,
                    )
                )
        finally:
            GreeterServicer._state.set(None)

    # For 'tests.reboot.GreeterMethods.FailWithAborted'.
    async def _FailWithAborted(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: Greeter.State,
        request: google.protobuf.empty_pb2.Empty,
    ) -> google.protobuf.empty_pb2.Empty:
        # We should have an asyncio task and thus context per request,
        # let's confirm this assumption by making sure that
        # `_state is None`.
        assert GreeterServicer._state.get() is None
        GreeterServicer._state.set(state)
        try:
            # Wrap the call to the developer's method in a `span` so that it
            # is traced using its fully-qualified Python name.
            instance = self._instance(context.state_id)
            with IMPORT_reboot_aio_tracing.span(
                    state_name=f"tests.reboot.Greeter('{context.state_id}')",
                    span_name=f"{IMPORT_reboot_aio_tracing.qualified_type_name(instance)}.FailWithAborted()",
                    level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                    python_specific=True,
            ):
                typed_request = GreeterFailWithAbortedRequestFromProto(request)

                return GreeterFailWithAbortedResponseToProto(
                    await instance.FailWithAborted(
                        context,
                        typed_request,
                    )
                )
        finally:
            GreeterServicer._state.set(None)

    # For 'tests.reboot.GreeterMethods.Workflow'.
    async def _Workflow(
        self,
        context: IMPORT_reboot_aio_contexts.WorkflowContext,
        request: google.protobuf.empty_pb2.Empty,
    ) -> tests.reboot.greeter_pb2.WorkflowResponse:
        # Wrap the call to the developer's method in a `span` so that it
        # is traced using its fully-qualified Python name.
        instance = self._instance(context.state_id)
        with IMPORT_reboot_aio_tracing.span(
                state_name=f"tests.reboot.Greeter('{context.state_id}')",
                span_name=f"{IMPORT_reboot_aio_tracing.qualified_type_name(instance)}.Workflow()",
                level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                python_specific=True,
        ):
            typed_request = GreeterWorkflowRequestFromProto(request)

            return GreeterWorkflowResponseToProto(
                await instance.Workflow(
                    context,
                    typed_request,
                )
            )

    # For 'tests.reboot.GreeterMethods.DangerousFields'.
    async def _DangerousFields(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: Greeter.State,
        request: tests.reboot.greeter_pb2.DangerousFieldsRequest,
    ) -> google.protobuf.empty_pb2.Empty:
        # We should have an asyncio task and thus context per request,
        # let's confirm this assumption by making sure that
        # `_state is None`.
        assert GreeterServicer._state.get() is None
        GreeterServicer._state.set(state)
        try:
            # Wrap the call to the developer's method in a `span` so that it
            # is traced using its fully-qualified Python name.
            instance = self._instance(context.state_id)
            with IMPORT_reboot_aio_tracing.span(
                    state_name=f"tests.reboot.Greeter('{context.state_id}')",
                    span_name=f"{IMPORT_reboot_aio_tracing.qualified_type_name(instance)}.DangerousFields()",
                    level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                    python_specific=True,
            ):
                typed_request = GreeterDangerousFieldsRequestFromProto(request)

                return GreeterDangerousFieldsResponseToProto(
                    await instance.DangerousFields(
                        context,
                        typed_request,
                    )
                )
        finally:
            GreeterServicer._state.set(None)

    # For 'tests.reboot.GreeterMethods.StoreRecursiveMessage'.
    async def _StoreRecursiveMessage(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: Greeter.State,
        request: tests.reboot.greeter_pb2.StoreRecursiveMessageRequest,
    ) -> tests.reboot.greeter_pb2.StoreRecursiveMessageResponse:
        # We should have an asyncio task and thus context per request,
        # let's confirm this assumption by making sure that
        # `_state is None`.
        assert GreeterServicer._state.get() is None
        GreeterServicer._state.set(state)
        try:
            # Wrap the call to the developer's method in a `span` so that it
            # is traced using its fully-qualified Python name.
            instance = self._instance(context.state_id)
            with IMPORT_reboot_aio_tracing.span(
                    state_name=f"tests.reboot.Greeter('{context.state_id}')",
                    span_name=f"{IMPORT_reboot_aio_tracing.qualified_type_name(instance)}.StoreRecursiveMessage()",
                    level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                    python_specific=True,
            ):
                typed_request = GreeterStoreRecursiveMessageRequestFromProto(request)

                return GreeterStoreRecursiveMessageResponseToProto(
                    await instance.StoreRecursiveMessage(
                        context,
                        typed_request,
                    )
                )
        finally:
            GreeterServicer._state.set(None)

    # For 'tests.reboot.GreeterMethods.ReadRecursiveMessage'.
    async def _ReadRecursiveMessage(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: Greeter.State,
        request: tests.reboot.greeter_pb2.ReadRecursiveMessageRequest,
    ) -> tests.reboot.greeter_pb2.ReadRecursiveMessageResponse:
        # We should have an asyncio task and thus context per request,
        # let's confirm this assumption by making sure that
        # `_state is None`.
        assert GreeterServicer._state.get() is None
        GreeterServicer._state.set(state)
        try:
            # Wrap the call to the developer's method in a `span` so that it
            # is traced using its fully-qualified Python name.
            instance = self._instance(context.state_id)
            with IMPORT_reboot_aio_tracing.span(
                    state_name=f"tests.reboot.Greeter('{context.state_id}')",
                    span_name=f"{IMPORT_reboot_aio_tracing.qualified_type_name(instance)}.ReadRecursiveMessage()",
                    level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                    python_specific=True,
            ):
                typed_request = GreeterReadRecursiveMessageRequestFromProto(request)

                return GreeterReadRecursiveMessageResponseToProto(
                    await instance.ReadRecursiveMessage(
                        context,
                        typed_request,
                    )
                )
        finally:
            GreeterServicer._state.set(None)

    # For 'tests.reboot.GreeterMethods.ConstructAndStoreRecursiveMessage'.
    async def _ConstructAndStoreRecursiveMessage(
        self,
        context: IMPORT_reboot_aio_contexts.TransactionContext,
        state: Greeter.State,
        request: tests.reboot.greeter_pb2.ConstructAndStoreRecursiveMessageRequest,
    ) -> tests.reboot.greeter_pb2.ConstructAndStoreRecursiveMessageResponse:
        # We should have an asyncio task and thus context per request,
        # let's confirm this assumption by making sure that
        # `_state is None`.
        assert GreeterServicer._state.get() is None
        GreeterServicer._state.set(state)
        try:
            # Wrap the call to the developer's method in a `span` so that it
            # is traced using its fully-qualified Python name.
            instance = self._instance(context.state_id)
            with IMPORT_reboot_aio_tracing.span(
                    state_name=f"tests.reboot.Greeter('{context.state_id}')",
                    span_name=f"{IMPORT_reboot_aio_tracing.qualified_type_name(instance)}.ConstructAndStoreRecursiveMessage()",
                    level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                    python_specific=True,
            ):
                typed_request = GreeterConstructAndStoreRecursiveMessageRequestFromProto(request)

                return GreeterConstructAndStoreRecursiveMessageResponseToProto(
                    await instance.ConstructAndStoreRecursiveMessage(
                        context,
                        typed_request,
                    )
                )
        finally:
            GreeterServicer._state.set(None)



############################ Clients ############################
# The main developer-facing entrypoints for any Reboot type. Relevant to both
# clients and servicers (who use it to find the right servicer base types, as well
# as often being clients themselves).

# Attach an explicit time time zone to "naive" `datetime` objects. A "naive" `datetime` doesn't have a
# time zone. Such objects are typically interpreted as representing local time, but could be confused
# for objects representing UTC. This helper function disambiguates by explicitly attaching the local
# time zone to `datetime` objects that don't already have an explicit time zone. If the `datetime` object
# is already timezone-aware, we still convert it to our custom `DateTimeWithTimeZone` type.
def ensure_has_timezone(
    *,
    when: IMPORT_typing.Optional[IMPORT_datetime_datetime | IMPORT_datetime_timedelta] = None,
) -> IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone | IMPORT_datetime_timedelta]:
    if isinstance(when, IMPORT_datetime_datetime):
        return IMPORT_reboot_time_DateTimeWithTimeZone.from_datetime(when)
    return when

Greeter_ScheduleTypeVar = IMPORT_typing.TypeVar('Greeter_ScheduleTypeVar', 'Greeter.WeakReference._Schedule', 'Greeter.WeakReference._WriterSchedule')
Greeter_IdempotentlyScheduleTypeVar = IMPORT_typing.TypeVar('Greeter_IdempotentlyScheduleTypeVar', 'Greeter.WeakReference._Schedule', 'Greeter.WeakReference._WriterSchedule')

Greeter_UntilCallableType = IMPORT_typing.TypeVar('Greeter_UntilCallableType')

class GreeterSingleton:
    Servicer: IMPORT_typing.TypeAlias = GreeterSingletonServicer


class Greeter:


    Servicer: IMPORT_typing.TypeAlias = GreeterServicer

    singleton: IMPORT_typing.TypeAlias = GreeterSingleton

    Effects: IMPORT_typing.TypeAlias = GreeterBaseServicer.Effects

    Authorizer: IMPORT_typing.TypeAlias = GreeterAuthorizer

    State: IMPORT_typing.TypeAlias = tests.reboot.greeter_pb2.Greeter

    CreateRequest: IMPORT_typing.TypeAlias = tests.reboot.greeter_pb2.CreateRequest
    CreateResponse: IMPORT_typing.TypeAlias = tests.reboot.greeter_pb2.CreateResponse

    GreetRequest: IMPORT_typing.TypeAlias = tests.reboot.greeter_pb2.GreetRequest
    GreetResponse: IMPORT_typing.TypeAlias = tests.reboot.greeter_pb2.GreetResponse

    SetAdjectiveRequest: IMPORT_typing.TypeAlias = tests.reboot.greeter_pb2.SetAdjectiveRequest
    SetAdjectiveResponse: IMPORT_typing.TypeAlias = tests.reboot.greeter_pb2.SetAdjectiveResponse

    TransactionSetAdjectiveRequest: IMPORT_typing.TypeAlias = tests.reboot.greeter_pb2.SetAdjectiveRequest
    TransactionSetAdjectiveResponse: IMPORT_typing.TypeAlias = tests.reboot.greeter_pb2.SetAdjectiveResponse

    TryToConstructContextRequest: IMPORT_typing.TypeAlias = google.protobuf.empty_pb2.Empty
    TryToConstructContextResponse: IMPORT_typing.TypeAlias = google.protobuf.empty_pb2.Empty

    TryToConstructExternalContextRequest: IMPORT_typing.TypeAlias = google.protobuf.empty_pb2.Empty
    TryToConstructExternalContextResponse: IMPORT_typing.TypeAlias = google.protobuf.empty_pb2.Empty

    TestLongRunningFetchRequest: IMPORT_typing.TypeAlias = tests.reboot.greeter_pb2.TestLongRunningFetchRequest
    TestLongRunningFetchResponse: IMPORT_typing.TypeAlias = google.protobuf.empty_pb2.Empty

    TestLongRunningWriterRequest: IMPORT_typing.TypeAlias = google.protobuf.empty_pb2.Empty
    TestLongRunningWriterResponse: IMPORT_typing.TypeAlias = google.protobuf.empty_pb2.Empty

    GetWholeStateRequest: IMPORT_typing.TypeAlias = tests.reboot.greeter_pb2.GetWholeStateRequest
    GetWholeStateResponse: IMPORT_typing.TypeAlias = tests.reboot.greeter_pb2.Greeter

    FailWithExceptionRequest: IMPORT_typing.TypeAlias = google.protobuf.empty_pb2.Empty
    FailWithExceptionResponse: IMPORT_typing.TypeAlias = google.protobuf.empty_pb2.Empty

    FailWithAbortedRequest: IMPORT_typing.TypeAlias = google.protobuf.empty_pb2.Empty
    FailWithAbortedResponse: IMPORT_typing.TypeAlias = google.protobuf.empty_pb2.Empty

    WorkflowRequest: IMPORT_typing.TypeAlias = google.protobuf.empty_pb2.Empty
    WorkflowResponse: IMPORT_typing.TypeAlias = tests.reboot.greeter_pb2.WorkflowResponse

    DangerousFieldsRequest: IMPORT_typing.TypeAlias = tests.reboot.greeter_pb2.DangerousFieldsRequest
    DangerousFieldsResponse: IMPORT_typing.TypeAlias = google.protobuf.empty_pb2.Empty

    StoreRecursiveMessageRequest: IMPORT_typing.TypeAlias = tests.reboot.greeter_pb2.StoreRecursiveMessageRequest
    StoreRecursiveMessageResponse: IMPORT_typing.TypeAlias = tests.reboot.greeter_pb2.StoreRecursiveMessageResponse

    ReadRecursiveMessageRequest: IMPORT_typing.TypeAlias = tests.reboot.greeter_pb2.ReadRecursiveMessageRequest
    ReadRecursiveMessageResponse: IMPORT_typing.TypeAlias = tests.reboot.greeter_pb2.ReadRecursiveMessageResponse

    ConstructAndStoreRecursiveMessageRequest: IMPORT_typing.TypeAlias = tests.reboot.greeter_pb2.ConstructAndStoreRecursiveMessageRequest
    ConstructAndStoreRecursiveMessageResponse: IMPORT_typing.TypeAlias = tests.reboot.greeter_pb2.ConstructAndStoreRecursiveMessageResponse

    __state_type_name__ = IMPORT_reboot_aio_types.StateTypeName("tests.reboot.Greeter")

    class CreateTask:
        """Represents a scheduled task running for the
        state. Note that this is not a coroutine because we are trying
        to convey the semantics that the task is already running (or
        will soon be).
        """

        @classmethod
        def retrieve(
            cls,
            context: IMPORT_reboot_aio_contexts.Context | IMPORT_reboot_aio_external.ExternalContext,
            *,
            task_id: IMPORT_rbt_v1alpha1.tasks_pb2.TaskId,
        ):
            return cls(context, task_id=task_id)

        def __init__(
            self,
            context: IMPORT_reboot_aio_contexts.Context | IMPORT_reboot_aio_external.ExternalContext,
            *,
            task_id: IMPORT_rbt_v1alpha1.tasks_pb2.TaskId,
        ) -> None:
            # Depending on the context type (inside or outside a Reboot application)
            # we may or may not know the application ID. If we don't know it, then
            # the `ExternalContext.gateway` will determine it.
            #
            # TODO: in the future we expect to support cross-application calls, in
            #       which case the developer may explicitly pass in an application ID
            #       here.
            self._application_id: IMPORT_typing.Optional[IMPORT_reboot_aio_types.ApplicationId] = None
            if isinstance(context, IMPORT_reboot_aio_contexts.Context):
                self._application_id = context.application_id
            self._channel_manager = context.channel_manager
            self._task_id = task_id

        @property
        def task_id(self) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
            return self._task_id

        def __await__(self) -> IMPORT_typing.Generator[None, None, Greeter.CreateResponse]:
            """Awaits for task to finish and returns its response."""
            async def wait_for_task() -> tests.reboot.greeter_pb2.CreateResponse:
                channel = self._channel_manager.get_channel_to_state(
                    IMPORT_reboot_aio_types.StateTypeName(self._task_id.state_type),
                    IMPORT_reboot_aio_types.StateRef(self._task_id.state_ref),
                )

                stub = IMPORT_rbt_v1alpha1.tasks_pb2_grpc.TasksStub(channel)

                try:
                    call = IMPORT_reboot_aio_stubs.UnaryRetriedCall(
                        call=None,  # `RetriedCall` can create the call itself.
                        stub_method=stub.Wait,
                        method_name="Wait",
                        request=IMPORT_rbt_v1alpha1.tasks_pb2.WaitRequest(task_id=self._task_id),
                        metadata=IMPORT_reboot_aio_headers.Headers(
                            state_ref=IMPORT_reboot_aio_types.StateRef(self._task_id.state_ref),
                            application_id=self._application_id,
                        ).to_grpc_metadata(),
                        aborted_type=IMPORT_reboot.aio.aborted.SystemAborted,
                    )

                    wait_for_task_response = await call
                except IMPORT_reboot.aio.aborted.SystemAborted as error:
                    if error.code == IMPORT_grpc.StatusCode.NOT_FOUND:
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.UnknownTask()
                        ) from None

                    raise
                else:
                    response_or_error: IMPORT_typing.Optional[IMPORT_google_protobuf_any_pb2.Any] = None
                    is_error = False

                    if wait_for_task_response.response_or_error.WhichOneof("response_or_error") == "response":
                        response_or_error = wait_for_task_response.response_or_error.response
                    else:
                        is_error = True
                        response_or_error = wait_for_task_response.response_or_error.error

                    assert response_or_error is not None
                    assert response_or_error.TypeName() != ""

                    response = tests.reboot.greeter_pb2.CreateResponse()

                    if (
                        not is_error and response_or_error.TypeName() != response.DESCRIPTOR.full_name
                    ):
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.InvalidArgument(),
                            message=
                            f"task with UUID {IMPORT_uuid.UUID(bytes=self._task_id.task_uuid)} "
                            f"has a response of type '{response_or_error.TypeName()}' "
                            "but expecting type 'tests.reboot.greeter_pb2.CreateResponse'; "
                            "are you waiting on a task of the correct method?",
                        ) from None

                    if is_error:
                        aborted_type = Greeter.CreateAborted

                        # In Reboot >= 0.40.2 we expect the error to be a `google.rpc.Status`.
                        if response_or_error.Is(IMPORT_google_rpc_status_pb2.Status.DESCRIPTOR):
                            status = IMPORT_google_rpc_status_pb2.Status()
                            response_or_error.Unpack(status)
                            raise aborted_type.from_status(status)

                        # In Reboot < 0.40.2 workflows throwing declared errors behaved poorly;
                        # we don't aim to emulate its behavior. Indicate that we don't know the
                        # reason for the abort.
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                        )

                    else:
                        response_or_error.Unpack(response)
                        return GreeterCreateResponseFromProto(response)

            return wait_for_task().__await__()

    CreateEffects: IMPORT_typing.TypeAlias = GreeterBaseServicer.CreateEffects

    class CreateAborted(IMPORT_reboot.aio.aborted.Aborted):


        Error = IMPORT_typing.Union[
            IMPORT_reboot.aio.aborted.GrpcError,
            IMPORT_reboot.aio.aborted.RebootError,
        ]

        METHOD_PROTOBUF_ERROR_TYPES: list[type[IMPORT_google_protobuf_message.Message]] = [
        ]

        PROTOBUF_ERROR_TYPES: list[type[IMPORT_google_protobuf_message.Message]] = (
            METHOD_PROTOBUF_ERROR_TYPES +
            IMPORT_reboot.aio.aborted.GRPC_ERROR_TYPES +
            IMPORT_reboot.aio.aborted.REBOOT_ERROR_TYPES
        )

        _error: Error

        MethodProtobufError = IMPORT_typing.Union[
            IMPORT_reboot.aio.aborted.GrpcError,
            IMPORT_reboot.aio.aborted.RebootError,
        ]
        _method_protobuf_error: MethodProtobufError

        _code: IMPORT_grpc.StatusCode
        _message: IMPORT_typing.Optional[str]

        def __init__(
            self,
            error:  IMPORT_reboot.aio.aborted.GrpcError,
            *,
            message: IMPORT_typing.Optional[str] = None,
            # Do not set this value when constructing in order to
            # raise. This is only used internally when constructing
            # from aborted calls.
            error_types: IMPORT_typing.Sequence[type[MethodProtobufError]] = (
                METHOD_PROTOBUF_ERROR_TYPES + IMPORT_reboot.aio.aborted.GRPC_ERROR_TYPES
            ),
        ):
            super().__init__()

            self._message = None

            self._error = error
            self._method_protobuf_error = error

            IMPORT_reboot_aio_types.assert_type(self._protobuf_error, error_types)

            code = self.grpc_status_code_from_error(self._protobuf_error)

            if code is None:
                # Must be a Reboot specific or declared method error.
                code = IMPORT_grpc.StatusCode.ABORTED

            self._code = code

            self._message = self._message if self._message is not None else message

        @property
        def error(self) -> Error :
            return self._error

        @property
        def _protobuf_error(self) -> MethodProtobufError:
            return self._method_protobuf_error

        @property
        def code(self) -> IMPORT_grpc.StatusCode:
            return self._code

        @property
        def message(self) -> IMPORT_typing.Optional[str]:
            return self._message

        @classmethod
        def from_status(cls, status: IMPORT_google_rpc_status_pb2.Status):
            error = cls.error_from_google_rpc_status_details(
                status,
                cls.PROTOBUF_ERROR_TYPES,
            )

            message = status.message if len(status.message) > 0 else None

            if error is not None:
                return cls(error, message=message, error_types=cls.PROTOBUF_ERROR_TYPES)

            error = cls.error_from_google_rpc_status_code(status)

            assert error is not None

            # TODO(benh): also consider getting the type names from
            # `status.details` and including that in `message` to make
            # debugging easier.

            return cls(error, message=message)

        @classmethod
        def from_grpc_aio_rpc_error(cls, aio_rpc_error: IMPORT_grpc.aio.AioRpcError):
            return cls(
                cls.error_from_grpc_aio_rpc_error(aio_rpc_error),
                message=aio_rpc_error.details(),
            )

        @classmethod
        def is_declared_error(cls, error: IMPORT_google_protobuf_message.Message) -> bool:
            return False

    class GreetTask:
        """Represents a scheduled task running for the
        state. Note that this is not a coroutine because we are trying
        to convey the semantics that the task is already running (or
        will soon be).
        """

        @classmethod
        def retrieve(
            cls,
            context: IMPORT_reboot_aio_contexts.Context | IMPORT_reboot_aio_external.ExternalContext,
            *,
            task_id: IMPORT_rbt_v1alpha1.tasks_pb2.TaskId,
        ):
            return cls(context, task_id=task_id)

        def __init__(
            self,
            context: IMPORT_reboot_aio_contexts.Context | IMPORT_reboot_aio_external.ExternalContext,
            *,
            task_id: IMPORT_rbt_v1alpha1.tasks_pb2.TaskId,
        ) -> None:
            # Depending on the context type (inside or outside a Reboot application)
            # we may or may not know the application ID. If we don't know it, then
            # the `ExternalContext.gateway` will determine it.
            #
            # TODO: in the future we expect to support cross-application calls, in
            #       which case the developer may explicitly pass in an application ID
            #       here.
            self._application_id: IMPORT_typing.Optional[IMPORT_reboot_aio_types.ApplicationId] = None
            if isinstance(context, IMPORT_reboot_aio_contexts.Context):
                self._application_id = context.application_id
            self._channel_manager = context.channel_manager
            self._task_id = task_id

        @property
        def task_id(self) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
            return self._task_id

        def __await__(self) -> IMPORT_typing.Generator[None, None, Greeter.GreetResponse]:
            """Awaits for task to finish and returns its response."""
            async def wait_for_task() -> tests.reboot.greeter_pb2.GreetResponse:
                channel = self._channel_manager.get_channel_to_state(
                    IMPORT_reboot_aio_types.StateTypeName(self._task_id.state_type),
                    IMPORT_reboot_aio_types.StateRef(self._task_id.state_ref),
                )

                stub = IMPORT_rbt_v1alpha1.tasks_pb2_grpc.TasksStub(channel)

                try:
                    call = IMPORT_reboot_aio_stubs.UnaryRetriedCall(
                        call=None,  # `RetriedCall` can create the call itself.
                        stub_method=stub.Wait,
                        method_name="Wait",
                        request=IMPORT_rbt_v1alpha1.tasks_pb2.WaitRequest(task_id=self._task_id),
                        metadata=IMPORT_reboot_aio_headers.Headers(
                            state_ref=IMPORT_reboot_aio_types.StateRef(self._task_id.state_ref),
                            application_id=self._application_id,
                        ).to_grpc_metadata(),
                        aborted_type=IMPORT_reboot.aio.aborted.SystemAborted,
                    )

                    wait_for_task_response = await call
                except IMPORT_reboot.aio.aborted.SystemAborted as error:
                    if error.code == IMPORT_grpc.StatusCode.NOT_FOUND:
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.UnknownTask()
                        ) from None

                    raise
                else:
                    response_or_error: IMPORT_typing.Optional[IMPORT_google_protobuf_any_pb2.Any] = None
                    is_error = False

                    if wait_for_task_response.response_or_error.WhichOneof("response_or_error") == "response":
                        response_or_error = wait_for_task_response.response_or_error.response
                    else:
                        is_error = True
                        response_or_error = wait_for_task_response.response_or_error.error

                    assert response_or_error is not None
                    assert response_or_error.TypeName() != ""

                    response = tests.reboot.greeter_pb2.GreetResponse()

                    if (
                        not is_error and response_or_error.TypeName() != response.DESCRIPTOR.full_name
                    ):
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.InvalidArgument(),
                            message=
                            f"task with UUID {IMPORT_uuid.UUID(bytes=self._task_id.task_uuid)} "
                            f"has a response of type '{response_or_error.TypeName()}' "
                            "but expecting type 'tests.reboot.greeter_pb2.GreetResponse'; "
                            "are you waiting on a task of the correct method?",
                        ) from None

                    if is_error:
                        aborted_type = Greeter.GreetAborted

                        # In Reboot >= 0.40.2 we expect the error to be a `google.rpc.Status`.
                        if response_or_error.Is(IMPORT_google_rpc_status_pb2.Status.DESCRIPTOR):
                            status = IMPORT_google_rpc_status_pb2.Status()
                            response_or_error.Unpack(status)
                            raise aborted_type.from_status(status)

                        # In Reboot < 0.40.2 workflows throwing declared errors behaved poorly;
                        # we don't aim to emulate its behavior. Indicate that we don't know the
                        # reason for the abort.
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                        )

                    else:
                        response_or_error.Unpack(response)
                        return GreeterGreetResponseFromProto(response)

            return wait_for_task().__await__()


    class GreetAborted(IMPORT_reboot.aio.aborted.Aborted):


        Error = IMPORT_typing.Union[
            IMPORT_reboot.aio.aborted.GrpcError,
            IMPORT_reboot.aio.aborted.RebootError,
        ]

        METHOD_PROTOBUF_ERROR_TYPES: list[type[IMPORT_google_protobuf_message.Message]] = [
        ]

        PROTOBUF_ERROR_TYPES: list[type[IMPORT_google_protobuf_message.Message]] = (
            METHOD_PROTOBUF_ERROR_TYPES +
            IMPORT_reboot.aio.aborted.GRPC_ERROR_TYPES +
            IMPORT_reboot.aio.aborted.REBOOT_ERROR_TYPES
        )

        _error: Error

        MethodProtobufError = IMPORT_typing.Union[
            IMPORT_reboot.aio.aborted.GrpcError,
            IMPORT_reboot.aio.aborted.RebootError,
        ]
        _method_protobuf_error: MethodProtobufError

        _code: IMPORT_grpc.StatusCode
        _message: IMPORT_typing.Optional[str]

        def __init__(
            self,
            error:  IMPORT_reboot.aio.aborted.GrpcError,
            *,
            message: IMPORT_typing.Optional[str] = None,
            # Do not set this value when constructing in order to
            # raise. This is only used internally when constructing
            # from aborted calls.
            error_types: IMPORT_typing.Sequence[type[MethodProtobufError]] = (
                METHOD_PROTOBUF_ERROR_TYPES + IMPORT_reboot.aio.aborted.GRPC_ERROR_TYPES
            ),
        ):
            super().__init__()

            self._message = None

            self._error = error
            self._method_protobuf_error = error

            IMPORT_reboot_aio_types.assert_type(self._protobuf_error, error_types)

            code = self.grpc_status_code_from_error(self._protobuf_error)

            if code is None:
                # Must be a Reboot specific or declared method error.
                code = IMPORT_grpc.StatusCode.ABORTED

            self._code = code

            self._message = self._message if self._message is not None else message

        @property
        def error(self) -> Error :
            return self._error

        @property
        def _protobuf_error(self) -> MethodProtobufError:
            return self._method_protobuf_error

        @property
        def code(self) -> IMPORT_grpc.StatusCode:
            return self._code

        @property
        def message(self) -> IMPORT_typing.Optional[str]:
            return self._message

        @classmethod
        def from_status(cls, status: IMPORT_google_rpc_status_pb2.Status):
            error = cls.error_from_google_rpc_status_details(
                status,
                cls.PROTOBUF_ERROR_TYPES,
            )

            message = status.message if len(status.message) > 0 else None

            if error is not None:
                return cls(error, message=message, error_types=cls.PROTOBUF_ERROR_TYPES)

            error = cls.error_from_google_rpc_status_code(status)

            assert error is not None

            # TODO(benh): also consider getting the type names from
            # `status.details` and including that in `message` to make
            # debugging easier.

            return cls(error, message=message)

        @classmethod
        def from_grpc_aio_rpc_error(cls, aio_rpc_error: IMPORT_grpc.aio.AioRpcError):
            return cls(
                cls.error_from_grpc_aio_rpc_error(aio_rpc_error),
                message=aio_rpc_error.details(),
            )

        @classmethod
        def is_declared_error(cls, error: IMPORT_google_protobuf_message.Message) -> bool:
            return False

    class SetAdjectiveTask:
        """Represents a scheduled task running for the
        state. Note that this is not a coroutine because we are trying
        to convey the semantics that the task is already running (or
        will soon be).
        """

        @classmethod
        def retrieve(
            cls,
            context: IMPORT_reboot_aio_contexts.Context | IMPORT_reboot_aio_external.ExternalContext,
            *,
            task_id: IMPORT_rbt_v1alpha1.tasks_pb2.TaskId,
        ):
            return cls(context, task_id=task_id)

        def __init__(
            self,
            context: IMPORT_reboot_aio_contexts.Context | IMPORT_reboot_aio_external.ExternalContext,
            *,
            task_id: IMPORT_rbt_v1alpha1.tasks_pb2.TaskId,
        ) -> None:
            # Depending on the context type (inside or outside a Reboot application)
            # we may or may not know the application ID. If we don't know it, then
            # the `ExternalContext.gateway` will determine it.
            #
            # TODO: in the future we expect to support cross-application calls, in
            #       which case the developer may explicitly pass in an application ID
            #       here.
            self._application_id: IMPORT_typing.Optional[IMPORT_reboot_aio_types.ApplicationId] = None
            if isinstance(context, IMPORT_reboot_aio_contexts.Context):
                self._application_id = context.application_id
            self._channel_manager = context.channel_manager
            self._task_id = task_id

        @property
        def task_id(self) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
            return self._task_id

        def __await__(self) -> IMPORT_typing.Generator[None, None, Greeter.SetAdjectiveResponse]:
            """Awaits for task to finish and returns its response."""
            async def wait_for_task() -> tests.reboot.greeter_pb2.SetAdjectiveResponse:
                channel = self._channel_manager.get_channel_to_state(
                    IMPORT_reboot_aio_types.StateTypeName(self._task_id.state_type),
                    IMPORT_reboot_aio_types.StateRef(self._task_id.state_ref),
                )

                stub = IMPORT_rbt_v1alpha1.tasks_pb2_grpc.TasksStub(channel)

                try:
                    call = IMPORT_reboot_aio_stubs.UnaryRetriedCall(
                        call=None,  # `RetriedCall` can create the call itself.
                        stub_method=stub.Wait,
                        method_name="Wait",
                        request=IMPORT_rbt_v1alpha1.tasks_pb2.WaitRequest(task_id=self._task_id),
                        metadata=IMPORT_reboot_aio_headers.Headers(
                            state_ref=IMPORT_reboot_aio_types.StateRef(self._task_id.state_ref),
                            application_id=self._application_id,
                        ).to_grpc_metadata(),
                        aborted_type=IMPORT_reboot.aio.aborted.SystemAborted,
                    )

                    wait_for_task_response = await call
                except IMPORT_reboot.aio.aborted.SystemAborted as error:
                    if error.code == IMPORT_grpc.StatusCode.NOT_FOUND:
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.UnknownTask()
                        ) from None

                    raise
                else:
                    response_or_error: IMPORT_typing.Optional[IMPORT_google_protobuf_any_pb2.Any] = None
                    is_error = False

                    if wait_for_task_response.response_or_error.WhichOneof("response_or_error") == "response":
                        response_or_error = wait_for_task_response.response_or_error.response
                    else:
                        is_error = True
                        response_or_error = wait_for_task_response.response_or_error.error

                    assert response_or_error is not None
                    assert response_or_error.TypeName() != ""

                    response = tests.reboot.greeter_pb2.SetAdjectiveResponse()

                    if (
                        not is_error and response_or_error.TypeName() != response.DESCRIPTOR.full_name
                    ):
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.InvalidArgument(),
                            message=
                            f"task with UUID {IMPORT_uuid.UUID(bytes=self._task_id.task_uuid)} "
                            f"has a response of type '{response_or_error.TypeName()}' "
                            "but expecting type 'tests.reboot.greeter_pb2.SetAdjectiveResponse'; "
                            "are you waiting on a task of the correct method?",
                        ) from None

                    if is_error:
                        aborted_type = Greeter.SetAdjectiveAborted

                        # In Reboot >= 0.40.2 we expect the error to be a `google.rpc.Status`.
                        if response_or_error.Is(IMPORT_google_rpc_status_pb2.Status.DESCRIPTOR):
                            status = IMPORT_google_rpc_status_pb2.Status()
                            response_or_error.Unpack(status)
                            raise aborted_type.from_status(status)

                        # In Reboot < 0.40.2 workflows throwing declared errors behaved poorly;
                        # we don't aim to emulate its behavior. Indicate that we don't know the
                        # reason for the abort.
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                        )

                    else:
                        response_or_error.Unpack(response)
                        return GreeterSetAdjectiveResponseFromProto(response)

            return wait_for_task().__await__()

    SetAdjectiveEffects: IMPORT_typing.TypeAlias = GreeterBaseServicer.SetAdjectiveEffects

    class SetAdjectiveAborted(IMPORT_reboot.aio.aborted.Aborted):


        Error = IMPORT_typing.Union[
            IMPORT_reboot.aio.aborted.GrpcError,
            IMPORT_reboot.aio.aborted.RebootError,
        ]

        METHOD_PROTOBUF_ERROR_TYPES: list[type[IMPORT_google_protobuf_message.Message]] = [
        ]

        PROTOBUF_ERROR_TYPES: list[type[IMPORT_google_protobuf_message.Message]] = (
            METHOD_PROTOBUF_ERROR_TYPES +
            IMPORT_reboot.aio.aborted.GRPC_ERROR_TYPES +
            IMPORT_reboot.aio.aborted.REBOOT_ERROR_TYPES
        )

        _error: Error

        MethodProtobufError = IMPORT_typing.Union[
            IMPORT_reboot.aio.aborted.GrpcError,
            IMPORT_reboot.aio.aborted.RebootError,
        ]
        _method_protobuf_error: MethodProtobufError

        _code: IMPORT_grpc.StatusCode
        _message: IMPORT_typing.Optional[str]

        def __init__(
            self,
            error:  IMPORT_reboot.aio.aborted.GrpcError,
            *,
            message: IMPORT_typing.Optional[str] = None,
            # Do not set this value when constructing in order to
            # raise. This is only used internally when constructing
            # from aborted calls.
            error_types: IMPORT_typing.Sequence[type[MethodProtobufError]] = (
                METHOD_PROTOBUF_ERROR_TYPES + IMPORT_reboot.aio.aborted.GRPC_ERROR_TYPES
            ),
        ):
            super().__init__()

            self._message = None

            self._error = error
            self._method_protobuf_error = error

            IMPORT_reboot_aio_types.assert_type(self._protobuf_error, error_types)

            code = self.grpc_status_code_from_error(self._protobuf_error)

            if code is None:
                # Must be a Reboot specific or declared method error.
                code = IMPORT_grpc.StatusCode.ABORTED

            self._code = code

            self._message = self._message if self._message is not None else message

        @property
        def error(self) -> Error :
            return self._error

        @property
        def _protobuf_error(self) -> MethodProtobufError:
            return self._method_protobuf_error

        @property
        def code(self) -> IMPORT_grpc.StatusCode:
            return self._code

        @property
        def message(self) -> IMPORT_typing.Optional[str]:
            return self._message

        @classmethod
        def from_status(cls, status: IMPORT_google_rpc_status_pb2.Status):
            error = cls.error_from_google_rpc_status_details(
                status,
                cls.PROTOBUF_ERROR_TYPES,
            )

            message = status.message if len(status.message) > 0 else None

            if error is not None:
                return cls(error, message=message, error_types=cls.PROTOBUF_ERROR_TYPES)

            error = cls.error_from_google_rpc_status_code(status)

            assert error is not None

            # TODO(benh): also consider getting the type names from
            # `status.details` and including that in `message` to make
            # debugging easier.

            return cls(error, message=message)

        @classmethod
        def from_grpc_aio_rpc_error(cls, aio_rpc_error: IMPORT_grpc.aio.AioRpcError):
            return cls(
                cls.error_from_grpc_aio_rpc_error(aio_rpc_error),
                message=aio_rpc_error.details(),
            )

        @classmethod
        def is_declared_error(cls, error: IMPORT_google_protobuf_message.Message) -> bool:
            return False

    class TransactionSetAdjectiveTask:
        """Represents a scheduled task running for the
        state. Note that this is not a coroutine because we are trying
        to convey the semantics that the task is already running (or
        will soon be).
        """

        @classmethod
        def retrieve(
            cls,
            context: IMPORT_reboot_aio_contexts.Context | IMPORT_reboot_aio_external.ExternalContext,
            *,
            task_id: IMPORT_rbt_v1alpha1.tasks_pb2.TaskId,
        ):
            return cls(context, task_id=task_id)

        def __init__(
            self,
            context: IMPORT_reboot_aio_contexts.Context | IMPORT_reboot_aio_external.ExternalContext,
            *,
            task_id: IMPORT_rbt_v1alpha1.tasks_pb2.TaskId,
        ) -> None:
            # Depending on the context type (inside or outside a Reboot application)
            # we may or may not know the application ID. If we don't know it, then
            # the `ExternalContext.gateway` will determine it.
            #
            # TODO: in the future we expect to support cross-application calls, in
            #       which case the developer may explicitly pass in an application ID
            #       here.
            self._application_id: IMPORT_typing.Optional[IMPORT_reboot_aio_types.ApplicationId] = None
            if isinstance(context, IMPORT_reboot_aio_contexts.Context):
                self._application_id = context.application_id
            self._channel_manager = context.channel_manager
            self._task_id = task_id

        @property
        def task_id(self) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
            return self._task_id

        def __await__(self) -> IMPORT_typing.Generator[None, None, Greeter.TransactionSetAdjectiveResponse]:
            """Awaits for task to finish and returns its response."""
            async def wait_for_task() -> tests.reboot.greeter_pb2.SetAdjectiveResponse:
                channel = self._channel_manager.get_channel_to_state(
                    IMPORT_reboot_aio_types.StateTypeName(self._task_id.state_type),
                    IMPORT_reboot_aio_types.StateRef(self._task_id.state_ref),
                )

                stub = IMPORT_rbt_v1alpha1.tasks_pb2_grpc.TasksStub(channel)

                try:
                    call = IMPORT_reboot_aio_stubs.UnaryRetriedCall(
                        call=None,  # `RetriedCall` can create the call itself.
                        stub_method=stub.Wait,
                        method_name="Wait",
                        request=IMPORT_rbt_v1alpha1.tasks_pb2.WaitRequest(task_id=self._task_id),
                        metadata=IMPORT_reboot_aio_headers.Headers(
                            state_ref=IMPORT_reboot_aio_types.StateRef(self._task_id.state_ref),
                            application_id=self._application_id,
                        ).to_grpc_metadata(),
                        aborted_type=IMPORT_reboot.aio.aborted.SystemAborted,
                    )

                    wait_for_task_response = await call
                except IMPORT_reboot.aio.aborted.SystemAborted as error:
                    if error.code == IMPORT_grpc.StatusCode.NOT_FOUND:
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.UnknownTask()
                        ) from None

                    raise
                else:
                    response_or_error: IMPORT_typing.Optional[IMPORT_google_protobuf_any_pb2.Any] = None
                    is_error = False

                    if wait_for_task_response.response_or_error.WhichOneof("response_or_error") == "response":
                        response_or_error = wait_for_task_response.response_or_error.response
                    else:
                        is_error = True
                        response_or_error = wait_for_task_response.response_or_error.error

                    assert response_or_error is not None
                    assert response_or_error.TypeName() != ""

                    response = tests.reboot.greeter_pb2.SetAdjectiveResponse()

                    if (
                        not is_error and response_or_error.TypeName() != response.DESCRIPTOR.full_name
                    ):
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.InvalidArgument(),
                            message=
                            f"task with UUID {IMPORT_uuid.UUID(bytes=self._task_id.task_uuid)} "
                            f"has a response of type '{response_or_error.TypeName()}' "
                            "but expecting type 'tests.reboot.greeter_pb2.SetAdjectiveResponse'; "
                            "are you waiting on a task of the correct method?",
                        ) from None

                    if is_error:
                        aborted_type = Greeter.TransactionSetAdjectiveAborted

                        # In Reboot >= 0.40.2 we expect the error to be a `google.rpc.Status`.
                        if response_or_error.Is(IMPORT_google_rpc_status_pb2.Status.DESCRIPTOR):
                            status = IMPORT_google_rpc_status_pb2.Status()
                            response_or_error.Unpack(status)
                            raise aborted_type.from_status(status)

                        # In Reboot < 0.40.2 workflows throwing declared errors behaved poorly;
                        # we don't aim to emulate its behavior. Indicate that we don't know the
                        # reason for the abort.
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                        )

                    else:
                        response_or_error.Unpack(response)
                        return GreeterTransactionSetAdjectiveResponseFromProto(response)

            return wait_for_task().__await__()


    class TransactionSetAdjectiveAborted(IMPORT_reboot.aio.aborted.Aborted):


        Error = IMPORT_typing.Union[
            IMPORT_reboot.aio.aborted.GrpcError,
            IMPORT_reboot.aio.aborted.RebootError,
        ]

        METHOD_PROTOBUF_ERROR_TYPES: list[type[IMPORT_google_protobuf_message.Message]] = [
        ]

        PROTOBUF_ERROR_TYPES: list[type[IMPORT_google_protobuf_message.Message]] = (
            METHOD_PROTOBUF_ERROR_TYPES +
            IMPORT_reboot.aio.aborted.GRPC_ERROR_TYPES +
            IMPORT_reboot.aio.aborted.REBOOT_ERROR_TYPES
        )

        _error: Error

        MethodProtobufError = IMPORT_typing.Union[
            IMPORT_reboot.aio.aborted.GrpcError,
            IMPORT_reboot.aio.aborted.RebootError,
        ]
        _method_protobuf_error: MethodProtobufError

        _code: IMPORT_grpc.StatusCode
        _message: IMPORT_typing.Optional[str]

        def __init__(
            self,
            error:  IMPORT_reboot.aio.aborted.GrpcError,
            *,
            message: IMPORT_typing.Optional[str] = None,
            # Do not set this value when constructing in order to
            # raise. This is only used internally when constructing
            # from aborted calls.
            error_types: IMPORT_typing.Sequence[type[MethodProtobufError]] = (
                METHOD_PROTOBUF_ERROR_TYPES + IMPORT_reboot.aio.aborted.GRPC_ERROR_TYPES
            ),
        ):
            super().__init__()

            self._message = None

            self._error = error
            self._method_protobuf_error = error

            IMPORT_reboot_aio_types.assert_type(self._protobuf_error, error_types)

            code = self.grpc_status_code_from_error(self._protobuf_error)

            if code is None:
                # Must be a Reboot specific or declared method error.
                code = IMPORT_grpc.StatusCode.ABORTED

            self._code = code

            self._message = self._message if self._message is not None else message

        @property
        def error(self) -> Error :
            return self._error

        @property
        def _protobuf_error(self) -> MethodProtobufError:
            return self._method_protobuf_error

        @property
        def code(self) -> IMPORT_grpc.StatusCode:
            return self._code

        @property
        def message(self) -> IMPORT_typing.Optional[str]:
            return self._message

        @classmethod
        def from_status(cls, status: IMPORT_google_rpc_status_pb2.Status):
            error = cls.error_from_google_rpc_status_details(
                status,
                cls.PROTOBUF_ERROR_TYPES,
            )

            message = status.message if len(status.message) > 0 else None

            if error is not None:
                return cls(error, message=message, error_types=cls.PROTOBUF_ERROR_TYPES)

            error = cls.error_from_google_rpc_status_code(status)

            assert error is not None

            # TODO(benh): also consider getting the type names from
            # `status.details` and including that in `message` to make
            # debugging easier.

            return cls(error, message=message)

        @classmethod
        def from_grpc_aio_rpc_error(cls, aio_rpc_error: IMPORT_grpc.aio.AioRpcError):
            return cls(
                cls.error_from_grpc_aio_rpc_error(aio_rpc_error),
                message=aio_rpc_error.details(),
            )

        @classmethod
        def is_declared_error(cls, error: IMPORT_google_protobuf_message.Message) -> bool:
            return False

    class TryToConstructContextTask:
        """Represents a scheduled task running for the
        state. Note that this is not a coroutine because we are trying
        to convey the semantics that the task is already running (or
        will soon be).
        """

        @classmethod
        def retrieve(
            cls,
            context: IMPORT_reboot_aio_contexts.Context | IMPORT_reboot_aio_external.ExternalContext,
            *,
            task_id: IMPORT_rbt_v1alpha1.tasks_pb2.TaskId,
        ):
            return cls(context, task_id=task_id)

        def __init__(
            self,
            context: IMPORT_reboot_aio_contexts.Context | IMPORT_reboot_aio_external.ExternalContext,
            *,
            task_id: IMPORT_rbt_v1alpha1.tasks_pb2.TaskId,
        ) -> None:
            # Depending on the context type (inside or outside a Reboot application)
            # we may or may not know the application ID. If we don't know it, then
            # the `ExternalContext.gateway` will determine it.
            #
            # TODO: in the future we expect to support cross-application calls, in
            #       which case the developer may explicitly pass in an application ID
            #       here.
            self._application_id: IMPORT_typing.Optional[IMPORT_reboot_aio_types.ApplicationId] = None
            if isinstance(context, IMPORT_reboot_aio_contexts.Context):
                self._application_id = context.application_id
            self._channel_manager = context.channel_manager
            self._task_id = task_id

        @property
        def task_id(self) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
            return self._task_id

        def __await__(self) -> IMPORT_typing.Generator[None, None, Greeter.TryToConstructContextResponse]:
            """Awaits for task to finish and returns its response."""
            async def wait_for_task() -> google.protobuf.empty_pb2.Empty:
                channel = self._channel_manager.get_channel_to_state(
                    IMPORT_reboot_aio_types.StateTypeName(self._task_id.state_type),
                    IMPORT_reboot_aio_types.StateRef(self._task_id.state_ref),
                )

                stub = IMPORT_rbt_v1alpha1.tasks_pb2_grpc.TasksStub(channel)

                try:
                    call = IMPORT_reboot_aio_stubs.UnaryRetriedCall(
                        call=None,  # `RetriedCall` can create the call itself.
                        stub_method=stub.Wait,
                        method_name="Wait",
                        request=IMPORT_rbt_v1alpha1.tasks_pb2.WaitRequest(task_id=self._task_id),
                        metadata=IMPORT_reboot_aio_headers.Headers(
                            state_ref=IMPORT_reboot_aio_types.StateRef(self._task_id.state_ref),
                            application_id=self._application_id,
                        ).to_grpc_metadata(),
                        aborted_type=IMPORT_reboot.aio.aborted.SystemAborted,
                    )

                    wait_for_task_response = await call
                except IMPORT_reboot.aio.aborted.SystemAborted as error:
                    if error.code == IMPORT_grpc.StatusCode.NOT_FOUND:
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.UnknownTask()
                        ) from None

                    raise
                else:
                    response_or_error: IMPORT_typing.Optional[IMPORT_google_protobuf_any_pb2.Any] = None
                    is_error = False

                    if wait_for_task_response.response_or_error.WhichOneof("response_or_error") == "response":
                        response_or_error = wait_for_task_response.response_or_error.response
                    else:
                        is_error = True
                        response_or_error = wait_for_task_response.response_or_error.error

                    assert response_or_error is not None
                    assert response_or_error.TypeName() != ""

                    response = google.protobuf.empty_pb2.Empty()

                    if (
                        not is_error and response_or_error.TypeName() != response.DESCRIPTOR.full_name
                    ):
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.InvalidArgument(),
                            message=
                            f"task with UUID {IMPORT_uuid.UUID(bytes=self._task_id.task_uuid)} "
                            f"has a response of type '{response_or_error.TypeName()}' "
                            "but expecting type 'google.protobuf.empty_pb2.Empty'; "
                            "are you waiting on a task of the correct method?",
                        ) from None

                    if is_error:
                        aborted_type = Greeter.TryToConstructContextAborted

                        # In Reboot >= 0.40.2 we expect the error to be a `google.rpc.Status`.
                        if response_or_error.Is(IMPORT_google_rpc_status_pb2.Status.DESCRIPTOR):
                            status = IMPORT_google_rpc_status_pb2.Status()
                            response_or_error.Unpack(status)
                            raise aborted_type.from_status(status)

                        # In Reboot < 0.40.2 workflows throwing declared errors behaved poorly;
                        # we don't aim to emulate its behavior. Indicate that we don't know the
                        # reason for the abort.
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                        )

                    else:
                        response_or_error.Unpack(response)
                        return GreeterTryToConstructContextResponseFromProto(response)

            return wait_for_task().__await__()


    class TryToConstructContextAborted(IMPORT_reboot.aio.aborted.Aborted):


        Error = IMPORT_typing.Union[
            IMPORT_reboot.aio.aborted.GrpcError,
            IMPORT_reboot.aio.aborted.RebootError,
        ]

        METHOD_PROTOBUF_ERROR_TYPES: list[type[IMPORT_google_protobuf_message.Message]] = [
        ]

        PROTOBUF_ERROR_TYPES: list[type[IMPORT_google_protobuf_message.Message]] = (
            METHOD_PROTOBUF_ERROR_TYPES +
            IMPORT_reboot.aio.aborted.GRPC_ERROR_TYPES +
            IMPORT_reboot.aio.aborted.REBOOT_ERROR_TYPES
        )

        _error: Error

        MethodProtobufError = IMPORT_typing.Union[
            IMPORT_reboot.aio.aborted.GrpcError,
            IMPORT_reboot.aio.aborted.RebootError,
        ]
        _method_protobuf_error: MethodProtobufError

        _code: IMPORT_grpc.StatusCode
        _message: IMPORT_typing.Optional[str]

        def __init__(
            self,
            error:  IMPORT_reboot.aio.aborted.GrpcError,
            *,
            message: IMPORT_typing.Optional[str] = None,
            # Do not set this value when constructing in order to
            # raise. This is only used internally when constructing
            # from aborted calls.
            error_types: IMPORT_typing.Sequence[type[MethodProtobufError]] = (
                METHOD_PROTOBUF_ERROR_TYPES + IMPORT_reboot.aio.aborted.GRPC_ERROR_TYPES
            ),
        ):
            super().__init__()

            self._message = None

            self._error = error
            self._method_protobuf_error = error

            IMPORT_reboot_aio_types.assert_type(self._protobuf_error, error_types)

            code = self.grpc_status_code_from_error(self._protobuf_error)

            if code is None:
                # Must be a Reboot specific or declared method error.
                code = IMPORT_grpc.StatusCode.ABORTED

            self._code = code

            self._message = self._message if self._message is not None else message

        @property
        def error(self) -> Error :
            return self._error

        @property
        def _protobuf_error(self) -> MethodProtobufError:
            return self._method_protobuf_error

        @property
        def code(self) -> IMPORT_grpc.StatusCode:
            return self._code

        @property
        def message(self) -> IMPORT_typing.Optional[str]:
            return self._message

        @classmethod
        def from_status(cls, status: IMPORT_google_rpc_status_pb2.Status):
            error = cls.error_from_google_rpc_status_details(
                status,
                cls.PROTOBUF_ERROR_TYPES,
            )

            message = status.message if len(status.message) > 0 else None

            if error is not None:
                return cls(error, message=message, error_types=cls.PROTOBUF_ERROR_TYPES)

            error = cls.error_from_google_rpc_status_code(status)

            assert error is not None

            # TODO(benh): also consider getting the type names from
            # `status.details` and including that in `message` to make
            # debugging easier.

            return cls(error, message=message)

        @classmethod
        def from_grpc_aio_rpc_error(cls, aio_rpc_error: IMPORT_grpc.aio.AioRpcError):
            return cls(
                cls.error_from_grpc_aio_rpc_error(aio_rpc_error),
                message=aio_rpc_error.details(),
            )

        @classmethod
        def is_declared_error(cls, error: IMPORT_google_protobuf_message.Message) -> bool:
            return False

    class TryToConstructExternalContextTask:
        """Represents a scheduled task running for the
        state. Note that this is not a coroutine because we are trying
        to convey the semantics that the task is already running (or
        will soon be).
        """

        @classmethod
        def retrieve(
            cls,
            context: IMPORT_reboot_aio_contexts.Context | IMPORT_reboot_aio_external.ExternalContext,
            *,
            task_id: IMPORT_rbt_v1alpha1.tasks_pb2.TaskId,
        ):
            return cls(context, task_id=task_id)

        def __init__(
            self,
            context: IMPORT_reboot_aio_contexts.Context | IMPORT_reboot_aio_external.ExternalContext,
            *,
            task_id: IMPORT_rbt_v1alpha1.tasks_pb2.TaskId,
        ) -> None:
            # Depending on the context type (inside or outside a Reboot application)
            # we may or may not know the application ID. If we don't know it, then
            # the `ExternalContext.gateway` will determine it.
            #
            # TODO: in the future we expect to support cross-application calls, in
            #       which case the developer may explicitly pass in an application ID
            #       here.
            self._application_id: IMPORT_typing.Optional[IMPORT_reboot_aio_types.ApplicationId] = None
            if isinstance(context, IMPORT_reboot_aio_contexts.Context):
                self._application_id = context.application_id
            self._channel_manager = context.channel_manager
            self._task_id = task_id

        @property
        def task_id(self) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
            return self._task_id

        def __await__(self) -> IMPORT_typing.Generator[None, None, Greeter.TryToConstructExternalContextResponse]:
            """Awaits for task to finish and returns its response."""
            async def wait_for_task() -> google.protobuf.empty_pb2.Empty:
                channel = self._channel_manager.get_channel_to_state(
                    IMPORT_reboot_aio_types.StateTypeName(self._task_id.state_type),
                    IMPORT_reboot_aio_types.StateRef(self._task_id.state_ref),
                )

                stub = IMPORT_rbt_v1alpha1.tasks_pb2_grpc.TasksStub(channel)

                try:
                    call = IMPORT_reboot_aio_stubs.UnaryRetriedCall(
                        call=None,  # `RetriedCall` can create the call itself.
                        stub_method=stub.Wait,
                        method_name="Wait",
                        request=IMPORT_rbt_v1alpha1.tasks_pb2.WaitRequest(task_id=self._task_id),
                        metadata=IMPORT_reboot_aio_headers.Headers(
                            state_ref=IMPORT_reboot_aio_types.StateRef(self._task_id.state_ref),
                            application_id=self._application_id,
                        ).to_grpc_metadata(),
                        aborted_type=IMPORT_reboot.aio.aborted.SystemAborted,
                    )

                    wait_for_task_response = await call
                except IMPORT_reboot.aio.aborted.SystemAborted as error:
                    if error.code == IMPORT_grpc.StatusCode.NOT_FOUND:
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.UnknownTask()
                        ) from None

                    raise
                else:
                    response_or_error: IMPORT_typing.Optional[IMPORT_google_protobuf_any_pb2.Any] = None
                    is_error = False

                    if wait_for_task_response.response_or_error.WhichOneof("response_or_error") == "response":
                        response_or_error = wait_for_task_response.response_or_error.response
                    else:
                        is_error = True
                        response_or_error = wait_for_task_response.response_or_error.error

                    assert response_or_error is not None
                    assert response_or_error.TypeName() != ""

                    response = google.protobuf.empty_pb2.Empty()

                    if (
                        not is_error and response_or_error.TypeName() != response.DESCRIPTOR.full_name
                    ):
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.InvalidArgument(),
                            message=
                            f"task with UUID {IMPORT_uuid.UUID(bytes=self._task_id.task_uuid)} "
                            f"has a response of type '{response_or_error.TypeName()}' "
                            "but expecting type 'google.protobuf.empty_pb2.Empty'; "
                            "are you waiting on a task of the correct method?",
                        ) from None

                    if is_error:
                        aborted_type = Greeter.TryToConstructExternalContextAborted

                        # In Reboot >= 0.40.2 we expect the error to be a `google.rpc.Status`.
                        if response_or_error.Is(IMPORT_google_rpc_status_pb2.Status.DESCRIPTOR):
                            status = IMPORT_google_rpc_status_pb2.Status()
                            response_or_error.Unpack(status)
                            raise aborted_type.from_status(status)

                        # In Reboot < 0.40.2 workflows throwing declared errors behaved poorly;
                        # we don't aim to emulate its behavior. Indicate that we don't know the
                        # reason for the abort.
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                        )

                    else:
                        response_or_error.Unpack(response)
                        return GreeterTryToConstructExternalContextResponseFromProto(response)

            return wait_for_task().__await__()


    class TryToConstructExternalContextAborted(IMPORT_reboot.aio.aborted.Aborted):


        Error = IMPORT_typing.Union[
            IMPORT_reboot.aio.aborted.GrpcError,
            IMPORT_reboot.aio.aborted.RebootError,
        ]

        METHOD_PROTOBUF_ERROR_TYPES: list[type[IMPORT_google_protobuf_message.Message]] = [
        ]

        PROTOBUF_ERROR_TYPES: list[type[IMPORT_google_protobuf_message.Message]] = (
            METHOD_PROTOBUF_ERROR_TYPES +
            IMPORT_reboot.aio.aborted.GRPC_ERROR_TYPES +
            IMPORT_reboot.aio.aborted.REBOOT_ERROR_TYPES
        )

        _error: Error

        MethodProtobufError = IMPORT_typing.Union[
            IMPORT_reboot.aio.aborted.GrpcError,
            IMPORT_reboot.aio.aborted.RebootError,
        ]
        _method_protobuf_error: MethodProtobufError

        _code: IMPORT_grpc.StatusCode
        _message: IMPORT_typing.Optional[str]

        def __init__(
            self,
            error:  IMPORT_reboot.aio.aborted.GrpcError,
            *,
            message: IMPORT_typing.Optional[str] = None,
            # Do not set this value when constructing in order to
            # raise. This is only used internally when constructing
            # from aborted calls.
            error_types: IMPORT_typing.Sequence[type[MethodProtobufError]] = (
                METHOD_PROTOBUF_ERROR_TYPES + IMPORT_reboot.aio.aborted.GRPC_ERROR_TYPES
            ),
        ):
            super().__init__()

            self._message = None

            self._error = error
            self._method_protobuf_error = error

            IMPORT_reboot_aio_types.assert_type(self._protobuf_error, error_types)

            code = self.grpc_status_code_from_error(self._protobuf_error)

            if code is None:
                # Must be a Reboot specific or declared method error.
                code = IMPORT_grpc.StatusCode.ABORTED

            self._code = code

            self._message = self._message if self._message is not None else message

        @property
        def error(self) -> Error :
            return self._error

        @property
        def _protobuf_error(self) -> MethodProtobufError:
            return self._method_protobuf_error

        @property
        def code(self) -> IMPORT_grpc.StatusCode:
            return self._code

        @property
        def message(self) -> IMPORT_typing.Optional[str]:
            return self._message

        @classmethod
        def from_status(cls, status: IMPORT_google_rpc_status_pb2.Status):
            error = cls.error_from_google_rpc_status_details(
                status,
                cls.PROTOBUF_ERROR_TYPES,
            )

            message = status.message if len(status.message) > 0 else None

            if error is not None:
                return cls(error, message=message, error_types=cls.PROTOBUF_ERROR_TYPES)

            error = cls.error_from_google_rpc_status_code(status)

            assert error is not None

            # TODO(benh): also consider getting the type names from
            # `status.details` and including that in `message` to make
            # debugging easier.

            return cls(error, message=message)

        @classmethod
        def from_grpc_aio_rpc_error(cls, aio_rpc_error: IMPORT_grpc.aio.AioRpcError):
            return cls(
                cls.error_from_grpc_aio_rpc_error(aio_rpc_error),
                message=aio_rpc_error.details(),
            )

        @classmethod
        def is_declared_error(cls, error: IMPORT_google_protobuf_message.Message) -> bool:
            return False

    class TestLongRunningFetchTask:
        """Represents a scheduled task running for the
        state. Note that this is not a coroutine because we are trying
        to convey the semantics that the task is already running (or
        will soon be).
        """

        @classmethod
        def retrieve(
            cls,
            context: IMPORT_reboot_aio_contexts.Context | IMPORT_reboot_aio_external.ExternalContext,
            *,
            task_id: IMPORT_rbt_v1alpha1.tasks_pb2.TaskId,
        ):
            return cls(context, task_id=task_id)

        def __init__(
            self,
            context: IMPORT_reboot_aio_contexts.Context | IMPORT_reboot_aio_external.ExternalContext,
            *,
            task_id: IMPORT_rbt_v1alpha1.tasks_pb2.TaskId,
        ) -> None:
            # Depending on the context type (inside or outside a Reboot application)
            # we may or may not know the application ID. If we don't know it, then
            # the `ExternalContext.gateway` will determine it.
            #
            # TODO: in the future we expect to support cross-application calls, in
            #       which case the developer may explicitly pass in an application ID
            #       here.
            self._application_id: IMPORT_typing.Optional[IMPORT_reboot_aio_types.ApplicationId] = None
            if isinstance(context, IMPORT_reboot_aio_contexts.Context):
                self._application_id = context.application_id
            self._channel_manager = context.channel_manager
            self._task_id = task_id

        @property
        def task_id(self) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
            return self._task_id

        def __await__(self) -> IMPORT_typing.Generator[None, None, Greeter.TestLongRunningFetchResponse]:
            """Awaits for task to finish and returns its response."""
            async def wait_for_task() -> google.protobuf.empty_pb2.Empty:
                channel = self._channel_manager.get_channel_to_state(
                    IMPORT_reboot_aio_types.StateTypeName(self._task_id.state_type),
                    IMPORT_reboot_aio_types.StateRef(self._task_id.state_ref),
                )

                stub = IMPORT_rbt_v1alpha1.tasks_pb2_grpc.TasksStub(channel)

                try:
                    call = IMPORT_reboot_aio_stubs.UnaryRetriedCall(
                        call=None,  # `RetriedCall` can create the call itself.
                        stub_method=stub.Wait,
                        method_name="Wait",
                        request=IMPORT_rbt_v1alpha1.tasks_pb2.WaitRequest(task_id=self._task_id),
                        metadata=IMPORT_reboot_aio_headers.Headers(
                            state_ref=IMPORT_reboot_aio_types.StateRef(self._task_id.state_ref),
                            application_id=self._application_id,
                        ).to_grpc_metadata(),
                        aborted_type=IMPORT_reboot.aio.aborted.SystemAborted,
                    )

                    wait_for_task_response = await call
                except IMPORT_reboot.aio.aborted.SystemAborted as error:
                    if error.code == IMPORT_grpc.StatusCode.NOT_FOUND:
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.UnknownTask()
                        ) from None

                    raise
                else:
                    response_or_error: IMPORT_typing.Optional[IMPORT_google_protobuf_any_pb2.Any] = None
                    is_error = False

                    if wait_for_task_response.response_or_error.WhichOneof("response_or_error") == "response":
                        response_or_error = wait_for_task_response.response_or_error.response
                    else:
                        is_error = True
                        response_or_error = wait_for_task_response.response_or_error.error

                    assert response_or_error is not None
                    assert response_or_error.TypeName() != ""

                    response = google.protobuf.empty_pb2.Empty()

                    if (
                        not is_error and response_or_error.TypeName() != response.DESCRIPTOR.full_name
                    ):
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.InvalidArgument(),
                            message=
                            f"task with UUID {IMPORT_uuid.UUID(bytes=self._task_id.task_uuid)} "
                            f"has a response of type '{response_or_error.TypeName()}' "
                            "but expecting type 'google.protobuf.empty_pb2.Empty'; "
                            "are you waiting on a task of the correct method?",
                        ) from None

                    if is_error:
                        aborted_type = Greeter.TestLongRunningFetchAborted

                        # In Reboot >= 0.40.2 we expect the error to be a `google.rpc.Status`.
                        if response_or_error.Is(IMPORT_google_rpc_status_pb2.Status.DESCRIPTOR):
                            status = IMPORT_google_rpc_status_pb2.Status()
                            response_or_error.Unpack(status)
                            raise aborted_type.from_status(status)

                        # In Reboot < 0.40.2 workflows throwing declared errors behaved poorly;
                        # we don't aim to emulate its behavior. Indicate that we don't know the
                        # reason for the abort.
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                        )

                    else:
                        response_or_error.Unpack(response)
                        return GreeterTestLongRunningFetchResponseFromProto(response)

            return wait_for_task().__await__()


    class TestLongRunningFetchAborted(IMPORT_reboot.aio.aborted.Aborted):


        Error = IMPORT_typing.Union[
            IMPORT_reboot.aio.aborted.GrpcError,
            IMPORT_reboot.aio.aborted.RebootError,
        ]

        METHOD_PROTOBUF_ERROR_TYPES: list[type[IMPORT_google_protobuf_message.Message]] = [
        ]

        PROTOBUF_ERROR_TYPES: list[type[IMPORT_google_protobuf_message.Message]] = (
            METHOD_PROTOBUF_ERROR_TYPES +
            IMPORT_reboot.aio.aborted.GRPC_ERROR_TYPES +
            IMPORT_reboot.aio.aborted.REBOOT_ERROR_TYPES
        )

        _error: Error

        MethodProtobufError = IMPORT_typing.Union[
            IMPORT_reboot.aio.aborted.GrpcError,
            IMPORT_reboot.aio.aborted.RebootError,
        ]
        _method_protobuf_error: MethodProtobufError

        _code: IMPORT_grpc.StatusCode
        _message: IMPORT_typing.Optional[str]

        def __init__(
            self,
            error:  IMPORT_reboot.aio.aborted.GrpcError,
            *,
            message: IMPORT_typing.Optional[str] = None,
            # Do not set this value when constructing in order to
            # raise. This is only used internally when constructing
            # from aborted calls.
            error_types: IMPORT_typing.Sequence[type[MethodProtobufError]] = (
                METHOD_PROTOBUF_ERROR_TYPES + IMPORT_reboot.aio.aborted.GRPC_ERROR_TYPES
            ),
        ):
            super().__init__()

            self._message = None

            self._error = error
            self._method_protobuf_error = error

            IMPORT_reboot_aio_types.assert_type(self._protobuf_error, error_types)

            code = self.grpc_status_code_from_error(self._protobuf_error)

            if code is None:
                # Must be a Reboot specific or declared method error.
                code = IMPORT_grpc.StatusCode.ABORTED

            self._code = code

            self._message = self._message if self._message is not None else message

        @property
        def error(self) -> Error :
            return self._error

        @property
        def _protobuf_error(self) -> MethodProtobufError:
            return self._method_protobuf_error

        @property
        def code(self) -> IMPORT_grpc.StatusCode:
            return self._code

        @property
        def message(self) -> IMPORT_typing.Optional[str]:
            return self._message

        @classmethod
        def from_status(cls, status: IMPORT_google_rpc_status_pb2.Status):
            error = cls.error_from_google_rpc_status_details(
                status,
                cls.PROTOBUF_ERROR_TYPES,
            )

            message = status.message if len(status.message) > 0 else None

            if error is not None:
                return cls(error, message=message, error_types=cls.PROTOBUF_ERROR_TYPES)

            error = cls.error_from_google_rpc_status_code(status)

            assert error is not None

            # TODO(benh): also consider getting the type names from
            # `status.details` and including that in `message` to make
            # debugging easier.

            return cls(error, message=message)

        @classmethod
        def from_grpc_aio_rpc_error(cls, aio_rpc_error: IMPORT_grpc.aio.AioRpcError):
            return cls(
                cls.error_from_grpc_aio_rpc_error(aio_rpc_error),
                message=aio_rpc_error.details(),
            )

        @classmethod
        def is_declared_error(cls, error: IMPORT_google_protobuf_message.Message) -> bool:
            return False

    class TestLongRunningWriterTask:
        """Represents a scheduled task running for the
        state. Note that this is not a coroutine because we are trying
        to convey the semantics that the task is already running (or
        will soon be).
        """

        @classmethod
        def retrieve(
            cls,
            context: IMPORT_reboot_aio_contexts.Context | IMPORT_reboot_aio_external.ExternalContext,
            *,
            task_id: IMPORT_rbt_v1alpha1.tasks_pb2.TaskId,
        ):
            return cls(context, task_id=task_id)

        def __init__(
            self,
            context: IMPORT_reboot_aio_contexts.Context | IMPORT_reboot_aio_external.ExternalContext,
            *,
            task_id: IMPORT_rbt_v1alpha1.tasks_pb2.TaskId,
        ) -> None:
            # Depending on the context type (inside or outside a Reboot application)
            # we may or may not know the application ID. If we don't know it, then
            # the `ExternalContext.gateway` will determine it.
            #
            # TODO: in the future we expect to support cross-application calls, in
            #       which case the developer may explicitly pass in an application ID
            #       here.
            self._application_id: IMPORT_typing.Optional[IMPORT_reboot_aio_types.ApplicationId] = None
            if isinstance(context, IMPORT_reboot_aio_contexts.Context):
                self._application_id = context.application_id
            self._channel_manager = context.channel_manager
            self._task_id = task_id

        @property
        def task_id(self) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
            return self._task_id

        def __await__(self) -> IMPORT_typing.Generator[None, None, Greeter.TestLongRunningWriterResponse]:
            """Awaits for task to finish and returns its response."""
            async def wait_for_task() -> google.protobuf.empty_pb2.Empty:
                channel = self._channel_manager.get_channel_to_state(
                    IMPORT_reboot_aio_types.StateTypeName(self._task_id.state_type),
                    IMPORT_reboot_aio_types.StateRef(self._task_id.state_ref),
                )

                stub = IMPORT_rbt_v1alpha1.tasks_pb2_grpc.TasksStub(channel)

                try:
                    call = IMPORT_reboot_aio_stubs.UnaryRetriedCall(
                        call=None,  # `RetriedCall` can create the call itself.
                        stub_method=stub.Wait,
                        method_name="Wait",
                        request=IMPORT_rbt_v1alpha1.tasks_pb2.WaitRequest(task_id=self._task_id),
                        metadata=IMPORT_reboot_aio_headers.Headers(
                            state_ref=IMPORT_reboot_aio_types.StateRef(self._task_id.state_ref),
                            application_id=self._application_id,
                        ).to_grpc_metadata(),
                        aborted_type=IMPORT_reboot.aio.aborted.SystemAborted,
                    )

                    wait_for_task_response = await call
                except IMPORT_reboot.aio.aborted.SystemAborted as error:
                    if error.code == IMPORT_grpc.StatusCode.NOT_FOUND:
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.UnknownTask()
                        ) from None

                    raise
                else:
                    response_or_error: IMPORT_typing.Optional[IMPORT_google_protobuf_any_pb2.Any] = None
                    is_error = False

                    if wait_for_task_response.response_or_error.WhichOneof("response_or_error") == "response":
                        response_or_error = wait_for_task_response.response_or_error.response
                    else:
                        is_error = True
                        response_or_error = wait_for_task_response.response_or_error.error

                    assert response_or_error is not None
                    assert response_or_error.TypeName() != ""

                    response = google.protobuf.empty_pb2.Empty()

                    if (
                        not is_error and response_or_error.TypeName() != response.DESCRIPTOR.full_name
                    ):
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.InvalidArgument(),
                            message=
                            f"task with UUID {IMPORT_uuid.UUID(bytes=self._task_id.task_uuid)} "
                            f"has a response of type '{response_or_error.TypeName()}' "
                            "but expecting type 'google.protobuf.empty_pb2.Empty'; "
                            "are you waiting on a task of the correct method?",
                        ) from None

                    if is_error:
                        aborted_type = Greeter.TestLongRunningWriterAborted

                        # In Reboot >= 0.40.2 we expect the error to be a `google.rpc.Status`.
                        if response_or_error.Is(IMPORT_google_rpc_status_pb2.Status.DESCRIPTOR):
                            status = IMPORT_google_rpc_status_pb2.Status()
                            response_or_error.Unpack(status)
                            raise aborted_type.from_status(status)

                        # In Reboot < 0.40.2 workflows throwing declared errors behaved poorly;
                        # we don't aim to emulate its behavior. Indicate that we don't know the
                        # reason for the abort.
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                        )

                    else:
                        response_or_error.Unpack(response)
                        return GreeterTestLongRunningWriterResponseFromProto(response)

            return wait_for_task().__await__()

    TestLongRunningWriterEffects: IMPORT_typing.TypeAlias = GreeterBaseServicer.TestLongRunningWriterEffects

    class TestLongRunningWriterAborted(IMPORT_reboot.aio.aborted.Aborted):


        Error = IMPORT_typing.Union[
            IMPORT_reboot.aio.aborted.GrpcError,
            IMPORT_reboot.aio.aborted.RebootError,
        ]

        METHOD_PROTOBUF_ERROR_TYPES: list[type[IMPORT_google_protobuf_message.Message]] = [
        ]

        PROTOBUF_ERROR_TYPES: list[type[IMPORT_google_protobuf_message.Message]] = (
            METHOD_PROTOBUF_ERROR_TYPES +
            IMPORT_reboot.aio.aborted.GRPC_ERROR_TYPES +
            IMPORT_reboot.aio.aborted.REBOOT_ERROR_TYPES
        )

        _error: Error

        MethodProtobufError = IMPORT_typing.Union[
            IMPORT_reboot.aio.aborted.GrpcError,
            IMPORT_reboot.aio.aborted.RebootError,
        ]
        _method_protobuf_error: MethodProtobufError

        _code: IMPORT_grpc.StatusCode
        _message: IMPORT_typing.Optional[str]

        def __init__(
            self,
            error:  IMPORT_reboot.aio.aborted.GrpcError,
            *,
            message: IMPORT_typing.Optional[str] = None,
            # Do not set this value when constructing in order to
            # raise. This is only used internally when constructing
            # from aborted calls.
            error_types: IMPORT_typing.Sequence[type[MethodProtobufError]] = (
                METHOD_PROTOBUF_ERROR_TYPES + IMPORT_reboot.aio.aborted.GRPC_ERROR_TYPES
            ),
        ):
            super().__init__()

            self._message = None

            self._error = error
            self._method_protobuf_error = error

            IMPORT_reboot_aio_types.assert_type(self._protobuf_error, error_types)

            code = self.grpc_status_code_from_error(self._protobuf_error)

            if code is None:
                # Must be a Reboot specific or declared method error.
                code = IMPORT_grpc.StatusCode.ABORTED

            self._code = code

            self._message = self._message if self._message is not None else message

        @property
        def error(self) -> Error :
            return self._error

        @property
        def _protobuf_error(self) -> MethodProtobufError:
            return self._method_protobuf_error

        @property
        def code(self) -> IMPORT_grpc.StatusCode:
            return self._code

        @property
        def message(self) -> IMPORT_typing.Optional[str]:
            return self._message

        @classmethod
        def from_status(cls, status: IMPORT_google_rpc_status_pb2.Status):
            error = cls.error_from_google_rpc_status_details(
                status,
                cls.PROTOBUF_ERROR_TYPES,
            )

            message = status.message if len(status.message) > 0 else None

            if error is not None:
                return cls(error, message=message, error_types=cls.PROTOBUF_ERROR_TYPES)

            error = cls.error_from_google_rpc_status_code(status)

            assert error is not None

            # TODO(benh): also consider getting the type names from
            # `status.details` and including that in `message` to make
            # debugging easier.

            return cls(error, message=message)

        @classmethod
        def from_grpc_aio_rpc_error(cls, aio_rpc_error: IMPORT_grpc.aio.AioRpcError):
            return cls(
                cls.error_from_grpc_aio_rpc_error(aio_rpc_error),
                message=aio_rpc_error.details(),
            )

        @classmethod
        def is_declared_error(cls, error: IMPORT_google_protobuf_message.Message) -> bool:
            return False

    class GetWholeStateTask:
        """Represents a scheduled task running for the
        state. Note that this is not a coroutine because we are trying
        to convey the semantics that the task is already running (or
        will soon be).
        """

        @classmethod
        def retrieve(
            cls,
            context: IMPORT_reboot_aio_contexts.Context | IMPORT_reboot_aio_external.ExternalContext,
            *,
            task_id: IMPORT_rbt_v1alpha1.tasks_pb2.TaskId,
        ):
            return cls(context, task_id=task_id)

        def __init__(
            self,
            context: IMPORT_reboot_aio_contexts.Context | IMPORT_reboot_aio_external.ExternalContext,
            *,
            task_id: IMPORT_rbt_v1alpha1.tasks_pb2.TaskId,
        ) -> None:
            # Depending on the context type (inside or outside a Reboot application)
            # we may or may not know the application ID. If we don't know it, then
            # the `ExternalContext.gateway` will determine it.
            #
            # TODO: in the future we expect to support cross-application calls, in
            #       which case the developer may explicitly pass in an application ID
            #       here.
            self._application_id: IMPORT_typing.Optional[IMPORT_reboot_aio_types.ApplicationId] = None
            if isinstance(context, IMPORT_reboot_aio_contexts.Context):
                self._application_id = context.application_id
            self._channel_manager = context.channel_manager
            self._task_id = task_id

        @property
        def task_id(self) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
            return self._task_id

        def __await__(self) -> IMPORT_typing.Generator[None, None, Greeter.GetWholeStateResponse]:
            """Awaits for task to finish and returns its response."""
            async def wait_for_task() -> tests.reboot.greeter_pb2.Greeter:
                channel = self._channel_manager.get_channel_to_state(
                    IMPORT_reboot_aio_types.StateTypeName(self._task_id.state_type),
                    IMPORT_reboot_aio_types.StateRef(self._task_id.state_ref),
                )

                stub = IMPORT_rbt_v1alpha1.tasks_pb2_grpc.TasksStub(channel)

                try:
                    call = IMPORT_reboot_aio_stubs.UnaryRetriedCall(
                        call=None,  # `RetriedCall` can create the call itself.
                        stub_method=stub.Wait,
                        method_name="Wait",
                        request=IMPORT_rbt_v1alpha1.tasks_pb2.WaitRequest(task_id=self._task_id),
                        metadata=IMPORT_reboot_aio_headers.Headers(
                            state_ref=IMPORT_reboot_aio_types.StateRef(self._task_id.state_ref),
                            application_id=self._application_id,
                        ).to_grpc_metadata(),
                        aborted_type=IMPORT_reboot.aio.aborted.SystemAborted,
                    )

                    wait_for_task_response = await call
                except IMPORT_reboot.aio.aborted.SystemAborted as error:
                    if error.code == IMPORT_grpc.StatusCode.NOT_FOUND:
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.UnknownTask()
                        ) from None

                    raise
                else:
                    response_or_error: IMPORT_typing.Optional[IMPORT_google_protobuf_any_pb2.Any] = None
                    is_error = False

                    if wait_for_task_response.response_or_error.WhichOneof("response_or_error") == "response":
                        response_or_error = wait_for_task_response.response_or_error.response
                    else:
                        is_error = True
                        response_or_error = wait_for_task_response.response_or_error.error

                    assert response_or_error is not None
                    assert response_or_error.TypeName() != ""

                    response = tests.reboot.greeter_pb2.Greeter()

                    if (
                        not is_error and response_or_error.TypeName() != response.DESCRIPTOR.full_name
                    ):
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.InvalidArgument(),
                            message=
                            f"task with UUID {IMPORT_uuid.UUID(bytes=self._task_id.task_uuid)} "
                            f"has a response of type '{response_or_error.TypeName()}' "
                            "but expecting type 'tests.reboot.greeter_pb2.Greeter'; "
                            "are you waiting on a task of the correct method?",
                        ) from None

                    if is_error:
                        aborted_type = Greeter.GetWholeStateAborted

                        # In Reboot >= 0.40.2 we expect the error to be a `google.rpc.Status`.
                        if response_or_error.Is(IMPORT_google_rpc_status_pb2.Status.DESCRIPTOR):
                            status = IMPORT_google_rpc_status_pb2.Status()
                            response_or_error.Unpack(status)
                            raise aborted_type.from_status(status)

                        # In Reboot < 0.40.2 workflows throwing declared errors behaved poorly;
                        # we don't aim to emulate its behavior. Indicate that we don't know the
                        # reason for the abort.
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                        )

                    else:
                        response_or_error.Unpack(response)
                        return GreeterGetWholeStateResponseFromProto(response)

            return wait_for_task().__await__()


    class GetWholeStateAborted(IMPORT_reboot.aio.aborted.Aborted):


        Error = IMPORT_typing.Union[
            IMPORT_reboot.aio.aborted.GrpcError,
            IMPORT_reboot.aio.aborted.RebootError,
        ]

        METHOD_PROTOBUF_ERROR_TYPES: list[type[IMPORT_google_protobuf_message.Message]] = [
        ]

        PROTOBUF_ERROR_TYPES: list[type[IMPORT_google_protobuf_message.Message]] = (
            METHOD_PROTOBUF_ERROR_TYPES +
            IMPORT_reboot.aio.aborted.GRPC_ERROR_TYPES +
            IMPORT_reboot.aio.aborted.REBOOT_ERROR_TYPES
        )

        _error: Error

        MethodProtobufError = IMPORT_typing.Union[
            IMPORT_reboot.aio.aborted.GrpcError,
            IMPORT_reboot.aio.aborted.RebootError,
        ]
        _method_protobuf_error: MethodProtobufError

        _code: IMPORT_grpc.StatusCode
        _message: IMPORT_typing.Optional[str]

        def __init__(
            self,
            error:  IMPORT_reboot.aio.aborted.GrpcError,
            *,
            message: IMPORT_typing.Optional[str] = None,
            # Do not set this value when constructing in order to
            # raise. This is only used internally when constructing
            # from aborted calls.
            error_types: IMPORT_typing.Sequence[type[MethodProtobufError]] = (
                METHOD_PROTOBUF_ERROR_TYPES + IMPORT_reboot.aio.aborted.GRPC_ERROR_TYPES
            ),
        ):
            super().__init__()

            self._message = None

            self._error = error
            self._method_protobuf_error = error

            IMPORT_reboot_aio_types.assert_type(self._protobuf_error, error_types)

            code = self.grpc_status_code_from_error(self._protobuf_error)

            if code is None:
                # Must be a Reboot specific or declared method error.
                code = IMPORT_grpc.StatusCode.ABORTED

            self._code = code

            self._message = self._message if self._message is not None else message

        @property
        def error(self) -> Error :
            return self._error

        @property
        def _protobuf_error(self) -> MethodProtobufError:
            return self._method_protobuf_error

        @property
        def code(self) -> IMPORT_grpc.StatusCode:
            return self._code

        @property
        def message(self) -> IMPORT_typing.Optional[str]:
            return self._message

        @classmethod
        def from_status(cls, status: IMPORT_google_rpc_status_pb2.Status):
            error = cls.error_from_google_rpc_status_details(
                status,
                cls.PROTOBUF_ERROR_TYPES,
            )

            message = status.message if len(status.message) > 0 else None

            if error is not None:
                return cls(error, message=message, error_types=cls.PROTOBUF_ERROR_TYPES)

            error = cls.error_from_google_rpc_status_code(status)

            assert error is not None

            # TODO(benh): also consider getting the type names from
            # `status.details` and including that in `message` to make
            # debugging easier.

            return cls(error, message=message)

        @classmethod
        def from_grpc_aio_rpc_error(cls, aio_rpc_error: IMPORT_grpc.aio.AioRpcError):
            return cls(
                cls.error_from_grpc_aio_rpc_error(aio_rpc_error),
                message=aio_rpc_error.details(),
            )

        @classmethod
        def is_declared_error(cls, error: IMPORT_google_protobuf_message.Message) -> bool:
            return False

    class FailWithExceptionTask:
        """Represents a scheduled task running for the
        state. Note that this is not a coroutine because we are trying
        to convey the semantics that the task is already running (or
        will soon be).
        """

        @classmethod
        def retrieve(
            cls,
            context: IMPORT_reboot_aio_contexts.Context | IMPORT_reboot_aio_external.ExternalContext,
            *,
            task_id: IMPORT_rbt_v1alpha1.tasks_pb2.TaskId,
        ):
            return cls(context, task_id=task_id)

        def __init__(
            self,
            context: IMPORT_reboot_aio_contexts.Context | IMPORT_reboot_aio_external.ExternalContext,
            *,
            task_id: IMPORT_rbt_v1alpha1.tasks_pb2.TaskId,
        ) -> None:
            # Depending on the context type (inside or outside a Reboot application)
            # we may or may not know the application ID. If we don't know it, then
            # the `ExternalContext.gateway` will determine it.
            #
            # TODO: in the future we expect to support cross-application calls, in
            #       which case the developer may explicitly pass in an application ID
            #       here.
            self._application_id: IMPORT_typing.Optional[IMPORT_reboot_aio_types.ApplicationId] = None
            if isinstance(context, IMPORT_reboot_aio_contexts.Context):
                self._application_id = context.application_id
            self._channel_manager = context.channel_manager
            self._task_id = task_id

        @property
        def task_id(self) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
            return self._task_id

        def __await__(self) -> IMPORT_typing.Generator[None, None, Greeter.FailWithExceptionResponse]:
            """Awaits for task to finish and returns its response."""
            async def wait_for_task() -> google.protobuf.empty_pb2.Empty:
                channel = self._channel_manager.get_channel_to_state(
                    IMPORT_reboot_aio_types.StateTypeName(self._task_id.state_type),
                    IMPORT_reboot_aio_types.StateRef(self._task_id.state_ref),
                )

                stub = IMPORT_rbt_v1alpha1.tasks_pb2_grpc.TasksStub(channel)

                try:
                    call = IMPORT_reboot_aio_stubs.UnaryRetriedCall(
                        call=None,  # `RetriedCall` can create the call itself.
                        stub_method=stub.Wait,
                        method_name="Wait",
                        request=IMPORT_rbt_v1alpha1.tasks_pb2.WaitRequest(task_id=self._task_id),
                        metadata=IMPORT_reboot_aio_headers.Headers(
                            state_ref=IMPORT_reboot_aio_types.StateRef(self._task_id.state_ref),
                            application_id=self._application_id,
                        ).to_grpc_metadata(),
                        aborted_type=IMPORT_reboot.aio.aborted.SystemAborted,
                    )

                    wait_for_task_response = await call
                except IMPORT_reboot.aio.aborted.SystemAborted as error:
                    if error.code == IMPORT_grpc.StatusCode.NOT_FOUND:
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.UnknownTask()
                        ) from None

                    raise
                else:
                    response_or_error: IMPORT_typing.Optional[IMPORT_google_protobuf_any_pb2.Any] = None
                    is_error = False

                    if wait_for_task_response.response_or_error.WhichOneof("response_or_error") == "response":
                        response_or_error = wait_for_task_response.response_or_error.response
                    else:
                        is_error = True
                        response_or_error = wait_for_task_response.response_or_error.error

                    assert response_or_error is not None
                    assert response_or_error.TypeName() != ""

                    response = google.protobuf.empty_pb2.Empty()

                    if (
                        not is_error and response_or_error.TypeName() != response.DESCRIPTOR.full_name
                    ):
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.InvalidArgument(),
                            message=
                            f"task with UUID {IMPORT_uuid.UUID(bytes=self._task_id.task_uuid)} "
                            f"has a response of type '{response_or_error.TypeName()}' "
                            "but expecting type 'google.protobuf.empty_pb2.Empty'; "
                            "are you waiting on a task of the correct method?",
                        ) from None

                    if is_error:
                        aborted_type = Greeter.FailWithExceptionAborted

                        # In Reboot >= 0.40.2 we expect the error to be a `google.rpc.Status`.
                        if response_or_error.Is(IMPORT_google_rpc_status_pb2.Status.DESCRIPTOR):
                            status = IMPORT_google_rpc_status_pb2.Status()
                            response_or_error.Unpack(status)
                            raise aborted_type.from_status(status)

                        # In Reboot < 0.40.2 workflows throwing declared errors behaved poorly;
                        # we don't aim to emulate its behavior. Indicate that we don't know the
                        # reason for the abort.
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                        )

                    else:
                        response_or_error.Unpack(response)
                        return GreeterFailWithExceptionResponseFromProto(response)

            return wait_for_task().__await__()


    class FailWithExceptionAborted(IMPORT_reboot.aio.aborted.Aborted):


        Error = IMPORT_typing.Union[
            IMPORT_reboot.aio.aborted.GrpcError,
            IMPORT_reboot.aio.aborted.RebootError,
        ]

        METHOD_PROTOBUF_ERROR_TYPES: list[type[IMPORT_google_protobuf_message.Message]] = [
        ]

        PROTOBUF_ERROR_TYPES: list[type[IMPORT_google_protobuf_message.Message]] = (
            METHOD_PROTOBUF_ERROR_TYPES +
            IMPORT_reboot.aio.aborted.GRPC_ERROR_TYPES +
            IMPORT_reboot.aio.aborted.REBOOT_ERROR_TYPES
        )

        _error: Error

        MethodProtobufError = IMPORT_typing.Union[
            IMPORT_reboot.aio.aborted.GrpcError,
            IMPORT_reboot.aio.aborted.RebootError,
        ]
        _method_protobuf_error: MethodProtobufError

        _code: IMPORT_grpc.StatusCode
        _message: IMPORT_typing.Optional[str]

        def __init__(
            self,
            error:  IMPORT_reboot.aio.aborted.GrpcError,
            *,
            message: IMPORT_typing.Optional[str] = None,
            # Do not set this value when constructing in order to
            # raise. This is only used internally when constructing
            # from aborted calls.
            error_types: IMPORT_typing.Sequence[type[MethodProtobufError]] = (
                METHOD_PROTOBUF_ERROR_TYPES + IMPORT_reboot.aio.aborted.GRPC_ERROR_TYPES
            ),
        ):
            super().__init__()

            self._message = None

            self._error = error
            self._method_protobuf_error = error

            IMPORT_reboot_aio_types.assert_type(self._protobuf_error, error_types)

            code = self.grpc_status_code_from_error(self._protobuf_error)

            if code is None:
                # Must be a Reboot specific or declared method error.
                code = IMPORT_grpc.StatusCode.ABORTED

            self._code = code

            self._message = self._message if self._message is not None else message

        @property
        def error(self) -> Error :
            return self._error

        @property
        def _protobuf_error(self) -> MethodProtobufError:
            return self._method_protobuf_error

        @property
        def code(self) -> IMPORT_grpc.StatusCode:
            return self._code

        @property
        def message(self) -> IMPORT_typing.Optional[str]:
            return self._message

        @classmethod
        def from_status(cls, status: IMPORT_google_rpc_status_pb2.Status):
            error = cls.error_from_google_rpc_status_details(
                status,
                cls.PROTOBUF_ERROR_TYPES,
            )

            message = status.message if len(status.message) > 0 else None

            if error is not None:
                return cls(error, message=message, error_types=cls.PROTOBUF_ERROR_TYPES)

            error = cls.error_from_google_rpc_status_code(status)

            assert error is not None

            # TODO(benh): also consider getting the type names from
            # `status.details` and including that in `message` to make
            # debugging easier.

            return cls(error, message=message)

        @classmethod
        def from_grpc_aio_rpc_error(cls, aio_rpc_error: IMPORT_grpc.aio.AioRpcError):
            return cls(
                cls.error_from_grpc_aio_rpc_error(aio_rpc_error),
                message=aio_rpc_error.details(),
            )

        @classmethod
        def is_declared_error(cls, error: IMPORT_google_protobuf_message.Message) -> bool:
            return False

    class FailWithAbortedTask:
        """Represents a scheduled task running for the
        state. Note that this is not a coroutine because we are trying
        to convey the semantics that the task is already running (or
        will soon be).
        """

        @classmethod
        def retrieve(
            cls,
            context: IMPORT_reboot_aio_contexts.Context | IMPORT_reboot_aio_external.ExternalContext,
            *,
            task_id: IMPORT_rbt_v1alpha1.tasks_pb2.TaskId,
        ):
            return cls(context, task_id=task_id)

        def __init__(
            self,
            context: IMPORT_reboot_aio_contexts.Context | IMPORT_reboot_aio_external.ExternalContext,
            *,
            task_id: IMPORT_rbt_v1alpha1.tasks_pb2.TaskId,
        ) -> None:
            # Depending on the context type (inside or outside a Reboot application)
            # we may or may not know the application ID. If we don't know it, then
            # the `ExternalContext.gateway` will determine it.
            #
            # TODO: in the future we expect to support cross-application calls, in
            #       which case the developer may explicitly pass in an application ID
            #       here.
            self._application_id: IMPORT_typing.Optional[IMPORT_reboot_aio_types.ApplicationId] = None
            if isinstance(context, IMPORT_reboot_aio_contexts.Context):
                self._application_id = context.application_id
            self._channel_manager = context.channel_manager
            self._task_id = task_id

        @property
        def task_id(self) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
            return self._task_id

        def __await__(self) -> IMPORT_typing.Generator[None, None, Greeter.FailWithAbortedResponse]:
            """Awaits for task to finish and returns its response."""
            async def wait_for_task() -> google.protobuf.empty_pb2.Empty:
                channel = self._channel_manager.get_channel_to_state(
                    IMPORT_reboot_aio_types.StateTypeName(self._task_id.state_type),
                    IMPORT_reboot_aio_types.StateRef(self._task_id.state_ref),
                )

                stub = IMPORT_rbt_v1alpha1.tasks_pb2_grpc.TasksStub(channel)

                try:
                    call = IMPORT_reboot_aio_stubs.UnaryRetriedCall(
                        call=None,  # `RetriedCall` can create the call itself.
                        stub_method=stub.Wait,
                        method_name="Wait",
                        request=IMPORT_rbt_v1alpha1.tasks_pb2.WaitRequest(task_id=self._task_id),
                        metadata=IMPORT_reboot_aio_headers.Headers(
                            state_ref=IMPORT_reboot_aio_types.StateRef(self._task_id.state_ref),
                            application_id=self._application_id,
                        ).to_grpc_metadata(),
                        aborted_type=IMPORT_reboot.aio.aborted.SystemAborted,
                    )

                    wait_for_task_response = await call
                except IMPORT_reboot.aio.aborted.SystemAborted as error:
                    if error.code == IMPORT_grpc.StatusCode.NOT_FOUND:
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.UnknownTask()
                        ) from None

                    raise
                else:
                    response_or_error: IMPORT_typing.Optional[IMPORT_google_protobuf_any_pb2.Any] = None
                    is_error = False

                    if wait_for_task_response.response_or_error.WhichOneof("response_or_error") == "response":
                        response_or_error = wait_for_task_response.response_or_error.response
                    else:
                        is_error = True
                        response_or_error = wait_for_task_response.response_or_error.error

                    assert response_or_error is not None
                    assert response_or_error.TypeName() != ""

                    response = google.protobuf.empty_pb2.Empty()

                    if (
                        not is_error and response_or_error.TypeName() != response.DESCRIPTOR.full_name
                    ):
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.InvalidArgument(),
                            message=
                            f"task with UUID {IMPORT_uuid.UUID(bytes=self._task_id.task_uuid)} "
                            f"has a response of type '{response_or_error.TypeName()}' "
                            "but expecting type 'google.protobuf.empty_pb2.Empty'; "
                            "are you waiting on a task of the correct method?",
                        ) from None

                    if is_error:
                        aborted_type = Greeter.FailWithAbortedAborted

                        # In Reboot >= 0.40.2 we expect the error to be a `google.rpc.Status`.
                        if response_or_error.Is(IMPORT_google_rpc_status_pb2.Status.DESCRIPTOR):
                            status = IMPORT_google_rpc_status_pb2.Status()
                            response_or_error.Unpack(status)
                            raise aborted_type.from_status(status)

                        # In Reboot < 0.40.2 workflows throwing declared errors behaved poorly;
                        # we don't aim to emulate its behavior. Indicate that we don't know the
                        # reason for the abort.
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                        )

                    else:
                        response_or_error.Unpack(response)
                        return GreeterFailWithAbortedResponseFromProto(response)

            return wait_for_task().__await__()


    class FailWithAbortedAborted(IMPORT_reboot.aio.aborted.Aborted):

        MethodError = IMPORT_typing.Union[
            tests.reboot.greeter_pb2.ErrorWithValue,
        ]

        Error = IMPORT_typing.Union[
            MethodError,
            IMPORT_reboot.aio.aborted.GrpcError,
            IMPORT_reboot.aio.aborted.RebootError,
        ]

        METHOD_PROTOBUF_ERROR_TYPES: list[type[IMPORT_google_protobuf_message.Message]] = [
            tests.reboot.greeter_pb2.ErrorWithValue        ]

        PROTOBUF_ERROR_TYPES: list[type[IMPORT_google_protobuf_message.Message]] = (
            METHOD_PROTOBUF_ERROR_TYPES +
            IMPORT_reboot.aio.aborted.GRPC_ERROR_TYPES +
            IMPORT_reboot.aio.aborted.REBOOT_ERROR_TYPES
        )

        _error: Error

        MethodProtobufError = IMPORT_typing.Union[
            tests.reboot.greeter_pb2.ErrorWithValue,
            IMPORT_reboot.aio.aborted.GrpcError,
            IMPORT_reboot.aio.aborted.RebootError,
        ]
        _method_protobuf_error: MethodProtobufError

        _code: IMPORT_grpc.StatusCode
        _message: IMPORT_typing.Optional[str]

        def __init__(
            self,
            error: MethodError |  IMPORT_reboot.aio.aborted.GrpcError,
            *,
            message: IMPORT_typing.Optional[str] = None,
            # Do not set this value when constructing in order to
            # raise. This is only used internally when constructing
            # from aborted calls.
            error_types: IMPORT_typing.Sequence[type[MethodProtobufError]] = (
                METHOD_PROTOBUF_ERROR_TYPES + IMPORT_reboot.aio.aborted.GRPC_ERROR_TYPES
            ),
        ):
            super().__init__()

            self._message = None

            self._error = error
            self._method_protobuf_error = error

            IMPORT_reboot_aio_types.assert_type(self._protobuf_error, error_types)

            code = self.grpc_status_code_from_error(self._protobuf_error)

            if code is None:
                # Must be a Reboot specific or declared method error.
                code = IMPORT_grpc.StatusCode.ABORTED

            self._code = code

            self._message = self._message if self._message is not None else message

        @property
        def error(self) -> Error :
            return self._error

        @property
        def _protobuf_error(self) -> MethodProtobufError:
            return self._method_protobuf_error

        @property
        def code(self) -> IMPORT_grpc.StatusCode:
            return self._code

        @property
        def message(self) -> IMPORT_typing.Optional[str]:
            return self._message

        @classmethod
        def from_status(cls, status: IMPORT_google_rpc_status_pb2.Status):
            error = cls.error_from_google_rpc_status_details(
                status,
                cls.PROTOBUF_ERROR_TYPES,
            )

            message = status.message if len(status.message) > 0 else None

            if error is not None:
                return cls(error, message=message, error_types=cls.PROTOBUF_ERROR_TYPES)

            error = cls.error_from_google_rpc_status_code(status)

            assert error is not None

            # TODO(benh): also consider getting the type names from
            # `status.details` and including that in `message` to make
            # debugging easier.

            return cls(error, message=message)

        @classmethod
        def from_grpc_aio_rpc_error(cls, aio_rpc_error: IMPORT_grpc.aio.AioRpcError):
            return cls(
                cls.error_from_grpc_aio_rpc_error(aio_rpc_error),
                message=aio_rpc_error.details(),
            )

        @classmethod
        def is_declared_error(cls, error: IMPORT_google_protobuf_message.Message) -> bool:
            if error.DESCRIPTOR.full_name == 'tests.reboot.ErrorWithValue':
                return True
            return False

    class WorkflowTask:
        """Represents a scheduled task running for the
        state. Note that this is not a coroutine because we are trying
        to convey the semantics that the task is already running (or
        will soon be).
        """

        @classmethod
        def retrieve(
            cls,
            context: IMPORT_reboot_aio_contexts.Context | IMPORT_reboot_aio_external.ExternalContext,
            *,
            task_id: IMPORT_rbt_v1alpha1.tasks_pb2.TaskId,
        ):
            return cls(context, task_id=task_id)

        def __init__(
            self,
            context: IMPORT_reboot_aio_contexts.Context | IMPORT_reboot_aio_external.ExternalContext,
            *,
            task_id: IMPORT_rbt_v1alpha1.tasks_pb2.TaskId,
        ) -> None:
            # Depending on the context type (inside or outside a Reboot application)
            # we may or may not know the application ID. If we don't know it, then
            # the `ExternalContext.gateway` will determine it.
            #
            # TODO: in the future we expect to support cross-application calls, in
            #       which case the developer may explicitly pass in an application ID
            #       here.
            self._application_id: IMPORT_typing.Optional[IMPORT_reboot_aio_types.ApplicationId] = None
            if isinstance(context, IMPORT_reboot_aio_contexts.Context):
                self._application_id = context.application_id
            self._channel_manager = context.channel_manager
            self._task_id = task_id

        @property
        def task_id(self) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
            return self._task_id

        def __await__(self) -> IMPORT_typing.Generator[None, None, Greeter.WorkflowResponse]:
            """Awaits for task to finish and returns its response."""
            async def wait_for_task() -> tests.reboot.greeter_pb2.WorkflowResponse:
                channel = self._channel_manager.get_channel_to_state(
                    IMPORT_reboot_aio_types.StateTypeName(self._task_id.state_type),
                    IMPORT_reboot_aio_types.StateRef(self._task_id.state_ref),
                )

                stub = IMPORT_rbt_v1alpha1.tasks_pb2_grpc.TasksStub(channel)

                try:
                    call = IMPORT_reboot_aio_stubs.UnaryRetriedCall(
                        call=None,  # `RetriedCall` can create the call itself.
                        stub_method=stub.Wait,
                        method_name="Wait",
                        request=IMPORT_rbt_v1alpha1.tasks_pb2.WaitRequest(task_id=self._task_id),
                        metadata=IMPORT_reboot_aio_headers.Headers(
                            state_ref=IMPORT_reboot_aio_types.StateRef(self._task_id.state_ref),
                            application_id=self._application_id,
                        ).to_grpc_metadata(),
                        aborted_type=IMPORT_reboot.aio.aborted.SystemAborted,
                    )

                    wait_for_task_response = await call
                except IMPORT_reboot.aio.aborted.SystemAborted as error:
                    if error.code == IMPORT_grpc.StatusCode.NOT_FOUND:
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.UnknownTask()
                        ) from None

                    raise
                else:
                    response_or_error: IMPORT_typing.Optional[IMPORT_google_protobuf_any_pb2.Any] = None
                    is_error = False

                    if wait_for_task_response.response_or_error.WhichOneof("response_or_error") == "response":
                        response_or_error = wait_for_task_response.response_or_error.response
                    else:
                        is_error = True
                        response_or_error = wait_for_task_response.response_or_error.error

                    assert response_or_error is not None
                    assert response_or_error.TypeName() != ""

                    response = tests.reboot.greeter_pb2.WorkflowResponse()

                    if (
                        not is_error and response_or_error.TypeName() != response.DESCRIPTOR.full_name
                    ):
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.InvalidArgument(),
                            message=
                            f"task with UUID {IMPORT_uuid.UUID(bytes=self._task_id.task_uuid)} "
                            f"has a response of type '{response_or_error.TypeName()}' "
                            "but expecting type 'tests.reboot.greeter_pb2.WorkflowResponse'; "
                            "are you waiting on a task of the correct method?",
                        ) from None

                    if is_error:
                        aborted_type = Greeter.WorkflowAborted

                        # In Reboot >= 0.40.2 we expect the error to be a `google.rpc.Status`.
                        if response_or_error.Is(IMPORT_google_rpc_status_pb2.Status.DESCRIPTOR):
                            status = IMPORT_google_rpc_status_pb2.Status()
                            response_or_error.Unpack(status)
                            raise aborted_type.from_status(status)

                        # In Reboot < 0.40.2 workflows throwing declared errors behaved poorly;
                        # we don't aim to emulate its behavior. Indicate that we don't know the
                        # reason for the abort.
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                        )

                    else:
                        response_or_error.Unpack(response)
                        return GreeterWorkflowResponseFromProto(response)

            return wait_for_task().__await__()


    class WorkflowAborted(IMPORT_reboot.aio.aborted.Aborted):

        MethodError = IMPORT_typing.Union[
            tests.reboot.greeter_pb2.ErrorWithValue,
        ]

        Error = IMPORT_typing.Union[
            MethodError,
            IMPORT_reboot.aio.aborted.GrpcError,
            IMPORT_reboot.aio.aborted.RebootError,
        ]

        METHOD_PROTOBUF_ERROR_TYPES: list[type[IMPORT_google_protobuf_message.Message]] = [
            tests.reboot.greeter_pb2.ErrorWithValue        ]

        PROTOBUF_ERROR_TYPES: list[type[IMPORT_google_protobuf_message.Message]] = (
            METHOD_PROTOBUF_ERROR_TYPES +
            IMPORT_reboot.aio.aborted.GRPC_ERROR_TYPES +
            IMPORT_reboot.aio.aborted.REBOOT_ERROR_TYPES
        )

        _error: Error

        MethodProtobufError = IMPORT_typing.Union[
            tests.reboot.greeter_pb2.ErrorWithValue,
            IMPORT_reboot.aio.aborted.GrpcError,
            IMPORT_reboot.aio.aborted.RebootError,
        ]
        _method_protobuf_error: MethodProtobufError

        _code: IMPORT_grpc.StatusCode
        _message: IMPORT_typing.Optional[str]

        def __init__(
            self,
            error: MethodError |  IMPORT_reboot.aio.aborted.GrpcError,
            *,
            message: IMPORT_typing.Optional[str] = None,
            # Do not set this value when constructing in order to
            # raise. This is only used internally when constructing
            # from aborted calls.
            error_types: IMPORT_typing.Sequence[type[MethodProtobufError]] = (
                METHOD_PROTOBUF_ERROR_TYPES + IMPORT_reboot.aio.aborted.GRPC_ERROR_TYPES
            ),
        ):
            super().__init__()

            self._message = None

            self._error = error
            self._method_protobuf_error = error

            IMPORT_reboot_aio_types.assert_type(self._protobuf_error, error_types)

            code = self.grpc_status_code_from_error(self._protobuf_error)

            if code is None:
                # Must be a Reboot specific or declared method error.
                code = IMPORT_grpc.StatusCode.ABORTED

            self._code = code

            self._message = self._message if self._message is not None else message

        @property
        def error(self) -> Error :
            return self._error

        @property
        def _protobuf_error(self) -> MethodProtobufError:
            return self._method_protobuf_error

        @property
        def code(self) -> IMPORT_grpc.StatusCode:
            return self._code

        @property
        def message(self) -> IMPORT_typing.Optional[str]:
            return self._message

        @classmethod
        def from_status(cls, status: IMPORT_google_rpc_status_pb2.Status):
            error = cls.error_from_google_rpc_status_details(
                status,
                cls.PROTOBUF_ERROR_TYPES,
            )

            message = status.message if len(status.message) > 0 else None

            if error is not None:
                return cls(error, message=message, error_types=cls.PROTOBUF_ERROR_TYPES)

            error = cls.error_from_google_rpc_status_code(status)

            assert error is not None

            # TODO(benh): also consider getting the type names from
            # `status.details` and including that in `message` to make
            # debugging easier.

            return cls(error, message=message)

        @classmethod
        def from_grpc_aio_rpc_error(cls, aio_rpc_error: IMPORT_grpc.aio.AioRpcError):
            return cls(
                cls.error_from_grpc_aio_rpc_error(aio_rpc_error),
                message=aio_rpc_error.details(),
            )

        @classmethod
        def is_declared_error(cls, error: IMPORT_google_protobuf_message.Message) -> bool:
            if error.DESCRIPTOR.full_name == 'tests.reboot.ErrorWithValue':
                return True
            return False

    class DangerousFieldsTask:
        """Represents a scheduled task running for the
        state. Note that this is not a coroutine because we are trying
        to convey the semantics that the task is already running (or
        will soon be).
        """

        @classmethod
        def retrieve(
            cls,
            context: IMPORT_reboot_aio_contexts.Context | IMPORT_reboot_aio_external.ExternalContext,
            *,
            task_id: IMPORT_rbt_v1alpha1.tasks_pb2.TaskId,
        ):
            return cls(context, task_id=task_id)

        def __init__(
            self,
            context: IMPORT_reboot_aio_contexts.Context | IMPORT_reboot_aio_external.ExternalContext,
            *,
            task_id: IMPORT_rbt_v1alpha1.tasks_pb2.TaskId,
        ) -> None:
            # Depending on the context type (inside or outside a Reboot application)
            # we may or may not know the application ID. If we don't know it, then
            # the `ExternalContext.gateway` will determine it.
            #
            # TODO: in the future we expect to support cross-application calls, in
            #       which case the developer may explicitly pass in an application ID
            #       here.
            self._application_id: IMPORT_typing.Optional[IMPORT_reboot_aio_types.ApplicationId] = None
            if isinstance(context, IMPORT_reboot_aio_contexts.Context):
                self._application_id = context.application_id
            self._channel_manager = context.channel_manager
            self._task_id = task_id

        @property
        def task_id(self) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
            return self._task_id

        def __await__(self) -> IMPORT_typing.Generator[None, None, Greeter.DangerousFieldsResponse]:
            """Awaits for task to finish and returns its response."""
            async def wait_for_task() -> google.protobuf.empty_pb2.Empty:
                channel = self._channel_manager.get_channel_to_state(
                    IMPORT_reboot_aio_types.StateTypeName(self._task_id.state_type),
                    IMPORT_reboot_aio_types.StateRef(self._task_id.state_ref),
                )

                stub = IMPORT_rbt_v1alpha1.tasks_pb2_grpc.TasksStub(channel)

                try:
                    call = IMPORT_reboot_aio_stubs.UnaryRetriedCall(
                        call=None,  # `RetriedCall` can create the call itself.
                        stub_method=stub.Wait,
                        method_name="Wait",
                        request=IMPORT_rbt_v1alpha1.tasks_pb2.WaitRequest(task_id=self._task_id),
                        metadata=IMPORT_reboot_aio_headers.Headers(
                            state_ref=IMPORT_reboot_aio_types.StateRef(self._task_id.state_ref),
                            application_id=self._application_id,
                        ).to_grpc_metadata(),
                        aborted_type=IMPORT_reboot.aio.aborted.SystemAborted,
                    )

                    wait_for_task_response = await call
                except IMPORT_reboot.aio.aborted.SystemAborted as error:
                    if error.code == IMPORT_grpc.StatusCode.NOT_FOUND:
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.UnknownTask()
                        ) from None

                    raise
                else:
                    response_or_error: IMPORT_typing.Optional[IMPORT_google_protobuf_any_pb2.Any] = None
                    is_error = False

                    if wait_for_task_response.response_or_error.WhichOneof("response_or_error") == "response":
                        response_or_error = wait_for_task_response.response_or_error.response
                    else:
                        is_error = True
                        response_or_error = wait_for_task_response.response_or_error.error

                    assert response_or_error is not None
                    assert response_or_error.TypeName() != ""

                    response = google.protobuf.empty_pb2.Empty()

                    if (
                        not is_error and response_or_error.TypeName() != response.DESCRIPTOR.full_name
                    ):
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.InvalidArgument(),
                            message=
                            f"task with UUID {IMPORT_uuid.UUID(bytes=self._task_id.task_uuid)} "
                            f"has a response of type '{response_or_error.TypeName()}' "
                            "but expecting type 'google.protobuf.empty_pb2.Empty'; "
                            "are you waiting on a task of the correct method?",
                        ) from None

                    if is_error:
                        aborted_type = Greeter.DangerousFieldsAborted

                        # In Reboot >= 0.40.2 we expect the error to be a `google.rpc.Status`.
                        if response_or_error.Is(IMPORT_google_rpc_status_pb2.Status.DESCRIPTOR):
                            status = IMPORT_google_rpc_status_pb2.Status()
                            response_or_error.Unpack(status)
                            raise aborted_type.from_status(status)

                        # In Reboot < 0.40.2 workflows throwing declared errors behaved poorly;
                        # we don't aim to emulate its behavior. Indicate that we don't know the
                        # reason for the abort.
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                        )

                    else:
                        response_or_error.Unpack(response)
                        return GreeterDangerousFieldsResponseFromProto(response)

            return wait_for_task().__await__()

    DangerousFieldsEffects: IMPORT_typing.TypeAlias = GreeterBaseServicer.DangerousFieldsEffects

    class DangerousFieldsAborted(IMPORT_reboot.aio.aborted.Aborted):


        Error = IMPORT_typing.Union[
            IMPORT_reboot.aio.aborted.GrpcError,
            IMPORT_reboot.aio.aborted.RebootError,
        ]

        METHOD_PROTOBUF_ERROR_TYPES: list[type[IMPORT_google_protobuf_message.Message]] = [
        ]

        PROTOBUF_ERROR_TYPES: list[type[IMPORT_google_protobuf_message.Message]] = (
            METHOD_PROTOBUF_ERROR_TYPES +
            IMPORT_reboot.aio.aborted.GRPC_ERROR_TYPES +
            IMPORT_reboot.aio.aborted.REBOOT_ERROR_TYPES
        )

        _error: Error

        MethodProtobufError = IMPORT_typing.Union[
            IMPORT_reboot.aio.aborted.GrpcError,
            IMPORT_reboot.aio.aborted.RebootError,
        ]
        _method_protobuf_error: MethodProtobufError

        _code: IMPORT_grpc.StatusCode
        _message: IMPORT_typing.Optional[str]

        def __init__(
            self,
            error:  IMPORT_reboot.aio.aborted.GrpcError,
            *,
            message: IMPORT_typing.Optional[str] = None,
            # Do not set this value when constructing in order to
            # raise. This is only used internally when constructing
            # from aborted calls.
            error_types: IMPORT_typing.Sequence[type[MethodProtobufError]] = (
                METHOD_PROTOBUF_ERROR_TYPES + IMPORT_reboot.aio.aborted.GRPC_ERROR_TYPES
            ),
        ):
            super().__init__()

            self._message = None

            self._error = error
            self._method_protobuf_error = error

            IMPORT_reboot_aio_types.assert_type(self._protobuf_error, error_types)

            code = self.grpc_status_code_from_error(self._protobuf_error)

            if code is None:
                # Must be a Reboot specific or declared method error.
                code = IMPORT_grpc.StatusCode.ABORTED

            self._code = code

            self._message = self._message if self._message is not None else message

        @property
        def error(self) -> Error :
            return self._error

        @property
        def _protobuf_error(self) -> MethodProtobufError:
            return self._method_protobuf_error

        @property
        def code(self) -> IMPORT_grpc.StatusCode:
            return self._code

        @property
        def message(self) -> IMPORT_typing.Optional[str]:
            return self._message

        @classmethod
        def from_status(cls, status: IMPORT_google_rpc_status_pb2.Status):
            error = cls.error_from_google_rpc_status_details(
                status,
                cls.PROTOBUF_ERROR_TYPES,
            )

            message = status.message if len(status.message) > 0 else None

            if error is not None:
                return cls(error, message=message, error_types=cls.PROTOBUF_ERROR_TYPES)

            error = cls.error_from_google_rpc_status_code(status)

            assert error is not None

            # TODO(benh): also consider getting the type names from
            # `status.details` and including that in `message` to make
            # debugging easier.

            return cls(error, message=message)

        @classmethod
        def from_grpc_aio_rpc_error(cls, aio_rpc_error: IMPORT_grpc.aio.AioRpcError):
            return cls(
                cls.error_from_grpc_aio_rpc_error(aio_rpc_error),
                message=aio_rpc_error.details(),
            )

        @classmethod
        def is_declared_error(cls, error: IMPORT_google_protobuf_message.Message) -> bool:
            return False

    class StoreRecursiveMessageTask:
        """Represents a scheduled task running for the
        state. Note that this is not a coroutine because we are trying
        to convey the semantics that the task is already running (or
        will soon be).
        """

        @classmethod
        def retrieve(
            cls,
            context: IMPORT_reboot_aio_contexts.Context | IMPORT_reboot_aio_external.ExternalContext,
            *,
            task_id: IMPORT_rbt_v1alpha1.tasks_pb2.TaskId,
        ):
            return cls(context, task_id=task_id)

        def __init__(
            self,
            context: IMPORT_reboot_aio_contexts.Context | IMPORT_reboot_aio_external.ExternalContext,
            *,
            task_id: IMPORT_rbt_v1alpha1.tasks_pb2.TaskId,
        ) -> None:
            # Depending on the context type (inside or outside a Reboot application)
            # we may or may not know the application ID. If we don't know it, then
            # the `ExternalContext.gateway` will determine it.
            #
            # TODO: in the future we expect to support cross-application calls, in
            #       which case the developer may explicitly pass in an application ID
            #       here.
            self._application_id: IMPORT_typing.Optional[IMPORT_reboot_aio_types.ApplicationId] = None
            if isinstance(context, IMPORT_reboot_aio_contexts.Context):
                self._application_id = context.application_id
            self._channel_manager = context.channel_manager
            self._task_id = task_id

        @property
        def task_id(self) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
            return self._task_id

        def __await__(self) -> IMPORT_typing.Generator[None, None, Greeter.StoreRecursiveMessageResponse]:
            """Awaits for task to finish and returns its response."""
            async def wait_for_task() -> tests.reboot.greeter_pb2.StoreRecursiveMessageResponse:
                channel = self._channel_manager.get_channel_to_state(
                    IMPORT_reboot_aio_types.StateTypeName(self._task_id.state_type),
                    IMPORT_reboot_aio_types.StateRef(self._task_id.state_ref),
                )

                stub = IMPORT_rbt_v1alpha1.tasks_pb2_grpc.TasksStub(channel)

                try:
                    call = IMPORT_reboot_aio_stubs.UnaryRetriedCall(
                        call=None,  # `RetriedCall` can create the call itself.
                        stub_method=stub.Wait,
                        method_name="Wait",
                        request=IMPORT_rbt_v1alpha1.tasks_pb2.WaitRequest(task_id=self._task_id),
                        metadata=IMPORT_reboot_aio_headers.Headers(
                            state_ref=IMPORT_reboot_aio_types.StateRef(self._task_id.state_ref),
                            application_id=self._application_id,
                        ).to_grpc_metadata(),
                        aborted_type=IMPORT_reboot.aio.aborted.SystemAborted,
                    )

                    wait_for_task_response = await call
                except IMPORT_reboot.aio.aborted.SystemAborted as error:
                    if error.code == IMPORT_grpc.StatusCode.NOT_FOUND:
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.UnknownTask()
                        ) from None

                    raise
                else:
                    response_or_error: IMPORT_typing.Optional[IMPORT_google_protobuf_any_pb2.Any] = None
                    is_error = False

                    if wait_for_task_response.response_or_error.WhichOneof("response_or_error") == "response":
                        response_or_error = wait_for_task_response.response_or_error.response
                    else:
                        is_error = True
                        response_or_error = wait_for_task_response.response_or_error.error

                    assert response_or_error is not None
                    assert response_or_error.TypeName() != ""

                    response = tests.reboot.greeter_pb2.StoreRecursiveMessageResponse()

                    if (
                        not is_error and response_or_error.TypeName() != response.DESCRIPTOR.full_name
                    ):
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.InvalidArgument(),
                            message=
                            f"task with UUID {IMPORT_uuid.UUID(bytes=self._task_id.task_uuid)} "
                            f"has a response of type '{response_or_error.TypeName()}' "
                            "but expecting type 'tests.reboot.greeter_pb2.StoreRecursiveMessageResponse'; "
                            "are you waiting on a task of the correct method?",
                        ) from None

                    if is_error:
                        aborted_type = Greeter.StoreRecursiveMessageAborted

                        # In Reboot >= 0.40.2 we expect the error to be a `google.rpc.Status`.
                        if response_or_error.Is(IMPORT_google_rpc_status_pb2.Status.DESCRIPTOR):
                            status = IMPORT_google_rpc_status_pb2.Status()
                            response_or_error.Unpack(status)
                            raise aborted_type.from_status(status)

                        # In Reboot < 0.40.2 workflows throwing declared errors behaved poorly;
                        # we don't aim to emulate its behavior. Indicate that we don't know the
                        # reason for the abort.
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                        )

                    else:
                        response_or_error.Unpack(response)
                        return GreeterStoreRecursiveMessageResponseFromProto(response)

            return wait_for_task().__await__()

    StoreRecursiveMessageEffects: IMPORT_typing.TypeAlias = GreeterBaseServicer.StoreRecursiveMessageEffects

    class StoreRecursiveMessageAborted(IMPORT_reboot.aio.aborted.Aborted):


        Error = IMPORT_typing.Union[
            IMPORT_reboot.aio.aborted.GrpcError,
            IMPORT_reboot.aio.aborted.RebootError,
        ]

        METHOD_PROTOBUF_ERROR_TYPES: list[type[IMPORT_google_protobuf_message.Message]] = [
        ]

        PROTOBUF_ERROR_TYPES: list[type[IMPORT_google_protobuf_message.Message]] = (
            METHOD_PROTOBUF_ERROR_TYPES +
            IMPORT_reboot.aio.aborted.GRPC_ERROR_TYPES +
            IMPORT_reboot.aio.aborted.REBOOT_ERROR_TYPES
        )

        _error: Error

        MethodProtobufError = IMPORT_typing.Union[
            IMPORT_reboot.aio.aborted.GrpcError,
            IMPORT_reboot.aio.aborted.RebootError,
        ]
        _method_protobuf_error: MethodProtobufError

        _code: IMPORT_grpc.StatusCode
        _message: IMPORT_typing.Optional[str]

        def __init__(
            self,
            error:  IMPORT_reboot.aio.aborted.GrpcError,
            *,
            message: IMPORT_typing.Optional[str] = None,
            # Do not set this value when constructing in order to
            # raise. This is only used internally when constructing
            # from aborted calls.
            error_types: IMPORT_typing.Sequence[type[MethodProtobufError]] = (
                METHOD_PROTOBUF_ERROR_TYPES + IMPORT_reboot.aio.aborted.GRPC_ERROR_TYPES
            ),
        ):
            super().__init__()

            self._message = None

            self._error = error
            self._method_protobuf_error = error

            IMPORT_reboot_aio_types.assert_type(self._protobuf_error, error_types)

            code = self.grpc_status_code_from_error(self._protobuf_error)

            if code is None:
                # Must be a Reboot specific or declared method error.
                code = IMPORT_grpc.StatusCode.ABORTED

            self._code = code

            self._message = self._message if self._message is not None else message

        @property
        def error(self) -> Error :
            return self._error

        @property
        def _protobuf_error(self) -> MethodProtobufError:
            return self._method_protobuf_error

        @property
        def code(self) -> IMPORT_grpc.StatusCode:
            return self._code

        @property
        def message(self) -> IMPORT_typing.Optional[str]:
            return self._message

        @classmethod
        def from_status(cls, status: IMPORT_google_rpc_status_pb2.Status):
            error = cls.error_from_google_rpc_status_details(
                status,
                cls.PROTOBUF_ERROR_TYPES,
            )

            message = status.message if len(status.message) > 0 else None

            if error is not None:
                return cls(error, message=message, error_types=cls.PROTOBUF_ERROR_TYPES)

            error = cls.error_from_google_rpc_status_code(status)

            assert error is not None

            # TODO(benh): also consider getting the type names from
            # `status.details` and including that in `message` to make
            # debugging easier.

            return cls(error, message=message)

        @classmethod
        def from_grpc_aio_rpc_error(cls, aio_rpc_error: IMPORT_grpc.aio.AioRpcError):
            return cls(
                cls.error_from_grpc_aio_rpc_error(aio_rpc_error),
                message=aio_rpc_error.details(),
            )

        @classmethod
        def is_declared_error(cls, error: IMPORT_google_protobuf_message.Message) -> bool:
            return False

    class ReadRecursiveMessageTask:
        """Represents a scheduled task running for the
        state. Note that this is not a coroutine because we are trying
        to convey the semantics that the task is already running (or
        will soon be).
        """

        @classmethod
        def retrieve(
            cls,
            context: IMPORT_reboot_aio_contexts.Context | IMPORT_reboot_aio_external.ExternalContext,
            *,
            task_id: IMPORT_rbt_v1alpha1.tasks_pb2.TaskId,
        ):
            return cls(context, task_id=task_id)

        def __init__(
            self,
            context: IMPORT_reboot_aio_contexts.Context | IMPORT_reboot_aio_external.ExternalContext,
            *,
            task_id: IMPORT_rbt_v1alpha1.tasks_pb2.TaskId,
        ) -> None:
            # Depending on the context type (inside or outside a Reboot application)
            # we may or may not know the application ID. If we don't know it, then
            # the `ExternalContext.gateway` will determine it.
            #
            # TODO: in the future we expect to support cross-application calls, in
            #       which case the developer may explicitly pass in an application ID
            #       here.
            self._application_id: IMPORT_typing.Optional[IMPORT_reboot_aio_types.ApplicationId] = None
            if isinstance(context, IMPORT_reboot_aio_contexts.Context):
                self._application_id = context.application_id
            self._channel_manager = context.channel_manager
            self._task_id = task_id

        @property
        def task_id(self) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
            return self._task_id

        def __await__(self) -> IMPORT_typing.Generator[None, None, Greeter.ReadRecursiveMessageResponse]:
            """Awaits for task to finish and returns its response."""
            async def wait_for_task() -> tests.reboot.greeter_pb2.ReadRecursiveMessageResponse:
                channel = self._channel_manager.get_channel_to_state(
                    IMPORT_reboot_aio_types.StateTypeName(self._task_id.state_type),
                    IMPORT_reboot_aio_types.StateRef(self._task_id.state_ref),
                )

                stub = IMPORT_rbt_v1alpha1.tasks_pb2_grpc.TasksStub(channel)

                try:
                    call = IMPORT_reboot_aio_stubs.UnaryRetriedCall(
                        call=None,  # `RetriedCall` can create the call itself.
                        stub_method=stub.Wait,
                        method_name="Wait",
                        request=IMPORT_rbt_v1alpha1.tasks_pb2.WaitRequest(task_id=self._task_id),
                        metadata=IMPORT_reboot_aio_headers.Headers(
                            state_ref=IMPORT_reboot_aio_types.StateRef(self._task_id.state_ref),
                            application_id=self._application_id,
                        ).to_grpc_metadata(),
                        aborted_type=IMPORT_reboot.aio.aborted.SystemAborted,
                    )

                    wait_for_task_response = await call
                except IMPORT_reboot.aio.aborted.SystemAborted as error:
                    if error.code == IMPORT_grpc.StatusCode.NOT_FOUND:
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.UnknownTask()
                        ) from None

                    raise
                else:
                    response_or_error: IMPORT_typing.Optional[IMPORT_google_protobuf_any_pb2.Any] = None
                    is_error = False

                    if wait_for_task_response.response_or_error.WhichOneof("response_or_error") == "response":
                        response_or_error = wait_for_task_response.response_or_error.response
                    else:
                        is_error = True
                        response_or_error = wait_for_task_response.response_or_error.error

                    assert response_or_error is not None
                    assert response_or_error.TypeName() != ""

                    response = tests.reboot.greeter_pb2.ReadRecursiveMessageResponse()

                    if (
                        not is_error and response_or_error.TypeName() != response.DESCRIPTOR.full_name
                    ):
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.InvalidArgument(),
                            message=
                            f"task with UUID {IMPORT_uuid.UUID(bytes=self._task_id.task_uuid)} "
                            f"has a response of type '{response_or_error.TypeName()}' "
                            "but expecting type 'tests.reboot.greeter_pb2.ReadRecursiveMessageResponse'; "
                            "are you waiting on a task of the correct method?",
                        ) from None

                    if is_error:
                        aborted_type = Greeter.ReadRecursiveMessageAborted

                        # In Reboot >= 0.40.2 we expect the error to be a `google.rpc.Status`.
                        if response_or_error.Is(IMPORT_google_rpc_status_pb2.Status.DESCRIPTOR):
                            status = IMPORT_google_rpc_status_pb2.Status()
                            response_or_error.Unpack(status)
                            raise aborted_type.from_status(status)

                        # In Reboot < 0.40.2 workflows throwing declared errors behaved poorly;
                        # we don't aim to emulate its behavior. Indicate that we don't know the
                        # reason for the abort.
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                        )

                    else:
                        response_or_error.Unpack(response)
                        return GreeterReadRecursiveMessageResponseFromProto(response)

            return wait_for_task().__await__()


    class ReadRecursiveMessageAborted(IMPORT_reboot.aio.aborted.Aborted):


        Error = IMPORT_typing.Union[
            IMPORT_reboot.aio.aborted.GrpcError,
            IMPORT_reboot.aio.aborted.RebootError,
        ]

        METHOD_PROTOBUF_ERROR_TYPES: list[type[IMPORT_google_protobuf_message.Message]] = [
        ]

        PROTOBUF_ERROR_TYPES: list[type[IMPORT_google_protobuf_message.Message]] = (
            METHOD_PROTOBUF_ERROR_TYPES +
            IMPORT_reboot.aio.aborted.GRPC_ERROR_TYPES +
            IMPORT_reboot.aio.aborted.REBOOT_ERROR_TYPES
        )

        _error: Error

        MethodProtobufError = IMPORT_typing.Union[
            IMPORT_reboot.aio.aborted.GrpcError,
            IMPORT_reboot.aio.aborted.RebootError,
        ]
        _method_protobuf_error: MethodProtobufError

        _code: IMPORT_grpc.StatusCode
        _message: IMPORT_typing.Optional[str]

        def __init__(
            self,
            error:  IMPORT_reboot.aio.aborted.GrpcError,
            *,
            message: IMPORT_typing.Optional[str] = None,
            # Do not set this value when constructing in order to
            # raise. This is only used internally when constructing
            # from aborted calls.
            error_types: IMPORT_typing.Sequence[type[MethodProtobufError]] = (
                METHOD_PROTOBUF_ERROR_TYPES + IMPORT_reboot.aio.aborted.GRPC_ERROR_TYPES
            ),
        ):
            super().__init__()

            self._message = None

            self._error = error
            self._method_protobuf_error = error

            IMPORT_reboot_aio_types.assert_type(self._protobuf_error, error_types)

            code = self.grpc_status_code_from_error(self._protobuf_error)

            if code is None:
                # Must be a Reboot specific or declared method error.
                code = IMPORT_grpc.StatusCode.ABORTED

            self._code = code

            self._message = self._message if self._message is not None else message

        @property
        def error(self) -> Error :
            return self._error

        @property
        def _protobuf_error(self) -> MethodProtobufError:
            return self._method_protobuf_error

        @property
        def code(self) -> IMPORT_grpc.StatusCode:
            return self._code

        @property
        def message(self) -> IMPORT_typing.Optional[str]:
            return self._message

        @classmethod
        def from_status(cls, status: IMPORT_google_rpc_status_pb2.Status):
            error = cls.error_from_google_rpc_status_details(
                status,
                cls.PROTOBUF_ERROR_TYPES,
            )

            message = status.message if len(status.message) > 0 else None

            if error is not None:
                return cls(error, message=message, error_types=cls.PROTOBUF_ERROR_TYPES)

            error = cls.error_from_google_rpc_status_code(status)

            assert error is not None

            # TODO(benh): also consider getting the type names from
            # `status.details` and including that in `message` to make
            # debugging easier.

            return cls(error, message=message)

        @classmethod
        def from_grpc_aio_rpc_error(cls, aio_rpc_error: IMPORT_grpc.aio.AioRpcError):
            return cls(
                cls.error_from_grpc_aio_rpc_error(aio_rpc_error),
                message=aio_rpc_error.details(),
            )

        @classmethod
        def is_declared_error(cls, error: IMPORT_google_protobuf_message.Message) -> bool:
            return False

    class ConstructAndStoreRecursiveMessageTask:
        """Represents a scheduled task running for the
        state. Note that this is not a coroutine because we are trying
        to convey the semantics that the task is already running (or
        will soon be).
        """

        @classmethod
        def retrieve(
            cls,
            context: IMPORT_reboot_aio_contexts.Context | IMPORT_reboot_aio_external.ExternalContext,
            *,
            task_id: IMPORT_rbt_v1alpha1.tasks_pb2.TaskId,
        ):
            return cls(context, task_id=task_id)

        def __init__(
            self,
            context: IMPORT_reboot_aio_contexts.Context | IMPORT_reboot_aio_external.ExternalContext,
            *,
            task_id: IMPORT_rbt_v1alpha1.tasks_pb2.TaskId,
        ) -> None:
            # Depending on the context type (inside or outside a Reboot application)
            # we may or may not know the application ID. If we don't know it, then
            # the `ExternalContext.gateway` will determine it.
            #
            # TODO: in the future we expect to support cross-application calls, in
            #       which case the developer may explicitly pass in an application ID
            #       here.
            self._application_id: IMPORT_typing.Optional[IMPORT_reboot_aio_types.ApplicationId] = None
            if isinstance(context, IMPORT_reboot_aio_contexts.Context):
                self._application_id = context.application_id
            self._channel_manager = context.channel_manager
            self._task_id = task_id

        @property
        def task_id(self) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
            return self._task_id

        def __await__(self) -> IMPORT_typing.Generator[None, None, Greeter.ConstructAndStoreRecursiveMessageResponse]:
            """Awaits for task to finish and returns its response."""
            async def wait_for_task() -> tests.reboot.greeter_pb2.ConstructAndStoreRecursiveMessageResponse:
                channel = self._channel_manager.get_channel_to_state(
                    IMPORT_reboot_aio_types.StateTypeName(self._task_id.state_type),
                    IMPORT_reboot_aio_types.StateRef(self._task_id.state_ref),
                )

                stub = IMPORT_rbt_v1alpha1.tasks_pb2_grpc.TasksStub(channel)

                try:
                    call = IMPORT_reboot_aio_stubs.UnaryRetriedCall(
                        call=None,  # `RetriedCall` can create the call itself.
                        stub_method=stub.Wait,
                        method_name="Wait",
                        request=IMPORT_rbt_v1alpha1.tasks_pb2.WaitRequest(task_id=self._task_id),
                        metadata=IMPORT_reboot_aio_headers.Headers(
                            state_ref=IMPORT_reboot_aio_types.StateRef(self._task_id.state_ref),
                            application_id=self._application_id,
                        ).to_grpc_metadata(),
                        aborted_type=IMPORT_reboot.aio.aborted.SystemAborted,
                    )

                    wait_for_task_response = await call
                except IMPORT_reboot.aio.aborted.SystemAborted as error:
                    if error.code == IMPORT_grpc.StatusCode.NOT_FOUND:
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.UnknownTask()
                        ) from None

                    raise
                else:
                    response_or_error: IMPORT_typing.Optional[IMPORT_google_protobuf_any_pb2.Any] = None
                    is_error = False

                    if wait_for_task_response.response_or_error.WhichOneof("response_or_error") == "response":
                        response_or_error = wait_for_task_response.response_or_error.response
                    else:
                        is_error = True
                        response_or_error = wait_for_task_response.response_or_error.error

                    assert response_or_error is not None
                    assert response_or_error.TypeName() != ""

                    response = tests.reboot.greeter_pb2.ConstructAndStoreRecursiveMessageResponse()

                    if (
                        not is_error and response_or_error.TypeName() != response.DESCRIPTOR.full_name
                    ):
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.InvalidArgument(),
                            message=
                            f"task with UUID {IMPORT_uuid.UUID(bytes=self._task_id.task_uuid)} "
                            f"has a response of type '{response_or_error.TypeName()}' "
                            "but expecting type 'tests.reboot.greeter_pb2.ConstructAndStoreRecursiveMessageResponse'; "
                            "are you waiting on a task of the correct method?",
                        ) from None

                    if is_error:
                        aborted_type = Greeter.ConstructAndStoreRecursiveMessageAborted

                        # In Reboot >= 0.40.2 we expect the error to be a `google.rpc.Status`.
                        if response_or_error.Is(IMPORT_google_rpc_status_pb2.Status.DESCRIPTOR):
                            status = IMPORT_google_rpc_status_pb2.Status()
                            response_or_error.Unpack(status)
                            raise aborted_type.from_status(status)

                        # In Reboot < 0.40.2 workflows throwing declared errors behaved poorly;
                        # we don't aim to emulate its behavior. Indicate that we don't know the
                        # reason for the abort.
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                        )

                    else:
                        response_or_error.Unpack(response)
                        return GreeterConstructAndStoreRecursiveMessageResponseFromProto(response)

            return wait_for_task().__await__()


    class ConstructAndStoreRecursiveMessageAborted(IMPORT_reboot.aio.aborted.Aborted):


        Error = IMPORT_typing.Union[
            IMPORT_reboot.aio.aborted.GrpcError,
            IMPORT_reboot.aio.aborted.RebootError,
        ]

        METHOD_PROTOBUF_ERROR_TYPES: list[type[IMPORT_google_protobuf_message.Message]] = [
        ]

        PROTOBUF_ERROR_TYPES: list[type[IMPORT_google_protobuf_message.Message]] = (
            METHOD_PROTOBUF_ERROR_TYPES +
            IMPORT_reboot.aio.aborted.GRPC_ERROR_TYPES +
            IMPORT_reboot.aio.aborted.REBOOT_ERROR_TYPES
        )

        _error: Error

        MethodProtobufError = IMPORT_typing.Union[
            IMPORT_reboot.aio.aborted.GrpcError,
            IMPORT_reboot.aio.aborted.RebootError,
        ]
        _method_protobuf_error: MethodProtobufError

        _code: IMPORT_grpc.StatusCode
        _message: IMPORT_typing.Optional[str]

        def __init__(
            self,
            error:  IMPORT_reboot.aio.aborted.GrpcError,
            *,
            message: IMPORT_typing.Optional[str] = None,
            # Do not set this value when constructing in order to
            # raise. This is only used internally when constructing
            # from aborted calls.
            error_types: IMPORT_typing.Sequence[type[MethodProtobufError]] = (
                METHOD_PROTOBUF_ERROR_TYPES + IMPORT_reboot.aio.aborted.GRPC_ERROR_TYPES
            ),
        ):
            super().__init__()

            self._message = None

            self._error = error
            self._method_protobuf_error = error

            IMPORT_reboot_aio_types.assert_type(self._protobuf_error, error_types)

            code = self.grpc_status_code_from_error(self._protobuf_error)

            if code is None:
                # Must be a Reboot specific or declared method error.
                code = IMPORT_grpc.StatusCode.ABORTED

            self._code = code

            self._message = self._message if self._message is not None else message

        @property
        def error(self) -> Error :
            return self._error

        @property
        def _protobuf_error(self) -> MethodProtobufError:
            return self._method_protobuf_error

        @property
        def code(self) -> IMPORT_grpc.StatusCode:
            return self._code

        @property
        def message(self) -> IMPORT_typing.Optional[str]:
            return self._message

        @classmethod
        def from_status(cls, status: IMPORT_google_rpc_status_pb2.Status):
            error = cls.error_from_google_rpc_status_details(
                status,
                cls.PROTOBUF_ERROR_TYPES,
            )

            message = status.message if len(status.message) > 0 else None

            if error is not None:
                return cls(error, message=message, error_types=cls.PROTOBUF_ERROR_TYPES)

            error = cls.error_from_google_rpc_status_code(status)

            assert error is not None

            # TODO(benh): also consider getting the type names from
            # `status.details` and including that in `message` to make
            # debugging easier.

            return cls(error, message=message)

        @classmethod
        def from_grpc_aio_rpc_error(cls, aio_rpc_error: IMPORT_grpc.aio.AioRpcError):
            return cls(
                cls.error_from_grpc_aio_rpc_error(aio_rpc_error),
                message=aio_rpc_error.details(),
            )

        @classmethod
        def is_declared_error(cls, error: IMPORT_google_protobuf_message.Message) -> bool:
            return False


    class WeakReference(IMPORT_typing.Generic[Greeter_ScheduleTypeVar]):

        _schedule_type: type[Greeter_ScheduleTypeVar]

        def __init__(
            self,
            # When application ID is None, refers to a state within the application given by the context.
            application_id: IMPORT_typing.Optional[IMPORT_reboot_aio_types.ApplicationId],
            state_id: IMPORT_reboot_aio_types.StateId,
            *,
            schedule_type: type[Greeter_ScheduleTypeVar],
            bearer_token: IMPORT_typing.Optional[str] = None,
            servicer: IMPORT_typing.Optional[GreeterBaseServicer] = None,
        ):
            self._application_id = application_id
            self._state_ref = IMPORT_reboot_aio_types.StateRef.from_id(
              Greeter.__state_type_name__,
              state_id,
            )
            self._schedule_type = schedule_type
            self._idempotency_manager: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.IdempotencyManager] = None
            self._reader_stub: IMPORT_typing.Optional[GreeterReaderStub] = None
            self._writer_stub: IMPORT_typing.Optional[GreeterWriterStub] = None
            self._workflow_stub: IMPORT_typing.Optional[GreeterWorkflowStub] = None
            self._tasks_stub: IMPORT_typing.Optional[GreeterTasksStub] = None
            self._bearer_token = bearer_token
            self._servicer = servicer

        @property
        def state_id(self) -> IMPORT_reboot_aio_types.StateId:
            return self._state_ref.id

        def _reader(
            self,
            context: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
        ) -> GreeterReaderStub:
            if self._reader_stub is None:
                self._reader_stub = GreeterReaderStub(
                    context=context,
                    state_ref=self._state_ref,
                    bearer_token=self._bearer_token,
                )
            assert self._reader_stub is not None
            if self._idempotency_manager is None:
                self._idempotency_manager = context
            elif self._idempotency_manager != context:
                raise IMPORT_reboot_aio_call.MixedContextsError(
                    "This `WeakReference` for `Greeter` with ID "
                    f"'{self.state_id}' has previously been used by a "
                    "different `Context`. That is not allowed. "
                    "Instead create a new `WeakReference` for every `Context` by calling "
                    f"`Greeter.ref('{self.state_id}')`."
                )
            return self._reader_stub

        def _writer(
            self,
            context: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
        ) -> GreeterWriterStub:
            if self._writer_stub is None:
                self._writer_stub = GreeterWriterStub(
                    context=context,
                    state_ref=self._state_ref,
                    bearer_token=self._bearer_token,
                )
            assert self._writer_stub is not None
            if self._idempotency_manager is None:
                self._idempotency_manager = context
            elif self._idempotency_manager != context:
                raise IMPORT_reboot_aio_call.MixedContextsError(
                    "This `WeakReference` for `Greeter` with ID "
                    f"'{self.state_id}' has previously been used by a "
                    "different `Context`. That is not allowed. "
                    "Instead create a new `WeakReference` for every `Context` by calling "
                    f"`Greeter.ref('{self.state_id}')`."
                )
            return self._writer_stub

        def _workflow(
            self,
            context: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
        ) -> GreeterWorkflowStub:
            if self._workflow_stub is None:
                self._workflow_stub = GreeterWorkflowStub(
                    context=context,
                    state_ref=self._state_ref,
                    bearer_token=self._bearer_token,
                )
            assert self._workflow_stub is not None
            if self._idempotency_manager is None:
                self._idempotency_manager = context
            elif self._idempotency_manager != context:
                raise IMPORT_reboot_aio_call.MixedContextsError(
                    "This `WeakReference` for `Greeter` with ID "
                    f"'{self.state_id}' has previously been used by a "
                    "different `Context`. That is not allowed. "
                    "Instead create a new `WeakReference` for every `Context` by calling "
                    f"`Greeter.ref('{self.state_id}')`."
                )
            return self._workflow_stub

        def _tasks(
            self,
            context: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
        ) -> GreeterTasksStub:
            if self._tasks_stub is None:
                self._tasks_stub = GreeterTasksStub(
                    context=context,
                    state_ref=self._state_ref,
                    bearer_token=self._bearer_token,
                )
            assert self._tasks_stub is not None
            if self._idempotency_manager is None:
                self._idempotency_manager = context
            elif self._idempotency_manager != context:
                raise IMPORT_reboot_aio_call.MixedContextsError(
                    "This `WeakReference` for `Greeter` with ID "
                    f"'{self.state_id}' has previously been used by a "
                    "different `Context`. That is not allowed. "
                    "Instead create a new `WeakReference` for every `Context` by calling "
                    f"`Greeter.ref('{self.state_id}')`."
                )
            return self._tasks_stub

        class _Reactively:

            def __init__(
                self,
                *,
                application_id: IMPORT_typing.Optional[IMPORT_reboot_aio_types.ApplicationId],
                state_ref: IMPORT_reboot_aio_types.StateRef,
                bearer_token: IMPORT_typing.Optional[str] = None,
            ):
                self._application_id = application_id
                self._state_ref = state_ref
                self._bearer_token = bearer_token

            @IMPORT_typing.overload
            async def Greet(
                __this__,
                __context__: IMPORT_reboot_aio_external.ExternalContext | IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WorkflowContext,
                __request_or_options__: Greeter.GreetRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_typing.AsyncIterator[Greeter.GreetResponse]:
                ...

            @IMPORT_typing.overload
            async def Greet(
                __this__,
                __context__: IMPORT_reboot_aio_external.ExternalContext | IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WorkflowContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                name: IMPORT_typing.Optional[str] | Unset = UNSET,
                metadata: IMPORT_typing.Optional[dict[str, str]] | Unset = UNSET,
            ) -> IMPORT_typing.AsyncIterator[Greeter.GreetResponse]:
                ...

            async def Greet( # type: ignore[misc]
                # In methods which are dealing with user input, (i.e.,
                # proto message field names), we should use '__double_underscored__'
                # variables to avoid any potential name conflicts with the method's
                # parameters.
                # The '__self__' parameter is a convention in Python to
                # indicate that this method is a bound method, so we use
                # '__this__' instead.
                __this__,
                # Explicitly-reactive calls only make sense in the context of either...
                # (A) an external client, or...
                # (B) methods that may reasonably run for a long time, which in Reboot means: readers or workflows.
                __context__: IMPORT_reboot_aio_external.ExternalContext | IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WorkflowContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.GreetRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                name: IMPORT_typing.Optional[str] | Unset = UNSET,
                metadata: IMPORT_typing.Optional[dict[str, str]] | Unset = UNSET,
            ) -> IMPORT_typing.AsyncIterator[Greeter.GreetResponse]:
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_external.ExternalContext, IMPORT_reboot_aio_contexts.ReaderContext, IMPORT_reboot_aio_contexts.WorkflowContext])
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.GreetRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.GreetRequest)

                __request__: IMPORT_typing.Optional[Greeter.GreetRequest] = None

                if isinstance(__request_or_options__, Greeter.GreetRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)
                    __request__ = __request_or_options__

                    assert name is None
                    assert metadata is None
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = GreeterGreetRequestFromInputFields(
                        name=name,
                        metadata=metadata,
                    )

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __bearer_token__: IMPORT_typing.Optional[str] = None
                __caller_id__: IMPORT_typing.Optional[IMPORT_reboot_aio_caller_id.CallerID] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                if __bearer_token__ is None:
                    __bearer_token__ = __this__._bearer_token
                if __bearer_token__ is None and isinstance(__context__, IMPORT_reboot_aio_external.ExternalContext):
                    # Within a Reboot context we do not pass on the caller's bearer token, as that might
                    # have security implications - we cannot simply trust any service we are calling with
                    # the user's credentials. Instead, the developer can rely on the default app-internal
                    # auth, or override that and set an explicit bearer token.
                    #
                    # In the case of `ExternalContext`, however, its `bearer_token` was set specifically
                    # by the developer for the purpose of making these calls. Note that only
                    # `ExternalContext` even has a `bearer_token` field.
                    __bearer_token__ = __context__.bearer_token

                if __metadata__ is None:
                    __metadata__ = ()

                if isinstance(__context__, IMPORT_reboot_aio_contexts.Context):
                    __caller_id__ = IMPORT_reboot_aio_caller_id.CallerID(
                        application_id=__context__.application_id,
                    )
                    if __this__._application_id is None:
                        # Given our context type (inside a Reboot application) we can default to
                        # making the application send traffic to itself.
                        __this__._application_id = __context__.application_id
                elif isinstance(__context__, IMPORT_reboot_aio_external.ExternalContext):
                    __caller_id__ = __context__.caller_id

                __headers__ = IMPORT_reboot_aio_headers.Headers(
                    bearer_token=__bearer_token__,
                    state_ref=__this__._state_ref,
                    application_id=__this__._application_id,
                    caller_id=__caller_id__,
                )

                __metadata__ += __headers__.to_grpc_metadata()

                __query_backoff__ = IMPORT_reboot_aio_backoff.Backoff()
                while True:
                    __call__ = None
                    try:
                        async with __context__.channel_manager.get_channel_to_state(
                            IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                            __this__._state_ref,
                        ) as __channel__:

                            __call__ = IMPORT_rbt_v1alpha1.react_pb2_grpc.ReactStub(
                                __channel__
                            ).Query(
                                IMPORT_rbt_v1alpha1.react_pb2.QueryRequest(
                                    method='Greet',
                                    request=GreeterGreetRequestToProto(
                                        __request__
                                    ).SerializeToString(),
                                ),
                                metadata=__metadata__,
                            )

                            async for __query_response__ in __call__:
                                # Clear the backoff so we don't wait
                                # as long the next time we get
                                # disconnected.
                                __query_backoff__.clear()

                                # The backend may have sent us this query
                                # response only to let us know that a new
                                # idempotency key has been recorded; there may
                                # not be a new response. Python callers don't
                                # (currently) care about such an event, so we
                                # simply ignore it.
                                if not __query_response__.HasField("response"):
                                    continue

                                __response__ = tests.reboot.greeter_pb2.GreetResponse()
                                __response__.ParseFromString(__query_response__.response)
                                yield GreeterGreetResponseFromProto(__response__)

                    except IMPORT_grpc.aio.AioRpcError as error:
                        # We expect to get disconnected from the server
                        # from time to time, e.g., when it is being
                        # updated, but we don't want that error to
                        # propagate, we just want to retry.
                        if IMPORT_reboot.aio.aborted.is_grpc_retryable_exception(error):
                            logger.debug(
                                "Reactive read to 'tests.reboot.GreeterMethods.Greet' "
                                f"failed with a retryable error: '{error}'; "
                                "will retry..."
                            )
                            await __query_backoff__()
                            continue
                        if error.code() == IMPORT_grpc.StatusCode.ABORTED:
                            # Reconstitute the error that the server threw, if it was a declared error.
                            status = await IMPORT_rpc_status_async.from_call(__call__)
                            if status is not None:
                                raise Greeter.GreetAborted.from_status(
                                    status
                                ) from None
                            raise Greeter.GreetAborted.from_grpc_aio_rpc_error(
                                error
                            ) from None

                        raise

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            greet = Greet
            @IMPORT_typing.overload
            async def TryToConstructContext(
                __this__,
                __context__: IMPORT_reboot_aio_external.ExternalContext | IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WorkflowContext,
                __request_or_options__: Greeter.TryToConstructContextRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_typing.AsyncIterator[Greeter.TryToConstructContextResponse]:
                ...

            @IMPORT_typing.overload
            async def TryToConstructContext(
                __this__,
                __context__: IMPORT_reboot_aio_external.ExternalContext | IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WorkflowContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_typing.AsyncIterator[Greeter.TryToConstructContextResponse]:
                ...

            async def TryToConstructContext( # type: ignore[misc]
                # In methods which are dealing with user input, (i.e.,
                # proto message field names), we should use '__double_underscored__'
                # variables to avoid any potential name conflicts with the method's
                # parameters.
                # The '__self__' parameter is a convention in Python to
                # indicate that this method is a bound method, so we use
                # '__this__' instead.
                __this__,
                # Explicitly-reactive calls only make sense in the context of either...
                # (A) an external client, or...
                # (B) methods that may reasonably run for a long time, which in Reboot means: readers or workflows.
                __context__: IMPORT_reboot_aio_external.ExternalContext | IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WorkflowContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.TryToConstructContextRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_typing.AsyncIterator[Greeter.TryToConstructContextResponse]:
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_external.ExternalContext, IMPORT_reboot_aio_contexts.ReaderContext, IMPORT_reboot_aio_contexts.WorkflowContext])
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.TryToConstructContextRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.TryToConstructContextRequest)

                __request__: IMPORT_typing.Optional[Greeter.TryToConstructContextRequest] = None

                if isinstance(__request_or_options__, Greeter.TryToConstructContextRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)
                    __request__ = __request_or_options__

                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = GreeterTryToConstructContextRequestFromInputFields(
                    )

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __bearer_token__: IMPORT_typing.Optional[str] = None
                __caller_id__: IMPORT_typing.Optional[IMPORT_reboot_aio_caller_id.CallerID] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                if __bearer_token__ is None:
                    __bearer_token__ = __this__._bearer_token
                if __bearer_token__ is None and isinstance(__context__, IMPORT_reboot_aio_external.ExternalContext):
                    # Within a Reboot context we do not pass on the caller's bearer token, as that might
                    # have security implications - we cannot simply trust any service we are calling with
                    # the user's credentials. Instead, the developer can rely on the default app-internal
                    # auth, or override that and set an explicit bearer token.
                    #
                    # In the case of `ExternalContext`, however, its `bearer_token` was set specifically
                    # by the developer for the purpose of making these calls. Note that only
                    # `ExternalContext` even has a `bearer_token` field.
                    __bearer_token__ = __context__.bearer_token

                if __metadata__ is None:
                    __metadata__ = ()

                if isinstance(__context__, IMPORT_reboot_aio_contexts.Context):
                    __caller_id__ = IMPORT_reboot_aio_caller_id.CallerID(
                        application_id=__context__.application_id,
                    )
                    if __this__._application_id is None:
                        # Given our context type (inside a Reboot application) we can default to
                        # making the application send traffic to itself.
                        __this__._application_id = __context__.application_id
                elif isinstance(__context__, IMPORT_reboot_aio_external.ExternalContext):
                    __caller_id__ = __context__.caller_id

                __headers__ = IMPORT_reboot_aio_headers.Headers(
                    bearer_token=__bearer_token__,
                    state_ref=__this__._state_ref,
                    application_id=__this__._application_id,
                    caller_id=__caller_id__,
                )

                __metadata__ += __headers__.to_grpc_metadata()

                __query_backoff__ = IMPORT_reboot_aio_backoff.Backoff()
                while True:
                    __call__ = None
                    try:
                        async with __context__.channel_manager.get_channel_to_state(
                            IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                            __this__._state_ref,
                        ) as __channel__:

                            __call__ = IMPORT_rbt_v1alpha1.react_pb2_grpc.ReactStub(
                                __channel__
                            ).Query(
                                IMPORT_rbt_v1alpha1.react_pb2.QueryRequest(
                                    method='TryToConstructContext',
                                    request=GreeterTryToConstructContextRequestToProto(
                                        __request__
                                    ).SerializeToString(),
                                ),
                                metadata=__metadata__,
                            )

                            async for __query_response__ in __call__:
                                # Clear the backoff so we don't wait
                                # as long the next time we get
                                # disconnected.
                                __query_backoff__.clear()

                                # The backend may have sent us this query
                                # response only to let us know that a new
                                # idempotency key has been recorded; there may
                                # not be a new response. Python callers don't
                                # (currently) care about such an event, so we
                                # simply ignore it.
                                if not __query_response__.HasField("response"):
                                    continue

                                __response__ = google.protobuf.empty_pb2.Empty()
                                __response__.ParseFromString(__query_response__.response)
                                yield GreeterTryToConstructContextResponseFromProto(__response__)

                    except IMPORT_grpc.aio.AioRpcError as error:
                        # We expect to get disconnected from the server
                        # from time to time, e.g., when it is being
                        # updated, but we don't want that error to
                        # propagate, we just want to retry.
                        if IMPORT_reboot.aio.aborted.is_grpc_retryable_exception(error):
                            logger.debug(
                                "Reactive read to 'tests.reboot.GreeterMethods.TryToConstructContext' "
                                f"failed with a retryable error: '{error}'; "
                                "will retry..."
                            )
                            await __query_backoff__()
                            continue
                        if error.code() == IMPORT_grpc.StatusCode.ABORTED:
                            # Reconstitute the error that the server threw, if it was a declared error.
                            status = await IMPORT_rpc_status_async.from_call(__call__)
                            if status is not None:
                                raise Greeter.TryToConstructContextAborted.from_status(
                                    status
                                ) from None
                            raise Greeter.TryToConstructContextAborted.from_grpc_aio_rpc_error(
                                error
                            ) from None

                        raise

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            try_to_construct_context = TryToConstructContext
            @IMPORT_typing.overload
            async def TryToConstructExternalContext(
                __this__,
                __context__: IMPORT_reboot_aio_external.ExternalContext | IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WorkflowContext,
                __request_or_options__: Greeter.TryToConstructExternalContextRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_typing.AsyncIterator[Greeter.TryToConstructExternalContextResponse]:
                ...

            @IMPORT_typing.overload
            async def TryToConstructExternalContext(
                __this__,
                __context__: IMPORT_reboot_aio_external.ExternalContext | IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WorkflowContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_typing.AsyncIterator[Greeter.TryToConstructExternalContextResponse]:
                ...

            async def TryToConstructExternalContext( # type: ignore[misc]
                # In methods which are dealing with user input, (i.e.,
                # proto message field names), we should use '__double_underscored__'
                # variables to avoid any potential name conflicts with the method's
                # parameters.
                # The '__self__' parameter is a convention in Python to
                # indicate that this method is a bound method, so we use
                # '__this__' instead.
                __this__,
                # Explicitly-reactive calls only make sense in the context of either...
                # (A) an external client, or...
                # (B) methods that may reasonably run for a long time, which in Reboot means: readers or workflows.
                __context__: IMPORT_reboot_aio_external.ExternalContext | IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WorkflowContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.TryToConstructExternalContextRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_typing.AsyncIterator[Greeter.TryToConstructExternalContextResponse]:
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_external.ExternalContext, IMPORT_reboot_aio_contexts.ReaderContext, IMPORT_reboot_aio_contexts.WorkflowContext])
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.TryToConstructExternalContextRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.TryToConstructExternalContextRequest)

                __request__: IMPORT_typing.Optional[Greeter.TryToConstructExternalContextRequest] = None

                if isinstance(__request_or_options__, Greeter.TryToConstructExternalContextRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)
                    __request__ = __request_or_options__

                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = GreeterTryToConstructExternalContextRequestFromInputFields(
                    )

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __bearer_token__: IMPORT_typing.Optional[str] = None
                __caller_id__: IMPORT_typing.Optional[IMPORT_reboot_aio_caller_id.CallerID] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                if __bearer_token__ is None:
                    __bearer_token__ = __this__._bearer_token
                if __bearer_token__ is None and isinstance(__context__, IMPORT_reboot_aio_external.ExternalContext):
                    # Within a Reboot context we do not pass on the caller's bearer token, as that might
                    # have security implications - we cannot simply trust any service we are calling with
                    # the user's credentials. Instead, the developer can rely on the default app-internal
                    # auth, or override that and set an explicit bearer token.
                    #
                    # In the case of `ExternalContext`, however, its `bearer_token` was set specifically
                    # by the developer for the purpose of making these calls. Note that only
                    # `ExternalContext` even has a `bearer_token` field.
                    __bearer_token__ = __context__.bearer_token

                if __metadata__ is None:
                    __metadata__ = ()

                if isinstance(__context__, IMPORT_reboot_aio_contexts.Context):
                    __caller_id__ = IMPORT_reboot_aio_caller_id.CallerID(
                        application_id=__context__.application_id,
                    )
                    if __this__._application_id is None:
                        # Given our context type (inside a Reboot application) we can default to
                        # making the application send traffic to itself.
                        __this__._application_id = __context__.application_id
                elif isinstance(__context__, IMPORT_reboot_aio_external.ExternalContext):
                    __caller_id__ = __context__.caller_id

                __headers__ = IMPORT_reboot_aio_headers.Headers(
                    bearer_token=__bearer_token__,
                    state_ref=__this__._state_ref,
                    application_id=__this__._application_id,
                    caller_id=__caller_id__,
                )

                __metadata__ += __headers__.to_grpc_metadata()

                __query_backoff__ = IMPORT_reboot_aio_backoff.Backoff()
                while True:
                    __call__ = None
                    try:
                        async with __context__.channel_manager.get_channel_to_state(
                            IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                            __this__._state_ref,
                        ) as __channel__:

                            __call__ = IMPORT_rbt_v1alpha1.react_pb2_grpc.ReactStub(
                                __channel__
                            ).Query(
                                IMPORT_rbt_v1alpha1.react_pb2.QueryRequest(
                                    method='TryToConstructExternalContext',
                                    request=GreeterTryToConstructExternalContextRequestToProto(
                                        __request__
                                    ).SerializeToString(),
                                ),
                                metadata=__metadata__,
                            )

                            async for __query_response__ in __call__:
                                # Clear the backoff so we don't wait
                                # as long the next time we get
                                # disconnected.
                                __query_backoff__.clear()

                                # The backend may have sent us this query
                                # response only to let us know that a new
                                # idempotency key has been recorded; there may
                                # not be a new response. Python callers don't
                                # (currently) care about such an event, so we
                                # simply ignore it.
                                if not __query_response__.HasField("response"):
                                    continue

                                __response__ = google.protobuf.empty_pb2.Empty()
                                __response__.ParseFromString(__query_response__.response)
                                yield GreeterTryToConstructExternalContextResponseFromProto(__response__)

                    except IMPORT_grpc.aio.AioRpcError as error:
                        # We expect to get disconnected from the server
                        # from time to time, e.g., when it is being
                        # updated, but we don't want that error to
                        # propagate, we just want to retry.
                        if IMPORT_reboot.aio.aborted.is_grpc_retryable_exception(error):
                            logger.debug(
                                "Reactive read to 'tests.reboot.GreeterMethods.TryToConstructExternalContext' "
                                f"failed with a retryable error: '{error}'; "
                                "will retry..."
                            )
                            await __query_backoff__()
                            continue
                        if error.code() == IMPORT_grpc.StatusCode.ABORTED:
                            # Reconstitute the error that the server threw, if it was a declared error.
                            status = await IMPORT_rpc_status_async.from_call(__call__)
                            if status is not None:
                                raise Greeter.TryToConstructExternalContextAborted.from_status(
                                    status
                                ) from None
                            raise Greeter.TryToConstructExternalContextAborted.from_grpc_aio_rpc_error(
                                error
                            ) from None

                        raise

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            try_to_construct_external_context = TryToConstructExternalContext
            @IMPORT_typing.overload
            async def TestLongRunningFetch(
                __this__,
                __context__: IMPORT_reboot_aio_external.ExternalContext | IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WorkflowContext,
                __request_or_options__: Greeter.TestLongRunningFetchRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_typing.AsyncIterator[Greeter.TestLongRunningFetchResponse]:
                ...

            @IMPORT_typing.overload
            async def TestLongRunningFetch(
                __this__,
                __context__: IMPORT_reboot_aio_external.ExternalContext | IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WorkflowContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                sleep_time_seconds: IMPORT_typing.Optional[int] | Unset = UNSET,
            ) -> IMPORT_typing.AsyncIterator[Greeter.TestLongRunningFetchResponse]:
                ...

            async def TestLongRunningFetch( # type: ignore[misc]
                # In methods which are dealing with user input, (i.e.,
                # proto message field names), we should use '__double_underscored__'
                # variables to avoid any potential name conflicts with the method's
                # parameters.
                # The '__self__' parameter is a convention in Python to
                # indicate that this method is a bound method, so we use
                # '__this__' instead.
                __this__,
                # Explicitly-reactive calls only make sense in the context of either...
                # (A) an external client, or...
                # (B) methods that may reasonably run for a long time, which in Reboot means: readers or workflows.
                __context__: IMPORT_reboot_aio_external.ExternalContext | IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WorkflowContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.TestLongRunningFetchRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                sleep_time_seconds: IMPORT_typing.Optional[int] | Unset = UNSET,
            ) -> IMPORT_typing.AsyncIterator[Greeter.TestLongRunningFetchResponse]:
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_external.ExternalContext, IMPORT_reboot_aio_contexts.ReaderContext, IMPORT_reboot_aio_contexts.WorkflowContext])
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.TestLongRunningFetchRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.TestLongRunningFetchRequest)

                __request__: IMPORT_typing.Optional[Greeter.TestLongRunningFetchRequest] = None

                if isinstance(__request_or_options__, Greeter.TestLongRunningFetchRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)
                    __request__ = __request_or_options__

                    assert sleep_time_seconds is None
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = GreeterTestLongRunningFetchRequestFromInputFields(
                        sleep_time_seconds=sleep_time_seconds,
                    )

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __bearer_token__: IMPORT_typing.Optional[str] = None
                __caller_id__: IMPORT_typing.Optional[IMPORT_reboot_aio_caller_id.CallerID] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                if __bearer_token__ is None:
                    __bearer_token__ = __this__._bearer_token
                if __bearer_token__ is None and isinstance(__context__, IMPORT_reboot_aio_external.ExternalContext):
                    # Within a Reboot context we do not pass on the caller's bearer token, as that might
                    # have security implications - we cannot simply trust any service we are calling with
                    # the user's credentials. Instead, the developer can rely on the default app-internal
                    # auth, or override that and set an explicit bearer token.
                    #
                    # In the case of `ExternalContext`, however, its `bearer_token` was set specifically
                    # by the developer for the purpose of making these calls. Note that only
                    # `ExternalContext` even has a `bearer_token` field.
                    __bearer_token__ = __context__.bearer_token

                if __metadata__ is None:
                    __metadata__ = ()

                if isinstance(__context__, IMPORT_reboot_aio_contexts.Context):
                    __caller_id__ = IMPORT_reboot_aio_caller_id.CallerID(
                        application_id=__context__.application_id,
                    )
                    if __this__._application_id is None:
                        # Given our context type (inside a Reboot application) we can default to
                        # making the application send traffic to itself.
                        __this__._application_id = __context__.application_id
                elif isinstance(__context__, IMPORT_reboot_aio_external.ExternalContext):
                    __caller_id__ = __context__.caller_id

                __headers__ = IMPORT_reboot_aio_headers.Headers(
                    bearer_token=__bearer_token__,
                    state_ref=__this__._state_ref,
                    application_id=__this__._application_id,
                    caller_id=__caller_id__,
                )

                __metadata__ += __headers__.to_grpc_metadata()

                __query_backoff__ = IMPORT_reboot_aio_backoff.Backoff()
                while True:
                    __call__ = None
                    try:
                        async with __context__.channel_manager.get_channel_to_state(
                            IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                            __this__._state_ref,
                        ) as __channel__:

                            __call__ = IMPORT_rbt_v1alpha1.react_pb2_grpc.ReactStub(
                                __channel__
                            ).Query(
                                IMPORT_rbt_v1alpha1.react_pb2.QueryRequest(
                                    method='TestLongRunningFetch',
                                    request=GreeterTestLongRunningFetchRequestToProto(
                                        __request__
                                    ).SerializeToString(),
                                ),
                                metadata=__metadata__,
                            )

                            async for __query_response__ in __call__:
                                # Clear the backoff so we don't wait
                                # as long the next time we get
                                # disconnected.
                                __query_backoff__.clear()

                                # The backend may have sent us this query
                                # response only to let us know that a new
                                # idempotency key has been recorded; there may
                                # not be a new response. Python callers don't
                                # (currently) care about such an event, so we
                                # simply ignore it.
                                if not __query_response__.HasField("response"):
                                    continue

                                __response__ = google.protobuf.empty_pb2.Empty()
                                __response__.ParseFromString(__query_response__.response)
                                yield GreeterTestLongRunningFetchResponseFromProto(__response__)

                    except IMPORT_grpc.aio.AioRpcError as error:
                        # We expect to get disconnected from the server
                        # from time to time, e.g., when it is being
                        # updated, but we don't want that error to
                        # propagate, we just want to retry.
                        if IMPORT_reboot.aio.aborted.is_grpc_retryable_exception(error):
                            logger.debug(
                                "Reactive read to 'tests.reboot.GreeterMethods.TestLongRunningFetch' "
                                f"failed with a retryable error: '{error}'; "
                                "will retry..."
                            )
                            await __query_backoff__()
                            continue
                        if error.code() == IMPORT_grpc.StatusCode.ABORTED:
                            # Reconstitute the error that the server threw, if it was a declared error.
                            status = await IMPORT_rpc_status_async.from_call(__call__)
                            if status is not None:
                                raise Greeter.TestLongRunningFetchAborted.from_status(
                                    status
                                ) from None
                            raise Greeter.TestLongRunningFetchAborted.from_grpc_aio_rpc_error(
                                error
                            ) from None

                        raise

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            test_long_running_fetch = TestLongRunningFetch
            @IMPORT_typing.overload
            async def GetWholeState(
                __this__,
                __context__: IMPORT_reboot_aio_external.ExternalContext | IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WorkflowContext,
                __request_or_options__: Greeter.GetWholeStateRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_typing.AsyncIterator[Greeter.GetWholeStateResponse]:
                ...

            @IMPORT_typing.overload
            async def GetWholeState(
                __this__,
                __context__: IMPORT_reboot_aio_external.ExternalContext | IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WorkflowContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_typing.AsyncIterator[Greeter.GetWholeStateResponse]:
                ...

            async def GetWholeState( # type: ignore[misc]
                # In methods which are dealing with user input, (i.e.,
                # proto message field names), we should use '__double_underscored__'
                # variables to avoid any potential name conflicts with the method's
                # parameters.
                # The '__self__' parameter is a convention in Python to
                # indicate that this method is a bound method, so we use
                # '__this__' instead.
                __this__,
                # Explicitly-reactive calls only make sense in the context of either...
                # (A) an external client, or...
                # (B) methods that may reasonably run for a long time, which in Reboot means: readers or workflows.
                __context__: IMPORT_reboot_aio_external.ExternalContext | IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WorkflowContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.GetWholeStateRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_typing.AsyncIterator[Greeter.GetWholeStateResponse]:
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_external.ExternalContext, IMPORT_reboot_aio_contexts.ReaderContext, IMPORT_reboot_aio_contexts.WorkflowContext])
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.GetWholeStateRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.GetWholeStateRequest)

                __request__: IMPORT_typing.Optional[Greeter.GetWholeStateRequest] = None

                if isinstance(__request_or_options__, Greeter.GetWholeStateRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)
                    __request__ = __request_or_options__

                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = GreeterGetWholeStateRequestFromInputFields(
                    )

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __bearer_token__: IMPORT_typing.Optional[str] = None
                __caller_id__: IMPORT_typing.Optional[IMPORT_reboot_aio_caller_id.CallerID] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                if __bearer_token__ is None:
                    __bearer_token__ = __this__._bearer_token
                if __bearer_token__ is None and isinstance(__context__, IMPORT_reboot_aio_external.ExternalContext):
                    # Within a Reboot context we do not pass on the caller's bearer token, as that might
                    # have security implications - we cannot simply trust any service we are calling with
                    # the user's credentials. Instead, the developer can rely on the default app-internal
                    # auth, or override that and set an explicit bearer token.
                    #
                    # In the case of `ExternalContext`, however, its `bearer_token` was set specifically
                    # by the developer for the purpose of making these calls. Note that only
                    # `ExternalContext` even has a `bearer_token` field.
                    __bearer_token__ = __context__.bearer_token

                if __metadata__ is None:
                    __metadata__ = ()

                if isinstance(__context__, IMPORT_reboot_aio_contexts.Context):
                    __caller_id__ = IMPORT_reboot_aio_caller_id.CallerID(
                        application_id=__context__.application_id,
                    )
                    if __this__._application_id is None:
                        # Given our context type (inside a Reboot application) we can default to
                        # making the application send traffic to itself.
                        __this__._application_id = __context__.application_id
                elif isinstance(__context__, IMPORT_reboot_aio_external.ExternalContext):
                    __caller_id__ = __context__.caller_id

                __headers__ = IMPORT_reboot_aio_headers.Headers(
                    bearer_token=__bearer_token__,
                    state_ref=__this__._state_ref,
                    application_id=__this__._application_id,
                    caller_id=__caller_id__,
                )

                __metadata__ += __headers__.to_grpc_metadata()

                __query_backoff__ = IMPORT_reboot_aio_backoff.Backoff()
                while True:
                    __call__ = None
                    try:
                        async with __context__.channel_manager.get_channel_to_state(
                            IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                            __this__._state_ref,
                        ) as __channel__:

                            __call__ = IMPORT_rbt_v1alpha1.react_pb2_grpc.ReactStub(
                                __channel__
                            ).Query(
                                IMPORT_rbt_v1alpha1.react_pb2.QueryRequest(
                                    method='GetWholeState',
                                    request=GreeterGetWholeStateRequestToProto(
                                        __request__
                                    ).SerializeToString(),
                                ),
                                metadata=__metadata__,
                            )

                            async for __query_response__ in __call__:
                                # Clear the backoff so we don't wait
                                # as long the next time we get
                                # disconnected.
                                __query_backoff__.clear()

                                # The backend may have sent us this query
                                # response only to let us know that a new
                                # idempotency key has been recorded; there may
                                # not be a new response. Python callers don't
                                # (currently) care about such an event, so we
                                # simply ignore it.
                                if not __query_response__.HasField("response"):
                                    continue

                                __response__ = tests.reboot.greeter_pb2.Greeter()
                                __response__.ParseFromString(__query_response__.response)
                                yield GreeterGetWholeStateResponseFromProto(__response__)

                    except IMPORT_grpc.aio.AioRpcError as error:
                        # We expect to get disconnected from the server
                        # from time to time, e.g., when it is being
                        # updated, but we don't want that error to
                        # propagate, we just want to retry.
                        if IMPORT_reboot.aio.aborted.is_grpc_retryable_exception(error):
                            logger.debug(
                                "Reactive read to 'tests.reboot.GreeterMethods.GetWholeState' "
                                f"failed with a retryable error: '{error}'; "
                                "will retry..."
                            )
                            await __query_backoff__()
                            continue
                        if error.code() == IMPORT_grpc.StatusCode.ABORTED:
                            # Reconstitute the error that the server threw, if it was a declared error.
                            status = await IMPORT_rpc_status_async.from_call(__call__)
                            if status is not None:
                                raise Greeter.GetWholeStateAborted.from_status(
                                    status
                                ) from None
                            raise Greeter.GetWholeStateAborted.from_grpc_aio_rpc_error(
                                error
                            ) from None

                        raise

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            get_whole_state = GetWholeState
            @IMPORT_typing.overload
            async def FailWithException(
                __this__,
                __context__: IMPORT_reboot_aio_external.ExternalContext | IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WorkflowContext,
                __request_or_options__: Greeter.FailWithExceptionRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_typing.AsyncIterator[Greeter.FailWithExceptionResponse]:
                ...

            @IMPORT_typing.overload
            async def FailWithException(
                __this__,
                __context__: IMPORT_reboot_aio_external.ExternalContext | IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WorkflowContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_typing.AsyncIterator[Greeter.FailWithExceptionResponse]:
                ...

            async def FailWithException( # type: ignore[misc]
                # In methods which are dealing with user input, (i.e.,
                # proto message field names), we should use '__double_underscored__'
                # variables to avoid any potential name conflicts with the method's
                # parameters.
                # The '__self__' parameter is a convention in Python to
                # indicate that this method is a bound method, so we use
                # '__this__' instead.
                __this__,
                # Explicitly-reactive calls only make sense in the context of either...
                # (A) an external client, or...
                # (B) methods that may reasonably run for a long time, which in Reboot means: readers or workflows.
                __context__: IMPORT_reboot_aio_external.ExternalContext | IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WorkflowContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.FailWithExceptionRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_typing.AsyncIterator[Greeter.FailWithExceptionResponse]:
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_external.ExternalContext, IMPORT_reboot_aio_contexts.ReaderContext, IMPORT_reboot_aio_contexts.WorkflowContext])
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.FailWithExceptionRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.FailWithExceptionRequest)

                __request__: IMPORT_typing.Optional[Greeter.FailWithExceptionRequest] = None

                if isinstance(__request_or_options__, Greeter.FailWithExceptionRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)
                    __request__ = __request_or_options__

                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = GreeterFailWithExceptionRequestFromInputFields(
                    )

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __bearer_token__: IMPORT_typing.Optional[str] = None
                __caller_id__: IMPORT_typing.Optional[IMPORT_reboot_aio_caller_id.CallerID] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                if __bearer_token__ is None:
                    __bearer_token__ = __this__._bearer_token
                if __bearer_token__ is None and isinstance(__context__, IMPORT_reboot_aio_external.ExternalContext):
                    # Within a Reboot context we do not pass on the caller's bearer token, as that might
                    # have security implications - we cannot simply trust any service we are calling with
                    # the user's credentials. Instead, the developer can rely on the default app-internal
                    # auth, or override that and set an explicit bearer token.
                    #
                    # In the case of `ExternalContext`, however, its `bearer_token` was set specifically
                    # by the developer for the purpose of making these calls. Note that only
                    # `ExternalContext` even has a `bearer_token` field.
                    __bearer_token__ = __context__.bearer_token

                if __metadata__ is None:
                    __metadata__ = ()

                if isinstance(__context__, IMPORT_reboot_aio_contexts.Context):
                    __caller_id__ = IMPORT_reboot_aio_caller_id.CallerID(
                        application_id=__context__.application_id,
                    )
                    if __this__._application_id is None:
                        # Given our context type (inside a Reboot application) we can default to
                        # making the application send traffic to itself.
                        __this__._application_id = __context__.application_id
                elif isinstance(__context__, IMPORT_reboot_aio_external.ExternalContext):
                    __caller_id__ = __context__.caller_id

                __headers__ = IMPORT_reboot_aio_headers.Headers(
                    bearer_token=__bearer_token__,
                    state_ref=__this__._state_ref,
                    application_id=__this__._application_id,
                    caller_id=__caller_id__,
                )

                __metadata__ += __headers__.to_grpc_metadata()

                __query_backoff__ = IMPORT_reboot_aio_backoff.Backoff()
                while True:
                    __call__ = None
                    try:
                        async with __context__.channel_manager.get_channel_to_state(
                            IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                            __this__._state_ref,
                        ) as __channel__:

                            __call__ = IMPORT_rbt_v1alpha1.react_pb2_grpc.ReactStub(
                                __channel__
                            ).Query(
                                IMPORT_rbt_v1alpha1.react_pb2.QueryRequest(
                                    method='FailWithException',
                                    request=GreeterFailWithExceptionRequestToProto(
                                        __request__
                                    ).SerializeToString(),
                                ),
                                metadata=__metadata__,
                            )

                            async for __query_response__ in __call__:
                                # Clear the backoff so we don't wait
                                # as long the next time we get
                                # disconnected.
                                __query_backoff__.clear()

                                # The backend may have sent us this query
                                # response only to let us know that a new
                                # idempotency key has been recorded; there may
                                # not be a new response. Python callers don't
                                # (currently) care about such an event, so we
                                # simply ignore it.
                                if not __query_response__.HasField("response"):
                                    continue

                                __response__ = google.protobuf.empty_pb2.Empty()
                                __response__.ParseFromString(__query_response__.response)
                                yield GreeterFailWithExceptionResponseFromProto(__response__)

                    except IMPORT_grpc.aio.AioRpcError as error:
                        # We expect to get disconnected from the server
                        # from time to time, e.g., when it is being
                        # updated, but we don't want that error to
                        # propagate, we just want to retry.
                        if IMPORT_reboot.aio.aborted.is_grpc_retryable_exception(error):
                            logger.debug(
                                "Reactive read to 'tests.reboot.GreeterMethods.FailWithException' "
                                f"failed with a retryable error: '{error}'; "
                                "will retry..."
                            )
                            await __query_backoff__()
                            continue
                        if error.code() == IMPORT_grpc.StatusCode.ABORTED:
                            # Reconstitute the error that the server threw, if it was a declared error.
                            status = await IMPORT_rpc_status_async.from_call(__call__)
                            if status is not None:
                                raise Greeter.FailWithExceptionAborted.from_status(
                                    status
                                ) from None
                            raise Greeter.FailWithExceptionAborted.from_grpc_aio_rpc_error(
                                error
                            ) from None

                        raise

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            fail_with_exception = FailWithException
            @IMPORT_typing.overload
            async def FailWithAborted(
                __this__,
                __context__: IMPORT_reboot_aio_external.ExternalContext | IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WorkflowContext,
                __request_or_options__: Greeter.FailWithAbortedRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_typing.AsyncIterator[Greeter.FailWithAbortedResponse]:
                ...

            @IMPORT_typing.overload
            async def FailWithAborted(
                __this__,
                __context__: IMPORT_reboot_aio_external.ExternalContext | IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WorkflowContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_typing.AsyncIterator[Greeter.FailWithAbortedResponse]:
                ...

            async def FailWithAborted( # type: ignore[misc]
                # In methods which are dealing with user input, (i.e.,
                # proto message field names), we should use '__double_underscored__'
                # variables to avoid any potential name conflicts with the method's
                # parameters.
                # The '__self__' parameter is a convention in Python to
                # indicate that this method is a bound method, so we use
                # '__this__' instead.
                __this__,
                # Explicitly-reactive calls only make sense in the context of either...
                # (A) an external client, or...
                # (B) methods that may reasonably run for a long time, which in Reboot means: readers or workflows.
                __context__: IMPORT_reboot_aio_external.ExternalContext | IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WorkflowContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.FailWithAbortedRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_typing.AsyncIterator[Greeter.FailWithAbortedResponse]:
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_external.ExternalContext, IMPORT_reboot_aio_contexts.ReaderContext, IMPORT_reboot_aio_contexts.WorkflowContext])
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.FailWithAbortedRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.FailWithAbortedRequest)

                __request__: IMPORT_typing.Optional[Greeter.FailWithAbortedRequest] = None

                if isinstance(__request_or_options__, Greeter.FailWithAbortedRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)
                    __request__ = __request_or_options__

                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = GreeterFailWithAbortedRequestFromInputFields(
                    )

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __bearer_token__: IMPORT_typing.Optional[str] = None
                __caller_id__: IMPORT_typing.Optional[IMPORT_reboot_aio_caller_id.CallerID] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                if __bearer_token__ is None:
                    __bearer_token__ = __this__._bearer_token
                if __bearer_token__ is None and isinstance(__context__, IMPORT_reboot_aio_external.ExternalContext):
                    # Within a Reboot context we do not pass on the caller's bearer token, as that might
                    # have security implications - we cannot simply trust any service we are calling with
                    # the user's credentials. Instead, the developer can rely on the default app-internal
                    # auth, or override that and set an explicit bearer token.
                    #
                    # In the case of `ExternalContext`, however, its `bearer_token` was set specifically
                    # by the developer for the purpose of making these calls. Note that only
                    # `ExternalContext` even has a `bearer_token` field.
                    __bearer_token__ = __context__.bearer_token

                if __metadata__ is None:
                    __metadata__ = ()

                if isinstance(__context__, IMPORT_reboot_aio_contexts.Context):
                    __caller_id__ = IMPORT_reboot_aio_caller_id.CallerID(
                        application_id=__context__.application_id,
                    )
                    if __this__._application_id is None:
                        # Given our context type (inside a Reboot application) we can default to
                        # making the application send traffic to itself.
                        __this__._application_id = __context__.application_id
                elif isinstance(__context__, IMPORT_reboot_aio_external.ExternalContext):
                    __caller_id__ = __context__.caller_id

                __headers__ = IMPORT_reboot_aio_headers.Headers(
                    bearer_token=__bearer_token__,
                    state_ref=__this__._state_ref,
                    application_id=__this__._application_id,
                    caller_id=__caller_id__,
                )

                __metadata__ += __headers__.to_grpc_metadata()

                __query_backoff__ = IMPORT_reboot_aio_backoff.Backoff()
                while True:
                    __call__ = None
                    try:
                        async with __context__.channel_manager.get_channel_to_state(
                            IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                            __this__._state_ref,
                        ) as __channel__:

                            __call__ = IMPORT_rbt_v1alpha1.react_pb2_grpc.ReactStub(
                                __channel__
                            ).Query(
                                IMPORT_rbt_v1alpha1.react_pb2.QueryRequest(
                                    method='FailWithAborted',
                                    request=GreeterFailWithAbortedRequestToProto(
                                        __request__
                                    ).SerializeToString(),
                                ),
                                metadata=__metadata__,
                            )

                            async for __query_response__ in __call__:
                                # Clear the backoff so we don't wait
                                # as long the next time we get
                                # disconnected.
                                __query_backoff__.clear()

                                # The backend may have sent us this query
                                # response only to let us know that a new
                                # idempotency key has been recorded; there may
                                # not be a new response. Python callers don't
                                # (currently) care about such an event, so we
                                # simply ignore it.
                                if not __query_response__.HasField("response"):
                                    continue

                                __response__ = google.protobuf.empty_pb2.Empty()
                                __response__.ParseFromString(__query_response__.response)
                                yield GreeterFailWithAbortedResponseFromProto(__response__)

                    except IMPORT_grpc.aio.AioRpcError as error:
                        # We expect to get disconnected from the server
                        # from time to time, e.g., when it is being
                        # updated, but we don't want that error to
                        # propagate, we just want to retry.
                        if IMPORT_reboot.aio.aborted.is_grpc_retryable_exception(error):
                            logger.debug(
                                "Reactive read to 'tests.reboot.GreeterMethods.FailWithAborted' "
                                f"failed with a retryable error: '{error}'; "
                                "will retry..."
                            )
                            await __query_backoff__()
                            continue
                        if error.code() == IMPORT_grpc.StatusCode.ABORTED:
                            # Reconstitute the error that the server threw, if it was a declared error.
                            status = await IMPORT_rpc_status_async.from_call(__call__)
                            if status is not None:
                                raise Greeter.FailWithAbortedAborted.from_status(
                                    status
                                ) from None
                            raise Greeter.FailWithAbortedAborted.from_grpc_aio_rpc_error(
                                error
                            ) from None

                        raise

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            fail_with_aborted = FailWithAborted
            @IMPORT_typing.overload
            async def ReadRecursiveMessage(
                __this__,
                __context__: IMPORT_reboot_aio_external.ExternalContext | IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WorkflowContext,
                __request_or_options__: Greeter.ReadRecursiveMessageRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_typing.AsyncIterator[Greeter.ReadRecursiveMessageResponse]:
                ...

            @IMPORT_typing.overload
            async def ReadRecursiveMessage(
                __this__,
                __context__: IMPORT_reboot_aio_external.ExternalContext | IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WorkflowContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_typing.AsyncIterator[Greeter.ReadRecursiveMessageResponse]:
                ...

            async def ReadRecursiveMessage( # type: ignore[misc]
                # In methods which are dealing with user input, (i.e.,
                # proto message field names), we should use '__double_underscored__'
                # variables to avoid any potential name conflicts with the method's
                # parameters.
                # The '__self__' parameter is a convention in Python to
                # indicate that this method is a bound method, so we use
                # '__this__' instead.
                __this__,
                # Explicitly-reactive calls only make sense in the context of either...
                # (A) an external client, or...
                # (B) methods that may reasonably run for a long time, which in Reboot means: readers or workflows.
                __context__: IMPORT_reboot_aio_external.ExternalContext | IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WorkflowContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.ReadRecursiveMessageRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_typing.AsyncIterator[Greeter.ReadRecursiveMessageResponse]:
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_external.ExternalContext, IMPORT_reboot_aio_contexts.ReaderContext, IMPORT_reboot_aio_contexts.WorkflowContext])
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.ReadRecursiveMessageRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.ReadRecursiveMessageRequest)

                __request__: IMPORT_typing.Optional[Greeter.ReadRecursiveMessageRequest] = None

                if isinstance(__request_or_options__, Greeter.ReadRecursiveMessageRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)
                    __request__ = __request_or_options__

                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = GreeterReadRecursiveMessageRequestFromInputFields(
                    )

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __bearer_token__: IMPORT_typing.Optional[str] = None
                __caller_id__: IMPORT_typing.Optional[IMPORT_reboot_aio_caller_id.CallerID] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                if __bearer_token__ is None:
                    __bearer_token__ = __this__._bearer_token
                if __bearer_token__ is None and isinstance(__context__, IMPORT_reboot_aio_external.ExternalContext):
                    # Within a Reboot context we do not pass on the caller's bearer token, as that might
                    # have security implications - we cannot simply trust any service we are calling with
                    # the user's credentials. Instead, the developer can rely on the default app-internal
                    # auth, or override that and set an explicit bearer token.
                    #
                    # In the case of `ExternalContext`, however, its `bearer_token` was set specifically
                    # by the developer for the purpose of making these calls. Note that only
                    # `ExternalContext` even has a `bearer_token` field.
                    __bearer_token__ = __context__.bearer_token

                if __metadata__ is None:
                    __metadata__ = ()

                if isinstance(__context__, IMPORT_reboot_aio_contexts.Context):
                    __caller_id__ = IMPORT_reboot_aio_caller_id.CallerID(
                        application_id=__context__.application_id,
                    )
                    if __this__._application_id is None:
                        # Given our context type (inside a Reboot application) we can default to
                        # making the application send traffic to itself.
                        __this__._application_id = __context__.application_id
                elif isinstance(__context__, IMPORT_reboot_aio_external.ExternalContext):
                    __caller_id__ = __context__.caller_id

                __headers__ = IMPORT_reboot_aio_headers.Headers(
                    bearer_token=__bearer_token__,
                    state_ref=__this__._state_ref,
                    application_id=__this__._application_id,
                    caller_id=__caller_id__,
                )

                __metadata__ += __headers__.to_grpc_metadata()

                __query_backoff__ = IMPORT_reboot_aio_backoff.Backoff()
                while True:
                    __call__ = None
                    try:
                        async with __context__.channel_manager.get_channel_to_state(
                            IMPORT_reboot_aio_types.StateTypeName('tests.reboot.Greeter'),
                            __this__._state_ref,
                        ) as __channel__:

                            __call__ = IMPORT_rbt_v1alpha1.react_pb2_grpc.ReactStub(
                                __channel__
                            ).Query(
                                IMPORT_rbt_v1alpha1.react_pb2.QueryRequest(
                                    method='ReadRecursiveMessage',
                                    request=GreeterReadRecursiveMessageRequestToProto(
                                        __request__
                                    ).SerializeToString(),
                                ),
                                metadata=__metadata__,
                            )

                            async for __query_response__ in __call__:
                                # Clear the backoff so we don't wait
                                # as long the next time we get
                                # disconnected.
                                __query_backoff__.clear()

                                # The backend may have sent us this query
                                # response only to let us know that a new
                                # idempotency key has been recorded; there may
                                # not be a new response. Python callers don't
                                # (currently) care about such an event, so we
                                # simply ignore it.
                                if not __query_response__.HasField("response"):
                                    continue

                                __response__ = tests.reboot.greeter_pb2.ReadRecursiveMessageResponse()
                                __response__.ParseFromString(__query_response__.response)
                                yield GreeterReadRecursiveMessageResponseFromProto(__response__)

                    except IMPORT_grpc.aio.AioRpcError as error:
                        # We expect to get disconnected from the server
                        # from time to time, e.g., when it is being
                        # updated, but we don't want that error to
                        # propagate, we just want to retry.
                        if IMPORT_reboot.aio.aborted.is_grpc_retryable_exception(error):
                            logger.debug(
                                "Reactive read to 'tests.reboot.GreeterMethods.ReadRecursiveMessage' "
                                f"failed with a retryable error: '{error}'; "
                                "will retry..."
                            )
                            await __query_backoff__()
                            continue
                        if error.code() == IMPORT_grpc.StatusCode.ABORTED:
                            # Reconstitute the error that the server threw, if it was a declared error.
                            status = await IMPORT_rpc_status_async.from_call(__call__)
                            if status is not None:
                                raise Greeter.ReadRecursiveMessageAborted.from_status(
                                    status
                                ) from None
                            raise Greeter.ReadRecursiveMessageAborted.from_grpc_aio_rpc_error(
                                error
                            ) from None

                        raise

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            read_recursive_message = ReadRecursiveMessage

        def reactively(self):
            return Greeter.WeakReference._Reactively(
                application_id=self._application_id,
                state_ref=self._state_ref,
                bearer_token=self._bearer_token,
            )

        class _Idempotently(IMPORT_typing.Generic[Greeter_IdempotentlyScheduleTypeVar]):

            _weak_reference: Greeter.WeakReference[Greeter_IdempotentlyScheduleTypeVar]

            def __init__(
                self,
                *,
                weak_reference: Greeter.WeakReference[Greeter_IdempotentlyScheduleTypeVar],
                idempotency: IMPORT_reboot_aio_idempotency.Idempotency,
            ):
                self._weak_reference = weak_reference
                self._idempotency = idempotency

            def schedule(
                self,
                *,
                when: IMPORT_typing.Optional[IMPORT_datetime_datetime | IMPORT_datetime_timedelta] = None,
            ) -> Greeter_IdempotentlyScheduleTypeVar:
                return self._weak_reference._schedule_type(
                    self._weak_reference._application_id,
                    self._weak_reference._tasks,
                    when=when,
                    idempotency=self._idempotency,
                )

            def spawn(
                self,
                *,
                when: IMPORT_typing.Optional[IMPORT_datetime_datetime | IMPORT_datetime_timedelta] = None,
            ) -> Greeter.WeakReference._Spawn:
                return Greeter.WeakReference._Spawn(
                    self._weak_reference._application_id,
                    self._weak_reference._tasks,
                    when=when,
                    idempotency=self._idempotency,
                )

            async def read(
                self,
                context: IMPORT_reboot_aio_contexts.WorkflowContext,
            ) -> Greeter.State:
                if self._weak_reference._servicer is None:
                    raise RuntimeError(
                        "`read()` is currently only supported within workflows; "
                        "Please reach out and let us know your use case if this "
                        "is important for you!"
                    )

                return await GreeterBaseServicer.WorkflowState._Idempotently._read(
                    self._weak_reference._servicer,
                    self._idempotency,
                    context,
                )

            async def write(
                self,
                context: IMPORT_reboot_aio_contexts.WorkflowContext,
                writer: GreeterBaseServicer.InlineWriterCallable[GreeterBaseServicer.InlineWriterCallableResult],
                *,
                type: IMPORT_reboot_aio_workflows.Type | IMPORT_reboot_aio_workflows._Unset = IMPORT_reboot_aio_workflows._UNSET,
            ) -> GreeterBaseServicer.InlineWriterCallableResult:
                if self._weak_reference._servicer is None:
                    raise RuntimeError(
                        "`write()` is currently only supported within workflows; "
                        "Please reach out and let us know your use case if this "
                        "is important for you!"
                    )

                # `type=` is optional: when omitted, `writer`'s
                # return annotation is used.
                type_t, _ = IMPORT_reboot_aio_workflows._resolve_callable_return_type(
                    writer, type,
                )

                return await GreeterBaseServicer.WorkflowState._Idempotently._write_validating_effects(
                    self._weak_reference._servicer,
                    self._idempotency,
                    context,
                    writer,
                    type_result=type_t,
                    check_type=not self._idempotency.always,
                    unidempotently=self._idempotency.always,
                    checkpoint=context.checkpoint(),
                )

            @IMPORT_typing.overload
            async def Greet(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: Greeter.GreetRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.GreetResponse:
                ...

            @IMPORT_typing.overload
            async def Greet(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                name: IMPORT_typing.Optional[str] | Unset = UNSET,
                metadata: IMPORT_typing.Optional[dict[str, str]] | Unset = UNSET,
            ) -> Greeter.GreetResponse:
                ...

            async def Greet( # type: ignore[misc]
                # In methods which are dealing with user input, (i.e.,
                # proto message field names), we should use '__double_underscored__'
                # variables to avoid any potential name conflicts with the method's
                # parameters.
                # The '__self__' parameter is a convention in Python to
                # indicate that this method is a bound method, so we use
                # '__this__' instead.
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.GreetRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                name: IMPORT_typing.Optional[str] | Unset = UNSET,
                metadata: IMPORT_typing.Optional[dict[str, str]] | Unset = UNSET,
            ) -> Greeter.GreetResponse:
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.GreetRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.GreetRequest)

                __request__: IMPORT_typing.Optional[Greeter.GreetRequest] = None
                if isinstance(__request_or_options__, Greeter.GreetRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __options__ or IMPORT_reboot_aio_call.Options()

                    assert name is UNSET
                    assert metadata is UNSET

                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__ or IMPORT_reboot_aio_call.Options()

                    __request__ = GreeterGreetRequestFromInputFields(
                        name=name,
                        metadata=metadata,
                    )
                IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])

                return await __this__._weak_reference.Greet(
                    __context__,
                    __request__,
                    __options__,
                    __idempotency__=__this__._idempotency,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            greet = Greet
            @IMPORT_typing.overload
            async def SetAdjective(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: Greeter.SetAdjectiveRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.SetAdjectiveResponse:
                ...

            @IMPORT_typing.overload
            async def SetAdjective(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                adjective: IMPORT_typing.Optional[str] | Unset = UNSET,
                arbitrary_message: IMPORT_typing.Optional[tests.reboot.greeter_pb2.SetAdjectiveRequest.NestedMessage] | Unset = UNSET,
                revert_after_1_second: IMPORT_typing.Optional[bool] | Unset = UNSET,
            ) -> Greeter.SetAdjectiveResponse:
                ...

            async def SetAdjective( # type: ignore[misc]
                # In methods which are dealing with user input, (i.e.,
                # proto message field names), we should use '__double_underscored__'
                # variables to avoid any potential name conflicts with the method's
                # parameters.
                # The '__self__' parameter is a convention in Python to
                # indicate that this method is a bound method, so we use
                # '__this__' instead.
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.SetAdjectiveRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                adjective: IMPORT_typing.Optional[str] | Unset = UNSET,
                arbitrary_message: IMPORT_typing.Optional[tests.reboot.greeter_pb2.SetAdjectiveRequest.NestedMessage] | Unset = UNSET,
                revert_after_1_second: IMPORT_typing.Optional[bool] | Unset = UNSET,
            ) -> Greeter.SetAdjectiveResponse:
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.SetAdjectiveRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.SetAdjectiveRequest)

                __request__: IMPORT_typing.Optional[Greeter.SetAdjectiveRequest] = None
                if isinstance(__request_or_options__, Greeter.SetAdjectiveRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __options__ or IMPORT_reboot_aio_call.Options()

                    assert adjective is UNSET
                    assert arbitrary_message is UNSET
                    assert revert_after_1_second is UNSET

                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__ or IMPORT_reboot_aio_call.Options()

                    __request__ = GreeterSetAdjectiveRequestFromInputFields(
                        adjective=adjective,
                        arbitrary_message=arbitrary_message,
                        revert_after_1_second=revert_after_1_second,
                    )
                IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])

                return await __this__._weak_reference.SetAdjective(
                    __context__,
                    __request__,
                    __options__,
                    __idempotency__=__this__._idempotency,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            set_adjective = SetAdjective
            @IMPORT_typing.overload
            async def TransactionSetAdjective(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: Greeter.TransactionSetAdjectiveRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.TransactionSetAdjectiveResponse:
                ...

            @IMPORT_typing.overload
            async def TransactionSetAdjective(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                adjective: IMPORT_typing.Optional[str] | Unset = UNSET,
                arbitrary_message: IMPORT_typing.Optional[tests.reboot.greeter_pb2.SetAdjectiveRequest.NestedMessage] | Unset = UNSET,
                revert_after_1_second: IMPORT_typing.Optional[bool] | Unset = UNSET,
            ) -> Greeter.TransactionSetAdjectiveResponse:
                ...

            async def TransactionSetAdjective( # type: ignore[misc]
                # In methods which are dealing with user input, (i.e.,
                # proto message field names), we should use '__double_underscored__'
                # variables to avoid any potential name conflicts with the method's
                # parameters.
                # The '__self__' parameter is a convention in Python to
                # indicate that this method is a bound method, so we use
                # '__this__' instead.
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.TransactionSetAdjectiveRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                adjective: IMPORT_typing.Optional[str] | Unset = UNSET,
                arbitrary_message: IMPORT_typing.Optional[tests.reboot.greeter_pb2.SetAdjectiveRequest.NestedMessage] | Unset = UNSET,
                revert_after_1_second: IMPORT_typing.Optional[bool] | Unset = UNSET,
            ) -> Greeter.TransactionSetAdjectiveResponse:
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.TransactionSetAdjectiveRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.TransactionSetAdjectiveRequest)

                __request__: IMPORT_typing.Optional[Greeter.TransactionSetAdjectiveRequest] = None
                if isinstance(__request_or_options__, Greeter.TransactionSetAdjectiveRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __options__ or IMPORT_reboot_aio_call.Options()

                    assert adjective is UNSET
                    assert arbitrary_message is UNSET
                    assert revert_after_1_second is UNSET

                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__ or IMPORT_reboot_aio_call.Options()

                    __request__ = GreeterTransactionSetAdjectiveRequestFromInputFields(
                        adjective=adjective,
                        arbitrary_message=arbitrary_message,
                        revert_after_1_second=revert_after_1_second,
                    )
                IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])

                return await __this__._weak_reference.TransactionSetAdjective(
                    __context__,
                    __request__,
                    __options__,
                    __idempotency__=__this__._idempotency,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            transaction_set_adjective = TransactionSetAdjective
            @IMPORT_typing.overload
            async def TryToConstructContext(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: Greeter.TryToConstructContextRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.TryToConstructContextResponse:
                ...

            @IMPORT_typing.overload
            async def TryToConstructContext(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.TryToConstructContextResponse:
                ...

            async def TryToConstructContext( # type: ignore[misc]
                # In methods which are dealing with user input, (i.e.,
                # proto message field names), we should use '__double_underscored__'
                # variables to avoid any potential name conflicts with the method's
                # parameters.
                # The '__self__' parameter is a convention in Python to
                # indicate that this method is a bound method, so we use
                # '__this__' instead.
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.TryToConstructContextRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.TryToConstructContextResponse:
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.TryToConstructContextRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.TryToConstructContextRequest)

                __request__: IMPORT_typing.Optional[Greeter.TryToConstructContextRequest] = None
                if isinstance(__request_or_options__, Greeter.TryToConstructContextRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __options__ or IMPORT_reboot_aio_call.Options()


                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__ or IMPORT_reboot_aio_call.Options()

                    __request__ = GreeterTryToConstructContextRequestFromInputFields(
                    )
                IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])

                return await __this__._weak_reference.TryToConstructContext(
                    __context__,
                    __request__,
                    __options__,
                    __idempotency__=__this__._idempotency,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            try_to_construct_context = TryToConstructContext
            @IMPORT_typing.overload
            async def TryToConstructExternalContext(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: Greeter.TryToConstructExternalContextRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.TryToConstructExternalContextResponse:
                ...

            @IMPORT_typing.overload
            async def TryToConstructExternalContext(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.TryToConstructExternalContextResponse:
                ...

            async def TryToConstructExternalContext( # type: ignore[misc]
                # In methods which are dealing with user input, (i.e.,
                # proto message field names), we should use '__double_underscored__'
                # variables to avoid any potential name conflicts with the method's
                # parameters.
                # The '__self__' parameter is a convention in Python to
                # indicate that this method is a bound method, so we use
                # '__this__' instead.
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.TryToConstructExternalContextRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.TryToConstructExternalContextResponse:
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.TryToConstructExternalContextRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.TryToConstructExternalContextRequest)

                __request__: IMPORT_typing.Optional[Greeter.TryToConstructExternalContextRequest] = None
                if isinstance(__request_or_options__, Greeter.TryToConstructExternalContextRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __options__ or IMPORT_reboot_aio_call.Options()


                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__ or IMPORT_reboot_aio_call.Options()

                    __request__ = GreeterTryToConstructExternalContextRequestFromInputFields(
                    )
                IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])

                return await __this__._weak_reference.TryToConstructExternalContext(
                    __context__,
                    __request__,
                    __options__,
                    __idempotency__=__this__._idempotency,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            try_to_construct_external_context = TryToConstructExternalContext
            @IMPORT_typing.overload
            async def TestLongRunningFetch(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: Greeter.TestLongRunningFetchRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.TestLongRunningFetchResponse:
                ...

            @IMPORT_typing.overload
            async def TestLongRunningFetch(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                sleep_time_seconds: IMPORT_typing.Optional[int] | Unset = UNSET,
            ) -> Greeter.TestLongRunningFetchResponse:
                ...

            async def TestLongRunningFetch( # type: ignore[misc]
                # In methods which are dealing with user input, (i.e.,
                # proto message field names), we should use '__double_underscored__'
                # variables to avoid any potential name conflicts with the method's
                # parameters.
                # The '__self__' parameter is a convention in Python to
                # indicate that this method is a bound method, so we use
                # '__this__' instead.
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.TestLongRunningFetchRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                sleep_time_seconds: IMPORT_typing.Optional[int] | Unset = UNSET,
            ) -> Greeter.TestLongRunningFetchResponse:
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.TestLongRunningFetchRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.TestLongRunningFetchRequest)

                __request__: IMPORT_typing.Optional[Greeter.TestLongRunningFetchRequest] = None
                if isinstance(__request_or_options__, Greeter.TestLongRunningFetchRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __options__ or IMPORT_reboot_aio_call.Options()

                    assert sleep_time_seconds is UNSET

                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__ or IMPORT_reboot_aio_call.Options()

                    __request__ = GreeterTestLongRunningFetchRequestFromInputFields(
                        sleep_time_seconds=sleep_time_seconds,
                    )
                IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])

                return await __this__._weak_reference.TestLongRunningFetch(
                    __context__,
                    __request__,
                    __options__,
                    __idempotency__=__this__._idempotency,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            test_long_running_fetch = TestLongRunningFetch
            @IMPORT_typing.overload
            async def TestLongRunningWriter(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: Greeter.TestLongRunningWriterRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.TestLongRunningWriterResponse:
                ...

            @IMPORT_typing.overload
            async def TestLongRunningWriter(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.TestLongRunningWriterResponse:
                ...

            async def TestLongRunningWriter( # type: ignore[misc]
                # In methods which are dealing with user input, (i.e.,
                # proto message field names), we should use '__double_underscored__'
                # variables to avoid any potential name conflicts with the method's
                # parameters.
                # The '__self__' parameter is a convention in Python to
                # indicate that this method is a bound method, so we use
                # '__this__' instead.
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.TestLongRunningWriterRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.TestLongRunningWriterResponse:
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.TestLongRunningWriterRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.TestLongRunningWriterRequest)

                __request__: IMPORT_typing.Optional[Greeter.TestLongRunningWriterRequest] = None
                if isinstance(__request_or_options__, Greeter.TestLongRunningWriterRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __options__ or IMPORT_reboot_aio_call.Options()


                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__ or IMPORT_reboot_aio_call.Options()

                    __request__ = GreeterTestLongRunningWriterRequestFromInputFields(
                    )
                IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])

                return await __this__._weak_reference.TestLongRunningWriter(
                    __context__,
                    __request__,
                    __options__,
                    __idempotency__=__this__._idempotency,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            test_long_running_writer = TestLongRunningWriter
            @IMPORT_typing.overload
            async def GetWholeState(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: Greeter.GetWholeStateRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.GetWholeStateResponse:
                ...

            @IMPORT_typing.overload
            async def GetWholeState(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.GetWholeStateResponse:
                ...

            async def GetWholeState( # type: ignore[misc]
                # In methods which are dealing with user input, (i.e.,
                # proto message field names), we should use '__double_underscored__'
                # variables to avoid any potential name conflicts with the method's
                # parameters.
                # The '__self__' parameter is a convention in Python to
                # indicate that this method is a bound method, so we use
                # '__this__' instead.
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.GetWholeStateRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.GetWholeStateResponse:
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.GetWholeStateRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.GetWholeStateRequest)

                __request__: IMPORT_typing.Optional[Greeter.GetWholeStateRequest] = None
                if isinstance(__request_or_options__, Greeter.GetWholeStateRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __options__ or IMPORT_reboot_aio_call.Options()


                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__ or IMPORT_reboot_aio_call.Options()

                    __request__ = GreeterGetWholeStateRequestFromInputFields(
                    )
                IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])

                return await __this__._weak_reference.GetWholeState(
                    __context__,
                    __request__,
                    __options__,
                    __idempotency__=__this__._idempotency,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            get_whole_state = GetWholeState
            @IMPORT_typing.overload
            async def FailWithException(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: Greeter.FailWithExceptionRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.FailWithExceptionResponse:
                ...

            @IMPORT_typing.overload
            async def FailWithException(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.FailWithExceptionResponse:
                ...

            async def FailWithException( # type: ignore[misc]
                # In methods which are dealing with user input, (i.e.,
                # proto message field names), we should use '__double_underscored__'
                # variables to avoid any potential name conflicts with the method's
                # parameters.
                # The '__self__' parameter is a convention in Python to
                # indicate that this method is a bound method, so we use
                # '__this__' instead.
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.FailWithExceptionRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.FailWithExceptionResponse:
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.FailWithExceptionRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.FailWithExceptionRequest)

                __request__: IMPORT_typing.Optional[Greeter.FailWithExceptionRequest] = None
                if isinstance(__request_or_options__, Greeter.FailWithExceptionRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __options__ or IMPORT_reboot_aio_call.Options()


                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__ or IMPORT_reboot_aio_call.Options()

                    __request__ = GreeterFailWithExceptionRequestFromInputFields(
                    )
                IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])

                return await __this__._weak_reference.FailWithException(
                    __context__,
                    __request__,
                    __options__,
                    __idempotency__=__this__._idempotency,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            fail_with_exception = FailWithException
            @IMPORT_typing.overload
            async def FailWithAborted(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: Greeter.FailWithAbortedRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.FailWithAbortedResponse:
                ...

            @IMPORT_typing.overload
            async def FailWithAborted(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.FailWithAbortedResponse:
                ...

            async def FailWithAborted( # type: ignore[misc]
                # In methods which are dealing with user input, (i.e.,
                # proto message field names), we should use '__double_underscored__'
                # variables to avoid any potential name conflicts with the method's
                # parameters.
                # The '__self__' parameter is a convention in Python to
                # indicate that this method is a bound method, so we use
                # '__this__' instead.
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.FailWithAbortedRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.FailWithAbortedResponse:
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.FailWithAbortedRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.FailWithAbortedRequest)

                __request__: IMPORT_typing.Optional[Greeter.FailWithAbortedRequest] = None
                if isinstance(__request_or_options__, Greeter.FailWithAbortedRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __options__ or IMPORT_reboot_aio_call.Options()


                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__ or IMPORT_reboot_aio_call.Options()

                    __request__ = GreeterFailWithAbortedRequestFromInputFields(
                    )
                IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])

                return await __this__._weak_reference.FailWithAborted(
                    __context__,
                    __request__,
                    __options__,
                    __idempotency__=__this__._idempotency,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            fail_with_aborted = FailWithAborted
            @IMPORT_typing.overload
            async def Workflow(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: Greeter.WorkflowRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.WorkflowResponse:
                ...

            @IMPORT_typing.overload
            async def Workflow(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.WorkflowResponse:
                ...

            async def Workflow( # type: ignore[misc]
                # In methods which are dealing with user input, (i.e.,
                # proto message field names), we should use '__double_underscored__'
                # variables to avoid any potential name conflicts with the method's
                # parameters.
                # The '__self__' parameter is a convention in Python to
                # indicate that this method is a bound method, so we use
                # '__this__' instead.
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.WorkflowRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.WorkflowResponse:
                def error_message_supplement():
                    if any([isinstance(__context__, t) for t in [IMPORT_reboot_aio_contexts.WriterContext, IMPORT_reboot_aio_contexts.TransactionContext]]):
                        return f"'Workflow' is a workflow and must be scheduled from a '{type(__context__).__name__}' via `await [...].schedule([...]).Workflow(context, [...])`"
                    else:
                        return f"'Workflow' is a workflow and can not be called from '{type(__context__).__name__}'"

                IMPORT_reboot_aio_types.assert_type(
                    __context__,
                    [
                        IMPORT_reboot_aio_contexts.WorkflowContext,
                        IMPORT_reboot_aio_external.ExternalContext,
                    ],
                    error_message_supplement=error_message_supplement(),
                )
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.WorkflowRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.WorkflowRequest)

                if isinstance(__request_or_options__, Greeter.WorkflowRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __options__ or IMPORT_reboot_aio_call.Options()


                    return await (
                        await __this__.spawn().Workflow(
                            __context__,
                            __request_or_options__,
                            __options__,
                        )
                    )
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__ or IMPORT_reboot_aio_call.Options()

                    return await (
                        await __this__.spawn().Workflow(
                            __context__,
                            GreeterWorkflowRequestFromInputFields(
                            ),
                            __options__,
                        )
                    )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            workflow = Workflow
            @IMPORT_typing.overload
            async def DangerousFields(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: Greeter.DangerousFieldsRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.DangerousFieldsResponse:
                ...

            @IMPORT_typing.overload
            async def DangerousFields(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                metadata: IMPORT_typing.Optional[int] | Unset = UNSET,
                request: IMPORT_typing.Optional[int] | Unset = UNSET,
                response: IMPORT_typing.Optional[int] | Unset = UNSET,
                self: IMPORT_typing.Optional[int] | Unset = UNSET,
                context: IMPORT_typing.Optional[int] | Unset = UNSET,
                bearer_token: IMPORT_typing.Optional[int] | Unset = UNSET,
                reboot: IMPORT_typing.Optional[int] | Unset = UNSET,
                uuid: IMPORT_typing.Optional[int] | Unset = UNSET,
                stub: IMPORT_typing.Optional[int] | Unset = UNSET,
                idempotency: IMPORT_typing.Optional[str] | Unset = UNSET,
                state_id: IMPORT_typing.Optional[str] | Unset = UNSET,
            ) -> Greeter.DangerousFieldsResponse:
                ...

            async def DangerousFields( # type: ignore[misc]
                # In methods which are dealing with user input, (i.e.,
                # proto message field names), we should use '__double_underscored__'
                # variables to avoid any potential name conflicts with the method's
                # parameters.
                # The '__self__' parameter is a convention in Python to
                # indicate that this method is a bound method, so we use
                # '__this__' instead.
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.DangerousFieldsRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                metadata: IMPORT_typing.Optional[int] | Unset = UNSET,
                request: IMPORT_typing.Optional[int] | Unset = UNSET,
                response: IMPORT_typing.Optional[int] | Unset = UNSET,
                self: IMPORT_typing.Optional[int] | Unset = UNSET,
                context: IMPORT_typing.Optional[int] | Unset = UNSET,
                bearer_token: IMPORT_typing.Optional[int] | Unset = UNSET,
                reboot: IMPORT_typing.Optional[int] | Unset = UNSET,
                uuid: IMPORT_typing.Optional[int] | Unset = UNSET,
                stub: IMPORT_typing.Optional[int] | Unset = UNSET,
                idempotency: IMPORT_typing.Optional[str] | Unset = UNSET,
                state_id: IMPORT_typing.Optional[str] | Unset = UNSET,
            ) -> Greeter.DangerousFieldsResponse:
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.DangerousFieldsRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.DangerousFieldsRequest)

                __request__: IMPORT_typing.Optional[Greeter.DangerousFieldsRequest] = None
                if isinstance(__request_or_options__, Greeter.DangerousFieldsRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __options__ or IMPORT_reboot_aio_call.Options()

                    assert metadata is UNSET
                    assert request is UNSET
                    assert response is UNSET
                    assert self is UNSET
                    assert context is UNSET
                    assert bearer_token is UNSET
                    assert reboot is UNSET
                    assert uuid is UNSET
                    assert stub is UNSET
                    assert idempotency is UNSET
                    assert state_id is UNSET

                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__ or IMPORT_reboot_aio_call.Options()

                    __request__ = GreeterDangerousFieldsRequestFromInputFields(
                        metadata=metadata,
                        request=request,
                        response=response,
                        self=self,
                        context=context,
                        bearer_token=bearer_token,
                        reboot=reboot,
                        uuid=uuid,
                        stub=stub,
                        idempotency=idempotency,
                        state_id=state_id,
                    )
                IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])

                return await __this__._weak_reference.DangerousFields(
                    __context__,
                    __request__,
                    __options__,
                    __idempotency__=__this__._idempotency,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            dangerous_fields = DangerousFields
            @IMPORT_typing.overload
            async def StoreRecursiveMessage(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: Greeter.StoreRecursiveMessageRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.StoreRecursiveMessageResponse:
                ...

            @IMPORT_typing.overload
            async def StoreRecursiveMessage(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                message: IMPORT_typing.Optional[tests.reboot.greeter_pb2.RecursiveMessage] | Unset = UNSET,
            ) -> Greeter.StoreRecursiveMessageResponse:
                ...

            async def StoreRecursiveMessage( # type: ignore[misc]
                # In methods which are dealing with user input, (i.e.,
                # proto message field names), we should use '__double_underscored__'
                # variables to avoid any potential name conflicts with the method's
                # parameters.
                # The '__self__' parameter is a convention in Python to
                # indicate that this method is a bound method, so we use
                # '__this__' instead.
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.StoreRecursiveMessageRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                message: IMPORT_typing.Optional[tests.reboot.greeter_pb2.RecursiveMessage] | Unset = UNSET,
            ) -> Greeter.StoreRecursiveMessageResponse:
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.StoreRecursiveMessageRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.StoreRecursiveMessageRequest)

                __request__: IMPORT_typing.Optional[Greeter.StoreRecursiveMessageRequest] = None
                if isinstance(__request_or_options__, Greeter.StoreRecursiveMessageRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __options__ or IMPORT_reboot_aio_call.Options()

                    assert message is UNSET

                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__ or IMPORT_reboot_aio_call.Options()

                    __request__ = GreeterStoreRecursiveMessageRequestFromInputFields(
                        message=message,
                    )
                IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])

                return await __this__._weak_reference.StoreRecursiveMessage(
                    __context__,
                    __request__,
                    __options__,
                    __idempotency__=__this__._idempotency,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            store_recursive_message = StoreRecursiveMessage
            @IMPORT_typing.overload
            async def ReadRecursiveMessage(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: Greeter.ReadRecursiveMessageRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.ReadRecursiveMessageResponse:
                ...

            @IMPORT_typing.overload
            async def ReadRecursiveMessage(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.ReadRecursiveMessageResponse:
                ...

            async def ReadRecursiveMessage( # type: ignore[misc]
                # In methods which are dealing with user input, (i.e.,
                # proto message field names), we should use '__double_underscored__'
                # variables to avoid any potential name conflicts with the method's
                # parameters.
                # The '__self__' parameter is a convention in Python to
                # indicate that this method is a bound method, so we use
                # '__this__' instead.
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.ReadRecursiveMessageRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.ReadRecursiveMessageResponse:
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.ReadRecursiveMessageRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.ReadRecursiveMessageRequest)

                __request__: IMPORT_typing.Optional[Greeter.ReadRecursiveMessageRequest] = None
                if isinstance(__request_or_options__, Greeter.ReadRecursiveMessageRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __options__ or IMPORT_reboot_aio_call.Options()


                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__ or IMPORT_reboot_aio_call.Options()

                    __request__ = GreeterReadRecursiveMessageRequestFromInputFields(
                    )
                IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])

                return await __this__._weak_reference.ReadRecursiveMessage(
                    __context__,
                    __request__,
                    __options__,
                    __idempotency__=__this__._idempotency,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            read_recursive_message = ReadRecursiveMessage
            @IMPORT_typing.overload
            async def ConstructAndStoreRecursiveMessage(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: Greeter.ConstructAndStoreRecursiveMessageRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.ConstructAndStoreRecursiveMessageResponse:
                ...

            @IMPORT_typing.overload
            async def ConstructAndStoreRecursiveMessage(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                depth: IMPORT_typing.Optional[int] | Unset = UNSET,
            ) -> Greeter.ConstructAndStoreRecursiveMessageResponse:
                ...

            async def ConstructAndStoreRecursiveMessage( # type: ignore[misc]
                # In methods which are dealing with user input, (i.e.,
                # proto message field names), we should use '__double_underscored__'
                # variables to avoid any potential name conflicts with the method's
                # parameters.
                # The '__self__' parameter is a convention in Python to
                # indicate that this method is a bound method, so we use
                # '__this__' instead.
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.ConstructAndStoreRecursiveMessageRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                depth: IMPORT_typing.Optional[int] | Unset = UNSET,
            ) -> Greeter.ConstructAndStoreRecursiveMessageResponse:
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.ConstructAndStoreRecursiveMessageRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.ConstructAndStoreRecursiveMessageRequest)

                __request__: IMPORT_typing.Optional[Greeter.ConstructAndStoreRecursiveMessageRequest] = None
                if isinstance(__request_or_options__, Greeter.ConstructAndStoreRecursiveMessageRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __options__ or IMPORT_reboot_aio_call.Options()

                    assert depth is UNSET

                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__ or IMPORT_reboot_aio_call.Options()

                    __request__ = GreeterConstructAndStoreRecursiveMessageRequestFromInputFields(
                        depth=depth,
                    )
                IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])

                return await __this__._weak_reference.ConstructAndStoreRecursiveMessage(
                    __context__,
                    __request__,
                    __options__,
                    __idempotency__=__this__._idempotency,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            construct_and_store_recursive_message = ConstructAndStoreRecursiveMessage

        @IMPORT_typing.overload
        def idempotently(self, alias: IMPORT_typing.Optional[str] = None, *, how: IMPORT_reboot_aio_idempotency.How = IMPORT_reboot_aio_idempotency.PER_WORKFLOW) -> Greeter.WeakReference._Idempotently[Greeter_ScheduleTypeVar]:
            ...

        @IMPORT_typing.overload
        def idempotently(self, *, key: IMPORT_uuid.UUID, how: IMPORT_reboot_aio_idempotency.How = IMPORT_reboot_aio_idempotency.PER_WORKFLOW) -> Greeter.WeakReference._Idempotently[Greeter_ScheduleTypeVar]:
            ...

        def idempotently(
            self,
            alias: IMPORT_typing.Optional[str] = None,
            *,
            key: IMPORT_typing.Optional[IMPORT_uuid.UUID] = None,
            how: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.How] = None,
        ) -> Greeter.WeakReference._Idempotently[Greeter_ScheduleTypeVar]:
            return Greeter.WeakReference._Idempotently(
                weak_reference=self,
                idempotency=IMPORT_reboot_aio_contexts.Context.idempotency(
                    alias=alias,
                    key=key,
                    how=how,
                )
            )

        def per_workflow(self, alias: IMPORT_typing.Optional[str] = None):
            return self.idempotently(alias)

        def per_iteration(self, alias: IMPORT_typing.Optional[str] = None):
            return self.idempotently(
                alias,
                how=IMPORT_reboot_aio_idempotency.PER_ITERATION,
            )

        def always(self):
            return self.idempotently(
                key=IMPORT_reboot_aio_idempotency.make_expiring_idempotency_key(),
                how=IMPORT_reboot_aio_idempotency.ALWAYS,
            )

        class _UntilChangesSatisfies(IMPORT_typing.Generic[Greeter_UntilCallableType]):

            _idempotency_alias: str
            _context: IMPORT_reboot_aio_contexts.WorkflowContext
            _callable: IMPORT_typing.Callable[[], IMPORT_typing.Awaitable[Greeter_UntilCallableType]]
            _type: type[Greeter_UntilCallableType]

            def __init__(
                self,
                *,
                idempotency_alias: str,
                context: IMPORT_reboot_aio_contexts.WorkflowContext,
                callable: IMPORT_typing.Callable[[], IMPORT_typing.Awaitable[Greeter_UntilCallableType]],
                type: type[Greeter_UntilCallableType],
            ):
                self._idempotency_alias = idempotency_alias
                self._context = context
                self._callable = callable
                self._type = type

            async def changes(self):
                return await IMPORT_reboot_aio_workflows.until_changes(
                    self._idempotency_alias,
                    self._context,
                    self._callable,
                    type=self._type,
                )

            async def satisfies(
                self,
                condition: IMPORT_typing.Callable[[Greeter_UntilCallableType], bool],
            ):

                async def converge():
                    response = await self._callable()
                    if condition(response):
                        return response
                    return False

                return await IMPORT_reboot_aio_workflows.until(
                    self._idempotency_alias,
                    self._context,
                    converge,
                    type=self._type,
                )

        class _Until:

            _weak_reference: Greeter.WeakReference
            _idempotency_alias: str

            def __init__(
                self,
                *,
                weak_reference: Greeter.WeakReference,
                idempotency_alias: str,
            ):
                self._weak_reference = weak_reference
                self._idempotency_alias = idempotency_alias

            def read(
                self,
                context: IMPORT_reboot_aio_contexts.WorkflowContext,
            ) -> Greeter.WeakReference._UntilChangesSatisfies[Greeter.State]:
                IMPORT_reboot_aio_types.assert_type(
                    context,
                    [IMPORT_reboot_aio_contexts.WorkflowContext],
                )

                async def callable():
                    return await self._weak_reference.read(context)

                return Greeter.WeakReference._UntilChangesSatisfies(
                    idempotency_alias=self._idempotency_alias,
                    context=context,
                    callable=callable,
                    type=Greeter.State,
                )

            @IMPORT_typing.overload
            async def Greet(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext,
                __request_or_options__: Greeter.GreetRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.WeakReference._UntilChangesSatisfies[Greeter.GreetResponse]:
                ...

            @IMPORT_typing.overload
            async def Greet(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                name: IMPORT_typing.Optional[str] | Unset = UNSET,
                metadata: IMPORT_typing.Optional[dict[str, str]] | Unset = UNSET,
            ) -> Greeter.WeakReference._UntilChangesSatisfies[Greeter.GreetResponse]:
                ...

            def Greet( # type: ignore[misc]
                # In methods which are dealing with user input, (i.e.,
                # proto message field names), we should use '__double_underscored__'
                # variables to avoid any potential name conflicts with the method's
                # parameters.
                # The '__self__' parameter is a convention in Python to
                # indicate that this method is a bound method, so we use
                # '__this__' instead.
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.GreetRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                name: IMPORT_typing.Optional[str] | Unset = UNSET,
                metadata: IMPORT_typing.Optional[dict[str, str]] | Unset = UNSET,
            ) -> Greeter.WeakReference._UntilChangesSatisfies[Greeter.GreetResponse]:
                IMPORT_reboot_aio_types.assert_type(
                    __context__,
                    [IMPORT_reboot_aio_contexts.WorkflowContext],
                )

                __request__: IMPORT_typing.Optional[Greeter.GreetRequest] = None
                if isinstance(__request_or_options__, Greeter.GreetRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __options__ or IMPORT_reboot_aio_call.Options()

                    assert name is UNSET
                    assert metadata is UNSET

                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__ or IMPORT_reboot_aio_call.Options()

                    __request__ = GreeterGreetRequestFromInputFields(
                        name=name,
                        metadata=metadata,
                    )

                async def callable():
                    return await __this__._weak_reference.Greet(
                        __context__,
                        __request__,
                        __options__,
                    )

                return Greeter.WeakReference._UntilChangesSatisfies(
                    idempotency_alias=__this__._idempotency_alias,
                    context=__context__,
                    callable=callable,
                    type=Greeter.GreetResponse,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            greet = Greet
            @IMPORT_typing.overload
            async def TryToConstructContext(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext,
                __request_or_options__: Greeter.TryToConstructContextRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.WeakReference._UntilChangesSatisfies[Greeter.TryToConstructContextResponse]:
                ...

            @IMPORT_typing.overload
            async def TryToConstructContext(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.WeakReference._UntilChangesSatisfies[Greeter.TryToConstructContextResponse]:
                ...

            def TryToConstructContext( # type: ignore[misc]
                # In methods which are dealing with user input, (i.e.,
                # proto message field names), we should use '__double_underscored__'
                # variables to avoid any potential name conflicts with the method's
                # parameters.
                # The '__self__' parameter is a convention in Python to
                # indicate that this method is a bound method, so we use
                # '__this__' instead.
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.TryToConstructContextRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.WeakReference._UntilChangesSatisfies[Greeter.TryToConstructContextResponse]:
                IMPORT_reboot_aio_types.assert_type(
                    __context__,
                    [IMPORT_reboot_aio_contexts.WorkflowContext],
                )

                __request__: IMPORT_typing.Optional[Greeter.TryToConstructContextRequest] = None
                if isinstance(__request_or_options__, Greeter.TryToConstructContextRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __options__ or IMPORT_reboot_aio_call.Options()


                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__ or IMPORT_reboot_aio_call.Options()

                    __request__ = GreeterTryToConstructContextRequestFromInputFields(
                    )

                async def callable():
                    return await __this__._weak_reference.TryToConstructContext(
                        __context__,
                        __request__,
                        __options__,
                    )

                return Greeter.WeakReference._UntilChangesSatisfies(
                    idempotency_alias=__this__._idempotency_alias,
                    context=__context__,
                    callable=callable,
                    type=Greeter.TryToConstructContextResponse,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            try_to_construct_context = TryToConstructContext
            @IMPORT_typing.overload
            async def TryToConstructExternalContext(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext,
                __request_or_options__: Greeter.TryToConstructExternalContextRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.WeakReference._UntilChangesSatisfies[Greeter.TryToConstructExternalContextResponse]:
                ...

            @IMPORT_typing.overload
            async def TryToConstructExternalContext(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.WeakReference._UntilChangesSatisfies[Greeter.TryToConstructExternalContextResponse]:
                ...

            def TryToConstructExternalContext( # type: ignore[misc]
                # In methods which are dealing with user input, (i.e.,
                # proto message field names), we should use '__double_underscored__'
                # variables to avoid any potential name conflicts with the method's
                # parameters.
                # The '__self__' parameter is a convention in Python to
                # indicate that this method is a bound method, so we use
                # '__this__' instead.
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.TryToConstructExternalContextRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.WeakReference._UntilChangesSatisfies[Greeter.TryToConstructExternalContextResponse]:
                IMPORT_reboot_aio_types.assert_type(
                    __context__,
                    [IMPORT_reboot_aio_contexts.WorkflowContext],
                )

                __request__: IMPORT_typing.Optional[Greeter.TryToConstructExternalContextRequest] = None
                if isinstance(__request_or_options__, Greeter.TryToConstructExternalContextRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __options__ or IMPORT_reboot_aio_call.Options()


                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__ or IMPORT_reboot_aio_call.Options()

                    __request__ = GreeterTryToConstructExternalContextRequestFromInputFields(
                    )

                async def callable():
                    return await __this__._weak_reference.TryToConstructExternalContext(
                        __context__,
                        __request__,
                        __options__,
                    )

                return Greeter.WeakReference._UntilChangesSatisfies(
                    idempotency_alias=__this__._idempotency_alias,
                    context=__context__,
                    callable=callable,
                    type=Greeter.TryToConstructExternalContextResponse,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            try_to_construct_external_context = TryToConstructExternalContext
            @IMPORT_typing.overload
            async def TestLongRunningFetch(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext,
                __request_or_options__: Greeter.TestLongRunningFetchRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.WeakReference._UntilChangesSatisfies[Greeter.TestLongRunningFetchResponse]:
                ...

            @IMPORT_typing.overload
            async def TestLongRunningFetch(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                sleep_time_seconds: IMPORT_typing.Optional[int] | Unset = UNSET,
            ) -> Greeter.WeakReference._UntilChangesSatisfies[Greeter.TestLongRunningFetchResponse]:
                ...

            def TestLongRunningFetch( # type: ignore[misc]
                # In methods which are dealing with user input, (i.e.,
                # proto message field names), we should use '__double_underscored__'
                # variables to avoid any potential name conflicts with the method's
                # parameters.
                # The '__self__' parameter is a convention in Python to
                # indicate that this method is a bound method, so we use
                # '__this__' instead.
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.TestLongRunningFetchRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                sleep_time_seconds: IMPORT_typing.Optional[int] | Unset = UNSET,
            ) -> Greeter.WeakReference._UntilChangesSatisfies[Greeter.TestLongRunningFetchResponse]:
                IMPORT_reboot_aio_types.assert_type(
                    __context__,
                    [IMPORT_reboot_aio_contexts.WorkflowContext],
                )

                __request__: IMPORT_typing.Optional[Greeter.TestLongRunningFetchRequest] = None
                if isinstance(__request_or_options__, Greeter.TestLongRunningFetchRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __options__ or IMPORT_reboot_aio_call.Options()

                    assert sleep_time_seconds is UNSET

                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__ or IMPORT_reboot_aio_call.Options()

                    __request__ = GreeterTestLongRunningFetchRequestFromInputFields(
                        sleep_time_seconds=sleep_time_seconds,
                    )

                async def callable():
                    return await __this__._weak_reference.TestLongRunningFetch(
                        __context__,
                        __request__,
                        __options__,
                    )

                return Greeter.WeakReference._UntilChangesSatisfies(
                    idempotency_alias=__this__._idempotency_alias,
                    context=__context__,
                    callable=callable,
                    type=Greeter.TestLongRunningFetchResponse,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            test_long_running_fetch = TestLongRunningFetch
            @IMPORT_typing.overload
            async def GetWholeState(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext,
                __request_or_options__: Greeter.GetWholeStateRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.WeakReference._UntilChangesSatisfies[Greeter.GetWholeStateResponse]:
                ...

            @IMPORT_typing.overload
            async def GetWholeState(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.WeakReference._UntilChangesSatisfies[Greeter.GetWholeStateResponse]:
                ...

            def GetWholeState( # type: ignore[misc]
                # In methods which are dealing with user input, (i.e.,
                # proto message field names), we should use '__double_underscored__'
                # variables to avoid any potential name conflicts with the method's
                # parameters.
                # The '__self__' parameter is a convention in Python to
                # indicate that this method is a bound method, so we use
                # '__this__' instead.
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.GetWholeStateRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.WeakReference._UntilChangesSatisfies[Greeter.GetWholeStateResponse]:
                IMPORT_reboot_aio_types.assert_type(
                    __context__,
                    [IMPORT_reboot_aio_contexts.WorkflowContext],
                )

                __request__: IMPORT_typing.Optional[Greeter.GetWholeStateRequest] = None
                if isinstance(__request_or_options__, Greeter.GetWholeStateRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __options__ or IMPORT_reboot_aio_call.Options()


                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__ or IMPORT_reboot_aio_call.Options()

                    __request__ = GreeterGetWholeStateRequestFromInputFields(
                    )

                async def callable():
                    return await __this__._weak_reference.GetWholeState(
                        __context__,
                        __request__,
                        __options__,
                    )

                return Greeter.WeakReference._UntilChangesSatisfies(
                    idempotency_alias=__this__._idempotency_alias,
                    context=__context__,
                    callable=callable,
                    type=Greeter.GetWholeStateResponse,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            get_whole_state = GetWholeState
            @IMPORT_typing.overload
            async def FailWithException(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext,
                __request_or_options__: Greeter.FailWithExceptionRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.WeakReference._UntilChangesSatisfies[Greeter.FailWithExceptionResponse]:
                ...

            @IMPORT_typing.overload
            async def FailWithException(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.WeakReference._UntilChangesSatisfies[Greeter.FailWithExceptionResponse]:
                ...

            def FailWithException( # type: ignore[misc]
                # In methods which are dealing with user input, (i.e.,
                # proto message field names), we should use '__double_underscored__'
                # variables to avoid any potential name conflicts with the method's
                # parameters.
                # The '__self__' parameter is a convention in Python to
                # indicate that this method is a bound method, so we use
                # '__this__' instead.
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.FailWithExceptionRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.WeakReference._UntilChangesSatisfies[Greeter.FailWithExceptionResponse]:
                IMPORT_reboot_aio_types.assert_type(
                    __context__,
                    [IMPORT_reboot_aio_contexts.WorkflowContext],
                )

                __request__: IMPORT_typing.Optional[Greeter.FailWithExceptionRequest] = None
                if isinstance(__request_or_options__, Greeter.FailWithExceptionRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __options__ or IMPORT_reboot_aio_call.Options()


                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__ or IMPORT_reboot_aio_call.Options()

                    __request__ = GreeterFailWithExceptionRequestFromInputFields(
                    )

                async def callable():
                    return await __this__._weak_reference.FailWithException(
                        __context__,
                        __request__,
                        __options__,
                    )

                return Greeter.WeakReference._UntilChangesSatisfies(
                    idempotency_alias=__this__._idempotency_alias,
                    context=__context__,
                    callable=callable,
                    type=Greeter.FailWithExceptionResponse,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            fail_with_exception = FailWithException
            @IMPORT_typing.overload
            async def FailWithAborted(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext,
                __request_or_options__: Greeter.FailWithAbortedRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.WeakReference._UntilChangesSatisfies[Greeter.FailWithAbortedResponse]:
                ...

            @IMPORT_typing.overload
            async def FailWithAborted(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.WeakReference._UntilChangesSatisfies[Greeter.FailWithAbortedResponse]:
                ...

            def FailWithAborted( # type: ignore[misc]
                # In methods which are dealing with user input, (i.e.,
                # proto message field names), we should use '__double_underscored__'
                # variables to avoid any potential name conflicts with the method's
                # parameters.
                # The '__self__' parameter is a convention in Python to
                # indicate that this method is a bound method, so we use
                # '__this__' instead.
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.FailWithAbortedRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.WeakReference._UntilChangesSatisfies[Greeter.FailWithAbortedResponse]:
                IMPORT_reboot_aio_types.assert_type(
                    __context__,
                    [IMPORT_reboot_aio_contexts.WorkflowContext],
                )

                __request__: IMPORT_typing.Optional[Greeter.FailWithAbortedRequest] = None
                if isinstance(__request_or_options__, Greeter.FailWithAbortedRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __options__ or IMPORT_reboot_aio_call.Options()


                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__ or IMPORT_reboot_aio_call.Options()

                    __request__ = GreeterFailWithAbortedRequestFromInputFields(
                    )

                async def callable():
                    return await __this__._weak_reference.FailWithAborted(
                        __context__,
                        __request__,
                        __options__,
                    )

                return Greeter.WeakReference._UntilChangesSatisfies(
                    idempotency_alias=__this__._idempotency_alias,
                    context=__context__,
                    callable=callable,
                    type=Greeter.FailWithAbortedResponse,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            fail_with_aborted = FailWithAborted
            @IMPORT_typing.overload
            async def ReadRecursiveMessage(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext,
                __request_or_options__: Greeter.ReadRecursiveMessageRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.WeakReference._UntilChangesSatisfies[Greeter.ReadRecursiveMessageResponse]:
                ...

            @IMPORT_typing.overload
            async def ReadRecursiveMessage(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.WeakReference._UntilChangesSatisfies[Greeter.ReadRecursiveMessageResponse]:
                ...

            def ReadRecursiveMessage( # type: ignore[misc]
                # In methods which are dealing with user input, (i.e.,
                # proto message field names), we should use '__double_underscored__'
                # variables to avoid any potential name conflicts with the method's
                # parameters.
                # The '__self__' parameter is a convention in Python to
                # indicate that this method is a bound method, so we use
                # '__this__' instead.
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.ReadRecursiveMessageRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.WeakReference._UntilChangesSatisfies[Greeter.ReadRecursiveMessageResponse]:
                IMPORT_reboot_aio_types.assert_type(
                    __context__,
                    [IMPORT_reboot_aio_contexts.WorkflowContext],
                )

                __request__: IMPORT_typing.Optional[Greeter.ReadRecursiveMessageRequest] = None
                if isinstance(__request_or_options__, Greeter.ReadRecursiveMessageRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __options__ or IMPORT_reboot_aio_call.Options()


                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__ or IMPORT_reboot_aio_call.Options()

                    __request__ = GreeterReadRecursiveMessageRequestFromInputFields(
                    )

                async def callable():
                    return await __this__._weak_reference.ReadRecursiveMessage(
                        __context__,
                        __request__,
                        __options__,
                    )

                return Greeter.WeakReference._UntilChangesSatisfies(
                    idempotency_alias=__this__._idempotency_alias,
                    context=__context__,
                    callable=callable,
                    type=Greeter.ReadRecursiveMessageResponse,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            read_recursive_message = ReadRecursiveMessage

        def until(self, alias: str):
            return Greeter.WeakReference._Until(
                weak_reference=self,
                idempotency_alias=alias,
            )

        def schedule(
            self,
            *,
            when: IMPORT_typing.Optional[IMPORT_datetime_datetime | IMPORT_datetime_timedelta] = None,
        ) -> Greeter_ScheduleTypeVar:
            return self._schedule_type(self._application_id, self._tasks, when=when)

        class _Schedule:

            def __init__(
                self,
                application_id: IMPORT_typing.Optional[IMPORT_reboot_aio_types.ApplicationId],
                tasks: IMPORT_typing.Callable[[IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext], GreeterTasksStub],
                *,
                when: IMPORT_typing.Optional[IMPORT_datetime_datetime | IMPORT_datetime_timedelta] = None,
                idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
            ) -> None:
                self._application_id = application_id
                self._tasks = tasks
                self._when = ensure_has_timezone(when=when)
                self._idempotency = idempotency

            # Greeter callable tasks:
            @IMPORT_typing.overload
            async def Greet(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: Greeter.GreetRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            @IMPORT_typing.overload
            async def Greet(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                name: IMPORT_typing.Optional[str] | Unset = UNSET,
                metadata: IMPORT_typing.Optional[dict[str, str]] | Unset = UNSET,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            async def Greet( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.GreetRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                name: IMPORT_typing.Optional[str] | Unset = UNSET,
                metadata: IMPORT_typing.Optional[dict[str, str]] | Unset = UNSET,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.TransactionContext])
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.GreetRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.GreetRequest)

                __request__: IMPORT_typing.Optional[Greeter.GreetRequest] = None
                if isinstance(__request_or_options__, Greeter.GreetRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

                    assert name is UNSET
                    assert metadata is UNSET

                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = GreeterGreetRequestFromInputFields(
                        name=name,
                        metadata=metadata,
                    )

                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                __task_id__ = await __this__._tasks(
                    __context__
                ).Greet(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

                return __task_id__

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            greet = Greet
            @IMPORT_typing.overload
            async def SetAdjective(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: Greeter.SetAdjectiveRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            @IMPORT_typing.overload
            async def SetAdjective(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                adjective: IMPORT_typing.Optional[str] | Unset = UNSET,
                arbitrary_message: IMPORT_typing.Optional[tests.reboot.greeter_pb2.SetAdjectiveRequest.NestedMessage] | Unset = UNSET,
                revert_after_1_second: IMPORT_typing.Optional[bool] | Unset = UNSET,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            async def SetAdjective( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.SetAdjectiveRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                adjective: IMPORT_typing.Optional[str] | Unset = UNSET,
                arbitrary_message: IMPORT_typing.Optional[tests.reboot.greeter_pb2.SetAdjectiveRequest.NestedMessage] | Unset = UNSET,
                revert_after_1_second: IMPORT_typing.Optional[bool] | Unset = UNSET,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.TransactionContext])
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.SetAdjectiveRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.SetAdjectiveRequest)

                __request__: IMPORT_typing.Optional[Greeter.SetAdjectiveRequest] = None
                if isinstance(__request_or_options__, Greeter.SetAdjectiveRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

                    assert adjective is UNSET
                    assert arbitrary_message is UNSET
                    assert revert_after_1_second is UNSET

                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = GreeterSetAdjectiveRequestFromInputFields(
                        adjective=adjective,
                        arbitrary_message=arbitrary_message,
                        revert_after_1_second=revert_after_1_second,
                    )

                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                __task_id__ = await __this__._tasks(
                    __context__
                ).SetAdjective(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

                return __task_id__

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            set_adjective = SetAdjective
            @IMPORT_typing.overload
            async def TransactionSetAdjective(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: Greeter.TransactionSetAdjectiveRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            @IMPORT_typing.overload
            async def TransactionSetAdjective(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                adjective: IMPORT_typing.Optional[str] | Unset = UNSET,
                arbitrary_message: IMPORT_typing.Optional[tests.reboot.greeter_pb2.SetAdjectiveRequest.NestedMessage] | Unset = UNSET,
                revert_after_1_second: IMPORT_typing.Optional[bool] | Unset = UNSET,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            async def TransactionSetAdjective( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.TransactionSetAdjectiveRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                adjective: IMPORT_typing.Optional[str] | Unset = UNSET,
                arbitrary_message: IMPORT_typing.Optional[tests.reboot.greeter_pb2.SetAdjectiveRequest.NestedMessage] | Unset = UNSET,
                revert_after_1_second: IMPORT_typing.Optional[bool] | Unset = UNSET,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.TransactionContext])
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.TransactionSetAdjectiveRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.TransactionSetAdjectiveRequest)

                __request__: IMPORT_typing.Optional[Greeter.TransactionSetAdjectiveRequest] = None
                if isinstance(__request_or_options__, Greeter.TransactionSetAdjectiveRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

                    assert adjective is UNSET
                    assert arbitrary_message is UNSET
                    assert revert_after_1_second is UNSET

                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = GreeterTransactionSetAdjectiveRequestFromInputFields(
                        adjective=adjective,
                        arbitrary_message=arbitrary_message,
                        revert_after_1_second=revert_after_1_second,
                    )

                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                __task_id__ = await __this__._tasks(
                    __context__
                ).TransactionSetAdjective(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

                return __task_id__

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            transaction_set_adjective = TransactionSetAdjective
            @IMPORT_typing.overload
            async def TryToConstructContext(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: Greeter.TryToConstructContextRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            @IMPORT_typing.overload
            async def TryToConstructContext(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            async def TryToConstructContext( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.TryToConstructContextRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.TransactionContext])
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.TryToConstructContextRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.TryToConstructContextRequest)

                __request__: IMPORT_typing.Optional[Greeter.TryToConstructContextRequest] = None
                if isinstance(__request_or_options__, Greeter.TryToConstructContextRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)


                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = GreeterTryToConstructContextRequestFromInputFields(
                    )

                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                __task_id__ = await __this__._tasks(
                    __context__
                ).TryToConstructContext(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

                return __task_id__

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            try_to_construct_context = TryToConstructContext
            @IMPORT_typing.overload
            async def TryToConstructExternalContext(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: Greeter.TryToConstructExternalContextRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            @IMPORT_typing.overload
            async def TryToConstructExternalContext(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            async def TryToConstructExternalContext( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.TryToConstructExternalContextRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.TransactionContext])
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.TryToConstructExternalContextRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.TryToConstructExternalContextRequest)

                __request__: IMPORT_typing.Optional[Greeter.TryToConstructExternalContextRequest] = None
                if isinstance(__request_or_options__, Greeter.TryToConstructExternalContextRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)


                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = GreeterTryToConstructExternalContextRequestFromInputFields(
                    )

                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                __task_id__ = await __this__._tasks(
                    __context__
                ).TryToConstructExternalContext(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

                return __task_id__

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            try_to_construct_external_context = TryToConstructExternalContext
            @IMPORT_typing.overload
            async def TestLongRunningFetch(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: Greeter.TestLongRunningFetchRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            @IMPORT_typing.overload
            async def TestLongRunningFetch(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                sleep_time_seconds: IMPORT_typing.Optional[int] | Unset = UNSET,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            async def TestLongRunningFetch( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.TestLongRunningFetchRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                sleep_time_seconds: IMPORT_typing.Optional[int] | Unset = UNSET,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.TransactionContext])
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.TestLongRunningFetchRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.TestLongRunningFetchRequest)

                __request__: IMPORT_typing.Optional[Greeter.TestLongRunningFetchRequest] = None
                if isinstance(__request_or_options__, Greeter.TestLongRunningFetchRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

                    assert sleep_time_seconds is UNSET

                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = GreeterTestLongRunningFetchRequestFromInputFields(
                        sleep_time_seconds=sleep_time_seconds,
                    )

                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                __task_id__ = await __this__._tasks(
                    __context__
                ).TestLongRunningFetch(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

                return __task_id__

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            test_long_running_fetch = TestLongRunningFetch
            @IMPORT_typing.overload
            async def TestLongRunningWriter(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: Greeter.TestLongRunningWriterRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            @IMPORT_typing.overload
            async def TestLongRunningWriter(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            async def TestLongRunningWriter( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.TestLongRunningWriterRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.TransactionContext])
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.TestLongRunningWriterRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.TestLongRunningWriterRequest)

                __request__: IMPORT_typing.Optional[Greeter.TestLongRunningWriterRequest] = None
                if isinstance(__request_or_options__, Greeter.TestLongRunningWriterRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)


                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = GreeterTestLongRunningWriterRequestFromInputFields(
                    )

                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                __task_id__ = await __this__._tasks(
                    __context__
                ).TestLongRunningWriter(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

                return __task_id__

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            test_long_running_writer = TestLongRunningWriter
            @IMPORT_typing.overload
            async def GetWholeState(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: Greeter.GetWholeStateRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            @IMPORT_typing.overload
            async def GetWholeState(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            async def GetWholeState( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.GetWholeStateRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.TransactionContext])
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.GetWholeStateRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.GetWholeStateRequest)

                __request__: IMPORT_typing.Optional[Greeter.GetWholeStateRequest] = None
                if isinstance(__request_or_options__, Greeter.GetWholeStateRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)


                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = GreeterGetWholeStateRequestFromInputFields(
                    )

                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                __task_id__ = await __this__._tasks(
                    __context__
                ).GetWholeState(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

                return __task_id__

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            get_whole_state = GetWholeState
            @IMPORT_typing.overload
            async def FailWithException(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: Greeter.FailWithExceptionRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            @IMPORT_typing.overload
            async def FailWithException(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            async def FailWithException( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.FailWithExceptionRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.TransactionContext])
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.FailWithExceptionRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.FailWithExceptionRequest)

                __request__: IMPORT_typing.Optional[Greeter.FailWithExceptionRequest] = None
                if isinstance(__request_or_options__, Greeter.FailWithExceptionRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)


                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = GreeterFailWithExceptionRequestFromInputFields(
                    )

                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                __task_id__ = await __this__._tasks(
                    __context__
                ).FailWithException(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

                return __task_id__

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            fail_with_exception = FailWithException
            @IMPORT_typing.overload
            async def FailWithAborted(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: Greeter.FailWithAbortedRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            @IMPORT_typing.overload
            async def FailWithAborted(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            async def FailWithAborted( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.FailWithAbortedRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.TransactionContext])
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.FailWithAbortedRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.FailWithAbortedRequest)

                __request__: IMPORT_typing.Optional[Greeter.FailWithAbortedRequest] = None
                if isinstance(__request_or_options__, Greeter.FailWithAbortedRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)


                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = GreeterFailWithAbortedRequestFromInputFields(
                    )

                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                __task_id__ = await __this__._tasks(
                    __context__
                ).FailWithAborted(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

                return __task_id__

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            fail_with_aborted = FailWithAborted
            @IMPORT_typing.overload
            async def Workflow(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: Greeter.WorkflowRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            @IMPORT_typing.overload
            async def Workflow(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            async def Workflow( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.WorkflowRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.TransactionContext])
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.WorkflowRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.WorkflowRequest)

                __request__: IMPORT_typing.Optional[Greeter.WorkflowRequest] = None
                if isinstance(__request_or_options__, Greeter.WorkflowRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)


                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = GreeterWorkflowRequestFromInputFields(
                    )

                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                __task_id__ = await __this__._tasks(
                    __context__
                ).Workflow(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

                return __task_id__

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            workflow = Workflow
            @IMPORT_typing.overload
            async def DangerousFields(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: Greeter.DangerousFieldsRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            @IMPORT_typing.overload
            async def DangerousFields(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                metadata: IMPORT_typing.Optional[int] | Unset = UNSET,
                request: IMPORT_typing.Optional[int] | Unset = UNSET,
                response: IMPORT_typing.Optional[int] | Unset = UNSET,
                self: IMPORT_typing.Optional[int] | Unset = UNSET,
                context: IMPORT_typing.Optional[int] | Unset = UNSET,
                bearer_token: IMPORT_typing.Optional[int] | Unset = UNSET,
                reboot: IMPORT_typing.Optional[int] | Unset = UNSET,
                uuid: IMPORT_typing.Optional[int] | Unset = UNSET,
                stub: IMPORT_typing.Optional[int] | Unset = UNSET,
                idempotency: IMPORT_typing.Optional[str] | Unset = UNSET,
                state_id: IMPORT_typing.Optional[str] | Unset = UNSET,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            async def DangerousFields( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.DangerousFieldsRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                metadata: IMPORT_typing.Optional[int] | Unset = UNSET,
                request: IMPORT_typing.Optional[int] | Unset = UNSET,
                response: IMPORT_typing.Optional[int] | Unset = UNSET,
                self: IMPORT_typing.Optional[int] | Unset = UNSET,
                context: IMPORT_typing.Optional[int] | Unset = UNSET,
                bearer_token: IMPORT_typing.Optional[int] | Unset = UNSET,
                reboot: IMPORT_typing.Optional[int] | Unset = UNSET,
                uuid: IMPORT_typing.Optional[int] | Unset = UNSET,
                stub: IMPORT_typing.Optional[int] | Unset = UNSET,
                idempotency: IMPORT_typing.Optional[str] | Unset = UNSET,
                state_id: IMPORT_typing.Optional[str] | Unset = UNSET,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.TransactionContext])
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.DangerousFieldsRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.DangerousFieldsRequest)

                __request__: IMPORT_typing.Optional[Greeter.DangerousFieldsRequest] = None
                if isinstance(__request_or_options__, Greeter.DangerousFieldsRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

                    assert metadata is UNSET
                    assert request is UNSET
                    assert response is UNSET
                    assert self is UNSET
                    assert context is UNSET
                    assert bearer_token is UNSET
                    assert reboot is UNSET
                    assert uuid is UNSET
                    assert stub is UNSET
                    assert idempotency is UNSET
                    assert state_id is UNSET

                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = GreeterDangerousFieldsRequestFromInputFields(
                        metadata=metadata,
                        request=request,
                        response=response,
                        self=self,
                        context=context,
                        bearer_token=bearer_token,
                        reboot=reboot,
                        uuid=uuid,
                        stub=stub,
                        idempotency=idempotency,
                        state_id=state_id,
                    )

                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                __task_id__ = await __this__._tasks(
                    __context__
                ).DangerousFields(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

                return __task_id__

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            dangerous_fields = DangerousFields
            @IMPORT_typing.overload
            async def StoreRecursiveMessage(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: Greeter.StoreRecursiveMessageRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            @IMPORT_typing.overload
            async def StoreRecursiveMessage(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                message: IMPORT_typing.Optional[tests.reboot.greeter_pb2.RecursiveMessage] | Unset = UNSET,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            async def StoreRecursiveMessage( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.StoreRecursiveMessageRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                message: IMPORT_typing.Optional[tests.reboot.greeter_pb2.RecursiveMessage] | Unset = UNSET,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.TransactionContext])
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.StoreRecursiveMessageRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.StoreRecursiveMessageRequest)

                __request__: IMPORT_typing.Optional[Greeter.StoreRecursiveMessageRequest] = None
                if isinstance(__request_or_options__, Greeter.StoreRecursiveMessageRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

                    assert message is UNSET

                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = GreeterStoreRecursiveMessageRequestFromInputFields(
                        message=message,
                    )

                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                __task_id__ = await __this__._tasks(
                    __context__
                ).StoreRecursiveMessage(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

                return __task_id__

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            store_recursive_message = StoreRecursiveMessage
            @IMPORT_typing.overload
            async def ReadRecursiveMessage(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: Greeter.ReadRecursiveMessageRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            @IMPORT_typing.overload
            async def ReadRecursiveMessage(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            async def ReadRecursiveMessage( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.ReadRecursiveMessageRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.TransactionContext])
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.ReadRecursiveMessageRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.ReadRecursiveMessageRequest)

                __request__: IMPORT_typing.Optional[Greeter.ReadRecursiveMessageRequest] = None
                if isinstance(__request_or_options__, Greeter.ReadRecursiveMessageRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)


                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = GreeterReadRecursiveMessageRequestFromInputFields(
                    )

                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                __task_id__ = await __this__._tasks(
                    __context__
                ).ReadRecursiveMessage(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

                return __task_id__

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            read_recursive_message = ReadRecursiveMessage
            @IMPORT_typing.overload
            async def ConstructAndStoreRecursiveMessage(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: Greeter.ConstructAndStoreRecursiveMessageRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            @IMPORT_typing.overload
            async def ConstructAndStoreRecursiveMessage(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                depth: IMPORT_typing.Optional[int] | Unset = UNSET,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            async def ConstructAndStoreRecursiveMessage( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.ConstructAndStoreRecursiveMessageRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                depth: IMPORT_typing.Optional[int] | Unset = UNSET,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.TransactionContext])
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.ConstructAndStoreRecursiveMessageRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.ConstructAndStoreRecursiveMessageRequest)

                __request__: IMPORT_typing.Optional[Greeter.ConstructAndStoreRecursiveMessageRequest] = None
                if isinstance(__request_or_options__, Greeter.ConstructAndStoreRecursiveMessageRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

                    assert depth is UNSET

                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = GreeterConstructAndStoreRecursiveMessageRequestFromInputFields(
                        depth=depth,
                    )

                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                __task_id__ = await __this__._tasks(
                    __context__
                ).ConstructAndStoreRecursiveMessage(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

                return __task_id__

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            construct_and_store_recursive_message = ConstructAndStoreRecursiveMessage

        # A `WriterContext` can not call any methods in `_Schedule` to
        # prevent a writer from doing a `Foo.ref()` and trying to
        # schedule. However, we want to allow a writer to schedule
        # when we are constructing a `WeakReference` from
        # `self.ref()` so instead we return a `_WriterSchedule` to
        # provide type safety that allows a `WriterContext` to
        # schedule (for itself).
        class _WriterSchedule:

            def __init__(
                self,
                application_id: IMPORT_typing.Optional[IMPORT_reboot_aio_types.ApplicationId],
                tasks: IMPORT_typing.Callable[[IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext], GreeterTasksStub],
                *,
                when: IMPORT_typing.Optional[IMPORT_datetime_datetime | IMPORT_datetime_timedelta] = None,
                idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
            ) -> None:
                self._tasks = tasks
                self._when = ensure_has_timezone(when=when)
                self._idempotency = idempotency

            # Greeter callable tasks:
            @IMPORT_typing.overload
            async def Greet(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: Greeter.GreetRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            @IMPORT_typing.overload
            async def Greet(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                name: IMPORT_typing.Optional[str] | Unset = UNSET,
                metadata: IMPORT_typing.Optional[dict[str, str]] | Unset = UNSET,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            async def Greet( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.GreetRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                name: IMPORT_typing.Optional[str] | Unset = UNSET,
                metadata: IMPORT_typing.Optional[dict[str, str]] | Unset = UNSET,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                # Only `writer`s and `transaction`s should ``schedule()`, a
                # `workflow` should `spawn()`.
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.WriterContext, IMPORT_reboot_aio_contexts.TransactionContext])

                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.GreetRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.GreetRequest)

                __request__: IMPORT_typing.Optional[Greeter.GreetRequest] = None
                if isinstance(__request_or_options__, Greeter.GreetRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

                    assert name is UNSET
                    assert metadata is UNSET

                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = GreeterGreetRequestFromInputFields(
                        name=name,
                        metadata=metadata,
                    )

                if isinstance(__context__, IMPORT_reboot_aio_contexts.WriterContext):
                    return (await GreeterServicerTasks(
                        context=__context__,
                        state_ref=__context__._state_ref,
                    ).Greet(
                        __request__,
                        schedule=__this__._when,
                    )).task_id

                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                return await __this__._tasks(
                    __context__
                ).Greet(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            greet = Greet
            @IMPORT_typing.overload
            async def SetAdjective(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: Greeter.SetAdjectiveRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            @IMPORT_typing.overload
            async def SetAdjective(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                adjective: IMPORT_typing.Optional[str] | Unset = UNSET,
                arbitrary_message: IMPORT_typing.Optional[tests.reboot.greeter_pb2.SetAdjectiveRequest.NestedMessage] | Unset = UNSET,
                revert_after_1_second: IMPORT_typing.Optional[bool] | Unset = UNSET,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            async def SetAdjective( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.SetAdjectiveRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                adjective: IMPORT_typing.Optional[str] | Unset = UNSET,
                arbitrary_message: IMPORT_typing.Optional[tests.reboot.greeter_pb2.SetAdjectiveRequest.NestedMessage] | Unset = UNSET,
                revert_after_1_second: IMPORT_typing.Optional[bool] | Unset = UNSET,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                # Only `writer`s and `transaction`s should ``schedule()`, a
                # `workflow` should `spawn()`.
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.WriterContext, IMPORT_reboot_aio_contexts.TransactionContext])

                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.SetAdjectiveRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.SetAdjectiveRequest)

                __request__: IMPORT_typing.Optional[Greeter.SetAdjectiveRequest] = None
                if isinstance(__request_or_options__, Greeter.SetAdjectiveRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

                    assert adjective is UNSET
                    assert arbitrary_message is UNSET
                    assert revert_after_1_second is UNSET

                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = GreeterSetAdjectiveRequestFromInputFields(
                        adjective=adjective,
                        arbitrary_message=arbitrary_message,
                        revert_after_1_second=revert_after_1_second,
                    )

                if isinstance(__context__, IMPORT_reboot_aio_contexts.WriterContext):
                    return (await GreeterServicerTasks(
                        context=__context__,
                        state_ref=__context__._state_ref,
                    ).SetAdjective(
                        __request__,
                        schedule=__this__._when,
                    )).task_id

                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                return await __this__._tasks(
                    __context__
                ).SetAdjective(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            set_adjective = SetAdjective
            @IMPORT_typing.overload
            async def TransactionSetAdjective(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: Greeter.TransactionSetAdjectiveRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            @IMPORT_typing.overload
            async def TransactionSetAdjective(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                adjective: IMPORT_typing.Optional[str] | Unset = UNSET,
                arbitrary_message: IMPORT_typing.Optional[tests.reboot.greeter_pb2.SetAdjectiveRequest.NestedMessage] | Unset = UNSET,
                revert_after_1_second: IMPORT_typing.Optional[bool] | Unset = UNSET,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            async def TransactionSetAdjective( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.TransactionSetAdjectiveRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                adjective: IMPORT_typing.Optional[str] | Unset = UNSET,
                arbitrary_message: IMPORT_typing.Optional[tests.reboot.greeter_pb2.SetAdjectiveRequest.NestedMessage] | Unset = UNSET,
                revert_after_1_second: IMPORT_typing.Optional[bool] | Unset = UNSET,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                # Only `writer`s and `transaction`s should ``schedule()`, a
                # `workflow` should `spawn()`.
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.WriterContext, IMPORT_reboot_aio_contexts.TransactionContext])

                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.TransactionSetAdjectiveRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.TransactionSetAdjectiveRequest)

                __request__: IMPORT_typing.Optional[Greeter.TransactionSetAdjectiveRequest] = None
                if isinstance(__request_or_options__, Greeter.TransactionSetAdjectiveRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

                    assert adjective is UNSET
                    assert arbitrary_message is UNSET
                    assert revert_after_1_second is UNSET

                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = GreeterTransactionSetAdjectiveRequestFromInputFields(
                        adjective=adjective,
                        arbitrary_message=arbitrary_message,
                        revert_after_1_second=revert_after_1_second,
                    )

                if isinstance(__context__, IMPORT_reboot_aio_contexts.WriterContext):
                    return (await GreeterServicerTasks(
                        context=__context__,
                        state_ref=__context__._state_ref,
                    ).TransactionSetAdjective(
                        __request__,
                        schedule=__this__._when,
                    )).task_id

                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                return await __this__._tasks(
                    __context__
                ).TransactionSetAdjective(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            transaction_set_adjective = TransactionSetAdjective
            @IMPORT_typing.overload
            async def TryToConstructContext(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: Greeter.TryToConstructContextRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            @IMPORT_typing.overload
            async def TryToConstructContext(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            async def TryToConstructContext( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.TryToConstructContextRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                # Only `writer`s and `transaction`s should ``schedule()`, a
                # `workflow` should `spawn()`.
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.WriterContext, IMPORT_reboot_aio_contexts.TransactionContext])

                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.TryToConstructContextRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.TryToConstructContextRequest)

                __request__: IMPORT_typing.Optional[Greeter.TryToConstructContextRequest] = None
                if isinstance(__request_or_options__, Greeter.TryToConstructContextRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)


                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = GreeterTryToConstructContextRequestFromInputFields(
                    )

                if isinstance(__context__, IMPORT_reboot_aio_contexts.WriterContext):
                    return (await GreeterServicerTasks(
                        context=__context__,
                        state_ref=__context__._state_ref,
                    ).TryToConstructContext(
                        __request__,
                        schedule=__this__._when,
                    )).task_id

                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                return await __this__._tasks(
                    __context__
                ).TryToConstructContext(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            try_to_construct_context = TryToConstructContext
            @IMPORT_typing.overload
            async def TryToConstructExternalContext(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: Greeter.TryToConstructExternalContextRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            @IMPORT_typing.overload
            async def TryToConstructExternalContext(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            async def TryToConstructExternalContext( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.TryToConstructExternalContextRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                # Only `writer`s and `transaction`s should ``schedule()`, a
                # `workflow` should `spawn()`.
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.WriterContext, IMPORT_reboot_aio_contexts.TransactionContext])

                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.TryToConstructExternalContextRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.TryToConstructExternalContextRequest)

                __request__: IMPORT_typing.Optional[Greeter.TryToConstructExternalContextRequest] = None
                if isinstance(__request_or_options__, Greeter.TryToConstructExternalContextRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)


                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = GreeterTryToConstructExternalContextRequestFromInputFields(
                    )

                if isinstance(__context__, IMPORT_reboot_aio_contexts.WriterContext):
                    return (await GreeterServicerTasks(
                        context=__context__,
                        state_ref=__context__._state_ref,
                    ).TryToConstructExternalContext(
                        __request__,
                        schedule=__this__._when,
                    )).task_id

                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                return await __this__._tasks(
                    __context__
                ).TryToConstructExternalContext(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            try_to_construct_external_context = TryToConstructExternalContext
            @IMPORT_typing.overload
            async def TestLongRunningFetch(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: Greeter.TestLongRunningFetchRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            @IMPORT_typing.overload
            async def TestLongRunningFetch(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                sleep_time_seconds: IMPORT_typing.Optional[int] | Unset = UNSET,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            async def TestLongRunningFetch( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.TestLongRunningFetchRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                sleep_time_seconds: IMPORT_typing.Optional[int] | Unset = UNSET,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                # Only `writer`s and `transaction`s should ``schedule()`, a
                # `workflow` should `spawn()`.
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.WriterContext, IMPORT_reboot_aio_contexts.TransactionContext])

                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.TestLongRunningFetchRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.TestLongRunningFetchRequest)

                __request__: IMPORT_typing.Optional[Greeter.TestLongRunningFetchRequest] = None
                if isinstance(__request_or_options__, Greeter.TestLongRunningFetchRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

                    assert sleep_time_seconds is UNSET

                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = GreeterTestLongRunningFetchRequestFromInputFields(
                        sleep_time_seconds=sleep_time_seconds,
                    )

                if isinstance(__context__, IMPORT_reboot_aio_contexts.WriterContext):
                    return (await GreeterServicerTasks(
                        context=__context__,
                        state_ref=__context__._state_ref,
                    ).TestLongRunningFetch(
                        __request__,
                        schedule=__this__._when,
                    )).task_id

                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                return await __this__._tasks(
                    __context__
                ).TestLongRunningFetch(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            test_long_running_fetch = TestLongRunningFetch
            @IMPORT_typing.overload
            async def TestLongRunningWriter(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: Greeter.TestLongRunningWriterRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            @IMPORT_typing.overload
            async def TestLongRunningWriter(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            async def TestLongRunningWriter( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.TestLongRunningWriterRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                # Only `writer`s and `transaction`s should ``schedule()`, a
                # `workflow` should `spawn()`.
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.WriterContext, IMPORT_reboot_aio_contexts.TransactionContext])

                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.TestLongRunningWriterRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.TestLongRunningWriterRequest)

                __request__: IMPORT_typing.Optional[Greeter.TestLongRunningWriterRequest] = None
                if isinstance(__request_or_options__, Greeter.TestLongRunningWriterRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)


                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = GreeterTestLongRunningWriterRequestFromInputFields(
                    )

                if isinstance(__context__, IMPORT_reboot_aio_contexts.WriterContext):
                    return (await GreeterServicerTasks(
                        context=__context__,
                        state_ref=__context__._state_ref,
                    ).TestLongRunningWriter(
                        __request__,
                        schedule=__this__._when,
                    )).task_id

                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                return await __this__._tasks(
                    __context__
                ).TestLongRunningWriter(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            test_long_running_writer = TestLongRunningWriter
            @IMPORT_typing.overload
            async def GetWholeState(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: Greeter.GetWholeStateRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            @IMPORT_typing.overload
            async def GetWholeState(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            async def GetWholeState( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.GetWholeStateRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                # Only `writer`s and `transaction`s should ``schedule()`, a
                # `workflow` should `spawn()`.
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.WriterContext, IMPORT_reboot_aio_contexts.TransactionContext])

                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.GetWholeStateRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.GetWholeStateRequest)

                __request__: IMPORT_typing.Optional[Greeter.GetWholeStateRequest] = None
                if isinstance(__request_or_options__, Greeter.GetWholeStateRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)


                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = GreeterGetWholeStateRequestFromInputFields(
                    )

                if isinstance(__context__, IMPORT_reboot_aio_contexts.WriterContext):
                    return (await GreeterServicerTasks(
                        context=__context__,
                        state_ref=__context__._state_ref,
                    ).GetWholeState(
                        __request__,
                        schedule=__this__._when,
                    )).task_id

                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                return await __this__._tasks(
                    __context__
                ).GetWholeState(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            get_whole_state = GetWholeState
            @IMPORT_typing.overload
            async def FailWithException(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: Greeter.FailWithExceptionRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            @IMPORT_typing.overload
            async def FailWithException(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            async def FailWithException( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.FailWithExceptionRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                # Only `writer`s and `transaction`s should ``schedule()`, a
                # `workflow` should `spawn()`.
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.WriterContext, IMPORT_reboot_aio_contexts.TransactionContext])

                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.FailWithExceptionRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.FailWithExceptionRequest)

                __request__: IMPORT_typing.Optional[Greeter.FailWithExceptionRequest] = None
                if isinstance(__request_or_options__, Greeter.FailWithExceptionRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)


                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = GreeterFailWithExceptionRequestFromInputFields(
                    )

                if isinstance(__context__, IMPORT_reboot_aio_contexts.WriterContext):
                    return (await GreeterServicerTasks(
                        context=__context__,
                        state_ref=__context__._state_ref,
                    ).FailWithException(
                        __request__,
                        schedule=__this__._when,
                    )).task_id

                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                return await __this__._tasks(
                    __context__
                ).FailWithException(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            fail_with_exception = FailWithException
            @IMPORT_typing.overload
            async def FailWithAborted(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: Greeter.FailWithAbortedRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            @IMPORT_typing.overload
            async def FailWithAborted(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            async def FailWithAborted( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.FailWithAbortedRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                # Only `writer`s and `transaction`s should ``schedule()`, a
                # `workflow` should `spawn()`.
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.WriterContext, IMPORT_reboot_aio_contexts.TransactionContext])

                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.FailWithAbortedRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.FailWithAbortedRequest)

                __request__: IMPORT_typing.Optional[Greeter.FailWithAbortedRequest] = None
                if isinstance(__request_or_options__, Greeter.FailWithAbortedRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)


                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = GreeterFailWithAbortedRequestFromInputFields(
                    )

                if isinstance(__context__, IMPORT_reboot_aio_contexts.WriterContext):
                    return (await GreeterServicerTasks(
                        context=__context__,
                        state_ref=__context__._state_ref,
                    ).FailWithAborted(
                        __request__,
                        schedule=__this__._when,
                    )).task_id

                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                return await __this__._tasks(
                    __context__
                ).FailWithAborted(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            fail_with_aborted = FailWithAborted
            @IMPORT_typing.overload
            async def Workflow(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: Greeter.WorkflowRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            @IMPORT_typing.overload
            async def Workflow(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            async def Workflow( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.WorkflowRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                # Only `writer`s and `transaction`s should ``schedule()`, a
                # `workflow` should `spawn()`.
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.WriterContext, IMPORT_reboot_aio_contexts.TransactionContext])

                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.WorkflowRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.WorkflowRequest)

                __request__: IMPORT_typing.Optional[Greeter.WorkflowRequest] = None
                if isinstance(__request_or_options__, Greeter.WorkflowRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)


                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = GreeterWorkflowRequestFromInputFields(
                    )

                if isinstance(__context__, IMPORT_reboot_aio_contexts.WriterContext):
                    return (await GreeterServicerTasks(
                        context=__context__,
                        state_ref=__context__._state_ref,
                    ).Workflow(
                        __request__,
                        schedule=__this__._when,
                    )).task_id

                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                return await __this__._tasks(
                    __context__
                ).Workflow(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            workflow = Workflow
            @IMPORT_typing.overload
            async def DangerousFields(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: Greeter.DangerousFieldsRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            @IMPORT_typing.overload
            async def DangerousFields(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                metadata: IMPORT_typing.Optional[int] | Unset = UNSET,
                request: IMPORT_typing.Optional[int] | Unset = UNSET,
                response: IMPORT_typing.Optional[int] | Unset = UNSET,
                self: IMPORT_typing.Optional[int] | Unset = UNSET,
                context: IMPORT_typing.Optional[int] | Unset = UNSET,
                bearer_token: IMPORT_typing.Optional[int] | Unset = UNSET,
                reboot: IMPORT_typing.Optional[int] | Unset = UNSET,
                uuid: IMPORT_typing.Optional[int] | Unset = UNSET,
                stub: IMPORT_typing.Optional[int] | Unset = UNSET,
                idempotency: IMPORT_typing.Optional[str] | Unset = UNSET,
                state_id: IMPORT_typing.Optional[str] | Unset = UNSET,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            async def DangerousFields( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.DangerousFieldsRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                metadata: IMPORT_typing.Optional[int] | Unset = UNSET,
                request: IMPORT_typing.Optional[int] | Unset = UNSET,
                response: IMPORT_typing.Optional[int] | Unset = UNSET,
                self: IMPORT_typing.Optional[int] | Unset = UNSET,
                context: IMPORT_typing.Optional[int] | Unset = UNSET,
                bearer_token: IMPORT_typing.Optional[int] | Unset = UNSET,
                reboot: IMPORT_typing.Optional[int] | Unset = UNSET,
                uuid: IMPORT_typing.Optional[int] | Unset = UNSET,
                stub: IMPORT_typing.Optional[int] | Unset = UNSET,
                idempotency: IMPORT_typing.Optional[str] | Unset = UNSET,
                state_id: IMPORT_typing.Optional[str] | Unset = UNSET,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                # Only `writer`s and `transaction`s should ``schedule()`, a
                # `workflow` should `spawn()`.
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.WriterContext, IMPORT_reboot_aio_contexts.TransactionContext])

                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.DangerousFieldsRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.DangerousFieldsRequest)

                __request__: IMPORT_typing.Optional[Greeter.DangerousFieldsRequest] = None
                if isinstance(__request_or_options__, Greeter.DangerousFieldsRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

                    assert metadata is UNSET
                    assert request is UNSET
                    assert response is UNSET
                    assert self is UNSET
                    assert context is UNSET
                    assert bearer_token is UNSET
                    assert reboot is UNSET
                    assert uuid is UNSET
                    assert stub is UNSET
                    assert idempotency is UNSET
                    assert state_id is UNSET

                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = GreeterDangerousFieldsRequestFromInputFields(
                        metadata=metadata,
                        request=request,
                        response=response,
                        self=self,
                        context=context,
                        bearer_token=bearer_token,
                        reboot=reboot,
                        uuid=uuid,
                        stub=stub,
                        idempotency=idempotency,
                        state_id=state_id,
                    )

                if isinstance(__context__, IMPORT_reboot_aio_contexts.WriterContext):
                    return (await GreeterServicerTasks(
                        context=__context__,
                        state_ref=__context__._state_ref,
                    ).DangerousFields(
                        __request__,
                        schedule=__this__._when,
                    )).task_id

                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                return await __this__._tasks(
                    __context__
                ).DangerousFields(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            dangerous_fields = DangerousFields
            @IMPORT_typing.overload
            async def StoreRecursiveMessage(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: Greeter.StoreRecursiveMessageRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            @IMPORT_typing.overload
            async def StoreRecursiveMessage(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                message: IMPORT_typing.Optional[tests.reboot.greeter_pb2.RecursiveMessage] | Unset = UNSET,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            async def StoreRecursiveMessage( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.StoreRecursiveMessageRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                message: IMPORT_typing.Optional[tests.reboot.greeter_pb2.RecursiveMessage] | Unset = UNSET,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                # Only `writer`s and `transaction`s should ``schedule()`, a
                # `workflow` should `spawn()`.
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.WriterContext, IMPORT_reboot_aio_contexts.TransactionContext])

                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.StoreRecursiveMessageRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.StoreRecursiveMessageRequest)

                __request__: IMPORT_typing.Optional[Greeter.StoreRecursiveMessageRequest] = None
                if isinstance(__request_or_options__, Greeter.StoreRecursiveMessageRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

                    assert message is UNSET

                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = GreeterStoreRecursiveMessageRequestFromInputFields(
                        message=message,
                    )

                if isinstance(__context__, IMPORT_reboot_aio_contexts.WriterContext):
                    return (await GreeterServicerTasks(
                        context=__context__,
                        state_ref=__context__._state_ref,
                    ).StoreRecursiveMessage(
                        __request__,
                        schedule=__this__._when,
                    )).task_id

                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                return await __this__._tasks(
                    __context__
                ).StoreRecursiveMessage(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            store_recursive_message = StoreRecursiveMessage
            @IMPORT_typing.overload
            async def ReadRecursiveMessage(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: Greeter.ReadRecursiveMessageRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            @IMPORT_typing.overload
            async def ReadRecursiveMessage(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            async def ReadRecursiveMessage( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.ReadRecursiveMessageRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                # Only `writer`s and `transaction`s should ``schedule()`, a
                # `workflow` should `spawn()`.
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.WriterContext, IMPORT_reboot_aio_contexts.TransactionContext])

                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.ReadRecursiveMessageRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.ReadRecursiveMessageRequest)

                __request__: IMPORT_typing.Optional[Greeter.ReadRecursiveMessageRequest] = None
                if isinstance(__request_or_options__, Greeter.ReadRecursiveMessageRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)


                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = GreeterReadRecursiveMessageRequestFromInputFields(
                    )

                if isinstance(__context__, IMPORT_reboot_aio_contexts.WriterContext):
                    return (await GreeterServicerTasks(
                        context=__context__,
                        state_ref=__context__._state_ref,
                    ).ReadRecursiveMessage(
                        __request__,
                        schedule=__this__._when,
                    )).task_id

                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                return await __this__._tasks(
                    __context__
                ).ReadRecursiveMessage(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            read_recursive_message = ReadRecursiveMessage
            @IMPORT_typing.overload
            async def ConstructAndStoreRecursiveMessage(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: Greeter.ConstructAndStoreRecursiveMessageRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            @IMPORT_typing.overload
            async def ConstructAndStoreRecursiveMessage(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                depth: IMPORT_typing.Optional[int] | Unset = UNSET,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            async def ConstructAndStoreRecursiveMessage( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.ConstructAndStoreRecursiveMessageRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                depth: IMPORT_typing.Optional[int] | Unset = UNSET,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                # Only `writer`s and `transaction`s should ``schedule()`, a
                # `workflow` should `spawn()`.
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.WriterContext, IMPORT_reboot_aio_contexts.TransactionContext])

                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.ConstructAndStoreRecursiveMessageRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.ConstructAndStoreRecursiveMessageRequest)

                __request__: IMPORT_typing.Optional[Greeter.ConstructAndStoreRecursiveMessageRequest] = None
                if isinstance(__request_or_options__, Greeter.ConstructAndStoreRecursiveMessageRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

                    assert depth is UNSET

                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = GreeterConstructAndStoreRecursiveMessageRequestFromInputFields(
                        depth=depth,
                    )

                if isinstance(__context__, IMPORT_reboot_aio_contexts.WriterContext):
                    return (await GreeterServicerTasks(
                        context=__context__,
                        state_ref=__context__._state_ref,
                    ).ConstructAndStoreRecursiveMessage(
                        __request__,
                        schedule=__this__._when,
                    )).task_id

                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                return await __this__._tasks(
                    __context__
                ).ConstructAndStoreRecursiveMessage(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            construct_and_store_recursive_message = ConstructAndStoreRecursiveMessage

        def spawn(
            self,
            *,
            when: IMPORT_typing.Optional[IMPORT_datetime_datetime | IMPORT_datetime_timedelta] = None,
        ) -> Greeter.WeakReference._Spawn:
            # Within a `workflow`, all "bare" `spawn()` calls are
            # syntactic sugar for `per_workflow()`, unless we're
            # within a control loop, in which case they are syntactic
            # sugar for `per_iteration()`.
            context = IMPORT_reboot_aio_contexts.Context.get()
            if context is not None:
                if isinstance(context, IMPORT_reboot_aio_contexts.WorkflowContext):
                    return (
                        self.per_iteration() if context.within_loop()
                        else self.per_workflow()
                    ).spawn(when=when)
                elif isinstance(context, IMPORT_reboot_aio_external.InitializeContext):
                    return self.idempotently().spawn(when=when)

            return Greeter.WeakReference._Spawn(
                self._application_id, self._tasks, when=when
            )

        class _Spawn:

            def __init__(
                self,
                application_id: IMPORT_typing.Optional[IMPORT_reboot_aio_types.ApplicationId],
                tasks: IMPORT_typing.Callable[[IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext], GreeterTasksStub],
                *,
                when: IMPORT_typing.Optional[IMPORT_datetime_datetime | IMPORT_datetime_timedelta] = None,
                idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
            ) -> None:
                self._application_id = application_id
                self._tasks = tasks
                self._when = ensure_has_timezone(when=when)
                self._idempotency = idempotency

            # Greeter callable tasks:
            @IMPORT_typing.overload
            async def Greet(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: Greeter.GreetRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.GreetTask:
                ...

            @IMPORT_typing.overload
            async def Greet(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                name: IMPORT_typing.Optional[str] | Unset = UNSET,
                metadata: IMPORT_typing.Optional[dict[str, str]] | Unset = UNSET,
            ) -> Greeter.GreetTask:
                ...

            async def Greet( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.GreetRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                name: IMPORT_typing.Optional[str] | Unset = UNSET,
                metadata: IMPORT_typing.Optional[dict[str, str]] | Unset = UNSET,
            ) -> Greeter.GreetTask:
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.WorkflowContext, IMPORT_reboot_aio_external.ExternalContext])
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.GreetRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.GreetRequest)


                __request__: IMPORT_typing.Optional[Greeter.GreetRequest] = None
                if isinstance(__request_or_options__, Greeter.GreetRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

                    assert name is UNSET
                    assert metadata is UNSET

                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = GreeterGreetRequestFromInputFields(
                        name=name,
                        metadata=metadata,
                    )
                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                __task_id__ = await __this__._tasks(
                    __context__
                ).Greet(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

                return Greeter.GreetTask(
                    __context__,
                    task_id=__task_id__,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            greet = Greet
            @IMPORT_typing.overload
            async def SetAdjective(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: Greeter.SetAdjectiveRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.SetAdjectiveTask:
                ...

            @IMPORT_typing.overload
            async def SetAdjective(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                adjective: IMPORT_typing.Optional[str] | Unset = UNSET,
                arbitrary_message: IMPORT_typing.Optional[tests.reboot.greeter_pb2.SetAdjectiveRequest.NestedMessage] | Unset = UNSET,
                revert_after_1_second: IMPORT_typing.Optional[bool] | Unset = UNSET,
            ) -> Greeter.SetAdjectiveTask:
                ...

            async def SetAdjective( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.SetAdjectiveRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                adjective: IMPORT_typing.Optional[str] | Unset = UNSET,
                arbitrary_message: IMPORT_typing.Optional[tests.reboot.greeter_pb2.SetAdjectiveRequest.NestedMessage] | Unset = UNSET,
                revert_after_1_second: IMPORT_typing.Optional[bool] | Unset = UNSET,
            ) -> Greeter.SetAdjectiveTask:
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.WorkflowContext, IMPORT_reboot_aio_external.ExternalContext])
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.SetAdjectiveRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.SetAdjectiveRequest)


                __request__: IMPORT_typing.Optional[Greeter.SetAdjectiveRequest] = None
                if isinstance(__request_or_options__, Greeter.SetAdjectiveRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

                    assert adjective is UNSET
                    assert arbitrary_message is UNSET
                    assert revert_after_1_second is UNSET

                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = GreeterSetAdjectiveRequestFromInputFields(
                        adjective=adjective,
                        arbitrary_message=arbitrary_message,
                        revert_after_1_second=revert_after_1_second,
                    )
                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                __task_id__ = await __this__._tasks(
                    __context__
                ).SetAdjective(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

                return Greeter.SetAdjectiveTask(
                    __context__,
                    task_id=__task_id__,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            set_adjective = SetAdjective
            @IMPORT_typing.overload
            async def TransactionSetAdjective(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: Greeter.TransactionSetAdjectiveRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.TransactionSetAdjectiveTask:
                ...

            @IMPORT_typing.overload
            async def TransactionSetAdjective(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                adjective: IMPORT_typing.Optional[str] | Unset = UNSET,
                arbitrary_message: IMPORT_typing.Optional[tests.reboot.greeter_pb2.SetAdjectiveRequest.NestedMessage] | Unset = UNSET,
                revert_after_1_second: IMPORT_typing.Optional[bool] | Unset = UNSET,
            ) -> Greeter.TransactionSetAdjectiveTask:
                ...

            async def TransactionSetAdjective( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.TransactionSetAdjectiveRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                adjective: IMPORT_typing.Optional[str] | Unset = UNSET,
                arbitrary_message: IMPORT_typing.Optional[tests.reboot.greeter_pb2.SetAdjectiveRequest.NestedMessage] | Unset = UNSET,
                revert_after_1_second: IMPORT_typing.Optional[bool] | Unset = UNSET,
            ) -> Greeter.TransactionSetAdjectiveTask:
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.WorkflowContext, IMPORT_reboot_aio_external.ExternalContext])
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.TransactionSetAdjectiveRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.TransactionSetAdjectiveRequest)


                __request__: IMPORT_typing.Optional[Greeter.TransactionSetAdjectiveRequest] = None
                if isinstance(__request_or_options__, Greeter.TransactionSetAdjectiveRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

                    assert adjective is UNSET
                    assert arbitrary_message is UNSET
                    assert revert_after_1_second is UNSET

                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = GreeterTransactionSetAdjectiveRequestFromInputFields(
                        adjective=adjective,
                        arbitrary_message=arbitrary_message,
                        revert_after_1_second=revert_after_1_second,
                    )
                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                __task_id__ = await __this__._tasks(
                    __context__
                ).TransactionSetAdjective(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

                return Greeter.TransactionSetAdjectiveTask(
                    __context__,
                    task_id=__task_id__,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            transaction_set_adjective = TransactionSetAdjective
            @IMPORT_typing.overload
            async def TryToConstructContext(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: Greeter.TryToConstructContextRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.TryToConstructContextTask:
                ...

            @IMPORT_typing.overload
            async def TryToConstructContext(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.TryToConstructContextTask:
                ...

            async def TryToConstructContext( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.TryToConstructContextRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.TryToConstructContextTask:
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.WorkflowContext, IMPORT_reboot_aio_external.ExternalContext])
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.TryToConstructContextRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.TryToConstructContextRequest)


                __request__: IMPORT_typing.Optional[Greeter.TryToConstructContextRequest] = None
                if isinstance(__request_or_options__, Greeter.TryToConstructContextRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)


                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = GreeterTryToConstructContextRequestFromInputFields(
                    )
                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                __task_id__ = await __this__._tasks(
                    __context__
                ).TryToConstructContext(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

                return Greeter.TryToConstructContextTask(
                    __context__,
                    task_id=__task_id__,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            try_to_construct_context = TryToConstructContext
            @IMPORT_typing.overload
            async def TryToConstructExternalContext(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: Greeter.TryToConstructExternalContextRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.TryToConstructExternalContextTask:
                ...

            @IMPORT_typing.overload
            async def TryToConstructExternalContext(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.TryToConstructExternalContextTask:
                ...

            async def TryToConstructExternalContext( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.TryToConstructExternalContextRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.TryToConstructExternalContextTask:
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.WorkflowContext, IMPORT_reboot_aio_external.ExternalContext])
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.TryToConstructExternalContextRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.TryToConstructExternalContextRequest)


                __request__: IMPORT_typing.Optional[Greeter.TryToConstructExternalContextRequest] = None
                if isinstance(__request_or_options__, Greeter.TryToConstructExternalContextRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)


                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = GreeterTryToConstructExternalContextRequestFromInputFields(
                    )
                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                __task_id__ = await __this__._tasks(
                    __context__
                ).TryToConstructExternalContext(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

                return Greeter.TryToConstructExternalContextTask(
                    __context__,
                    task_id=__task_id__,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            try_to_construct_external_context = TryToConstructExternalContext
            @IMPORT_typing.overload
            async def TestLongRunningFetch(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: Greeter.TestLongRunningFetchRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.TestLongRunningFetchTask:
                ...

            @IMPORT_typing.overload
            async def TestLongRunningFetch(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                sleep_time_seconds: IMPORT_typing.Optional[int] | Unset = UNSET,
            ) -> Greeter.TestLongRunningFetchTask:
                ...

            async def TestLongRunningFetch( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.TestLongRunningFetchRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                sleep_time_seconds: IMPORT_typing.Optional[int] | Unset = UNSET,
            ) -> Greeter.TestLongRunningFetchTask:
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.WorkflowContext, IMPORT_reboot_aio_external.ExternalContext])
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.TestLongRunningFetchRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.TestLongRunningFetchRequest)


                __request__: IMPORT_typing.Optional[Greeter.TestLongRunningFetchRequest] = None
                if isinstance(__request_or_options__, Greeter.TestLongRunningFetchRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

                    assert sleep_time_seconds is UNSET

                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = GreeterTestLongRunningFetchRequestFromInputFields(
                        sleep_time_seconds=sleep_time_seconds,
                    )
                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                __task_id__ = await __this__._tasks(
                    __context__
                ).TestLongRunningFetch(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

                return Greeter.TestLongRunningFetchTask(
                    __context__,
                    task_id=__task_id__,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            test_long_running_fetch = TestLongRunningFetch
            @IMPORT_typing.overload
            async def TestLongRunningWriter(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: Greeter.TestLongRunningWriterRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.TestLongRunningWriterTask:
                ...

            @IMPORT_typing.overload
            async def TestLongRunningWriter(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.TestLongRunningWriterTask:
                ...

            async def TestLongRunningWriter( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.TestLongRunningWriterRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.TestLongRunningWriterTask:
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.WorkflowContext, IMPORT_reboot_aio_external.ExternalContext])
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.TestLongRunningWriterRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.TestLongRunningWriterRequest)


                __request__: IMPORT_typing.Optional[Greeter.TestLongRunningWriterRequest] = None
                if isinstance(__request_or_options__, Greeter.TestLongRunningWriterRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)


                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = GreeterTestLongRunningWriterRequestFromInputFields(
                    )
                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                __task_id__ = await __this__._tasks(
                    __context__
                ).TestLongRunningWriter(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

                return Greeter.TestLongRunningWriterTask(
                    __context__,
                    task_id=__task_id__,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            test_long_running_writer = TestLongRunningWriter
            @IMPORT_typing.overload
            async def GetWholeState(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: Greeter.GetWholeStateRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.GetWholeStateTask:
                ...

            @IMPORT_typing.overload
            async def GetWholeState(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.GetWholeStateTask:
                ...

            async def GetWholeState( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.GetWholeStateRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.GetWholeStateTask:
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.WorkflowContext, IMPORT_reboot_aio_external.ExternalContext])
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.GetWholeStateRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.GetWholeStateRequest)


                __request__: IMPORT_typing.Optional[Greeter.GetWholeStateRequest] = None
                if isinstance(__request_or_options__, Greeter.GetWholeStateRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)


                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = GreeterGetWholeStateRequestFromInputFields(
                    )
                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                __task_id__ = await __this__._tasks(
                    __context__
                ).GetWholeState(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

                return Greeter.GetWholeStateTask(
                    __context__,
                    task_id=__task_id__,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            get_whole_state = GetWholeState
            @IMPORT_typing.overload
            async def FailWithException(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: Greeter.FailWithExceptionRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.FailWithExceptionTask:
                ...

            @IMPORT_typing.overload
            async def FailWithException(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.FailWithExceptionTask:
                ...

            async def FailWithException( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.FailWithExceptionRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.FailWithExceptionTask:
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.WorkflowContext, IMPORT_reboot_aio_external.ExternalContext])
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.FailWithExceptionRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.FailWithExceptionRequest)


                __request__: IMPORT_typing.Optional[Greeter.FailWithExceptionRequest] = None
                if isinstance(__request_or_options__, Greeter.FailWithExceptionRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)


                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = GreeterFailWithExceptionRequestFromInputFields(
                    )
                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                __task_id__ = await __this__._tasks(
                    __context__
                ).FailWithException(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

                return Greeter.FailWithExceptionTask(
                    __context__,
                    task_id=__task_id__,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            fail_with_exception = FailWithException
            @IMPORT_typing.overload
            async def FailWithAborted(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: Greeter.FailWithAbortedRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.FailWithAbortedTask:
                ...

            @IMPORT_typing.overload
            async def FailWithAborted(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.FailWithAbortedTask:
                ...

            async def FailWithAborted( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.FailWithAbortedRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.FailWithAbortedTask:
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.WorkflowContext, IMPORT_reboot_aio_external.ExternalContext])
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.FailWithAbortedRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.FailWithAbortedRequest)


                __request__: IMPORT_typing.Optional[Greeter.FailWithAbortedRequest] = None
                if isinstance(__request_or_options__, Greeter.FailWithAbortedRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)


                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = GreeterFailWithAbortedRequestFromInputFields(
                    )
                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                __task_id__ = await __this__._tasks(
                    __context__
                ).FailWithAborted(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

                return Greeter.FailWithAbortedTask(
                    __context__,
                    task_id=__task_id__,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            fail_with_aborted = FailWithAborted
            @IMPORT_typing.overload
            async def Workflow(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: Greeter.WorkflowRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.WorkflowTask:
                ...

            @IMPORT_typing.overload
            async def Workflow(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.WorkflowTask:
                ...

            async def Workflow( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.WorkflowRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.WorkflowTask:
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.WorkflowContext, IMPORT_reboot_aio_external.ExternalContext])
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.WorkflowRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.WorkflowRequest)


                __request__: IMPORT_typing.Optional[Greeter.WorkflowRequest] = None
                if isinstance(__request_or_options__, Greeter.WorkflowRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)


                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = GreeterWorkflowRequestFromInputFields(
                    )
                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                __task_id__ = await __this__._tasks(
                    __context__
                ).Workflow(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

                return Greeter.WorkflowTask(
                    __context__,
                    task_id=__task_id__,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            workflow = Workflow
            @IMPORT_typing.overload
            async def DangerousFields(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: Greeter.DangerousFieldsRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.DangerousFieldsTask:
                ...

            @IMPORT_typing.overload
            async def DangerousFields(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                metadata: IMPORT_typing.Optional[int] | Unset = UNSET,
                request: IMPORT_typing.Optional[int] | Unset = UNSET,
                response: IMPORT_typing.Optional[int] | Unset = UNSET,
                self: IMPORT_typing.Optional[int] | Unset = UNSET,
                context: IMPORT_typing.Optional[int] | Unset = UNSET,
                bearer_token: IMPORT_typing.Optional[int] | Unset = UNSET,
                reboot: IMPORT_typing.Optional[int] | Unset = UNSET,
                uuid: IMPORT_typing.Optional[int] | Unset = UNSET,
                stub: IMPORT_typing.Optional[int] | Unset = UNSET,
                idempotency: IMPORT_typing.Optional[str] | Unset = UNSET,
                state_id: IMPORT_typing.Optional[str] | Unset = UNSET,
            ) -> Greeter.DangerousFieldsTask:
                ...

            async def DangerousFields( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.DangerousFieldsRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                metadata: IMPORT_typing.Optional[int] | Unset = UNSET,
                request: IMPORT_typing.Optional[int] | Unset = UNSET,
                response: IMPORT_typing.Optional[int] | Unset = UNSET,
                self: IMPORT_typing.Optional[int] | Unset = UNSET,
                context: IMPORT_typing.Optional[int] | Unset = UNSET,
                bearer_token: IMPORT_typing.Optional[int] | Unset = UNSET,
                reboot: IMPORT_typing.Optional[int] | Unset = UNSET,
                uuid: IMPORT_typing.Optional[int] | Unset = UNSET,
                stub: IMPORT_typing.Optional[int] | Unset = UNSET,
                idempotency: IMPORT_typing.Optional[str] | Unset = UNSET,
                state_id: IMPORT_typing.Optional[str] | Unset = UNSET,
            ) -> Greeter.DangerousFieldsTask:
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.WorkflowContext, IMPORT_reboot_aio_external.ExternalContext])
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.DangerousFieldsRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.DangerousFieldsRequest)


                __request__: IMPORT_typing.Optional[Greeter.DangerousFieldsRequest] = None
                if isinstance(__request_or_options__, Greeter.DangerousFieldsRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

                    assert metadata is UNSET
                    assert request is UNSET
                    assert response is UNSET
                    assert self is UNSET
                    assert context is UNSET
                    assert bearer_token is UNSET
                    assert reboot is UNSET
                    assert uuid is UNSET
                    assert stub is UNSET
                    assert idempotency is UNSET
                    assert state_id is UNSET

                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = GreeterDangerousFieldsRequestFromInputFields(
                        metadata=metadata,
                        request=request,
                        response=response,
                        self=self,
                        context=context,
                        bearer_token=bearer_token,
                        reboot=reboot,
                        uuid=uuid,
                        stub=stub,
                        idempotency=idempotency,
                        state_id=state_id,
                    )
                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                __task_id__ = await __this__._tasks(
                    __context__
                ).DangerousFields(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

                return Greeter.DangerousFieldsTask(
                    __context__,
                    task_id=__task_id__,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            dangerous_fields = DangerousFields
            @IMPORT_typing.overload
            async def StoreRecursiveMessage(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: Greeter.StoreRecursiveMessageRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.StoreRecursiveMessageTask:
                ...

            @IMPORT_typing.overload
            async def StoreRecursiveMessage(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                message: IMPORT_typing.Optional[tests.reboot.greeter_pb2.RecursiveMessage] | Unset = UNSET,
            ) -> Greeter.StoreRecursiveMessageTask:
                ...

            async def StoreRecursiveMessage( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.StoreRecursiveMessageRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                message: IMPORT_typing.Optional[tests.reboot.greeter_pb2.RecursiveMessage] | Unset = UNSET,
            ) -> Greeter.StoreRecursiveMessageTask:
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.WorkflowContext, IMPORT_reboot_aio_external.ExternalContext])
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.StoreRecursiveMessageRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.StoreRecursiveMessageRequest)


                __request__: IMPORT_typing.Optional[Greeter.StoreRecursiveMessageRequest] = None
                if isinstance(__request_or_options__, Greeter.StoreRecursiveMessageRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

                    assert message is UNSET

                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = GreeterStoreRecursiveMessageRequestFromInputFields(
                        message=message,
                    )
                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                __task_id__ = await __this__._tasks(
                    __context__
                ).StoreRecursiveMessage(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

                return Greeter.StoreRecursiveMessageTask(
                    __context__,
                    task_id=__task_id__,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            store_recursive_message = StoreRecursiveMessage
            @IMPORT_typing.overload
            async def ReadRecursiveMessage(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: Greeter.ReadRecursiveMessageRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.ReadRecursiveMessageTask:
                ...

            @IMPORT_typing.overload
            async def ReadRecursiveMessage(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.ReadRecursiveMessageTask:
                ...

            async def ReadRecursiveMessage( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.ReadRecursiveMessageRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.ReadRecursiveMessageTask:
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.WorkflowContext, IMPORT_reboot_aio_external.ExternalContext])
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.ReadRecursiveMessageRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.ReadRecursiveMessageRequest)


                __request__: IMPORT_typing.Optional[Greeter.ReadRecursiveMessageRequest] = None
                if isinstance(__request_or_options__, Greeter.ReadRecursiveMessageRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)


                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = GreeterReadRecursiveMessageRequestFromInputFields(
                    )
                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                __task_id__ = await __this__._tasks(
                    __context__
                ).ReadRecursiveMessage(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

                return Greeter.ReadRecursiveMessageTask(
                    __context__,
                    task_id=__task_id__,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            read_recursive_message = ReadRecursiveMessage
            @IMPORT_typing.overload
            async def ConstructAndStoreRecursiveMessage(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: Greeter.ConstructAndStoreRecursiveMessageRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> Greeter.ConstructAndStoreRecursiveMessageTask:
                ...

            @IMPORT_typing.overload
            async def ConstructAndStoreRecursiveMessage(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                depth: IMPORT_typing.Optional[int] | Unset = UNSET,
            ) -> Greeter.ConstructAndStoreRecursiveMessageTask:
                ...

            async def ConstructAndStoreRecursiveMessage( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[Greeter.ConstructAndStoreRecursiveMessageRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                depth: IMPORT_typing.Optional[int] | Unset = UNSET,
            ) -> Greeter.ConstructAndStoreRecursiveMessageTask:
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.WorkflowContext, IMPORT_reboot_aio_external.ExternalContext])
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.ConstructAndStoreRecursiveMessageRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.ConstructAndStoreRecursiveMessageRequest)


                __request__: IMPORT_typing.Optional[Greeter.ConstructAndStoreRecursiveMessageRequest] = None
                if isinstance(__request_or_options__, Greeter.ConstructAndStoreRecursiveMessageRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

                    assert depth is UNSET

                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = GreeterConstructAndStoreRecursiveMessageRequestFromInputFields(
                        depth=depth,
                    )
                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                __task_id__ = await __this__._tasks(
                    __context__
                ).ConstructAndStoreRecursiveMessage(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

                return Greeter.ConstructAndStoreRecursiveMessageTask(
                    __context__,
                    task_id=__task_id__,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            construct_and_store_recursive_message = ConstructAndStoreRecursiveMessage

        async def read(
            self,
            context: IMPORT_reboot_aio_contexts.WorkflowContext,
        ) -> Greeter.State:
            return await (
                self.always() if context.within_until()
                else (
                    self.per_iteration() if context.within_loop()
                    else self.per_workflow()
                )
            ).read(context)

        async def write(
            self,
            context: IMPORT_reboot_aio_contexts.WorkflowContext,
            writer: GreeterBaseServicer.InlineWriterCallable[GreeterBaseServicer.InlineWriterCallableResult],
            *,
            type: IMPORT_reboot_aio_workflows.Type | IMPORT_reboot_aio_workflows._Unset = IMPORT_reboot_aio_workflows._UNSET,
        ) -> GreeterBaseServicer.InlineWriterCallableResult:
            """Perform an "inline write" within a workflow.

            `type=` is optional: when omitted, `writer`'s return
            annotation is used.
            """
            # We forward `type=` along to the inner `write` (which
            # also infers if not passed). Pass through whichever
            # form the user supplied -- explicit class or the
            # `_UNSET` sentinel to trigger inference downstream.
            return await (
                self.always() if context.within_until()
                else (
                    self.per_iteration() if context.within_loop()
                    else self.per_workflow()
                )
            ).write(
                context, writer, type=type
            )

        # Greeter specific methods:
        @IMPORT_typing.overload
        async def Greet(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: Greeter.GreetRequest,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        ) -> Greeter.GreetResponse:
            ...

        @IMPORT_typing.overload
        async def Greet(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
            name: IMPORT_typing.Optional[str] | Unset = UNSET,
            metadata: IMPORT_typing.Optional[dict[str, str]] | Unset = UNSET,
        ) -> Greeter.GreetResponse:
            ...

        async def Greet( # type: ignore[misc]
            __this__,
            __context__: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[Greeter.GreetRequest | IMPORT_reboot_aio_call.Options] = None,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
            name: IMPORT_typing.Optional[str] | Unset = UNSET,
            metadata: IMPORT_typing.Optional[dict[str, str]] | Unset = UNSET,
        ) -> Greeter.GreetResponse:
            # UX improvement: check that neither positional argument was accidentally
            # given a gRPC request type.
            IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.GreetRequest)
            IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.GreetRequest)

            __request__: IMPORT_typing.Optional[Greeter.GreetRequest] = None

            if isinstance(__request_or_options__, Greeter.GreetRequest):
                assert __request_or_options__ is not None
                assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

                assert name is UNSET
                assert metadata is UNSET

                __request__ = __request_or_options__
            else:
                assert __options__ is None
                assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                __options__ = __request_or_options__

                __request__ = GreeterGreetRequestFromInputFields(
                    name=name,
                    metadata=metadata,
                )

            # Within a `workflow`, all "bare" calls are
            # `per_workflow()` calls, unless we're within a control
            # loop, in which case they are syntactic sugar for
            # `per_iteration()`.
            #
            # Unless we are "within until" in which case all "bare"
            # calls are `.always().
            if __idempotency__ is None:
                if isinstance(__context__, IMPORT_reboot_aio_contexts.WorkflowContext):
                    return await (
                        __this__.always() if __context__.within_until()
                        else (
                            __this__.per_iteration() if __context__.within_loop()
                            else __this__.per_workflow()
                        )
                    ).Greet(
                        __context__,
                        __request__,
                        __options__ or IMPORT_reboot_aio_call.Options(),
                    )
                elif isinstance(__context__, IMPORT_reboot_aio_external.InitializeContext):
                    return await __this__.idempotently().Greet(
                        __context__,
                        __request__,
                        __options__ or IMPORT_reboot_aio_call.Options(),
                    )

            __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
            __bearer_token__: IMPORT_typing.Optional[str] = None
            if __options__ is not None:
                IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                if __options__.metadata is not None:
                    __metadata__ = __options__.metadata
                if __options__.bearer_token is not None:
                    __bearer_token__ = __options__.bearer_token
            return GreeterGreetResponseFromProto(
                await __this__._reader(__context__).Greet(
                    __request__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                    idempotency=__idempotency__,
                )
            )
        # Keep the original functions on the client, so old code will
        # continue to work, but use the new 'snake_case' method in
        # the new code.
        greet = Greet
        @IMPORT_typing.overload
        async def SetAdjective(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: Greeter.SetAdjectiveRequest,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        ) -> Greeter.SetAdjectiveResponse:
            ...

        @IMPORT_typing.overload
        async def SetAdjective(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
            adjective: IMPORT_typing.Optional[str] | Unset = UNSET,
            arbitrary_message: IMPORT_typing.Optional[tests.reboot.greeter_pb2.SetAdjectiveRequest.NestedMessage] | Unset = UNSET,
            revert_after_1_second: IMPORT_typing.Optional[bool] | Unset = UNSET,
        ) -> Greeter.SetAdjectiveResponse:
            ...

        async def SetAdjective( # type: ignore[misc]
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[Greeter.SetAdjectiveRequest | IMPORT_reboot_aio_call.Options] = None,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
            adjective: IMPORT_typing.Optional[str] | Unset = UNSET,
            arbitrary_message: IMPORT_typing.Optional[tests.reboot.greeter_pb2.SetAdjectiveRequest.NestedMessage] | Unset = UNSET,
            revert_after_1_second: IMPORT_typing.Optional[bool] | Unset = UNSET,
        ) -> Greeter.SetAdjectiveResponse:
            # UX improvement: check that neither positional argument was accidentally
            # given a gRPC request type.
            IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.SetAdjectiveRequest)
            IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.SetAdjectiveRequest)

            __request__: IMPORT_typing.Optional[Greeter.SetAdjectiveRequest] = None
            if isinstance(__request_or_options__, Greeter.SetAdjectiveRequest):
                assert __request_or_options__ is not None
                assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

                assert adjective is UNSET
                assert arbitrary_message is UNSET
                assert revert_after_1_second is UNSET

                __request__ = __request_or_options__
            else:
                assert __options__ is None
                assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                __options__ = __request_or_options__

                __request__ = GreeterSetAdjectiveRequestFromInputFields(
                    adjective=adjective,
                    arbitrary_message=arbitrary_message,
                    revert_after_1_second=revert_after_1_second,
                )

            # Within a `workflow`, all "bare" calls are
            # `per_workflow()` calls, unless we're within a control
            # loop, in which case they are syntactic sugar for
            # `per_iteration()`.
            if __idempotency__ is None:
                if isinstance(__context__, IMPORT_reboot_aio_contexts.WorkflowContext):
                    return await (
                        __this__.per_iteration() if __context__.within_loop()
                        else __this__.per_workflow()
                    ).SetAdjective(
                        __context__,
                        __request__,
                        __options__ or IMPORT_reboot_aio_call.Options(),
                    )
                elif isinstance(__context__, IMPORT_reboot_aio_external.InitializeContext):
                    return await __this__.idempotently().SetAdjective(
                        __context__,
                        __request__,
                        __options__ or IMPORT_reboot_aio_call.Options(),
                    )

            __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
            __bearer_token__: IMPORT_typing.Optional[str] = None
            if __options__ is not None:
                IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                if __options__.metadata is not None:
                    __metadata__ = __options__.metadata
                if __options__.bearer_token is not None:
                    __bearer_token__ = __options__.bearer_token

            return GreeterSetAdjectiveResponseFromProto(
                await __this__._writer(__context__).SetAdjective(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )
            )

        # Keep the original functions on the client, so old code will
        # continue to work, but use the new 'snake_case' method in
        # the new code.
        set_adjective = SetAdjective
        @IMPORT_typing.overload
        async def TransactionSetAdjective(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: Greeter.TransactionSetAdjectiveRequest,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        ) -> Greeter.TransactionSetAdjectiveResponse:
            ...

        @IMPORT_typing.overload
        async def TransactionSetAdjective(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
            adjective: IMPORT_typing.Optional[str] | Unset = UNSET,
            arbitrary_message: IMPORT_typing.Optional[tests.reboot.greeter_pb2.SetAdjectiveRequest.NestedMessage] | Unset = UNSET,
            revert_after_1_second: IMPORT_typing.Optional[bool] | Unset = UNSET,
        ) -> Greeter.TransactionSetAdjectiveResponse:
            ...

        async def TransactionSetAdjective( # type: ignore[misc]
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[Greeter.TransactionSetAdjectiveRequest | IMPORT_reboot_aio_call.Options] = None,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
            adjective: IMPORT_typing.Optional[str] | Unset = UNSET,
            arbitrary_message: IMPORT_typing.Optional[tests.reboot.greeter_pb2.SetAdjectiveRequest.NestedMessage] | Unset = UNSET,
            revert_after_1_second: IMPORT_typing.Optional[bool] | Unset = UNSET,
        ) -> Greeter.TransactionSetAdjectiveResponse:
            # UX improvement: check that neither positional argument was accidentally
            # given a gRPC request type.
            IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.TransactionSetAdjectiveRequest)
            IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.TransactionSetAdjectiveRequest)

            __request__: IMPORT_typing.Optional[Greeter.TransactionSetAdjectiveRequest] = None
            if isinstance(__request_or_options__, Greeter.TransactionSetAdjectiveRequest):
                assert __request_or_options__ is not None
                assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

                assert adjective is UNSET
                assert arbitrary_message is UNSET
                assert revert_after_1_second is UNSET

                __request__ = __request_or_options__
            else:
                assert __options__ is None
                assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                __options__ = __request_or_options__

                __request__ = GreeterTransactionSetAdjectiveRequestFromInputFields(
                    adjective=adjective,
                    arbitrary_message=arbitrary_message,
                    revert_after_1_second=revert_after_1_second,
                )

            # Within a `workflow`, all "bare" calls are
            # `per_workflow()` calls, unless we're within a control
            # loop, in which case they are syntactic sugar for
            # `per_iteration()`.
            if __idempotency__ is None:
                if isinstance(__context__, IMPORT_reboot_aio_contexts.WorkflowContext):
                    return await (
                        __this__.per_iteration() if __context__.within_loop()
                        else __this__.per_workflow()
                    ).TransactionSetAdjective(
                        __context__,
                        __request__,
                        __options__ or IMPORT_reboot_aio_call.Options(),
                    )
                elif isinstance(__context__, IMPORT_reboot_aio_external.InitializeContext):
                    return await __this__.idempotently().TransactionSetAdjective(
                        __context__,
                        __request__,
                        __options__ or IMPORT_reboot_aio_call.Options(),
                    )

            __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
            __bearer_token__: IMPORT_typing.Optional[str] = None
            if __options__ is not None:
                IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                if __options__.metadata is not None:
                    __metadata__ = __options__.metadata
                if __options__.bearer_token is not None:
                    __bearer_token__ = __options__.bearer_token

            return GreeterTransactionSetAdjectiveResponseFromProto(
                await __this__._workflow(__context__).TransactionSetAdjective(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )
            )

        # Keep the original functions on the client, so old code will
        # continue to work, but use the new 'snake_case' method in
        # the new code.
        transaction_set_adjective = TransactionSetAdjective
        @IMPORT_typing.overload
        async def TryToConstructContext(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: Greeter.TryToConstructContextRequest,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        ) -> Greeter.TryToConstructContextResponse:
            ...

        @IMPORT_typing.overload
        async def TryToConstructContext(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        ) -> Greeter.TryToConstructContextResponse:
            ...

        async def TryToConstructContext( # type: ignore[misc]
            __this__,
            __context__: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[Greeter.TryToConstructContextRequest | IMPORT_reboot_aio_call.Options] = None,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        ) -> Greeter.TryToConstructContextResponse:
            # UX improvement: check that neither positional argument was accidentally
            # given a gRPC request type.
            IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.TryToConstructContextRequest)
            IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.TryToConstructContextRequest)

            __request__: IMPORT_typing.Optional[Greeter.TryToConstructContextRequest] = None

            if isinstance(__request_or_options__, Greeter.TryToConstructContextRequest):
                assert __request_or_options__ is not None
                assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)


                __request__ = __request_or_options__
            else:
                assert __options__ is None
                assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                __options__ = __request_or_options__

                __request__ = GreeterTryToConstructContextRequestFromInputFields(
                )

            # Within a `workflow`, all "bare" calls are
            # `per_workflow()` calls, unless we're within a control
            # loop, in which case they are syntactic sugar for
            # `per_iteration()`.
            #
            # Unless we are "within until" in which case all "bare"
            # calls are `.always().
            if __idempotency__ is None:
                if isinstance(__context__, IMPORT_reboot_aio_contexts.WorkflowContext):
                    return await (
                        __this__.always() if __context__.within_until()
                        else (
                            __this__.per_iteration() if __context__.within_loop()
                            else __this__.per_workflow()
                        )
                    ).TryToConstructContext(
                        __context__,
                        __request__,
                        __options__ or IMPORT_reboot_aio_call.Options(),
                    )
                elif isinstance(__context__, IMPORT_reboot_aio_external.InitializeContext):
                    return await __this__.idempotently().TryToConstructContext(
                        __context__,
                        __request__,
                        __options__ or IMPORT_reboot_aio_call.Options(),
                    )

            __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
            __bearer_token__: IMPORT_typing.Optional[str] = None
            if __options__ is not None:
                IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                if __options__.metadata is not None:
                    __metadata__ = __options__.metadata
                if __options__.bearer_token is not None:
                    __bearer_token__ = __options__.bearer_token
            return GreeterTryToConstructContextResponseFromProto(
                await __this__._reader(__context__).TryToConstructContext(
                    __request__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                    idempotency=__idempotency__,
                )
            )
        # Keep the original functions on the client, so old code will
        # continue to work, but use the new 'snake_case' method in
        # the new code.
        try_to_construct_context = TryToConstructContext
        @IMPORT_typing.overload
        async def TryToConstructExternalContext(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: Greeter.TryToConstructExternalContextRequest,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        ) -> Greeter.TryToConstructExternalContextResponse:
            ...

        @IMPORT_typing.overload
        async def TryToConstructExternalContext(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        ) -> Greeter.TryToConstructExternalContextResponse:
            ...

        async def TryToConstructExternalContext( # type: ignore[misc]
            __this__,
            __context__: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[Greeter.TryToConstructExternalContextRequest | IMPORT_reboot_aio_call.Options] = None,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        ) -> Greeter.TryToConstructExternalContextResponse:
            # UX improvement: check that neither positional argument was accidentally
            # given a gRPC request type.
            IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.TryToConstructExternalContextRequest)
            IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.TryToConstructExternalContextRequest)

            __request__: IMPORT_typing.Optional[Greeter.TryToConstructExternalContextRequest] = None

            if isinstance(__request_or_options__, Greeter.TryToConstructExternalContextRequest):
                assert __request_or_options__ is not None
                assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)


                __request__ = __request_or_options__
            else:
                assert __options__ is None
                assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                __options__ = __request_or_options__

                __request__ = GreeterTryToConstructExternalContextRequestFromInputFields(
                )

            # Within a `workflow`, all "bare" calls are
            # `per_workflow()` calls, unless we're within a control
            # loop, in which case they are syntactic sugar for
            # `per_iteration()`.
            #
            # Unless we are "within until" in which case all "bare"
            # calls are `.always().
            if __idempotency__ is None:
                if isinstance(__context__, IMPORT_reboot_aio_contexts.WorkflowContext):
                    return await (
                        __this__.always() if __context__.within_until()
                        else (
                            __this__.per_iteration() if __context__.within_loop()
                            else __this__.per_workflow()
                        )
                    ).TryToConstructExternalContext(
                        __context__,
                        __request__,
                        __options__ or IMPORT_reboot_aio_call.Options(),
                    )
                elif isinstance(__context__, IMPORT_reboot_aio_external.InitializeContext):
                    return await __this__.idempotently().TryToConstructExternalContext(
                        __context__,
                        __request__,
                        __options__ or IMPORT_reboot_aio_call.Options(),
                    )

            __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
            __bearer_token__: IMPORT_typing.Optional[str] = None
            if __options__ is not None:
                IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                if __options__.metadata is not None:
                    __metadata__ = __options__.metadata
                if __options__.bearer_token is not None:
                    __bearer_token__ = __options__.bearer_token
            return GreeterTryToConstructExternalContextResponseFromProto(
                await __this__._reader(__context__).TryToConstructExternalContext(
                    __request__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                    idempotency=__idempotency__,
                )
            )
        # Keep the original functions on the client, so old code will
        # continue to work, but use the new 'snake_case' method in
        # the new code.
        try_to_construct_external_context = TryToConstructExternalContext
        @IMPORT_typing.overload
        async def TestLongRunningFetch(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: Greeter.TestLongRunningFetchRequest,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        ) -> Greeter.TestLongRunningFetchResponse:
            ...

        @IMPORT_typing.overload
        async def TestLongRunningFetch(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
            sleep_time_seconds: IMPORT_typing.Optional[int] | Unset = UNSET,
        ) -> Greeter.TestLongRunningFetchResponse:
            ...

        async def TestLongRunningFetch( # type: ignore[misc]
            __this__,
            __context__: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[Greeter.TestLongRunningFetchRequest | IMPORT_reboot_aio_call.Options] = None,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
            sleep_time_seconds: IMPORT_typing.Optional[int] | Unset = UNSET,
        ) -> Greeter.TestLongRunningFetchResponse:
            # UX improvement: check that neither positional argument was accidentally
            # given a gRPC request type.
            IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.TestLongRunningFetchRequest)
            IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.TestLongRunningFetchRequest)

            __request__: IMPORT_typing.Optional[Greeter.TestLongRunningFetchRequest] = None

            if isinstance(__request_or_options__, Greeter.TestLongRunningFetchRequest):
                assert __request_or_options__ is not None
                assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

                assert sleep_time_seconds is UNSET

                __request__ = __request_or_options__
            else:
                assert __options__ is None
                assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                __options__ = __request_or_options__

                __request__ = GreeterTestLongRunningFetchRequestFromInputFields(
                    sleep_time_seconds=sleep_time_seconds,
                )

            # Within a `workflow`, all "bare" calls are
            # `per_workflow()` calls, unless we're within a control
            # loop, in which case they are syntactic sugar for
            # `per_iteration()`.
            #
            # Unless we are "within until" in which case all "bare"
            # calls are `.always().
            if __idempotency__ is None:
                if isinstance(__context__, IMPORT_reboot_aio_contexts.WorkflowContext):
                    return await (
                        __this__.always() if __context__.within_until()
                        else (
                            __this__.per_iteration() if __context__.within_loop()
                            else __this__.per_workflow()
                        )
                    ).TestLongRunningFetch(
                        __context__,
                        __request__,
                        __options__ or IMPORT_reboot_aio_call.Options(),
                    )
                elif isinstance(__context__, IMPORT_reboot_aio_external.InitializeContext):
                    return await __this__.idempotently().TestLongRunningFetch(
                        __context__,
                        __request__,
                        __options__ or IMPORT_reboot_aio_call.Options(),
                    )

            __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
            __bearer_token__: IMPORT_typing.Optional[str] = None
            if __options__ is not None:
                IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                if __options__.metadata is not None:
                    __metadata__ = __options__.metadata
                if __options__.bearer_token is not None:
                    __bearer_token__ = __options__.bearer_token
            return GreeterTestLongRunningFetchResponseFromProto(
                await __this__._reader(__context__).TestLongRunningFetch(
                    __request__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                    idempotency=__idempotency__,
                )
            )
        # Keep the original functions on the client, so old code will
        # continue to work, but use the new 'snake_case' method in
        # the new code.
        test_long_running_fetch = TestLongRunningFetch
        @IMPORT_typing.overload
        async def TestLongRunningWriter(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: Greeter.TestLongRunningWriterRequest,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        ) -> Greeter.TestLongRunningWriterResponse:
            ...

        @IMPORT_typing.overload
        async def TestLongRunningWriter(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        ) -> Greeter.TestLongRunningWriterResponse:
            ...

        async def TestLongRunningWriter( # type: ignore[misc]
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[Greeter.TestLongRunningWriterRequest | IMPORT_reboot_aio_call.Options] = None,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        ) -> Greeter.TestLongRunningWriterResponse:
            # UX improvement: check that neither positional argument was accidentally
            # given a gRPC request type.
            IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.TestLongRunningWriterRequest)
            IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.TestLongRunningWriterRequest)

            __request__: IMPORT_typing.Optional[Greeter.TestLongRunningWriterRequest] = None
            if isinstance(__request_or_options__, Greeter.TestLongRunningWriterRequest):
                assert __request_or_options__ is not None
                assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)


                __request__ = __request_or_options__
            else:
                assert __options__ is None
                assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                __options__ = __request_or_options__

                __request__ = GreeterTestLongRunningWriterRequestFromInputFields(
                )

            # Within a `workflow`, all "bare" calls are
            # `per_workflow()` calls, unless we're within a control
            # loop, in which case they are syntactic sugar for
            # `per_iteration()`.
            if __idempotency__ is None:
                if isinstance(__context__, IMPORT_reboot_aio_contexts.WorkflowContext):
                    return await (
                        __this__.per_iteration() if __context__.within_loop()
                        else __this__.per_workflow()
                    ).TestLongRunningWriter(
                        __context__,
                        __request__,
                        __options__ or IMPORT_reboot_aio_call.Options(),
                    )
                elif isinstance(__context__, IMPORT_reboot_aio_external.InitializeContext):
                    return await __this__.idempotently().TestLongRunningWriter(
                        __context__,
                        __request__,
                        __options__ or IMPORT_reboot_aio_call.Options(),
                    )

            __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
            __bearer_token__: IMPORT_typing.Optional[str] = None
            if __options__ is not None:
                IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                if __options__.metadata is not None:
                    __metadata__ = __options__.metadata
                if __options__.bearer_token is not None:
                    __bearer_token__ = __options__.bearer_token

            return GreeterTestLongRunningWriterResponseFromProto(
                await __this__._writer(__context__).TestLongRunningWriter(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )
            )

        # Keep the original functions on the client, so old code will
        # continue to work, but use the new 'snake_case' method in
        # the new code.
        test_long_running_writer = TestLongRunningWriter
        @IMPORT_typing.overload
        async def GetWholeState(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: Greeter.GetWholeStateRequest,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        ) -> Greeter.GetWholeStateResponse:
            ...

        @IMPORT_typing.overload
        async def GetWholeState(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        ) -> Greeter.GetWholeStateResponse:
            ...

        async def GetWholeState( # type: ignore[misc]
            __this__,
            __context__: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[Greeter.GetWholeStateRequest | IMPORT_reboot_aio_call.Options] = None,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        ) -> Greeter.GetWholeStateResponse:
            # UX improvement: check that neither positional argument was accidentally
            # given a gRPC request type.
            IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.GetWholeStateRequest)
            IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.GetWholeStateRequest)

            __request__: IMPORT_typing.Optional[Greeter.GetWholeStateRequest] = None

            if isinstance(__request_or_options__, Greeter.GetWholeStateRequest):
                assert __request_or_options__ is not None
                assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)


                __request__ = __request_or_options__
            else:
                assert __options__ is None
                assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                __options__ = __request_or_options__

                __request__ = GreeterGetWholeStateRequestFromInputFields(
                )

            # Within a `workflow`, all "bare" calls are
            # `per_workflow()` calls, unless we're within a control
            # loop, in which case they are syntactic sugar for
            # `per_iteration()`.
            #
            # Unless we are "within until" in which case all "bare"
            # calls are `.always().
            if __idempotency__ is None:
                if isinstance(__context__, IMPORT_reboot_aio_contexts.WorkflowContext):
                    return await (
                        __this__.always() if __context__.within_until()
                        else (
                            __this__.per_iteration() if __context__.within_loop()
                            else __this__.per_workflow()
                        )
                    ).GetWholeState(
                        __context__,
                        __request__,
                        __options__ or IMPORT_reboot_aio_call.Options(),
                    )
                elif isinstance(__context__, IMPORT_reboot_aio_external.InitializeContext):
                    return await __this__.idempotently().GetWholeState(
                        __context__,
                        __request__,
                        __options__ or IMPORT_reboot_aio_call.Options(),
                    )

            __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
            __bearer_token__: IMPORT_typing.Optional[str] = None
            if __options__ is not None:
                IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                if __options__.metadata is not None:
                    __metadata__ = __options__.metadata
                if __options__.bearer_token is not None:
                    __bearer_token__ = __options__.bearer_token
            return GreeterGetWholeStateResponseFromProto(
                await __this__._reader(__context__).GetWholeState(
                    __request__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                    idempotency=__idempotency__,
                )
            )
        # Keep the original functions on the client, so old code will
        # continue to work, but use the new 'snake_case' method in
        # the new code.
        get_whole_state = GetWholeState
        @IMPORT_typing.overload
        async def FailWithException(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: Greeter.FailWithExceptionRequest,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        ) -> Greeter.FailWithExceptionResponse:
            ...

        @IMPORT_typing.overload
        async def FailWithException(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        ) -> Greeter.FailWithExceptionResponse:
            ...

        async def FailWithException( # type: ignore[misc]
            __this__,
            __context__: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[Greeter.FailWithExceptionRequest | IMPORT_reboot_aio_call.Options] = None,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        ) -> Greeter.FailWithExceptionResponse:
            # UX improvement: check that neither positional argument was accidentally
            # given a gRPC request type.
            IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.FailWithExceptionRequest)
            IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.FailWithExceptionRequest)

            __request__: IMPORT_typing.Optional[Greeter.FailWithExceptionRequest] = None

            if isinstance(__request_or_options__, Greeter.FailWithExceptionRequest):
                assert __request_or_options__ is not None
                assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)


                __request__ = __request_or_options__
            else:
                assert __options__ is None
                assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                __options__ = __request_or_options__

                __request__ = GreeterFailWithExceptionRequestFromInputFields(
                )

            # Within a `workflow`, all "bare" calls are
            # `per_workflow()` calls, unless we're within a control
            # loop, in which case they are syntactic sugar for
            # `per_iteration()`.
            #
            # Unless we are "within until" in which case all "bare"
            # calls are `.always().
            if __idempotency__ is None:
                if isinstance(__context__, IMPORT_reboot_aio_contexts.WorkflowContext):
                    return await (
                        __this__.always() if __context__.within_until()
                        else (
                            __this__.per_iteration() if __context__.within_loop()
                            else __this__.per_workflow()
                        )
                    ).FailWithException(
                        __context__,
                        __request__,
                        __options__ or IMPORT_reboot_aio_call.Options(),
                    )
                elif isinstance(__context__, IMPORT_reboot_aio_external.InitializeContext):
                    return await __this__.idempotently().FailWithException(
                        __context__,
                        __request__,
                        __options__ or IMPORT_reboot_aio_call.Options(),
                    )

            __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
            __bearer_token__: IMPORT_typing.Optional[str] = None
            if __options__ is not None:
                IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                if __options__.metadata is not None:
                    __metadata__ = __options__.metadata
                if __options__.bearer_token is not None:
                    __bearer_token__ = __options__.bearer_token
            return GreeterFailWithExceptionResponseFromProto(
                await __this__._reader(__context__).FailWithException(
                    __request__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                    idempotency=__idempotency__,
                )
            )
        # Keep the original functions on the client, so old code will
        # continue to work, but use the new 'snake_case' method in
        # the new code.
        fail_with_exception = FailWithException
        @IMPORT_typing.overload
        async def FailWithAborted(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: Greeter.FailWithAbortedRequest,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        ) -> Greeter.FailWithAbortedResponse:
            ...

        @IMPORT_typing.overload
        async def FailWithAborted(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        ) -> Greeter.FailWithAbortedResponse:
            ...

        async def FailWithAborted( # type: ignore[misc]
            __this__,
            __context__: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[Greeter.FailWithAbortedRequest | IMPORT_reboot_aio_call.Options] = None,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        ) -> Greeter.FailWithAbortedResponse:
            # UX improvement: check that neither positional argument was accidentally
            # given a gRPC request type.
            IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.FailWithAbortedRequest)
            IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.FailWithAbortedRequest)

            __request__: IMPORT_typing.Optional[Greeter.FailWithAbortedRequest] = None

            if isinstance(__request_or_options__, Greeter.FailWithAbortedRequest):
                assert __request_or_options__ is not None
                assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)


                __request__ = __request_or_options__
            else:
                assert __options__ is None
                assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                __options__ = __request_or_options__

                __request__ = GreeterFailWithAbortedRequestFromInputFields(
                )

            # Within a `workflow`, all "bare" calls are
            # `per_workflow()` calls, unless we're within a control
            # loop, in which case they are syntactic sugar for
            # `per_iteration()`.
            #
            # Unless we are "within until" in which case all "bare"
            # calls are `.always().
            if __idempotency__ is None:
                if isinstance(__context__, IMPORT_reboot_aio_contexts.WorkflowContext):
                    return await (
                        __this__.always() if __context__.within_until()
                        else (
                            __this__.per_iteration() if __context__.within_loop()
                            else __this__.per_workflow()
                        )
                    ).FailWithAborted(
                        __context__,
                        __request__,
                        __options__ or IMPORT_reboot_aio_call.Options(),
                    )
                elif isinstance(__context__, IMPORT_reboot_aio_external.InitializeContext):
                    return await __this__.idempotently().FailWithAborted(
                        __context__,
                        __request__,
                        __options__ or IMPORT_reboot_aio_call.Options(),
                    )

            __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
            __bearer_token__: IMPORT_typing.Optional[str] = None
            if __options__ is not None:
                IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                if __options__.metadata is not None:
                    __metadata__ = __options__.metadata
                if __options__.bearer_token is not None:
                    __bearer_token__ = __options__.bearer_token
            return GreeterFailWithAbortedResponseFromProto(
                await __this__._reader(__context__).FailWithAborted(
                    __request__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                    idempotency=__idempotency__,
                )
            )
        # Keep the original functions on the client, so old code will
        # continue to work, but use the new 'snake_case' method in
        # the new code.
        fail_with_aborted = FailWithAborted
        @IMPORT_typing.overload
        async def Workflow(
            __this__,
            __context__: IMPORT_reboot_aio_external.ExternalContext | IMPORT_reboot_aio_contexts.WorkflowContext,
            __request_or_options__: Greeter.WorkflowRequest,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
        ) -> Greeter.WorkflowResponse:
            ...

        @IMPORT_typing.overload
        async def Workflow(
            __this__,
            __context__: IMPORT_reboot_aio_external.ExternalContext | IMPORT_reboot_aio_contexts.WorkflowContext,
            __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
        ) -> Greeter.WorkflowResponse:
            ...

        async def Workflow( # type: ignore[misc]
            # In methods which are dealing with user input, (i.e.,
            # proto message field names), we should use '__double_underscored__'
            # variables to avoid any potential name conflicts with the method's
            # parameters.
            # The '__self__' parameter is a convention in Python to
            # indicate that this method is a bound method, so we use
            # '__this__' instead.
            __this__,
            __context__: IMPORT_reboot_aio_external.ExternalContext | IMPORT_reboot_aio_contexts.WorkflowContext,
            __request_or_options__: IMPORT_typing.Optional[Greeter.WorkflowRequest | IMPORT_reboot_aio_call.Options] = None,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
        ) -> Greeter.WorkflowResponse:
            def error_message_supplement():
                if any([isinstance(__context__, t) for t in [IMPORT_reboot_aio_contexts.WriterContext, IMPORT_reboot_aio_contexts.TransactionContext]]):
                    return f"'Workflow' is a workflow and must be scheduled from a '{type(__context__).__name__}' via `await [...].schedule([...]).Workflow(context, [...])`"
                else:
                    return f"'Workflow' is a workflow and can not be called from a '{type(__context__).__name__}'"

            IMPORT_reboot_aio_types.assert_type(
                __context__,
                [IMPORT_reboot_aio_external.ExternalContext, IMPORT_reboot_aio_contexts.WorkflowContext],
                error_message_supplement=error_message_supplement(),
            )

            # UX improvement: check that neither positional argument was accidentally
            # given a gRPC request type.
            IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.WorkflowRequest)
            IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.WorkflowRequest)

            if isinstance(__request_or_options__, Greeter.WorkflowRequest):
                assert __request_or_options__ is not None
                assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)
                __options__ = __options__ or IMPORT_reboot_aio_call.Options()


                return await (
                    await __this__.spawn().Workflow(
                        __context__,
                        __request_or_options__,
                        __options__,
                    )
                )
            else:
                assert __options__ is None
                assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                __options__ = __request_or_options__ or IMPORT_reboot_aio_call.Options()

                return await (
                    await __this__.spawn().Workflow(
                        __context__,
                        GreeterWorkflowRequestFromInputFields(
                        ),
                        __options__,
                    )
                )

        # Keep the original functions on the client, so old code will
        # continue to work, but use the new 'snake_case' method in
        # the new code.
        workflow = Workflow
        @IMPORT_typing.overload
        async def DangerousFields(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: Greeter.DangerousFieldsRequest,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        ) -> Greeter.DangerousFieldsResponse:
            ...

        @IMPORT_typing.overload
        async def DangerousFields(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
            metadata: IMPORT_typing.Optional[int] | Unset = UNSET,
            request: IMPORT_typing.Optional[int] | Unset = UNSET,
            response: IMPORT_typing.Optional[int] | Unset = UNSET,
            self: IMPORT_typing.Optional[int] | Unset = UNSET,
            context: IMPORT_typing.Optional[int] | Unset = UNSET,
            bearer_token: IMPORT_typing.Optional[int] | Unset = UNSET,
            reboot: IMPORT_typing.Optional[int] | Unset = UNSET,
            uuid: IMPORT_typing.Optional[int] | Unset = UNSET,
            stub: IMPORT_typing.Optional[int] | Unset = UNSET,
            idempotency: IMPORT_typing.Optional[str] | Unset = UNSET,
            state_id: IMPORT_typing.Optional[str] | Unset = UNSET,
        ) -> Greeter.DangerousFieldsResponse:
            ...

        async def DangerousFields( # type: ignore[misc]
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[Greeter.DangerousFieldsRequest | IMPORT_reboot_aio_call.Options] = None,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
            metadata: IMPORT_typing.Optional[int] | Unset = UNSET,
            request: IMPORT_typing.Optional[int] | Unset = UNSET,
            response: IMPORT_typing.Optional[int] | Unset = UNSET,
            self: IMPORT_typing.Optional[int] | Unset = UNSET,
            context: IMPORT_typing.Optional[int] | Unset = UNSET,
            bearer_token: IMPORT_typing.Optional[int] | Unset = UNSET,
            reboot: IMPORT_typing.Optional[int] | Unset = UNSET,
            uuid: IMPORT_typing.Optional[int] | Unset = UNSET,
            stub: IMPORT_typing.Optional[int] | Unset = UNSET,
            idempotency: IMPORT_typing.Optional[str] | Unset = UNSET,
            state_id: IMPORT_typing.Optional[str] | Unset = UNSET,
        ) -> Greeter.DangerousFieldsResponse:
            # UX improvement: check that neither positional argument was accidentally
            # given a gRPC request type.
            IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.DangerousFieldsRequest)
            IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.DangerousFieldsRequest)

            __request__: IMPORT_typing.Optional[Greeter.DangerousFieldsRequest] = None
            if isinstance(__request_or_options__, Greeter.DangerousFieldsRequest):
                assert __request_or_options__ is not None
                assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

                assert metadata is UNSET
                assert request is UNSET
                assert response is UNSET
                assert self is UNSET
                assert context is UNSET
                assert bearer_token is UNSET
                assert reboot is UNSET
                assert uuid is UNSET
                assert stub is UNSET
                assert idempotency is UNSET
                assert state_id is UNSET

                __request__ = __request_or_options__
            else:
                assert __options__ is None
                assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                __options__ = __request_or_options__

                __request__ = GreeterDangerousFieldsRequestFromInputFields(
                    metadata=metadata,
                    request=request,
                    response=response,
                    self=self,
                    context=context,
                    bearer_token=bearer_token,
                    reboot=reboot,
                    uuid=uuid,
                    stub=stub,
                    idempotency=idempotency,
                    state_id=state_id,
                )

            # Within a `workflow`, all "bare" calls are
            # `per_workflow()` calls, unless we're within a control
            # loop, in which case they are syntactic sugar for
            # `per_iteration()`.
            if __idempotency__ is None:
                if isinstance(__context__, IMPORT_reboot_aio_contexts.WorkflowContext):
                    return await (
                        __this__.per_iteration() if __context__.within_loop()
                        else __this__.per_workflow()
                    ).DangerousFields(
                        __context__,
                        __request__,
                        __options__ or IMPORT_reboot_aio_call.Options(),
                    )
                elif isinstance(__context__, IMPORT_reboot_aio_external.InitializeContext):
                    return await __this__.idempotently().DangerousFields(
                        __context__,
                        __request__,
                        __options__ or IMPORT_reboot_aio_call.Options(),
                    )

            __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
            __bearer_token__: IMPORT_typing.Optional[str] = None
            if __options__ is not None:
                IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                if __options__.metadata is not None:
                    __metadata__ = __options__.metadata
                if __options__.bearer_token is not None:
                    __bearer_token__ = __options__.bearer_token

            return GreeterDangerousFieldsResponseFromProto(
                await __this__._writer(__context__).DangerousFields(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )
            )

        # Keep the original functions on the client, so old code will
        # continue to work, but use the new 'snake_case' method in
        # the new code.
        dangerous_fields = DangerousFields
        @IMPORT_typing.overload
        async def StoreRecursiveMessage(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: Greeter.StoreRecursiveMessageRequest,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        ) -> Greeter.StoreRecursiveMessageResponse:
            ...

        @IMPORT_typing.overload
        async def StoreRecursiveMessage(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
            message: IMPORT_typing.Optional[tests.reboot.greeter_pb2.RecursiveMessage] | Unset = UNSET,
        ) -> Greeter.StoreRecursiveMessageResponse:
            ...

        async def StoreRecursiveMessage( # type: ignore[misc]
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[Greeter.StoreRecursiveMessageRequest | IMPORT_reboot_aio_call.Options] = None,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
            message: IMPORT_typing.Optional[tests.reboot.greeter_pb2.RecursiveMessage] | Unset = UNSET,
        ) -> Greeter.StoreRecursiveMessageResponse:
            # UX improvement: check that neither positional argument was accidentally
            # given a gRPC request type.
            IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.StoreRecursiveMessageRequest)
            IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.StoreRecursiveMessageRequest)

            __request__: IMPORT_typing.Optional[Greeter.StoreRecursiveMessageRequest] = None
            if isinstance(__request_or_options__, Greeter.StoreRecursiveMessageRequest):
                assert __request_or_options__ is not None
                assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

                assert message is UNSET

                __request__ = __request_or_options__
            else:
                assert __options__ is None
                assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                __options__ = __request_or_options__

                __request__ = GreeterStoreRecursiveMessageRequestFromInputFields(
                    message=message,
                )

            # Within a `workflow`, all "bare" calls are
            # `per_workflow()` calls, unless we're within a control
            # loop, in which case they are syntactic sugar for
            # `per_iteration()`.
            if __idempotency__ is None:
                if isinstance(__context__, IMPORT_reboot_aio_contexts.WorkflowContext):
                    return await (
                        __this__.per_iteration() if __context__.within_loop()
                        else __this__.per_workflow()
                    ).StoreRecursiveMessage(
                        __context__,
                        __request__,
                        __options__ or IMPORT_reboot_aio_call.Options(),
                    )
                elif isinstance(__context__, IMPORT_reboot_aio_external.InitializeContext):
                    return await __this__.idempotently().StoreRecursiveMessage(
                        __context__,
                        __request__,
                        __options__ or IMPORT_reboot_aio_call.Options(),
                    )

            __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
            __bearer_token__: IMPORT_typing.Optional[str] = None
            if __options__ is not None:
                IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                if __options__.metadata is not None:
                    __metadata__ = __options__.metadata
                if __options__.bearer_token is not None:
                    __bearer_token__ = __options__.bearer_token

            return GreeterStoreRecursiveMessageResponseFromProto(
                await __this__._writer(__context__).StoreRecursiveMessage(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )
            )

        # Keep the original functions on the client, so old code will
        # continue to work, but use the new 'snake_case' method in
        # the new code.
        store_recursive_message = StoreRecursiveMessage
        @IMPORT_typing.overload
        async def ReadRecursiveMessage(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: Greeter.ReadRecursiveMessageRequest,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        ) -> Greeter.ReadRecursiveMessageResponse:
            ...

        @IMPORT_typing.overload
        async def ReadRecursiveMessage(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        ) -> Greeter.ReadRecursiveMessageResponse:
            ...

        async def ReadRecursiveMessage( # type: ignore[misc]
            __this__,
            __context__: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[Greeter.ReadRecursiveMessageRequest | IMPORT_reboot_aio_call.Options] = None,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        ) -> Greeter.ReadRecursiveMessageResponse:
            # UX improvement: check that neither positional argument was accidentally
            # given a gRPC request type.
            IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.ReadRecursiveMessageRequest)
            IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.ReadRecursiveMessageRequest)

            __request__: IMPORT_typing.Optional[Greeter.ReadRecursiveMessageRequest] = None

            if isinstance(__request_or_options__, Greeter.ReadRecursiveMessageRequest):
                assert __request_or_options__ is not None
                assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)


                __request__ = __request_or_options__
            else:
                assert __options__ is None
                assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                __options__ = __request_or_options__

                __request__ = GreeterReadRecursiveMessageRequestFromInputFields(
                )

            # Within a `workflow`, all "bare" calls are
            # `per_workflow()` calls, unless we're within a control
            # loop, in which case they are syntactic sugar for
            # `per_iteration()`.
            #
            # Unless we are "within until" in which case all "bare"
            # calls are `.always().
            if __idempotency__ is None:
                if isinstance(__context__, IMPORT_reboot_aio_contexts.WorkflowContext):
                    return await (
                        __this__.always() if __context__.within_until()
                        else (
                            __this__.per_iteration() if __context__.within_loop()
                            else __this__.per_workflow()
                        )
                    ).ReadRecursiveMessage(
                        __context__,
                        __request__,
                        __options__ or IMPORT_reboot_aio_call.Options(),
                    )
                elif isinstance(__context__, IMPORT_reboot_aio_external.InitializeContext):
                    return await __this__.idempotently().ReadRecursiveMessage(
                        __context__,
                        __request__,
                        __options__ or IMPORT_reboot_aio_call.Options(),
                    )

            __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
            __bearer_token__: IMPORT_typing.Optional[str] = None
            if __options__ is not None:
                IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                if __options__.metadata is not None:
                    __metadata__ = __options__.metadata
                if __options__.bearer_token is not None:
                    __bearer_token__ = __options__.bearer_token
            return GreeterReadRecursiveMessageResponseFromProto(
                await __this__._reader(__context__).ReadRecursiveMessage(
                    __request__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                    idempotency=__idempotency__,
                )
            )
        # Keep the original functions on the client, so old code will
        # continue to work, but use the new 'snake_case' method in
        # the new code.
        read_recursive_message = ReadRecursiveMessage
        @IMPORT_typing.overload
        async def ConstructAndStoreRecursiveMessage(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: Greeter.ConstructAndStoreRecursiveMessageRequest,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        ) -> Greeter.ConstructAndStoreRecursiveMessageResponse:
            ...

        @IMPORT_typing.overload
        async def ConstructAndStoreRecursiveMessage(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
            depth: IMPORT_typing.Optional[int] | Unset = UNSET,
        ) -> Greeter.ConstructAndStoreRecursiveMessageResponse:
            ...

        async def ConstructAndStoreRecursiveMessage( # type: ignore[misc]
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[Greeter.ConstructAndStoreRecursiveMessageRequest | IMPORT_reboot_aio_call.Options] = None,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
            depth: IMPORT_typing.Optional[int] | Unset = UNSET,
        ) -> Greeter.ConstructAndStoreRecursiveMessageResponse:
            # UX improvement: check that neither positional argument was accidentally
            # given a gRPC request type.
            IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.ConstructAndStoreRecursiveMessageRequest)
            IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.ConstructAndStoreRecursiveMessageRequest)

            __request__: IMPORT_typing.Optional[Greeter.ConstructAndStoreRecursiveMessageRequest] = None
            if isinstance(__request_or_options__, Greeter.ConstructAndStoreRecursiveMessageRequest):
                assert __request_or_options__ is not None
                assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

                assert depth is UNSET

                __request__ = __request_or_options__
            else:
                assert __options__ is None
                assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                __options__ = __request_or_options__

                __request__ = GreeterConstructAndStoreRecursiveMessageRequestFromInputFields(
                    depth=depth,
                )

            # Within a `workflow`, all "bare" calls are
            # `per_workflow()` calls, unless we're within a control
            # loop, in which case they are syntactic sugar for
            # `per_iteration()`.
            if __idempotency__ is None:
                if isinstance(__context__, IMPORT_reboot_aio_contexts.WorkflowContext):
                    return await (
                        __this__.per_iteration() if __context__.within_loop()
                        else __this__.per_workflow()
                    ).ConstructAndStoreRecursiveMessage(
                        __context__,
                        __request__,
                        __options__ or IMPORT_reboot_aio_call.Options(),
                    )
                elif isinstance(__context__, IMPORT_reboot_aio_external.InitializeContext):
                    return await __this__.idempotently().ConstructAndStoreRecursiveMessage(
                        __context__,
                        __request__,
                        __options__ or IMPORT_reboot_aio_call.Options(),
                    )

            __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
            __bearer_token__: IMPORT_typing.Optional[str] = None
            if __options__ is not None:
                IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                if __options__.metadata is not None:
                    __metadata__ = __options__.metadata
                if __options__.bearer_token is not None:
                    __bearer_token__ = __options__.bearer_token

            return GreeterConstructAndStoreRecursiveMessageResponseFromProto(
                await __this__._workflow(__context__).ConstructAndStoreRecursiveMessage(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )
            )

        # Keep the original functions on the client, so old code will
        # continue to work, but use the new 'snake_case' method in
        # the new code.
        construct_and_store_recursive_message = ConstructAndStoreRecursiveMessage

    class _Forall:

        _ids: IMPORT_typing.Iterable[str]

        def __init__(self, ids: IMPORT_typing.Iterable[str]):
            self._ids = ids

        @IMPORT_typing.overload
        async def Greet(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: Greeter.GreetRequest,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
        ) -> list[Greeter.GreetResponse]:
            ...

        @IMPORT_typing.overload
        async def Greet(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            name: IMPORT_typing.Optional[str] | Unset = UNSET,
            metadata: IMPORT_typing.Optional[dict[str, str]] | Unset = UNSET,
        ) -> list[Greeter.GreetResponse]:
            ...

        async def Greet( # type: ignore[misc]
            # In methods which are dealing with user input, (i.e.,
            # proto message field names), we should use '__double_underscored__'
            # variables to avoid any potential name conflicts with the method's
            # parameters.
            # The '__self__' parameter is a convention in Python to
            # indicate that this method is a bound method, so we use
            # '__this__' instead.
            __this__,
            __context__: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[Greeter.GreetRequest | IMPORT_reboot_aio_call.Options] = None,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            name: IMPORT_typing.Optional[str] | Unset = UNSET,
            metadata: IMPORT_typing.Optional[dict[str, str]] | Unset = UNSET,
        ) -> list[Greeter.GreetResponse]:
            if isinstance(__request_or_options__, Greeter.GreetRequest):
                assert __request_or_options__ is not None
                assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)
                __options__ = __options__ or IMPORT_reboot_aio_call.Options()

                assert name is UNSET
                assert metadata is UNSET

                return await IMPORT_asyncio.gather(
                    *[
                        Greeter.ref(
                            id
                        ).Greet(
                            __context__,
                            __request_or_options__,
                            __options__,
                        ) for id in __this__._ids
                    ]
                )
            else:
                assert __options__ is None
                assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                __options__ = __request_or_options__ or IMPORT_reboot_aio_call.Options()

                return await IMPORT_asyncio.gather(
                    *[
                        Greeter.ref(
                            id
                        ).Greet(
                            __context__,
                            GreeterGreetRequestFromInputFields(
                                name=name,
                                metadata=metadata,
                            ),
                            __options__,
                        ) for id in __this__._ids
                    ]
                )

        # Keep the original functions on the client, so old code will
        # continue to work, but use the new 'snake_case' method in
        # the new code.
        greet = Greet
        @IMPORT_typing.overload
        async def SetAdjective(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: Greeter.SetAdjectiveRequest,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
        ) -> list[Greeter.SetAdjectiveResponse]:
            ...

        @IMPORT_typing.overload
        async def SetAdjective(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            adjective: IMPORT_typing.Optional[str] | Unset = UNSET,
            arbitrary_message: IMPORT_typing.Optional[tests.reboot.greeter_pb2.SetAdjectiveRequest.NestedMessage] | Unset = UNSET,
            revert_after_1_second: IMPORT_typing.Optional[bool] | Unset = UNSET,
        ) -> list[Greeter.SetAdjectiveResponse]:
            ...

        async def SetAdjective( # type: ignore[misc]
            # In methods which are dealing with user input, (i.e.,
            # proto message field names), we should use '__double_underscored__'
            # variables to avoid any potential name conflicts with the method's
            # parameters.
            # The '__self__' parameter is a convention in Python to
            # indicate that this method is a bound method, so we use
            # '__this__' instead.
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[Greeter.SetAdjectiveRequest | IMPORT_reboot_aio_call.Options] = None,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            adjective: IMPORT_typing.Optional[str] | Unset = UNSET,
            arbitrary_message: IMPORT_typing.Optional[tests.reboot.greeter_pb2.SetAdjectiveRequest.NestedMessage] | Unset = UNSET,
            revert_after_1_second: IMPORT_typing.Optional[bool] | Unset = UNSET,
        ) -> list[Greeter.SetAdjectiveResponse]:
            if isinstance(__request_or_options__, Greeter.SetAdjectiveRequest):
                assert __request_or_options__ is not None
                assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)
                __options__ = __options__ or IMPORT_reboot_aio_call.Options()

                assert adjective is UNSET
                assert arbitrary_message is UNSET
                assert revert_after_1_second is UNSET

                return await IMPORT_asyncio.gather(
                    *[
                        Greeter.ref(
                            id
                        ).SetAdjective(
                            __context__,
                            __request_or_options__,
                            __options__,
                        ) for id in __this__._ids
                    ]
                )
            else:
                assert __options__ is None
                assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                __options__ = __request_or_options__ or IMPORT_reboot_aio_call.Options()

                return await IMPORT_asyncio.gather(
                    *[
                        Greeter.ref(
                            id
                        ).SetAdjective(
                            __context__,
                            GreeterSetAdjectiveRequestFromInputFields(
                                adjective=adjective,
                                arbitrary_message=arbitrary_message,
                                revert_after_1_second=revert_after_1_second,
                            ),
                            __options__,
                        ) for id in __this__._ids
                    ]
                )

        # Keep the original functions on the client, so old code will
        # continue to work, but use the new 'snake_case' method in
        # the new code.
        set_adjective = SetAdjective
        @IMPORT_typing.overload
        async def TransactionSetAdjective(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: Greeter.TransactionSetAdjectiveRequest,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
        ) -> list[Greeter.TransactionSetAdjectiveResponse]:
            ...

        @IMPORT_typing.overload
        async def TransactionSetAdjective(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            adjective: IMPORT_typing.Optional[str] | Unset = UNSET,
            arbitrary_message: IMPORT_typing.Optional[tests.reboot.greeter_pb2.SetAdjectiveRequest.NestedMessage] | Unset = UNSET,
            revert_after_1_second: IMPORT_typing.Optional[bool] | Unset = UNSET,
        ) -> list[Greeter.TransactionSetAdjectiveResponse]:
            ...

        async def TransactionSetAdjective( # type: ignore[misc]
            # In methods which are dealing with user input, (i.e.,
            # proto message field names), we should use '__double_underscored__'
            # variables to avoid any potential name conflicts with the method's
            # parameters.
            # The '__self__' parameter is a convention in Python to
            # indicate that this method is a bound method, so we use
            # '__this__' instead.
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[Greeter.TransactionSetAdjectiveRequest | IMPORT_reboot_aio_call.Options] = None,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            adjective: IMPORT_typing.Optional[str] | Unset = UNSET,
            arbitrary_message: IMPORT_typing.Optional[tests.reboot.greeter_pb2.SetAdjectiveRequest.NestedMessage] | Unset = UNSET,
            revert_after_1_second: IMPORT_typing.Optional[bool] | Unset = UNSET,
        ) -> list[Greeter.TransactionSetAdjectiveResponse]:
            if isinstance(__request_or_options__, Greeter.TransactionSetAdjectiveRequest):
                assert __request_or_options__ is not None
                assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)
                __options__ = __options__ or IMPORT_reboot_aio_call.Options()

                assert adjective is UNSET
                assert arbitrary_message is UNSET
                assert revert_after_1_second is UNSET

                return await IMPORT_asyncio.gather(
                    *[
                        Greeter.ref(
                            id
                        ).TransactionSetAdjective(
                            __context__,
                            __request_or_options__,
                            __options__,
                        ) for id in __this__._ids
                    ]
                )
            else:
                assert __options__ is None
                assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                __options__ = __request_or_options__ or IMPORT_reboot_aio_call.Options()

                return await IMPORT_asyncio.gather(
                    *[
                        Greeter.ref(
                            id
                        ).TransactionSetAdjective(
                            __context__,
                            GreeterTransactionSetAdjectiveRequestFromInputFields(
                                adjective=adjective,
                                arbitrary_message=arbitrary_message,
                                revert_after_1_second=revert_after_1_second,
                            ),
                            __options__,
                        ) for id in __this__._ids
                    ]
                )

        # Keep the original functions on the client, so old code will
        # continue to work, but use the new 'snake_case' method in
        # the new code.
        transaction_set_adjective = TransactionSetAdjective
        @IMPORT_typing.overload
        async def TryToConstructContext(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: Greeter.TryToConstructContextRequest,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
        ) -> list[Greeter.TryToConstructContextResponse]:
            ...

        @IMPORT_typing.overload
        async def TryToConstructContext(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
        ) -> list[Greeter.TryToConstructContextResponse]:
            ...

        async def TryToConstructContext( # type: ignore[misc]
            # In methods which are dealing with user input, (i.e.,
            # proto message field names), we should use '__double_underscored__'
            # variables to avoid any potential name conflicts with the method's
            # parameters.
            # The '__self__' parameter is a convention in Python to
            # indicate that this method is a bound method, so we use
            # '__this__' instead.
            __this__,
            __context__: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[Greeter.TryToConstructContextRequest | IMPORT_reboot_aio_call.Options] = None,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
        ) -> list[Greeter.TryToConstructContextResponse]:
            if isinstance(__request_or_options__, Greeter.TryToConstructContextRequest):
                assert __request_or_options__ is not None
                assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)
                __options__ = __options__ or IMPORT_reboot_aio_call.Options()


                return await IMPORT_asyncio.gather(
                    *[
                        Greeter.ref(
                            id
                        ).TryToConstructContext(
                            __context__,
                            __request_or_options__,
                            __options__,
                        ) for id in __this__._ids
                    ]
                )
            else:
                assert __options__ is None
                assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                __options__ = __request_or_options__ or IMPORT_reboot_aio_call.Options()

                return await IMPORT_asyncio.gather(
                    *[
                        Greeter.ref(
                            id
                        ).TryToConstructContext(
                            __context__,
                            GreeterTryToConstructContextRequestFromInputFields(
                            ),
                            __options__,
                        ) for id in __this__._ids
                    ]
                )

        # Keep the original functions on the client, so old code will
        # continue to work, but use the new 'snake_case' method in
        # the new code.
        try_to_construct_context = TryToConstructContext
        @IMPORT_typing.overload
        async def TryToConstructExternalContext(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: Greeter.TryToConstructExternalContextRequest,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
        ) -> list[Greeter.TryToConstructExternalContextResponse]:
            ...

        @IMPORT_typing.overload
        async def TryToConstructExternalContext(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
        ) -> list[Greeter.TryToConstructExternalContextResponse]:
            ...

        async def TryToConstructExternalContext( # type: ignore[misc]
            # In methods which are dealing with user input, (i.e.,
            # proto message field names), we should use '__double_underscored__'
            # variables to avoid any potential name conflicts with the method's
            # parameters.
            # The '__self__' parameter is a convention in Python to
            # indicate that this method is a bound method, so we use
            # '__this__' instead.
            __this__,
            __context__: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[Greeter.TryToConstructExternalContextRequest | IMPORT_reboot_aio_call.Options] = None,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
        ) -> list[Greeter.TryToConstructExternalContextResponse]:
            if isinstance(__request_or_options__, Greeter.TryToConstructExternalContextRequest):
                assert __request_or_options__ is not None
                assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)
                __options__ = __options__ or IMPORT_reboot_aio_call.Options()


                return await IMPORT_asyncio.gather(
                    *[
                        Greeter.ref(
                            id
                        ).TryToConstructExternalContext(
                            __context__,
                            __request_or_options__,
                            __options__,
                        ) for id in __this__._ids
                    ]
                )
            else:
                assert __options__ is None
                assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                __options__ = __request_or_options__ or IMPORT_reboot_aio_call.Options()

                return await IMPORT_asyncio.gather(
                    *[
                        Greeter.ref(
                            id
                        ).TryToConstructExternalContext(
                            __context__,
                            GreeterTryToConstructExternalContextRequestFromInputFields(
                            ),
                            __options__,
                        ) for id in __this__._ids
                    ]
                )

        # Keep the original functions on the client, so old code will
        # continue to work, but use the new 'snake_case' method in
        # the new code.
        try_to_construct_external_context = TryToConstructExternalContext
        @IMPORT_typing.overload
        async def TestLongRunningFetch(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: Greeter.TestLongRunningFetchRequest,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
        ) -> list[Greeter.TestLongRunningFetchResponse]:
            ...

        @IMPORT_typing.overload
        async def TestLongRunningFetch(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            sleep_time_seconds: IMPORT_typing.Optional[int] | Unset = UNSET,
        ) -> list[Greeter.TestLongRunningFetchResponse]:
            ...

        async def TestLongRunningFetch( # type: ignore[misc]
            # In methods which are dealing with user input, (i.e.,
            # proto message field names), we should use '__double_underscored__'
            # variables to avoid any potential name conflicts with the method's
            # parameters.
            # The '__self__' parameter is a convention in Python to
            # indicate that this method is a bound method, so we use
            # '__this__' instead.
            __this__,
            __context__: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[Greeter.TestLongRunningFetchRequest | IMPORT_reboot_aio_call.Options] = None,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            sleep_time_seconds: IMPORT_typing.Optional[int] | Unset = UNSET,
        ) -> list[Greeter.TestLongRunningFetchResponse]:
            if isinstance(__request_or_options__, Greeter.TestLongRunningFetchRequest):
                assert __request_or_options__ is not None
                assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)
                __options__ = __options__ or IMPORT_reboot_aio_call.Options()

                assert sleep_time_seconds is UNSET

                return await IMPORT_asyncio.gather(
                    *[
                        Greeter.ref(
                            id
                        ).TestLongRunningFetch(
                            __context__,
                            __request_or_options__,
                            __options__,
                        ) for id in __this__._ids
                    ]
                )
            else:
                assert __options__ is None
                assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                __options__ = __request_or_options__ or IMPORT_reboot_aio_call.Options()

                return await IMPORT_asyncio.gather(
                    *[
                        Greeter.ref(
                            id
                        ).TestLongRunningFetch(
                            __context__,
                            GreeterTestLongRunningFetchRequestFromInputFields(
                                sleep_time_seconds=sleep_time_seconds,
                            ),
                            __options__,
                        ) for id in __this__._ids
                    ]
                )

        # Keep the original functions on the client, so old code will
        # continue to work, but use the new 'snake_case' method in
        # the new code.
        test_long_running_fetch = TestLongRunningFetch
        @IMPORT_typing.overload
        async def TestLongRunningWriter(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: Greeter.TestLongRunningWriterRequest,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
        ) -> list[Greeter.TestLongRunningWriterResponse]:
            ...

        @IMPORT_typing.overload
        async def TestLongRunningWriter(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
        ) -> list[Greeter.TestLongRunningWriterResponse]:
            ...

        async def TestLongRunningWriter( # type: ignore[misc]
            # In methods which are dealing with user input, (i.e.,
            # proto message field names), we should use '__double_underscored__'
            # variables to avoid any potential name conflicts with the method's
            # parameters.
            # The '__self__' parameter is a convention in Python to
            # indicate that this method is a bound method, so we use
            # '__this__' instead.
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[Greeter.TestLongRunningWriterRequest | IMPORT_reboot_aio_call.Options] = None,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
        ) -> list[Greeter.TestLongRunningWriterResponse]:
            if isinstance(__request_or_options__, Greeter.TestLongRunningWriterRequest):
                assert __request_or_options__ is not None
                assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)
                __options__ = __options__ or IMPORT_reboot_aio_call.Options()


                return await IMPORT_asyncio.gather(
                    *[
                        Greeter.ref(
                            id
                        ).TestLongRunningWriter(
                            __context__,
                            __request_or_options__,
                            __options__,
                        ) for id in __this__._ids
                    ]
                )
            else:
                assert __options__ is None
                assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                __options__ = __request_or_options__ or IMPORT_reboot_aio_call.Options()

                return await IMPORT_asyncio.gather(
                    *[
                        Greeter.ref(
                            id
                        ).TestLongRunningWriter(
                            __context__,
                            GreeterTestLongRunningWriterRequestFromInputFields(
                            ),
                            __options__,
                        ) for id in __this__._ids
                    ]
                )

        # Keep the original functions on the client, so old code will
        # continue to work, but use the new 'snake_case' method in
        # the new code.
        test_long_running_writer = TestLongRunningWriter
        @IMPORT_typing.overload
        async def GetWholeState(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: Greeter.GetWholeStateRequest,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
        ) -> list[Greeter.GetWholeStateResponse]:
            ...

        @IMPORT_typing.overload
        async def GetWholeState(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
        ) -> list[Greeter.GetWholeStateResponse]:
            ...

        async def GetWholeState( # type: ignore[misc]
            # In methods which are dealing with user input, (i.e.,
            # proto message field names), we should use '__double_underscored__'
            # variables to avoid any potential name conflicts with the method's
            # parameters.
            # The '__self__' parameter is a convention in Python to
            # indicate that this method is a bound method, so we use
            # '__this__' instead.
            __this__,
            __context__: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[Greeter.GetWholeStateRequest | IMPORT_reboot_aio_call.Options] = None,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
        ) -> list[Greeter.GetWholeStateResponse]:
            if isinstance(__request_or_options__, Greeter.GetWholeStateRequest):
                assert __request_or_options__ is not None
                assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)
                __options__ = __options__ or IMPORT_reboot_aio_call.Options()


                return await IMPORT_asyncio.gather(
                    *[
                        Greeter.ref(
                            id
                        ).GetWholeState(
                            __context__,
                            __request_or_options__,
                            __options__,
                        ) for id in __this__._ids
                    ]
                )
            else:
                assert __options__ is None
                assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                __options__ = __request_or_options__ or IMPORT_reboot_aio_call.Options()

                return await IMPORT_asyncio.gather(
                    *[
                        Greeter.ref(
                            id
                        ).GetWholeState(
                            __context__,
                            GreeterGetWholeStateRequestFromInputFields(
                            ),
                            __options__,
                        ) for id in __this__._ids
                    ]
                )

        # Keep the original functions on the client, so old code will
        # continue to work, but use the new 'snake_case' method in
        # the new code.
        get_whole_state = GetWholeState
        @IMPORT_typing.overload
        async def FailWithException(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: Greeter.FailWithExceptionRequest,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
        ) -> list[Greeter.FailWithExceptionResponse]:
            ...

        @IMPORT_typing.overload
        async def FailWithException(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
        ) -> list[Greeter.FailWithExceptionResponse]:
            ...

        async def FailWithException( # type: ignore[misc]
            # In methods which are dealing with user input, (i.e.,
            # proto message field names), we should use '__double_underscored__'
            # variables to avoid any potential name conflicts with the method's
            # parameters.
            # The '__self__' parameter is a convention in Python to
            # indicate that this method is a bound method, so we use
            # '__this__' instead.
            __this__,
            __context__: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[Greeter.FailWithExceptionRequest | IMPORT_reboot_aio_call.Options] = None,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
        ) -> list[Greeter.FailWithExceptionResponse]:
            if isinstance(__request_or_options__, Greeter.FailWithExceptionRequest):
                assert __request_or_options__ is not None
                assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)
                __options__ = __options__ or IMPORT_reboot_aio_call.Options()


                return await IMPORT_asyncio.gather(
                    *[
                        Greeter.ref(
                            id
                        ).FailWithException(
                            __context__,
                            __request_or_options__,
                            __options__,
                        ) for id in __this__._ids
                    ]
                )
            else:
                assert __options__ is None
                assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                __options__ = __request_or_options__ or IMPORT_reboot_aio_call.Options()

                return await IMPORT_asyncio.gather(
                    *[
                        Greeter.ref(
                            id
                        ).FailWithException(
                            __context__,
                            GreeterFailWithExceptionRequestFromInputFields(
                            ),
                            __options__,
                        ) for id in __this__._ids
                    ]
                )

        # Keep the original functions on the client, so old code will
        # continue to work, but use the new 'snake_case' method in
        # the new code.
        fail_with_exception = FailWithException
        @IMPORT_typing.overload
        async def FailWithAborted(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: Greeter.FailWithAbortedRequest,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
        ) -> list[Greeter.FailWithAbortedResponse]:
            ...

        @IMPORT_typing.overload
        async def FailWithAborted(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
        ) -> list[Greeter.FailWithAbortedResponse]:
            ...

        async def FailWithAborted( # type: ignore[misc]
            # In methods which are dealing with user input, (i.e.,
            # proto message field names), we should use '__double_underscored__'
            # variables to avoid any potential name conflicts with the method's
            # parameters.
            # The '__self__' parameter is a convention in Python to
            # indicate that this method is a bound method, so we use
            # '__this__' instead.
            __this__,
            __context__: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[Greeter.FailWithAbortedRequest | IMPORT_reboot_aio_call.Options] = None,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
        ) -> list[Greeter.FailWithAbortedResponse]:
            if isinstance(__request_or_options__, Greeter.FailWithAbortedRequest):
                assert __request_or_options__ is not None
                assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)
                __options__ = __options__ or IMPORT_reboot_aio_call.Options()


                return await IMPORT_asyncio.gather(
                    *[
                        Greeter.ref(
                            id
                        ).FailWithAborted(
                            __context__,
                            __request_or_options__,
                            __options__,
                        ) for id in __this__._ids
                    ]
                )
            else:
                assert __options__ is None
                assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                __options__ = __request_or_options__ or IMPORT_reboot_aio_call.Options()

                return await IMPORT_asyncio.gather(
                    *[
                        Greeter.ref(
                            id
                        ).FailWithAborted(
                            __context__,
                            GreeterFailWithAbortedRequestFromInputFields(
                            ),
                            __options__,
                        ) for id in __this__._ids
                    ]
                )

        # Keep the original functions on the client, so old code will
        # continue to work, but use the new 'snake_case' method in
        # the new code.
        fail_with_aborted = FailWithAborted
        @IMPORT_typing.overload
        async def DangerousFields(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: Greeter.DangerousFieldsRequest,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
        ) -> list[Greeter.DangerousFieldsResponse]:
            ...

        @IMPORT_typing.overload
        async def DangerousFields(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            metadata: IMPORT_typing.Optional[int] | Unset = UNSET,
            request: IMPORT_typing.Optional[int] | Unset = UNSET,
            response: IMPORT_typing.Optional[int] | Unset = UNSET,
            self: IMPORT_typing.Optional[int] | Unset = UNSET,
            context: IMPORT_typing.Optional[int] | Unset = UNSET,
            bearer_token: IMPORT_typing.Optional[int] | Unset = UNSET,
            reboot: IMPORT_typing.Optional[int] | Unset = UNSET,
            uuid: IMPORT_typing.Optional[int] | Unset = UNSET,
            stub: IMPORT_typing.Optional[int] | Unset = UNSET,
            idempotency: IMPORT_typing.Optional[str] | Unset = UNSET,
            state_id: IMPORT_typing.Optional[str] | Unset = UNSET,
        ) -> list[Greeter.DangerousFieldsResponse]:
            ...

        async def DangerousFields( # type: ignore[misc]
            # In methods which are dealing with user input, (i.e.,
            # proto message field names), we should use '__double_underscored__'
            # variables to avoid any potential name conflicts with the method's
            # parameters.
            # The '__self__' parameter is a convention in Python to
            # indicate that this method is a bound method, so we use
            # '__this__' instead.
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[Greeter.DangerousFieldsRequest | IMPORT_reboot_aio_call.Options] = None,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            metadata: IMPORT_typing.Optional[int] | Unset = UNSET,
            request: IMPORT_typing.Optional[int] | Unset = UNSET,
            response: IMPORT_typing.Optional[int] | Unset = UNSET,
            self: IMPORT_typing.Optional[int] | Unset = UNSET,
            context: IMPORT_typing.Optional[int] | Unset = UNSET,
            bearer_token: IMPORT_typing.Optional[int] | Unset = UNSET,
            reboot: IMPORT_typing.Optional[int] | Unset = UNSET,
            uuid: IMPORT_typing.Optional[int] | Unset = UNSET,
            stub: IMPORT_typing.Optional[int] | Unset = UNSET,
            idempotency: IMPORT_typing.Optional[str] | Unset = UNSET,
            state_id: IMPORT_typing.Optional[str] | Unset = UNSET,
        ) -> list[Greeter.DangerousFieldsResponse]:
            if isinstance(__request_or_options__, Greeter.DangerousFieldsRequest):
                assert __request_or_options__ is not None
                assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)
                __options__ = __options__ or IMPORT_reboot_aio_call.Options()

                assert metadata is UNSET
                assert request is UNSET
                assert response is UNSET
                assert self is UNSET
                assert context is UNSET
                assert bearer_token is UNSET
                assert reboot is UNSET
                assert uuid is UNSET
                assert stub is UNSET
                assert idempotency is UNSET
                assert state_id is UNSET

                return await IMPORT_asyncio.gather(
                    *[
                        Greeter.ref(
                            id
                        ).DangerousFields(
                            __context__,
                            __request_or_options__,
                            __options__,
                        ) for id in __this__._ids
                    ]
                )
            else:
                assert __options__ is None
                assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                __options__ = __request_or_options__ or IMPORT_reboot_aio_call.Options()

                return await IMPORT_asyncio.gather(
                    *[
                        Greeter.ref(
                            id
                        ).DangerousFields(
                            __context__,
                            GreeterDangerousFieldsRequestFromInputFields(
                                metadata=metadata,
                                request=request,
                                response=response,
                                self=self,
                                context=context,
                                bearer_token=bearer_token,
                                reboot=reboot,
                                uuid=uuid,
                                stub=stub,
                                idempotency=idempotency,
                                state_id=state_id,
                            ),
                            __options__,
                        ) for id in __this__._ids
                    ]
                )

        # Keep the original functions on the client, so old code will
        # continue to work, but use the new 'snake_case' method in
        # the new code.
        dangerous_fields = DangerousFields
        @IMPORT_typing.overload
        async def StoreRecursiveMessage(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: Greeter.StoreRecursiveMessageRequest,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
        ) -> list[Greeter.StoreRecursiveMessageResponse]:
            ...

        @IMPORT_typing.overload
        async def StoreRecursiveMessage(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            message: IMPORT_typing.Optional[tests.reboot.greeter_pb2.RecursiveMessage] | Unset = UNSET,
        ) -> list[Greeter.StoreRecursiveMessageResponse]:
            ...

        async def StoreRecursiveMessage( # type: ignore[misc]
            # In methods which are dealing with user input, (i.e.,
            # proto message field names), we should use '__double_underscored__'
            # variables to avoid any potential name conflicts with the method's
            # parameters.
            # The '__self__' parameter is a convention in Python to
            # indicate that this method is a bound method, so we use
            # '__this__' instead.
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[Greeter.StoreRecursiveMessageRequest | IMPORT_reboot_aio_call.Options] = None,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            message: IMPORT_typing.Optional[tests.reboot.greeter_pb2.RecursiveMessage] | Unset = UNSET,
        ) -> list[Greeter.StoreRecursiveMessageResponse]:
            if isinstance(__request_or_options__, Greeter.StoreRecursiveMessageRequest):
                assert __request_or_options__ is not None
                assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)
                __options__ = __options__ or IMPORT_reboot_aio_call.Options()

                assert message is UNSET

                return await IMPORT_asyncio.gather(
                    *[
                        Greeter.ref(
                            id
                        ).StoreRecursiveMessage(
                            __context__,
                            __request_or_options__,
                            __options__,
                        ) for id in __this__._ids
                    ]
                )
            else:
                assert __options__ is None
                assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                __options__ = __request_or_options__ or IMPORT_reboot_aio_call.Options()

                return await IMPORT_asyncio.gather(
                    *[
                        Greeter.ref(
                            id
                        ).StoreRecursiveMessage(
                            __context__,
                            GreeterStoreRecursiveMessageRequestFromInputFields(
                                message=message,
                            ),
                            __options__,
                        ) for id in __this__._ids
                    ]
                )

        # Keep the original functions on the client, so old code will
        # continue to work, but use the new 'snake_case' method in
        # the new code.
        store_recursive_message = StoreRecursiveMessage
        @IMPORT_typing.overload
        async def ReadRecursiveMessage(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: Greeter.ReadRecursiveMessageRequest,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
        ) -> list[Greeter.ReadRecursiveMessageResponse]:
            ...

        @IMPORT_typing.overload
        async def ReadRecursiveMessage(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
        ) -> list[Greeter.ReadRecursiveMessageResponse]:
            ...

        async def ReadRecursiveMessage( # type: ignore[misc]
            # In methods which are dealing with user input, (i.e.,
            # proto message field names), we should use '__double_underscored__'
            # variables to avoid any potential name conflicts with the method's
            # parameters.
            # The '__self__' parameter is a convention in Python to
            # indicate that this method is a bound method, so we use
            # '__this__' instead.
            __this__,
            __context__: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[Greeter.ReadRecursiveMessageRequest | IMPORT_reboot_aio_call.Options] = None,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
        ) -> list[Greeter.ReadRecursiveMessageResponse]:
            if isinstance(__request_or_options__, Greeter.ReadRecursiveMessageRequest):
                assert __request_or_options__ is not None
                assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)
                __options__ = __options__ or IMPORT_reboot_aio_call.Options()


                return await IMPORT_asyncio.gather(
                    *[
                        Greeter.ref(
                            id
                        ).ReadRecursiveMessage(
                            __context__,
                            __request_or_options__,
                            __options__,
                        ) for id in __this__._ids
                    ]
                )
            else:
                assert __options__ is None
                assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                __options__ = __request_or_options__ or IMPORT_reboot_aio_call.Options()

                return await IMPORT_asyncio.gather(
                    *[
                        Greeter.ref(
                            id
                        ).ReadRecursiveMessage(
                            __context__,
                            GreeterReadRecursiveMessageRequestFromInputFields(
                            ),
                            __options__,
                        ) for id in __this__._ids
                    ]
                )

        # Keep the original functions on the client, so old code will
        # continue to work, but use the new 'snake_case' method in
        # the new code.
        read_recursive_message = ReadRecursiveMessage
        @IMPORT_typing.overload
        async def ConstructAndStoreRecursiveMessage(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: Greeter.ConstructAndStoreRecursiveMessageRequest,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
        ) -> list[Greeter.ConstructAndStoreRecursiveMessageResponse]:
            ...

        @IMPORT_typing.overload
        async def ConstructAndStoreRecursiveMessage(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            depth: IMPORT_typing.Optional[int] | Unset = UNSET,
        ) -> list[Greeter.ConstructAndStoreRecursiveMessageResponse]:
            ...

        async def ConstructAndStoreRecursiveMessage( # type: ignore[misc]
            # In methods which are dealing with user input, (i.e.,
            # proto message field names), we should use '__double_underscored__'
            # variables to avoid any potential name conflicts with the method's
            # parameters.
            # The '__self__' parameter is a convention in Python to
            # indicate that this method is a bound method, so we use
            # '__this__' instead.
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[Greeter.ConstructAndStoreRecursiveMessageRequest | IMPORT_reboot_aio_call.Options] = None,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            depth: IMPORT_typing.Optional[int] | Unset = UNSET,
        ) -> list[Greeter.ConstructAndStoreRecursiveMessageResponse]:
            if isinstance(__request_or_options__, Greeter.ConstructAndStoreRecursiveMessageRequest):
                assert __request_or_options__ is not None
                assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)
                __options__ = __options__ or IMPORT_reboot_aio_call.Options()

                assert depth is UNSET

                return await IMPORT_asyncio.gather(
                    *[
                        Greeter.ref(
                            id
                        ).ConstructAndStoreRecursiveMessage(
                            __context__,
                            __request_or_options__,
                            __options__,
                        ) for id in __this__._ids
                    ]
                )
            else:
                assert __options__ is None
                assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                __options__ = __request_or_options__ or IMPORT_reboot_aio_call.Options()

                return await IMPORT_asyncio.gather(
                    *[
                        Greeter.ref(
                            id
                        ).ConstructAndStoreRecursiveMessage(
                            __context__,
                            GreeterConstructAndStoreRecursiveMessageRequestFromInputFields(
                                depth=depth,
                            ),
                            __options__,
                        ) for id in __this__._ids
                    ]
                )

        # Keep the original functions on the client, so old code will
        # continue to work, but use the new 'snake_case' method in
        # the new code.
        construct_and_store_recursive_message = ConstructAndStoreRecursiveMessage

    @classmethod
    def forall(cls, ids: IMPORT_typing.Iterable[str]) -> Greeter._Forall:
        return Greeter._Forall(ids)

    @classmethod
    def ref(
        cls,
        state_id: IMPORT_typing.Optional[IMPORT_reboot_aio_types.StateId] = None,
        *,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> Greeter.WeakReference[Greeter.WeakReference._Schedule] | Greeter.WeakReference[Greeter.WeakReference._WriterSchedule]:
        # We support calling `Greeter.ref()` with
        # no `state_id` __only__ inside a workflow to be able to call an
        # inline writer, inline reader or other method call, since
        # workflow is a `classmethod` and therefor we can't get a
        # reference to outselves as `self.ref()`.
        if state_id is None:
            context = IMPORT_reboot_aio_contexts.Context.get()

            if context is None:
                raise RuntimeError(
                    'Missing asyncio context variable `context`; '
                    'are you using this class without Reboot?'
                )

            if not isinstance(context, IMPORT_reboot_aio_contexts.WorkflowContext):
                raise RuntimeError(
                    '`ref()` called without a `state_id` can only be used within a Workflow.'
                )

            servicer = GreeterBaseServicer.__servicer__.get()

            if servicer is None:
                raise RuntimeError(
                    'Missing asyncio context variable `servicer`; '
                    'are you using this class without Reboot?'
                )

            return Greeter.WeakReference(
                # TODO(https://github.com/reboot-dev/mono/issues/3226): add support for calling other applications.
                # For now this always stays within the application that creates the context.
                application_id=None,
                state_id=context._state_ref.id,
                schedule_type=Greeter.WeakReference._WriterSchedule,
                # If the user didn't specify a bearer token we may still end up using the app-internal bearer token,
                # but that's decided at the time of the call.
                bearer_token=bearer_token,
                servicer=servicer,
            )

        return Greeter.WeakReference(
            # TODO(https://github.com/reboot-dev/mono/issues/3226): add support for calling other applications.
            # For now this always stays within the application that creates the context.
            application_id=None,
            state_id=state_id,
            schedule_type=Greeter.WeakReference._Schedule,
            bearer_token=bearer_token,
        )

    @IMPORT_typing.overload
    @classmethod
    async def Create(
        __cls__,
        __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
        __state_id_or_request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.StateId | Greeter.CreateRequest] = None,
        __request_or_options_or_idempotency__: IMPORT_typing.Optional[Greeter.CreateRequest | IMPORT_reboot_aio_call.Options] = None,
        __options_or_idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options | IMPORT_reboot_aio_idempotency.Idempotency] = None,
        __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
    ) -> tuple[Greeter.WeakReference, Greeter.CreateResponse]:
        ...

    @IMPORT_typing.overload
    @classmethod
    async def Create(
        __cls__,
        __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
        __state_id_or_request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.StateId | IMPORT_reboot_aio_call.Options] = None,
        __request_or_options_or_idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options | IMPORT_reboot_aio_idempotency.Idempotency] = None,
        __options_or_idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        *,
        title: IMPORT_typing.Optional[str] | Unset = UNSET,
        name: IMPORT_typing.Optional[str] | Unset = UNSET,
        adjective: IMPORT_typing.Optional[str] | Unset = UNSET,
    ) -> tuple[Greeter.WeakReference, Greeter.CreateResponse]:
        ...

    @classmethod
    async def Create( # type: ignore[misc]
        __cls__,
        __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
        __state_id_or_request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.StateId | Greeter.CreateRequest | IMPORT_reboot_aio_call.Options] = None,
        __request_or_options_or_idempotency__: IMPORT_typing.Optional[Greeter.CreateRequest | IMPORT_reboot_aio_call.Options | IMPORT_reboot_aio_idempotency.Idempotency] = None,
        __options_or_idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options | IMPORT_reboot_aio_idempotency.Idempotency] = None,
        __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        *,
        title: IMPORT_typing.Optional[str] | Unset = UNSET,
        name: IMPORT_typing.Optional[str] | Unset = UNSET,
        adjective: IMPORT_typing.Optional[str] | Unset = UNSET,
    ) -> tuple[Greeter.WeakReference, Greeter.CreateResponse]:
        # Within a `workflow`, all "bare" calls are
        # `per_workflow()` calls, unless we're within a control
        # loop, in which case they are syntactic sugar for
        # `per_iteration()`.
        #
        # Unless we are "within until" in which case all "bare"
        # calls are `.always()`.

        __request__: IMPORT_typing.Optional[Greeter.CreateRequest] = None
        __state_id__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.StateId] = None
        __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None

        if isinstance(__state_id_or_request_or_options__, IMPORT_reboot_aio_types.StateId):
            __state_id__ = __state_id_or_request_or_options__
            if isinstance(__request_or_options_or_idempotency__, Greeter.CreateRequest):
                __request__ = __request_or_options_or_idempotency__
                assert __options_or_idempotency__ is None or isinstance(__options_or_idempotency__, IMPORT_reboot_aio_call.Options)
                __options__ = __options_or_idempotency__
                assert __idempotency__ is None or isinstance(__idempotency__, IMPORT_reboot_aio_idempotency.Idempotency)

                assert title is UNSET
                assert name is UNSET
                assert adjective is UNSET
            else:
                assert __request_or_options_or_idempotency__ is None or isinstance(__request_or_options_or_idempotency__, IMPORT_reboot_aio_call.Options)
                __options__ = __request_or_options_or_idempotency__
                assert __options_or_idempotency__ is None or isinstance(__options_or_idempotency__, IMPORT_reboot_aio_idempotency.Idempotency)
                __idempotency__ = __options_or_idempotency__

                __request__ = GreeterCreateRequestFromInputFields(
                    title=title,
                    name=name,
                    adjective=adjective,
                )
        elif isinstance(__state_id_or_request_or_options__, Greeter.CreateRequest):
            __request__ = __state_id_or_request_or_options__
            assert __request_or_options_or_idempotency__ is None or isinstance(__request_or_options_or_idempotency__, IMPORT_reboot_aio_call.Options)
            __options__ = __request_or_options_or_idempotency__
            assert __options_or_idempotency__ is None or isinstance(__options_or_idempotency__, IMPORT_reboot_aio_idempotency.Idempotency)
            __idempotency__ = __options_or_idempotency__
        else:
            assert (
                __state_id_or_request_or_options__ is None
                or isinstance(__state_id_or_request_or_options__, IMPORT_reboot_aio_call.Options)
            ), "Invalid argument type. Did you pass:\n" \
            "  - A 'state_id' that is not of type 'reboot.aio.types.StateId'?\n" \
            "  - 'options' that are not of type 'reboot.aio.call.Options'?"
            __options__ = __state_id_or_request_or_options__
            assert __request_or_options_or_idempotency__ is None or isinstance(__request_or_options_or_idempotency__, IMPORT_reboot_aio_idempotency.Idempotency)
            __idempotency__ = __request_or_options_or_idempotency__

            __request__ = GreeterCreateRequestFromInputFields(
                title=title,
                name=name,
                adjective=adjective,
            )

        if __idempotency__ is None:
            __args__: tuple[IMPORT_typing.Any, ...] = (
                __context__,
            )
            if __state_id__ is not None:
                # Don't include `state_id` if it is `None`, so that
                # we won't break the positional argument deduction.
                __args__ += (__state_id__,)
            __args__ += (
                __request__,
                __options__,
            )
            if isinstance(__context__, IMPORT_reboot_aio_contexts.WorkflowContext):
                return await (
                    __cls__.always() if __context__.within_until()
                    else (
                        __cls__.per_iteration() if __context__.within_loop()
                        else __cls__.per_workflow()
                    )
                ).Create(
                    *__args__
                )
            elif isinstance(__context__, IMPORT_reboot_aio_external.InitializeContext):
                return await __cls__.idempotently().Create(
                    *__args__
                )

        __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
        __bearer_token__: IMPORT_typing.Optional[str] = None

        if __options__ is not None:
            IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
            if __options__.metadata is not None:
                __metadata__ = __options__.metadata
            if __options__.bearer_token is not None:
                __bearer_token__ = __options__.bearer_token

        if __state_id__ is None:
            if __idempotency__ is None:
                __state_id__ = str(IMPORT_uuid.uuid4())
            else:
                __state_id__ = __context__.generate_idempotent_state_id(
                    state_type_name=__cls__.__state_type_name__,
                    service_name=IMPORT_reboot_aio_types.ServiceName('tests.reboot.GreeterMethods'),
                    method='Create',
                    idempotency=__idempotency__,
                )

        __reference__ = Greeter.ref(
            __state_id__, bearer_token=__bearer_token__
        )
        __stub__ = __reference__._writer(__context__)
        return (
            __reference__,
            GreeterCreateResponseFromProto(
                await __stub__.Create(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )
            ),
        )

    # Keep the original functions on the client, so old code will
    # continue to work, but use the new 'snake_case' method in
    # the new code.
    create = Create

    @IMPORT_typing.overload
    @classmethod
    def idempotently(cls, alias: IMPORT_typing.Optional[str] = None, *, how: IMPORT_reboot_aio_idempotency.How = IMPORT_reboot_aio_idempotency.PER_WORKFLOW) -> Greeter._ConstructIdempotently:
        ...

    @IMPORT_typing.overload
    @classmethod
    def idempotently(cls, *, key: IMPORT_uuid.UUID, how: IMPORT_reboot_aio_idempotency.How = IMPORT_reboot_aio_idempotency.PER_WORKFLOW) -> Greeter._ConstructIdempotently:
        ...

    @classmethod
    def idempotently(
        cls,
        alias: IMPORT_typing.Optional[str] = None,
        *,
        key: IMPORT_typing.Optional[IMPORT_uuid.UUID] = None,
        how: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.How] = None,
    ) -> Greeter._ConstructIdempotently:
        return Greeter._ConstructIdempotently(
            _idempotency=IMPORT_reboot_aio_contexts.Context.idempotency(
                alias=alias,
                key=key,
                how=how,
            ),
        )

    @classmethod
    def per_workflow(
        cls,
        alias: IMPORT_typing.Optional[str] = None,
    ):
        return cls.idempotently(alias)

    @classmethod
    def per_iteration(
        cls,
        alias: IMPORT_typing.Optional[str] = None,
    ):
        return cls.idempotently(
            alias,
            how=IMPORT_reboot_aio_idempotency.PER_ITERATION,
        )

    @classmethod
    def always(cls):
        return cls.idempotently(
            key=IMPORT_reboot_aio_idempotency.make_expiring_idempotency_key(),
            how=IMPORT_reboot_aio_idempotency.ALWAYS,
        )

    @IMPORT_dataclasses.dataclass(frozen=True)
    class _ConstructIdempotently:

        _idempotency: IMPORT_reboot_aio_idempotency.Idempotency

        @IMPORT_typing.overload
        async def Create(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __state_id_or_request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.StateId | Greeter.CreateRequest] = None,
            __request_or_options__: IMPORT_typing.Optional[Greeter.CreateRequest | IMPORT_reboot_aio_call.Options] = None,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
        ) -> tuple[Greeter.WeakReference, Greeter.CreateResponse]:
            ...

        @IMPORT_typing.overload
        async def Create(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __state_id_or_request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.StateId | IMPORT_reboot_aio_call.Options] = None,
            __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            title: IMPORT_typing.Optional[str] | Unset = UNSET,
            name: IMPORT_typing.Optional[str] | Unset = UNSET,
            adjective: IMPORT_typing.Optional[str] | Unset = UNSET,
        ) -> tuple[Greeter.WeakReference, Greeter.CreateResponse]:
            ...

        async def Create( # type: ignore[misc]
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __state_id_or_request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.StateId | Greeter.CreateRequest | IMPORT_reboot_aio_call.Options] = None,
            __request_or_options__: IMPORT_typing.Optional[Greeter.CreateRequest | IMPORT_reboot_aio_call.Options] = None,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            title: IMPORT_typing.Optional[str] | Unset = UNSET,
            name: IMPORT_typing.Optional[str] | Unset = UNSET,
            adjective: IMPORT_typing.Optional[str] | Unset = UNSET,
        ) -> tuple[Greeter.WeakReference, Greeter.CreateResponse]:
            # UX improvement: check that neither positional argument was accidentally
            # given a gRPC request type.
            IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=Greeter.CreateRequest)
            IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=Greeter.CreateRequest)

            __state_id__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.StateId] = None
            __request__: IMPORT_typing.Optional[Greeter.CreateRequest] = None

            if isinstance(__state_id_or_request_or_options__, IMPORT_reboot_aio_types.StateId):
                __state_id__ = __state_id_or_request_or_options__
                if isinstance(__request_or_options__, Greeter.CreateRequest):
                    __request__ = __request_or_options__
                    if __options__ is not None and not isinstance(__options__, IMPORT_reboot_aio_call.Options):
                        raise TypeError(f"Expecting fourth positional argument to be of type 'reboot.aio.call.Options', got '{type(__request_or_options__).__name__}'")

                    assert title is UNSET
                    assert name is UNSET
                    assert adjective is UNSET
                else:
                    if __request_or_options__ is not None and not isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options):
                        raise TypeError(f"Expecting third positional argument to be of type 'reboot.aio.call.Options', got '{type(__request_or_options__).__name__}'")

                    __options__ = __request_or_options__

                    __request__ = GreeterCreateRequestFromInputFields(
                        title=title,
                        name=name,
                        adjective=adjective,
                    )
            elif isinstance(__state_id_or_request_or_options__, Greeter.CreateRequest):
                __request__ = __state_id_or_request_or_options__
                if __request_or_options__ is not None and not isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options):
                    raise TypeError(f"Expecting third positional argument to be of type 'reboot.aio.call.Options', got '{type(__request_or_options__).__name__}'")

                __options__ = __request_or_options__
            else:
                if __state_id_or_request_or_options__ is not None and not isinstance(__state_id_or_request_or_options__, IMPORT_reboot_aio_call.Options):
                    raise TypeError(f"Expecting second positional argument to either be of type 'str' or 'reboot.aio.call.Options', got '{type(__state_id_or_request_or_options__).__name__}'")

                __options__ = __state_id_or_request_or_options__

                __request__ = GreeterCreateRequestFromInputFields(
                    title=title,
                    name=name,
                    adjective=adjective,
                )
            __args__: tuple[IMPORT_typing.Any, ...]

            if __state_id__ is not None:
                __args__ = (
                    __context__,
                    __state_id__,
                    __request__,
                    __options__,
                    __this__._idempotency,
                )
            else:
                __args__ = (
                    __context__,
                    __request__,
                    __options__,
                    __this__._idempotency,
                )

            return await Greeter.Create(
                *__args__,
            )

        # Keep the original functions on the client, so old code will
        # continue to work, but use the new 'snake_case' method in
        # the new code.
        create = Create


############################ Servicer Node adapters ############################
# Used by Node.js servicer implementations to access Python code and vice-versa.
# Relevant to servicers, irrelevant to clients.

class GreeterServicerNodeAdaptor(Greeter.singleton.Servicer):

    @classmethod
    async def _wait_for_cancelled(
        cls,
        future: IMPORT_asyncio.Future,
        method: str,
    ):
        while True:
            done, pending = await IMPORT_asyncio.wait(
                [future],
                timeout=5,  # seconds
            )
            # Check if we've timed out and log a warning that their
            # call has been cancelled but it is still running.
            if len(done) == 0:
                logger.warning(
                    f"Call to method '{method}' has been cancelled by the caller, "
                    "BUT WE ARE STILL WAITING for it complete. You can use the promise "
                    "`context.cancelled` to check if the caller has cancelled so you "
                    "don't do unnecessary work or wait for something that may never occur."
                )
                continue
            break

        # Now need to actually `await` the future so that we don't
        # have an unretrieved exception that gets logged.
        #
        # NOTE: this will raise an exception if the method raised even
        # though the call has already been cancelled but it makes it
        # more clear that the method raised so that is why we're not
        # catching and swallowing any exception.
        await future

    def __init__(self):
        self._js_servicer_reference = self._construct_js_servicer()  # type: ignore[attr-defined]

    def authorizer(self) -> IMPORT_typing.Optional[IMPORT_reboot_aio_auth_authorizers.Authorizer]:
        return self._construct_authorizer(self._js_servicer_reference)  # type: ignore[attr-defined]

    async def _read(
        self,
        context: IMPORT_reboot_aio_contexts.WorkflowContext,
        json_options: str,
    ) -> str:
        options = IMPORT_json.loads(json_options)

        alias = options.get('alias')

        assert 'how' in options
        how = options['how']
        if how == IMPORT_reboot_aio_workflows.ALWAYS:
            assert alias is None
            return IMPORT_google_protobuf_json_format.MessageToJson(
                await super().state.always().read(context)
            )

        assert how in [
            IMPORT_reboot_aio_workflows.PER_WORKFLOW,
            IMPORT_reboot_aio_workflows.PER_ITERATION,
        ]

        return IMPORT_google_protobuf_json_format.MessageToJson(
            await (
                super().state.per_workflow(alias)
                if how == IMPORT_reboot_aio_workflows.PER_WORKFLOW
                else super().state.per_iteration(alias)
            ).read(context)
        )

    async def _write(
        self,
        context: IMPORT_reboot_aio_contexts.WorkflowContext,
        writer: IMPORT_typing.Callable[[str], IMPORT_typing.Awaitable[str]],
        json_options: str,
    ) -> str:

        async def _writer(state: IMPORT_google_protobuf_message.Message):
            with IMPORT_reboot_aio_tracing.span(
                state_name=f"{context.state_type_name}('{context.state_id}')",
                span_name="_writer on NodeAdaptor",
                # The naming above matches Python, but not TypeScript.
                python_specific=True,
                level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
            ):
                json_result_state = await writer(
                    IMPORT_google_protobuf_json_format.MessageToJson(state)
                )

                with IMPORT_reboot_aio_tracing.span(
                    state_name=f"{context.state_type_name}('{context.state_id}')",
                    span_name="_write - State Copy on NodeAdaptor",
                    # The naming above matches Python, but not TypeScript.
                    python_specific=True,
                    level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                ):
                    result_state = IMPORT_json.loads(json_result_state)

                    state.CopyFrom(
                        IMPORT_google_protobuf_json_format.ParseDict(
                            result_state['state'],
                            self.__state_type__(),
                        )
                    )

                    assert 'result' in result_state
                    result = result_state['result']
                    assert type(result) == str
                    return result

        options = IMPORT_json.loads(json_options)

        alias = options.get('alias')

        assert 'how' in options
        how = options['how']

        if how == IMPORT_reboot_aio_workflows.ALWAYS:
            assert alias is None
            return await super().state.always().write(
                context,
                _writer,
            )

        assert how in [
            IMPORT_reboot_aio_workflows.PER_WORKFLOW,
            IMPORT_reboot_aio_workflows.PER_ITERATION,
        ]

        return await (
            super().state.per_workflow(alias)
            if how == IMPORT_reboot_aio_workflows.PER_WORKFLOW
            else super().state.per_iteration(alias)
        ).write(context, _writer, type=str)

    # Greeter specific methods:
    async def Create(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: tests.reboot.greeter_pb2.Greeter,
        request: tests.reboot.greeter_pb2.CreateRequest,
    ) -> tests.reboot.greeter_pb2.CreateResponse:
        with IMPORT_reboot_aio_tracing.span(
                state_name=f"{context.state_type_name}('{context.state_id}')",
                span_name="NodeAdaptor Create",
                # The naming above matches Python, but not TypeScript.
                python_specific=True,
                level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
        ):
            with IMPORT_reboot_aio_tracing.span(
                state_name=f"{context.state_type_name}('{context.state_id}')",
                span_name="Create and serialize `TrampolineCall`",
                # The naming above matches Python, but not TypeScript.
                python_specific=True,
                level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
            ):
                bytes_call = IMPORT_rbt_v1alpha1_nodejs_pb2.TrampolineCall(
                    kind=IMPORT_rbt_v1alpha1_nodejs_pb2.writer,
                    context=IMPORT_rbt_v1alpha1_nodejs_pb2.Context(
                        method='Create',
                        state_id=context.state_id,
                        state_type_name=context.state_type_name,
                        caller_bearer_token=context.caller_bearer_token,
                        cookie=context.cookie,
                        app_internal=context.app_internal,
                        auth=(
                            None if context.auth is None
                            else context.auth.to_proto_bytes()
                        ),
                        workflow_id=(
                            None if context.workflow_id is None
                            else str(context.workflow_id)
                        ),
                    ),
                    state=state.SerializeToString(),
                    request=request.SerializeToString(),
                ).SerializeToString()

            cancelled: IMPORT_asyncio.Future[None] = IMPORT_asyncio.Future()

            bytes_result_future: IMPORT_typing.Optional[IMPORT_asyncio.Future[str]] = None

            try:
                with IMPORT_reboot_aio_tracing.span(
                    state_name=f"{context.state_type_name}('{context.state_id}')",
                    span_name="trampoline",
                    # The naming above matches Python, but not TypeScript.
                    python_specific=True,
                    level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                ):
                    bytes_result_future = self._trampoline(  # type: ignore[attr-defined]
                        self._js_servicer_reference,
                        context,
                        cancelled,
                        bytes_call,
                    )
                    # NOTE: we need to `asyncio.shield` so that we can still
                    # correctly wait for this future to complete even if we
                    # are cancelled.
                    assert bytes_result_future is not None
                    bytes_result = await IMPORT_asyncio.shield(bytes_result_future)
            except IMPORT_asyncio.CancelledError:
                cancelled.set_result(None)

                # NOTE: we MUST wait for `bytes_result_future` because this
                # is a `writer` or `transaction` and we CAN NOT execute
                # multiple simultaneously.
                if bytes_result_future is not None:
                    await self._wait_for_cancelled(
                        bytes_result_future,
                        'Greeter.Create',
                    )

                raise
            except:
                # Make sure we cancel the `cancelled` future either if an
                # exception is thrown or if the result is reeturned so
                # that we don't keep around resources related to it that
                # might cause us to run out of memory or worse, keep Node
                # from exiting because it is waiting for Python.
                cancelled.cancel()
                raise
            else:
                cancelled.cancel()

                with IMPORT_reboot_aio_tracing.span(
                    state_name=f"{context.state_type_name}('{context.state_id}')",
                    span_name="result ParseFromString",
                    # The naming above matches Python, but not TypeScript.
                    python_specific=True,
                    level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                ):
                    result = IMPORT_rbt_v1alpha1_nodejs_pb2.TrampolineResult.FromString(
                        bytes_result
                    )

                if result.HasField('state'):
                    with IMPORT_reboot_aio_tracing.span(
                        state_name=f"{context.state_type_name}('{context.state_id}')",
                        span_name="state ParseFromString",
                        # The naming above matches Python, but not TypeScript.
                        python_specific=True,
                        level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                    ):
                        state.CopyFrom(
                            tests.reboot.greeter_pb2.Greeter.FromString(
                                result.state
                            )
                        )

                if result.HasField('status_json'):
                    raise (
                        Greeter
                        .CreateAborted
                        .from_status(
                            IMPORT_google_protobuf_json_format.Parse(
                                result.status_json,
                                IMPORT_google_rpc_status_pb2.Status(),
                            )
                        )
                    )

                assert result.HasField('response')

                with IMPORT_reboot_aio_tracing.span(
                    state_name=f"{context.state_type_name}('{context.state_id}')",
                    span_name="response ParseFromString",
                ):
                    return tests.reboot.greeter_pb2.CreateResponse.FromString(result.response)
        raise RuntimeError("Unexpected result from Create")

    async def Greet(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: tests.reboot.greeter_pb2.Greeter,
        request: tests.reboot.greeter_pb2.GreetRequest,
    ) -> tests.reboot.greeter_pb2.GreetResponse:
        bytes_call = IMPORT_rbt_v1alpha1_nodejs_pb2.TrampolineCall(
            kind=IMPORT_rbt_v1alpha1_nodejs_pb2.reader,
            context=IMPORT_rbt_v1alpha1_nodejs_pb2.Context(
                method='Greet',
                state_id=context.state_id,
                state_type_name=context.state_type_name,
                caller_bearer_token=context.caller_bearer_token,
                cookie=context.cookie,
                app_internal=context.app_internal,
                auth=(
                    None if context.auth is None
                    else context.auth.to_proto_bytes()
                ),
                workflow_id=(
                    None if context.workflow_id is None
                    else str(context.workflow_id)
                ),
            ),
            state=state.SerializeToString(),
            request=request.SerializeToString(),
        ).SerializeToString()

        cancelled: IMPORT_asyncio.Future[None] = IMPORT_asyncio.Future()

        bytes_result_future: IMPORT_typing.Optional[IMPORT_asyncio.Future[str]] = None

        try:
            bytes_result_future = self._trampoline(  # type: ignore[attr-defined]
                self._js_servicer_reference,
                context,
                cancelled,
                bytes_call,
            )
            # NOTE: we need to `asyncio.shield` so that we can still
            # correctly wait for this future to complete even if we
            # are cancelled.
            assert bytes_result_future is not None
            bytes_result = await IMPORT_asyncio.shield(bytes_result_future)
        except IMPORT_asyncio.CancelledError:
            cancelled.set_result(None)

            # NOTE: unlike for a `writer` or `transaction`, we DO NOT
            # _need_ to wait for `bytes_result_future` because this is a
            # reader and we can execute multiple readers
            # simultaneously. That being said, we still want to give
            # good feedback that the RPC has been cancelled, and there
            # is no harm waiting because other readers can still be
            # called.
            if bytes_result_future is not None:
                await self._wait_for_cancelled(
                    bytes_result_future,
                    'Greeter.Greet',
                )

            raise
        except:
            # Make sure we cancel the `cancelled` future either if an
            # exception is thrown or if the result is reeturned so
            # that we don't keep around resources related to it that
            # might cause us to run out of memory or worse, keep Node
            # from exiting because it is waiting for Python.
            cancelled.cancel()
            raise
        else:
            cancelled.cancel()

            result = IMPORT_rbt_v1alpha1_nodejs_pb2.TrampolineResult.FromString(
                bytes_result
            )

            if result.HasField('status_json'):
                raise (
                    Greeter
                    .GreetAborted
                    .from_status(
                        IMPORT_google_protobuf_json_format.Parse(
                            result.status_json,
                            IMPORT_google_rpc_status_pb2.Status(),
                        )
                    )
                )

            assert result.HasField('response')

            return tests.reboot.greeter_pb2.GreetResponse.FromString(result.response)
        raise RuntimeError("Unexpected result from Greet")

    async def SetAdjective(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: tests.reboot.greeter_pb2.Greeter,
        request: tests.reboot.greeter_pb2.SetAdjectiveRequest,
    ) -> tests.reboot.greeter_pb2.SetAdjectiveResponse:
        with IMPORT_reboot_aio_tracing.span(
                state_name=f"{context.state_type_name}('{context.state_id}')",
                span_name="NodeAdaptor SetAdjective",
                # The naming above matches Python, but not TypeScript.
                python_specific=True,
                level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
        ):
            with IMPORT_reboot_aio_tracing.span(
                state_name=f"{context.state_type_name}('{context.state_id}')",
                span_name="Create and serialize `TrampolineCall`",
                # The naming above matches Python, but not TypeScript.
                python_specific=True,
                level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
            ):
                bytes_call = IMPORT_rbt_v1alpha1_nodejs_pb2.TrampolineCall(
                    kind=IMPORT_rbt_v1alpha1_nodejs_pb2.writer,
                    context=IMPORT_rbt_v1alpha1_nodejs_pb2.Context(
                        method='SetAdjective',
                        state_id=context.state_id,
                        state_type_name=context.state_type_name,
                        caller_bearer_token=context.caller_bearer_token,
                        cookie=context.cookie,
                        app_internal=context.app_internal,
                        auth=(
                            None if context.auth is None
                            else context.auth.to_proto_bytes()
                        ),
                        workflow_id=(
                            None if context.workflow_id is None
                            else str(context.workflow_id)
                        ),
                    ),
                    state=state.SerializeToString(),
                    request=request.SerializeToString(),
                ).SerializeToString()

            cancelled: IMPORT_asyncio.Future[None] = IMPORT_asyncio.Future()

            bytes_result_future: IMPORT_typing.Optional[IMPORT_asyncio.Future[str]] = None

            try:
                with IMPORT_reboot_aio_tracing.span(
                    state_name=f"{context.state_type_name}('{context.state_id}')",
                    span_name="trampoline",
                    # The naming above matches Python, but not TypeScript.
                    python_specific=True,
                    level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                ):
                    bytes_result_future = self._trampoline(  # type: ignore[attr-defined]
                        self._js_servicer_reference,
                        context,
                        cancelled,
                        bytes_call,
                    )
                    # NOTE: we need to `asyncio.shield` so that we can still
                    # correctly wait for this future to complete even if we
                    # are cancelled.
                    assert bytes_result_future is not None
                    bytes_result = await IMPORT_asyncio.shield(bytes_result_future)
            except IMPORT_asyncio.CancelledError:
                cancelled.set_result(None)

                # NOTE: we MUST wait for `bytes_result_future` because this
                # is a `writer` or `transaction` and we CAN NOT execute
                # multiple simultaneously.
                if bytes_result_future is not None:
                    await self._wait_for_cancelled(
                        bytes_result_future,
                        'Greeter.SetAdjective',
                    )

                raise
            except:
                # Make sure we cancel the `cancelled` future either if an
                # exception is thrown or if the result is reeturned so
                # that we don't keep around resources related to it that
                # might cause us to run out of memory or worse, keep Node
                # from exiting because it is waiting for Python.
                cancelled.cancel()
                raise
            else:
                cancelled.cancel()

                with IMPORT_reboot_aio_tracing.span(
                    state_name=f"{context.state_type_name}('{context.state_id}')",
                    span_name="result ParseFromString",
                    # The naming above matches Python, but not TypeScript.
                    python_specific=True,
                    level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                ):
                    result = IMPORT_rbt_v1alpha1_nodejs_pb2.TrampolineResult.FromString(
                        bytes_result
                    )

                if result.HasField('state'):
                    with IMPORT_reboot_aio_tracing.span(
                        state_name=f"{context.state_type_name}('{context.state_id}')",
                        span_name="state ParseFromString",
                        # The naming above matches Python, but not TypeScript.
                        python_specific=True,
                        level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                    ):
                        state.CopyFrom(
                            tests.reboot.greeter_pb2.Greeter.FromString(
                                result.state
                            )
                        )

                if result.HasField('status_json'):
                    raise (
                        Greeter
                        .SetAdjectiveAborted
                        .from_status(
                            IMPORT_google_protobuf_json_format.Parse(
                                result.status_json,
                                IMPORT_google_rpc_status_pb2.Status(),
                            )
                        )
                    )

                assert result.HasField('response')

                with IMPORT_reboot_aio_tracing.span(
                    state_name=f"{context.state_type_name}('{context.state_id}')",
                    span_name="response ParseFromString",
                ):
                    return tests.reboot.greeter_pb2.SetAdjectiveResponse.FromString(result.response)
        raise RuntimeError("Unexpected result from SetAdjective")

    async def TransactionSetAdjective(
        self,
        context: IMPORT_reboot_aio_contexts.TransactionContext,
        state: tests.reboot.greeter_pb2.Greeter,
        request: tests.reboot.greeter_pb2.SetAdjectiveRequest,
    ) -> tests.reboot.greeter_pb2.SetAdjectiveResponse:
        with IMPORT_reboot_aio_tracing.span(
                state_name=f"{context.state_type_name}('{context.state_id}')",
                span_name="NodeAdaptor TransactionSetAdjective",
                # The naming above matches Python, but not TypeScript.
                python_specific=True,
                level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
        ):
            with IMPORT_reboot_aio_tracing.span(
                state_name=f"{context.state_type_name}('{context.state_id}')",
                span_name="Create and serialize `TrampolineCall`",
                # The naming above matches Python, but not TypeScript.
                python_specific=True,
                level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
            ):
                bytes_call = IMPORT_rbt_v1alpha1_nodejs_pb2.TrampolineCall(
                    kind=IMPORT_rbt_v1alpha1_nodejs_pb2.transaction,
                    context=IMPORT_rbt_v1alpha1_nodejs_pb2.Context(
                        method='TransactionSetAdjective',
                        state_id=context.state_id,
                        state_type_name=context.state_type_name,
                        caller_bearer_token=context.caller_bearer_token,
                        cookie=context.cookie,
                        app_internal=context.app_internal,
                        auth=(
                            None if context.auth is None
                            else context.auth.to_proto_bytes()
                        ),
                        workflow_id=(
                            None if context.workflow_id is None
                            else str(context.workflow_id)
                        ),
                    ),
                    state=state.SerializeToString(),
                    request=request.SerializeToString(),
                ).SerializeToString()

            cancelled: IMPORT_asyncio.Future[None] = IMPORT_asyncio.Future()

            bytes_result_future: IMPORT_typing.Optional[IMPORT_asyncio.Future[str]] = None

            try:
                with IMPORT_reboot_aio_tracing.span(
                    state_name=f"{context.state_type_name}('{context.state_id}')",
                    span_name="trampoline",
                    # The naming above matches Python, but not TypeScript.
                    python_specific=True,
                    level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                ):
                    bytes_result_future = self._trampoline(  # type: ignore[attr-defined]
                        self._js_servicer_reference,
                        context,
                        cancelled,
                        bytes_call,
                    )
                    # NOTE: we need to `asyncio.shield` so that we can still
                    # correctly wait for this future to complete even if we
                    # are cancelled.
                    assert bytes_result_future is not None
                    bytes_result = await IMPORT_asyncio.shield(bytes_result_future)
            except IMPORT_asyncio.CancelledError:
                cancelled.set_result(None)

                # NOTE: we MUST wait for `bytes_result_future` because this
                # is a `writer` or `transaction` and we CAN NOT execute
                # multiple simultaneously.
                if bytes_result_future is not None:
                    await self._wait_for_cancelled(
                        bytes_result_future,
                        'Greeter.TransactionSetAdjective',
                    )

                raise
            except:
                # Make sure we cancel the `cancelled` future either if an
                # exception is thrown or if the result is reeturned so
                # that we don't keep around resources related to it that
                # might cause us to run out of memory or worse, keep Node
                # from exiting because it is waiting for Python.
                cancelled.cancel()
                raise
            else:
                cancelled.cancel()

                with IMPORT_reboot_aio_tracing.span(
                    state_name=f"{context.state_type_name}('{context.state_id}')",
                    span_name="result ParseFromString",
                    # The naming above matches Python, but not TypeScript.
                    python_specific=True,
                    level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                ):
                    result = IMPORT_rbt_v1alpha1_nodejs_pb2.TrampolineResult.FromString(
                        bytes_result
                    )

                if result.HasField('state'):
                    with IMPORT_reboot_aio_tracing.span(
                        state_name=f"{context.state_type_name}('{context.state_id}')",
                        span_name="state ParseFromString",
                        # The naming above matches Python, but not TypeScript.
                        python_specific=True,
                        level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                    ):
                        state.CopyFrom(
                            tests.reboot.greeter_pb2.Greeter.FromString(
                                result.state
                            )
                        )

                if result.HasField('status_json'):
                    raise (
                        Greeter
                        .TransactionSetAdjectiveAborted
                        .from_status(
                            IMPORT_google_protobuf_json_format.Parse(
                                result.status_json,
                                IMPORT_google_rpc_status_pb2.Status(),
                            )
                        )
                    )

                assert result.HasField('response')

                with IMPORT_reboot_aio_tracing.span(
                    state_name=f"{context.state_type_name}('{context.state_id}')",
                    span_name="response ParseFromString",
                ):
                    return tests.reboot.greeter_pb2.SetAdjectiveResponse.FromString(result.response)
        raise RuntimeError("Unexpected result from TransactionSetAdjective")

    async def TryToConstructContext(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: tests.reboot.greeter_pb2.Greeter,
        request: google.protobuf.empty_pb2.Empty,
    ) -> google.protobuf.empty_pb2.Empty:
        bytes_call = IMPORT_rbt_v1alpha1_nodejs_pb2.TrampolineCall(
            kind=IMPORT_rbt_v1alpha1_nodejs_pb2.reader,
            context=IMPORT_rbt_v1alpha1_nodejs_pb2.Context(
                method='TryToConstructContext',
                state_id=context.state_id,
                state_type_name=context.state_type_name,
                caller_bearer_token=context.caller_bearer_token,
                cookie=context.cookie,
                app_internal=context.app_internal,
                auth=(
                    None if context.auth is None
                    else context.auth.to_proto_bytes()
                ),
                workflow_id=(
                    None if context.workflow_id is None
                    else str(context.workflow_id)
                ),
            ),
            state=state.SerializeToString(),
            request=request.SerializeToString(),
        ).SerializeToString()

        cancelled: IMPORT_asyncio.Future[None] = IMPORT_asyncio.Future()

        bytes_result_future: IMPORT_typing.Optional[IMPORT_asyncio.Future[str]] = None

        try:
            bytes_result_future = self._trampoline(  # type: ignore[attr-defined]
                self._js_servicer_reference,
                context,
                cancelled,
                bytes_call,
            )
            # NOTE: we need to `asyncio.shield` so that we can still
            # correctly wait for this future to complete even if we
            # are cancelled.
            assert bytes_result_future is not None
            bytes_result = await IMPORT_asyncio.shield(bytes_result_future)
        except IMPORT_asyncio.CancelledError:
            cancelled.set_result(None)

            # NOTE: unlike for a `writer` or `transaction`, we DO NOT
            # _need_ to wait for `bytes_result_future` because this is a
            # reader and we can execute multiple readers
            # simultaneously. That being said, we still want to give
            # good feedback that the RPC has been cancelled, and there
            # is no harm waiting because other readers can still be
            # called.
            if bytes_result_future is not None:
                await self._wait_for_cancelled(
                    bytes_result_future,
                    'Greeter.TryToConstructContext',
                )

            raise
        except:
            # Make sure we cancel the `cancelled` future either if an
            # exception is thrown or if the result is reeturned so
            # that we don't keep around resources related to it that
            # might cause us to run out of memory or worse, keep Node
            # from exiting because it is waiting for Python.
            cancelled.cancel()
            raise
        else:
            cancelled.cancel()

            result = IMPORT_rbt_v1alpha1_nodejs_pb2.TrampolineResult.FromString(
                bytes_result
            )

            if result.HasField('status_json'):
                raise (
                    Greeter
                    .TryToConstructContextAborted
                    .from_status(
                        IMPORT_google_protobuf_json_format.Parse(
                            result.status_json,
                            IMPORT_google_rpc_status_pb2.Status(),
                        )
                    )
                )

            assert result.HasField('response')

            return google.protobuf.empty_pb2.Empty.FromString(result.response)
        raise RuntimeError("Unexpected result from TryToConstructContext")

    async def TryToConstructExternalContext(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: tests.reboot.greeter_pb2.Greeter,
        request: google.protobuf.empty_pb2.Empty,
    ) -> google.protobuf.empty_pb2.Empty:
        bytes_call = IMPORT_rbt_v1alpha1_nodejs_pb2.TrampolineCall(
            kind=IMPORT_rbt_v1alpha1_nodejs_pb2.reader,
            context=IMPORT_rbt_v1alpha1_nodejs_pb2.Context(
                method='TryToConstructExternalContext',
                state_id=context.state_id,
                state_type_name=context.state_type_name,
                caller_bearer_token=context.caller_bearer_token,
                cookie=context.cookie,
                app_internal=context.app_internal,
                auth=(
                    None if context.auth is None
                    else context.auth.to_proto_bytes()
                ),
                workflow_id=(
                    None if context.workflow_id is None
                    else str(context.workflow_id)
                ),
            ),
            state=state.SerializeToString(),
            request=request.SerializeToString(),
        ).SerializeToString()

        cancelled: IMPORT_asyncio.Future[None] = IMPORT_asyncio.Future()

        bytes_result_future: IMPORT_typing.Optional[IMPORT_asyncio.Future[str]] = None

        try:
            bytes_result_future = self._trampoline(  # type: ignore[attr-defined]
                self._js_servicer_reference,
                context,
                cancelled,
                bytes_call,
            )
            # NOTE: we need to `asyncio.shield` so that we can still
            # correctly wait for this future to complete even if we
            # are cancelled.
            assert bytes_result_future is not None
            bytes_result = await IMPORT_asyncio.shield(bytes_result_future)
        except IMPORT_asyncio.CancelledError:
            cancelled.set_result(None)

            # NOTE: unlike for a `writer` or `transaction`, we DO NOT
            # _need_ to wait for `bytes_result_future` because this is a
            # reader and we can execute multiple readers
            # simultaneously. That being said, we still want to give
            # good feedback that the RPC has been cancelled, and there
            # is no harm waiting because other readers can still be
            # called.
            if bytes_result_future is not None:
                await self._wait_for_cancelled(
                    bytes_result_future,
                    'Greeter.TryToConstructExternalContext',
                )

            raise
        except:
            # Make sure we cancel the `cancelled` future either if an
            # exception is thrown or if the result is reeturned so
            # that we don't keep around resources related to it that
            # might cause us to run out of memory or worse, keep Node
            # from exiting because it is waiting for Python.
            cancelled.cancel()
            raise
        else:
            cancelled.cancel()

            result = IMPORT_rbt_v1alpha1_nodejs_pb2.TrampolineResult.FromString(
                bytes_result
            )

            if result.HasField('status_json'):
                raise (
                    Greeter
                    .TryToConstructExternalContextAborted
                    .from_status(
                        IMPORT_google_protobuf_json_format.Parse(
                            result.status_json,
                            IMPORT_google_rpc_status_pb2.Status(),
                        )
                    )
                )

            assert result.HasField('response')

            return google.protobuf.empty_pb2.Empty.FromString(result.response)
        raise RuntimeError("Unexpected result from TryToConstructExternalContext")

    async def TestLongRunningFetch(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: tests.reboot.greeter_pb2.Greeter,
        request: tests.reboot.greeter_pb2.TestLongRunningFetchRequest,
    ) -> google.protobuf.empty_pb2.Empty:
        bytes_call = IMPORT_rbt_v1alpha1_nodejs_pb2.TrampolineCall(
            kind=IMPORT_rbt_v1alpha1_nodejs_pb2.reader,
            context=IMPORT_rbt_v1alpha1_nodejs_pb2.Context(
                method='TestLongRunningFetch',
                state_id=context.state_id,
                state_type_name=context.state_type_name,
                caller_bearer_token=context.caller_bearer_token,
                cookie=context.cookie,
                app_internal=context.app_internal,
                auth=(
                    None if context.auth is None
                    else context.auth.to_proto_bytes()
                ),
                workflow_id=(
                    None if context.workflow_id is None
                    else str(context.workflow_id)
                ),
            ),
            state=state.SerializeToString(),
            request=request.SerializeToString(),
        ).SerializeToString()

        cancelled: IMPORT_asyncio.Future[None] = IMPORT_asyncio.Future()

        bytes_result_future: IMPORT_typing.Optional[IMPORT_asyncio.Future[str]] = None

        try:
            bytes_result_future = self._trampoline(  # type: ignore[attr-defined]
                self._js_servicer_reference,
                context,
                cancelled,
                bytes_call,
            )
            # NOTE: we need to `asyncio.shield` so that we can still
            # correctly wait for this future to complete even if we
            # are cancelled.
            assert bytes_result_future is not None
            bytes_result = await IMPORT_asyncio.shield(bytes_result_future)
        except IMPORT_asyncio.CancelledError:
            cancelled.set_result(None)

            # NOTE: unlike for a `writer` or `transaction`, we DO NOT
            # _need_ to wait for `bytes_result_future` because this is a
            # reader and we can execute multiple readers
            # simultaneously. That being said, we still want to give
            # good feedback that the RPC has been cancelled, and there
            # is no harm waiting because other readers can still be
            # called.
            if bytes_result_future is not None:
                await self._wait_for_cancelled(
                    bytes_result_future,
                    'Greeter.TestLongRunningFetch',
                )

            raise
        except:
            # Make sure we cancel the `cancelled` future either if an
            # exception is thrown or if the result is reeturned so
            # that we don't keep around resources related to it that
            # might cause us to run out of memory or worse, keep Node
            # from exiting because it is waiting for Python.
            cancelled.cancel()
            raise
        else:
            cancelled.cancel()

            result = IMPORT_rbt_v1alpha1_nodejs_pb2.TrampolineResult.FromString(
                bytes_result
            )

            if result.HasField('status_json'):
                raise (
                    Greeter
                    .TestLongRunningFetchAborted
                    .from_status(
                        IMPORT_google_protobuf_json_format.Parse(
                            result.status_json,
                            IMPORT_google_rpc_status_pb2.Status(),
                        )
                    )
                )

            assert result.HasField('response')

            return google.protobuf.empty_pb2.Empty.FromString(result.response)
        raise RuntimeError("Unexpected result from TestLongRunningFetch")

    async def TestLongRunningWriter(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: tests.reboot.greeter_pb2.Greeter,
        request: google.protobuf.empty_pb2.Empty,
    ) -> google.protobuf.empty_pb2.Empty:
        with IMPORT_reboot_aio_tracing.span(
                state_name=f"{context.state_type_name}('{context.state_id}')",
                span_name="NodeAdaptor TestLongRunningWriter",
                # The naming above matches Python, but not TypeScript.
                python_specific=True,
                level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
        ):
            with IMPORT_reboot_aio_tracing.span(
                state_name=f"{context.state_type_name}('{context.state_id}')",
                span_name="Create and serialize `TrampolineCall`",
                # The naming above matches Python, but not TypeScript.
                python_specific=True,
                level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
            ):
                bytes_call = IMPORT_rbt_v1alpha1_nodejs_pb2.TrampolineCall(
                    kind=IMPORT_rbt_v1alpha1_nodejs_pb2.writer,
                    context=IMPORT_rbt_v1alpha1_nodejs_pb2.Context(
                        method='TestLongRunningWriter',
                        state_id=context.state_id,
                        state_type_name=context.state_type_name,
                        caller_bearer_token=context.caller_bearer_token,
                        cookie=context.cookie,
                        app_internal=context.app_internal,
                        auth=(
                            None if context.auth is None
                            else context.auth.to_proto_bytes()
                        ),
                        workflow_id=(
                            None if context.workflow_id is None
                            else str(context.workflow_id)
                        ),
                    ),
                    state=state.SerializeToString(),
                    request=request.SerializeToString(),
                ).SerializeToString()

            cancelled: IMPORT_asyncio.Future[None] = IMPORT_asyncio.Future()

            bytes_result_future: IMPORT_typing.Optional[IMPORT_asyncio.Future[str]] = None

            try:
                with IMPORT_reboot_aio_tracing.span(
                    state_name=f"{context.state_type_name}('{context.state_id}')",
                    span_name="trampoline",
                    # The naming above matches Python, but not TypeScript.
                    python_specific=True,
                    level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                ):
                    bytes_result_future = self._trampoline(  # type: ignore[attr-defined]
                        self._js_servicer_reference,
                        context,
                        cancelled,
                        bytes_call,
                    )
                    # NOTE: we need to `asyncio.shield` so that we can still
                    # correctly wait for this future to complete even if we
                    # are cancelled.
                    assert bytes_result_future is not None
                    bytes_result = await IMPORT_asyncio.shield(bytes_result_future)
            except IMPORT_asyncio.CancelledError:
                cancelled.set_result(None)

                # NOTE: we MUST wait for `bytes_result_future` because this
                # is a `writer` or `transaction` and we CAN NOT execute
                # multiple simultaneously.
                if bytes_result_future is not None:
                    await self._wait_for_cancelled(
                        bytes_result_future,
                        'Greeter.TestLongRunningWriter',
                    )

                raise
            except:
                # Make sure we cancel the `cancelled` future either if an
                # exception is thrown or if the result is reeturned so
                # that we don't keep around resources related to it that
                # might cause us to run out of memory or worse, keep Node
                # from exiting because it is waiting for Python.
                cancelled.cancel()
                raise
            else:
                cancelled.cancel()

                with IMPORT_reboot_aio_tracing.span(
                    state_name=f"{context.state_type_name}('{context.state_id}')",
                    span_name="result ParseFromString",
                    # The naming above matches Python, but not TypeScript.
                    python_specific=True,
                    level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                ):
                    result = IMPORT_rbt_v1alpha1_nodejs_pb2.TrampolineResult.FromString(
                        bytes_result
                    )

                if result.HasField('state'):
                    with IMPORT_reboot_aio_tracing.span(
                        state_name=f"{context.state_type_name}('{context.state_id}')",
                        span_name="state ParseFromString",
                        # The naming above matches Python, but not TypeScript.
                        python_specific=True,
                        level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                    ):
                        state.CopyFrom(
                            tests.reboot.greeter_pb2.Greeter.FromString(
                                result.state
                            )
                        )

                if result.HasField('status_json'):
                    raise (
                        Greeter
                        .TestLongRunningWriterAborted
                        .from_status(
                            IMPORT_google_protobuf_json_format.Parse(
                                result.status_json,
                                IMPORT_google_rpc_status_pb2.Status(),
                            )
                        )
                    )

                assert result.HasField('response')

                with IMPORT_reboot_aio_tracing.span(
                    state_name=f"{context.state_type_name}('{context.state_id}')",
                    span_name="response ParseFromString",
                ):
                    return google.protobuf.empty_pb2.Empty.FromString(result.response)
        raise RuntimeError("Unexpected result from TestLongRunningWriter")

    async def GetWholeState(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: tests.reboot.greeter_pb2.Greeter,
        request: tests.reboot.greeter_pb2.GetWholeStateRequest,
    ) -> tests.reboot.greeter_pb2.Greeter:
        bytes_call = IMPORT_rbt_v1alpha1_nodejs_pb2.TrampolineCall(
            kind=IMPORT_rbt_v1alpha1_nodejs_pb2.reader,
            context=IMPORT_rbt_v1alpha1_nodejs_pb2.Context(
                method='GetWholeState',
                state_id=context.state_id,
                state_type_name=context.state_type_name,
                caller_bearer_token=context.caller_bearer_token,
                cookie=context.cookie,
                app_internal=context.app_internal,
                auth=(
                    None if context.auth is None
                    else context.auth.to_proto_bytes()
                ),
                workflow_id=(
                    None if context.workflow_id is None
                    else str(context.workflow_id)
                ),
            ),
            state=state.SerializeToString(),
            request=request.SerializeToString(),
        ).SerializeToString()

        cancelled: IMPORT_asyncio.Future[None] = IMPORT_asyncio.Future()

        bytes_result_future: IMPORT_typing.Optional[IMPORT_asyncio.Future[str]] = None

        try:
            bytes_result_future = self._trampoline(  # type: ignore[attr-defined]
                self._js_servicer_reference,
                context,
                cancelled,
                bytes_call,
            )
            # NOTE: we need to `asyncio.shield` so that we can still
            # correctly wait for this future to complete even if we
            # are cancelled.
            assert bytes_result_future is not None
            bytes_result = await IMPORT_asyncio.shield(bytes_result_future)
        except IMPORT_asyncio.CancelledError:
            cancelled.set_result(None)

            # NOTE: unlike for a `writer` or `transaction`, we DO NOT
            # _need_ to wait for `bytes_result_future` because this is a
            # reader and we can execute multiple readers
            # simultaneously. That being said, we still want to give
            # good feedback that the RPC has been cancelled, and there
            # is no harm waiting because other readers can still be
            # called.
            if bytes_result_future is not None:
                await self._wait_for_cancelled(
                    bytes_result_future,
                    'Greeter.GetWholeState',
                )

            raise
        except:
            # Make sure we cancel the `cancelled` future either if an
            # exception is thrown or if the result is reeturned so
            # that we don't keep around resources related to it that
            # might cause us to run out of memory or worse, keep Node
            # from exiting because it is waiting for Python.
            cancelled.cancel()
            raise
        else:
            cancelled.cancel()

            result = IMPORT_rbt_v1alpha1_nodejs_pb2.TrampolineResult.FromString(
                bytes_result
            )

            if result.HasField('status_json'):
                raise (
                    Greeter
                    .GetWholeStateAborted
                    .from_status(
                        IMPORT_google_protobuf_json_format.Parse(
                            result.status_json,
                            IMPORT_google_rpc_status_pb2.Status(),
                        )
                    )
                )

            assert result.HasField('response')

            return tests.reboot.greeter_pb2.Greeter.FromString(result.response)
        raise RuntimeError("Unexpected result from GetWholeState")

    async def FailWithException(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: tests.reboot.greeter_pb2.Greeter,
        request: google.protobuf.empty_pb2.Empty,
    ) -> google.protobuf.empty_pb2.Empty:
        bytes_call = IMPORT_rbt_v1alpha1_nodejs_pb2.TrampolineCall(
            kind=IMPORT_rbt_v1alpha1_nodejs_pb2.reader,
            context=IMPORT_rbt_v1alpha1_nodejs_pb2.Context(
                method='FailWithException',
                state_id=context.state_id,
                state_type_name=context.state_type_name,
                caller_bearer_token=context.caller_bearer_token,
                cookie=context.cookie,
                app_internal=context.app_internal,
                auth=(
                    None if context.auth is None
                    else context.auth.to_proto_bytes()
                ),
                workflow_id=(
                    None if context.workflow_id is None
                    else str(context.workflow_id)
                ),
            ),
            state=state.SerializeToString(),
            request=request.SerializeToString(),
        ).SerializeToString()

        cancelled: IMPORT_asyncio.Future[None] = IMPORT_asyncio.Future()

        bytes_result_future: IMPORT_typing.Optional[IMPORT_asyncio.Future[str]] = None

        try:
            bytes_result_future = self._trampoline(  # type: ignore[attr-defined]
                self._js_servicer_reference,
                context,
                cancelled,
                bytes_call,
            )
            # NOTE: we need to `asyncio.shield` so that we can still
            # correctly wait for this future to complete even if we
            # are cancelled.
            assert bytes_result_future is not None
            bytes_result = await IMPORT_asyncio.shield(bytes_result_future)
        except IMPORT_asyncio.CancelledError:
            cancelled.set_result(None)

            # NOTE: unlike for a `writer` or `transaction`, we DO NOT
            # _need_ to wait for `bytes_result_future` because this is a
            # reader and we can execute multiple readers
            # simultaneously. That being said, we still want to give
            # good feedback that the RPC has been cancelled, and there
            # is no harm waiting because other readers can still be
            # called.
            if bytes_result_future is not None:
                await self._wait_for_cancelled(
                    bytes_result_future,
                    'Greeter.FailWithException',
                )

            raise
        except:
            # Make sure we cancel the `cancelled` future either if an
            # exception is thrown or if the result is reeturned so
            # that we don't keep around resources related to it that
            # might cause us to run out of memory or worse, keep Node
            # from exiting because it is waiting for Python.
            cancelled.cancel()
            raise
        else:
            cancelled.cancel()

            result = IMPORT_rbt_v1alpha1_nodejs_pb2.TrampolineResult.FromString(
                bytes_result
            )

            if result.HasField('status_json'):
                raise (
                    Greeter
                    .FailWithExceptionAborted
                    .from_status(
                        IMPORT_google_protobuf_json_format.Parse(
                            result.status_json,
                            IMPORT_google_rpc_status_pb2.Status(),
                        )
                    )
                )

            assert result.HasField('response')

            return google.protobuf.empty_pb2.Empty.FromString(result.response)
        raise RuntimeError("Unexpected result from FailWithException")

    async def FailWithAborted(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: tests.reboot.greeter_pb2.Greeter,
        request: google.protobuf.empty_pb2.Empty,
    ) -> google.protobuf.empty_pb2.Empty:
        bytes_call = IMPORT_rbt_v1alpha1_nodejs_pb2.TrampolineCall(
            kind=IMPORT_rbt_v1alpha1_nodejs_pb2.reader,
            context=IMPORT_rbt_v1alpha1_nodejs_pb2.Context(
                method='FailWithAborted',
                state_id=context.state_id,
                state_type_name=context.state_type_name,
                caller_bearer_token=context.caller_bearer_token,
                cookie=context.cookie,
                app_internal=context.app_internal,
                auth=(
                    None if context.auth is None
                    else context.auth.to_proto_bytes()
                ),
                workflow_id=(
                    None if context.workflow_id is None
                    else str(context.workflow_id)
                ),
            ),
            state=state.SerializeToString(),
            request=request.SerializeToString(),
        ).SerializeToString()

        cancelled: IMPORT_asyncio.Future[None] = IMPORT_asyncio.Future()

        bytes_result_future: IMPORT_typing.Optional[IMPORT_asyncio.Future[str]] = None

        try:
            bytes_result_future = self._trampoline(  # type: ignore[attr-defined]
                self._js_servicer_reference,
                context,
                cancelled,
                bytes_call,
            )
            # NOTE: we need to `asyncio.shield` so that we can still
            # correctly wait for this future to complete even if we
            # are cancelled.
            assert bytes_result_future is not None
            bytes_result = await IMPORT_asyncio.shield(bytes_result_future)
        except IMPORT_asyncio.CancelledError:
            cancelled.set_result(None)

            # NOTE: unlike for a `writer` or `transaction`, we DO NOT
            # _need_ to wait for `bytes_result_future` because this is a
            # reader and we can execute multiple readers
            # simultaneously. That being said, we still want to give
            # good feedback that the RPC has been cancelled, and there
            # is no harm waiting because other readers can still be
            # called.
            if bytes_result_future is not None:
                await self._wait_for_cancelled(
                    bytes_result_future,
                    'Greeter.FailWithAborted',
                )

            raise
        except:
            # Make sure we cancel the `cancelled` future either if an
            # exception is thrown or if the result is reeturned so
            # that we don't keep around resources related to it that
            # might cause us to run out of memory or worse, keep Node
            # from exiting because it is waiting for Python.
            cancelled.cancel()
            raise
        else:
            cancelled.cancel()

            result = IMPORT_rbt_v1alpha1_nodejs_pb2.TrampolineResult.FromString(
                bytes_result
            )

            if result.HasField('status_json'):
                raise (
                    Greeter
                    .FailWithAbortedAborted
                    .from_status(
                        IMPORT_google_protobuf_json_format.Parse(
                            result.status_json,
                            IMPORT_google_rpc_status_pb2.Status(),
                        )
                    )
                )

            assert result.HasField('response')

            return google.protobuf.empty_pb2.Empty.FromString(result.response)
        raise RuntimeError("Unexpected result from FailWithAborted")

    @classmethod
    async def Workflow(
        cls,
        context: IMPORT_reboot_aio_contexts.WorkflowContext,
        request: google.protobuf.empty_pb2.Empty,
    ) -> tests.reboot.greeter_pb2.WorkflowResponse:
        bytes_call = IMPORT_rbt_v1alpha1_nodejs_pb2.TrampolineCall(
            kind=IMPORT_rbt_v1alpha1_nodejs_pb2.workflow,
            context=IMPORT_rbt_v1alpha1_nodejs_pb2.Context(
                method='Workflow',
                state_id=context.state_id,
                state_type_name=context.state_type_name,
                caller_bearer_token=context.caller_bearer_token,
                cookie=context.cookie,
                app_internal=context.app_internal,
                auth=(
                    None if context.auth is None
                    else context.auth.to_proto_bytes()
                ),
                workflow_id=(
                    None if context.workflow_id is None
                    else str(context.workflow_id)
                ),
            ),
            request=request.SerializeToString(),
        ).SerializeToString()

        cancelled: IMPORT_asyncio.Future[None] = IMPORT_asyncio.Future()

        # Link up this `cancelled` future with the context so that it
        # waiting within Python via `context.wait()` will correctly
        # propagate `CancelledError` back through Node.js.
        context._cancelled = cancelled

        bytes_result_future: IMPORT_typing.Optional[IMPORT_asyncio.Future[str]] = None

        servicer = cls.__servicer__.get()
        assert servicer is not None

        try:
            bytes_result_future = servicer._trampoline(  # type: ignore[attr-defined]
                servicer._js_servicer_reference,
                context,
                cancelled,
                bytes_call,
            )
            # NOTE: we need to `asyncio.shield` so that we can still
            # correctly wait for this future to complete even if we
            # are cancelled.
            assert bytes_result_future is not None
            bytes_result = await IMPORT_asyncio.shield(bytes_result_future)
        except IMPORT_asyncio.CancelledError:
            cancelled.set_result(None)

            # NOTE: unlike for a `writer` or `transaction`, we DO NOT
            # _need_ to wait for `bytes_result_future` because this is
            # a workflow and we can execute multiple workflows
            # simultaneously. That being said, we still want to give
            # good feedback that the RPC has been cancelled, and there
            # is no harm waiting because other writers can still be
            # called.
            if bytes_result_future is not None:
                await cls._wait_for_cancelled(
                    bytes_result_future,
                    'Greeter.Workflow',
                )

            raise
        except:
            # Make sure we cancel the `cancelled` future either if an
            # exception is thrown or if the result is reeturned so
            # that we don't keep around resources related to it that
            # might cause us to run out of memory or worse, keep Node
            # from exiting because it is waiting for Python.
            cancelled.cancel()
            raise
        else:
            cancelled.cancel()

            result = IMPORT_rbt_v1alpha1_nodejs_pb2.TrampolineResult.FromString(
                bytes_result
            )

            if result.HasField('status_json'):
                raise (
                    Greeter
                    .WorkflowAborted
                    .from_status(
                        IMPORT_google_protobuf_json_format.Parse(
                            result.status_json,
                            IMPORT_google_rpc_status_pb2.Status(),
                        )
                    )
                )

            assert result.HasField('response')
            return tests.reboot.greeter_pb2.WorkflowResponse.FromString(result.response)
        raise RuntimeError("Unexpected result from Workflow")

    async def DangerousFields(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: tests.reboot.greeter_pb2.Greeter,
        request: tests.reboot.greeter_pb2.DangerousFieldsRequest,
    ) -> google.protobuf.empty_pb2.Empty:
        with IMPORT_reboot_aio_tracing.span(
                state_name=f"{context.state_type_name}('{context.state_id}')",
                span_name="NodeAdaptor DangerousFields",
                # The naming above matches Python, but not TypeScript.
                python_specific=True,
                level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
        ):
            with IMPORT_reboot_aio_tracing.span(
                state_name=f"{context.state_type_name}('{context.state_id}')",
                span_name="Create and serialize `TrampolineCall`",
                # The naming above matches Python, but not TypeScript.
                python_specific=True,
                level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
            ):
                bytes_call = IMPORT_rbt_v1alpha1_nodejs_pb2.TrampolineCall(
                    kind=IMPORT_rbt_v1alpha1_nodejs_pb2.writer,
                    context=IMPORT_rbt_v1alpha1_nodejs_pb2.Context(
                        method='DangerousFields',
                        state_id=context.state_id,
                        state_type_name=context.state_type_name,
                        caller_bearer_token=context.caller_bearer_token,
                        cookie=context.cookie,
                        app_internal=context.app_internal,
                        auth=(
                            None if context.auth is None
                            else context.auth.to_proto_bytes()
                        ),
                        workflow_id=(
                            None if context.workflow_id is None
                            else str(context.workflow_id)
                        ),
                    ),
                    state=state.SerializeToString(),
                    request=request.SerializeToString(),
                ).SerializeToString()

            cancelled: IMPORT_asyncio.Future[None] = IMPORT_asyncio.Future()

            bytes_result_future: IMPORT_typing.Optional[IMPORT_asyncio.Future[str]] = None

            try:
                with IMPORT_reboot_aio_tracing.span(
                    state_name=f"{context.state_type_name}('{context.state_id}')",
                    span_name="trampoline",
                    # The naming above matches Python, but not TypeScript.
                    python_specific=True,
                    level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                ):
                    bytes_result_future = self._trampoline(  # type: ignore[attr-defined]
                        self._js_servicer_reference,
                        context,
                        cancelled,
                        bytes_call,
                    )
                    # NOTE: we need to `asyncio.shield` so that we can still
                    # correctly wait for this future to complete even if we
                    # are cancelled.
                    assert bytes_result_future is not None
                    bytes_result = await IMPORT_asyncio.shield(bytes_result_future)
            except IMPORT_asyncio.CancelledError:
                cancelled.set_result(None)

                # NOTE: we MUST wait for `bytes_result_future` because this
                # is a `writer` or `transaction` and we CAN NOT execute
                # multiple simultaneously.
                if bytes_result_future is not None:
                    await self._wait_for_cancelled(
                        bytes_result_future,
                        'Greeter.DangerousFields',
                    )

                raise
            except:
                # Make sure we cancel the `cancelled` future either if an
                # exception is thrown or if the result is reeturned so
                # that we don't keep around resources related to it that
                # might cause us to run out of memory or worse, keep Node
                # from exiting because it is waiting for Python.
                cancelled.cancel()
                raise
            else:
                cancelled.cancel()

                with IMPORT_reboot_aio_tracing.span(
                    state_name=f"{context.state_type_name}('{context.state_id}')",
                    span_name="result ParseFromString",
                    # The naming above matches Python, but not TypeScript.
                    python_specific=True,
                    level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                ):
                    result = IMPORT_rbt_v1alpha1_nodejs_pb2.TrampolineResult.FromString(
                        bytes_result
                    )

                if result.HasField('state'):
                    with IMPORT_reboot_aio_tracing.span(
                        state_name=f"{context.state_type_name}('{context.state_id}')",
                        span_name="state ParseFromString",
                        # The naming above matches Python, but not TypeScript.
                        python_specific=True,
                        level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                    ):
                        state.CopyFrom(
                            tests.reboot.greeter_pb2.Greeter.FromString(
                                result.state
                            )
                        )

                if result.HasField('status_json'):
                    raise (
                        Greeter
                        .DangerousFieldsAborted
                        .from_status(
                            IMPORT_google_protobuf_json_format.Parse(
                                result.status_json,
                                IMPORT_google_rpc_status_pb2.Status(),
                            )
                        )
                    )

                assert result.HasField('response')

                with IMPORT_reboot_aio_tracing.span(
                    state_name=f"{context.state_type_name}('{context.state_id}')",
                    span_name="response ParseFromString",
                ):
                    return google.protobuf.empty_pb2.Empty.FromString(result.response)
        raise RuntimeError("Unexpected result from DangerousFields")

    async def StoreRecursiveMessage(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: tests.reboot.greeter_pb2.Greeter,
        request: tests.reboot.greeter_pb2.StoreRecursiveMessageRequest,
    ) -> tests.reboot.greeter_pb2.StoreRecursiveMessageResponse:
        with IMPORT_reboot_aio_tracing.span(
                state_name=f"{context.state_type_name}('{context.state_id}')",
                span_name="NodeAdaptor StoreRecursiveMessage",
                # The naming above matches Python, but not TypeScript.
                python_specific=True,
                level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
        ):
            with IMPORT_reboot_aio_tracing.span(
                state_name=f"{context.state_type_name}('{context.state_id}')",
                span_name="Create and serialize `TrampolineCall`",
                # The naming above matches Python, but not TypeScript.
                python_specific=True,
                level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
            ):
                bytes_call = IMPORT_rbt_v1alpha1_nodejs_pb2.TrampolineCall(
                    kind=IMPORT_rbt_v1alpha1_nodejs_pb2.writer,
                    context=IMPORT_rbt_v1alpha1_nodejs_pb2.Context(
                        method='StoreRecursiveMessage',
                        state_id=context.state_id,
                        state_type_name=context.state_type_name,
                        caller_bearer_token=context.caller_bearer_token,
                        cookie=context.cookie,
                        app_internal=context.app_internal,
                        auth=(
                            None if context.auth is None
                            else context.auth.to_proto_bytes()
                        ),
                        workflow_id=(
                            None if context.workflow_id is None
                            else str(context.workflow_id)
                        ),
                    ),
                    state=state.SerializeToString(),
                    request=request.SerializeToString(),
                ).SerializeToString()

            cancelled: IMPORT_asyncio.Future[None] = IMPORT_asyncio.Future()

            bytes_result_future: IMPORT_typing.Optional[IMPORT_asyncio.Future[str]] = None

            try:
                with IMPORT_reboot_aio_tracing.span(
                    state_name=f"{context.state_type_name}('{context.state_id}')",
                    span_name="trampoline",
                    # The naming above matches Python, but not TypeScript.
                    python_specific=True,
                    level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                ):
                    bytes_result_future = self._trampoline(  # type: ignore[attr-defined]
                        self._js_servicer_reference,
                        context,
                        cancelled,
                        bytes_call,
                    )
                    # NOTE: we need to `asyncio.shield` so that we can still
                    # correctly wait for this future to complete even if we
                    # are cancelled.
                    assert bytes_result_future is not None
                    bytes_result = await IMPORT_asyncio.shield(bytes_result_future)
            except IMPORT_asyncio.CancelledError:
                cancelled.set_result(None)

                # NOTE: we MUST wait for `bytes_result_future` because this
                # is a `writer` or `transaction` and we CAN NOT execute
                # multiple simultaneously.
                if bytes_result_future is not None:
                    await self._wait_for_cancelled(
                        bytes_result_future,
                        'Greeter.StoreRecursiveMessage',
                    )

                raise
            except:
                # Make sure we cancel the `cancelled` future either if an
                # exception is thrown or if the result is reeturned so
                # that we don't keep around resources related to it that
                # might cause us to run out of memory or worse, keep Node
                # from exiting because it is waiting for Python.
                cancelled.cancel()
                raise
            else:
                cancelled.cancel()

                with IMPORT_reboot_aio_tracing.span(
                    state_name=f"{context.state_type_name}('{context.state_id}')",
                    span_name="result ParseFromString",
                    # The naming above matches Python, but not TypeScript.
                    python_specific=True,
                    level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                ):
                    result = IMPORT_rbt_v1alpha1_nodejs_pb2.TrampolineResult.FromString(
                        bytes_result
                    )

                if result.HasField('state'):
                    with IMPORT_reboot_aio_tracing.span(
                        state_name=f"{context.state_type_name}('{context.state_id}')",
                        span_name="state ParseFromString",
                        # The naming above matches Python, but not TypeScript.
                        python_specific=True,
                        level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                    ):
                        state.CopyFrom(
                            tests.reboot.greeter_pb2.Greeter.FromString(
                                result.state
                            )
                        )

                if result.HasField('status_json'):
                    raise (
                        Greeter
                        .StoreRecursiveMessageAborted
                        .from_status(
                            IMPORT_google_protobuf_json_format.Parse(
                                result.status_json,
                                IMPORT_google_rpc_status_pb2.Status(),
                            )
                        )
                    )

                assert result.HasField('response')

                with IMPORT_reboot_aio_tracing.span(
                    state_name=f"{context.state_type_name}('{context.state_id}')",
                    span_name="response ParseFromString",
                ):
                    return tests.reboot.greeter_pb2.StoreRecursiveMessageResponse.FromString(result.response)
        raise RuntimeError("Unexpected result from StoreRecursiveMessage")

    async def ReadRecursiveMessage(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: tests.reboot.greeter_pb2.Greeter,
        request: tests.reboot.greeter_pb2.ReadRecursiveMessageRequest,
    ) -> tests.reboot.greeter_pb2.ReadRecursiveMessageResponse:
        bytes_call = IMPORT_rbt_v1alpha1_nodejs_pb2.TrampolineCall(
            kind=IMPORT_rbt_v1alpha1_nodejs_pb2.reader,
            context=IMPORT_rbt_v1alpha1_nodejs_pb2.Context(
                method='ReadRecursiveMessage',
                state_id=context.state_id,
                state_type_name=context.state_type_name,
                caller_bearer_token=context.caller_bearer_token,
                cookie=context.cookie,
                app_internal=context.app_internal,
                auth=(
                    None if context.auth is None
                    else context.auth.to_proto_bytes()
                ),
                workflow_id=(
                    None if context.workflow_id is None
                    else str(context.workflow_id)
                ),
            ),
            state=state.SerializeToString(),
            request=request.SerializeToString(),
        ).SerializeToString()

        cancelled: IMPORT_asyncio.Future[None] = IMPORT_asyncio.Future()

        bytes_result_future: IMPORT_typing.Optional[IMPORT_asyncio.Future[str]] = None

        try:
            bytes_result_future = self._trampoline(  # type: ignore[attr-defined]
                self._js_servicer_reference,
                context,
                cancelled,
                bytes_call,
            )
            # NOTE: we need to `asyncio.shield` so that we can still
            # correctly wait for this future to complete even if we
            # are cancelled.
            assert bytes_result_future is not None
            bytes_result = await IMPORT_asyncio.shield(bytes_result_future)
        except IMPORT_asyncio.CancelledError:
            cancelled.set_result(None)

            # NOTE: unlike for a `writer` or `transaction`, we DO NOT
            # _need_ to wait for `bytes_result_future` because this is a
            # reader and we can execute multiple readers
            # simultaneously. That being said, we still want to give
            # good feedback that the RPC has been cancelled, and there
            # is no harm waiting because other readers can still be
            # called.
            if bytes_result_future is not None:
                await self._wait_for_cancelled(
                    bytes_result_future,
                    'Greeter.ReadRecursiveMessage',
                )

            raise
        except:
            # Make sure we cancel the `cancelled` future either if an
            # exception is thrown or if the result is reeturned so
            # that we don't keep around resources related to it that
            # might cause us to run out of memory or worse, keep Node
            # from exiting because it is waiting for Python.
            cancelled.cancel()
            raise
        else:
            cancelled.cancel()

            result = IMPORT_rbt_v1alpha1_nodejs_pb2.TrampolineResult.FromString(
                bytes_result
            )

            if result.HasField('status_json'):
                raise (
                    Greeter
                    .ReadRecursiveMessageAborted
                    .from_status(
                        IMPORT_google_protobuf_json_format.Parse(
                            result.status_json,
                            IMPORT_google_rpc_status_pb2.Status(),
                        )
                    )
                )

            assert result.HasField('response')

            return tests.reboot.greeter_pb2.ReadRecursiveMessageResponse.FromString(result.response)
        raise RuntimeError("Unexpected result from ReadRecursiveMessage")

    async def ConstructAndStoreRecursiveMessage(
        self,
        context: IMPORT_reboot_aio_contexts.TransactionContext,
        state: tests.reboot.greeter_pb2.Greeter,
        request: tests.reboot.greeter_pb2.ConstructAndStoreRecursiveMessageRequest,
    ) -> tests.reboot.greeter_pb2.ConstructAndStoreRecursiveMessageResponse:
        with IMPORT_reboot_aio_tracing.span(
                state_name=f"{context.state_type_name}('{context.state_id}')",
                span_name="NodeAdaptor ConstructAndStoreRecursiveMessage",
                # The naming above matches Python, but not TypeScript.
                python_specific=True,
                level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
        ):
            with IMPORT_reboot_aio_tracing.span(
                state_name=f"{context.state_type_name}('{context.state_id}')",
                span_name="Create and serialize `TrampolineCall`",
                # The naming above matches Python, but not TypeScript.
                python_specific=True,
                level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
            ):
                bytes_call = IMPORT_rbt_v1alpha1_nodejs_pb2.TrampolineCall(
                    kind=IMPORT_rbt_v1alpha1_nodejs_pb2.transaction,
                    context=IMPORT_rbt_v1alpha1_nodejs_pb2.Context(
                        method='ConstructAndStoreRecursiveMessage',
                        state_id=context.state_id,
                        state_type_name=context.state_type_name,
                        caller_bearer_token=context.caller_bearer_token,
                        cookie=context.cookie,
                        app_internal=context.app_internal,
                        auth=(
                            None if context.auth is None
                            else context.auth.to_proto_bytes()
                        ),
                        workflow_id=(
                            None if context.workflow_id is None
                            else str(context.workflow_id)
                        ),
                    ),
                    state=state.SerializeToString(),
                    request=request.SerializeToString(),
                ).SerializeToString()

            cancelled: IMPORT_asyncio.Future[None] = IMPORT_asyncio.Future()

            bytes_result_future: IMPORT_typing.Optional[IMPORT_asyncio.Future[str]] = None

            try:
                with IMPORT_reboot_aio_tracing.span(
                    state_name=f"{context.state_type_name}('{context.state_id}')",
                    span_name="trampoline",
                    # The naming above matches Python, but not TypeScript.
                    python_specific=True,
                    level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                ):
                    bytes_result_future = self._trampoline(  # type: ignore[attr-defined]
                        self._js_servicer_reference,
                        context,
                        cancelled,
                        bytes_call,
                    )
                    # NOTE: we need to `asyncio.shield` so that we can still
                    # correctly wait for this future to complete even if we
                    # are cancelled.
                    assert bytes_result_future is not None
                    bytes_result = await IMPORT_asyncio.shield(bytes_result_future)
            except IMPORT_asyncio.CancelledError:
                cancelled.set_result(None)

                # NOTE: we MUST wait for `bytes_result_future` because this
                # is a `writer` or `transaction` and we CAN NOT execute
                # multiple simultaneously.
                if bytes_result_future is not None:
                    await self._wait_for_cancelled(
                        bytes_result_future,
                        'Greeter.ConstructAndStoreRecursiveMessage',
                    )

                raise
            except:
                # Make sure we cancel the `cancelled` future either if an
                # exception is thrown or if the result is reeturned so
                # that we don't keep around resources related to it that
                # might cause us to run out of memory or worse, keep Node
                # from exiting because it is waiting for Python.
                cancelled.cancel()
                raise
            else:
                cancelled.cancel()

                with IMPORT_reboot_aio_tracing.span(
                    state_name=f"{context.state_type_name}('{context.state_id}')",
                    span_name="result ParseFromString",
                    # The naming above matches Python, but not TypeScript.
                    python_specific=True,
                    level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                ):
                    result = IMPORT_rbt_v1alpha1_nodejs_pb2.TrampolineResult.FromString(
                        bytes_result
                    )

                if result.HasField('state'):
                    with IMPORT_reboot_aio_tracing.span(
                        state_name=f"{context.state_type_name}('{context.state_id}')",
                        span_name="state ParseFromString",
                        # The naming above matches Python, but not TypeScript.
                        python_specific=True,
                        level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                    ):
                        state.CopyFrom(
                            tests.reboot.greeter_pb2.Greeter.FromString(
                                result.state
                            )
                        )

                if result.HasField('status_json'):
                    raise (
                        Greeter
                        .ConstructAndStoreRecursiveMessageAborted
                        .from_status(
                            IMPORT_google_protobuf_json_format.Parse(
                                result.status_json,
                                IMPORT_google_rpc_status_pb2.Status(),
                            )
                        )
                    )

                assert result.HasField('response')

                with IMPORT_reboot_aio_tracing.span(
                    state_name=f"{context.state_type_name}('{context.state_id}')",
                    span_name="response ParseFromString",
                ):
                    return tests.reboot.greeter_pb2.ConstructAndStoreRecursiveMessageResponse.FromString(result.response)
        raise RuntimeError("Unexpected result from ConstructAndStoreRecursiveMessage")



############################ Reference Node adapters ############################
# Used by Node.js WeakReference implementations to access Python code and
# vice-versa. Relevant to clients.

class GreeterWeakReferenceNodeAdaptor(Greeter.WeakReference[Greeter.WeakReference._Schedule]):

    async def _call(  # type: ignore[override]
        self,
        *,
        callable: IMPORT_typing.Callable[[IMPORT_google_protobuf_message.Message], IMPORT_typing.Awaitable],
        aborted_type: type[IMPORT_reboot.aio.aborted.Aborted],
        request_type: type[IMPORT_google_protobuf_message.Message],
        json_request: str,
    ) -> str:
        request = request_type()

        try:
            IMPORT_google_protobuf_json_format.Parse(json_request, request)
            response = await callable(request)
        except IMPORT_google_protobuf_json_format.ParseError as parse_error:
            aborted_error = IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                message=f"{parse_error}; "
                       "This is usually caused by a deeply nested protobuf message, which is not supported by protobuf.\n"
                        "See the limits here: https://protobuf.dev/programming-guides/proto-limits/"
                )

            return IMPORT_json.dumps(
                {
                    'status': IMPORT_google_protobuf_json_format.MessageToDict(
                        aborted_error.to_status()
                    )
                }
            )
        except BaseException as exception:
            if isinstance(exception, aborted_type):
                return IMPORT_json.dumps(
                    {
                        'status': IMPORT_google_protobuf_json_format.MessageToDict(
                            exception.to_status()
                        )
                    }
                )
            raise
        else:
            return IMPORT_json.dumps(
                {
                    'response': IMPORT_google_protobuf_json_format.MessageToDict(
                        response
                    )
                }
            )

    async def _schedule(  # type: ignore[override]
        self,
        *,
        method: str,
        context: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
        schedule: IMPORT_reboot_time_DateTimeWithTimeZone,
        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency],
        request_type: type[IMPORT_google_protobuf_message.Message],
        json_request: str,
    ) -> str:
        request = request_type()

        IMPORT_google_protobuf_json_format.Parse(json_request, request)

        if isinstance(context, IMPORT_reboot_aio_contexts.WriterContext):
            task = await getattr(
                GreeterServicerTasks(
                    context=context,
                    state_ref=context._state_ref,
                ),
                method,
            )(request, schedule=schedule)

            return IMPORT_json.dumps(
                {
                    'taskId': IMPORT_google_protobuf_json_format.MessageToDict(
                        task.task_id
                    )
                }
            )

        # Add scheduling information to the metadata.
        metadata: IMPORT_reboot_aio_types.GrpcMetadata = (
            (IMPORT_reboot_aio_headers.TASK_SCHEDULE, schedule.isoformat()),
        )

        task_id = await getattr(super()._tasks(context), method)(
            request,
            idempotency=idempotency,
            metadata=metadata,
        )

        return IMPORT_json.dumps(
            {
                'taskId': IMPORT_google_protobuf_json_format.MessageToDict(task_id)
            }
        )

    async def _reader(  # type: ignore[override]
        self,
        method: str,
        context: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
        request_type: type[IMPORT_google_protobuf_message.Message],
        json_request: str,
        json_options: str,
    ) -> str:
        options = IMPORT_json.loads(json_options)

        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None

        if 'idempotency' in options:
            idempotency = IMPORT_reboot_aio_contexts.Context.idempotency(
                alias=options['idempotency'].get('alias'),
                key=options['idempotency'].get('key'),
                how=(
                    IMPORT_reboot_aio_idempotency.ALWAYS
                    if options['idempotency'].get('always', False)
                    else IMPORT_reboot_aio_idempotency.PER_ITERATION
                    if options['idempotency'].get('perIteration')
                    else None
                ),
            )

        method_handle = IMPORT_functools.partial(
            getattr(super()._reader(context), method),
            bearer_token=options.get("bearerToken"),
            idempotency=idempotency,
        )
        return await self._call(
            callable=method_handle,
            aborted_type=getattr(
                Greeter, method + 'Aborted'
            ),
            request_type=request_type,
            json_request=json_request,
        )

    async def _writer(  # type: ignore[override]
        self,
        method: str,
        context: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
        request_type: type[IMPORT_google_protobuf_message.Message],
        json_request: str,
        json_options: str,
    ) -> str:
        options = IMPORT_json.loads(json_options)

        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None

        if 'idempotency' in options:
            idempotency = IMPORT_reboot_aio_contexts.Context.idempotency(
                alias=options['idempotency'].get('alias'),
                key=options['idempotency'].get('key'),
                how=(
                    IMPORT_reboot_aio_idempotency.ALWAYS
                    if options['idempotency'].get('always', False)
                    else IMPORT_reboot_aio_idempotency.PER_ITERATION
                    if options['idempotency'].get('perIteration')
                    else None
                ),
            )

        if 'schedule' in options:
            when = IMPORT_google_protobuf_timestamp_pb2.Timestamp()
            when.FromJsonString(options['schedule']['when'])
            return await self._schedule(
                method=method,
                context=context,
                schedule=IMPORT_reboot_time_DateTimeWithTimeZone.from_protobuf_timestamp(when),
                idempotency=idempotency,
                request_type=request_type,
                json_request=json_request,
            )

        method_handle = IMPORT_functools.partial(
            getattr(super()._writer(context), method),
            idempotency=idempotency,
            bearer_token=options.get("bearerToken"),
        )
        return await self._call(
            callable=method_handle,
            aborted_type=getattr(
                Greeter, method + 'Aborted'
            ),
            request_type=request_type,
            json_request=json_request,
        )

    async def _transaction(  # type: ignore[override]
        self,
        method: str,
        context: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
        request_type: type[IMPORT_google_protobuf_message.Message],
        json_request: str,
        json_options: str,
    ) -> str:
        options = IMPORT_json.loads(json_options)

        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None

        if 'idempotency' in options:
            idempotency = IMPORT_reboot_aio_contexts.Context.idempotency(
                alias=options['idempotency'].get('alias'),
                key=options['idempotency'].get('key'),
                how=(
                    IMPORT_reboot_aio_idempotency.ALWAYS
                    if options['idempotency'].get('always', False)
                    else IMPORT_reboot_aio_idempotency.PER_ITERATION
                    if options['idempotency'].get('perIteration')
                    else None
                ),
            )

        if 'schedule' in options:
            when = IMPORT_google_protobuf_timestamp_pb2.Timestamp()
            when.FromJsonString(options['schedule']['when'])
            return await self._schedule(
                method=method,
                context=context,
                schedule=IMPORT_reboot_time_DateTimeWithTimeZone.from_protobuf_timestamp(when),
                idempotency=idempotency,
                request_type=request_type,
                json_request=json_request,
            )

        method_handle = IMPORT_functools.partial(
            getattr(super()._workflow(context), method),
            idempotency=idempotency,
            bearer_token=options.get("bearerToken"),
        )
        return await self._call(
            callable=method_handle,
            aborted_type=getattr(
                Greeter, method + 'Aborted'
            ),
            request_type=request_type,
            json_request=json_request,
        )

    async def _workflow(  # type: ignore[override]
        self,
        method: str,
        context: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
        request_type: type[IMPORT_google_protobuf_message.Message],
        json_request: str,
        json_options: str,
    ) -> str:
        options = IMPORT_json.loads(json_options)

        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None

        if 'idempotency' in options:
            idempotency = IMPORT_reboot_aio_contexts.Context.idempotency(
                alias=options['idempotency'].get('alias'),
                key=options['idempotency'].get('key'),
                how=(
                    IMPORT_reboot_aio_idempotency.ALWAYS
                    if options['idempotency'].get('always', False)
                    else IMPORT_reboot_aio_idempotency.PER_ITERATION
                    if options['idempotency'].get('perIteration')
                    else None
                ),
            )

        assert 'schedule' in options

        when = IMPORT_google_protobuf_timestamp_pb2.Timestamp()
        when.FromJsonString(options['schedule']['when'])

        return await self._schedule(
            method=method,
            context=context,
            schedule=IMPORT_reboot_time_DateTimeWithTimeZone.from_protobuf_timestamp(when),
            idempotency=idempotency,
            request_type=request_type,
            json_request=json_request,
        )


# yapf: enable
