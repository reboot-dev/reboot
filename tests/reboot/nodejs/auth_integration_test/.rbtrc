# Generate 'nodejs' code from our '.proto' files in 'api/'.
generate --nodejs=api/

# Find '.proto' files in 'api/'.
generate api/

dev run --name=hello-nodejs
dev run --transpile='npx tsc'
dev run --nodejs
dev run --application=dist/backend/src/main.js

dev expunge --name=hello-nodejs
