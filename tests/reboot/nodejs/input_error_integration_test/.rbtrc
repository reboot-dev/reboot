# Generate 'nodejs' code from our '.proto' files in 'api/'.
generate --nodejs=api/

# Find '.proto' files in 'api/'.
generate api/

dev run --application-name=hello-nodejs
dev run --nodejs
dev run --application=backend/src/main.ts

dev expunge --application-name=hello-nodejs
