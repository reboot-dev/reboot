syntax = "proto3";

import "rbt/v1alpha1/options.proto";
import "tests/reboot/protoc/shared.proto";

package tests.reboot.protoc;

message Echo {
  option (rbt.v1alpha1.state) = {
    implements: [ "EchoMethods", "EchoMoreMethods" ]
  };
  Text last_message = 1;
}

service EchoMethods {
  option (rbt.v1alpha1.service) = {
  };

  rpc Reply(Text) returns (Text) {
    option (rbt.v1alpha1.method).writer = {
    };
  }
}

service EchoMoreMethods {
  option (rbt.v1alpha1.service) = {
    // TESTS: there are two services that supply methods to "Echo", and both
    //        provide a `Reply` method (duplicate).
    state: "Echo"
  };

  rpc Reply(Text) returns (Text) {
    option (rbt.v1alpha1.method).writer = {
    };
  }
}
