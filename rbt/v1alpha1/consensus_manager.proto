syntax = "proto3";

package rbt.v1alpha1;

// Metadata written by a LocalConsensusManager to track the expected
// consensuses it is managing.
message ConsensusManagerMetadata {
  // The number of consensuses expected in a particular application directory.
  int32 consensus_count = 1;
}
