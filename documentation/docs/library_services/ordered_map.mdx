import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";

# OrderedMap

A larger than memory map / dictionary, sorted by its keys. It supports
insertion, removal, sorted range queries, and point-lookup queries.

Keys are strings and can be associated with a `Value`, `bytes`, or `Any` object
when inserted into an `OrderedMap`.
  * `Value` represents a dynamically typed value which can be either null, a
    number, a string, a boolean, a recursive struct value, or a list of values.
    A producer of a `Value` is expected to set one of these variants; the
    absence of any variant indicates an error. This type is meant to represent all possible JSON values.
  * `bytes` is used to store a string of `bytes`.
  * `Any` refers to the `Any` protobuf and is the best choice if you are using protobufs
    to structure your data.

`OrderedMap`s are frequently used to create indexes of other state types,
but can also be used to efficiently store large collections of primitive values.

For example:
  * To index a data type in its natural state-ID order, you can create a
    `OrderedMap` with the type's ID as keys and empty values. This is effectively
    a sorted set.
  * To index a data type by creation time, you can use time-ordered UUIDs
    (such as UUIDv1 or UUIDv7) as keys and the data type's state-ID as
    values.
  * To index a large collection of Protobuf message types, you can use a
    key of your choice and the serialized value of each Protobuf message
    as a value.

To use an `OrderedMap` in your application, you typically want to choose an ID for
the map and store it somewhere (e.g., as a field in the appropriate state data
type). To use the map, first get a `ref()` to the map using its ID and then invoke
the appropriate operations. The map will be implicitly constructed when the first
writer method is invoked (e.g., [`insert()`](#insert)) or can be explicitly
constructed with [`create()`](#create).


## Imports and set up

To use an `OrderedMap`, import the library where you would like to use it.

<Tabs groupId="language">
  <TabItem value="python" label="Python" default>
<!-- MARKDOWN-AUTO-DOCS:START
(CODE:src=../../../tests/reboot/std/collections/ordered_map/v1/ordered_map_tests.py&lines=14-18) -->
<!-- The below code snippet is automatically added from ../../../tests/reboot/std/collections/ordered_map/v1/ordered_map_tests.py -->

```py
from reboot.std.collections.ordered_map.v1.ordered_map import (
    InvalidRangeError,
    OrderedMap,
    ordered_map_library,
)
```

<!-- MARKDOWN-AUTO-DOCS:END -->
  </TabItem>
  <TabItem value="typescript" label="TypeScript">
<!-- MARKDOWN-AUTO-DOCS:START
(CODE:src=../../../tests/reboot/std/collections/ordered_map/v1/ordered_map_tests.ts&lines=10-15) -->
<!-- The below code snippet is automatically added from ../../../tests/reboot/std/collections/ordered_map/v1/ordered_map_tests.ts -->

```ts
import {
  InvalidRangeError,
  Node,
  OrderedMap,
  orderedMapLibrary,
} from "@reboot-dev/reboot-std/collections/ordered_map/v1";
```

<!-- MARKDOWN-AUTO-DOCS:END -->
  </TabItem>
</Tabs>

Also make sure to include the `OrderedMap` library when starting up your `Application`.
(Note: this import is different from above.)

<Tabs groupId="language">
  <TabItem value="python" label="Python" default>
<!-- MARKDOWN-AUTO-DOCS:START
(CODE:src=../../../tests/reboot/std/collections/ordered_map/v1/ordered_map_tests.py&lines=1894-1897) -->
<!-- The below code snippet is automatically added from ../../../tests/reboot/std/collections/ordered_map/v1/ordered_map_tests.py -->

```py
async def main():
    await Application(
        libraries=[ordered_map_library()],
    ).run()
```

<!-- MARKDOWN-AUTO-DOCS:END -->
  </TabItem>
  <TabItem value="typescript" label="TypeScript">
<!-- MARKDOWN-AUTO-DOCS:START
(CODE:src=../../../tests/reboot/std/collections/ordered_map/v1/ordered_map_tests.ts&lines=94-96) -->
<!-- The below code snippet is automatically added from ../../../tests/reboot/std/collections/ordered_map/v1/ordered_map_tests.ts -->

```ts
new Application({
  libraries: [orderedMapLibrary()],
}).run();
```

<!-- MARKDOWN-AUTO-DOCS:END -->
  </TabItem>
</Tabs>


### Authorizer

By default, `OrderedMap` allows internal calls from the application to the
library. However, this can be overridden by providing your own
[authorizer](/learn_more/auth#authorizers).

<Tabs groupId="language">
  <TabItem value="python" label="Python" default>
<!-- MARKDOWN-AUTO-DOCS:START
(CODE:src=../../../tests/reboot/std/collections/ordered_map/v1/ordered_map_tests.py&lines=1912-1919) -->
<!-- The below code snippet is automatically added from ../../../tests/reboot/std/collections/ordered_map/v1/ordered_map_tests.py -->

```py
authorizer = OrderedMap.Authorizer(
    insert=allow_if(all=[is_app_internal]),
    search=allow(),
)

application = Application(
    libraries=[ordered_map_library(authorizer=authorizer)],
)
```

<!-- MARKDOWN-AUTO-DOCS:END -->
  </TabItem>
  <TabItem value="typescript" label="TypeScript">
<!-- MARKDOWN-AUTO-DOCS:START
(CODE:src=../../../tests/reboot/std/collections/ordered_map/v1/ordered_map_tests.ts&lines=138-145) -->
<!-- The below code snippet is automatically added from ../../../tests/reboot/std/collections/ordered_map/v1/ordered_map_tests.ts -->

```ts
const authorizer = new OrderedMap.Authorizer({
  insert: allowIf({ all: [isAppInternal] }),
  search: allow(),
});

const application = new Application({
  libraries: [orderedMapLibrary({ authorizer })],
});
```

<!-- MARKDOWN-AUTO-DOCS:END -->
  </TabItem>
</Tabs>


## OrderedMap

Each `OrderedMap` you create will need a unique ID. Using a reference to that
`OrderedMap`, you will be able to perform operations on keys and associated data.

### Getting a reference

This creates a reference to an `OrderedMap` with ID `"my-map"`.

<Tabs groupId="language">
  <TabItem value="python" label="Python" default>
<!-- MARKDOWN-AUTO-DOCS:START
(CODE:src=../../../tests/reboot/std/collections/ordered_map/v1/ordered_map_tests.py&lines=1928-1928) -->
<!-- The below code snippet is automatically added from ../../../tests/reboot/std/collections/ordered_map/v1/ordered_map_tests.py -->

```py
my_map = OrderedMap.ref("my-map")
```

<!-- MARKDOWN-AUTO-DOCS:END -->
  </TabItem>
  <TabItem value="typescript" label="TypeScript">
<!-- MARKDOWN-AUTO-DOCS:START
(CODE:src=../../../tests/reboot/std/collections/ordered_map/v1/ordered_map_tests.ts&lines=153-153) -->
<!-- The below code snippet is automatically added from ../../../tests/reboot/std/collections/ordered_map/v1/ordered_map_tests.ts -->

```ts
const myMap = OrderedMap.ref("my-map");
```

<!-- MARKDOWN-AUTO-DOCS:END -->
  </TabItem>
</Tabs>

## Methods

### Create

An optional call to create an ordered map with construction options.

- `degree` specifies the maximum number of continuously stored items per node
  (an `OrderedMap` is implemented as a B+Tree, so increasing `degree` will
  _decrease_ the total depth of the tree). Note that this defaults to 128 if
  the map is implicitly created via [`insert`](#insert).
- `maintain_size` controls whether the map tracks the total number of entries.
  When `true`, `total_size` is returned in [`range`](#range) responses, but
  every insert and remove requires a write to the root — which serializes all
  mutations. When `false` (default), mutations to different parts of the tree
  can execute concurrently.

`create` is idempotent: calling it multiple times with the same options is a
no-op. Calling it with different options after the map has been created will
raise a `StateAlreadyConstructed` error.

By default, `OrderedMap`s are implicitly created on the first [`insert`](#insert)
call. `create` is only necessary to override defaults or to ensure the map exists
before reading.

<Tabs groupId="language">
  <TabItem value="python" label="Python" default>
<!-- MARKDOWN-AUTO-DOCS:START
(CODE:src=../../../tests/reboot/std/collections/ordered_map/v1/ordered_map_tests.py&lines=1930-1934) -->
<!-- The below code snippet is automatically added from ../../../tests/reboot/std/collections/ordered_map/v1/ordered_map_tests.py -->

```py
await my_map.create(
    context,
    degree=128,
    maintain_size=True,
)
```

<!-- MARKDOWN-AUTO-DOCS:END -->
  </TabItem>
  <TabItem value="typescript" label="TypeScript">
<!-- MARKDOWN-AUTO-DOCS:START
(CODE:src=../../../tests/reboot/std/collections/ordered_map/v1/ordered_map_tests.ts&lines=155-158) -->
<!-- The below code snippet is automatically added from ../../../tests/reboot/std/collections/ordered_map/v1/ordered_map_tests.ts -->

```ts
await myMap.create(context, {
  degree: 128,
  maintainSize: true,
});
```

<!-- MARKDOWN-AUTO-DOCS:END -->
  </TabItem>
</Tabs>

### Insert

Insert a key and its associated data into the `OrderedMap`.

You can optionally pass `degree` and `maintain_size` to `insert()`. On
the first `insert()` (when the map does not yet exist), these are used
as construction options — equivalent to calling [`create`](#create)
first. On subsequent `insert` calls, if set, they are validated to
match the existing configuration. Note that you do not have to pass
the options after the map has been constructed, but this pattern is
useful when you don't know whether the map has been created or not and
you want to ensure the correct options get applied upon implicit
construction.

<Tabs groupId="language">
  <TabItem value="python" label="Python" default>
<!-- MARKDOWN-AUTO-DOCS:START
(CODE:src=../../../tests/reboot/std/collections/ordered_map/v1/ordered_map_tests.py&lines=1948-1956) -->
<!-- The below code snippet is automatically added from ../../../tests/reboot/std/collections/ordered_map/v1/ordered_map_tests.py -->

```py
# Implicitly creates with degree=64 if not yet
# created, or validates that the existing map
# was created with degree=64.
await my_map.insert(
    context,
    key="key-a",
    value=from_str("a value!"),
    degree=64,
)
```

<!-- MARKDOWN-AUTO-DOCS:END -->
  </TabItem>
  <TabItem value="typescript" label="TypeScript">
<!-- MARKDOWN-AUTO-DOCS:START
(CODE:src=../../../tests/reboot/std/collections/ordered_map/v1/ordered_map_tests.ts&lines=170-177) -->
<!-- The below code snippet is automatically added from ../../../tests/reboot/std/collections/ordered_map/v1/ordered_map_tests.ts -->

```ts
// Implicitly creates with degree 64 if not yet
// created, or validates that the existing map
// was created with degree 64.
await myMap.insert(context, {
  key: "key-a",
  value: Value.fromJson("a value!"),
  degree: 64,
});
```

<!-- MARKDOWN-AUTO-DOCS:END -->
  </TabItem>
</Tabs>

Keys which are already in the map will be overwritten.

Only one of `value`, `bytes`, or `any` can be set.

The examples below show the different ways data can be associated with a key, but
in practice, you should use a uniform structure for your value.
For more information on how to format data, check out the [`Value`](/library_services/item#value)
and [`Any`](/library_services/item#any) docs for `Item`s.

<Tabs groupId="language">
  <TabItem value="python" label="Python" default>
<!-- MARKDOWN-AUTO-DOCS:START
(CODE:src=../../../tests/reboot/std/collections/ordered_map/v1/ordered_map_tests.py&lines=1961-1977) -->
<!-- The below code snippet is automatically added from ../../../tests/reboot/std/collections/ordered_map/v1/ordered_map_tests.py -->

```py
await my_map.insert(
    context,
    key="key-a",
    value=from_str("a value!"),
)

await my_map.insert(
    context,
    key="key-b",
    bytes=b"some bytes",
)

await my_map.insert(
    context,
    key="key-c",
    any=any_value,
)
```

<!-- MARKDOWN-AUTO-DOCS:END -->
  </TabItem>
  <TabItem value="typescript" label="TypeScript">
<!-- MARKDOWN-AUTO-DOCS:START
(CODE:src=../../../tests/reboot/std/collections/ordered_map/v1/ordered_map_tests.ts&lines=107-120) -->
<!-- The below code snippet is automatically added from ../../../tests/reboot/std/collections/ordered_map/v1/ordered_map_tests.ts -->

```ts
await myMap.insert(context, {
  key: "key-a",
  value: Value.fromJson("a value!"),
});

await myMap.insert(context, {
  key: "key-b",
  bytes: new TextEncoder().encode("some bytes"),
});

await myMap.insert(context, {
  key: "key-c",
  any: Any.pack(Value.fromJson("any value!")),
});
```

<!-- MARKDOWN-AUTO-DOCS:END -->
  </TabItem>
</Tabs>


#### Bulk Insert

Insert many key-value pairs in a single call using the `entries` parameter.
This is significantly more efficient than inserting keys one at a time because
the entries are merged and dispatched through the tree concurrently.

Keys which are already in the map will be overwritten. When using `entries`,
the single-key fields (`key`, `value`, `bytes`, `any`) must all be unset.

Each entry's value is specified as an [`Item`](/library_services/item).

<Tabs groupId="language">
  <TabItem value="python" label="Python" default>
<!-- MARKDOWN-AUTO-DOCS:START
(CODE:src=../../../tests/reboot/std/collections/ordered_map/v1/ordered_map_tests.py&lines=1979-1985) -->
<!-- The below code snippet is automatically added from ../../../tests/reboot/std/collections/ordered_map/v1/ordered_map_tests.py -->

```py
await my_map.insert(
    context,
    entries={
        "key-a": Item(value=from_str("a value!")),
        "key-b": Item(bytes=b"some bytes"),
    },
)
```

<!-- MARKDOWN-AUTO-DOCS:END -->
  </TabItem>
  <TabItem value="typescript" label="TypeScript">
<!-- MARKDOWN-AUTO-DOCS:START
(CODE:src=../../../tests/reboot/std/collections/ordered_map/v1/ordered_map_tests.ts&lines=189-194) -->
<!-- The below code snippet is automatically added from ../../../tests/reboot/std/collections/ordered_map/v1/ordered_map_tests.ts -->

```ts
await myMap.insert(context, {
  entries: {
    "key-a": { value: Value.fromJson("a value!") },
    "key-b": { bytes: new TextEncoder().encode("some bytes") },
  },
});
```

<!-- MARKDOWN-AUTO-DOCS:END -->
  </TabItem>
</Tabs>


### Search

Search for whether a given key is in the `OrderedMap`.

Returns whether or not the key exists. If the key exists, this will also
return the associated `Value`, `bytes`, or `Any`.

<Tabs groupId="language">
  <TabItem value="python" label="Python" default>
<!-- MARKDOWN-AUTO-DOCS:START
(CODE:src=../../../tests/reboot/std/collections/ordered_map/v1/ordered_map_tests.py&lines=1987-2001) -->
<!-- The below code snippet is automatically added from ../../../tests/reboot/std/collections/ordered_map/v1/ordered_map_tests.py -->

```py
# Search for key with associated `Value`.
response = await my_map.search(context, key="key-a")
print(response.value)

# Search for key with associated `bytes`.
response = await my_map.search(context, key="key-b")
print(response.bytes)

# Search for key with associated `Any`.
response = await my_map.search(context, key="key-c")
print(response.any)

# Search for key with no associated data.
missing_response = await my_map.search(context, key="missing-key")
assert not missing_response.found
```

<!-- MARKDOWN-AUTO-DOCS:END -->
  </TabItem>
  <TabItem value="typescript" label="TypeScript">
<!-- MARKDOWN-AUTO-DOCS:START
(CODE:src=../../../tests/reboot/std/collections/ordered_map/v1/ordered_map_tests.ts&lines=196-212) -->
<!-- The below code snippet is automatically added from ../../../tests/reboot/std/collections/ordered_map/v1/ordered_map_tests.ts -->

```ts
// Search for key with associated `Value`.
const { value } = await myMap.search(context, { key: "key-a" });
console.log(value?.toJson());

// Search for key with associated `bytes`.
const { bytes } = await myMap.search(context, { key: "key-b" });
console.log(new TextDecoder().decode(bytes));

// Search for key with associated `Any`.
const { any } = await myMap.search(context, { key: "key-c" });
console.log(any);

// Search for key with no associated data.
const missingResponse = await myMap.search(context, {
  key: "missing-key",
});
assert(missingResponse.found === false);
```

<!-- MARKDOWN-AUTO-DOCS:END -->
  </TabItem>
</Tabs>


### Remove

Remove a key and its associated data from the `OrderedMap`.

Any key that is not present will be ignored.

<Tabs groupId="language">
  <TabItem value="python" label="Python" default>
<!-- MARKDOWN-AUTO-DOCS:START
(CODE:src=../../../tests/reboot/std/collections/ordered_map/v1/ordered_map_tests.py&lines=2003-2003) -->
<!-- The below code snippet is automatically added from ../../../tests/reboot/std/collections/ordered_map/v1/ordered_map_tests.py -->

```py
await my_map.remove(context, key="key-a")
```

<!-- MARKDOWN-AUTO-DOCS:END -->
  </TabItem>
  <TabItem value="typescript" label="TypeScript">
<!-- MARKDOWN-AUTO-DOCS:START
(CODE:src=../../../tests/reboot/std/collections/ordered_map/v1/ordered_map_tests.ts&lines=214-214) -->
<!-- The below code snippet is automatically added from ../../../tests/reboot/std/collections/ordered_map/v1/ordered_map_tests.ts -->

```ts
await myMap.remove(context, { key: "key-a" });
```

<!-- MARKDOWN-AUTO-DOCS:END -->
  </TabItem>
</Tabs>



#### Bulk Remove

Remove many keys in a single call using the `keys` parameter. This is
significantly more efficient than removing keys one at a time because the
removals are dispatched through the tree concurrently.

Keys that are not present are silently ignored. When using `keys`, the
single-key `key` field must be unset.

<Tabs groupId="language">
  <TabItem value="python" label="Python" default>
<!-- MARKDOWN-AUTO-DOCS:START
(CODE:src=../../../tests/reboot/std/collections/ordered_map/v1/ordered_map_tests.py&lines=2011-2014) -->
<!-- The below code snippet is automatically added from ../../../tests/reboot/std/collections/ordered_map/v1/ordered_map_tests.py -->

```py
await my_map.remove(
    context,
    keys=["key-a", "key-b", "key-c"],
)
```

<!-- MARKDOWN-AUTO-DOCS:END -->
  </TabItem>
  <TabItem value="typescript" label="TypeScript">
<!-- MARKDOWN-AUTO-DOCS:START
(CODE:src=../../../tests/reboot/std/collections/ordered_map/v1/ordered_map_tests.ts&lines=221-223) -->
<!-- The below code snippet is automatically added from ../../../tests/reboot/std/collections/ordered_map/v1/ordered_map_tests.ts -->

```ts
await myMap.remove(context, {
  keys: ["key-a", "key-b", "key-c"],
});
```

<!-- MARKDOWN-AUTO-DOCS:END -->
  </TabItem>
</Tabs>


### Range

Read a range of data in ascending key order from the `OrderedMap`.

You can optionally specify a `start_key` (Python) / `startKey` (TypeScript) to limit the lower bound (inclusive) of
the returned entries. If `start_key` (Python) / `startKey` (TypeScript) is not set, the range will start from the
smallest key.

You will receive up to `limit` items in the response. The `limit` argument
is always required to avoid exhausting memory in the client or server.

This returns `entries` that have a `key` and one of `value`, `bytes`, or `any`
set dependent on how you inserted the data.

<Tabs groupId="language">
  <TabItem value="python" label="Python" default>
<!-- MARKDOWN-AUTO-DOCS:START
(CODE:src=../../../tests/reboot/std/collections/ordered_map/v1/ordered_map_tests.py&lines=2026-2036) -->
<!-- The below code snippet is automatically added from ../../../tests/reboot/std/collections/ordered_map/v1/ordered_map_tests.py -->

```py
range1 = await my_map.range(
    context,
    start_key="key-b",
    limit=2,
)

for entry in range1.entries:
    print(entry.key, entry.value, entry.bytes, entry.any)

# Returns entries associated with the 3 smallest keys.
range2 = await my_map.range(context, limit=3)
```

<!-- MARKDOWN-AUTO-DOCS:END -->
  </TabItem>
  <TabItem value="typescript" label="TypeScript">
<!-- MARKDOWN-AUTO-DOCS:START
(CODE:src=../../../tests/reboot/std/collections/ordered_map/v1/ordered_map_tests.ts&lines=234-246) -->
<!-- The below code snippet is automatically added from ../../../tests/reboot/std/collections/ordered_map/v1/ordered_map_tests.ts -->

```ts
const range1 = await myMap.range(context, {
  startKey: "key-b",
  limit: 2,
});

for (const entry of range1.entries) {
  console.log(entry.key, entry.value, entry.bytes, entry.any);
}

// Returns entries associated with the 3 smallest keys.
const range2 = await myMap.range(context, {
  limit: 3,
});
```

<!-- MARKDOWN-AUTO-DOCS:END -->
  </TabItem>
</Tabs>

If you have an error in your range request (i.e. you don't specify a `limit`),
you'll receive an [`InvalidRangeError`](#invalidrangeerror) explaining why
your range is not valid.


### Reverse Range

Read a range of data in descending key order from the `OrderedMap`. This is
similar to `range` but reads data in reverse order.

You can optionally specify a `start_key` (Python) / `startKey` (TypeScript) to limit the upper bound (inclusive) of
the returned entries. If `start_key` (Python) / `startKey` (TypeScript) is not set, the range will start from the
largest key.

You will receive up to `limit` items in the response. The `limit` argument
is always required to avoid exhausting memory in the client or server.

This returns `entries` that have a `key` and one of `value`, `bytes`, or `any`
set dependent on how you inserted the data.

<Tabs groupId="language">
  <TabItem value="python" label="Python" default>
<!-- MARKDOWN-AUTO-DOCS:START
(CODE:src=../../../tests/reboot/std/collections/ordered_map/v1/ordered_map_tests.py&lines=2040-2050) -->
<!-- The below code snippet is automatically added from ../../../tests/reboot/std/collections/ordered_map/v1/ordered_map_tests.py -->

```py
range1 = await my_map.reverse_range(
    context,
    start_key="key-z",
    limit=2,
)

for entry in range1.entries:
    print(entry.key, entry.value, entry.bytes, entry.any)

# Returns entries associated with the 3 largest keys.
range2 = await my_map.reverse_range(context, limit=3)
```

<!-- MARKDOWN-AUTO-DOCS:END -->
  </TabItem>
  <TabItem value="typescript" label="TypeScript">
<!-- MARKDOWN-AUTO-DOCS:START
(CODE:src=../../../tests/reboot/std/collections/ordered_map/v1/ordered_map_tests.ts&lines=250-262) -->
<!-- The below code snippet is automatically added from ../../../tests/reboot/std/collections/ordered_map/v1/ordered_map_tests.ts -->

```ts
const reverseRange1 = await myMap.reverseRange(context, {
  startKey: "key-z",
  limit: 2,
});

for (const entry of reverseRange1.entries) {
  console.log(entry.key, entry.value, entry.bytes, entry.any);
}

// Returns entries associated with the 3 largest keys.
const reverseRange2 = await myMap.reverseRange(context, {
  limit: 3,
});
```

<!-- MARKDOWN-AUTO-DOCS:END -->
  </TabItem>
</Tabs>

If you have an error in your range request (i.e. you don't specify a `limit`),
you'll receive an [`InvalidRangeError`](#invalidrangeerror) explaining why
your range is not valid.


## Errors

### InvalidRangeError {#invalidrangeerror}
See [`range`](#range) and [`reverse_range`/`reverseRange`](#reverse-range).
Raised when the requested range is not valid.

The `message` field of the error contains the string explaining why the range
is not valid.

<Tabs groupId="language">
  <TabItem value="python" label="Python" default>
<!-- MARKDOWN-AUTO-DOCS:START
(CODE:src=../../../tests/reboot/std/collections/ordered_map/v1/ordered_map_tests.py&lines=2054-2058) -->
<!-- The below code snippet is automatically added from ../../../tests/reboot/std/collections/ordered_map/v1/ordered_map_tests.py -->

```py
try:
    await my_map.range(context)
except OrderedMap.RangeAborted as e:
    # isinstance(e.error, InvalidRangeError) == True
    print(e.error.message)
```

<!-- MARKDOWN-AUTO-DOCS:END -->
  </TabItem>
  <TabItem value="typescript" label="TypeScript">
<!-- MARKDOWN-AUTO-DOCS:START
(CODE:src=../../../tests/reboot/std/collections/ordered_map/v1/ordered_map_tests.ts&lines=266-274) -->
<!-- The below code snippet is automatically added from ../../../tests/reboot/std/collections/ordered_map/v1/ordered_map_tests.ts -->

```ts
try {
  await myMap.range(context);
} catch (e) {
  if (
    e instanceof OrderedMap.RangeAborted &&
    e.error instanceof InvalidRangeError
  ) {
    console.error(e.error.message);
  }
```

<!-- MARKDOWN-AUTO-DOCS:END -->
  </TabItem>
</Tabs>

## React

Once you've [set up](/learn_more/call/from_react) your React app to call into
your Reboot API, you can also use `OrderedMap` directly from your React app.

First, on your backend, you will need to [override the authorizer](#authorizer) to allow
calls from your front end directly to `OrderedMap` methods.

Second, add `@reboot-dev/reboot-std-api` to your `package.json`.

```
npm install -S @reboot-dev/reboot-std-api
```

Third, import the React library for `OrderedMap` in your React app.

```ts
import { useOrderedMap } from "@reboot-dev/reboot-std-api/collections/ordered_map/v1/ordered_map_rbt_react.js";
```

Then, you can access your `OrderedMap`. For example, you can call a writer method, such as [`insert()`](#insert):

<!-- MARKDOWN-AUTO-DOCS:START
(CODE:src=../../../tests/reboot/react/test_ordered_map/ordered_map.test.tsx&lines=34-44) -->
<!-- The below code snippet is automatically added from ../../../tests/reboot/react/test_ordered_map/ordered_map.test.tsx -->

```tsx
const map = useOrderedMap({ id: "my-map" });

const handleClick = () => {
  map.insert({ key: "my-key", value: Value.fromJson("new value") });
};

return (
  <button id="button" onClick={handleClick}>
    Insert New Value
  </button>
);
```

<!-- MARKDOWN-AUTO-DOCS:END -->

Or a reader, such as [`search()`](#search):

<!-- MARKDOWN-AUTO-DOCS:START
(CODE:src=../../../tests/reboot/react/test_ordered_map/ordered_map.test.tsx&lines=49-61) -->
<!-- The below code snippet is automatically added from ../../../tests/reboot/react/test_ordered_map/ordered_map.test.tsx -->

```tsx
const map = useOrderedMap({ id: "my-map" });

const { aborted, response } = map.useSearch({ key: "my-key" });

if (aborted !== undefined) {
  return <div id="error">Errored while searching.</div>;
} else if (response === undefined) {
  return <div id="loading">Loading...</div>;
} else if (!response.found) {
  return <div id="not-found">Not found</div>;
} else {
  return <div id="map-value">{response.value?.toJson() as string}</div>;
}
```

<!-- MARKDOWN-AUTO-DOCS:END -->

See [Call your API from
React](/learn_more/call/from_react) to learn more about how to call methods from
React.
