import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";

# Readers

A `reader` method has _read-only_ access to state, so any
modifications you make to state are ignored.

A `reader` method is passed a context of type `ReaderContext`. A
`ReaderContext` can only be used to make calls to other `reader`
methods.

Here's an example of a `reader` method called `balance` on our
`Account` state that returns the account's current balance:

<Tabs groupId="language">
  <TabItem value="python" label="Python" default>
<!-- MARKDOWN-AUTO-DOCS:START
(CODE:src=../../../../tests/reboot/documentation/bank_servicer.py&lines=40-45) -->
<!-- The below code snippet is automatically added from ../../../../tests/reboot/documentation/bank_servicer.py -->

```py
async def balance(
    self,
    context: ReaderContext,
    request: BalanceRequest,
) -> BalanceResponse:
    return BalanceResponse(balance=self.state.balance)
```

<!-- MARKDOWN-AUTO-DOCS:END -->
  </TabItem>
  <TabItem value="typescript" label="TypeScript">
    <Tabs groupId="typescript-object-class">
      <TabItem value="object" label="Object literal">
<!-- MARKDOWN-AUTO-DOCS:START
(CODE:src=../../../../tests/reboot/documentation/bank.ts&lines=27-33) -->
<!-- The below code snippet is automatically added from ../../../../tests/reboot/documentation/bank.ts -->

```ts
balance: async (
  context: ReaderContext,
  state: Account.State,
  request: Account.BalanceRequest
): Promise<Account.PartialBalanceResponse> => {
  return { balance: state.balance };
},
```

<!-- MARKDOWN-AUTO-DOCS:END -->
      </TabItem>
      <TabItem value="class" label="Class">
<!-- MARKDOWN-AUTO-DOCS:START
(CODE:src=../../../../tests/reboot/documentation/bank.ts&lines=111-116) -->
<!-- The below code snippet is automatically added from ../../../../tests/reboot/documentation/bank.ts -->

```ts
async balance(
  context: ReaderContext,
  request: Account.BalanceRequest
): Promise<Account.PartialBalanceResponse> {
  return { balance: this.state.balance };
}
```

<!-- MARKDOWN-AUTO-DOCS:END -->
      </TabItem>
    </Tabs>
  </TabItem>
</Tabs>
